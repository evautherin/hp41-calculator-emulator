---
phase: quick-260603-o2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src-tauri/src/types.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
  - hp41-gui/src/App.tsx
  - hp41-gui/src/App.css
  - hp41-gui/src/themes.css
  - hp41-gui/src/App.test.tsx
autonomous: false
requirements: [PRGM-AUTHENTIC-01]

must_haves:
  truths:
    - "In PRGM mode the GUI main display shows the current program step (e.g. '000 END', '001 XEQ CLRG'), not the X register"
    - "SST/BST navigation updates the step shown in the main display (sst_step/bst_step already update pc; display_str now reflects it)"
    - "During program ENTRY (entry_buf non-empty), the entry buffer still wins over the step display — mirrors CLI priority exactly"
    - "No PROGRAM bottom sheet appears on iOS in PRGM mode (multi-line listing removed)"
    - "No .prgm-panel appears on desktop in PRGM mode (multi-line listing removed)"
    - "The print BottomSheet (print-sheet) and its CSS/refs remain fully functional and untouched"
    - "GUI and CLI agree on display priority (D-25.6 parity restored)"
  artifacts:
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "display_str priority chain with a prgm_mode branch mirroring the CLI"
      contains: "state.prgm_mode"
    - path: "hp41-gui/src-tauri/src/prgm_display.rs"
      provides: "format_step active (no longer dead_code) — consumed by from_state"
    - path: "hp41-gui/src/App.tsx"
      provides: "PRGM listing UI removed (no prgm-sheet, no .prgm-panel, no activeStepRef/scrollIntoView-on-pc)"
    - path: "hp41-gui/src/App.test.tsx"
      provides: "regression test: prgm=true + display_str step => main display shows the step"
  key_links:
    - from: "hp41-gui/src-tauri/src/types.rs (from_state)"
      to: "hp41-gui/src-tauri/src/prgm_display.rs (format_step)"
      via: "prgm_display::format_step(state) in the display_str chain"
      pattern: "prgm_display::format_step"
    - from: "hp41-gui/src/App.tsx (.display)"
      to: "calcState.display_str"
      via: "displayText = display_override ?? display_str rendered via Display14Seg"
      pattern: "display_str"
---

<objective>
Make the GUI PRGM view authentic to the real HP-41CV: the single main display shows the
CURRENT program step (navigated via SST/BST), exactly as the CLI already does. Remove the
inauthentic multi-line program listing on BOTH iOS (the PROGRAM BottomSheet) and desktop
(the .prgm-panel).

Purpose: Restore hardware fidelity (the real HP-41 has one display row, not a scrolling
listing) AND restore CLI<->GUI parity (D-25.6) — the CLI already shows format_step in PRGM
mode; the GUI silently diverged by showing the X register and bolting on a listing UI.

Output:
- Backend: a prgm_mode branch added to CalcStateView::from_state display_str chain, placed to
  mirror the CLI priority order exactly (after entry_buf, before alpha). format_step loses its
  #[allow(dead_code)].
- Frontend: the PRGM listing UI (iOS sheet + desktop panel + their refs/effects/CSS) deleted.
- A new frontend regression test pinning the authentic behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Backend: the divergence and the fix site
@hp41-gui/src-tauri/src/types.rs
@hp41-gui/src-tauri/src/prgm_display.rs

# The AUTHENTIC reference the GUI must mirror (CLI already does this)
@hp41-cli/src/ui.rs

# Frontend: listing UI to delete + display render site
@hp41-gui/src/App.tsx
@hp41-gui/src/App.css
@hp41-gui/src/App.test.tsx

<interfaces>
<!-- Verified contracts the executor needs. No exploration required. -->

CLI authoritative priority order (hp41-cli/src/ui.rs get_display_string, verified L133-160):
  clock -> stopwatch -> entry_buf -> prgm_mode (format_step) -> alpha -> format_hpnum(X)

GUI current chain (hp41-gui/src-tauri/src/types.rs from_state, verified L136-156):
  clock -> stopwatch -> modal_prompt(when modal & empty entry_buf) -> entry_buf -> alpha -> format_hpnum(X)
  -- NO prgm_mode branch. This is the divergence.

The GUI fix MUST insert the prgm branch AFTER the "!state.entry_buf.is_empty()" branch (so entry
wins during program entry — matches CLI) and BEFORE the "state.alpha_mode" branch. The extra
modal_prompt branch (GUI-only, top of chain) stays where it is — it is gated by
"state.modal_program.is_some() && state.entry_buf.is_empty()", disjoint from a plain prgm-mode
step view; leave it untouched.

format_step signature (hp41-gui/src-tauri/src/prgm_display.rs, verified L16-25):
  pub fn format_step(state: &CalcState) -> String
  Returns "{pc:03} {op_name}", or "{pc:03} END" past program end. Currently #[allow(dead_code)].

Frontend display render (hp41-gui/src/App.tsx, verified L1166-1172, L1221):
  const displayText = modalPreview ?? (calcState.display_override ?? calcState.display_str);
  rendered via Display14Seg inside the .display div (data-displaytext={displayText}).
  -- The frontend already renders display_str verbatim. The backend fix is what makes display_str
  carry the step. No frontend display-render change needed.

Listing UI to DELETE (hp41-gui/src/App.tsx, verified):
  - L268  : const activeStepRef = useRef ...  -- used ONLY by the listing
  - L1136-1139 : useEffect scrolling activeStepRef on [calcState?.pc]  -- delete whole effect
  - L1311-1351 : the "isIos ? BottomSheet id=prgm-sheet : (annunciators.prgm && div.prgm-panel)"
                 block  -- delete entire ternary (incl. its "Phase 55 Plan 05 — PRGM panel" comment)
  - L554  : recompute-scale effect deps array includes "calcState?.annunciators.prgm" -- remove that
            one dep (added for the now-removed sheet's position:fixed scale bug; keep helpOpen,
            settingsOpen, printLog.length > 0)

KEEP (do NOT touch):
  - print-sheet BottomSheet (App.tsx ~L1355) + printEndRef + printLog + the L1125-1134 print scroll effect
  - the generic BottomSheet component + BottomSheet.test.tsx (generic id)
  - program_steps / pc fields in CalcStateView and makeEmptyView (IPC shape + TS interface; removal
    is a separate follow-up)

CSS to DELETE (hp41-gui/src/App.css, verified L350-392):
  .prgm-panel, .prgm-panel-header, .prgm-panel-content, .step-row, .step-active
  (the "Program Listing Panel (Phase 18)" block). Do NOT touch .bottom-sheet* (shared with print).

CSS tokens orphaned by .step-active removal (hp41-gui/src/themes.css, verified 4 theme blocks at
L56-57, L100-101, L144-145, L195-196): --step-active-bg / --step-active-text. Remove these two
custom-property lines from all four [data-theme] blocks (no consumer after .step-active is gone).

Test fixture (hp41-gui/src/App.test.tsx, verified): makeEmptyView() returns a full CalcStateView;
overrides via Partial. getDisplayText(container) reads the rendered display text. Existing PRGM
tests seed annunciators.prgm=true via mockInvoke.mockResolvedValueOnce(makeEmptyView({...})).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — add prgm_mode branch to GUI display_str (restore CLI parity)</name>
  <files>hp41-gui/src-tauri/src/types.rs, hp41-gui/src-tauri/src/prgm_display.rs</files>
  <behavior>
    - prgm_mode true AND entry_buf empty AND not clock/stopwatch/modal-prompt -> display_str == format_step(state) (e.g. "000 END" for empty program at pc 0).
    - prgm_mode true but entry_buf non-empty (program entry in progress) -> display_str == entry_buf (entry wins, mirrors CLI).
    - prgm_mode false -> display_str unchanged (X register / alpha / etc.).
    - format_step is reachable (no dead_code warning).
  </behavior>
  <action>
    In hp41-gui/src-tauri/src/types.rs, CalcStateView::from_state, add a new branch to the
    display_str if/else chain placed AFTER the "} else if !state.entry_buf.is_empty() {" arm and
    BEFORE the "} else if state.alpha_mode {" arm: "} else if state.prgm_mode { prgm_display::format_step(state) }".
    This mirrors the CLI priority order in hp41-cli/src/ui.rs (clock -> stopwatch -> entry_buf ->
    prgm_mode -> alpha -> X) — see the interfaces block. Do NOT move or alter the existing
    clock/stopwatch/modal_prompt branches at the top of the chain. Update the priority-order comment
    above the chain (currently lists -1/0/1/2/3) to include the prgm step between entry_buf and alpha,
    noting parity with CLI ui.rs.
    In hp41-gui/src-tauri/src/prgm_display.rs, remove the "#[allow(dead_code)]" attribute on
    format_step (it now has a real caller). Leave format_all_steps and its tests in place (still
    serialized into program_steps; removal is a deliberate follow-up).
    This is a priority-chain addition only — NOT a new Op variant, so the 4-way exhaustive-match
    invariant is NOT triggered. No hp41-core change (types.rs/prgm_display.rs live in
    hp41-gui/src-tauri; format_step is an SC-4-exempt display helper).
  </action>
  <verify>
    <automated>cd hp41-gui/src-tauri && cargo check 2>&1 | grep -iE 'warning: .*dead_code|never used|^error'; echo "grep-exit:$?"; cargo test --quiet prgm_display 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check passes with no dead_code/never-used warning on format_step; from_state has a prgm_mode arm between entry_buf and alpha_mode; existing prgm_display tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — delete PRGM listing UI (iOS sheet + desktop panel + refs/effects/CSS)</name>
  <files>hp41-gui/src/App.tsx, hp41-gui/src/App.css, hp41-gui/src/themes.css</files>
  <action>
    In hp41-gui/src/App.tsx:
    1. Delete the entire PRGM ternary block (the "isIos ? BottomSheet id=prgm-sheet ... : (calcState.annunciators.prgm && div.prgm-panel ...)" region, ~L1311-1351, including its preceding "Phase 55 Plan 05 — PRGM panel" comment). Leave the print-sheet block immediately after it fully intact.
    2. Delete "const activeStepRef = useRef ..." (~L268) — no remaining consumer.
    3. Delete the whole useEffect that scrolls activeStepRef on [calcState?.pc] (~L1136-1139, including its "Auto-scroll active program step" comment).
    4. In the recompute-scale useEffect deps array (~L554), remove "calcState?.annunciators.prgm". Keep helpOpen, settingsOpen, and "printLog.length > 0". Update the adjacent comment so it no longer claims to cover the PRGM sheet (the mxg print-sheet scale fix stays — keep "printLog.length > 0").
    Do NOT touch: BottomSheet component, print-sheet, printEndRef, printLog, the print scroll effect (~L1125-1134), or the CalcStateView interface (program_steps/pc stay).

    In hp41-gui/src/App.css: delete the "Program Listing Panel (Phase 18)" block — .prgm-panel, .prgm-panel-header, .prgm-panel-content, .step-row, .step-active (~L350-392). Do NOT touch any .bottom-sheet* rules (shared with the print sheet).

    In hp41-gui/src/themes.css: remove the now-orphaned "--step-active-bg" and "--step-active-text" custom-property lines from all four [data-theme] blocks (verified at L56-57, L100-101, L144-145, L195-196). These had no consumer other than .step-active.
  </action>
  <verify>
    <automated>cd hp41-gui && N=$(grep -REc 'prgm-panel|step-row|step-active|prgm-sheet|activeStepRef|step-active-bg|step-active-text' src/App.tsx src/App.css src/themes.css | grep -v ':0$' | wc -l); [ "$N" -eq 0 ] && echo "GATE-PASS: no listing-UI references remain" && npx tsc --noEmit</automated>
  </verify>
  <done>No prgm-panel / prgm-sheet / step-row / step-active / activeStepRef / step-active-* references remain in App.tsx, App.css, themes.css; tsc --noEmit passes; print-sheet, BottomSheet, printLog, printEndRef, and CalcStateView.program_steps/pc untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend regression test — PRGM mode shows the step in the main display</name>
  <files>hp41-gui/src/App.test.tsx</files>
  <behavior>
    - get_state resolves a view with annunciators.prgm=true and display_str="000 END" -> the main display (getDisplayText) shows "000 END", NOT the X register ("0.0000"). Pins the authentic behavior: frontend renders display_str verbatim and the backend now puts the step there.
    - A second test with display_str="001 XEQ CLRG" (prgm=true) confirms a non-empty step renders (guards against an accidental hardcoded "END").
  </behavior>
  <action>
    Add a new "describe('quick-task 260603-o2e — authentic PRGM step in main display', ...)" block.
    Use the existing pattern: mockInvoke.mockResolvedValueOnce(makeEmptyView({ display_str: '000 END',
    annunciators: { user: false, prgm: true, alpha: false, rad: false, grad: false } })) then
    "const { container } = await renderAppAndWait();" and assert
    expect(getDisplayText(container)).toBe('000 END') and
    expect(getDisplayText(container)).not.toBe('0.0000').
    Add a second test seeding display_str: '001 XEQ CLRG' (prgm=true) asserting the display shows
    '001 XEQ CLRG'. Do NOT modify makeEmptyView's program_steps/pc defaults. Do NOT add a backend
    assertion here — the from_state prgm branch is covered by Task 1's Rust gate; this test pins the
    frontend contract (display_str rendered verbatim, no listing element queried).
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- --run App.test 2>&1 | tail -15</automated>
  </verify>
  <done>New regression tests pass; full App.test.tsx suite green; tests assert getDisplayText shows the step string (not the X register) when annunciators.prgm=true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PRGM view is now authentic on every frontend: the single main display shows the current
    program step (navigated by SST/BST), and the multi-line PROGRAM listing (iOS bottom sheet +
    desktop .prgm-panel) is gone. Backend mirrors the CLI display priority (D-25.6 parity).
  </what-built>
  <how-to-verify>
    On-device iPhone (run the iOS build) AND desktop GUI (just gui-dev):

    iPhone:
    1. Enter PRGM mode (PRGM annunciator on).
    2. CONFIRM the main display shows a step like "000 END" (or "001 XEQ CLRG" after entering a step) — NOT a number like "0.0000".
    3. Press SST / BST repeatedly. CONFIRM the step shown in the MAIN DISPLAY changes (000 -> 001 -> ... and back) — navigation works with no listing visible.
    4. CONFIRM NO "PROGRAM" bottom sheet appears in PRGM mode (no sheet overlapping the keys).
    5. Trigger a print op (e.g. PRX). CONFIRM the PRINT LOG bottom sheet still appears and scrolls correctly (print sheet untouched).
    6. CONFIRM there is no layout/scale glitch when entering/leaving PRGM mode (display stays correctly scaled).

    Desktop:
    7. Enter PRGM mode. CONFIRM single-step main display (same as iPhone) and NO inline .prgm-panel below the calculator.
    8. CONFIRM SST/BST navigate steps; print panel and the rest of the desktop UI are otherwise unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any divergence (wrong display in PRGM, listing still showing, broken SST/BST, broken print sheet, or scale glitch).</resume-signal>
</task>

</tasks>

<verification>
- Backend: cd hp41-gui/src-tauri && cargo check — clean, no dead_code warning on format_step; cargo test prgm_display green.
- Frontend: cd hp41-gui && npx tsc --noEmit && npm test -- --run — all green; no listing-UI references remain (grep gate).
- Parity: GUI from_state display chain order now matches hp41-cli/src/ui.rs get_display_string (clock -> stopwatch -> [modal_prompt] -> entry_buf -> prgm_mode -> alpha -> X). The modal_prompt branch is the one documented GUI-only addition and is gated disjointly.
- No hp41-core edit (verify git diff --name-only lists only hp41-gui/* files).
- 4-way exhaustive-match invariant NOT triggered (no new Op variant added).
- On-device + desktop human-verify checkpoint passed.
</verification>

<success_criteria>
- PRGM mode shows the current step in the GUI main display on iOS and desktop, navigable via SST/BST.
- The multi-line PROGRAM listing is removed from both iOS (bottom sheet) and desktop (.prgm-panel).
- The print BottomSheet, BottomSheet component, printLog/printEndRef, and CalcStateView shape are untouched.
- CLI<->GUI display-priority parity (D-25.6) is restored.
- All automated gates pass; on-device + desktop verification approved.
</success_criteria>

<followups>
- program_steps: Vec<String> and pc remain in CalcStateView (still computed by format_all_steps in
  from_state) but are now UNUSED by the frontend. Removing them from the IPC contract (and
  format_all_steps + its tests + makeEmptyView fields) is a separate optimization — defer; the CLI
  and other consumers/tests may reference the field shape and removal risks unrelated churn.
</followups>

<output>
Create .planning/quick/260603-o2e-authentic-hp-41-prgm-view-single-program/260603-o2e-SUMMARY.md when done.
</output>
