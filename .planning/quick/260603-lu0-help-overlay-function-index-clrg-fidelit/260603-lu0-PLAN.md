---
phase: quick-260603-lu0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41cv-functions.json
  - hp41-core/src/ops/program.rs
  - hp41-cli/src/prgm_display.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
  - docs/hp41cv-function-matrix.md
  - hp41-gui/src/HelpOverlay.tsx
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/help_data.test.ts
autonomous: false
requirements: [HELP-IDX, CLRG-FIDELITY, HELP-GUARDRAIL]
must_haves:
  truths:
    - "XEQ \"CLRG\" (the authentic HP-41 mnemonic) resolves and clears all data registers"
    - "XEQ \"CLREG\" (legacy alias) still resolves to the same op (back-compat)"
    - "The ? overlay program-display label and JSON display_name for the clear-registers op reads CLRG"
    - "The ? overlay has two tabs: Keyboard Shortcuts and All Functions"
    - "Every status=implemented function across all 6 JSON pools appears in the All Functions tab (including key_path:null ops like SIN, LN, CLREG/CLRG, AVIEW)"
    - "On iOS the overlay defaults to the All Functions tab; on Desktop it defaults to Keyboard Shortcuts"
    - "Search filters within the active tab across display_name + description + category"
    - "A guardrail test fails if any implemented function becomes invisible in the All Functions dataset"
  artifacts:
    - path: "docs/hp41cv-functions.json"
      provides: "CLRG display_name (renamed from CLREG)"
      contains: "\"display_name\": \"CLRG\""
    - path: "hp41-core/src/ops/program.rs"
      provides: "CLRG + CLREG resolver arms in builtin_card_op"
      contains: "\"CLRG\""
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "Tabbed overlay (Keyboard Shortcuts | All Functions)"
      min_lines: 300
    - path: "hp41-gui/src/help_data.ts"
      provides: "allFunctionsEntries() builder including key_path:null implemented entries"
    - path: "hp41-gui/src/help_data.test.ts"
      provides: "Overlay completeness guardrail (Vitest)"
  key_links:
    - from: "hp41-core/src/ops/program.rs builtin_card_op"
      to: "Op::Clreg"
      via: "\"CLRG\" | \"CLREG\" match arm"
      pattern: "\"CLRG\"\\s*\\|\\s*\"CLREG\"\\s*=>\\s*Some\\(Op::Clreg\\)"
    - from: "hp41-gui/src/HelpOverlay.tsx All Functions tab"
      to: "hp41-gui/src/help_data.ts allFunctionsEntries()"
      via: "import + filter status===implemented (no key_path exclusion)"
      pattern: "allFunctionsEntries"
    - from: "hp41-gui/src/help_data.test.ts"
      to: "helpEntriesAll() implemented subset"
      via: "assert every implemented entry present in All Functions dataset"
      pattern: "status === 'implemented'"
---

<objective>
Make the GUI `?` help overlay a complete, tabbed function reference, fix the CLRG
mnemonic fidelity bug, and add a coverage guardrail.

Three work items in dependency order:

- **Item A — CLRG fidelity fix** (small, self-contained, independently shippable):
  rename the clear-registers mnemonic CLREG→CLRG for hardware fidelity, add a
  `"CLRG"` XEQ-by-name resolver arm, and keep `"CLREG"` as a back-compat alias.
- **Item B — Tabbed full-function overlay**: add two tabs to the `?` overlay —
  "Keyboard Shortcuts" (current behavior) and "All Functions" (every
  `status:"implemented"` function, including the 74 `key_path:null` ones that are
  currently invisible).
- **Item C — Coverage guardrail**: a Vitest test asserting every implemented
  function across all 6 pools is findable in the "All Functions" dataset.

Purpose: 74 implemented HP-41 functions (SIN, LN, SQRT, AVIEW, CLREG, …) are
rendered nowhere in the overlay and unfindable by search; `XEQ "CLRG"` (the real
mnemonic) returns None. This restores fidelity and discoverability and adds a
test so the gap can never silently reopen.

Output: corrected JSON/resolver/display strings, a tabbed overlay component, an
updated help_data builder, and a guardrail test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Ground truth extracted from the codebase — executor needs these directly, no exploration. -->

CLRG bug surface (4-way exhaustive-match invariant + resolver):
- docs/hp41cv-functions.json (lines 479-487): the clear-registers entry —
    { "op_variant": "Clreg", "display_name": "CLREG", "category": "Registers",
      "status": "implemented", "phase": "2", "key_path": null,
      "description": "Clear all storage registers R00..R99 to zero" }
- hp41-core/src/ops/program.rs:1418 — `"CLREG" => Some(Op::Clreg),` (builtin_card_op,
    XEQ-by-name resolver). Sibling precedent at line 1427 shows the alias pattern
    already in use: `"CL SIGMA" | "CL\u{03A3}" | "CLSIGMA" => Some(Op::ClSigmaStat),`
- hp41-cli/src/prgm_display.rs:109 — `Op::Clreg => "CLREG".to_string(),` (CLI op_display_name)
- hp41-gui/src-tauri/src/prgm_display.rs:126 — `Op::Clreg => "CLREG".to_string(),` (GUI op_display_name)
- hp41-cli/tests/xeq_builtin_resolver.rs:76 — `assert_eq!(resolve("CLREG"), Some(Clreg));`
    (MUST keep passing — CLREG back-compat alias preserves this)
- docs/operations-reference.md:122 — already uses `CLRG` (engine currently disagrees)

NOT affected by the rename (verified — do NOT change these):
- `op_variant: "Clreg"` stays "Clreg" → ALL_OP_VARIANT_NAMES (parity test line 77) unchanged.
- key_map string id `"clreg"` is an internal IPC token (key_map.rs:73, key_map.ts) —
    lowercase, independent of display_name. Stays "clreg".
- App.tsx:198 `'g': 'clreg'` keybinding maps a key to the lowercase IPC id. Stays "clreg".

Parity test mechanics (function_matrix_parity.rs):
- FORWARD parity keys off `op_variant` ("Clreg") — unaffected by display_name rename.
- REVERSE parity for built-ins resolves `entry.display_name` via
    `builtin_card_op(entry.display_name.as_str())` (same pattern as xmem test at
    line 1130). After JSON display_name becomes "CLRG", builtin_card_op MUST accept
    "CLRG" or this test fails. → resolver arm change is load-bearing for the JSON change.
- Count sentinel (`ALL_OP_VARIANT_NAMES` length) unchanged — no op added/removed.

Overlay current behavior (HelpOverlay.tsx):
- Line 138-140: `helpEntriesAll().filter(e => e.key_path !== null)` — the hard drop.
- Line 143-151: search predicate matches display_name + description + category.
- SECTIONS array (lines 60-99): 6 module sections with predicates on `e.xrom?.module`
    ("Math 1", "Stat 1", "Time", "Adv Conv", "Adv Math") and built-in (`!e.xrom`).
- Lines 154-177: sectionGroups groups filtered entries by section.predicate → category.
- Lines 245-273: Keyboard Shortcuts section (getKeyboardShortcuts(), NOT search-filtered).
- Lines 274-332: module sections render (collapsible, category headers, expandable rows).

iOS gate (App.tsx): `const [isIos, setIsIos] = useState(false)` (line 284),
    set via `invoke<boolean>('is_ios')` (line 523). The `is_ios` Tauri command exists.
    HelpOverlay currently receives only `{ open, onClose }` — it must receive an
    `isIos` prop (or call the gate) to set the default tab.

help_data.ts pools (all 6 already loaded):
- helpEntries() (cv built-in), helpEntriesMath1, helpEntriesStat1, helpEntriesTime,
    helpEntriesAdvantage, helpEntriesXmem → concatenated by helpEntriesAll() (line 248-257).
- The 5 module pools have ZERO key_path:null entries. Only the cv pool has the 74 hidden ones.
- HelpEntry.status: 'implemented' | 'deferred-v3' | 'na'.
</interfaces>
</context>

<tasks>

<!-- ════════════════════ ITEM A — CLRG FIDELITY FIX ════════════════════ -->
<!-- Self-contained, independently shippable. Touches hp41-core resolver + the
     4-way op_display_name mirrors + JSON. Ends with an on-device checkpoint. -->

<task type="auto">
  <name>Task A1: Rename CLREG→CLRG + add CLRG resolver alias (4-way invariant + JSON)</name>
  <files>docs/hp41cv-functions.json, hp41-core/src/ops/program.rs, hp41-cli/src/prgm_display.rs, hp41-gui/src-tauri/src/prgm_display.rs, docs/hp41cv-function-matrix.md</files>
  <action>
Apply the locked decision (i): make CLRG the authentic display name, keep CLREG as a
back-compat XEQ alias. Do NOT change `op_variant: "Clreg"` (that keeps ALL_OP_VARIANT_NAMES
and forward parity stable).

1. docs/hp41cv-functions.json (the entry at ~line 480): change `"display_name": "CLREG"`
   to `"display_name": "CLRG"`. Leave op_variant, category, status, phase, key_path,
   description untouched.

2. hp41-core/src/ops/program.rs:1418 — change the resolver arm from
   `"CLREG" => Some(Op::Clreg),` to `"CLRG" | "CLREG" => Some(Op::Clreg),`.
   This mirrors the existing multi-alias pattern at line 1427 (CL SIGMA). CLRG is the
   authentic mnemonic; CLREG stays as a back-compat alias so XEQ "CLREG" still works
   (required by hp41-cli/tests/xeq_builtin_resolver.rs:76).

3. hp41-cli/src/prgm_display.rs:109 — change `Op::Clreg => "CLREG".to_string(),` to
   `Op::Clreg => "CLRG".to_string(),`.

4. hp41-gui/src-tauri/src/prgm_display.rs:126 — same change: `"CLREG"` → `"CLRG"`.
   (Items 3 + 4 are the duplicated-by-design op_display_name halves of the 4-way invariant.)

5. Regenerate the function-matrix doc: run `just docs-matrix` (JSON canonical data flow —
   the JSON display_name change must propagate to docs/hp41cv-function-matrix.md, and
   `just docs-matrix-check` is the CI drift guard). docs/operations-reference.md already
   says CLRG (line 122) — no change needed there.

Do NOT touch the lowercase `"clreg"` IPC string id in key_map.rs / key_map.ts / App.tsx —
those are internal resolver tokens, not display names.

SURFACE TO USER (do not fix in this plan): sibling mnemonic divergences noticed while here —
`CL SIGMA` already carries the authentic `CLΣ` alias (program.rs:1427), but its JSON
display_name should be checked; `CLRALPHA` is a duplicate of `CLA`. Record these in the
SUMMARY as follow-up candidates, do not act on them.
  </action>
  <verify>
    <automated>cd hp41-cli &amp;&amp; cargo test --test xeq_builtin_resolver &amp;&amp; cargo test --test function_matrix_parity</automated>
  </verify>
  <done>JSON display_name reads CLRG; builtin_card_op resolves both "CLRG" and "CLREG" to Op::Clreg; both op_display_name mirrors return "CLRG"; xeq_builtin_resolver + function_matrix_parity pass; docs-matrix-check clean.</done>
</task>

<task type="auto">
  <name>Task A2: Add CLRG resolver regression test + full gate</name>
  <files>hp41-cli/tests/xeq_builtin_resolver.rs</files>
  <action>
Add an assertion next to the existing CLREG test (line 76) proving the authentic
mnemonic now resolves: `assert_eq!(resolve("CLRG"), Some(Clreg));`. Keep the existing
`assert_eq!(resolve("CLREG"), Some(Clreg));` line so both the authentic name and the
back-compat alias are guarded. Match the surrounding test style.

Then run the full quality gate to confirm the 4-way invariant compiles everywhere and
no parity/coverage test regressed: `just docs-matrix-check` (drift), CLI test suite, and
a GUI src-tauri check (the GUI op_display_name half).
  </action>
  <verify>
    <automated>just docs-matrix-check &amp;&amp; cd hp41-cli &amp;&amp; cargo test &amp;&amp; cd ../hp41-gui/src-tauri &amp;&amp; cargo check</automated>
  </verify>
  <done>resolve("CLRG") and resolve("CLREG") both return Some(Clreg); full CLI test suite green; GUI src-tauri compiles (4-way exhaustive match satisfied); docs-matrix-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Item A — CLRG mnemonic fidelity fix. The clear-registers op now displays as CLRG (authentic HP-41 mnemonic) in PRGM mode and resolves via XEQ "CLRG"; XEQ "CLREG" still works as a legacy alias.</what-built>
  <how-to-verify>
On iPhone 15 Pro (build + install via the standard flow):
1. Build the iOS app: `cd hp41-gui &amp;&amp; npm run tauri ios build`
2. Install/launch on the device via `xcrun devicectl` (the established Item-A on-device flow).
3. In the running app: XEQ "CLRG" — set a register (e.g. 5 STO 01), then XEQ "CLRG", then RCL 01 → expect 0.000.
4. XEQ "CLREG" (legacy) — confirm it also clears registers (back-compat).
5. Enter PRGM mode, key the clear-registers op into a program, and confirm the program line displays "CLRG" (not "CLREG").
  </how-to-verify>
  <resume-signal>Type "approved" (CLRG works, displays CLRG, CLREG still works) or describe the issue.</resume-signal>
</task>

<!-- ════════════ ITEM B — TABBED FULL-FUNCTION OVERLAY ════════════ -->
<!-- GUI-frontend + tests only. Depends on the existing 6-pool help_data.ts.
     Independent of Item A's source files (no overlap). Ends with on-device checkpoint. -->

<task type="auto" tdd="true">
  <name>Task B1: All-Functions dataset builder in help_data.ts + unit tests</name>
  <files>hp41-gui/src/help_data.ts, hp41-gui/src/help_data.test.ts</files>
  <behavior>
    - allFunctionsEntries() returns every helpEntriesAll() entry with status === 'implemented' (does NOT filter on key_path).
    - Entries with status !== 'implemented' (deferred-v3 / na) are excluded.
    - The 74 cv-pool key_path:null implemented entries (SIN, COS, LN, LOG, SQRT, AVIEW, CLRG, …) ARE present.
    - All 5 module pools' implemented entries are present (they have no key_path:null rows already).
    - The existing keyboard-shortcuts dataset (key_path != null) is unchanged and still produced by the existing accessor.
  </behavior>
  <action>
Add an exported `allFunctionsEntries(): readonly HelpEntry[]` to help_data.ts that returns
`helpEntriesAll().filter(e => e.status === 'implemented')` — note: NO `key_path !== null`
exclusion (this is the whole point; the All Functions tab includes XEQ-by-name-only ops).
Memoize the same way helpEntriesAll() does (cache a stable reference for React referential
equality). Document it: revises D-26.8's "exclude key_path:null" rule for the new tab only;
the keyboard tab keeps the exclusion.

Do NOT modify the existing keyboard-shortcut path (getKeyboardShortcuts) or the existing
key_path-filtered helpers — the Keyboard Shortcuts tab reuses them as-is.

Write Vitest tests in help_data.test.ts (create if absent, else append) asserting the
behavior block above. Use concrete probe names from the audit (e.g. assert allFunctionsEntries
contains an entry with display_name "SIN" and one with display_name "CLRG", and that a
known deferred/na entry is absent).
  </action>
  <verify>
    <automated>cd hp41-gui &amp;&amp; npx vitest run src/help_data.test.ts</automated>
  </verify>
  <done>allFunctionsEntries() exported and memoized; Vitest proves implemented key_path:null entries (SIN, CLRG, …) are included and non-implemented entries excluded; keyboard-shortcut path untouched.</done>
</task>

<task type="auto">
  <name>Task B2: Two-tab HelpOverlay (Keyboard Shortcuts | All Functions) with iOS-aware default</name>
  <files>hp41-gui/src/HelpOverlay.tsx, hp41-gui/src/App.tsx</files>
  <action>
Refactor HelpOverlay.tsx into two tabs per the locked decision:

1. Add an `isIos: boolean` prop to HelpOverlayProps and pass it from App.tsx (App already
   holds `isIos` state at line 284; HelpOverlay is rendered there). Use it ONLY to pick the
   default active tab.

2. Add tab state: `const [activeTab, setActiveTab] = useState<'shortcuts' | 'all'>(...)`.
   Default: iOS → 'all', Desktop → 'shortcuts'. Reset to the default on each overlay open
   (mirror the existing `useEffect(..., [open])` reset block at line 127-134).

3. Render a tab bar (two buttons: "Keyboard Shortcuts", "All Functions") with
   aria-selected / role="tab" semantics. Reuse existing overlay header/search markup.

4. Tab 1 "Keyboard Shortcuts" — render the CURRENT behavior unchanged: the keyboard
   shortcuts table (lines 245-273) PLUS the existing key_path-filtered module sections
   (the current `helpEntriesAll().filter(e => e.key_path !== null)` flow at lines 138-177
   and the sectionGroups render at 274-332). Search applies here as today.

5. Tab 2 "All Functions" — build from `allFunctionsEntries()` (Item B1). Apply the SAME
   two-level Module → Category grouping the keyboard tab already uses (reuse SECTIONS
   predicates + the category-grouping logic), but source from allFunctionsEntries() so
   key_path:null rows are INCLUDED. Module order per locked decision:
   HP-41CV Built-in → Math Pac (MATH 1) → Stat Pac (STAT 1) → Time Pac (TIME 2C) →
   Advantage Pac (single header) → Extended Memory (X-MEM). The current SECTIONS array
   already encodes built-in + Math1 + Stat1 + Time + Adv22 + Adv24 + (xmem via predicate);
   for the All Functions tab present Advantage as a SINGLE header (combine Adv Conv + Adv
   Math under one "Advantage Pac" section) and add an Extended Memory section
   (predicate `e.xrom === undefined && category === "Extended Memory"`, or the X-MEM pool's
   marker — confirm the xmem entries' shape; they have no xrom field per CLAUDE.md D-52.4).
   Rows with key_path:null render without a key chip (the existing row markup already shows
   `entry.key_path` — guard for null so the key column is empty/omitted, not "null").
   Search applies WITHIN the All Functions tab across display_name + description + category.

6. Both tabs: status !== 'implemented' entries stay hidden (keyboard tab already excludes
   them implicitly via key_path filter for most; All Functions excludes them via the
   allFunctionsEntries status filter). Keep the per-entry expand (example/notes) affordance.

Keep the component a pure presentation change — no IPC, no new Tauri command (is_ios already
exists). Preserve Esc-close and search-reset-on-open behavior.
  </action>
  <verify>
    <automated>cd hp41-gui &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Overlay shows two tabs; All Functions tab renders implemented key_path:null functions grouped Module→Category in the locked order; default tab is iOS-aware; search filters within the active tab; tsc clean; full Vitest suite green.</done>
</task>

<!-- ════════════ ITEM C — COVERAGE GUARDRAIL ════════════ -->
<!-- Co-located with Item B (same wave, GUI/TS). The closest guard to the overlay
     is a Vitest test over the help_data builder. -->

<task type="auto">
  <name>Task C1: Overlay-completeness guardrail test</name>
  <files>hp41-gui/src/help_data.test.ts</files>
  <action>
Add a guardrail test (mirroring the spirit of hp41-cli/tests/key_coverage.rs and
function_matrix_parity.rs, but in Vitest over the TS builder since the overlay is GUI/TS):

Assert that EVERY entry in `helpEntriesAll()` with `status === 'implemented'` is present in
the dataset backing the "All Functions" tab (`allFunctionsEntries()`), keyed by op_variant.
Concretely: build the set of implemented op_variants from helpEntriesAll(), build the set of
op_variants from allFunctionsEntries(), and assert the first is a subset of the second —
failing with a list of any missing op_variants. This fails loudly if a future change ever
re-introduces a key_path-style exclusion that hides an implemented function.

Rationale to document in the test header: the original gap (74 implemented functions invisible)
went undetected because parity tests assert JSON↔Op completeness, not overlay visibility. This
test closes that hole at the overlay-dataset boundary.

Note in the SUMMARY (do not implement here): CLI parity. The CLI TUI `?` overlay
(hp41-cli/src/help_data.rs `help_overlay_rows`, line 268) still applies the key_path:null
exclusion. Per the GUI-first recommendation, CLI tab parity is OUT OF SCOPE for this plan —
flag it as a follow-up so CLI↔GUI parity (CLAUDE.md) is consciously deferred, not forgotten.
  </action>
  <verify>
    <automated>cd hp41-gui &amp;&amp; npx vitest run src/help_data.test.ts</automated>
  </verify>
  <done>Guardrail test passes today and would fail if any implemented function were dropped from the All Functions dataset; CLI-parity deferral recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Item B+C — Tabbed `?` overlay. Two tabs (Keyboard Shortcuts | All Functions). All Functions lists every implemented HP-41 function (including SIN, LN, SQRT, AVIEW, CLRG and the other previously-invisible ops) grouped Module→Category; search works within the active tab; default tab is iOS-aware. A guardrail test prevents the gap from reopening.</what-built>
  <how-to-verify>
On iPhone 15 Pro:
1. Build: `cd hp41-gui &amp;&amp; npm run tauri ios build`; install/launch via `xcrun devicectl`.
2. Open the `?` overlay. Confirm it defaults to the "All Functions" tab (iOS default).
3. In All Functions: scroll and confirm previously-missing functions are now listed under
   "HP-41CV Built-in" — e.g. SIN, COS, LN, LOG, SQRT, AVIEW, CLRG. Confirm module headers
   appear in order: HP-41CV Built-in → Math Pac → Stat Pac → Time Pac → Advantage Pac
   (single header) → Extended Memory.
4. Search "sin" → SIN appears (it would NOT have before). Search "clrg" → CLRG appears.
5. Switch to the "Keyboard Shortcuts" tab → confirm the original keyboard-shortcut content
   and key-mapped sections are intact.
6. (Optional desktop sanity) On desktop the overlay should default to "Keyboard Shortcuts".
  </how-to-verify>
  <resume-signal>Type "approved" (tabs work, All Functions complete, search finds SIN/CLRG, default tab correct) or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time JSON → bundle | docs/*.json static-imported into the GUI bundle (Vite); malformed JSON is a hard build blocker (D-25.17), not a runtime input. |
| XEQ name → resolver | User-typed XEQ name string crosses into builtin_card_op; unknown names already return None and surface as a toast (never silently swallowed, D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lu0-01 | Tampering | docs/hp41cv-functions.json display_name rename | mitigate | function_matrix_parity reverse test resolves display_name via builtin_card_op; CLRG arm added in same task → drift caught at compile/test time. `just docs-matrix-check` gates doc drift. |
| T-lu0-02 | Tampering | 4-way exhaustive-match (op_display_name mirrors) | mitigate | Both prgm_display.rs mirrors updated; GUI src-tauri `cargo check` in A2 proves the exhaustive match compiles. |
| T-lu0-03 | Information disclosure | All Functions tab exposing internal/deferred ops | accept | Only status==implemented entries shown; deferred/na hidden by allFunctionsEntries filter and asserted by guardrail. No PII; function list is reference documentation. |
| T-lu0-SC | Tampering | npm/cargo installs | accept | No new dependencies added (zero-new-runtime-dep invariant; pure source + test edits). No package install tasks → no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `just docs-matrix-check` clean (JSON ↔ generated matrix in sync after CLRG rename).
- `cd hp41-cli && cargo test` green (xeq_builtin_resolver: CLRG + CLREG; function_matrix_parity forward+reverse).
- `cd hp41-gui/src-tauri && cargo check` green (4-way exhaustive match compiles).
- `cd hp41-gui && npx tsc --noEmit && npx vitest run` green (overlay tabs, allFunctionsEntries, guardrail).
- On-device (iPhone 15 Pro): Item A checkpoint (CLRG resolves + displays) and Item B+C checkpoint (tabs + complete All Functions + search) both approved.
</verification>

<success_criteria>
- XEQ "CLRG" clears all data registers; XEQ "CLREG" still works (back-compat).
- The clear-registers op displays as CLRG in PRGM mode (both CLI and GUI op_display_name).
- The `?` overlay has Keyboard Shortcuts and All Functions tabs; default tab is iOS-aware.
- Every status:implemented function across all 6 pools (including the 74 previously-hidden cv key_path:null ops) appears in the All Functions tab and is findable by search.
- A Vitest guardrail fails if any implemented function is dropped from the All Functions dataset.
- No new runtime deps; math1/ freeze untouched; 4-way invariant + JSON canonical data flow respected.
</success_criteria>

<output>
Create `.planning/quick/260603-lu0-help-overlay-function-index-clrg-fidelit/260603-lu0-SUMMARY.md` when done. Record:
- The CLRG fix decision (option i: rename + CLREG back-compat alias) and the files touched.
- CLI↔GUI parity deferral: the CLI TUI overlay still excludes key_path:null (GUI-first; follow-up).
- Sibling-mnemonic follow-ups surfaced (CL SIGMA / CLΣ JSON display_name check; CLRALPHA vs CLA duplicate) — NOT fixed in this plan.
</output>
