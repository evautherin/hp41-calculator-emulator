---
phase: quick-260602-kw4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src/scale.ts
  - hp41-gui/src/main.tsx
  - hp41-gui/src/App.css
  - hp41-gui/src/scale.test.ts
autonomous: false
requirements: [WS-01]
must_haves:
  truths:
    - "On a macOS window larger than the design box, the calculator grows uniformly to fill the viewport (scale > 1), not centered at native size with empty margins."
    - "On a tall iPhone viewport the calculator fills the available height (scale > 1, aspect preserved)."
    - "On a short macOS menu-bar popover (viewport SMALLER than the design box) the calculator still downscales below 1 so nothing is clipped."
    - "The upscale never exceeds MAX_SCALE (~2×) on a huge 4K display — keys do not become absurdly large."
    - "There is no dead space inside the scaled box: the design box hugs the real rendered calculator footprint (392 × 900) instead of the old 440 × 1020."
  artifacts:
    - path: "hp41-gui/src/scale.ts"
      provides: "computeScale with MAX_SCALE upscale cap; corrected DESIGN_WIDTH/DESIGN_HEIGHT"
      contains: "MAX_SCALE"
    - path: "hp41-gui/src/scale.test.ts"
      provides: "tests proving upscaling happens and the MAX_SCALE cap holds, plus retained downscale + guard cases"
      contains: "MAX_SCALE"
  key_links:
    - from: "hp41-gui/src/main.tsx"
      to: "hp41-gui/src/scale.ts"
      via: "imports DESIGN_WIDTH, DESIGN_HEIGHT, computeScale"
      pattern: "computeScale|DESIGN_WIDTH|DESIGN_HEIGHT"
    - from: "hp41-gui/src/scale.test.ts"
      to: "hp41-gui/src/scale.ts"
      via: "imports computeScale, MAX_SCALE, DESIGN_WIDTH, DESIGN_HEIGHT"
      pattern: "from \"./scale\""
---

<objective>
Eliminate the whitespace around the calculator by (1) correcting the fixed design box to hug the real rendered footprint and (2) replacing the "never upscale" cap with an upscale-to-fill behavior capped at ~2×. The calculator must grow uniformly (aspect preserved, no distortion) to fill the viewport in all three contexts: macOS window mode, macOS menu-bar popover, and iPhone — while still downscaling below 1 when the viewport is smaller than the design box.

Purpose: Two root causes leave large empty margins around the calculator: a fixed design box (440 × 1020) much larger than the real content (~392 × ~900) leaves internal dead space, and `Math.min(1, …)` caps the scale at native size so it never grows to fill big viewports.

Output: `scale.ts` with corrected design constants + a `MAX_SCALE` upscale cap; `main.tsx` wrapper unchanged in structure but consuming the new constants; `App.css` `.calculator` margin removed so the box hugs content; `scale.test.ts` updated to the new behavior with new upscale + cap cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Frontend-only quick task. NO Rust / hp41-core changes (frozen invariant). NO ROADMAP.md update.
# Task runner is `just` — never bare cargo/npm (CLAUDE.md Tech Stack). GUI: `just gui-ci`, `just gui-dev`.

<measurements>
<!-- Verified by reading the source. Use these numbers; do not re-derive from scratch. -->

Keyboard SVG (hp41-gui/src/Keyboard.tsx, constants lines 104–122):
  COLS=5, KEY_W=68, KEY_H=44, GAP=8, PAD=10, TOP_GAP=12,
  SHIFT_LABEL_H=11, ALPHA_LABEL_H=11
  GRID_ROW_H = 11 + 44 + 11 + 8 = 74
  TOP_ROW_H  = KEY_H = 44
  KEYBOARD_W = 10*2 + 5*68 + 4*8 = 392
  KEYBOARD_H = 10*2 + 44 + 12 + 8*74 = 668
  SVG renders width="100%" viewBox="0 0 392 668" → at .calculator width 392px the
  keyboard renders 392 × 668 (aspect 1:1 with viewBox; no distortion).

Header rows above the keyboard (App.tsx lines 1060–1112; CSS in App.css; global box-sizing:border-box):
  .calculator-title-bar  padding 2px + 28px icon button  ≈ 32px
  .annunciators          padding 4px + ~14px line        ≈ 22px
  .display               min-height 2em @22px font        = 44px
  .stack-panel           5 stack-rows + padding           ≈ 115px
  .error-row             TRANSIENT (only on error)        — excluded from baseline
  header subtotal ≈ 213px

.calculator wrapper:
  width: 392px (border-box, includes 1px border)
  margin: 16px auto  → 32px vertical (this task REMOVES it)

Realistic rendered footprint:
  width  ≈ 392px
  height ≈ 213 (header) + 668 (keyboard) + 32 (margins, removed by this task) ≈ ~913px

CORRECTED DESIGN BOX (this plan):
  DESIGN_WIDTH  = 392   (was 440 — removes ~48px horizontal slack)
  DESIGN_HEIGHT = 900   (was 1020 — removes ~120px vertical dead space; 900 hugs the
                         ~913px footprint AFTER the .calculator margin is removed, with a
                         small safety cushion so font-metric variance never clips).
</measurements>

<interfaces>
Current hp41-gui/src/scale.ts exports:
  export const DESIGN_WIDTH = 440;
  export const DESIGN_HEIGHT = 1020;
  export function computeScale(viewportWidth, viewportHeight, designWidth?, designHeight?): number
    → returns Math.min(1, vw/designW, vh/designH); returns 1 for non-positive viewport.

Current hp41-gui/src/main.tsx ScaledApp consumes:
  computeScale(window.innerWidth, window.innerHeight) on mount + on resize;
  inner box style width: DESIGN_WIDTH, height: DESIGN_HEIGHT, transform: scale(${scale}),
  transformOrigin: 'top center', flex: '0 0 auto';
  outer box: 100vw × 100vh, overflow hidden, flex centered, alignItems flex-start.

Current scale.test.ts asserts the OLD cap-at-1 behavior (lines 5–12, 26–29) and downscale
math (lines 14–24). The downscale + zero-guard cases stay; the cap-at-1 cases are replaced.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the cap-at-1 with a MAX_SCALE upscale cap and correct the design box, with tests</name>
  <files>hp41-gui/src/scale.ts, hp41-gui/src/scale.test.ts</files>
  <behavior>
    scale.test.ts (write/adjust these expectations FIRST, then make scale.ts pass):
    - Export a numeric MAX_SCALE constant ≈ 2; tests import it from "./scale".
    - DESIGN_WIDTH === 392 and DESIGN_HEIGHT === 900 (corrected design box).
    - Upscale happens: computeScale(DESIGN_WIDTH * 2, DESIGN_HEIGHT * 2) === 2 (both ratios = 2, equals MAX_SCALE) — proves scale now exceeds 1.
    - Upscale below the cap: computeScale(DESIGN_WIDTH * 1.5, DESIGN_HEIGHT * 1.5) toBeCloseTo(1.5, 5) — grows past 1 but under MAX_SCALE.
    - Cap holds: computeScale(10000, 10000) === MAX_SCALE (never exceeds the cap on a huge display).
    - At exactly the design size: computeScale(DESIGN_WIDTH, DESIGN_HEIGHT) === 1 (neutral).
    - Downscale still works (menu-bar popover smaller than design): computeScale(DESIGN_WIDTH, DESIGN_HEIGHT / 2) toBeCloseTo(0.5, 5); computeScale(DESIGN_WIDTH / 4, DESIGN_HEIGHT) toBeCloseTo(0.25, 5) — scale still drops below 1 when viewport is smaller.
    - Smaller-of-two-ratios still chosen: computeScale(DESIGN_WIDTH / 2, DESIGN_HEIGHT / 4) toBeCloseTo(0.25, 5).
    - Zero/negative guard retained: computeScale(0, 0) === 1; computeScale(-100, -100) === 1.
    - Remove the obsolete "never scales above 1 (no upscaling)" assertion and the old "returns 1 when the viewport is at least the design size" 2000×2000→1 assertion (those encode the deleted cap-at-1 contract). Replace the describe-block intent so the file reads as the new MAX_SCALE contract.
  </behavior>
  <action>
    In hp41-gui/src/scale.ts: change DESIGN_WIDTH from 440 to 392 and DESIGN_HEIGHT from 1020 to 900 (per the corrected footprint in <measurements> — keyboard 392×668 + ~213px header, margin removed). Add `export const MAX_SCALE = 2;` with a comment explaining it caps absurd upscaling on 4K displays per the locked decision. Rewrite the computeScale return to `Math.min(MAX_SCALE, viewportWidth / designWidth, viewportHeight / designHeight)` — the ONLY change to the cap is replacing the literal `1` with `MAX_SCALE`; the two ratio terms and the non-positive-viewport early-return-1 guard stay exactly as-is so downscaling below 1 (menu-bar popover) is preserved. Update the file-top doc comment and the computeScale JSDoc: it now UPSCALES up to MAX_SCALE to fill the viewport and still downscales when the viewport is smaller; drop the "Never upscales (capped at 1)" sentence. Then update scale.test.ts to the <behavior> expectations above (TDD: write the new test expectations, run RED, then make scale.ts pass GREEN). Do NOT inline-edit only the constant — verify the JSDoc no longer claims cap-at-1.
  </action>
  <verify>
    <automated>cd hp41-gui && npx vitest run src/scale.test.ts</automated>
  </verify>
  <done>scale.ts exports MAX_SCALE≈2 with DESIGN_WIDTH=392, DESIGN_HEIGHT=900; computeScale upscales to MAX_SCALE on large viewports, downscales below 1 on small ones; scale.test.ts passes including the new upscale + cap cases and retained downscale/guard cases; no remaining assertion encodes the old cap-at-1 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Make the wrapper box hug the corrected design footprint (remove dead-space sources)</name>
  <files>hp41-gui/src/main.tsx, hp41-gui/src/App.css</files>
  <action>
    In hp41-gui/src/App.css, the `.calculator` rule (lines 5–17): remove the `margin: 16px auto;` declaration (line 14) so the calculator no longer adds 32px of vertical centering margin INSIDE the design box — the outer flex wrapper in main.tsx already centers horizontally, and the corrected DESIGN_HEIGHT=900 assumes this margin is gone. Keep every other `.calculator` property (display:flex, flex-direction:column, width:392px, background/border/border-radius, overflow:hidden, font-family, position:relative) byte-identical. In hp41-gui/src/main.tsx: no structural change is required — ScaledApp already imports DESIGN_WIDTH/DESIGN_HEIGHT/computeScale from ./scale and applies them to the inner box width/height + transform:scale, so it picks up the corrected 392×900 box and the new MAX_SCALE automatically. Update only the ScaledApp doc comment so it no longer claims "On a full-height window the scale is 1 (no visual change)" — it now upscales to fill (up to MAX_SCALE). Confirm transformOrigin stays 'top center' and alignItems stays 'flex-start' so the scaled box anchors at the top while filling width.
  </action>
  <verify>
    <automated>cd hp41-gui && grep -n "margin: 16px auto" src/App.css | grep -v '^#'; test $(cd hp41-gui && grep -c "margin: 16px auto" src/App.css) -eq 0 && echo "MARGIN_REMOVED_OK"</automated>
  </verify>
  <done>`.calculator` no longer has `margin: 16px auto` (all other properties intact); main.tsx ScaledApp doc comment reflects upscale-to-fill behavior; the wrapper consumes the corrected DESIGN_WIDTH/DESIGN_HEIGHT and MAX_SCALE with no structural change.</done>
</task>

<task type="auto">
  <name>Task 3: Full GUI CI gate (typecheck + lint + all frontend tests)</name>
  <files>hp41-gui/src/scale.ts, hp41-gui/src/scale.test.ts, hp41-gui/src/main.tsx, hp41-gui/src/App.css</files>
  <action>
    Run the project's GUI CI aggregate via `just` (per CLAUDE.md Tech Stack — never bare cargo/npm) to confirm the change does not break TypeScript types, ESLint, or any existing frontend test (including snapshot/component tests that might reference the design box). If `just gui-ci` includes a Rust src-tauri build step that is slow or unrelated, the frontend gate alone is sufficient evidence — but prefer the full `just gui-ci` so no parity check is missed. Fix any type or lint fallout strictly within the four files this plan owns; do NOT touch hp41-core or any Rust file (frozen invariant).
  </action>
  <verify>
    <automated>just gui-ci</automated>
  </verify>
  <done>`just gui-ci` passes: TypeScript typecheck clean, ESLint clean, all frontend tests green (scale.test.ts + any others). No Rust/core files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport → CSS transform | Window/viewport dimensions feed `computeScale`; output drives a visual CSS `scale()` transform only. No data crosses into the Rust core or IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kw4-01 | Denial of Service | computeScale on zero/negative viewport (first paint) | mitigate | Existing non-positive-viewport early-return-1 guard is retained verbatim; covered by retained zero/negative test cases. |
| T-kw4-02 | Tampering | npm/pip/cargo installs | accept | No new dependencies added; this task only edits existing TS/CSS files. No package install tasks present, so the Package Legitimacy Gate is not triggered. |
</threat_model>

<verification>
- `npx vitest run src/scale.test.ts` (in hp41-gui): upscale, cap, downscale, neutral, and guard cases all pass.
- `just gui-ci`: typecheck + lint + full frontend test suite green.
- Manual (deferred to checkpoint): `just gui-dev` and resize the macOS window large → calculator grows to fill, no whitespace; shrink window below 900px tall → calculator downscales, nothing clipped.
- Frozen-invariant check: `git diff --name-only` shows ONLY hp41-gui/src/{scale.ts,scale.test.ts,main.tsx,App.css}; no hp41-core or Rust file touched.
</verification>

<success_criteria>
- DESIGN_WIDTH=392, DESIGN_HEIGHT=900, MAX_SCALE≈2 exported from scale.ts.
- computeScale upscales above 1 on large viewports (proven by test), caps at MAX_SCALE, and still downscales below 1 on small viewports (proven by test).
- `.calculator` margin removed; no internal dead space in the design box.
- `just gui-ci` green; no Rust/core changes.
</success_criteria>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Upscale-to-fill scaling: corrected design box (392×900), MAX_SCALE≈2 upscale cap replacing the cap-at-1, and removal of the `.calculator` centering margin. computeScale tests and `just gui-ci` are green.</what-built>
  <how-to-verify>
    1. Run `just gui-dev` to launch the desktop GUI.
    2. macOS WINDOW mode: enlarge the window well beyond the calculator's native size. EXPECT: the calculator grows uniformly to fill the viewport (aspect preserved, keys not distorted), with the surrounding whitespace eliminated. On a very large/4K display it stops growing at ~2× and centers with minimal margin.
    3. SHORT viewport (simulate the menu-bar popover): make the window shorter than ~900px tall. EXPECT: the calculator downscales below native size so nothing is clipped — the bottom keyboard row stays fully visible.
    4. If you can run on iPhone (`just ios-dev` per project tooling): EXPECT the calculator fills the tall viewport top-anchored with no large empty margins.
    5. Confirm there is no internal dead band at the bottom of the scaled box (the old ~120px gap below the keyboard is gone).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still has whitespace / is clipped / is distorted.</resume-signal>
</task>

<output>
Create `.planning/quick/260602-kw4-eliminate-whitespace-around-the-calculat/260602-kw4-SUMMARY.md` when done.
</output>
