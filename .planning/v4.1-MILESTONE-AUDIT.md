---
milestone: 4.1
audited: 2026-06-04T12:35:00Z
status: passed
scores:
  requirements: 25/25
  phases: 5/5
  integration: 4/4
  flows: 4/4
gaps: {}
tech_debt:
  - phase: 57-signing-testflight-pipeline
    items:
      - "SHIP-02: PrivacyInfo.xcprivacy exists in gen/apple/ and is declared in project.yml, but is NOT wired into the committed .pbxproj, so it is absent from the shipped bundle → Apple emits ITMS-91053 on upload. Non-blocking for internal TestFlight (D-57.9); MUST be closed before any App Store (external) submission. Tracked: .planning/todos/pending/privacy-manifest-bundle-wiring.md. The ios-init regen route is environment-sensitive (duplicates libapp.a) — fix at the project.yml/source level or via a surgical .pbxproj edit."
nyquist:
  note: "Nyquist validation not adopted for this milestone (VALIDATION.md only on phase 57 as a planning artifact); phase verification + the live device/CI evidence is the coverage basis."
---

# v4.1 iOS Foundation — Milestone Audit

**Verdict: PASSED.** All 25 requirements satisfied, all 5 phases verified, cross-phase
integration fully wired, the headline E2E flow proven on a physical device and via a green CI
run. One tracked tech-debt item (SHIP-02 privacy-manifest bundle-wiring) is deferred by design
and is out of scope for the internal-TestFlight definition of done.

## Requirements coverage (3-source cross-reference)

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| BUILD | 01–04 | 53 | satisfied (spike + device install + ASC app registration; integration-confirmed) |
| PERSIST | 01–03 | 54 | satisfied (device round-trip approved iPhone 15 Pro; checkboxes + Phase-54 VERIFICATION corrected during this audit) |
| TOUCH | 01–11 | 55 | satisfied (VERIFICATION passed; device-verified) |
| LIFE | 01–02 | 56 | satisfied (VERIFICATION passed; device-verified) |
| SHIP | 01–05 | 57 | satisfied (VERIFICATION added this audit; CI green + device install); **SHIP-02 bundle-wiring deferred (tech debt)** |

25/25 requirement checkboxes now `[x]` in REQUIREMENTS.md.

## Phase verification

| Phase | VERIFICATION.md | Status |
|-------|-----------------|--------|
| 53 spike | none (spike-exempt) | Verified via 53-0x SUMMARYs + ADR v4.1-002 + on-device install; integration-confirmed |
| 54 persistence | yes | passed (flipped from human_needed — device round-trip approved) |
| 55 touch UI | yes | passed |
| 56 lifecycle/clock | yes | passed |
| 57 signing/TestFlight | yes (added this audit) | passed (SHIP-02 bundle-wiring deferred) |

## Cross-phase integration (gsd-integration-checker)

**WIRED — 0 broken flows, 0 orphaned exports, 0 missing connections.** Verified:
- Persistence path resolver used at all save/load sites (`lib.rs:79/113`, `commands.rs:593`); desktop `~/.hp41/` unchanged via `#[cfg(not(mobile))]`.
- Single `visibilitychange` listener serves both autosave (54) and foreground `tick_time` (56) without conflict; `isIosRef`/`needsTickRef` sync avoids the stale-closure trap.
- Touch keys dispatch through `hp41-core` via `dispatch_op` → `key_map::resolve` → `ops::dispatch`; SC-4 clean (no core-op duplication in GUI); `hp41-core` has zero UI/CLI deps.
- `ci-ios.yml` builds only via `just ios-build-release` (no raw cargo/xcodebuild in run: steps); bundle id `ch.talent-factory.hp41` + team `2P4R8QSWT4` consistent across project.yml / Info.plist / ExportOptions / workflow.

## E2E flow

Cold launch → key dispatch (engine) → background autosave → kill → relaunch (state restored) →
clock correct on foreground: **wired in code and empirically confirmed** (TestFlight install on a
physical iPhone + green ci-ios `workflow_dispatch` run 26950247621).

## Tech debt (1 item — accepted, tracked)

- **SHIP-02 PrivacyInfo bundle-wiring** — see frontmatter + `privacy-manifest-bundle-wiring.md`.
  Required before public App Store submission; not before internal TestFlight.

## Audit corrections applied

- REQUIREMENTS.md PERSIST-01..03 `[ ]`→`[x]` and traceability Pending→Complete (work was done + device-verified; checkboxes were stale).
- Phase 54 VERIFICATION status `human_needed`→`passed` (device round-trip was approved per 54-03-SUMMARY).
- Added `57-VERIFICATION.md` (phase 57 had been verified by live CI + device rather than a host verifier subagent).
