---
phase: 51-x-mem-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/error.rs
  - hp41-core/src/ops/xmem/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/state.rs
autonomous: true
requirements: [XMEM-02]
must_haves:
  truths:
    - "XmemFile struct exists with name, kind (Program|Data), data: Vec<u8>, reg_count: usize"
    - "XmemFile::register_count() returns ⌈data.len()/7⌉+1 for Program files and reg_count+1 for Data files"
    - "XMEM_CAPACITY constant equals 600"
    - "HpError has FileNotFound, FileType, NoRoom variants with display strings 'fl not found', 'fl type err', 'no room'"
    - "CalcState carries xmem_files and xmem_active_file, both #[serde(default)] WITHOUT #[serde(skip)]"
    - "A v3.3 save file (JSON with no xmem fields) deserializes into CalcState with empty xmem_files and None xmem_active_file"
    - "xmem_files + xmem_active_file survive a serde round-trip"
  artifacts:
    - path: "hp41-core/src/ops/xmem/mod.rs"
      provides: "XmemFile struct, XmemKind enum, register_count(), XMEM_CAPACITY"
      contains: "pub struct XmemFile"
    - path: "hp41-core/src/error.rs"
      provides: "FileNotFound, FileType, NoRoom HpError variants"
      contains: "FileNotFound"
    - path: "hp41-core/src/state.rs"
      provides: "xmem_files + xmem_active_file persistent fields + CalcState::new() init"
      contains: "xmem_files"
  key_links:
    - from: "hp41-core/src/ops/mod.rs"
      to: "hp41-core/src/ops/xmem/mod.rs"
      via: "pub mod xmem declaration"
      pattern: "pub mod xmem"
    - from: "hp41-core/src/state.rs"
      to: "crate::ops::xmem::XmemFile"
      via: "xmem_files field type"
      pattern: "xmem_files: Vec<crate::ops::xmem::XmemFile>"
---

<objective>
Build the X-MEM contract layer in `hp41-core`: the `XmemFile` data model, the three new `HpError` variants, and the two persistent `CalcState` fields. This is the foundation the 8 X-MEM ops (Plan 02) implement against — type definitions and capacity constant land first so the op functions have concrete contracts to call.

Purpose: Establish the isolated X-MEM storage model (P56 / D-51.0a, repeating the D-43.5 named-matrix isolation pattern) and the error vocabulary (D-07 never-discard) before any op logic exists. Also satisfies XMEM-02 by defining `XMEM_CAPACITY = 600` and `register_count()`, the inputs to the EMROOM formula.

Output: `hp41-core/src/ops/xmem/mod.rs` (new), modified `error.rs`, `ops/mod.rs` (module declaration), and `state.rs` (two new fields + init). Backward-compat serde proven by a round-trip and a v3.3-style legacy-JSON load test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-x-mem-core/51-CONTEXT.md
@.planning/phases/51-x-mem-core/51-RESEARCH.md
@.planning/phases/51-x-mem-core/51-PATTERNS.md

<interfaces>
<!-- Verified signatures the executor must build against. Extracted from codebase 2026-05-28. No exploration needed. -->

AdvMatrix struct precedent (hp41-core/src/ops/advantage/mod.rs:111) — XmemFile mirrors this exactly:
  #[derive(Debug, Clone, Serialize, Deserialize, Default)]
  pub struct AdvMatrix { pub name: String, pub rows: u8, ..., pub data: Vec<HpNum> }

CalcState isolation precedent (hp41-core/src/state.rs:356-388) — adv_matrices / adv_tvm_state:
  #[serde(default)] pub adv_matrices: Vec<crate::ops::advantage::AdvMatrix>,
  #[serde(default)] pub adv_tvm_state: Option<crate::ops::advantage::TvmState>,
  (persistent fields: #[serde(default)] WITHOUT #[serde(skip)])

CalcState relevant existing fields (hp41-core/src/state.rs):
  pub regs: Vec<HpValue>       (line 57)  — X-MEM MUST NOT touch this except via SAVED/GETD
  pub program: Vec<crate::ops::Op>  (line 71)
  pub alpha_reg: String        (line 59)
  pub print_buffer: Vec<String> (line 111)

HpError enum (hp41-core/src/error.rs) — thiserror, last variant NoRoot at line 55, closing brace line 56:
  #[derive(Error, Debug, PartialEq, Clone)]
  pub enum HpError { #[error("out of range")] OutOfRange, #[error("alpha data")] AlphaData, ..., NoRoot }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create XmemFile model + add HpError variants</name>
  <files>hp41-core/src/ops/xmem/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/error.rs</files>
  <read_first>
    - hp41-core/src/ops/advantage/mod.rs (lines 100-173: AdvMatrix struct, constants, and #[cfg(test)] #[allow(clippy::unwrap_used)] test module — the exact structural template for XmemFile)
    - hp41-core/src/error.rs (full file: existing HpError variants, thiserror #[error(...)] Display pattern, last variant NoRoot at line 55, test module at lines 58-81)
    - hp41-core/src/ops/mod.rs (top of file: where `pub mod advantage;` / sibling module declarations live — add `pub mod xmem;` alongside)
    - .planning/phases/51-x-mem-core/51-PATTERNS.md (section "hp41-core/src/ops/xmem/mod.rs" and section "hp41-core/src/error.rs")
  </read_first>
  <behavior>
    - XmemFile::default() has empty name, kind Program, empty data, reg_count 0
    - register_count() on a default (Program, empty data) file returns 1 (0_usize.div_ceil(7) == 0, +1 header)
    - register_count() on a Program file with 8 bytes returns 3 (8.div_ceil(7) == 2, +1 header)
    - register_count() on a Program file with 922 bytes returns 133 (922.div_ceil(7) == 132, +1 header) — the OM-verified community example
    - register_count() on a Data file with reg_count 5 returns 6 (5 + 1 header)
    - XMEM_CAPACITY == 600
    - HpError::FileNotFound.to_string() == "fl not found"
    - HpError::FileType.to_string() == "fl type err"
    - HpError::NoRoom.to_string() == "no room"
  </behavior>
  <action>
    Create `hp41-core/src/ops/xmem/mod.rs`. Declare `pub mod xmem;` in `hp41-core/src/ops/mod.rs` alongside the existing sibling module declarations (e.g. next to `pub mod advantage;`).

    In `xmem/mod.rs`, mirror the `AdvMatrix` precedent. Imports: `use serde::{Deserialize, Serialize};` (do NOT import HpNum — XmemFile.data is `Vec<u8>`, not `Vec<HpNum>`).

    Define `pub enum XmemKind { Program, Data }` deriving `Debug, Clone, Serialize, Deserialize, Default, PartialEq` — mark `Program` as `#[default]`.

    Define `pub struct XmemFile` deriving `Debug, Clone, Serialize, Deserialize, Default` with fields: `pub name: String` (ALPHA-register file name per D-51.0c), `pub kind: XmemKind`, `pub data: Vec<u8>` (PROGRAM = encode_program output bytes; DATA = encode_data output bytes), and `#[serde(default)] pub reg_count: usize` (DATA-file register count stored at SAVED time to avoid decoding on every EMROOM call — Research Pitfall 1). Add a doc comment recording the D-51.0a / P56 isolation invariant (no connection to state.regs or adv_matrices).

    Implement `impl XmemFile { pub fn register_count(&self) -> usize { ... } }` per D-51.2: `XmemKind::Program => self.data.len().div_ceil(7) + 1` (7 program bytes packed per X-MEM register, +1 directory/header register); `XmemKind::Data => self.reg_count + 1` (N data registers + 1 header). Use `div_ceil` (stable on MSRV 1.88) — NEVER `floor()`/`fmod()` per CLAUDE.md decimal-field discipline.

    Add `pub const XMEM_CAPACITY: usize = 600;` with a comment "124 built-in + 2×238 (two 82181A modules)".

    Add a `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests` block covering every behavior listed above (default shape, the four register_count cases, XMEM_CAPACITY value).

    In `hp41-core/src/error.rs`, add three new variants after `NoRoot` (before the closing `}` of the enum): `#[error("fl not found")] FileNotFound`, `#[error("fl type err")] FileType`, `#[error("no room")] NoRoom`. Add doc comments mapping each to its HP-41CX QRG p.39 source string. Add three Display tests to the existing test module (FileNotFound → "fl not found", FileType → "fl type err", NoRoom → "no room").

    Do NOT add any `Op` enum variants or dispatch arms here — that is Plan 02. Do NOT touch `state.regs` or `adv_matrices`.
  </action>
  <verify>
    <automated>just test-core xmem::mod::tests 2>&1 | tail -5 && just test-core error 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/src/ops/xmem/mod.rs contains `pub struct XmemFile`, `pub enum XmemKind`, `pub const XMEM_CAPACITY: usize = 600`
    - `pub mod xmem;` present in hp41-core/src/ops/mod.rs
    - hp41-core/src/error.rs contains `FileNotFound`, `FileType`, `NoRoom` variants with the three exact `#[error(...)]` strings
    - Behavior: `XmemFile { kind: Program, data: vec![0u8;922], ..Default::default() }.register_count() == 133`
    - Behavior: `XmemFile { kind: Data, reg_count: 5, ..Default::default() }.register_count() == 6`
    - `just test-core xmem::mod::tests` exits 0
    - `just test-core error` exits 0 (the three new Display tests pass)
    - `register_count()` uses `div_ceil`, contains no `floor`/`fmod` call (grep `grep -v '^[[:space:]]*//' hp41-core/src/ops/xmem/mod.rs | grep -c 'floor\|fmod'` returns 0)
  </acceptance_criteria>
  <done>XmemFile model + XMEM_CAPACITY + three HpError variants exist and compile; all unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add xmem_files + xmem_active_file to CalcState with backward-compat serde</name>
  <files>hp41-core/src/state.rs</files>
  <read_first>
    - hp41-core/src/state.rs (lines 55-115: existing field block incl. regs/program/alpha_reg/print_buffer; lines 356-388: Phase 43 adv_matrices / adv_tvm_state serde-attribute block; lines 500-513: CalcState::new() initialization block; lines 547-579: migrate_after_load — do NOT add migration here, Phase 52 owns that)
    - hp41-core/src/ops/xmem/mod.rs (the XmemFile type created in Task 1 — the field references it)
    - .planning/phases/51-x-mem-core/51-PATTERNS.md (section "hp41-core/src/state.rs")
  </read_first>
  <behavior>
    - A fresh CalcState::new() has empty xmem_files and xmem_active_file == None
    - Serializing a CalcState with one XmemFile + Some("MYFILE") active, then deserializing, reproduces xmem_files (len 1, same name) and xmem_active_file == Some("MYFILE")
    - Deserializing a legacy JSON object that omits the xmem fields entirely yields xmem_files empty and xmem_active_file None (no error) — proves #[serde(default)] backward compat
  </behavior>
  <action>
    In `hp41-core/src/state.rs`, add a `// ── Phase 51 (v4.0): X-MEM (Extended Memory) ──` comment block immediately after the Phase 43 advantage field block (~line 388). Add two fields, both PERSISTENT (`#[serde(default)]` WITHOUT `#[serde(skip)]` — they must survive save/load; this mirrors `adv_matrices` / `adv_tvm_state`, NOT the transient `adv_current_matrix` pattern):

    `#[serde(default)] pub xmem_files: Vec<crate::ops::xmem::XmemFile>,` — doc comment recording the D-51.0a / P56 isolation invariant (NO connection to state.regs or adv_matrices; X-MEM ops never read/write state.regs except via the explicit SAVED/GETD transfer).

    `#[serde(default)] pub xmem_active_file: Option<String>,` — doc comment: active X-MEM DATA file for EMREG/SAVERX, set as a side effect of SAVED/GETD (D-51.4); persistent so EMREG/SAVERX survive save/load (analogous to adv_tvm_state). Per Research Pitfall 2, do NOT add `#[serde(skip)]`.

    In `CalcState::new()` (~lines 500-513), after the Phase 43 init lines, add `xmem_files: Vec::new(),` and `xmem_active_file: None,`.

    Do NOT modify `migrate_after_load()` — the new fields default cleanly via `#[serde(default)]`; the v3.3→v4.0 migration is Phase 52.

    Add a `#[cfg(test)] #[allow(clippy::unwrap_used)]` test (in the existing state.rs test module, or a new one if none) covering: (a) `xmem_serde_round_trip` — build a CalcState, push one `XmemFile` (name "MYFILE", kind Data, reg_count 3), set `xmem_active_file = Some("MYFILE".into())`, `serde_json::to_string` then `from_str`, assert the two fields survive; (b) `v33_save_loads_with_xmem_defaults` — `serde_json::from_value` (or from_str) on a minimal JSON object that omits both xmem keys, assert it deserializes Ok with empty xmem_files and None xmem_active_file.
  </action>
  <verify>
    <automated>just test-core state::tests::xmem 2>&1 | tail -8 && just test-core state::tests::v33 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/src/state.rs contains `pub xmem_files: Vec<crate::ops::xmem::XmemFile>` and `pub xmem_active_file: Option<String>`, each preceded by `#[serde(default)]` and NEITHER carrying `#[serde(skip)]`
    - CalcState::new() initializes both fields (`xmem_files: Vec::new()`, `xmem_active_file: None`)
    - Behavior: `just test-core state::tests::xmem_serde_round_trip` exits 0 (fields survive serialize→deserialize)
    - Behavior: `just test-core state::tests::v33_save_loads_with_xmem_defaults` exits 0 (legacy JSON without xmem keys loads with empty/None defaults)
    - `grep -c 'serde(skip)' ` on the two new field declarations is 0 (verify the two new fields are NOT marked skip)
    - migrate_after_load() is unchanged (no new xmem migration logic added)
  </acceptance_criteria>
  <done>CalcState carries the two persistent X-MEM fields; round-trip and legacy-load tests green; migrate_after_load untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save-file (JSON) → CalcState deserialization | Untrusted/legacy on-disk autosave.json crosses into in-memory state on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | XmemFile.data: Vec<u8> deserialized from save file | accept | Plan 02 op functions validate kind before decoding `data`; this plan only defines the type. Malformed bytes surface as decode errors (FileType / decode_data error) at op time, never silently. |
| T-51-02 | Denial of Service | xmem_files Vec growth from a crafted save file | mitigate | Capacity (XMEM_CAPACITY = 600) is enforced at SAVEP/SAVED in Plan 02; this plan defines the constant and register_count() inputs to that check. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (zero new runtime deps per CLAUDE.md). RESEARCH.md Package Legitimacy Audit is trivially satisfied (no external packages). slopcheck N/A. |
</threat_model>

<verification>
- `just test-core xmem` — XmemFile model tests green
- `just test-core error` — three new variant Display tests green
- `just test-core state::tests::xmem` and `state::tests::v33` — serde round-trip + backward-compat green
- `just lint` — no clippy::unwrap_used violations in production code (tests carry `#[allow(clippy::unwrap_used)]`)
- Workspace compiles (`just build`) — Plan 02 depends on these contracts existing
</verification>

<success_criteria>
- XmemFile struct + XmemKind enum + register_count() + XMEM_CAPACITY (600) exist and compile
- Three new HpError variants (FileNotFound, FileType, NoRoom) with correct Display strings
- CalcState carries xmem_files + xmem_active_file as persistent (#[serde(default)], no skip) fields, initialized in new()
- Backward-compat proven: v3.3-style JSON (no xmem keys) loads with empty defaults
- No floor()/fmod() in register_count(); no println! added; no touch of state.regs or adv_matrices
</success_criteria>

<output>
Create `.planning/phases/51-x-mem-core/51-01-SUMMARY.md` when done
</output>
