---
phase: 48-gui-infrastructure-theming
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src/themes.css
  - hp41-gui/src/App.css
autonomous: true
requirements: [THEME-01, THEME-03, THEME-04]

must_haves:
  truths:
    - "Four complete [data-theme] blocks exist in themes.css with all ~30 CSS custom properties each"
    - "App.css hardcoded hex colors are replaced with var(--token) references"
    - ".key transform-box: fill-box and transform-origin: center are never overridden in themes.css (P51)"
    - "High-contrast theme uses #ffffff on #000000 for display text (21:1 contrast, WCAG AAA)"
    - "No CSS transition on themed color properties (D-48.12 instant switch)"
  artifacts:
    - path: "hp41-gui/src/themes.css"
      provides: "Four [data-theme] blocks: dark, light, classic-beige, high-contrast"
      contains: "[data-theme=\"dark\"]"
    - path: "hp41-gui/src/App.css"
      provides: "Structural layout with CSS variable references instead of hardcoded hex colors"
      contains: "var(--calc-bg)"
  key_links:
    - from: "hp41-gui/src/App.css"
      to: "hp41-gui/src/themes.css"
      via: "CSS variable references consuming [data-theme] definitions"
      pattern: "var\\(--"
---

<objective>
Create the CSS theme system: a `themes.css` file with four complete `[data-theme]` variable blocks, and migrate all hardcoded color values in `App.css` to `var(--token)` references.

Purpose: Enables runtime theme switching via the `data-theme` attribute on `<body>` without any JavaScript color injection. The four themes (dark, light, classic beige, high-contrast) each fully re-skin the calculator appearance (D-48.7). This plan is CSS-only; no JS/TSX changes. Frontend wiring happens in Plan 03.

Output: `themes.css` with 4 complete theme blocks and `App.css` with all colors migrated to CSS variables. The calculator renders identically to current behavior when `data-theme="dark"` is set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-gui-infrastructure-theming/48-CONTEXT.md
@.planning/phases/48-gui-infrastructure-theming/48-RESEARCH.md
@.planning/phases/48-gui-infrastructure-theming/48-PATTERNS.md
@.planning/phases/48-gui-infrastructure-theming/48-UI-SPEC.md

<interfaces>
<!-- CSS color inventory extracted from App.css and UI-SPEC.md -->
<!-- The executor MUST use the exact hex values from 48-UI-SPEC.md Color section -->
<!-- for all four themes. The dark theme values must match current App.css exactly. -->

Color migration table (App.css hardcoded -> CSS variable):
  .calculator background #0d0d0d -> var(--calc-bg)
  .calculator border #333 -> var(--calc-border)
  .annunciators background #1a1a1a -> var(--annunciator-bg) [note: maps to --panel-bg]
  .annunciator color #555 -> var(--text-muted)
  .annunciator.active color #e8e8c0 -> var(--annunciator-active)
  .display background #111 -> var(--display-bg)
  .display color #c8e6c9 -> var(--display-text)
  .display border-bottom #222 -> var(--display-border)
  .stack-panel background #1a1a1a -> var(--panel-bg)
  .stack-row color #aaa -> var(--text-secondary)
  .stack-label color #666 -> var(--text-muted)
  .print-panel background #1a1a1a -> var(--panel-bg)
  .print-panel-header background #252525 -> var(--panel-header-bg)
  .print-panel-header border #3a3a3a -> var(--panel-border)
  .print-panel-header color #888 -> var(--text-secondary)
  .step-active background #1e3a1e -> var(--step-active-bg)
  .step-active color #c8e6c9 -> var(--step-active-text)

P51 INVARIANT: .key { transform-box: fill-box; transform-origin: center; } stays in App.css ONLY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create themes.css with four complete data-theme blocks</name>
  <files>hp41-gui/src/themes.css</files>
  <read_first>
    hp41-gui/src/App.css
    .planning/phases/48-gui-infrastructure-theming/48-UI-SPEC.md
    .planning/phases/48-gui-infrastructure-theming/48-PATTERNS.md
    .planning/phases/48-gui-infrastructure-theming/48-CONTEXT.md
  </read_first>
  <action>
    Create hp41-gui/src/themes.css with four `[data-theme="..."]` blocks per D-48.11.

    Use the EXACT hex color values from 48-UI-SPEC.md section "Color" for all four themes. The UI-SPEC defines ~30 CSS custom properties per theme in detailed tables. Each theme block must define ALL variables (no inheritance between themes — each block is self-contained per D-48.7 full re-skin).

    The ~30 CSS custom properties to define per theme (from UI-SPEC):
    --calc-bg, --calc-border, --panel-bg, --panel-header-bg, --panel-border, --display-bg, --display-text, --display-border, --key-face-top, --key-face-mid, --key-face-bot, --key-label, --key-pressed-shadow, --key-stroke, --enter-face-top, --enter-face-mid, --enter-face-bot, --shift-label, --shift-face-top, --shift-face-mid, --shift-face-bot, --shift-armed-top, --shift-armed-mid, --shift-armed-bot, --alpha-label, --alpha-label-active, --gold-trim, --error-bg, --error-text, --toast-bg, --toast-text, --accent, --overlay-bg, --text-secondary, --text-muted

    Additional variables needed for App.css migration (from PATTERNS.md color migration table):
    --annunciator-active, --annunciator-shift-active (shift annunciator color, uses --shift-label value for dark), --step-active-bg, --step-active-text

    Dark theme values MUST match current App.css hardcoded hex values exactly (visual regression = zero). Light, Classic Beige, and High Contrast use the values from 48-UI-SPEC.md Color section tables.

    Add a header comment referencing D-48.9, D-48.11. Add a P51 warning comment: "DO NOT set transform-box or transform-origin in any [data-theme] block."

    Do NOT add any `transition` properties on color values (D-48.12 instant switch).

    Four theme data-theme attribute values: "dark", "light", "classic-beige", "high-contrast".
  </action>
  <verify>
    <automated>grep -c '\[data-theme=' /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src/themes.css</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/src/themes.css exists with exactly 4 `[data-theme="..."]` blocks
    - Dark theme block contains `--calc-bg: #0d0d0d` (matching current App.css)
    - Dark theme block contains `--display-text: #c8e6c9` (matching current App.css)
    - High-contrast block contains `--display-text: #ffffff` and `--display-bg: #000000` (21:1 WCAG AAA)
    - Classic Beige block contains `--calc-bg: #c8b890` and `--display-bg: #4a5030` (from UI-SPEC)
    - No `transform-box` or `transform-origin` anywhere in themes.css (P51: `grep -c 'transform-box\|transform-origin' hp41-gui/src/themes.css` returns 0)
    - No `transition` on color properties in themes.css (D-48.12: `grep -c 'transition' hp41-gui/src/themes.css` returns 0)
    - All four themes define the complete set of ~30+ CSS variables (no missing vars that would cause unstyled elements)
  </acceptance_criteria>
  <done>themes.css contains 4 complete, self-contained theme blocks with all CSS variables defined per UI-SPEC color tables</done>
</task>

<task type="auto">
  <name>Task 2: Migrate App.css hardcoded colors to CSS variable references</name>
  <files>hp41-gui/src/App.css</files>
  <read_first>
    hp41-gui/src/App.css
    hp41-gui/src/themes.css
    .planning/phases/48-gui-infrastructure-theming/48-PATTERNS.md
    .planning/phases/48-gui-infrastructure-theming/48-UI-SPEC.md
  </read_first>
  <action>
    Replace every hardcoded hex color value in App.css that represents a theme-dependent color with its corresponding `var(--token)` reference. Use the migration table from 48-PATTERNS.md section "Hardcoded colors to migrate."

    Key migrations (not exhaustive — scan the entire file for ALL hex color values):
    - `.calculator { background: #0d0d0d; }` -> `background: var(--calc-bg);`
    - `.calculator` border color `#333` -> `var(--calc-border)`
    - `.annunciators` background `#1a1a1a` -> `var(--panel-bg)`
    - `.annunciator` color `#555` -> `var(--text-muted)`
    - `.annunciator.active` color `#e8e8c0` -> `var(--annunciator-active)`
    - `.display` background `#111` -> `var(--display-bg)`
    - `.display` color `#c8e6c9` -> `var(--display-text)`
    - `.display` border color `#222` -> `var(--display-border)`
    - All stack, print panel, program panel background/text/border colors -> corresponding vars
    - Error row colors -> `var(--error-bg)` and `var(--error-text)`
    - Toast colors -> `var(--toast-bg)` and `var(--toast-text)`
    - Help overlay background -> `var(--overlay-bg)`
    - Gold trim -> `var(--gold-trim)`
    - Accent colors (search focus ring, section headings) -> `var(--accent)`

    PRESERVE without changes:
    - `.key { transform-box: fill-box; transform-origin: center; transition: transform 80ms ease-out; }` — P51 invariant, do NOT touch
    - Structural values: margins, padding, font-size, font-family, border-radius, width, height, z-index
    - SVG-specific positioning constants
    - Any color that is purely structural (e.g., `transparent`, `inherit`)

    Also scan for shift/alpha label colors used in the SVG keyboard CSS selectors (shift label orange, alpha label blue) and migrate those to `var(--shift-label)`, `var(--alpha-label)`, `var(--alpha-label-active)`.

    Add SettingsPanel structural CSS rules at the end of App.css:
    - `.settings-gear-btn` — transparent background, no border, cursor pointer, font-size 16px, min-width/min-height 28px for click target, color `var(--text-secondary)`, hover color `var(--accent)`
    - `.settings-panel` — position absolute, z-index 70 (above help overlay at 60), background `var(--panel-bg)`, border `1px solid var(--panel-border)`, border-radius 8px, padding using spacing tokens (16px)
    - `.settings-section-heading` — font-size 13px, font-weight 700, color `var(--accent)`, margin-bottom 8px
    - `.settings-radio-row` — display flex, align-items center, gap 8px, padding 6px 0, cursor pointer, font-size 14px, color `var(--text-secondary)`
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - App.css contains `var(--calc-bg)` replacing `#0d0d0d`
    - App.css contains `var(--display-text)` replacing `#c8e6c9`
    - App.css `.key` rule still contains `transform-box: fill-box` and `transform-origin: center` (P51 intact)
    - `grep -cE '#[0-9a-fA-F]{3,8}' hp41-gui/src/App.css` count is significantly lower than before migration (most theme-dependent hex values removed)
    - `.settings-gear-btn`, `.settings-panel`, `.settings-section-heading`, `.settings-radio-row` CSS rules exist in App.css
    - `npm run build` succeeds (valid CSS syntax)
  </acceptance_criteria>
  <done>All theme-dependent hardcoded hex colors in App.css are replaced with var(--token) references; structural and animation properties are preserved unchanged; settings panel CSS rules are added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file -> browser rendering | Malformed CSS variables gracefully fall back to initial/inherited values in all browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | themes.css on disk | accept | CSS files are part of the app bundle, not user-editable at runtime; Tauri packages them into the binary |
| T-48-05 | Denial of Service | Missing CSS variable | mitigate | All four theme blocks define the complete variable set — no missing vars that would cause invisible text or broken layout |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | zero new packages installed; purely CSS file changes |
</threat_model>

<verification>
cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npm run build
grep -c '\[data-theme=' hp41-gui/src/themes.css
grep -c 'transform-box: fill-box' hp41-gui/src/App.css
grep -c 'var(--' hp41-gui/src/App.css
</verification>

<success_criteria>
- themes.css exists with 4 complete [data-theme] blocks
- App.css theme-dependent colors are all migrated to var(--token) references
- P51 invariant preserved: .key transform-box and transform-origin untouched
- D-48.12 enforced: no CSS transitions on color properties
- `npm run build` succeeds
</success_criteria>

<output>
Create `.planning/phases/48-gui-infrastructure-theming/48-02-SUMMARY.md` when done
</output>
