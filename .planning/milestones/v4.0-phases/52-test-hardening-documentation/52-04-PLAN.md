---
phase: 52-test-hardening-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/v4.0-001-xmem-os-builtin.md
  - docs/adr/v4.0-002-xmem-capacity.md
  - docs/adr/v4.0-003-xmem-register-transfer.md
  - docs/hp41-xmem-divergences.md
  - docs/architecture-history.md
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [XMEM-10]

must_haves:
  truths:
    - "The architecturally significant Phase-51 X-MEM decisions are captured as granular per-decision ADRs"
    - "The two X-MEM OM divergences (overwrite-on-duplicate, full-register-set SAVED/GETD) are documented in a dedicated divergences doc"
    - "The README X-MEM claim is scoped/honest (PROGRAM + DATA only), NOT a feature-complete hard claim"
    - "CLAUDE.md + architecture-history.md carry the v4.0 X-MEM narrative per the per-phase documentation pattern"
  artifacts:
    - path: "docs/adr/v4.0-001-xmem-os-builtin.md"
      provides: "ADR: X-MEM as HP-41CX OS built-ins, no XROM bit (D-52.7)"
      contains: "ADR-v4.0-001"
    - path: "docs/adr/v4.0-002-xmem-capacity.md"
      provides: "ADR: fixed 600-register capacity (D-52.7 / D-51.1)"
      contains: "600"
    - path: "docs/adr/v4.0-003-xmem-register-transfer.md"
      provides: "ADR: full-register-set SAVED/GETD (D-52.7 / D-51.5)"
      contains: "SAVED"
    - path: "docs/hp41-xmem-divergences.md"
      provides: "X-MEM OM divergences: D-51.6 overwrite + D-51.5 full-reg-set (D-52.8)"
      contains: "DUP FL"
    - path: "README.md"
      provides: "Scoped/honest X-MEM claim (D-52.6)"
      contains: "Extended Memory"
  key_links:
    - from: "docs/architecture-history.md"
      to: "docs/adr/v4.0-001-xmem-os-builtin.md"
      via: "v4.0 X-MEM narrative cross-reference"
      pattern: "X-MEM|Extended Memory"
---

<objective>
Write the X-MEM documentation suite: granular per-decision ADRs (D-52.7), the new divergences doc (D-52.8), the scoped/honest README claim (D-52.6), and the standard CLAUDE.md + architecture-history.md follow-through (D-52.9). This is pure-doc work with no code coupling, so it runs fully parallel in Wave 1.

CRITICAL claim discipline: the README X-MEM line stays SCOPED/HONEST — "Extended Memory: named PROGRAM + DATA file storage (HP-41CX X-Functions)". Do NOT use "feature-complete" wording; the OM-cited hard claim graduates only when ASCII/STATUS land (XMEM-F01/F02, v4.1). This is a project value (D-52.6).

Purpose: Closes the documentation half of the production-ready bar; records the architecturally significant Phase-51 choices and OM divergences before they fade.

Output: 3 ADRs, 1 divergences doc, README update, CLAUDE.md update, architecture-history.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-test-hardening-documentation/52-CONTEXT.md
@.planning/phases/52-test-hardening-documentation/52-RESEARCH.md
@.planning/phases/52-test-hardening-documentation/52-PATTERNS.md
@.planning/phases/51-x-mem-core/51-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 3 granular X-MEM ADRs (v4.0-001/002/003)</name>
  <files>docs/adr/v4.0-001-xmem-os-builtin.md, docs/adr/v4.0-002-xmem-capacity.md, docs/adr/v4.0-003-xmem-register-transfer.md</files>
  <read_first>
    - docs/adr/v3.3-001-named-matrix-storage-model.md (the exact ADR template analog — header shape: Status/Owner/Requirement refs/Downstream consumer; Context/Decision/Consequences/Alternatives Considered sections)
    - docs/adr/v3.3-003-dual-xrom-id-design.md (a second ADR sample for tone/length calibration)
    - .planning/phases/51-x-mem-core/51-CONTEXT.md (the source decisions: D-51.1 600-register capacity, D-51.5 full-register-set SAVED/GETD, the "X-MEM is OS built-in not XROM" resolution note)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (sections "docs/adr/v4.0-001-xmem-os-builtin.md" — header pattern + which decisions each ADR covers)
  </read_first>
  <action>
    Write three ADRs following the v3.3-001 template (Status: Locked 2026-05-28; Owner: Plan 52-04; Requirement refs: XMEM-10; Downstream consumer references), each with Context / Decision / Consequences / Alternatives Considered:
    - docs/adr/v4.0-001-xmem-os-builtin.md "X-MEM as HP-41CX OS Built-ins — No XROM Bit": why X-MEM resolves via builtin_card_op (not xrom_resolve / no XROM bit), since HP-41CX Extended Functions are OS built-ins; consumers builtin_card_op + help_data.rs/ts. Alternatives: allocating an XROM bit (rejected — bits 0-4 taken, X-MEM is not a plug-in ROM).
    - docs/adr/v4.0-002-xmem-capacity.md "Fixed 600-Register X-MEM Capacity" (D-51.1): 600 = fully-expanded HP-41CX (124 built-in + 2×238 X-Memory modules); EMROOM returns 600 - used; SAVEP/SAVED raise NO ROOM when over. Alternatives: 124 (too tight), unlimited (EMROOM meaningless).
    - docs/adr/v4.0-003-xmem-register-transfer.md "Full-Register-Set SAVED/GETD" (D-51.5): SAVED captures full R00..R(SIZE-1) via capture_data_card; GETD replaces state.regs wholesale via load_data_card padded to MIN_REGS_AFTER_LOAD=100. Alternatives: bbb.eee block control word (deferred — see divergences doc).
  </action>
  <verify>
    <automated>test -f docs/adr/v4.0-001-xmem-os-builtin.md && test -f docs/adr/v4.0-002-xmem-capacity.md && test -f docs/adr/v4.0-003-xmem-register-transfer.md && grep -q "ADR-v4.0-001" docs/adr/v4.0-001-xmem-os-builtin.md && grep -q "600" docs/adr/v4.0-002-xmem-capacity.md</automated>
  </verify>
  <acceptance_criteria>
    - All three ADR files exist with the v3.3-001 header shape (Status/Owner/Requirement refs/Downstream consumer)
    - Each contains Context, Decision, Consequences, and Alternatives Considered sections
    - v4.0-001 explains no-XROM-bit; v4.0-002 explains 600 capacity; v4.0-003 explains full-register-set transfer
  </acceptance_criteria>
  <done>Three granular per-decision ADRs capture the architecturally significant Phase-51 X-MEM choices (D-52.7).</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/hp41-xmem-divergences.md</name>
  <files>docs/hp41-xmem-divergences.md</files>
  <read_first>
    - docs/hp41-time-divergences.md (the structure/header analog — 5-field entry shape: OM citation / Our behavior / OM behavior / Rationale / See; the D-NN-NN entry-ID scheme at line ~38)
    - docs/hp41-advantage-divergences.md (a second divergences-doc sample for tone)
    - .planning/phases/51-x-mem-core/51-CONTEXT.md (D-51.6 overwrite-on-duplicate, D-51.5 full-register-set — the two divergences to document, with the "DUP FL"/PURFL and bbb.eee context)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "docs/hp41-xmem-divergences.md" — header + two required divergence entries)
  </read_first>
  <action>
    Create docs/hp41-xmem-divergences.md mirroring docs/hp41-time-divergences.md: title "HP-41CX Extended Memory (X-MEM) Emulator Divergences", a status line ("Established in Phase 52 / Plan 52-04"), and two divergence entries using the 5-field template (OM citation / Our behavior / OM behavior / Rationale / See). Entry 1 (D-52-01): overwrite-on-duplicate (D-51.6) — real CX raises "DUP FL" and requires PURFL first; our emulator silently overwrites in place because the Phase-51 op set has no purge/clear op (cite the OM page if known, else "N/A — documented divergence"); See ADR + D-51.6 + test. Entry 2 (D-52-02): full-register-set SAVED/GETD (D-51.5) — real CX uses the bbb.eee block control word for partial transfers; our emulator always saves/loads the full register set; See docs/adr/v4.0-003 + D-51.5. Use the D-52-NN phase-origin entry-ID scheme from the time-divergences analog.
  </action>
  <verify>
    <automated>test -f docs/hp41-xmem-divergences.md && grep -q "DUP FL" docs/hp41-xmem-divergences.md && grep -qi "bbb.eee\|block control" docs/hp41-xmem-divergences.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hp41-xmem-divergences.md` exists with two entries using the 5-field template
    - Entry 1 documents overwrite-on-duplicate vs real CX "DUP FL"/PURFL (D-51.6)
    - Entry 2 documents full-register-set SAVED/GETD vs the bbb.eee block control word (D-51.5)
    - Each entry cross-references the relevant ADR / decision ID
  </acceptance_criteria>
  <done>The two X-MEM OM divergences are documented in the dedicated divergences doc following the per-module pattern (D-52.8).</done>
</task>

<task type="auto">
  <name>Task 3: Scoped README claim + CLAUDE.md + architecture-history.md follow-through</name>
  <files>README.md, CLAUDE.md, docs/architecture-history.md</files>
  <read_first>
    - README.md (the feature list / module-claim section — find where the existing XROM modules / built-in feature claims live; mirror the honest-claim phrasing)
    - CLAUDE.md (the "XROM Module Registry" table + "v3.x Design Rules" + the project-history pointers — where to record the X-MEM integration note; X-MEM is NOT an XROM so it does NOT get a registry-table bit row)
    - docs/architecture-history.md (the narrative tail — the per-module history pattern, e.g. the v3.3 Advantage entry)
    - .planning/phases/52-test-hardening-documentation/52-CONTEXT.md (D-52.6 scoped claim wording; D-52.9 standard follow-through)
  </read_first>
  <action>
    README.md: add/adjust the X-MEM line to the SCOPED/HONEST claim — "Extended Memory: named PROGRAM + DATA file storage (HP-41CX X-Functions)". Do NOT write "feature-complete" or imply ASCII/STATUS support (deferred to v4.1 — D-52.6). Mention the 8 XEQ-by-name functions (EMDIR/EMROOM/SAVEP/GETP/SAVED/GETD/EMREG/SAVERX) if the feature list enumerates functions.
    CLAUDE.md: add a brief X-MEM integration note in the appropriate section — X-MEM is an OS built-in family resolved by builtin_card_op (XEQ-by-name only, no XROM bit, no comfort keys), with its own help pool (docs/hp41-xmem-functions.json) and matrix doc. Do NOT add an X-MEM row to the XROM Module Registry table (X-MEM is not an XROM). Cross-reference the new ADRs + divergences doc.
    docs/architecture-history.md: append a v4.0 X-MEM narrative entry following the per-module history pattern (the integration story: 8 ops from Phase 51, XEQ-by-name wiring, dedicated help pool, backward-compat + isolation hardening), cross-referencing docs/adr/v4.0-001..003 and docs/hp41-xmem-divergences.md.
  </action>
  <verify>
    <automated>grep -qi "Extended Memory" README.md && ! grep -qi "feature-complete" README.md && grep -qi "X-MEM\|Extended Memory" CLAUDE.md && grep -qi "X-MEM\|Extended Memory" docs/architecture-history.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md X-MEM line is the scoped claim (PROGRAM + DATA), NO "feature-complete" wording (D-52.6)
    - CLAUDE.md carries an X-MEM integration note and does NOT add an X-MEM row to the XROM Module Registry table
    - `docs/architecture-history.md` has a v4.0 X-MEM narrative entry cross-referencing the ADRs + divergences doc
  </acceptance_criteria>
  <done>README claim is scoped/honest; CLAUDE.md + architecture-history.md carry the standard v4.0 X-MEM follow-through (D-52.6, D-52.9).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes Markdown documentation only (ADRs, divergences doc, README, CLAUDE.md, architecture-history.md). No trust boundary is crossed — no code, no network, no auth, no runtime input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Information Disclosure | README claim accuracy (an over-broad "feature-complete" claim would misrepresent capability to users) | mitigate | D-52.6 scoped/honest claim enforced in Task 3 acceptance criteria; no hard claim until ASCII/STATUS land (v4.1) |
| T-52-SC | Tampering | npm/cargo installs | accept | No package installs (zero new deps, VERIFIED in RESEARCH.md). Package Legitimacy Gate N/A |

Per RESEARCH.md "Security Domain": no changes to authentication, sessions, access control, cryptography, or network code. No ASVS categories apply (Security domain: N/A). The only "security-adjacent" concern is claim honesty, addressed by D-52.6.
</threat_model>

<verification>
- `test -f docs/adr/v4.0-001-xmem-os-builtin.md` (and 002, 003) — ADRs exist
- `test -f docs/hp41-xmem-divergences.md` — divergences doc exists
- `grep -qi "Extended Memory" README.md` and absence of a "feature-complete" X-MEM claim
- Manual: ADRs follow the v3.3-001 section structure; divergences doc follows the time-divergences template
</verification>

<success_criteria>
- 3 granular per-decision ADRs written (D-52.7)
- Dedicated divergences doc with the 2 X-MEM divergences (D-52.8)
- README X-MEM claim scoped/honest, no feature-complete wording (D-52.6)
- CLAUDE.md + architecture-history.md carry the v4.0 X-MEM narrative; no XROM-registry row for X-MEM (D-52.9)
</success_criteria>

<output>
Create `.planning/phases/52-test-hardening-documentation/52-04-SUMMARY.md` when done
</output>
