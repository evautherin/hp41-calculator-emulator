---
phase: 52-test-hardening-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/fixtures/v33-autosave.json
  - hp41-core/tests/xmem_backward_compat.rs
autonomous: true
requirements: [XMEM-08, XMEM-09, XMEM-10]

must_haves:
  truths:
    - "XEQ \"EMDIR\", XEQ \"SAVEP\", and all 8 X-MEM mnemonics resolve via builtin_card_op (reachable from CLI + GUI + programmatic XEQ)"
    - "A v3.3 save file lacking xmem fields loads without error; xmem_files is empty and xmem_active_file is None"
    - "X-MEM ops never read/write state.regs (except SAVED/GETD transfer) and never touch adv_matrices"
    - "migrate_after_load() needs no new v3.3->v4.0 arm; #[serde(default)] IS the migration (confirmed by idempotency test)"
  artifacts:
    - path: "hp41-core/src/ops/program.rs"
      provides: "8 X-MEM mnemonic arms in builtin_card_op (D-52.4)"
      contains: "Some(Op::EmDir)"
    - path: "hp41-core/tests/fixtures/v33-autosave.json"
      provides: "v3.3 fixture with xrom_modules:31 and NO xmem fields (D-52.10)"
      contains: "\"xrom_modules\": 31"
    - path: "hp41-core/tests/xmem_backward_compat.rs"
      provides: "Backward-compat + isolation + supplementary per-op tests (D-52.10, D-52.11, D-52.12, D-52.13 prerequisite)"
      contains: "v33_save_loads_without_error"
  key_links:
    - from: "hp41-core/src/ops/program.rs"
      to: "Op::EmDir..Op::SaveRx"
      via: "builtin_card_op match arms"
      pattern: "\"EMDIR\"\\s*=>\\s*Some\\(Op::EmDir\\)"
    - from: "hp41-core/tests/xmem_backward_compat.rs"
      to: "hp41-core/tests/fixtures/v33-autosave.json"
      via: "include_str! fixture"
      pattern: "include_str!.*v33-autosave.json"
---

<objective>
Wire XEQ-by-name resolution for all 8 X-MEM ops via the single `builtin_card_op()` integration point, and prove backward-compat + isolation with a v3.3 fixture and a new core test file. The 8 arms in `builtin_card_op` simultaneously unlock CLI XEQ-by-name (`xeq_by_name_local_resolve`), GUI XEQ-by-name (`op_xeq` via the existing `xeq_<name>` strip_prefix arm), and programmatic XEQ — NO changes to keys.rs or key_map.rs are needed (D-52.4).

This plan also adds the supplementary per-op tests required BEFORE Plan 03 activates the per-op test-count floor gate (D-52.13 ordering: EmDir/EmRoom/GetD/SaveRx are currently below the ≥5 floor — Pitfall 4).

Purpose: Satisfies success criterion 1 (v3.3 loads, xmem empty), criterion 2 (isolation), and the resolution half of criterion 3 (XEQ reachable from CLI + GUI).

Output: 8 `builtin_card_op` arms, `v33-autosave.json` fixture, `xmem_backward_compat.rs` (backward-compat + isolation + supplementary tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-test-hardening-documentation/52-CONTEXT.md
@.planning/phases/52-test-hardening-documentation/52-RESEARCH.md
@.planning/phases/52-test-hardening-documentation/52-PATTERNS.md
@.planning/phases/51-x-mem-core/51-CONTEXT.md

<interfaces>
<!-- Confirmed by RESEARCH.md + codebase grep. Executor uses these directly. -->

builtin_card_op (hp41-core/src/ops/program.rs line 1368):
  pub fn builtin_card_op(name: &str) -> Option<Op>
  Current end-of-match (confirmed line 1446): `"PRSTK" => Some(Op::PRSTK),` then `_ => None,` at 1447.
  Already `pub` — tests/parity call it directly (a `__test_builtin_card_op` alias also exists, prefer the real fn).

X-MEM op fns (hp41-core/src/ops/xmem/ops.rs, all pub, reachable via hp41_core::ops::xmem::ops::*):
  op_emdir, op_emroom, op_savep, op_getp, op_saved, op_getd, op_emreg, op_saverx
  signature: pub fn op_*(state: &mut CalcState) -> Result<(), HpError>

CalcState (hp41-core/src/state.rs): xmem_files: Vec<XmemFile> (#[serde(default)], line ~432),
  xmem_active_file: Option<...> (#[serde(default)], line ~442); migrate_after_load() at line ~570
  has 4 arms (v3.0->3.1, 3.1->3.2, 3.2->3.3 bits 3+4, stopwatch freeze) — NO v3.3->v4.0 arm needed.
  GETD/SAVED set xmem_active_file as a side effect (D-51.4).

v32-autosave.json fields (the v33 template): stack, regs (100 zeros), alpha_reg, alpha_mode,
  angle_mode, display_mode, entry_buf, program, prgm_mode, pc, call_stack, is_running, user_mode,
  key_assignments, assignments, text_regs, last_key_code, reg_m/n/o, flags, xrom_modules:7,
  complex_mode, rand_seed:"0.5", matrix_dim, matrix_active_reg, time_offset_secs:3600, clock_12h:true.
  No Advantage Pac fields. v33 = identical but xrom_modules:31, still NO xmem fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 8 X-MEM arms to builtin_card_op + resolver test</name>
  <files>hp41-core/src/ops/program.rs, hp41-cli/tests/xeq_builtin_resolver.rs</files>
  <read_first>
    - hp41-core/src/ops/program.rs (builtin_card_op, lines 1368-1447 — the existing arms e.g. `"PI" => Some(Op::Pi)` and the `"PRSTK" => Some(Op::PRSTK)` / `_ => None` end-of-match)
    - hp41-cli/tests/xeq_builtin_resolver.rs (the XEQ-by-name resolver test analog — extend with X-MEM cases)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "hp41-core/src/ops/program.rs — builtin_card_op() modification" — the exact 8-arm block + insertion point)
    - .planning/phases/52-test-hardening-documentation/52-RESEARCH.md ("Pattern 1" — confirms NO keys.rs / key_map.rs changes needed; "System Architecture Diagram")
  </read_first>
  <behavior>
    - builtin_card_op("EMDIR") == Some(Op::EmDir)
    - builtin_card_op("EMROOM") == Some(Op::EmRoom)
    - builtin_card_op("SAVEP") == Some(Op::SaveP), "GETP" => GetP, "SAVED" => SaveD, "GETD" => GetD
    - builtin_card_op("EMREG") == Some(Op::EmReg), "SAVERX" => SaveRx
    - builtin_card_op of an unknown name still returns None (no regression)
  </behavior>
  <action>
    In hp41-core/src/ops/program.rs, insert 8 arms into the builtin_card_op match immediately before `_ => None` (after `"PRSTK" => Some(Op::PRSTK),` at line 1446): "EMDIR"=>Some(Op::EmDir), "EMROOM"=>Some(Op::EmRoom), "SAVEP"=>Some(Op::SaveP), "GETP"=>Some(Op::GetP), "SAVED"=>Some(Op::SaveD), "GETD"=>Some(Op::GetD), "EMREG"=>Some(Op::EmReg), "SAVERX"=>Some(Op::SaveRx). Add a comment "Phase 52 (v4.0): X-MEM built-in ops (HP-41CX Extended Functions / D-52.4)". Make NO changes to hp41-cli/src/keys.rs or hp41-gui/src-tauri/src/key_map.rs — the existing `xeq_by_name_local_resolve` (keys.rs:369) and `xeq_<name>` strip_prefix arm (key_map.rs:427) already route through builtin_card_op (anti-pattern: adding explicit X-MEM arms to key_map creates a divergent second resolution path).
    Extend hp41-cli/tests/xeq_builtin_resolver.rs (or add an inline test in program.rs) with cases asserting all 8 mnemonics resolve to the correct Op variant and that an unknown name returns None.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test xeq_builtin_resolver && cargo test -p hp41-core builtin_card_op</automated>
  </verify>
  <acceptance_criteria>
    - `builtin_card_op("EMDIR")` returns `Some(Op::EmDir)` and the analogous 7 mnemonics resolve correctly
    - `grep -c 'Some(Op::EmDir)\|Some(Op::SaveRx)' hp41-core/src/ops/program.rs` shows the new arms present
    - No edits to `hp41-cli/src/keys.rs` or `hp41-gui/src-tauri/src/key_map.rs` (git diff shows them untouched)
    - The resolver test exits 0
  </acceptance_criteria>
  <done>All 8 X-MEM mnemonics resolve via builtin_card_op; CLI + GUI + programmatic XEQ-by-name reach the ops with a single-point change.</done>
</task>

<task type="auto">
  <name>Task 2: Create v33-autosave.json backward-compat fixture</name>
  <files>hp41-core/tests/fixtures/v33-autosave.json</files>
  <read_first>
    - hp41-core/tests/fixtures/v32-autosave.json (the exact template — copy verbatim)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "hp41-core/tests/fixtures/v33-autosave.json" — change exactly `xrom_modules` 7→31, must NOT contain xmem fields)
  </read_first>
  <action>
    Copy hp41-core/tests/fixtures/v32-autosave.json to hp41-core/tests/fixtures/v33-autosave.json and change exactly ONE field: `"xrom_modules": 7` → `"xrom_modules": 31` (0b0001_1111, the v3.3 value after the Advantage Pac migration). Keep all other v32 fields verbatim (stack, 100-zero regs, rand_seed "0.5", time_offset_secs 3600, clock_12h true, etc.). The fixture MUST NOT contain `xmem_files` or `xmem_active_file` — their absence is precisely what exercises the `#[serde(default)]` path that XMEM-08 tests. Add no Advantage Pac fields (none were in v32; v3.3 added the XROM bits, not new CalcState fields beyond what already defaults).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('hp41-core/tests/fixtures/v33-autosave.json')); assert d['xrom_modules']==31, d['xrom_modules']; assert 'xmem_files' not in d; assert 'xmem_active_file' not in d; assert d['rand_seed']=='0.5'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/tests/fixtures/v33-autosave.json` parses; `xrom_modules == 31`
    - Neither `xmem_files` nor `xmem_active_file` is present (exercises serde-default path)
    - `rand_seed == "0.5"` and the v32 field set is otherwise preserved
  </acceptance_criteria>
  <done>A faithful v3.3 fixture exists that genuinely lacks the X-MEM fields, joining the v20/v30/v31/v32 family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create xmem_backward_compat.rs — backward-compat + isolation + supplementary per-op tests</name>
  <files>hp41-core/tests/xmem_backward_compat.rs</files>
  <read_first>
    - hp41-core/tests/adv_backward_compat.rs (full file — the exact backward-compat + isolation analog; imports, fixture inclusion, migrate idempotency, rand_seed guard)
    - hp41-core/src/ops/xmem/ops.rs (confirm the pub fn op_* signatures + the 15 inline tests, so the supplementary tests target real call sites)
    - hp41-core/src/state.rs (CalcState xmem_files / xmem_active_file fields + migrate_after_load() arms — confirm no v3.3->v4.0 arm exists)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "hp41-core/tests/xmem_backward_compat.rs" — the backward-compat + isolation test skeletons)
    - .planning/phases/52-test-hardening-documentation/52-RESEARCH.md ("Pattern 4", "Pattern 5", Pitfall 4 — which ops need extra mentions: EmDir, EmRoom, GetD, SaveRx)
  </read_first>
  <behavior>
    - v33 fixture deserializes without error; xmem_files empty, xmem_active_file None (XMEM-08)
    - migrate_after_load() leaves xrom_modules == 0b0001_1111 (no new v4.0 arm — D-52.12 confirmed)
    - rand_seed survives migration as Decimal 0.5 (Pitfall 20 guard)
    - XMEM ops never create adv_matrices entries (XMEM-09)
    - SAVEP/GETP/EMDIR/EMROOM/EMREG/SAVERX never modify state.regs (XMEM-09)
    - SAVED reads state.regs and GETD restores them — the sanctioned transfer (XMEM-09)
    - EmDir, EmRoom, GetD, SaveRx each reach >=5 test mentions across this file + inline ops.rs tests (D-52.13 prerequisite)
  </behavior>
  <action>
    Create hp41-core/tests/xmem_backward_compat.rs with `#![allow(clippy::unwrap_used)]` and imports (CalcState, rust_decimal::Decimal, std::str::FromStr, hp41_core::ops::xmem::ops::* for the op fns, hp41_core::num::HpNum). Backward-compat tests (mirror adv_backward_compat.rs, swap fixture var to V33_FIXTURE / include_str! v33-autosave.json): v33_save_loads_without_error, v33_save_xmem_fields_default_cleanly, v33_save_migrate_after_load_is_idempotent (assert xrom_modules == 0b0001_1111 — confirms D-52.12 no new arm), v33_save_rand_seed_preserved. Isolation tests (XMEM-09): xmem_ops_never_touch_adv_matrices (run op_savep/op_emdir/op_emroom, assert adv_matrices empty), savep_getp_do_not_touch_state_regs (set regs[0]=42, run op_savep+op_getp, assert regs[0]==42), saved_getd_transfer_is_isolated (set regs[5]=99, op_saved, clear, op_getd, assert regs[5]==99 — the sanctioned transfer). Supplementary per-op coverage (Pitfall 4 — bring EmDir/EmRoom/GetD/SaveRx to >=5 mentions): add focused tests exercising op_emdir (empty + populated EMDIR catalog into print_buffer), op_emroom (EMROOM returns 600 minus used registers), op_getd (round-trip + active-file side effect), op_saverx (store into active file register N). Use real op fn names and Op variant names in assertions so the per-op floor scan in Plan 03 counts them. NEVER use `println!` — assert on state.print_buffer for EMDIR output.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test xmem_backward_compat</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-core --test xmem_backward_compat` exits 0
    - `v33_save_loads_without_error` asserts `xmem_files.is_empty()` and `xmem_active_file.is_none()`
    - `v33_save_migrate_after_load_is_idempotent` asserts `xrom_modules == 0b0001_1111` (no v4.0 migration arm)
    - Isolation tests prove no adv_matrices mutation and no state.regs mutation outside SAVED/GETD
    - The file contains >=5 mentions each of EmDir, EmRoom, GetD, SaveRx (counting Op variant names) across its tests so the Plan 03 floor gate passes
  </acceptance_criteria>
  <done>Backward-compat (XMEM-08), isolation (XMEM-09), D-52.12 confirmation, and the per-op floor prerequisite are all proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds Rust match arms in hp41-core, a JSON test fixture, and a Rust test file. No trust boundary is crossed: no network, no auth, no access control, no runtime user input beyond the already-validated ALPHA-name convention from Phase 51. The fixture is compile-time-embedded via `include_str!`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Tampering | `builtin_card_op` resolution table (an unknown XEQ name must surface, never silently no-op — never-discard D-07) | mitigate | New arms return concrete `Some(Op::*)`; unknown names still hit `_ => None` and surface as CLI status / GUI toast (existing behavior, unchanged); resolver test asserts unknown returns None |
| T-52-03 | Tampering | Save-file deserialization (v3.3 save without xmem fields) | mitigate | `#[serde(default)]` on both fields (Phase 51) + `xmem_backward_compat.rs` proves clean load; no migration code path to exploit |
| T-52-SC | Tampering | npm/cargo installs | accept | No package installs (zero new deps, VERIFIED in RESEARCH.md). Package Legitimacy Gate N/A |

Per RESEARCH.md "Security Domain": no changes to authentication, sessions, access control, cryptography, or network code. No ASVS categories apply (Security domain: N/A).
</threat_model>

<verification>
- `cargo test -p hp41-cli --test xeq_builtin_resolver` — all 8 mnemonics resolve
- `cargo test -p hp41-core --test xmem_backward_compat` — backward-compat + isolation green
- `python3` fixture assertion — v33 fixture shape correct
- `just test-core` — no core regressions
- `just lint` — `#![deny(clippy::unwrap_used)]` clean in non-test code
</verification>

<success_criteria>
- All 8 X-MEM mnemonics resolve via `builtin_card_op`; CLI + GUI + programmatic XEQ reachable (D-52.4; success criterion 3 resolution-half)
- v3.3 save loads, `xmem_files` empty, `xmem_active_file` None (XMEM-08; success criterion 1)
- Isolation proven: no adv_matrices / state.regs mutation outside SAVED/GETD (XMEM-09; success criterion 2)
- migrate_after_load needs no new arm (D-52.12 confirmed by idempotency test)
- EmDir/EmRoom/GetD/SaveRx reach ≥5 test mentions (D-52.13 floor-gate prerequisite)
</success_criteria>

<output>
Create `.planning/phases/52-test-hardening-documentation/52-02-SUMMARY.md` when done
</output>
