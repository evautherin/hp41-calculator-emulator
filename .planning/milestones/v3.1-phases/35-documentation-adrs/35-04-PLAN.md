---
phase: 35-documentation-adrs
plan: 04
type: execute
wave: 2
depends_on:
  - 35-03
files_modified:
  - README.md
  - CLAUDE.md
  - .planning/PROJECT.md
  - docs/architecture-history.md
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - STAT-DOC-05
  - STAT-DOC-06
tags:
  - docs
  - readme
  - claude-md
  - project-md
  - architecture-history
  - milestones
  - v3-1-additions-block
must_haves:
  truths:
    - "README.md carries the D-35.3 verbatim-locked soft-claim line under `## Features`: a sibling bullet to the v3.0 soft-claim with the wording 'Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points, RAND/SEED extension, documented divergences)' plus a link to `docs/hp41-stat1-function-matrix.md` from Plan 35-01. The `## Documentation` table also gains a row for the Stat 1 Pac matrix."
    - "CLAUDE.md gains TWO edits per D-35.5: (1) a NEW `### v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` block introduced AFTER `## Frozen Invariants` (after the Free42 GPL-contamination guard sub-section ~CLAUDE.md line 119) and BEFORE `## Tech Stack` — Phase 33/34/35 sub-sections fully populated; Phase 36/37 stub-headers with (in progress) markers; (2) the `## Frozen Invariants → Core engine` section's math1/ freeze sentence gains an exception clause listing xrom.rs + modal.rs as carve-outs gated by ADR-v3.1-004 (Plan 35-03)."
    - "The hard claim 'feature-complete Stat 1 Pac behavioral emulation per OM 00041-90030' does NOT appear in README or CLAUDE.md (deferred to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11 per D-35.3)."
    - "PROJECT.md gains a v3.1 milestone progress block: Shipped sub-list gains entries for Phase 33 (2026-05-22) + Phase 34 (2026-05-23) + Phase 35 (IN PROGRESS shipped today); Current focus line updated to Phase 36 — GUI integration."
    - "docs/architecture-history.md gains a new `## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` section parallel to the existing `## v3.0 additions` section (line 117 of HEAD); sub-section-per-phase narrative; Phase 33/34/35 fully populated; Phase 36/37 stubs with `(in progress)` markers."
    - ".planning/MILESTONES.md gains a v3.1 one-liner stub (full milestone-summary entry deferred to Phase 37 `/gsd-complete-milestone` ship — D-35 carry-forward of v3.0 D-30.8 pattern)."
    - "Every link target referenced in the README v3.1 lines exists: `docs/hp41-stat1-function-matrix.md` (created in Plan 35-01) + `docs/hp41-stat1-divergences.md` (created in Plan 35-02) are both live file paths. CLAUDE.md cross-references to docs/adr/v3.1-001..005-*.md resolve (all 5 ADRs created in Plan 35-03)."
  artifacts:
    - path: "README.md"
      provides: "v3.1 soft-claim + matrix link"
      contains: "Stat 1 Pac behavioral emulation"
    - path: "CLAUDE.md"
      provides: "First-ever v3.x additions block in CLAUDE.md (v3.1 only, no v3.0 back-fill) + math1/ freeze carve-out amendment"
      contains: "### v3.1 additions (Stat 1 Pac Emulation"
    - path: ".planning/PROJECT.md"
      provides: "v3.1 milestone progress + Current focus update"
      contains: "v3.1 Stat 1 Pac"
    - path: "docs/architecture-history.md"
      provides: "v3.1 phase narrative (Phases 33-35 populated; 36-37 stub)"
      contains: "## v3.1 additions (Stat 1 Pac Emulation"
      min_lines_grown: 60
    - path: ".planning/MILESTONES.md"
      provides: "v3.1 milestone-summary stub"
      contains: "v3.1"
  key_links:
    - from: "README.md::## Features"
      to: "docs/hp41-stat1-function-matrix.md"
      via: "Inline matrix link in v3.1 soft-claim bullet"
      pattern: "hp41-stat1-function-matrix"
    - from: "README.md::## Documentation"
      to: "docs/hp41-stat1-function-matrix.md"
      via: "Documentation table row"
      pattern: "hp41-stat1-function-matrix"
    - from: "CLAUDE.md::Frozen Invariants Core engine"
      to: "docs/adr/v3.1-004-math1-freeze-second-carve-out.md"
      via: "math1/ freeze sentence exception clause"
      pattern: "v3\\.1-004-math1-freeze"
    - from: "CLAUDE.md::### v3.1 additions"
      to: "docs/adr/v3.1-001..005 + docs/hp41-stat1-divergences.md + 33-SPEC-AMENDMENT.md"
      via: "Synthesized decision-summary bullets per phase sub-section"
      pattern: "v3\\.1-00[1-5]\\|stat1-divergences\\|SPEC-AMENDMENT"
    - from: "docs/architecture-history.md::## v3.1 additions"
      to: "docs/adr/v3.1-001..005 + Phase 33-35 narrative"
      via: "Sub-section-per-phase narrative per v3.0 § additions precedent"
      pattern: "Phase 3[3-5]"
---

<objective>
Land the user-facing v3.1 narrative in README + CLAUDE.md + PROJECT.md + docs/architecture-history.md + .planning/MILESTONES.md. This is Wave 2 — depends on Plan 35-03 ADRs existing because:

1. CLAUDE.md `## Frozen Invariants → Core engine` amendment explicitly cites ADR-v3.1-004's Status: Locked 2026-05-22 line as the policy gate for the math1/ freeze carve-out clause.
2. docs/architecture-history.md v3.1 narrative cites all 5 ADRs (v3.1-001..005) by file path; those files must exist when this plan runs.
3. CLAUDE.md `### v3.1 additions` block cites the 5 ADRs in the Phase 33 / Phase 35 sub-sections.

Critical correction per D-35.5 (locked in CONTEXT.md after 35-PATTERNS.md surfaced the issue): CLAUDE.md does NOT currently have a `### v3.0 additions` block (the v3.0 narrative lives in docs/architecture-history.md). Phase 35 introduces the FIRST-EVER `### v3.x additions` block in CLAUDE.md (v3.1 only — no v3.0 back-fill). Insertion point: AFTER `## Frozen Invariants` (after the Free42 GPL-contamination guard sub-section ~line 119) and BEFORE `## Tech Stack`.

Purpose: a reader landing on README sees "Stat 1 Pac behavioral emulation included" with a link to the function matrix. A developer landing on CLAUDE.md sees the v3.1 additions block + the math1/ freeze amendment clause. A future archaeologist landing on architecture-history.md sees the v3.1 phase narrative parallel to the v3.0 one. The hard claim "feature-complete Stat 1 Pac" is deferred to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11 per D-35.3.

Output:
- README.md (modified: v3.1 feature bullet + Documentation table row)
- CLAUDE.md (modified: NEW `### v3.1 additions` block + `## Frozen Invariants → Core engine` math1/ freeze amendment)
- .planning/PROJECT.md (modified: Shipped milestones + Current focus lines)
- docs/architecture-history.md (modified: new `## v3.1 additions` section after the v3.0 section)
- .planning/MILESTONES.md (modified: v3.1 one-liner stub)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-documentation-adrs/35-CONTEXT.md
@.planning/phases/35-documentation-adrs/35-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-VERIFICATION.md
@.planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md
@.planning/milestones/v3.0-phases/30-documentation-adrs/30-03-PLAN.md
@CLAUDE.md
@README.md
@.planning/PROJECT.md
@docs/architecture-history.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
README.md current structure (insertion targets):

- Releases table at line 29-36 — leave UNCHANGED (release tag entries are added at ship time by gsd-complete-milestone, not Phase 35)
- `## Features` block starts at line 38; the v3.0 soft-claim bullet is at lines 51-52: `- v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034 ([documented divergences](docs/hp41-math1-divergences.md)) — see [Math Pac I Function Matrix](docs/hp41-math1-function-matrix.md)`
- `## Documentation` table at line 131+ with existing rows including `[Function Matrix](docs/hp41cv-function-matrix.md)` (line 135) and `[Math Pac I Function Matrix](docs/hp41-math1-function-matrix.md)` (line 136)

README v3.1 soft-claim wording (D-35.3 LOCKED — verbatim, no paraphrasing):

```
- Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points,
  RAND/SEED extension, documented divergences) — see [Stat 1 Pac Function
  Matrix](docs/hp41-stat1-function-matrix.md)
```

The soft-claim goes IMMEDIATELY AFTER the v3.0 hard-claim bullet (line 51-52 region) as a sibling list item under `## Features` → `**Calculator engine (hp41-core)**`.

CLAUDE.md current structure (per D-35.5 + 35-PATTERNS.md "Note A"):

- `## Frozen Invariants` header at line 34
- Free42 GPL-contamination guard sub-section ends at line 118 (text "bare `Free42` excluded from the pattern because legitimate cross-check references exist.")
- `## Tech Stack` header at line 120 (current line numbering)
- **Insertion point for the NEW `### v3.1 additions` block:** BETWEEN line 118 (end of Free42 GPL guard) and line 120 (`## Tech Stack` header). Insert as a NEW H3 section.
- **DO NOT** instruct the executor to "insert immediately after the ### v3.0 additions block" — that block does NOT exist in CLAUDE.md (only in docs/architecture-history.md). This is the FIRST-EVER `### v3.x additions` block in CLAUDE.md.

CLAUDE.md `## Frozen Invariants → Core engine` math1/ freeze sentence (line 56):

```
- **`hp41-core/src/ops/math1/` is frozen** since Plan 25-01. Math Pac I algorithms re-derived from HP OM 00041-90034 (1979); Free42 consulted as sanity-check oracle only, **not** copied. Every file in this directory carries the verbatim disclaim header.
```

Amendment: extend this sentence (or add a follow-up sentence) listing the v3.1 carve-outs. Per CONTEXT.md Claude's Discretion line 156 the substance is fixed:

```
**Exception (v3.1):** `xrom.rs` (D-33.3 / ADR-v3.1-004 — XROM registry for v3.1+ extension; bit-1 stub was always intended for v3.1+) and `modal.rs` (D-33.3b / ADR-v3.1-004 — ~8-line dispatch wiring for `ModalProgram::Stat1` variant; `Stat1Step` semantics live in the new `stat1/modal.rs` so no Stat 1 Pac code leaks into the frozen module). All OTHER files in `math1/` remain frozen.
```

Verbatim-locked CLAUDE.md heading (D-30.8 carry-forward + D-35.5):

```
### v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)
```

Sub-section headings (CONTEXT.md `<specifics>` lines 326-333):

```
#### Phase 33 — XROM Activation + Distribution Primitives + All Stat 1 Ops (shipped 2026-05-22)
#### Phase 34 — CLI Integration (shipped 2026-05-23)
#### Phase 35 — Documentation & ADRs (shipped 2026-05-24)
#### Phase 36 — GUI Integration (in progress)
#### Phase 37 — Test Hardening & Quality Gates (in progress)
```

(Phase 35 ship date: planner uses today's date when this plan ships; placeholder 2026-05-24 is the expected Phase 35 ship date but planner verifies against the actual ship-time date by checking `date` at execution time.)

PROJECT.md current structure:

- "Last shipped:" line at line 5
- "Status:" line at line 7 — currently reads "v3.1 Stat 1 Pac Emulation — planning phase"; UPDATE to reflect Phase 35 shipped + Phase 36 next
- `## Project History` header at line 60
- "Shipped milestones:" sub-list at line 62-73 (v1.0 through v3.0 entries); v3.0 has its own Phase 28-32 sub-list at lines 68-73
- INSERTION POINT: after line 73 (end of v3.0 sub-list), add v3.1 Stat 1 Pac block with Phase 33 / Phase 34 / Phase 35 sub-entries

docs/architecture-history.md current structure:

- `## v3.0 additions (Math Pac I Emulation, Phases 28–32)` at line 117
- v3.0 section ends at line 187 (Frozen invariants preserved across v3.0)
- `## Quality Gate History` header at line 190
- INSERTION POINT: BETWEEN line 187 and line 190 (after v3.0 additions section, before Quality Gate History)
- New section heading: `## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)`
- Same sub-section-per-phase shape as v3.0; depth mirror per CONTEXT.md Claude's Discretion line 155

MILESTONES.md current structure:

- v3.0 entry at line 232 (`## v3.0 — Math Pac I Emulation`)
- INSERTION POINT: BEFORE line 232 (since MILESTONES.md is newest-on-top OR after — planner checks file ordering at execution time). The v3.1 stub is a one-liner placeholder; full milestone entry lands at Phase 37 ship via `/gsd-complete-milestone`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v3.1 soft-claim to README.md (D-35.3 verbatim) and matrix link to Documentation table</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file at HEAD — small, ~182 lines — to see the exact `## Features` block + `**Calculator engine (hp41-core)**` sub-list shape, and the `## Documentation` table shape)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md `<specifics>` lines 296-301 — D-35.3 verbatim-locked soft-claim wording
    - .planning/milestones/v3.0-phases/30-documentation-adrs/30-03-PLAN.md Task 1 (analog README edit pattern — v3.0 soft-claim insertion that Phase 35 mirrors)
  </read_first>
  <action>
Edit README.md to add two pieces:

Insertion 1 — v3.1 soft-claim under `## Features` (sibling bullet to v3.0):

Locate the `**Calculator engine (hp41-core)**` sub-list (starts at line 41). The current LAST bullet at line 51-52 is the v3.0 hard-claim line:

```
- v3.0 ships Math Pac I behavioral emulation, feature-complete per Owner's Manual 00041-90034
  ([documented divergences](docs/hp41-math1-divergences.md)) — see [Math Pac I Function Matrix](docs/hp41-math1-function-matrix.md)
```

Add a new bullet IMMEDIATELY AFTER the v3.0 line (still inside the `**Calculator engine (hp41-core)**` sub-list, before the next bold sub-header `**Terminal UI (hp41-cli)**`). Use the D-35.3 VERBATIM-LOCKED wording — no paraphrasing:

```
- Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points,
  RAND/SEED extension, documented divergences) — see [Stat 1 Pac Function Matrix](docs/hp41-stat1-function-matrix.md)
```

The inline matrix link goes on the same bullet (per D-35.3 + CONTEXT.md Claude's Discretion line 158). The line wrap matches the surrounding bullet style (existing bullets in this section run to ~80 chars per line; the wrap puts "RAND/SEED extension, documented divergences" on the second line).

Insertion 2 — Matrix link in `## Documentation` table:

The current Documentation table at line 131+ already has rows for `Function Matrix` (line 135) and `Math Pac I Function Matrix` (line 136). Add a NEW row immediately after the Math Pac I row:

```
| [Stat 1 Pac Function Matrix](docs/hp41-stat1-function-matrix.md) | Stat 1 Pac XROM entries (module 2) with function IDs and divergences |
```

This puts the v3.1 matrix link directly alongside the v2.2 + v3.0 matrix links in the user-facing documentation index.

Do NOT touch:
- The `## Documented Divergences from HP-41 Hardware` section (line 142+) — its v2.2/v3.0 content stays unchanged; Stat 1 Pac divergences are documented in the new docs/hp41-stat1-divergences.md (Plan 35-02) which is reachable via the matrix link — no need to inline them in README.
- Any v2.x or v3.0 feature bullets — they remain accurate.
- The Releases table at line 29-36 — release entries are added at ship time by gsd-complete-milestone, NOT Phase 35 (v3.1 release tag does not exist yet).
- The `## Variants Emulated` table (lines 74-83) — Stat 1 Pac is software-emulated XROM, not a variant difference.

Do NOT add the hard claim "feature-complete Stat 1 Pac behavioral emulation per OM 00041-90030" anywhere in README — D-35.3 explicitly defers the hard claim to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11. Only the soft claim ships now.

After both insertions, run `git diff README.md | head -40` to sanity-check that only ~3-4 lines were added (the bullet + the table row).
  </action>
  <verify>
    <automated>grep -qF "Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points" README.md && grep -qF "docs/hp41-stat1-function-matrix.md" README.md && ! grep -qF "feature-complete Stat 1 Pac" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points" README.md` returns 1 match (D-35.3 verbatim soft-claim wording present)
    - `grep -cF "docs/hp41-stat1-function-matrix.md" README.md` returns at least 2 (link appears in BOTH the Features bullet AND the Documentation table)
    - `grep -cF "docs/hp41-math1-function-matrix.md" README.md` returns 2 (existing v3.0 links UNCHANGED — bullet + table)
    - `grep -cF "docs/hp41cv-function-matrix.md" README.md` returns 1 (existing v2.2 link UNCHANGED)
    - `grep -cF "feature-complete Stat 1 Pac" README.md` returns 0 (hard claim NOT present — deferred per D-35.3)
    - `grep -cF "Stat 1 Pac" README.md` returns at least 3 (soft-claim bullet + Documentation table row + matrix link description)
    - `git diff --stat README.md` shows roughly `+3 to +4 insertions` (single bullet that wraps + single table row); 0 deletions
  </acceptance_criteria>
  <done>Soft-claim bullet present under `## Features` with inline matrix link; matrix link also appears in Documentation table; hard claim deferred per D-35.3.</done>
</task>

<task type="auto">
  <name>Task 2: Insert NEW v3.1 additions block in CLAUDE.md + amend math1/ freeze sentence with carve-out clause</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (entire file at HEAD — ~188 lines — to confirm: (a) the insertion point for the NEW v3.1 additions block is between line 118 (Free42 GPL guard end) and line 120 (`## Tech Stack` header); (b) the math1/ freeze sentence at line 56; (c) CLAUDE.md does NOT currently have any `### v3.x additions` block — D-35.5 confirmed by 35-PATTERNS.md Note A)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md D-35.5 (lines 132-138) — the FIRST-EVER v3.x additions block insertion + Option B v3.1-only rationale
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md `<specifics>` lines 304-307 (verbatim H3 heading) + lines 326-333 (sub-section headings)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md Claude's Discretion line 156 (math1/ freeze amendment exact wording substance)
    - docs/adr/v3.1-004-math1-freeze-second-carve-out.md (created Plan 35-03 — confirms ADR file exists; the Status: Locked 2026-05-22 line is the gate for this amendment)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md (D-33.x decisions for Phase 33 sub-section content synthesis)
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md (D-34.x decisions for Phase 34 sub-section content synthesis)
    - docs/architecture-history.md lines 117-187 (the v3.0 additions section is the structural template for the NEW v3.1 additions block content per D-35.5 supersedes clause)
  </read_first>
  <action>
Edit CLAUDE.md with TWO insertions:

INSERTION A — math1/ freeze amendment in `## Frozen Invariants → Core engine` section (line 56):

Locate the math1/ freeze sentence at line 56 (the bullet starting "- **`hp41-core/src/ops/math1/` is frozen** since Plan 25-01."). DO NOT modify the existing sentence text. Add a follow-up sentence immediately after the existing one (within the same bullet, on a new line or as a continuation). The new clause text:

> **Exception (v3.1):** `xrom.rs` (D-33.3 / [ADR-v3.1-004](docs/adr/v3.1-004-math1-freeze-second-carve-out.md) — XROM registry for v3.1+ extension; bit-1 stub was always intended for v3.1+) and `modal.rs` (D-33.3b / ADR-v3.1-004 — ~8-line dispatch wiring for `ModalProgram::Stat1` variant; `Stat1Step` semantics live in the new `stat1/modal.rs` so no Stat 1 Pac code leaks into the frozen module). All OTHER files in `math1/` remain frozen.

The exact wording substance is locked per CONTEXT.md Claude's Discretion line 156. Planner picks final formatting (one line vs sub-bullet) — recommendation: continuation paragraph within the existing bullet (mirrors the surrounding "Origin:" italic-comment style).

INSERTION B — NEW `### v3.1 additions` block (per D-35.5 — the FIRST-EVER v3.x additions block in CLAUDE.md):

Insertion point: AFTER the Free42 GPL-contamination guard sub-section (ends at CLAUDE.md line 118 text "bare `Free42` excluded from the pattern because legitimate cross-check references exist.") and BEFORE the `## Tech Stack` header (line 120).

The new block uses the H3 heading from CONTEXT.md `<specifics>` line 305 verbatim:

```
### v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)
```

Block content — sub-sections per phase per CONTEXT.md `<specifics>` lines 326-333:

```
### v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)

#### Phase 33 — XROM Activation + Distribution Primitives + All Stat 1 Ops (shipped 2026-05-22)

- **STAT_1 XromModule (XROM ID 2) registered (STAT-FW-01):** `MATH_1.id = 7` + `STAT_1.id = 2` per hardware records; `xrom_resolve` bit-1 arm fires LAST after bit-0 (Pitfall 1 + Pitfall 22 preserved).
- **`default_xrom_modules() = 0b0000_0011` + `migrate_after_load()` (STAT-FW-02):** v3.0 save files with `xrom_modules: 1` auto-upgrade at startup; `state.rs:386-391` is the canonical migration site (D-33.7 single-source-of-truth).
- **3 hand-coded distribution primitives ([ADR-v3.1-002](docs/adr/v3.1-002-distribution-primitives-policy.md)):** `norm_cdf_inv_f64` (Acklam/Wichura AS 241), `gamma_regularized_f64` (Cody AS 239), `beta_regularized_f64` (Lentz AS 63); ~140 LOC total in `hp41-core/src/ops/stat1/distributions.rs`; statrs runtime dep REJECTED per `.planning/research/SUMMARY.md`; Free42 disclaim verbatim (Pitfall 19); `scripts/check-free42-contamination.sh` extended to 18 tokens (12 + 6 stat1-era prefixes per Phase 33 Plan 33-00 D-32.7 reassignment, STAT-QUAL-09 met).
- **OM Storage Registers transcription policy ([ADR-v3.1-003](docs/adr/v3.1-003-anova-register-layout.md)):** `hp41-core/src/ops/stat1/mod.rs` doc-comment header + ~30 named consts (STAT1_AOV_*_REG, STAT1_MLRXY_*_REG, etc.); P21 silent-wrong-answer trap mitigated before any Op was written; all ANOVA / regression / contingency table register accesses go through named consts (REVIEW WR-01 fix in `anova.rs` commit `b29aa03`).
- **`rand_seed: HpNum` on CalcState with `#[serde(default)]` WITHOUT `#[serde(skip)]` ([ADR-v3.1-001](docs/adr/v3.1-001-rng-state-placement.md)):** the ONLY v3.1 CalcState field with this serde shape (Pitfall 20 muscle-memory trap mitigated — every other v3.1 CalcState field follows the standard `skip` pattern); reproducible RNG across save/load cycles per STAT-RNG-03; field at `state.rs:201-202`; `rand_seed_serde_round_trip` test is the field-level CI guard.
- **`math1/` freeze second carve-out ([ADR-v3.1-004](docs/adr/v3.1-004-math1-freeze-second-carve-out.md)):** `xrom.rs` (D-33.3) + `modal.rs` (D-33.3b) sanctioned exceptions; `Stat1Step` enum + 5 modal variants live in NEW `stat1/modal.rs` (outside freeze); rejected ~80-line parallel `Stat1ModalProgram` alternative.
- **`ModalProgram::Stat1(Stat1Step)` enum extension ([ADR-v3.1-005](docs/adr/v3.1-005-modalprogram-stat1-enum-extension.md)):** single ModalProgram enum gains the Stat1 variant; 4-way exhaustive-match invariant items 1+2 preserved; future v3.2+ pacs inherit the pattern.
- **26 new `Op` variants** in `dispatch()` + `execute_op()` (4-way invariant items 1+2 complete; items 3+4 deferred to Phase 34 / Phase 36 per CONTEXT.md verification_context).
- **Quality gates held:** hp41-core line coverage 95.39% / region coverage 94.26% preserved (Phase 33 invariant); 1962 lib + integration tests pass; clippy -D warnings clean; Free42 contamination guard exits OK on both math1/ and stat1/ trees.

#### Phase 34 — CLI Integration (shipped 2026-05-23)

- **`docs/hp41-stat1-functions.json` authored (D-34.1):** 26-entry canonical source; 7-category convention (Stat1 Univariate / ANOVA / Regression / Hypothesis / Nonparam / Distributions / RNG); xrom block per entry per Phase 28 D-28.3 schema; surgical inline `divergences` field (RAND/SEED + ΣTSTAT + ΣPOLYP only per D-34.3) cross-referenced (not duplicated) from `docs/hp41-stat1-divergences.md`.
- **Third `OnceLock<Vec<HelpEntry>>` in `hp41-cli/src/help_data.rs`:** `STAT1_HELP_ENTRIES` static + `help_entries_stat1()` accessor + merged `help_entries_all()` 3-pool chain (cv + math1 + stat1); malformed JSON panics at first access (hard build-blocker per D-25.17 carry-forward).
- **26 new `op_display_name` arms in `hp41-cli/src/prgm_display.rs`** (4-way invariant item 3 complete; no `_ =>` catch-all). `function_matrix_parity.rs` 3-pool partition test cross-checks Op ↔ JSON bidirectional consistency.
- **`?` help overlay "Stat 1 Pac (XROM 2)" section** parallel-loads alongside "Math 1 Pac (XROM 7)"; search across all three JSON pools.
- **Modal-prompt routing reuses v3.0 infrastructure (D-34.x):** ΣPOLYP `DEGREE=?`, SEED `SEED?`, ΣCHISQD ν entry — all route through existing `print_buffer` + `modal_program` + `modal_prompt` channels; no new transient CalcState fields beyond `rand_seed` (STAT-RNG-03 / Pitfall 20).
- **`xrom_shadowing.rs` extended to STAT_1.ops:** all 26 Stat 1 mnemonics confirmed disjoint from MATH_1.ops + builtin_card_op allowlist.
- **No core/GUI changes in Phase 34:** SC-4 invariant trivially preserved; v3.0 surface unchanged.

#### Phase 35 — Documentation & ADRs (shipped 2026-05-24 — in this plan)

- **`scripts/docs-matrix` three-input extension (D-35.1 / D-30.1 carry-forward):** justfile invokes the renderer three times (cv + math1 + stat1); binary signature stays 1-in/1-out — only edit is a 4-line basename dispatch branch in `scripts/docs-matrix/src/main.rs:70-76`; hp41cv + math1 matrices bit-for-bit unchanged after regeneration (D-30.2 invariant preserved).
- **`docs/hp41-stat1-function-matrix.md` generated (26 entries):** new sibling file; carries XROM column showing `Stat 1 / 2-N` per entry; reachable via README v3.1 soft-claim link.
- **`docs/hp41-stat1-divergences.md` authored (D-35.4):** three-bucket numbered catalog (OM Divergences / Emulator Extensions / Behavioral Policies) with ≥ 11 `D-35-NN` entries in the 5-field shape (OM citation / Our behavior / OM behavior / Rationale / See); the 6 oracle drifts queued from 33-VERIFICATION.md land as D-35-01..06 (bucket 3 — Behavioral Policies); citation provenance per Pitfall 18 enforced.
- **`33-SPEC-AMENDMENT.md` history-preserving supplement (D-35.1):** sibling to 33-SPEC.md (NOT in-place edit); 6-row table mapping each oracle drift to scipy-correct value + test file:line + drift root cause + D-35-NN cross-ref. Tests are ground truth; SPEC.md preserved as planning-archaeology.
- **5 new ADRs (D-35.2):** [v3.1-001 rng-state-placement](docs/adr/v3.1-001-rng-state-placement.md) + [v3.1-002 distribution-primitives-policy](docs/adr/v3.1-002-distribution-primitives-policy.md) (Free42 disclaim verbatim per Pitfall 19) + [v3.1-003 anova-register-layout](docs/adr/v3.1-003-anova-register-layout.md) + [v3.1-004 math1-freeze-second-carve-out](docs/adr/v3.1-004-math1-freeze-second-carve-out.md) + [v3.1-005 modalprogram-stat1-enum-extension](docs/adr/v3.1-005-modalprogram-stat1-enum-extension.md); each long-form per D-30.6; `## Alternatives Considered` quotes 33-CONTEXT.md verbatim per D-30.7.
- **README v3.1 soft-claim (D-35.3):** `- Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points, RAND/SEED extension, documented divergences)` under `## Features`. Hard claim "feature-complete Stat 1 Pac" deferred to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11.
- **`## Frozen Invariants → Core engine` amendment landed in this plan:** the math1/ freeze sentence now lists `xrom.rs` + `modal.rs` as carve-outs gated by ADR-v3.1-004 (this is the amendment you're reading above).
- **`docs/architecture-history.md` v3.1 narrative:** new `## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` section parallel to the v3.0 section; Phase 33-35 populated; Phase 36-37 stubs.

#### Phase 36 — GUI Integration (in progress)

#### Phase 37 — Test Hardening & Quality Gates (in progress)

**Frozen invariants preserved across v3.1 (so far):**
- SC-4 invariant: every Phase 33–35 change respects the stricter grep — Stat 1 Pac math lives in `hp41-core/src/ops/stat1/`. The `math1/` second carve-out (xrom.rs + modal.rs) is documented per ADR-v3.1-004; no Stat 1 Pac code leaks INTO the frozen `math1/` module.
- 4-exhaustive-match invariant: every new Op variant landed in `dispatch()` + `execute_op()` (Phase 33) + CLI `prgm_display.rs` (Phase 34) before any caller could compile; GUI `prgm_display.rs` (Phase 36, in progress) closes the 4th item.
- `#![deny(clippy::unwrap_used)]` continues to apply in `hp41-core`; new test files in v3.1 carry `#[allow]` at file scope per the established pattern.
- Save-file backward compat: every new `CalcState` field in Phase 33 carries `#[serde(default)]`; transient fields use `skip`. The `rand_seed` field is the documented exception (`default` WITHOUT `skip` per STAT-RNG-03 / Pitfall 20).
- MSRV 1.88 unchanged through Phase 33-35. Zero new runtime deps (statrs rejected per ADR-v3.1-002).
```

Synthesis discipline:

The Phase 33 / Phase 34 bullet bodies above are the recommended content — they synthesize 33-CONTEXT.md / 34-CONTEXT.md decisions into terse declarative prose matching the docs/architecture-history.md v3.0 sub-section voice. The executor adjusts wording to match the surrounding CLAUDE.md voice but does NOT add new technical claims (the substance is already established by Phases 33 / 34 / 35).

For Phase 35 bullets, the content is already specific to this plan's deliverables (matrix file, divergences, ADRs, README claim, etc.) — no synthesis required.

Phase 36 / Phase 37 sub-sections are stub headers ONLY — D-30.8 carry-forward specifies "(in progress)" markers; the body fills in at each phase's ship-time. Do NOT add body content to these subsections.

Tail section: the "Frozen invariants preserved across v3.1 (so far)" block is the v3.1 analog of the v3.0 "Frozen invariants preserved" line. It explicitly notes which invariants are preserved through v3.1 so far — covers SC-4, 4-exhaustive-match, deny(unwrap_used), save-file backward compat with rand_seed exception, MSRV.

Do NOT touch:
- Any pre-existing CLAUDE.md content above line 56 (Workspace structure section) or below line 120 except the math1/ freeze amendment + the new v3.1 additions block insertion
- Quality Gates table at line 130+ — Phase 37 will update the v3.1 column when it ships
- Key Files table at line 145+ — Phase 33/34/35 file additions are deferred to a future maintenance pass (out of scope for Phase 35; the Key Files table is documentation about existing files, not a critical reference for v3.1 work)
- The Free42 GPL-contamination guard sub-section (line 114-118) — its v3.0 content stays unchanged; the v3.1 contamination-guard extension to 18 tokens is mentioned in the v3.1 additions block but does NOT modify the existing Free42 section.
  </action>
  <verify>
    <automated>grep -qF "### v3.1 additions (Stat 1 Pac Emulation, Phases 33" CLAUDE.md && grep -qF "#### Phase 33 — XROM Activation" CLAUDE.md && grep -qF "#### Phase 34 — CLI Integration" CLAUDE.md && grep -qF "#### Phase 35 — Documentation" CLAUDE.md && grep -qF "#### Phase 36" CLAUDE.md && grep -qF "(in progress)" CLAUDE.md && grep -qF "v3.1-004-math1-freeze-second-carve-out" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "### v3.1 additions (Stat 1 Pac Emulation" CLAUDE.md` returns 1 (NEW v3.1 additions block heading present)
    - `grep -c "^#### Phase 3[3-7]" CLAUDE.md` returns 5 (Phase 33 + 34 + 35 + 36 + 37 sub-section headers)
    - `grep -cF "(in progress)" CLAUDE.md` returns at least 2 (Phase 36 + Phase 37 stub markers)
    - `grep -cF "v3.1-001-rng-state-placement\|v3.1-002-distribution-primitives-policy\|v3.1-003-anova-register-layout\|v3.1-004-math1-freeze-second-carve-out\|v3.1-005-modalprogram-stat1-enum-extension" CLAUDE.md` returns at least 5 (all 5 ADRs referenced in the v3.1 additions block)
    - `grep -cF "v3.1-004-math1-freeze-second-carve-out" CLAUDE.md` returns at least 2 (ADR-v3.1-004 referenced from BOTH the math1/ freeze amendment AND the Phase 33 sub-section)
    - `grep -cF "Exception (v3.1):" CLAUDE.md` returns 1 (math1/ freeze amendment present)
    - `grep -cF "modal.rs" CLAUDE.md` returns at least 2 (carve-out clause + v3.1 block reference)
    - `grep -cF "xrom.rs" CLAUDE.md` returns at least 2 (carve-out clause + v3.1 block reference)
    - The NEW v3.1 block is positioned BETWEEN the Free42 GPL guard and Tech Stack: `awk '/### Free42 GPL-contamination guard/{f=1} /## Tech Stack/{print NR; exit} f' CLAUDE.md` shows the v3.1 block within this range
    - `grep -cE "D-33\\.[0-9]" CLAUDE.md` returns at least 5 (Phase 33 decisions referenced)
    - `grep -cE "D-34\\.[0-9]" CLAUDE.md` returns at least 2 (Phase 34 decisions referenced)
    - `grep -cE "D-35\\.[0-9]" CLAUDE.md` returns at least 5 (Phase 35 decisions referenced)
    - `wc -l CLAUDE.md` returns at least 250 (HEAD is 188 lines; the v3.1 additions block adds at least 60 lines of body)
    - `! grep -qF "feature-complete Stat 1 Pac" CLAUDE.md` succeeds (the hard claim is ABSENT; deferred to Phase 37 per D-35.3). Equivalent form: `grep -cF "feature-complete Stat 1 Pac" CLAUDE.md` returns 0.
    - `grep -cF "v3.0 Math Pac I" CLAUDE.md` returns 1 (existing v3.0 "Current" line at HEAD line 11 preserved — no v3.0 back-fill block per D-35.5 Option B)
  </acceptance_criteria>
  <done>NEW v3.1 additions block inserted between Free42 GPL guard and Tech Stack header; Phase 33/34/35 sub-sections populated; Phase 36/37 stub headers with `(in progress)` markers; math1/ freeze amendment landed citing ADR-v3.1-004.</done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md Shipped milestones and Current focus</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 1-100 (Current State + Current Milestone block + Project History block)
    - .planning/PROJECT.md lines 60-75 (the Shipped milestones sub-list; v3.0 with its Phase 28-32 entries is the structural template; v3.1 entry mirrors)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md Claude's Discretion line 154 — PROJECT.md gets concise milestone lines, NOT the full additions block
    - .planning/milestones/v3.0-phases/30-documentation-adrs/30-03-PLAN.md Task 2 (analog PROJECT.md edit pattern from v3.0)
  </read_first>
  <action>
Edit `.planning/PROJECT.md` per D-30.8 / D-35 Claude's Discretion: high-level milestone-progress lines only, NOT the full additions block (that lands in CLAUDE.md per Task 2).

Insertion 1 — Update "Last shipped" + "Status" at lines 5-7:

Currently:
```
**Last shipped:** v3.0 Math Pac I Emulation — tag `v3.0` cut 2026-05-20; bookkeeping archive 2026-05-21.

**Status:** v3.1 Stat 1 Pac Emulation — planning phase (defining requirements per `/gsd-new-milestone`, started 2026-05-21).
```

Update Status line to reflect Phase 35 shipped + Phase 36 next:
```
**Status:** v3.1 Stat 1 Pac Emulation — Phase 35 shipped (2026-05-24); Phase 36 GUI Integration next.
```

(Last shipped line stays unchanged — v3.0 is still the last TAGGED ship; v3.1 milestone tag waits for Phase 37.)

Insertion 2 — Add v3.1 entry to Shipped milestones sub-list (after the v3.0 block at lines 68-73):

Append after line 73 (the v3.0 Phase 32 line ends with "..."):

```
- v3.1 Stat 1 Pac Emulation (Phase 35 shipped 2026-05-24) — Phases 33–37, second XROM application module (13 programs, 26 XEQ entry points, RAND/SEED extension, distribution primitives, ANOVA family, multiple + polynomial regression, hypothesis tests). Phase 36 (GUI Integration) + Phase 37 (Test Hardening) IN PROGRESS — milestone tag waits for Phase 37 ship via `/gsd-complete-milestone`.
  - Phase 33 hp41-core — XROM Activation + Distribution Primitives + All Stat 1 Ops (2026-05-22) — 9 plans; 39 requirements; ~24 Op variants; ADR-v3.1-001..005 architectural locks; 95.39% line coverage / 94.26% region coverage preserved
  - Phase 34 hp41-cli — CLI Integration (2026-05-23) — 2 plans; 5 requirements; 26 op_display_name arms (4-way invariant item 3 complete); 3-pool JSON parity test; modal-prompt routing reuses v3.0 infrastructure
  - Phase 35 Documentation & ADRs (2026-05-24) — 4 plans; 6 requirements; matrix-renderer 3-input extension; 5 new ADRs (v3.1-001..005); divergence catalog three-bucket expansion; 33-SPEC-AMENDMENT.md history-preserving sibling; README v3.1 soft-claim + CLAUDE.md FIRST-EVER ### v3.x additions block (v3.1 only, no v3.0 back-fill per D-35.5)
```

Use real ship dates: Phase 33 = 2026-05-22 (from STATE.md last_activity); Phase 34 = 2026-05-23; Phase 35 = today (planner picks; expected 2026-05-24).

Insertion 3 — Update "Current Milestone" Goal block if needed:

Current Milestone block at lines 9-25 already describes v3.1 Stat 1 Pac comprehensively. The "Build sequence (provisional)" line at line 22 says "Phase numbering continues from v3.0 (starts at Phase 33)." — leave unchanged. The "Scope boundary (locked 2026-05-21)" line at line 24 — leave unchanged.

If desired, the planner may add a "Status" sub-line within the Current Milestone block noting "Phase 35 shipped 2026-05-24; Phase 36 next." Planner discretion; not required.

Do NOT touch:
- `## Requirements` section (lines 51+ if present) — Phase 33-37 requirements live in REQUIREMENTS.md; PROJECT.md tracks high-level shipped state
- `## What This Is` description (line 75+) — accurate; mentions "v2.0 will add a Tauri-based graphical desktop app" which is HISTORICAL (v2.0 shipped); leave as-is
- The collapsed v3.0 milestone scope block (`<details>` summary at line 28-56) — preserved as v3.0 archaeology
- Any other section in PROJECT.md
  </action>
  <verify>
    <automated>grep -qF "v3.1 Stat 1 Pac Emulation (Phase 35 shipped" .planning/PROJECT.md && grep -qF "Phase 33 hp41-core" .planning/PROJECT.md && grep -qF "Phase 34 hp41-cli" .planning/PROJECT.md && grep -qF "Phase 35 Documentation" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "v3.1 Stat 1 Pac Emulation" .planning/PROJECT.md` returns at least 3 (already present in Current Milestone block + new Shipped sub-list entry + Status line)
    - `grep -cF "Phase 33 hp41-core" .planning/PROJECT.md` returns 1 (Phase 33 ship line with real date)
    - `grep -cF "Phase 34 hp41-cli" .planning/PROJECT.md` returns 1 (Phase 34 ship line)
    - `grep -cF "Phase 35 Documentation" .planning/PROJECT.md` returns 1 (Phase 35 ship line)
    - `grep -cF "v3.0 Math Pac I Emulation" .planning/PROJECT.md` returns at least 1 (existing v3.0 milestone line UNCHANGED)
    - `grep -cF "v2.2 HP-41CV Feature Completeness (2026-05-15)" .planning/PROJECT.md` returns 1 (existing v2.2 milestone line UNCHANGED)
    - `grep -cF "Phase 36 GUI Integration" .planning/PROJECT.md` returns at least 1 (Status line OR Shipped sub-list "IN PROGRESS" reference)
    - `git diff --stat .planning/PROJECT.md` shows ≤ 10 line insertions and ≤ 2 line deletions (Status line replaced; v3.1 block appended)
  </acceptance_criteria>
  <done>PROJECT.md Status line updated; Shipped sub-list gains v3.1 entry with Phase 33/34/35 sub-entries.</done>
</task>

<task type="auto">
  <name>Task 4: Add docs/architecture-history.md v3.1 additions section (parallel to v3.0)</name>
  <files>docs/architecture-history.md</files>
  <read_first>
    - docs/architecture-history.md lines 117-187 (the entire `## v3.0 additions (Math Pac I Emulation, Phases 28–32)` section — this is the STRUCTURAL TEMPLATE for the new v3.1 section; per CONTEXT.md Claude's Discretion line 155 the depth mirrors directly)
    - docs/architecture-history.md lines 188-201 (Quality Gate History table — insertion point is BEFORE this section)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md Claude's Discretion line 155 — sub-section-per-phase depth + OM-citation discipline + P21/P22/P27 cross-phase trap mitigations
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md + 33-VERIFICATION.md (primary sources for Phase 33 narrative)
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md (primary source for Phase 34 narrative)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md `<domain>` block (deliverables list 1-6 — primary source for Phase 35 narrative)
  </read_first>
  <action>
Edit `docs/architecture-history.md` by inserting a new `## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` section BETWEEN line 187 (end of v3.0 additions section, ending with "MSRV 1.88 unchanged through Phase 28–32. `approx 0.5.1` is the only post-v2.2 dev-dep.") and line 190 (`## Quality Gate History` header).

The new section uses sub-section-per-phase depth matching v3.0 § additions. Each sub-section is 2-4 paragraphs of narrative (NOT a bullet list — architecture-history.md uses prose). Cite OM pages where relevant; explicitly call out cross-phase trap mitigations (P21 for ANOVA register layout, P22 for mnemonic shadowing, P27 for Free42 stats-domain guard).

Structure to author:

```
## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)

### Phase 33 — XROM Activation + Distribution Primitives + All Stat 1 Ops (shipped 2026-05-22)

[2-4 paragraphs of narrative. Topics to cover:
- The XROM framework second-module extension (STAT_1 with id=2 hardware-accurate; xrom_resolve bit-1 arm fires LAST after bit-0; Pitfall 1 + Pitfall 22 mitigation preserved)
- Three hand-coded distribution primitives (Acklam AS 241 + Cody AS 239 + Lentz AS 63; ~140 LOC in stat1/distributions.rs; statrs runtime dep REJECTED per research SUMMARY.md; ADR-v3.1-002 captures the policy; Pitfall 19 Free42 disclaim verbatim; scripts/check-free42-contamination.sh extended to 18 tokens per STAT-QUAL-09 reassignment)
- OM Storage Registers transcription policy (stat1/mod.rs doc-comment header + ~30 named consts; ADR-v3.1-003 captures the policy; P21 silent-wrong-answer-trap mitigation; spec-phase gate)
- rand_seed CalcState field with unique serde shape (default WITHOUT skip; STAT-RNG-03; ADR-v3.1-001; Pitfall 20 muscle-memory trap mitigation)
- math1/ freeze second carve-out (xrom.rs + modal.rs; ADR-v3.1-004; rejected ~80-line parallel Stat1ModalProgram alternative — ADR-v3.1-005 captures the enum-extension lock)
- 26 new Op variants; 4-way exhaustive-match invariant items 1+2 complete; 9 plans across spec-phase + framework + primitives + univariate + ANOVA + regression + hypothesis + distributions + RNG]

### Phase 34 — CLI Integration (shipped 2026-05-23)

[2-3 paragraphs covering:
- docs/hp41-stat1-functions.json (26 entries; 7-category convention per D-34.1; surgical inline divergences per D-34.3; JSON-canonical pipeline third source-of-truth extends CLAUDE.md invariant)
- Third OnceLock<Vec<HelpEntry>> in help_data.rs; help_entries_all() 3-pool chain; ? overlay "Stat 1 Pac (XROM 2)" section parallel to v3.0 Math 1 section
- 26 new op_display_name arms in hp41-cli/src/prgm_display.rs (4-way invariant item 3 complete); modal-prompt routing reuses v3.0 infrastructure (no new transient CalcState fields beyond rand_seed)
- xrom_shadowing.rs extended to STAT_1.ops; 26 mnemonics confirmed disjoint from MATH_1.ops + builtin_card_op allowlist (Pitfall 22 verified)]

### Phase 35 — Documentation & ADRs (shipped 2026-05-24)

[3-5 paragraphs covering:
- scripts/docs-matrix three-input extension (4-line basename dispatch branch; binary signature 1-in/1-out preserved per D-30.1 carry-forward; just docs-matrix-check CI gate stays green for all three pairs)
- docs/hp41-stat1-function-matrix.md (26 entries; XROM column showing Stat 1 / 2-N per row)
- docs/hp41-stat1-divergences.md (three-bucket catalog with ≥ 11 D-35-NN entries; the 6 oracle drifts queued from 33-VERIFICATION.md land as D-35-01..06 in bucket 3 — Behavioral Policies — and cross-reference 33-SPEC-AMENDMENT.md; preamble + 5-field-per-entry shape mirrors docs/hp41-math1-divergences.md per D-30.5 carry-forward)
- 33-SPEC-AMENDMENT.md history-preserving supplement (D-35.1 — sibling to 33-SPEC.md, NOT in-place edit; 6-row drift reconciliation table with scipy-correct values + test file:line + drift root cause + D-35-NN cross-refs)
- 5 new ADRs (v3.1-001 RNG state, v3.1-002 distribution primitives w/ Free42 disclaim, v3.1-003 ANOVA register layout, v3.1-004 math1/ freeze second carve-out, v3.1-005 ModalProgram::Stat1 enum extension); each long-form per D-30.6; Alternatives Considered quotes 33-CONTEXT.md verbatim per D-30.7
- README v3.1 soft-claim (D-35.3 verbatim wording; hard claim deferred to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11 — same gating discipline as v3.0 D-30.9 → D-32.5 graduation pattern)
- CLAUDE.md FIRST-EVER ### v3.x additions block (per D-35.5 the v3.0 additions block does NOT exist in CLAUDE.md; Phase 35 introduces the v3.1 block; no v3.0 back-fill) + math1/ freeze sentence amendment listing xrom.rs + modal.rs carve-outs gated by ADR-v3.1-004]

### Phase 36 — GUI Integration (in progress)

[Single placeholder paragraph: "Phase 36 ships at a future date. Subsection will be populated at Phase 36 ship-time."]

### Phase 37 — Test Hardening & Quality Gates (in progress)

[Single placeholder paragraph: "Phase 37 ships at a future date. Subsection will be populated at Phase 37 ship-time."]

**Frozen invariants preserved across v3.1 (so far):**
- SC-4 invariant: every Phase 33–35 change respects the stricter grep on `hp41-gui/src-tauri/src/op_*` ... math1/ second carve-out (xrom.rs + modal.rs) per ADR-v3.1-004 — no Stat 1 Pac code leaks INTO the frozen math1/ tree (Stat1Step semantics live in stat1/modal.rs, outside the freeze boundary).
- 4-exhaustive-match invariant: items 1+2 complete in Phase 33; item 3 closed in Phase 34; item 4 closes in Phase 36 (in progress).
- `#![deny(clippy::unwrap_used)]` continues to apply in hp41-core; v3.1 test files carry `#[allow]` at file scope per the established pattern.
- Save-file backward compat: every new CalcState field in Phase 33 carries `#[serde(default)]`; transient fields use `skip`. The rand_seed field is the documented exception (default WITHOUT skip per STAT-RNG-03 / Pitfall 20).
- MSRV 1.88 unchanged through Phase 33-35. Zero new runtime deps (statrs rejected per ADR-v3.1-002).
- Free42 GPL contamination guard: extended to 18 tokens per Phase 33 Plan 33-00 D-32.7 reassignment (STAT-QUAL-09 met); both math1/ and stat1/ trees scanned at every CI run; script exits OK.

---
```

(Final horizontal rule before the next `## Quality Gate History` section.)

After this insertion, the architecture-history.md file grows from 201 lines (HEAD) to approximately 270-290 lines.

Do NOT touch:
- Any v1.x / v2.x / v3.0 additions sections (lines 1-187) — they are historical
- The Quality Gate History table at line 190+ — Phase 37 will add the v3.1 column when it ships
- Any other section in architecture-history.md
  </action>
  <verify>
    <automated>grep -qF "## v3.1 additions (Stat 1 Pac Emulation, Phases 33" docs/architecture-history.md && grep -qF "### Phase 33 — XROM Activation" docs/architecture-history.md && grep -qF "### Phase 34 — CLI Integration" docs/architecture-history.md && grep -qF "### Phase 35 — Documentation" docs/architecture-history.md && grep -qF "(in progress)" docs/architecture-history.md && [ "$(wc -l < docs/architecture-history.md)" -ge 250 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "## v3.1 additions (Stat 1 Pac Emulation" docs/architecture-history.md` returns 1 (new section heading present)
    - `grep -c "^### Phase 3[3-7]" docs/architecture-history.md` returns 5 (Phase 33 + 34 + 35 + 36 + 37 sub-section headers)
    - `grep -cF "(in progress)" docs/architecture-history.md` returns at least 2 (Phase 36 + Phase 37 markers)
    - `grep -cF "ADR-v3.1-001\|v3.1-001-rng-state-placement\|v3.1-002\|v3.1-003\|v3.1-004\|v3.1-005" docs/architecture-history.md` returns at least 5 (all 5 ADRs cited in narrative)
    - The new section is positioned BETWEEN the v3.0 section and Quality Gate History: `awk '/## v3.0 additions/{a=NR} /## v3.1 additions/{b=NR} /## Quality Gate History/{c=NR; exit} END{print (a<b && b<c) ? "OK" : "FAIL"}' docs/architecture-history.md` outputs "OK"
    - `wc -l < docs/architecture-history.md` returns at least 250 (HEAD is 201 lines; v3.1 section adds at least 50-70 lines)
    - `grep -cF "scripts/check-free42-contamination.sh" docs/architecture-history.md` returns at least 2 (existing v3.0 references + new v3.1 mention)
    - `grep -cF "Pitfall 21\|Pitfall 22\|Pitfall 27\|P21\|P22\|P27" docs/architecture-history.md` returns at least 3 (cross-phase trap mitigations cited)
    - `grep -cF "v2.2\|v3.0" docs/architecture-history.md` returns at least 5 (existing v2.2 + v3.0 narrative UNCHANGED)
  </acceptance_criteria>
  <done>New `## v3.1 additions` section positioned between v3.0 and Quality Gate History; Phase 33-35 narrative populated with 2-5 paragraph depth; Phase 36-37 placeholder paragraphs; Frozen invariants tail summary present.</done>
</task>

<task type="auto">
  <name>Task 5: Add v3.1 milestone stub to .planning/MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (entire file — to understand the milestone-summary entry shape; v3.0 at line 232 is the closest analog)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md `<domain>` line 48 (MILESTONES.md v3.1 milestone-summary one-liner stub; full entry after Phase 37 ship via `/gsd-complete-milestone`)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md Claude's Discretion line 151 (Plan 35-04 task)
  </read_first>
  <action>
Edit `.planning/MILESTONES.md` to add a v3.1 one-liner stub. The full milestone-summary entry (parallel to v1.0..v3.0 entries) is DEFERRED to Phase 37 ship via `/gsd-complete-milestone`. Phase 35 lands only the stub placeholder.

Locate the v3.0 entry at line 232 (`## v3.0 — Math Pac I Emulation`). The MILESTONES.md file structure has milestones in chronological order. Insert the v3.1 stub AFTER the v3.0 entry block (after the v3.0 entry's "Archives" or "Known Deferred Items" sub-section closes — planner reads the v3.0 entry to find the closing point).

The v3.1 stub:

```
## v3.1 — Stat 1 Pac Emulation (IN PROGRESS)

**Status:** Phase 35 shipped 2026-05-24; Phase 36 (GUI Integration) + Phase 37 (Test Hardening & Quality Gates) IN PROGRESS. Milestone tag waits for Phase 37 ship via `/gsd-complete-milestone`.

**Scope:** Behavioral emulation of the HP-41C Stat 1 Pac (HP 00041-90030, 1979) as the second XROM application module (XROM ID 2). 13 programs / 26 XEQ entry points across univariate / ANOVA / regression / hypothesis / nonparametric / distribution / RNG families. Phases 33–37.

**Shipped Phases:** Phase 33 (2026-05-22) hp41-core XROM activation + distribution primitives + all 26 Op variants. Phase 34 (2026-05-23) hp41-cli integration: 26 op_display_name arms + 3-pool JSON help + ? overlay "Stat 1 Pac (XROM 2)" section. Phase 35 (2026-05-24) documentation & ADRs: 5 new ADRs (v3.1-001..005), divergence catalog (≥ 11 D-35-NN entries), function matrix, 33-SPEC-AMENDMENT.md, README v3.1 soft-claim, CLAUDE.md ### v3.1 additions block + math1/ freeze carve-out amendment.

**Status placeholder — full milestone-summary entry lands at Phase 37 ship.**

---
```

The exact wording is planner-author's choice; the substance is fixed (above). The stub is intentionally short — it's a placeholder pending Phase 37 `/gsd-complete-milestone`.

Do NOT touch any existing milestone entry (v1.0 through v3.0).
  </action>
  <verify>
    <automated>grep -qF "## v3.1 — Stat 1 Pac Emulation" .planning/MILESTONES.md && grep -qF "IN PROGRESS" .planning/MILESTONES.md && grep -qF "Phase 35 shipped" .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "## v3.1 — Stat 1 Pac Emulation" .planning/MILESTONES.md` returns 1 (v3.1 stub heading present)
    - `grep -cF "IN PROGRESS" .planning/MILESTONES.md` returns at least 1 (status placeholder present)
    - `grep -cF "Phase 35 shipped" .planning/MILESTONES.md` returns at least 1
    - `grep -cF "Phase 37 ship" .planning/MILESTONES.md` returns at least 1 (milestone-tag-waits placeholder)
    - `grep -cF "## v3.0 — Math Pac I Emulation" .planning/MILESTONES.md` returns 1 (existing v3.0 entry UNCHANGED)
    - `grep -cF "## v2.2 — HP-41CV Feature Completeness\|## v1.0 — HP-41 Calculator" .planning/MILESTONES.md` returns at least 2 (existing entries UNCHANGED)
    - `git diff --stat .planning/MILESTONES.md` shows roughly +8 to +12 insertions, 0 deletions (single stub block appended)
  </acceptance_criteria>
  <done>MILESTONES.md gains a v3.1 stub block (IN PROGRESS status placeholder); existing v1.0..v3.0 entries unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → users / public web | README is published in the GitHub repo; soft-claim is a public assertion about emulator capabilities. |
| CLAUDE.md → AI coding assistant | CLAUDE.md drives AI behavior on future coding sessions; the v3.1 additions block becomes the canonical reference for v3.1 decisions. |
| docs/architecture-history.md → archaeology | History document; consumed by future readers / archaeologists / new contributors. |
| .planning/PROJECT.md + .planning/MILESTONES.md → internal planning | Internal planning ledger; consumed by gsd-progress + gsd-complete-milestone tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Information Disclosure | README v3.1 soft-claim | accept | D-35.3 explicitly defers the hard claim ("feature-complete") to Phase 37 conditional on STAT-QUAL-04 + STAT-QUAL-11; the soft-claim wording is conservative and accurate. |
| T-35-13 | Tampering | CLAUDE.md v3.1 block ordering | mitigate | Insertion point is explicitly between Free42 GPL guard and `## Tech Stack` header — verified by `awk` line-range check in the acceptance criteria. The block heading is verbatim-locked by D-30.8 / D-35.5. |
| T-35-14 | Repudiation | Phase 33/34 ship dates in PROJECT.md and CLAUDE.md | mitigate | Dates are git-verifiable (`git log --before=2026-05-24 -- .planning/phases/33-*/ .planning/phases/34-*/`). Phase 35 ship date matches Plan 35-04 commit date. |
| T-35-15 | Tampering | math1/ freeze amendment in CLAUDE.md | mitigate | Amendment cites ADR-v3.1-004 (Plan 35-03) by file path; the ADR's Status: Locked 2026-05-22 line is the gate; both files committed together (Plan 35-03 + Plan 35-04 in same Phase 35 ship). |
| T-35-16 | Tampering | docs/architecture-history.md v3.1 section structural parallel with v3.0 | mitigate | The new section uses identical sub-section-per-phase shape; acceptance criterion enforces sub-section count + (in progress) markers + Phase 36/37 stub depth. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies — documentation-only plan touches README.md, CLAUDE.md, .planning/PROJECT.md, docs/architecture-history.md, .planning/MILESTONES.md. No package legitimacy gate required. |
</threat_model>

<verification>
After all five tasks complete:

1. README soft-claim: present verbatim per D-35.3; hard claim absent; matrix link reachable.
2. CLAUDE.md: new `### v3.1 additions` block positioned correctly; Phase 33/34/35 sub-sections populated; Phase 36/37 stub headers present; math1/ freeze amendment landed citing ADR-v3.1-004.
3. PROJECT.md: v3.1 milestone block reflects Phase 33 + Phase 34 + Phase 35 shipped; Status line refreshed.
4. docs/architecture-history.md: new `## v3.1 additions` section positioned between v3.0 section and Quality Gate History; Phase 33-35 narrative populated; Phase 36-37 stubs; Frozen invariants tail summary.
5. MILESTONES.md: v3.1 stub block appended; existing v1.0..v3.0 entries unchanged.
6. SC-4 invariant trivially preserved (plan touches only documentation files); MSRV unchanged; zero new dependencies.
7. All link targets resolve:
   - docs/hp41-stat1-function-matrix.md (Plan 35-01)
   - docs/hp41-stat1-divergences.md (Plan 35-02)
   - docs/adr/v3.1-001..005-*.md (Plan 35-03)
   - .planning/phases/33-.../33-SPEC-AMENDMENT.md (Plan 35-01)
</verification>

<success_criteria>
- README.md carries the D-35.3 verbatim soft-claim under `## Features` AND a matrix link in `## Documentation` table
- README.md does NOT carry any "feature-complete Stat 1 Pac" hard claim (deferred to Phase 37)
- CLAUDE.md gains the FIRST-EVER `### v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` block between Free42 GPL guard and `## Tech Stack` (per D-35.5; no v3.0 back-fill)
- CLAUDE.md gains the math1/ freeze "Exception (v3.1)" clause listing xrom.rs + modal.rs as carve-outs gated by ADR-v3.1-004
- CLAUDE.md v3.1 block has fully populated Phase 33/34/35 subsections and stub `(in progress)` markers for Phase 36/37
- PROJECT.md `## Project History` lists v3.1 with Phase 33 (2026-05-22), Phase 34 (2026-05-23), Phase 35 (today); Status line updated to "Phase 35 shipped; Phase 36 next"
- docs/architecture-history.md gains `## v3.1 additions (Stat 1 Pac Emulation, Phases 33–35 — 36–37 IN PROGRESS)` section parallel to v3.0; Phase 33-35 narrative populated 2-5 paragraphs each
- .planning/MILESTONES.md gains a v3.1 IN PROGRESS stub block; full milestone-summary entry deferred to Phase 37 ship
</success_criteria>

<output>
Create `.planning/phases/35-documentation-adrs/35-04-SUMMARY.md` when done, summarizing:
- The exact text of the README soft-claim bullet (proof it matches D-35.3 verbatim)
- The exact line number range where the CLAUDE.md v3.1 additions block sits (before/after which existing sections; confirm NO `### v3.0 additions` block was created — Option B v3.1-only per D-35.5)
- The PROJECT.md ship dates used (Phase 33 / 34 / 35) — should match STATE.md last_activity for Phase 33 + Phase 34
- The docs/architecture-history.md line count growth (`wc -l` before/after)
- The MILESTONES.md stub block content (a one-paragraph quote)
- Confirmation that no hard claim ("feature-complete Stat 1 Pac") appears in any of the five files
- Output of `git diff --stat README.md CLAUDE.md .planning/PROJECT.md docs/architecture-history.md .planning/MILESTONES.md` showing the net line additions per file
- Confirmation that all link targets from this plan resolve to live files (docs/hp41-stat1-function-matrix.md from 35-01, docs/hp41-stat1-divergences.md from 35-02, docs/adr/v3.1-001..005 from 35-03, .planning/phases/33-.../33-SPEC-AMENDMENT.md from 35-01)
</output>
