---
phase: 34-hp41-cli-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41-stat1-functions.json
  - hp41-cli/src/help_data.rs
  - hp41-cli/tests/phase34_help_data_stat1.rs
autonomous: true
requirements: [STAT-CLI-02]
requirements_addressed: [STAT-CLI-02]

must_haves:
  truths:
    - "`docs/hp41-stat1-functions.json` exists with exactly 26 entries, one per row in `hp41-core/src/ops/math1/xrom.rs::STAT_1.ops` (D-34.1)"
    - "`help_entries_stat1()` returns the parsed 26-entry slice on first call; subsequent calls return the cached slice (D-25.17 / D-29.2 hard-build-blocker pattern, third-file copy)"
    - "`help_entries_all()` chains all three pools in fixed order: built-ins → Math 1 Pac → Stat 1 Pac (D-34.6)"
    - "Every Stat 1 JSON entry's `xrom.module_id == 2` AND `xrom.module == \"Stat 1\"` AND `function_id` is dense 1..=26 (C-28.3 invariant; mirrors Math Pac I shape)"
    - "Exactly 3 entries carry surgical `divergences` notes — RAND, SEED, ΣTSTAT, ΣPOLYP (D-34.3 — RAND/SEED share their string verbatim; full catalog deferred to Phase 35)"
    - "JSON entries use 7 per-family categories per D-34.1: `Stat1 Univariate` (4), `Stat1 ANOVA` (3), `Stat1 Regression` (8), `Stat1 Hypothesis` (2), `Stat1 Nonparam` (5), `Stat1 Distributions` (2), `Stat1 RNG` (2)"
  artifacts:
    - path: "docs/hp41-stat1-functions.json"
      provides: "Canonical JSON source-of-truth for the 26 Stat 1 Pac XEQ entry points; mirrors the `hp41-math1-functions.json` schema with `xrom: { module: \"Stat 1\", module_id: 2, function_id: <1..=26> }` per entry (D-34.1, D-34.3, D-34.5, C-28.3)"
      contains: "26 unique op_variant rows; every entry carries xrom.module_id == 2 and category begins with \"Stat1 \""
    - path: "hp41-cli/src/help_data.rs"
      provides: "Third `OnceLock<Vec<HelpEntry>>` over the new JSON; `help_entries_stat1()` accessor; `help_entries_all()` extended to chain all three pools (D-29.2 third-pool extension)"
      contains: "static STAT1_HELP_ENTRIES; pub fn help_entries_stat1; help_entries_all chains three iterators"
    - path: "hp41-cli/tests/phase34_help_data_stat1.rs"
      provides: "Wave-0 smoke test mirror of `phase29_help_data_math1.rs` against `help_entries_stat1()` (D-34.1 / D-34.6 / D-34.3 verification)"
      contains: "stat1_help_entries_loads_at_runtime; stat1_help_entries_count_meets_26_target; stat1_help_entries_all_xrom_module_id_is_2; stat1_help_entries_categories_prefix_with_stat1; stat1_help_entries_divergences_are_surgical"
  key_links:
    - from: "hp41-cli/src/help_data.rs::help_entries_stat1"
      to: "docs/hp41-stat1-functions.json"
      via: "include_str! + OnceLock + serde_json::from_str + .expect(\"hp41-stat1-functions.json is malformed — fix the JSON\")"
      pattern: "include_str!\\(\"\\.\\./\\.\\./docs/hp41-stat1-functions.json\"\\)"
    - from: "hp41-cli/src/help_data.rs::help_entries_all"
      to: "help_entries_stat1"
      via: "third .chain() arm appended to the existing two-pool chain (D-34.6 fixed order)"
      pattern: "help_entries\\(\\)\\.iter\\(\\)\\.chain\\(help_entries_math1\\(\\)\\.iter\\(\\)\\)\\.chain\\(help_entries_stat1\\(\\)\\.iter\\(\\)\\)"
    - from: "docs/hp41-stat1-functions.json"
      to: "hp41-core/src/ops/math1/xrom.rs::STAT_1.ops"
      via: "bidirectional ordering parity (function_id 1..=26 matches STAT_1.ops row order; Plan 34-02 asserts both directions via function_matrix_parity.rs)"
      pattern: "\"function_id\": 1\\b.*\"function_id\": 26"
---

<objective>
Author the canonical `docs/hp41-stat1-functions.json` data file with 26 entries (one per row in `STAT_1.ops`) and wire it into `hp41-cli/src/help_data.rs` as the third `OnceLock<Vec<HelpEntry>>` pool. Extend `help_entries_all()` from a two-pool chain (built-ins + Math 1) to a three-pool chain (built-ins + Math 1 + Stat 1) per D-34.6. Add `phase34_help_data_stat1.rs` smoke test (9 assertions mirroring `phase29_help_data_math1.rs`) so the third file's hard-build-blocker, count, category prefix, xrom invariant, and surgical-divergences policy (D-34.3) are CI-gated.

Purpose: This plan **does not** close the `non-exhaustive patterns` CI break in `hp41-cli` — that closure happens in Plan 34-02 Task 1 when the 26 `op_display_name` arms land. But this plan IS a precondition for Plan 34-02 because (a) the JSON file must exist before `include_str!` can compile, (b) `function_matrix_parity.rs`'s third-pool walk in Plan 34-02 consumes `help_entries_stat1()`, and (c) Plan 34-02's right-panel and modal-flow assertions consume the merged accessor's three-pool chain. End-state for this plan: `cargo check -p hp41-cli` still fails with the pre-existing 26-arm gap (expected), but `cargo test -p hp41-cli --test phase34_help_data_stat1` is green and `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` returns `1` (the existing break, unchanged in count).

Output: 1 new JSON data file (26 entries), 1 new smoke test file (9 tests), schema-additive extension to `hp41-cli/src/help_data.rs` (third `const`, third `static OnceLock`, third accessor, three-pool chain in `help_entries_all`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md
@.planning/phases/34-hp41-cli-cli-integration/34-DISCUSSION-LOG.md
@.planning/milestones/v3.0-phases/29-cli-integration/29-01-PLAN.md
@.planning/milestones/v3.0-phases/29-cli-integration/29-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key types and signatures the executor needs from the shipped public surface. -->
<!-- Source-of-truth files: hp41-cli/src/help_data.rs (Phase 29), hp41-core/src/ops/math1/xrom.rs (Phase 33). -->

From hp41-cli/src/help_data.rs:33-80 (Phase 29 shipped — schema to mirror):
  #[derive(Debug, Clone, Deserialize)]
  #[allow(dead_code)]
  pub struct XromEntry {
      pub module: String,
      pub module_id: u8,
      pub function_id: u16,
  }
  #[derive(Debug, Clone, Deserialize)]
  pub struct HelpEntry {
      pub op_variant: String,
      pub display_name: String,
      pub category: String,
      pub status: String,
      pub phase: Option<String>,
      pub key_path: Option<String>,
      pub description: String,
      #[serde(default)] pub divergences: Vec<String>,
      #[serde(default)] pub xrom: Option<XromEntry>,
  }
  // XromEntry block is already wired (Phase 29) — Stat 1 reuses the same struct
  // verbatim; no schema extension required, only data extension.

From hp41-cli/src/help_data.rs:102-135 (Phase 29 shipped — pattern to mirror):
  const MATH1_FUNCTIONS_JSON: &str = include_str!("../../docs/hp41-math1-functions.json");
  static MATH1_HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();
  pub fn help_entries_math1() -> &'static [HelpEntry]
    // .expect("hp41-math1-functions.json is malformed — fix the JSON")
  pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry> {
      help_entries().iter().chain(help_entries_math1().iter())
  }

From hp41-core/src/ops/math1/xrom.rs:141-186 (Phase 33 shipped — authoritative ordering):
  pub const STAT_1: XromModule = XromModule {
      id: 2,
      name: "STAT 1B",
      ops: &[
          // Row 1..=4: Univariate / Moments (Stat1 Univariate)
          ("\u{03A3}BSTAT", Op::SigmaBstat),  // ΣBSTAT
          ("\u{03A3}BSTG", Op::SigmaBstg),    // ΣBSTG
          ("\u{03A3}MMTUG", Op::SigmaMmtug),  // ΣMMTUG
          ("\u{03A3}MMTGD", Op::SigmaMmtgd),  // ΣMMTGD
          // Row 5..=7: ANOVA Family (Stat1 ANOVA)
          ("\u{03A3}AOVONE", Op::SigmaAovone), // ΣAOVONE
          ("\u{03A3}AOVTWO", Op::SigmaAovtwo), // ΣAOVTWO
          ("\u{03A3}ANOCOV", Op::SigmaAnocov), // ΣANOCOV
          // Row 8..=15: Curve Fitting + Regression (Stat1 Regression)
          ("\u{03A3}LIN", Op::SigmaLin),       // ΣLIN
          ("\u{03A3}EXP", Op::SigmaExp),       // ΣEXP
          ("\u{03A3}LOGI", Op::SigmaLogi),     // ΣLOGI
          ("\u{03A3}POW", Op::SigmaPow),       // ΣPOW
          ("\u{03A3}MLRXY", Op::SigmaMlrxy),   // ΣMLRXY
          ("\u{03A3}MLRXYZ", Op::SigmaMlrxyz), // ΣMLRXYZ
          ("\u{03A3}POLYP", Op::SigmaPolypWorkflow), // ΣPOLYP
          ("\u{03A3}POLYC", Op::SigmaPolyc),   // ΣPOLYC
          // Row 16..=17: Hypothesis (Stat1 Hypothesis)
          ("\u{03A3}PTST", Op::SigmaPtst),     // ΣPTST
          ("\u{03A3}TSTAT", Op::SigmaTstat),   // ΣTSTAT
          // Row 18..=22: Nonparam / Chi-Sq Eval / Contingency (Stat1 Nonparam)
          ("\u{03A3}XSQEV", Op::SigmaXsqev),   // ΣXSQEV
          ("\u{03A3}EFXSQ", Op::SigmaEfxsq),   // ΣEFXSQ
          ("\u{03A3}CTKKK", Op::SigmaCtkkk),   // ΣCTKKK
          ("\u{03A3}CTKK", Op::SigmaCtkk),     // ΣCTKK
          ("\u{03A3}SPEAR", Op::SigmaSpear),   // ΣSPEAR
          // Row 23..=24: Distributions (Stat1 Distributions)
          ("\u{03A3}NORMD", Op::SigmaNormdWorkflow), // ΣNORMD
          ("\u{03A3}CHISQD", Op::SigmaChisqdWorkflow), // ΣCHISQD
          // Row 25..=26: RNG (Stat1 RNG)
          ("RAND", Op::Rand),                  // RAND
          ("SEED", Op::Seed),                  // SEED
      ],
  };

From docs/hp41-math1-functions.json (entry shape — direct schema mirror):
  {
      "op_variant": "Sinh",
      "display_name": "SINH",
      "category": "Math1 Hyperbolics",
      "status": "implemented",
      "phase": "28",
      "key_path": "XEQ \"SINH\"",
      "description": "Hyperbolic sine: X <- sinh(X)",
      "xrom": { "module": "Math 1", "module_id": 7, "function_id": 1 }
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author docs/hp41-stat1-functions.json with 26 entries (D-34.1 / D-34.3 / D-34.5 / C-28.3)</name>
  <files>docs/hp41-stat1-functions.json</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md (decisions D-34.1, D-34.3, D-34.5, C-28.3; the 26-row STAT_1.ops listing in §"In scope" item 3; the specifics §"JSON `xrom` block content per entry" with function_id mapping)
    - .planning/phases/34-hp41-cli-cli-integration/34-DISCUSSION-LOG.md (selected options for category granularity + divergences policy)
    - .planning/milestones/v3.0-phases/29-cli-integration/29-01-PLAN.md (the precedent — Task 1's per-program category list at lines 154–164 is the structural template)
    - docs/hp41-math1-functions.json (entire file — 45-entry schema mirror; copy structure exactly)
    - hp41-core/src/ops/math1/xrom.rs:141-186 (authoritative row order — function_id 1..=26 mirrors STAT_1.ops sequence)
    - hp41-core/src/ops/stat1/modal.rs:333-343 (prompt strings — used as cross-reference for description wording, NOT copied into description because description is for the `?` overlay row, not the modal prompt)
  </read_first>
  <action>
    Create `docs/hp41-stat1-functions.json` as a JSON array of exactly 26 entries. The schema for each entry mirrors `docs/hp41-math1-functions.json` shape verbatim (no schema changes — D-29.2 / Phase 29's `HelpEntry` already supports `xrom: Option<XromEntry>` via `#[serde(default)]`).

    **Mandatory per-entry shape:**
    - `op_variant`: PascalCase Rust enum name matching `Op::*` exactly (e.g. `"SigmaBstat"`, `"SigmaNormdWorkflow"`, `"Rand"`, `"Seed"` — note `Workflow` suffix on ΣNORMD/ΣCHISQD/ΣPOLYP per CONTEXT §"Specific Ideas")
    - `display_name`: HP-41 mnemonic exactly as in `STAT_1.ops` — Unicode Σ encoded as `\u{03A3}` (the JSON-level escape becomes literal `Σ` after parsing). Use the literal escape sequence `Σ` (NOT raw `Σ`) for cross-platform consistency with `hp41-math1-functions.json` precedent.
    - `category`: one of 7 strings per D-34.1 (see entry-by-entry table below)
    - `status`: `"implemented"` for all 26 entries (Phase 33 shipped everything)
    - `phase`: `"33"` for all 26 entries (Phase 33 delivered the Op variants)
    - `key_path`: `"XEQ \"<display_name>\""` form per D-28.6 (e.g. `"XEQ \"ΣBSTAT\""`, `"XEQ \"RAND\""`)
    - `description`: ≤ 80 chars, recommended phrasing starts with primary purpose (suggested wording in entry table below; planner may refine to fit ≤ 80 chars)
    - `divergences`: ARRAY field. For 22 entries OMIT the key entirely (defaults to `[]` via `#[serde(default)]`). For 4 entries populate per D-34.3 (RAND, SEED, ΣTSTAT, ΣPOLYP — exact strings below).
    - `xrom`: `{ "module": "Stat 1", "module_id": 2, "function_id": <1..=26> }` — 1-indexed, dense, matches STAT_1.ops row order

    **Entry table — copy verbatim including function_id mapping:**

    1. `op_variant="SigmaBstat"`, `display_name="ΣBSTAT"`, `category="Stat1 Univariate"`, `key_path="XEQ \"ΣBSTAT\""`, `description="Extended univariate summary (weighted mean, CV)"`, `function_id=1`
    2. `op_variant="SigmaBstg"`, `display_name="ΣBSTG"`, `category="Stat1 Univariate"`, `key_path="XEQ \"ΣBSTG\""`, `description="Grouped-data univariate summary"`, `function_id=2`
    3. `op_variant="SigmaMmtug"`, `display_name="ΣMMTUG"`, `category="Stat1 Univariate"`, `key_path="XEQ \"ΣMMTUG\""`, `description="Moments — ungrouped data (mean, variance, skew, kurtosis)"`, `function_id=3`
    4. `op_variant="SigmaMmtgd"`, `display_name="ΣMMTGD"`, `category="Stat1 Univariate"`, `key_path="XEQ \"ΣMMTGD\""`, `description="Moments — grouped data (mean, variance, skew, kurtosis)"`, `function_id=4`
    5. `op_variant="SigmaAovone"`, `display_name="ΣAOVONE"`, `category="Stat1 ANOVA"`, `key_path="XEQ \"ΣAOVONE\""`, `description="One-way ANOVA: F-ratio across multiple groups"`, `function_id=5`
    6. `op_variant="SigmaAovtwo"`, `display_name="ΣAOVTWO"`, `category="Stat1 ANOVA"`, `key_path="XEQ \"ΣAOVTWO\""`, `description="Two-way ANOVA: two-factor analysis of variance"`, `function_id=6`
    7. `op_variant="SigmaAnocov"`, `display_name="ΣANOCOV"`, `category="Stat1 ANOVA"`, `key_path="XEQ \"ΣANOCOV\""`, `description="Analysis of covariance (single covariate)"`, `function_id=7`
    8. `op_variant="SigmaLin"`, `display_name="ΣLIN"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣLIN\""`, `description="Linear curve fit: y = a + b·x"`, `function_id=8`
    9. `op_variant="SigmaExp"`, `display_name="ΣEXP"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣEXP\""`, `description="Exponential curve fit: y = a·e^(b·x)"`, `function_id=9`
    10. `op_variant="SigmaLogi"`, `display_name="ΣLOGI"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣLOGI\""`, `description="Logarithmic curve fit: y = a + b·ln(x)"`, `function_id=10`
    11. `op_variant="SigmaPow"`, `display_name="ΣPOW"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣPOW\""`, `description="Power curve fit: y = a·x^b"`, `function_id=11`
    12. `op_variant="SigmaMlrxy"`, `display_name="ΣMLRXY"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣMLRXY\""`, `description="Multiple linear regression — two predictors (Y on X)"`, `function_id=12`
    13. `op_variant="SigmaMlrxyz"`, `display_name="ΣMLRXYZ"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣMLRXYZ\""`, `description="Multiple linear regression — three predictors (Y on X,Z)"`, `function_id=13`
    14. `op_variant="SigmaPolypWorkflow"`, `display_name="ΣPOLYP"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣPOLYP\""`, `description="Polynomial regression — accepts degree via modal prompt"`, `function_id=14`, **DIVERGENCE per D-34.3 (verbatim):** `"DEGREE=? modal prompt is HP-41 emulator convention transcribed from Math Pac I POLY precedent (OM 00041-90030 does not specify the degree-entry interaction)."`
    15. `op_variant="SigmaPolyc"`, `display_name="ΣPOLYC"`, `category="Stat1 Regression"`, `key_path="XEQ \"ΣPOLYC\""`, `description="Polynomial coefficient recall after ΣPOLYP fit"`, `function_id=15`
    16. `op_variant="SigmaPtst"`, `display_name="ΣPTST"`, `category="Stat1 Hypothesis"`, `key_path="XEQ \"ΣPTST\""`, `description="Paired-sample t-test (within-subject differences)"`, `function_id=16`
    17. `op_variant="SigmaTstat"`, `display_name="ΣTSTAT"`, `category="Stat1 Hypothesis"`, `key_path="XEQ \"ΣTSTAT\""`, `description="Two-sample t-test (pooled-variance assumption)"`, `function_id=17`, **DIVERGENCE per D-34.3 (verbatim):** `"Pooled-variance two-sample t-test only; Welch's unequal-variance variant is explicitly excluded per REQUIREMENTS.md Out-of-Scope (Phase 33 SPEC.md Req. 25)."`
    18. `op_variant="SigmaXsqev"`, `display_name="ΣXSQEV"`, `category="Stat1 Nonparam"`, `key_path="XEQ \"ΣXSQEV\""`, `description="Chi-square goodness-of-fit evaluation"`, `function_id=18`
    19. `op_variant="SigmaEfxsq"`, `display_name="ΣEFXSQ"`, `category="Stat1 Nonparam"`, `key_path="XEQ \"ΣEFXSQ\""`, `description="Chi-square with expected-frequency input"`, `function_id=19`
    20. `op_variant="SigmaCtkkk"`, `display_name="ΣCTKKK"`, `category="Stat1 Nonparam"`, `key_path="XEQ \"ΣCTKKK\""`, `description="Contingency table — Kendall's tau-c statistic"`, `function_id=20`
    21. `op_variant="SigmaCtkk"`, `display_name="ΣCTKK"`, `category="Stat1 Nonparam"`, `key_path="XEQ \"ΣCTKK\""`, `description="Contingency table — Kendall's tau-b (corrected)"`, `function_id=21`
    22. `op_variant="SigmaSpear"`, `display_name="ΣSPEAR"`, `category="Stat1 Nonparam"`, `key_path="XEQ \"ΣSPEAR\""`, `description="Spearman rank correlation coefficient"`, `function_id=22`
    23. `op_variant="SigmaNormdWorkflow"`, `display_name="ΣNORMD"`, `category="Stat1 Distributions"`, `key_path="XEQ \"ΣNORMD\""`, `description="Standard normal CDF / PDF / inverse (probit) — three-mode dispatcher"`, `function_id=23`
    24. `op_variant="SigmaChisqdWorkflow"`, `display_name="ΣCHISQD"`, `category="Stat1 Distributions"`, `key_path="XEQ \"ΣCHISQD\""`, `description="Chi-square distribution CDF / PDF (prompts for ν)"`, `function_id=24`
    25. `op_variant="Rand"`, `display_name="RAND"`, `category="Stat1 RNG"`, `key_path="XEQ \"RAND\""`, `description="Next pseudorandom uniform on [0,1) via Don Malm LCG"`, `function_id=25`, **DIVERGENCE per D-34.3 (verbatim):** `"v3.1 emulator extension — not part of OM 00041-90030 'feature-complete' claim. LCG formula r_{n+1} = FRC(9821*r_n + 0.211327) per NPS p. 21 / Don Malm community convention."`
    26. `op_variant="Seed"`, `display_name="SEED"`, `category="Stat1 RNG"`, `key_path="XEQ \"SEED\""`, `description="Seed the RAND generator from X (modal prompt 'SEED?')"`, `function_id=26`, **DIVERGENCE per D-34.3 (verbatim):** `"v3.1 emulator extension — not part of OM 00041-90030 'feature-complete' claim. LCG formula r_{n+1} = FRC(9821*r_n + 0.211327) per NPS p. 21 / Don Malm community convention."`

    **Formatting rules:**
    - One entry per JSON object, separated by commas, top-level array.
    - Use 4-space indentation matching `hp41-math1-functions.json`.
    - The `divergences` key is OMITTED for the 22 plain entries (NOT written as `"divergences": []` — let `#[serde(default)]` synthesize the empty Vec).
    - For the 4 divergent entries, write `"divergences": ["<verbatim string from D-34.3>"]` (single-element string array).
    - Σ encoded as the JSON escape `Σ`; ν encoded as `ν` (only needed inside the ΣCHISQD description if you choose to use it — the recommended `description` above already does).
    - File MUST parse as valid JSON: `python3 -c "import json; json.load(open('docs/hp41-stat1-functions.json'))"` exits 0.

    Per D-34.1 (7-category mapping), D-34.3 (surgical divergences for RAND, SEED, ΣTSTAT, ΣPOLYP only), D-34.5 (the section header in `?` overlay derives from category prefix `"Stat1 "` + a label-rewriter the executor does NOT need to implement here — Plan 34-02 handles the label rewrite), C-28.3 (every entry must carry the xrom block with module_id=2), D-28.6 (XEQ-by-name only — key_path always `"XEQ \"<name>\""`).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); assert len(d) == 26, f'expected 26, got {len(d)}'; assert all(e['xrom']['module_id'] == 2 for e in d), 'module_id mismatch'; assert sorted(e['xrom']['function_id'] for e in d) == list(range(1, 27)), 'function_id not dense 1..=26'; assert sum(1 for e in d if e.get('divergences')) == 4, 'expected exactly 4 entries with divergences'; print('OK 26 entries, dense function_ids, 4 divergent entries')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is non-empty: `test -s docs/hp41-stat1-functions.json` exits 0
    - JSON parses: `python3 -c "import json; json.load(open('docs/hp41-stat1-functions.json'))"` exits 0
    - Exactly 26 entries: `python3 -c "import json; print(len(json.load(open('docs/hp41-stat1-functions.json'))))"` outputs `26`
    - Every entry has `xrom.module_id == 2`: `grep -c '"module_id": 2' docs/hp41-stat1-functions.json` outputs `26`
    - Every entry has `xrom.module == "Stat 1"`: `grep -c '"module": "Stat 1"' docs/hp41-stat1-functions.json` outputs `26`
    - Every entry has `category` beginning with `"Stat1 "`: `grep -c '"category": "Stat1 ' docs/hp41-stat1-functions.json` outputs `26`
    - Every entry has `status: "implemented"`: `grep -c '"status": "implemented"' docs/hp41-stat1-functions.json` outputs `26`
    - Every entry has `phase: "33"`: `grep -c '"phase": "33"' docs/hp41-stat1-functions.json` outputs `26`
    - All 26 expected `op_variant` names present (one each): for each of `SigmaBstat`, `SigmaBstg`, `SigmaMmtug`, `SigmaMmtgd`, `SigmaAovone`, `SigmaAovtwo`, `SigmaAnocov`, `SigmaLin`, `SigmaExp`, `SigmaLogi`, `SigmaPow`, `SigmaMlrxy`, `SigmaMlrxyz`, `SigmaPolypWorkflow`, `SigmaPolyc`, `SigmaPtst`, `SigmaTstat`, `SigmaXsqev`, `SigmaEfxsq`, `SigmaCtkkk`, `SigmaCtkk`, `SigmaSpear`, `SigmaNormdWorkflow`, `SigmaChisqdWorkflow`, `Rand`, `Seed` — `grep -c '"op_variant": "<NAME>"' docs/hp41-stat1-functions.json` outputs `1`
    - Exactly 4 entries carry a `divergences` block: `python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); print(sum(1 for e in d if e.get('divergences')))"` outputs `4`
    - RAND and SEED carry the verbatim D-34.3 string: `python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); rand=[e for e in d if e['op_variant']=='Rand'][0]; seed=[e for e in d if e['op_variant']=='Seed'][0]; assert rand['divergences'] == seed['divergences'], 'RAND and SEED must share verbatim divergence string'; assert 'Don Malm' in rand['divergences'][0], 'D-34.3 string must mention Don Malm'; print('OK')"` exits 0
    - ΣTSTAT divergence references "Welch": `python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); t=[e for e in d if e['op_variant']=='SigmaTstat'][0]; assert 'Welch' in t['divergences'][0]; print('OK')"` exits 0
    - ΣPOLYP divergence references "DEGREE=?": `python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); p=[e for e in d if e['op_variant']=='SigmaPolypWorkflow'][0]; assert 'DEGREE=?' in p['divergences'][0]; print('OK')"` exits 0
    - Function IDs are dense 1..=26 (no gaps, no duplicates): `python3 -c "import json; d=json.load(open('docs/hp41-stat1-functions.json')); ids=sorted(e['xrom']['function_id'] for e in d); assert ids == list(range(1, 27)); print('OK')"` exits 0
    - Every `key_path` matches `XEQ "<name>"` pattern: `python3 -c "import json,re; d=json.load(open('docs/hp41-stat1-functions.json')); bad=[e['op_variant'] for e in d if not re.match(r'^XEQ \"[^\"]+\"$', e['key_path'])]; assert not bad, bad; print('OK')"` exits 0
    - 7 distinct categories, with exact counts: `python3 -c "import json; from collections import Counter; d=json.load(open('docs/hp41-stat1-functions.json')); c=Counter(e['category'] for e in d); assert c == {'Stat1 Univariate': 4, 'Stat1 ANOVA': 3, 'Stat1 Regression': 8, 'Stat1 Hypothesis': 2, 'Stat1 Nonparam': 5, 'Stat1 Distributions': 2, 'Stat1 RNG': 2}, c; print('OK')"` exits 0
  </acceptance_criteria>
  <done>JSON file with 26 entries authored; all schema invariants hold (xrom.module_id=2, dense function_ids 1..=26, 7 categories per D-34.1, exactly 4 surgical divergences per D-34.3); file is parseable JSON; ready for `include_str!` in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire third OnceLock + help_entries_stat1() accessor + extend help_entries_all() to three-pool chain</name>
  <files>hp41-cli/src/help_data.rs, hp41-cli/tests/phase34_help_data_stat1.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md (decisions D-34.5, D-34.6; reusable assets §"`hp41-cli/src/help_data.rs`"; established patterns §"JSON canonical pipeline (D-25.16/17/18)")
    - .planning/milestones/v3.0-phases/29-cli-integration/29-01-PLAN.md Task 1 (the precedent — Phase 29's identical pattern, swap MATH1→STAT1)
    - hp41-cli/src/help_data.rs (entire file — lines 1–135 are the pattern to mirror; the third pool slots in immediately after the MATH1 pool block at lines 102–135)
    - hp41-cli/tests/phase29_help_data_math1.rs (entire file — direct structural template; swap MATH1→STAT1, count 45→26, module_id 7→2, category prefix `"Math1 "`→`"Stat1 "`)
    - docs/hp41-stat1-functions.json (created in Task 1 — must exist before `include_str!` compiles)
  </read_first>
  <behavior>
    - Test 1 (`stat1_help_entries_loads_at_runtime`): `help_entries_stat1()` returns non-empty slice on first call; hard-build-blocker fires on malformed JSON per D-25.17 third-file copy.
    - Test 2 (`stat1_help_entries_count_meets_26_target`): `help_entries_stat1().len() == 26` — exact match (no `>=` because Stat 1 Pac is feature-frozen for v3.1).
    - Test 3 (`stat1_help_entries_has_no_duplicate_op_variants`): HashSet insertion confirms unique op_variant strings across the 26 entries.
    - Test 4 (`stat1_help_entries_all_have_non_empty_description`): every entry's `description` is non-empty and ≤ 200 chars (matches Math Pac I smoke test bound).
    - Test 5 (`stat1_help_entries_status_is_closed_enum`): every status ∈ {"implemented", "deferred-v3", "na"} (Phase 34 entries all `"implemented"`).
    - Test 6 (`stat1_help_entries_all_xrom_module_id_is_2`): every entry's `xrom.module_id == 2` (C-28.3 invariant; Stat 1 hardware ID per Phase 33 D-33.1).
    - Test 7 (`stat1_help_entries_categories_prefix_with_stat1`): every category starts with `"Stat1 "` (D-34.1 per-family naming).
    - Test 8 (`stat1_help_entries_xrom_function_ids_are_dense`): function_ids form `1..=26` with no gaps and no duplicates.
    - Test 9 (`stat1_help_entries_all_key_path_is_xeq_form`): every entry has `key_path == Some("XEQ \"<MNEMONIC>\"")` per D-28.6.
    - Test 10 (`stat1_help_entries_divergences_are_surgical`): exactly 4 entries carry a non-empty `divergences` Vec, and they ARE `Rand`, `Seed`, `SigmaTstat`, `SigmaPolypWorkflow` (D-34.3 per-entry policy enforcement — drift catch).
    - Test 11 (`help_entries_all_returns_three_pools`): `help_entries_all().count() >= 130 + 45 + 26` (the three pools chained — v2.2 baseline + Math 1 + Stat 1).
    - Test 12 (`stat1_help_entries_no_collision_with_other_pools`): every Stat 1 `op_variant` is absent from `help_entries()` AND from `help_entries_math1()` (cross-pool uniqueness gate — drift catch for accidentally inheriting a Math 1 mnemonic).
  </behavior>
  <action>
    1. Append a third `OnceLock` block to `hp41-cli/src/help_data.rs` immediately after the existing Math 1 block (after line 135 — the `help_entries_all()` definition becomes the place where the third `.chain()` slots in). Mirror the Phase 29 D-29.2 / D-25.17 pattern verbatim:

       - Add `const STAT1_FUNCTIONS_JSON: &str = include_str!("../../docs/hp41-stat1-functions.json");`
       - Add `static STAT1_HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();`
       - Add `pub fn help_entries_stat1() -> &'static [HelpEntry] { STAT1_HELP_ENTRIES.get_or_init(|| serde_json::from_str(STAT1_FUNCTIONS_JSON).expect("hp41-stat1-functions.json is malformed — fix the JSON")) }`. The panic message MUST be the literal string `"hp41-stat1-functions.json is malformed — fix the JSON"` (distinct from v2.2 and Math 1 wordings — the per-file panic message is what makes routing failures surgical per D-25.17).
       - Doc-comment the accessor as a third-pool narrow accessor, mirroring the v3.0 Math 1 doc-comment voice. Note that `help_entries_all` is the production consumer; this narrow accessor exists for the per-file smoke test ONLY.

    2. **Extend** `help_entries_all()` from the existing two-pool `.chain` to a three-pool `.chain` per D-34.6 (fixed insertion order built-ins → Math 1 → Stat 1):

       Current (lines 133–135):
       ```
       pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry> {
           help_entries().iter().chain(help_entries_math1().iter())
       }
       ```

       After this task:
       ```
       pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry> {
           help_entries().iter()
               .chain(help_entries_math1().iter())
               .chain(help_entries_stat1().iter())
       }
       ```

       Update the doc-comment above `help_entries_all` to reference three pools (the comment block currently at lines 124–132 says "chains both JSON pools (v2.2 built-ins + Math Pac I) in order" — change to "chains all three JSON pools (v2.2 built-ins + Math Pac I + Stat 1 Pac) in order" and add Stat 1 to the narrow-accessors list in the body of the comment). Reference D-34.6 in the doc-comment so future contributors learn why the order is fixed and not e.g. alphabetical.

    3. Create `hp41-cli/tests/phase34_help_data_stat1.rs` mirroring `hp41-cli/tests/phase29_help_data_math1.rs` verbatim (header comment, `#![allow(clippy::unwrap_used)]`, `use hp41_cli::help_data::{help_entries, help_entries_all, help_entries_math1, help_entries_stat1};`). Implement the 12 tests listed in `<behavior>`. Each test name prefixed `stat1_`. Per-test failure message includes the literal filename `hp41-stat1-functions.json` so failures are routable to the correct source-of-truth file.

       Special test bodies that deviate from the Math 1 template:

       - **`stat1_help_entries_count_meets_26_target`** uses `assert_eq!(entries.len(), 26)` (exact, NOT `>=`) — Stat 1 Pac is feature-frozen per CONTEXT §"Specific Ideas" smoke-test-entry-count-assertions: "If a future Stat 1 Pac entry is added, the test fails loudly".

       - **`stat1_help_entries_divergences_are_surgical`** is new (no Math 1 analog because Phase 29 chose to omit divergences entirely). Body:
         ```
         use std::collections::HashSet;
         let entries = help_entries_stat1();
         let with_div: HashSet<&str> = entries.iter()
             .filter(|e| !e.divergences.is_empty())
             .map(|e| e.op_variant.as_str())
             .collect();
         let expected: HashSet<&str> = ["Rand", "Seed", "SigmaTstat", "SigmaPolypWorkflow"]
             .iter().copied().collect();
         assert_eq!(
             with_div, expected,
             "D-34.3: divergences must be populated ONLY for Rand, Seed, SigmaTstat, \
              SigmaPolypWorkflow. Found: {with_div:?}, expected: {expected:?}. \
              Adding/removing divergences without updating D-34.3 surfaces here."
         );
         ```

       - **`help_entries_all_returns_three_pools`** asserts the chain length is `>= 130 + 45 + 26 == 201` (v2.2 entry count is 130 per `phase25_help_data::help_entries_count_meets_130_target`; Math 1 is 45 per `phase29_help_data_math1::math1_help_entries_count_meets_45_target`; Stat 1 is exactly 26).

       - **`stat1_help_entries_no_collision_with_other_pools`** body:
         ```
         use std::collections::HashSet;
         let v22: HashSet<&str> = help_entries().iter().map(|e| e.op_variant.as_str()).collect();
         let math1: HashSet<&str> = help_entries_math1().iter().map(|e| e.op_variant.as_str()).collect();
         for entry in help_entries_stat1() {
             assert!(
                 !v22.contains(entry.op_variant.as_str()),
                 "Stat 1 op_variant '{}' collides with v2.2 built-in pool — \
                  cross-pool op_variant uniqueness is a precondition for \
                  function_matrix_parity.rs 3-pool partition (Plan 34-02)",
                 entry.op_variant
             );
             assert!(
                 !math1.contains(entry.op_variant.as_str()),
                 "Stat 1 op_variant '{}' collides with Math Pac I pool — \
                  cross-pool op_variant uniqueness is a precondition for \
                  function_matrix_parity.rs 3-pool partition (Plan 34-02)",
                 entry.op_variant
             );
         }
         ```

    4. Run `cargo test -p hp41-cli --test phase34_help_data_stat1 --no-fail-fast` and confirm all 12 tests pass.

    Per D-34.1, D-34.3, D-34.5, D-34.6, D-25.16, D-25.17, D-29.2, C-28.3. The `include_str!` macro fails at compile time if `docs/hp41-stat1-functions.json` is missing (Task 1 produced it). The `.expect("hp41-stat1-functions.json is malformed — fix the JSON")` panic is the D-25.17 hard-build-blocker — first access to a malformed file panics with a routable message; the smoke test exercises first access so CI catches malformed commits.

    **Critical:** this task MUST NOT touch `hp41-cli/src/prgm_display.rs` — the 26 `op_display_name` arms land in Plan 34-02 Task 1. The pre-existing CI break (`error[E0004]: non-exhaustive patterns` over 26 Stat 1 Op variants) REMAINS after this plan completes; it is intentional and Plan 34-02 closes it. The plan-checker should verify the break count is unchanged at the end of this plan (1 error in prgm_display.rs).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase34_help_data_stat1 --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase34_help_data_stat1` reports `12 passed; 0 failed`
    - Source assertion: `grep -c "static STAT1_HELP_ENTRIES" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "const STAT1_FUNCTIONS_JSON" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "include_str!(\"../../docs/hp41-stat1-functions.json\")" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "pub fn help_entries_stat1" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion: `grep -c "hp41-stat1-functions.json is malformed — fix the JSON" hp41-cli/src/help_data.rs` outputs `1` (D-25.17 hard-build-blocker third copy)
    - Source assertion (three-pool chain): `grep -c "\.chain(help_entries_stat1()\.iter())" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion (Math 1 chain still present, NOT replaced): `grep -c "\.chain(help_entries_math1()\.iter())" hp41-cli/src/help_data.rs` outputs `1`
    - Source assertion (test file exists): `test -f hp41-cli/tests/phase34_help_data_stat1.rs` succeeds
    - Source assertion (surgical-divergences test names the 4 entries): `grep -c "\"Rand\", \"Seed\", \"SigmaTstat\", \"SigmaPolypWorkflow\"" hp41-cli/tests/phase34_help_data_stat1.rs` outputs at least `1`
    - Existing v2.2 + Math 1 narrow-pool tests still green: `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0 AND `cargo test -p hp41-cli --test phase29_help_data_math1` reports the existing 9+ tests passing
    - Pre-existing CI break unchanged (Plan 34-02 closes it): `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `1` (exactly 1 error, in `hp41-cli/src/prgm_display.rs`; if 0, the executor accidentally closed Plan 34-02's work; if >1, a regression was introduced)
    - JSON parses through `include_str!` round-trip: the test `stat1_help_entries_loads_at_runtime` is the empirical proof
  </acceptance_criteria>
  <done>Third `OnceLock` block added; `help_entries_stat1()` accessor live; `help_entries_all()` extended to three-pool chain in fixed order per D-34.6; 12 smoke tests green; pre-existing `non-exhaustive patterns` CI break unchanged in count (Plan 34-02 closes it); v2.2 and Math 1 narrow-pool tests still passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → CLI App | Stat 1 Pac mnemonics enter via XeqByName modal accumulator (existing v2.1 alpha-collection UX; trusted-but-untyped string); Plan 34-02 is where the resolver chain consumes them. This plan adds no new runtime input path. |
| JSON file → OnceLock | `docs/hp41-stat1-functions.json` is compile-time-embedded via `include_str!`; parsed at first access (D-25.17 hard-build-blocker third copy). Malformed JSON panics with `"hp41-stat1-functions.json is malformed — fix the JSON"`. |
| Pool boundaries | Three `OnceLock`s — `HELP_ENTRIES`, `MATH1_HELP_ENTRIES`, `STAT1_HELP_ENTRIES` — must remain independently testable (per-file panic messages) and chain-able (production `help_entries_all`). Cross-pool op_variant collision = drift signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | `docs/hp41-stat1-functions.json` (untracked malformed edits) | mitigate | D-25.17 hard-build-blocker: `.expect("hp41-stat1-functions.json is malformed — fix the JSON")` panics on first access; smoke test `stat1_help_entries_loads_at_runtime` is the CI gate. Per-file panic message routes failures correctly. |
| T-34-01-02 | Tampering | Stat 1 JSON entries vs `STAT_1.ops` row-order drift | mitigate | Function-ID density assertion (`stat1_help_entries_xrom_function_ids_are_dense`) catches gaps; full bidirectional STAT_1.ops ↔ JSON parity is Plan 34-02 Task 2 (`function_matrix_parity.rs` 3-pool extension). |
| T-34-01-03 | Tampering | Divergences policy drift (D-34.3 → docs/hp41-stat1-divergences.md Phase 35) | mitigate | `stat1_help_entries_divergences_are_surgical` enforces exactly {Rand, Seed, SigmaTstat, SigmaPolypWorkflow} as the divergent set; any future addition must be acknowledged by updating D-34.3 and the test in lockstep. |
| T-34-01-04 | Spoofing | Stat 1 mnemonic shadows existing Math 1 mnemonic or v2.2 built-in (Pitfall 1/22 reprise) | mitigate | `stat1_help_entries_no_collision_with_other_pools` catches op_variant collisions; full mnemonic-level shadowing check is Phase 37 / STAT-QUAL-08 (`xrom_shadowing.rs` STAT_1 extension — out of scope for Phase 34). |
| T-34-01-05 | Information disclosure | JSON-embedded panic messages (no secrets) | accept | All strings are public source code; D-25.17 messages are routing aids, not security boundaries. |
| T-34-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure source edits + JSON authoring + test scaffolding. Zero new runtime dependencies (CLAUDE.md Tech Stack section unchanged). |
</threat_model>

<verification>
- `python3 -c "import json; json.load(open('docs/hp41-stat1-functions.json'))"` exits 0 (Task 1 file validity)
- `cargo test -p hp41-cli --test phase34_help_data_stat1 --no-fail-fast` reports 12 tests passing (Task 2 smoke gate)
- Existing v2.2 130-target invariant preserved: `cargo test -p hp41-cli --test phase25_help_data help_entries_count_meets_130_target` exits 0
- Existing Math 1 45-target invariant preserved: `cargo test -p hp41-cli --test phase29_help_data_math1` reports the existing pass count unchanged (≥ 9 tests)
- Pre-existing CI break unchanged in count: `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `1` (Plan 34-02 Task 1 closes this break — DO NOT close it in this plan)
- `just docs-matrix-check` may fail at this point because `scripts/docs-matrix/` is still two-input (Phase 35 / STAT-DOC-02 extends to three). The Plan 34-01 acceptance criteria do NOT include `just docs-matrix-check` — that gate is owned by Phase 35.
</verification>

<success_criteria>
- STAT-CLI-02 (a): `docs/hp41-stat1-functions.json` loaded via `include_str!` into the third `OnceLock<Vec<HelpEntry>>` (`STAT1_HELP_ENTRIES`) — proven by `phase34_help_data_stat1::stat1_help_entries_loads_at_runtime`
- STAT-CLI-02 (b): `help_entries_all()` chains all three pools in fixed order built-ins → Math 1 → Stat 1 per D-34.6 — proven by `help_entries_all_returns_three_pools` + source assertion
- STAT-CLI-02 (c): malformed JSON panics with the literal third-file message `"hp41-stat1-functions.json is malformed — fix the JSON"` (D-25.17 hard-build-blocker) — proven by source assertion (the test exercising the panic is intentionally NOT in the smoke set because triggering it requires a malformed JSON fixture; the smoke test exercises the success path, the panic message is the failure-path documentation)
- D-34.1: 7 per-family categories with exact counts (`Stat1 Univariate`=4, `Stat1 ANOVA`=3, `Stat1 Regression`=8, `Stat1 Hypothesis`=2, `Stat1 Nonparam`=5, `Stat1 Distributions`=2, `Stat1 RNG`=2) — proven by Task 1 verify command
- D-34.3: exactly 4 entries carry verbatim divergences (Rand, Seed, SigmaTstat, SigmaPolypWorkflow) — proven by `stat1_help_entries_divergences_are_surgical`
- C-28.3: every Stat 1 JSON entry has `xrom.module_id == 2`, dense `function_id` 1..=26 — proven by `stat1_help_entries_all_xrom_module_id_is_2` + `stat1_help_entries_xrom_function_ids_are_dense`
- D-28.6: every entry's `key_path == Some("XEQ \"<name>\"")` — proven by `stat1_help_entries_all_key_path_is_xeq_form`
- Save-file backward compat preserved: no new `CalcState` fields; no schema changes to `HelpEntry` (`XromEntry` already shipped Phase 29 with `#[serde(default)]`)
- CLI break sequencing preserved: pre-existing 26-arm `non-exhaustive patterns` error count is `1` before AND after this plan (Plan 34-02 Task 1 is the closure)
</success_criteria>

<output>
Create `.planning/phases/34-hp41-cli-cli-integration/34-01-SUMMARY.md` when done. Include the Task 1 verify-command stdout in the "Task 1 verification" section, and the `cargo test -p hp41-cli --test phase34_help_data_stat1` pass count in the "Task 2 verification" section.
</output>
