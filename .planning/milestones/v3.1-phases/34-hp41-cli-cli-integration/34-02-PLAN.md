---
phase: 34-hp41-cli-cli-integration
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - hp41-cli/src/prgm_display.rs
  - hp41-cli/tests/function_matrix_parity.rs
  - hp41-cli/tests/phase25_xeq_by_name.rs
  - hp41-cli/tests/phase34_key_ref_includes_stat1.rs
  - hp41-cli/tests/phase34_modal_flow.rs
autonomous: true
requirements: [STAT-CLI-01, STAT-CLI-03, STAT-CLI-04, STAT-CLI-05]
requirements_addressed: [STAT-CLI-01, STAT-CLI-03, STAT-CLI-04, STAT-CLI-05]

must_haves:
  truths:
    - "`cargo check -p hp41-cli` exits 0 — the 4-way exhaustive-match invariant item 3 (CLI `op_display_name`) is sealed; the pre-existing `non-exhaustive patterns` CI break is closed by adding 26 arms covering every Phase-33 Op variant (STAT-CLI-03, FN-CLI-04, CLAUDE.md frozen invariant)"
    - "`just ci` exits 0 for the CLI path — the only remaining sanctioned `non-exhaustive patterns` warning is in `hp41-gui/src-tauri/src/prgm_display.rs` (Phase 36 owns; verified via `just ci` NOT including `gui-ci`)"
    - "`xeq_by_name_local_resolve(\"\u{03A3}NORMD\", 0b0000_0011)` returns `Some(Op::SigmaNormdWorkflow)` — confirms the existing Phase 29 final-fallback to `xrom_resolve` routes Stat 1 hits with the default v3.1 xrom_modules bitfield (STAT-CLI-01 verification — no `keys.rs` code change required, only test additions)"
    - "`function_matrix_parity.rs` walks all three JSON pools (built-ins by `xrom.is_none()`, Math 1 by `xrom.module_id == 7`, Stat 1 by `xrom.module_id == 2`) and asserts bidirectional Op ↔ JSON parity for the Stat 1 pool (STAT-CLI-04 + JSON canonical data flow invariant)"
    - "All 5 Stat1Step variants surface their OM-cited modal prompts via `state.modal_prompt` (`\u{03A3}NORMD MODE?` / `\u{03BD}=?` / `\u{03A3}CHISQD MODE?` / `DEGREE=?` / `SEED?`) AND `requires_alpha_label` returns `false` for all 5 — confirms STAT-CLI-05 modal-prompt routing reuses existing infrastructure with no new transient `CalcState` fields"
    - "Stat 1 entries with non-null `key_path` are EXCLUDED from `key_ref_entries()` by the existing post-v3.0 `entry.xrom.is_none()` filter — Stat 1 functions live in the `?` overlay's `Stat 1 Pac (XROM 2)` section per D-34.5, not the right-panel keyboard reference (STAT-CLI-04 / right-panel-revert continuity)"
  artifacts:
    - path: "hp41-cli/src/prgm_display.rs"
      provides: "26 new `op_display_name` arms for every Phase-33 Stat 1 Op variant; updated header comment reflecting the new Op count; NO `_ =>` catch-all (FN-CLI-04 4-way invariant item 3 sealed)"
      contains: "Op::SigmaBstat => ...; Op::Rand => ...; Op::Seed => ...; 26 total new arms"
    - path: "hp41-cli/tests/function_matrix_parity.rs"
      provides: "Extension from 2-pool walk to 3-pool walk via partition-by-`xrom`-field; 4 new tests for Stat 1 pool (inventory drift sentinel + forward parity + reverse parity + cross-pool partition check)"
      contains: "STAT1_OP_VARIANT_NAMES const (26 entries); test_stat1_op_inventory_count; test_every_stat1_rom_op_has_stat1_json_entry; test_every_stat1_json_entry_has_xrom_resolver_match; test_pool_partition_is_exhaustive"
    - path: "hp41-cli/tests/phase25_xeq_by_name.rs"
      provides: "Extension of `cli_resolver_matches_core_resolver` with Stat 1 name cases — verifies the Phase 29 resolver chain still fires `xrom_resolve` last and Stat 1 hits route through the default v3.1 `xrom_modules = 0b0000_0011` (STAT-CLI-01 drift detection)"
      contains: "stat1_cases canonical table with at least 5 positive cases (ΣNORMD, ΣSPEAR, ΣBSTAT, RAND, SEED) and matching negative cases at xrom_modules=0b0000_0000"
    - path: "hp41-cli/tests/phase34_modal_flow.rs"
      provides: "5 unit tests (one per Stat1Step variant) asserting `current_prompt` returns the OM-cited string AND `requires_alpha_label` returns `false` — STAT-CLI-05 modal-prompt routing verification"
      contains: "stat1_normd_mode_choice_prompt; stat1_chisqd_nu_prompt; stat1_chisqd_mode_choice_prompt; stat1_polyp_degree_prompt; stat1_seed_prompt; all 5 also assert requires_alpha_label is false"
    - path: "hp41-cli/tests/phase34_key_ref_includes_stat1.rs"
      provides: "Regression guards asserting Stat 1 entries are EXCLUDED from the right-panel (`key_ref_entries`) by the post-v3.0 xrom-exclusion filter — Stat 1 lives in the `?` overlay, not the keyboard reference (continuation of `phase29_key_ref_includes_math1.rs` pattern)"
      contains: "key_ref_entries_excludes_stat1_sigma_normd; key_ref_entries_excludes_stat1_rand; key_ref_entries_preserves_v22_entries"
  key_links:
    - from: "hp41-cli/src/prgm_display.rs::op_display_name"
      to: "hp41-core/src/ops/mod.rs::Op (Sigma* / Rand / Seed variants)"
      via: "exhaustive match — 26 new arms close the 4-way invariant item 3"
      pattern: "Op::SigmaBstat =>.*Op::SigmaChisqdWorkflow =>.*Op::Rand =>.*Op::Seed =>"
    - from: "hp41-cli/tests/function_matrix_parity.rs::test_every_stat1_json_entry_has_xrom_resolver_match"
      to: "hp41-core/src/ops/math1/xrom::xrom_resolve"
      via: "bidirectional parity assertion (every Stat 1 JSON display_name resolves via xrom_resolve with modules=0b0000_0011)"
      pattern: "xrom_resolve\\(entry\\.display_name\\.as_str\\(\\), 0b0000_0011\\)"
    - from: "hp41-cli/tests/phase34_modal_flow.rs"
      to: "hp41-core/src/ops/stat1/modal::current_prompt"
      via: "5 Stat1Step variants each cross-checked against the OM-cited prompt string"
      pattern: "current_prompt\\(&Stat1Step::"
---

<objective>
Close the 4-way exhaustive-match invariant item 3 by adding 26 `op_display_name` arms in `hp41-cli/src/prgm_display.rs` — one per Phase-33 Op variant (`Op::SigmaBstat` through `Op::SigmaChisqdWorkflow`, `Op::Rand`, `Op::Seed`). This SINGLE task closes the pre-existing `error[E0004]: non-exhaustive patterns` CI break that has been open since Phase 33 shipped (CONTEXT §"Critical Invariant"). Then extend the bidirectional parity infrastructure to cover the new Stat 1 JSON pool (`function_matrix_parity.rs` 2-pool → 3-pool partition, `phase25_xeq_by_name.rs::cli_resolver_matches_core_resolver` Stat 1 case extension) and add three new smoke-test files (`phase34_modal_flow.rs`, `phase34_key_ref_includes_stat1.rs`) covering modal-prompt routing (STAT-CLI-05) and right-panel exclusion continuity (STAT-CLI-04 / post-v3.0 filter regression guard).

Purpose: Plan 34-02 is the **closure** plan — after it lands, `cargo check -p hp41-cli` exits 0, `just ci` is green for the CLI path, 4-way invariant item 3 is sealed, and STAT-CLI-01/03/04/05 are all closed in their CLI dimension. (STAT-CLI-02 closed in Plan 34-01.) Item 4 of the 4-way invariant (`hp41-gui/src-tauri/src/prgm_display.rs`) remains intentionally open until Phase 36 per CONTEXT §"Out of scope" — this plan does NOT touch any GUI source file, and `just ci` deliberately does NOT include `gui-ci` so the open GUI break does not block Phase 34 closure.

Output: 26 new `op_display_name` arms (1 file edit, ~30 line insertion in `prgm_display.rs`); `STAT1_OP_VARIANT_NAMES` const + 4 new tests in `function_matrix_parity.rs`; `cli_resolver_matches_core_resolver` extension in `phase25_xeq_by_name.rs`; 1 new test file with 5 modal-flow tests; 1 new test file with 3 right-panel exclusion tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md
@.planning/phases/34-hp41-cli-cli-integration/34-01-PLAN.md
@.planning/phases/34-hp41-cli-cli-integration/34-01-SUMMARY.md
@.planning/milestones/v3.0-phases/29-cli-integration/29-02-PLAN.md
@.planning/milestones/v3.0-phases/29-cli-integration/29-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key types and signatures the executor needs from the shipped public surface. -->

From hp41-cli/src/prgm_display.rs:23-292 (current state — exhaustive match with PRE-EXISTING `non-exhaustive patterns` error for 26 Stat 1 variants):
  fn op_display_name(op: &Op) -> String {
      match op {
          // ... 175 existing arms (Phase 1 through Phase 28 / Math Pac I, ending at Op::Trans3d at line 291) ...
      }
  }
  // No `_ =>` catch-all — adding one would silence the CI break BUT violate FN-CLI-04 and CLAUDE.md
  // 4-way invariant. The correct fix is 26 explicit arms (this plan).
  //
  // Stale doc-comment at line 26: `/// Covers all 35 Op variants exhaustively — no non-exhaustive patterns warning.`
  // The "35" was last accurate ~Phase 12; current Op count is 175+ (post-Math 1) and becomes 200+ post-Stat 1.
  // This plan updates the comment to drop the numeric count (which keeps going stale) and instead asserts
  // the invariant: "Exhaustive match — every Op variant must have an arm (FN-CLI-04 / 4-way invariant item 3)."

From hp41-core/src/ops/mod.rs (Phase 33 shipped — the 26 Op variants needing arms; line numbers from `grep -n`):
  pub enum Op {
      // ... Phase 1-28 variants ...
      SigmaSpear,         // line 822  — ΣSPEAR
      SigmaXsqev,         // line 839  — ΣXSQEV
      SigmaEfxsq,         // line 854  — ΣEFXSQ
      SigmaBstat,         // line 876  — ΣBSTAT
      SigmaBstg,          // line 890  — ΣBSTG
      SigmaLin,           // line 904  — ΣLIN
      SigmaExp,           // line 915  — ΣEXP
      SigmaLogi,          // line 926  — ΣLOGI
      SigmaPow,           // line 938  — ΣPOW
      SigmaMmtug,         // line 955  — ΣMMTUG
      SigmaMmtgd,         // line 965  — ΣMMTGD
      SigmaAovone,        // line 980  — ΣAOVONE
      SigmaAovtwo,        // line 992  — ΣAOVTWO
      SigmaAnocov,        // line 1006 — ΣANOCOV
      SigmaCtkkk,         // line 1019 — ΣCTKKK
      SigmaCtkk,          // line 1028 — ΣCTKK
      SigmaPtst,          // line 1052 — ΣPTST
      SigmaTstat,         // line 1077 — ΣTSTAT
      SigmaMlrxy,         // line 1091 — ΣMLRXY
      SigmaMlrxyz,        // line 1102 — ΣMLRXYZ
      SigmaPolypWorkflow, // line 1124 — ΣPOLYP
      SigmaPolyc,         // line 1136 — ΣPOLYC
      SigmaNormdWorkflow, // line 1162 — ΣNORMD
      SigmaChisqdWorkflow, // line ~   — ΣCHISQD
      Rand,                // line ~   — RAND
      Seed,                // line ~   — SEED
  }
  // All 26 are unit variants (no fields) — `op_display_name` arms are simple
  // `Op::<Variant> => "<MNEMONIC>".to_string(),` shape per the Phase 28 Math 1 precedent.

From hp41-core/src/ops/stat1/modal.rs:333-361 (Phase 33 shipped — modal prompt + alpha-label contracts):
  pub fn current_prompt(step: &Stat1Step) -> Option<String> {
      match step {
          Stat1Step::NormdModeChoice => Some("\u{03A3}NORMD MODE?".to_string()),  // ΣNORMD MODE?
          Stat1Step::ChisqdNuPrompt => Some("\u{03BD}=?".to_string()),            // ν=?
          Stat1Step::ChisqdModeChoice => Some("\u{03A3}CHISQD MODE?".to_string()),// ΣCHISQD MODE?
          Stat1Step::PolypDegreePrompt(_) => Some("DEGREE=?".to_string()),
          Stat1Step::SeedPrompt => Some("SEED?".to_string()),
      }
  }
  pub fn requires_alpha_label(step: &Stat1Step) -> bool {
      match step {
          Stat1Step::NormdModeChoice | Stat1Step::ChisqdNuPrompt | Stat1Step::ChisqdModeChoice
          | Stat1Step::PolypDegreePrompt(_) | Stat1Step::SeedPrompt => false,
      }
  }

From hp41-core/src/ops/math1/modal.rs (Phase 33 shipped — carrier-enum dispatch):
  pub enum ModalProgram {
      // ... Math 1 variants ...
      Stat1(Stat1Step),
  }
  impl ModalProgram {
      pub fn current_prompt(&self) -> Option<String> { /* delegates to stat1::modal::current_prompt for Stat1(_) */ }
      pub fn requires_alpha_label(&self) -> bool { /* delegates to stat1::modal::requires_alpha_label for Stat1(_) */ }
  }

From hp41-cli/src/keys.rs:397 (Phase 29 shipped — the final-fallback line; NO CODE CHANGE in this plan):
  _ => hp41_core::ops::math1::xrom::xrom_resolve(name, xrom_modules),
  // After Phase 33 default_xrom_modules() returns 0b0000_0011 (Math 1 bit 0 + Stat 1 bit 1),
  // so this fallback routes Stat 1 names automatically. STAT-CLI-01 is verification-only.

From hp41-cli/src/keys.rs:431-451 (Phase 29 shipped — right-panel filter; NO CODE CHANGE in this plan):
  pub fn key_ref_entries() -> Vec<(String, String)> {
      // ... iterates help_entries_all() ...
      if entry.xrom.is_some() {
          continue;  // XROM-module functions EXCLUDED from right-panel (post-v3.0 UX revert)
      }
      // ... emits (key_path, display_name) tuples for built-ins only ...
  }
  // Stat 1 entries (Plan 34-01 authored with xrom: Some(_)) are EXCLUDED automatically.
  // This plan adds regression tests asserting the exclusion (mirroring the post-v3.0
  // `phase29_key_ref_includes_math1.rs` inversion).

From hp41-cli/tests/function_matrix_parity.rs:285-340 (current state — Math 1 hand-curated inventory):
  const MATH1_OP_VARIANT_NAMES: &[&str] = &[ /* 45 entries */ ];
  #[test] fn test_math1_op_inventory_count() { assert_eq!(MATH1_OP_VARIANT_NAMES.len(), 45, ...); }
  #[test] fn test_every_math1_rom_op_has_math1_json_entry() { /* forward */ }
  #[test] fn test_every_math1_json_entry_has_xrom_resolver_match() { /* reverse via xrom_resolve(_, 0b0000_0001) */ }

From hp41-cli/tests/phase29_modal_flow.rs (Phase 29 shipped — structural template for Stat 1 modal-flow):
  // Uses App::new + CalcState + key/raw_key helpers; constructs ModalProgram::Matrix(...) / Solve(...)
  // For Stat 1, the analog constructs ModalProgram::Stat1(Stat1Step::*) — but the simpler shape is
  // direct unit tests on `current_prompt` / `requires_alpha_label` without spinning up `App` (this plan
  // uses the simpler shape per CONTEXT §"Claude's Discretion — phase34_modal_flow.rs test shape").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 26 op_display_name arms — closes the pre-existing CI break (4-way invariant item 3)</name>
  <files>hp41-cli/src/prgm_display.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Critical Invariant" (the CI-break-closure framing — this task is where it lands)
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"In scope" item 3 (the 26 Op variant names — copy verbatim, do not paraphrase)
    - .planning/milestones/v3.0-phases/29-cli-integration/29-02-PLAN.md (Phase 29 closed item 3 for Math 1; this task closes it for Stat 1 — same shape, swap variants)
    - hp41-cli/src/prgm_display.rs (entire file, ~393 lines — read to confirm there is NO `_ =>` catch-all, and to see the existing arm-style for Math 1 hyperbolic / matrix variants which are the structural template)
    - hp41-core/src/ops/math1/xrom.rs:141-186 (authoritative display_name strings — the JSON `display_name` field MUST match the `prgm_display` arm exactly because both flow from STAT_1.ops)
    - hp41-core/src/ops/mod.rs (lines 822–1170 — the 26 Op variant declarations; cross-reference variant names exactly)
  </read_first>
  <action>
    Insert 26 new arms in `hp41-cli/src/prgm_display.rs` immediately after the existing last Math 1 arm `Op::Trans3d => "T3D".to_string(),` at line 291, BEFORE the closing brace of the `match op { }` block at line 292.

    **Use ASCII section dividers matching the existing Phase 28 style** (e.g. `// ── Phase 33: Stat 1 Pac Univariate / Bivariate (Plan 33-05) ──────────────`). Group arms by family per D-34.1, in the SAME row order as `STAT_1.ops` (function_id 1..=26 from Plan 34-01's JSON) so a future reader can grep either file and find the same sequence.

    **Insertion content (verbatim — every arm is a simple `Op::<Variant> => "<MNEMONIC>".to_string(),`):**

    ```
            // ── Phase 33: Stat 1 Pac Univariate / Bivariate (Plan 33-05/06) ────────────
            Op::SigmaBstat => "\u{03A3}BSTAT".to_string(),
            Op::SigmaBstg => "\u{03A3}BSTG".to_string(),
            Op::SigmaMmtug => "\u{03A3}MMTUG".to_string(),
            Op::SigmaMmtgd => "\u{03A3}MMTGD".to_string(),
            // ── Phase 33: Stat 1 Pac ANOVA Family (Plan 33-06) ─────────────────────────
            Op::SigmaAovone => "\u{03A3}AOVONE".to_string(),
            Op::SigmaAovtwo => "\u{03A3}AOVTWO".to_string(),
            Op::SigmaAnocov => "\u{03A3}ANOCOV".to_string(),
            // ── Phase 33: Stat 1 Pac Curve Fitting + Regression (Plan 33-05/08) ────────
            Op::SigmaLin => "\u{03A3}LIN".to_string(),
            Op::SigmaExp => "\u{03A3}EXP".to_string(),
            Op::SigmaLogi => "\u{03A3}LOGI".to_string(),
            Op::SigmaPow => "\u{03A3}POW".to_string(),
            Op::SigmaMlrxy => "\u{03A3}MLRXY".to_string(),
            Op::SigmaMlrxyz => "\u{03A3}MLRXYZ".to_string(),
            Op::SigmaPolypWorkflow => "\u{03A3}POLYP".to_string(),
            Op::SigmaPolyc => "\u{03A3}POLYC".to_string(),
            // ── Phase 33: Stat 1 Pac Hypothesis Tests (Plan 33-07) ─────────────────────
            Op::SigmaPtst => "\u{03A3}PTST".to_string(),
            Op::SigmaTstat => "\u{03A3}TSTAT".to_string(),
            // ── Phase 33: Stat 1 Pac Nonparam / Chi-Sq Eval / Contingency (Plan 33-04/06) ─
            Op::SigmaXsqev => "\u{03A3}XSQEV".to_string(),
            Op::SigmaEfxsq => "\u{03A3}EFXSQ".to_string(),
            Op::SigmaCtkkk => "\u{03A3}CTKKK".to_string(),
            Op::SigmaCtkk => "\u{03A3}CTKK".to_string(),
            Op::SigmaSpear => "\u{03A3}SPEAR".to_string(),
            // ── Phase 33: Stat 1 Pac Distributions (Plan 33-03) ────────────────────────
            Op::SigmaNormdWorkflow => "\u{03A3}NORMD".to_string(),
            Op::SigmaChisqdWorkflow => "\u{03A3}CHISQD".to_string(),
            // ── Phase 33: Stat 1 Pac RNG (Plan 33-08) ──────────────────────────────────
            Op::Rand => "RAND".to_string(),
            Op::Seed => "SEED".to_string(),
    ```

    **Then update the stale header comment at line 26** of `hp41-cli/src/prgm_display.rs`. Current text:
    ```
    /// Covers all 35 Op variants exhaustively — no non-exhaustive patterns warning.
    ```
    Replace with (drops the numeric count which has gone stale 5+ times since Phase 1):
    ```
    /// Exhaustive match over every Op variant — no `_ =>` catch-all (FN-CLI-04 /
    /// CLAUDE.md frozen invariant: 4-way exhaustive-match invariant item 3). Adding
    /// a new Op variant in hp41-core forces a compile error here until the arm lands;
    /// removing this exhaustiveness would silently degrade program-listing display.
    ```

    **DO NOT** add a `_ =>` catch-all under any circumstance — that would silence the build error but VIOLATE FN-CLI-04 and the CLAUDE.md frozen invariant. The compile-time exhaustiveness IS the invariant.

    **Mnemonic-string convention:** Σ encoded as `\u{03A3}` per CLAUDE.md `## Frozen Invariants → Core engine` Unicode-escape convention (verified by `Op::SigmaPlus => "\u{03A3}+".to_string(),` at line 124 of the same file). Match the existing Math 1 arms' indentation level (8 spaces — inside `match op { match arm }` body within `fn op_display_name`).

    Run `cargo check -p hp41-cli` and confirm exit code 0 — the pre-existing `non-exhaustive patterns` error MUST be gone. Then run `cargo test -p hp41-cli --test prgm_display` (or `cargo test -p hp41-cli -- prgm_display::tests`) to confirm the existing in-file unit tests (`test_display_phase12_op_labels` / `test_display_phase20_op_labels` / `test_display_phase24_ind_op_labels` at lines 295–393) still pass — they are negative-regression guards on the rendered mnemonic strings.

    Per STAT-CLI-03, FN-CLI-04, D-25.6 (CLI ↔ GUI parity — Phase 36 mirrors these arms verbatim in `hp41-gui/src-tauri/src/prgm_display.rs`), CLAUDE.md `## Frozen Invariants → 4-way exhaustive-match invariant` items 3 + 4 (item 3 sealed here; item 4 remains intentionally open until Phase 36).
  </action>
  <verify>
    <automated>cargo check -p hp41-cli 2>&1 | (! grep -q "non-exhaustive patterns") && cargo test -p hp41-cli --lib -- prgm_display</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-cli` exits 0 (CI-break closure — STAT-CLI-03 sealed)
    - Pre-existing CI break is GONE: `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `0`
    - No `_ =>` catch-all snuck in: `grep -A 400 "fn op_display_name" hp41-cli/src/prgm_display.rs | grep -c "^\s*_ =>"` outputs `0`
    - All 26 new arms present, each exactly once:
      - `grep -c "Op::SigmaBstat =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaBstg =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaMmtug =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaMmtgd =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaAovone =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaAovtwo =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaAnocov =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaLin =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaExp =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaLogi =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaPow =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaMlrxy =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaMlrxyz =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaPolypWorkflow =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaPolyc =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaPtst =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaTstat =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaXsqev =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaEfxsq =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaCtkkk =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaCtkk =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaSpear =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaNormdWorkflow =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::SigmaChisqdWorkflow =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::Rand =>" hp41-cli/src/prgm_display.rs` outputs `1`
      - `grep -c "Op::Seed =>" hp41-cli/src/prgm_display.rs` outputs `1`
    - Mnemonics use the Σ Unicode escape: `grep -c "\\\\u{03A3}BSTAT" hp41-cli/src/prgm_display.rs` outputs `1` (sentinel for the convention; same applies to other Σ-prefixed arms)
    - Stale `35 Op variants` header comment is gone: `grep -c "Covers all 35 Op variants" hp41-cli/src/prgm_display.rs` outputs `0`
    - New invariant doc-comment present: `grep -c "FN-CLI-04" hp41-cli/src/prgm_display.rs` outputs at least `1`
    - Existing in-file unit tests still pass: `cargo test -p hp41-cli --lib prgm_display` reports the 3 existing tests passing (`test_display_phase12_op_labels`, `test_display_phase20_op_labels`, `test_display_phase24_ind_op_labels`)
  </acceptance_criteria>
  <done>26 new `op_display_name` arms landed; CI break closed; `cargo check -p hp41-cli` exits 0; FN-CLI-04 / 4-way invariant item 3 sealed; stale numeric-count comment replaced with invariant-style comment; existing prgm_display in-file tests still green; GUI's analogous CI break remains intentionally open (Phase 36 scope).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend function_matrix_parity.rs from 2-pool to 3-pool partition + 4 Stat 1 parity tests</name>
  <files>hp41-cli/tests/function_matrix_parity.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"In scope" item 4 (the 3-pool walk specification)
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Claude's Discretion" `function_matrix_parity.rs` bullet (planner picks partition-by-`xrom` approach for code reuse)
    - .planning/milestones/v3.0-phases/29-cli-integration/29-01-PLAN.md Task 3 (the 2-pool extension precedent — Phase 29's 3 tests are the structural template)
    - hp41-cli/tests/function_matrix_parity.rs (entire file, 399 lines — read lines 269–399 for the Phase 29 Math 1 extension as the direct template)
    - hp41-core/src/ops/math1/xrom.rs:141-186 (STAT_1.ops — the 26-row source of truth that STAT1_OP_VARIANT_NAMES mirrors)
  </read_first>
  <behavior>
    - Test 1 (`test_stat1_op_inventory_count`): `STAT1_OP_VARIANT_NAMES.len() == 26` (exact match — Stat 1 Pac is feature-frozen for v3.1). Drift detector: if a future plan adds a Stat 1 Op without updating this list, fails loudly.
    - Test 2 (`test_every_stat1_rom_op_has_stat1_json_entry`): forward parity — every `STAT1_OP_VARIANT_NAMES` entry has a matching `op_variant` row in `help_entries_stat1()`. Failure message lists missing variants by name.
    - Test 3 (`test_every_stat1_json_entry_has_xrom_resolver_match`): reverse parity — every `help_entries_stat1()` entry's `display_name` resolves via `xrom_resolve(display_name, 0b0000_0011)` to `Some(_)`. Failure message names the orphan entry. Note: `0b0000_0011` (NOT `0b0000_0010`) because the v3.1 default per Phase 33 D-33.1 is BOTH Math 1 + Stat 1 loaded, and the bidirectional invariant must hold for the default bitfield not a hypothetical Stat-1-only bitfield.
    - Test 4 (`test_pool_partition_is_exhaustive`): partition `help_entries_all()` by `xrom.as_ref().map(|x| x.module_id)`; assert the buckets are `None` (= 130), `Some(7)` (= 45), `Some(2)` (= 26), no other values. Drift detector for any rogue `module_id` value.
    - All 4 existing v2.2 parity tests (lines 195–267) continue to pass against the narrow `help_entries()` accessor.
    - All 3 existing Math 1 parity tests (lines 342–398) continue to pass against `help_entries_math1()`.
  </behavior>
  <action>
    Append to `hp41-cli/tests/function_matrix_parity.rs` (after the existing last test `test_every_math1_json_entry_has_xrom_resolver_match` at line ~398):

    1. Add `STAT1_OP_VARIANT_NAMES` const, mirroring the `MATH1_OP_VARIANT_NAMES` const at line 285 verbatim except for the entries. The 26 entries in `STAT_1.ops`-row-order are:
       ```
       const STAT1_OP_VARIANT_NAMES: &[&str] = &[
           // Plan 33-05/06: Stat 1 Pac Univariate / Bivariate (4)
           "SigmaBstat",
           "SigmaBstg",
           "SigmaMmtug",
           "SigmaMmtgd",
           // Plan 33-06: Stat 1 Pac ANOVA Family (3)
           "SigmaAovone",
           "SigmaAovtwo",
           "SigmaAnocov",
           // Plan 33-05/08: Stat 1 Pac Curve Fitting + Regression (8)
           "SigmaLin",
           "SigmaExp",
           "SigmaLogi",
           "SigmaPow",
           "SigmaMlrxy",
           "SigmaMlrxyz",
           "SigmaPolypWorkflow",
           "SigmaPolyc",
           // Plan 33-07: Stat 1 Pac Hypothesis Tests (2)
           "SigmaPtst",
           "SigmaTstat",
           // Plan 33-04/06: Stat 1 Pac Nonparam / Chi-Sq Eval / Contingency (5)
           "SigmaXsqev",
           "SigmaEfxsq",
           "SigmaCtkkk",
           "SigmaCtkk",
           "SigmaSpear",
           // Plan 33-03: Stat 1 Pac Distributions (2)
           "SigmaNormdWorkflow",
           "SigmaChisqdWorkflow",
           // Plan 33-08: Stat 1 Pac RNG (2)
           "Rand",
           "Seed",
       ];
       ```

    2. Add Test 1 (`test_stat1_op_inventory_count`): `assert_eq!(STAT1_OP_VARIANT_NAMES.len(), 26, "STAT1_OP_VARIANT_NAMES inventory drift — expected 26 Stat 1 Pac Op variants per Phase 33 ship. Did a future plan add Op variants without updating this inventory and docs/hp41-stat1-functions.json?");`

    3. Add Test 2 (`test_every_stat1_rom_op_has_stat1_json_entry`): mirror lines 357–376 (`test_every_math1_rom_op_has_math1_json_entry`) verbatim, swapping `MATH1`→`STAT1`, `help_entries_math1`→`help_entries_stat1`, and adjusting the failure message to reference `docs/hp41-stat1-functions.json`.

    4. Add Test 3 (`test_every_stat1_json_entry_has_xrom_resolver_match`): mirror lines 378–398 verbatim, swapping `math1`→`stat1`, `help_entries_math1`→`help_entries_stat1`, and **changing the bitfield from `0b0000_0001` to `0b0000_0011`** (the v3.1 default per Phase 33 D-33.1). Failure message updated to mention `xrom_resolve` with `xrom_modules = 0b0000_0011`.

    5. Add Test 4 (`test_pool_partition_is_exhaustive`):
       ```rust
       #[test]
       fn test_pool_partition_is_exhaustive() {
           // Partition help_entries_all() by xrom.module_id. The three buckets
           // are: None (built-ins, ≥ 130), Some(7) (Math Pac I, == 45), Some(2)
           // (Stat 1 Pac, == 26). Any other value is rogue and fails the test.
           //
           // This guards future v3.2+ Time / Advantage / Stat 2 Pac additions:
           // before they merge, this test fires (because Some(<new_id>) is not
           // in the recognized set), forcing the new pool to be registered in
           // the partition.
           use hp41_cli::help_data::help_entries_all;
           let mut builtin_count = 0usize;
           let mut math1_count = 0usize;
           let mut stat1_count = 0usize;
           let mut rogue: Vec<(String, u8)> = Vec::new();
           for entry in help_entries_all() {
               match entry.xrom.as_ref().map(|x| x.module_id) {
                   None => builtin_count += 1,
                   Some(7) => math1_count += 1,
                   Some(2) => stat1_count += 1,
                   Some(other) => rogue.push((entry.op_variant.clone(), other)),
               }
           }
           assert!(
               rogue.is_empty(),
               "Unknown xrom.module_id values in help_entries_all(): {rogue:?}. \
                v3.1 supports only module_id ∈ {{None (built-ins), 7 (Math 1), 2 (Stat 1)}}. \
                Adding a new XROM module requires updating function_matrix_parity.rs partition."
           );
           assert!(builtin_count >= 130, "built-in pool shrank: {builtin_count}");
           assert_eq!(math1_count, 45, "Math 1 pool count drift: {math1_count}");
           assert_eq!(stat1_count, 26, "Stat 1 pool count drift: {stat1_count}");
       }
       ```

    6. Update the top-of-file `use` statement at line 15 from `use hp41_cli::help_data::{help_entries, help_entries_math1};` to `use hp41_cli::help_data::{help_entries, help_entries_math1, help_entries_stat1};`.

    7. Run `cargo test -p hp41-cli --test function_matrix_parity` and confirm `11 passed; 0 failed` (4 v2.2 + 3 Math 1 + 4 new Stat 1).

    Per STAT-CLI-04, D-29.2 (merged accessor enforces single source of truth — three-pool flavor), D-25.16 (JSON-canonical pipeline parity), C-28.4 (`xrom_resolve` is the canonical name→Op mapping), CLAUDE.md `## Frozen Invariants → JSON canonical data flow` (bidirectional Op ↔ JSON parity asserted by this test file). Tests are SURGICAL — v2.2 and Math 1 pools stay untouched (per-pool tests use narrow accessors `help_entries()` / `help_entries_math1()`); Stat 1 pool gets its own 3 tests; the 4th partition test guards future v3.2+ additions.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test function_matrix_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test function_matrix_parity` reports `11 passed; 0 failed` (4 existing v2.2 + 3 existing Math 1 + 4 new Stat 1)
    - Source assertion: `grep -c "STAT1_OP_VARIANT_NAMES" hp41-cli/tests/function_matrix_parity.rs` outputs at least `3` (1 declaration + 2 references in tests)
    - Source assertion: `grep -c "fn test_stat1_op_inventory_count" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "fn test_every_stat1_rom_op_has_stat1_json_entry" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "fn test_every_stat1_json_entry_has_xrom_resolver_match" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion: `grep -c "fn test_pool_partition_is_exhaustive" hp41-cli/tests/function_matrix_parity.rs` outputs `1`
    - Source assertion (v3.1 default bitfield used for reverse parity): `grep -c "0b0000_0011" hp41-cli/tests/function_matrix_parity.rs` outputs at least `1`
    - Source assertion (`help_entries_stat1` imported): `grep -c "help_entries_stat1" hp41-cli/tests/function_matrix_parity.rs` outputs at least `3` (1 use + 2 test calls minimum)
    - All 4 existing v2.2 narrow-pool tests still green AND all 3 existing Math 1 tests still green (covered by the 11-pass total)
  </acceptance_criteria>
  <done>3-pool walk live; STAT1_OP_VARIANT_NAMES inventory drift sentinel; forward + reverse Stat 1 parity proven against `xrom_resolve(_, 0b0000_0011)`; rogue-module-id partition guard for future v3.2+; v2.2 and Math 1 pool tests preserved unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend cli_resolver_matches_core_resolver with Stat 1 cases (STAT-CLI-01 drift detection)</name>
  <files>hp41-cli/tests/phase25_xeq_by_name.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Already locked" STAT-CLI-01 bullet (verification-only — `xrom_resolve` fall-through already wired Phase 29)
    - .planning/milestones/v3.0-phases/29-cli-integration/29-01-PLAN.md Task 2 (Phase 29's Math 1 extension at lines 232–236 — direct template)
    - hp41-cli/tests/phase25_xeq_by_name.rs:416-492 (entire `cli_resolver_matches_core_resolver` function — read lines 463–491 to see the existing Math 1 cases as the structural template)
    - hp41-cli/src/keys.rs:397 (the `_ => hp41_core::ops::math1::xrom::xrom_resolve(name, xrom_modules)` fall-through — unchanged, this task tests the existing wiring)
    - hp41-core/src/ops/math1/xrom.rs:195-212 (`xrom_resolve` body — bit 0 = Math 1, bit 1 = Stat 1)
  </read_first>
  <behavior>
    - Positive cases (xrom_modules = `0b0000_0011`): each of the 5+ canonical Stat 1 mnemonics resolves via `xeq_by_name_local_resolve` to the expected `Op::*` variant.
    - Negative cases (xrom_modules = `0b0000_0000`): same mnemonics return `None` (no XROM loaded).
    - Bit-1-only cases (xrom_modules = `0b0000_0010`): same mnemonics resolve correctly (Stat 1 alone is enough — proves the bit-1 arm in `xrom_resolve` works independently of bit-0).
    - Existing 10 Math 1 positive cases + 10 negative cases (lines 466–491) continue to pass — `cli_resolver_matches_core_resolver` is a single test with multiple assertion blocks; this task ADDS a block, does not modify existing blocks.
  </behavior>
  <action>
    Insert a new block in `hp41-cli/tests/phase25_xeq_by_name.rs::cli_resolver_matches_core_resolver` (the test currently ends at line 492 with the closing `}` of the function). Add the block AFTER the existing Math 1 cases block at lines 463–491, BEFORE the closing brace at line 492.

    **Block content (verbatim):**
    ```rust
        // ── Phase 34 extension: Stat 1 Pac canonical table (STAT-CLI-01 verification) ──
        // The existing Phase 29 final-fallback `_ => xrom_resolve(name, xrom_modules)` at
        // keys.rs:397 routes Stat 1 names automatically once xrom_modules has bit 1 set.
        // Phase 33 changed `default_xrom_modules()` to `0b0000_0011` so the default state
        // has BOTH Math 1 + Stat 1 loaded. This block is verification-only — no `keys.rs`
        // code change in Phase 34 (STAT-CLI-01 is verification-only per CONTEXT).
        //
        // 5 positive cases (xrom_modules = 0b0000_0011, the v3.1 default):
        let stat1_cases: &[(&str, Op)] = &[
            ("\u{03A3}NORMD", Op::SigmaNormdWorkflow),
            ("\u{03A3}SPEAR", Op::SigmaSpear),
            ("\u{03A3}BSTAT", Op::SigmaBstat),
            ("RAND", Op::Rand),
            ("SEED", Op::Seed),
        ];
        for (name, expected_op) in stat1_cases {
            // Positive — v3.1 default (Math 1 + Stat 1 loaded): must resolve.
            assert_eq!(
                xeq_by_name_local_resolve(name, 0b0000_0011),
                Some(expected_op.clone()),
                "CLI-local resolver must agree with core xrom_resolve for Stat 1 \
                 name {name:?} when v3.1 default xrom_modules=0b0000_0011 is set \
                 (STAT-CLI-01 verification)"
            );
            // Stat 1 alone (bit 1, NOT bit 0): must also resolve — proves the
            // bit-1 arm in xrom_resolve fires independently of bit 0.
            assert_eq!(
                xeq_by_name_local_resolve(name, 0b0000_0010),
                Some(expected_op.clone()),
                "Stat 1 name {name:?} must resolve when ONLY bit 1 (Stat 1) is \
                 set in xrom_modules — proves the bit-1 arm in xrom_resolve \
                 fires independently of bit 0"
            );
            // Negative — no XROM modules loaded: must NOT resolve.
            assert_eq!(
                xeq_by_name_local_resolve(name, 0b0000_0000),
                None,
                "Stat 1 name {name:?} must return None when xrom_modules=0 \
                 (XROM-unloaded state — bit-gating in xrom_resolve must hold)"
            );
        }
    ```

    **Notes:**
    - The Σ glyph is encoded as `\u{03A3}` per the file's existing convention (line 423 uses the same escape for `X\u{2260}Y?`).
    - The block reuses the same `Op` import the existing Math 1 block uses — no new imports needed (the `use hp41_core::ops::Op;` at the top of the file already brings in `Op::Sigma*` / `Op::Rand` / `Op::Seed` because they're variants of the same enum).
    - No code change to `hp41-cli/src/keys.rs` — STAT-CLI-01 is verification-only per CONTEXT §"Already locked".

    Run `cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver` and confirm exit 0.

    Per STAT-CLI-01 (verification — Phase 29 already wired the chain; this task asserts the wiring works for the new bit-1 arm), C-28.4 (`xrom_resolve` fires LAST), D-25.6 (CLI ↔ GUI parity via shared `xrom_resolve`).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver` reports `1 passed; 0 failed` (test stays a single test — block added to existing function)
    - Source assertion: `grep -c "stat1_cases" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `2` (1 declaration + 1 for-loop reference)
    - Source assertion (all 5 Stat 1 cases present):
      - `grep -c "Op::SigmaNormdWorkflow" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
      - `grep -c "Op::SigmaSpear" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
      - `grep -c "Op::SigmaBstat" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
      - `grep -c "Op::Rand" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
      - `grep -c "Op::Seed" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
    - Source assertion (three bitfield variants tested): `grep -c "0b0000_0011\|0b0000_0010\|0b0000_0000" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `3`
    - Source assertion (Σ escape used): `grep -c "\\\\u{03A3}NORMD" hp41-cli/tests/phase25_xeq_by_name.rs` outputs at least `1`
    - No code change to `hp41-cli/src/keys.rs`: `git diff --name-only hp41-cli/src/keys.rs` (run from repo root after the edit) returns empty
    - All Phase 25 tests still green: `cargo test -p hp41-cli --test phase25_xeq_by_name` reports the existing pass count unchanged plus this single test still in the green set
  </acceptance_criteria>
  <done>`cli_resolver_matches_core_resolver` extended with 5 Stat 1 cases × 3 bitfield states (positive default, positive bit-1-only, negative); `hp41-cli/src/keys.rs` untouched (STAT-CLI-01 is verification-only); existing Phase 25 + Phase 29 tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add phase34_modal_flow.rs — 5 tests asserting Stat1Step OM-cited prompts (STAT-CLI-05)</name>
  <files>hp41-cli/tests/phase34_modal_flow.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Claude's Discretion" `phase34_modal_flow.rs` bullet (recommended structure: 5 unit tests, one per Stat1Step variant, asserting both `current_prompt` AND `requires_alpha_label`)
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Specifics" Stat1Step modal-prompt strings (the 5 verbatim OM strings)
    - hp41-cli/tests/phase29_modal_flow.rs (entire file, ~288 lines — structural template; this task uses the SIMPLER unit-test shape per CONTEXT discretion, not the full App+CalcState shape, because Stat1Step variants don't require keyboard-event simulation)
    - hp41-core/src/ops/stat1/modal.rs:333-361 (the source-of-truth for `current_prompt` + `requires_alpha_label` — the test cross-checks against these exact strings)
    - hp41-core/src/ops/math1/modal.rs (the `ModalProgram::Stat1(Stat1Step)` carrier enum + its `current_prompt` / `requires_alpha_label` dispatch arms — the test exercises the dispatch through the carrier enum, not via direct Stat1Step calls, so a future carrier-enum bug surfaces here)
  </read_first>
  <behavior>
    - Test 1 (`stat1_normd_mode_choice_prompt`): `ModalProgram::Stat1(Stat1Step::NormdModeChoice).current_prompt()` returns `Some("ΣNORMD MODE?")`; `requires_alpha_label()` returns `false`.
    - Test 2 (`stat1_chisqd_nu_prompt`): `ModalProgram::Stat1(Stat1Step::ChisqdNuPrompt).current_prompt()` returns `Some("ν=?")`; `requires_alpha_label()` returns `false`.
    - Test 3 (`stat1_chisqd_mode_choice_prompt`): `ModalProgram::Stat1(Stat1Step::ChisqdModeChoice).current_prompt()` returns `Some("ΣCHISQD MODE?")`; `requires_alpha_label()` returns `false`.
    - Test 4 (`stat1_polyp_degree_prompt`): `ModalProgram::Stat1(Stat1Step::PolypDegreePrompt(degree)).current_prompt()` returns `Some("DEGREE=?")` for both `degree = 0` and `degree = 5` (the inner `usize` is opaque to the prompt — confirms it does NOT leak into the rendered string); `requires_alpha_label()` returns `false`.
    - Test 5 (`stat1_seed_prompt`): `ModalProgram::Stat1(Stat1Step::SeedPrompt).current_prompt()` returns `Some("SEED?")`; `requires_alpha_label()` returns `false`.
  </behavior>
  <action>
    Create `hp41-cli/tests/phase34_modal_flow.rs` with the simpler unit-test shape (no `App` / no `CalcState` — direct cross-checks on `ModalProgram::current_prompt` + `requires_alpha_label`). Header comment + content:

    ```rust
    //! Phase 34 / Plan 34-02 Task 4 — Stat 1 Pac modal-prompt routing tests (STAT-CLI-05).
    //!
    //! Asserts that every Stat1Step variant surfaces its OM-cited prompt via
    //! `ModalProgram::Stat1(_).current_prompt()` AND that NONE of them require an
    //! alpha label (so the post-dispatch CollectForModal auto-open hook does NOT
    //! fire for Stat 1 — confirmed by CONTEXT §"In scope" item 7 + CONTEXT
    //! §"Specifics" `requires_alpha_label` bullet).
    //!
    //! Verification strategy: cross-checks at the CARRIER enum level
    //! (`ModalProgram::Stat1(_)`), NOT at the inner `stat1::modal::current_prompt`
    //! level directly. This proves the Phase 33 / D-33.3b carrier-enum dispatch
    //! routes Stat 1 prompts correctly through the same `ModalProgram` enum the
    //! CLI / GUI / `ui::pending_prompt` consume. A regression in the dispatch arm
    //! in `hp41-core/src/ops/math1/modal.rs` would surface here.
    //!
    //! Prompt strings per CONTEXT §"Specifics" (cross-referenced against
    //! `hp41-core/src/ops/stat1/modal.rs:333-343`):
    //! - NormdModeChoice → "ΣNORMD MODE?"
    //! - ChisqdNuPrompt → "ν=?"
    //! - ChisqdModeChoice → "ΣCHISQD MODE?"
    //! - PolypDegreePrompt(_) → "DEGREE=?" (degree value is OPAQUE to prompt)
    //! - SeedPrompt → "SEED?"

    #![allow(clippy::unwrap_used)]

    use hp41_core::ops::math1::modal::ModalProgram;
    use hp41_core::ops::stat1::modal::Stat1Step;

    #[test]
    fn stat1_normd_mode_choice_prompt() {
        let prog = ModalProgram::Stat1(Stat1Step::NormdModeChoice);
        assert_eq!(
            prog.current_prompt(),
            Some("\u{03A3}NORMD MODE?".to_string()),
            "ModalProgram::Stat1(NormdModeChoice).current_prompt() must \
             return 'ΣNORMD MODE?' (Phase 33 OM-cited per stat1/modal.rs:335)"
        );
        assert!(
            !prog.requires_alpha_label(),
            "NormdModeChoice must NOT require alpha label — numeric mode choice only"
        );
    }

    #[test]
    fn stat1_chisqd_nu_prompt() {
        let prog = ModalProgram::Stat1(Stat1Step::ChisqdNuPrompt);
        assert_eq!(
            prog.current_prompt(),
            Some("\u{03BD}=?".to_string()),
            "ModalProgram::Stat1(ChisqdNuPrompt).current_prompt() must \
             return 'ν=?' (Phase 33 OM-cited per stat1/modal.rs:336)"
        );
        assert!(
            !prog.requires_alpha_label(),
            "ChisqdNuPrompt must NOT require alpha label — numeric ν entry only"
        );
    }

    #[test]
    fn stat1_chisqd_mode_choice_prompt() {
        let prog = ModalProgram::Stat1(Stat1Step::ChisqdModeChoice);
        assert_eq!(
            prog.current_prompt(),
            Some("\u{03A3}CHISQD MODE?".to_string()),
            "ModalProgram::Stat1(ChisqdModeChoice).current_prompt() must \
             return 'ΣCHISQD MODE?' (Phase 33 OM-cited per stat1/modal.rs:337)"
        );
        assert!(
            !prog.requires_alpha_label(),
            "ChisqdModeChoice must NOT require alpha label — numeric mode choice only"
        );
    }

    #[test]
    fn stat1_polyp_degree_prompt() {
        // Inner usize is opaque — the prompt must be identical for any degree.
        for degree in [0usize, 1, 3, 5, 99] {
            let prog = ModalProgram::Stat1(Stat1Step::PolypDegreePrompt(degree));
            assert_eq!(
                prog.current_prompt(),
                Some("DEGREE=?".to_string()),
                "ModalProgram::Stat1(PolypDegreePrompt({degree})).current_prompt() \
                 must return 'DEGREE=?' regardless of the inner degree value \
                 (CONTEXT §Specifics: inner usize is OPAQUE to the rendered prompt; \
                 D-34.3 divergence note)"
            );
            assert!(
                !prog.requires_alpha_label(),
                "PolypDegreePrompt({degree}) must NOT require alpha label — \
                 numeric degree entry only"
            );
        }
    }

    #[test]
    fn stat1_seed_prompt() {
        let prog = ModalProgram::Stat1(Stat1Step::SeedPrompt);
        assert_eq!(
            prog.current_prompt(),
            Some("SEED?".to_string()),
            "ModalProgram::Stat1(SeedPrompt).current_prompt() must \
             return 'SEED?' (Phase 33 OM-cited per stat1/modal.rs:342)"
        );
        assert!(
            !prog.requires_alpha_label(),
            "SeedPrompt must NOT require alpha label — numeric seed value only \
             (per CONTEXT §Already locked D-29.7/D-29.9: all 5 Stat1Step \
             variants are NUMERIC-input steps, so CollectForModal auto-open \
             never fires for Stat 1)"
        );
    }
    ```

    Run `cargo test -p hp41-cli --test phase34_modal_flow` and confirm `5 passed; 0 failed`.

    **Critical notes:**
    - The test uses `ModalProgram::current_prompt` (the carrier-enum method) NOT `stat1::modal::current_prompt` (the per-step function) directly. This is intentional: the carrier-enum dispatch arm is what the CLI / GUI / `ui::pending_prompt` actually invoke, so a regression in the dispatch wiring (e.g., a copy-paste bug in `hp41-core/src/ops/math1/modal.rs` that returns the wrong inner step's prompt) is caught.
    - The Σ + ν glyphs are encoded as `\u{03A3}` + `\u{03BD}` per the CLAUDE.md Unicode-escape convention.
    - The 5 inner usize values for `PolypDegreePrompt` (0, 1, 3, 5, 99) include both edge cases (0 = uninitialized, 99 = unrealistic but legal upper bound) — confirms the prompt is genuinely opaque to the inner value.

    Per STAT-CLI-05 (modal-prompt routing via existing `print_buffer` + `modal_program` infrastructure with no new transient `CalcState` fields), D-28.4 / XROM-09 / D-33.3b (modal_prompt is the channel; `print_buffer` is for PRX/PRA/PRSTK only), CONTEXT §"Specifics" Stat1Step modal-prompt strings.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase34_modal_flow</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase34_modal_flow` reports `5 passed; 0 failed`
    - Source assertion (test file exists): `test -f hp41-cli/tests/phase34_modal_flow.rs` succeeds
    - Source assertion (5 test fns): `grep -c "^fn stat1_" hp41-cli/tests/phase34_modal_flow.rs` outputs `5`
    - Source assertion (carrier-enum dispatch tested, not inner-fn direct): `grep -c "ModalProgram::Stat1" hp41-cli/tests/phase34_modal_flow.rs` outputs at least `5`
    - Source assertion (no direct `stat1::modal::current_prompt` calls — proves the test goes through the dispatch): `grep -c "stat1::modal::current_prompt" hp41-cli/tests/phase34_modal_flow.rs` outputs `0`
    - Source assertion (Σ + ν Unicode escapes present): `grep -c "\\\\u{03A3}NORMD MODE?" hp41-cli/tests/phase34_modal_flow.rs` outputs at least `1` AND `grep -c "\\\\u{03BD}=?" hp41-cli/tests/phase34_modal_flow.rs` outputs at least `1`
    - Source assertion (PolypDegreePrompt opaqueness covered): `grep -c "PolypDegreePrompt" hp41-cli/tests/phase34_modal_flow.rs` outputs at least `1` AND the test iterates over multiple degree values (visible via `for degree in`)
    - Source assertion (no new `CalcState` field references — STAT-CLI-05 / D-33.3b invariant): `grep -c "CalcState" hp41-cli/tests/phase34_modal_flow.rs` outputs `0` (this test doesn't need a `CalcState` instance — direct ModalProgram tests)
    - All `requires_alpha_label` assertions present: `grep -c "requires_alpha_label" hp41-cli/tests/phase34_modal_flow.rs` outputs at least `5`
  </acceptance_criteria>
  <done>5 modal-flow tests green; OM-cited prompts cross-checked at the carrier-enum dispatch level; all 5 Stat1Step variants asserted as numeric-input-only (`requires_alpha_label = false`); no new CalcState fields introduced; STAT-CLI-05 routing confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add phase34_key_ref_includes_stat1.rs — right-panel exclusion regression guards (STAT-CLI-04 continuity)</name>
  <files>hp41-cli/tests/phase34_key_ref_includes_stat1.rs</files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"In scope" item 10 ("New `phase34_key_ref_includes_stat1.rs` smoke test asserting Stat 1 entries with non-null `key_path` appear in `keys::key_ref_entries()`...")
    - **IMPORTANT:** the CONTEXT.md wording says "appear in `keys::key_ref_entries()` after the merged accessor migration (parallel to `phase29_key_ref_includes_math1.rs`)" — BUT `phase29_key_ref_includes_math1.rs` was INVERTED post-v3.0 to assert EXCLUSION (right-panel UX revert; XROM-module functions live in the `?` overlay, not the right-panel). Read the entire `phase29_key_ref_includes_math1.rs` file (lines 1–98) — the post-v3.0 inversion is the actual current behavior, and the Phase 34 analog MUST follow the inverted shape, NOT the literal CONTEXT.md wording. CONTEXT.md was written before the planner had read the post-v3.0 revert state; the planner is authorized to follow the actual `key_ref_entries::xrom.is_some()` filter behavior.
    - hp41-cli/tests/phase29_key_ref_includes_math1.rs (entire file — direct structural template; the file's own header comment explains the post-v3.0 inversion rationale at lines 1–28)
    - hp41-cli/src/keys.rs:431-451 (the `key_ref_entries()` body with the `if entry.xrom.is_some() { continue; }` filter at line 437)
  </read_first>
  <behavior>
    - Test 1 (`key_ref_entries_excludes_stat1_sigma_normd`): `key_ref_entries()` does NOT contain `("XEQ \"ΣNORMD\"", "ΣNORMD")` — ΣNORMD is the canonical Stat 1 sentinel (used in ROADMAP success criteria + E2E smoke per Phase 37 STAT-QUAL-11).
    - Test 2 (`key_ref_entries_excludes_stat1_rand`): `key_ref_entries()` does NOT contain `("XEQ \"RAND\"", "RAND")` — confirms the filter applies to ASCII-named RNG entries too (not just Σ-prefixed entries), proving the gate is xrom-presence-based, not display-name-pattern-based.
    - Test 3 (`key_ref_entries_preserves_v22_entries`): `key_ref_entries()` still contains `("+", "+")` — negative regression guard (v2.2 built-ins with `xrom: None` must NOT be caught by the xrom-exclusion filter).
  </behavior>
  <action>
    Create `hp41-cli/tests/phase34_key_ref_includes_stat1.rs`. The file name follows the `phase29_key_ref_includes_math1.rs` convention exactly (per CONTEXT §"Claude's Discretion — test file naming convention") so a future v3.2 reader can grep `phase\d+_key_ref_includes_\w+\.rs` to find every XROM-module exclusion regression file.

    **File content:**
    ```rust
    //! Phase 34 / Plan 34-02 Task 5 — Stat 1 Pac right-panel exclusion guards
    //! (STAT-CLI-04 continuity — post-v3.0 right-panel UX revert).
    //!
    //! ## Why this file inverts CONTEXT.md's literal wording
    //!
    //! CONTEXT.md §"In scope" item 10 says "Stat 1 entries with non-null
    //! `key_path` appear in `keys::key_ref_entries()`". That wording was
    //! authored before the post-v3.0 right-panel UX revert was discovered (see
    //! `phase29_key_ref_includes_math1.rs` header comment for the full
    //! revert rationale: ~45 XEQ rows crowded out the keyboard reference).
    //!
    //! The actual production behavior — confirmed by reading
    //! `hp41-cli/src/keys.rs:437` (`if entry.xrom.is_some() { continue; }`) —
    //! EXCLUDES every XROM-module function from the right-panel. Phase 34's
    //! correct continuation of Phase 29's inverted Math 1 tests is to assert
    //! the SAME exclusion for Stat 1. Stat 1 functions remain fully
    //! discoverable via the `?` overlay's `Stat 1 Pac (XROM 2)` section per
    //! D-34.5 — the discoverability story is intact, just routed through the
    //! overlay rather than the right-panel.
    //!
    //! ## Test inversion rationale
    //!
    //! Without the `entry.xrom.is_none()` filter, the right-panel would balloon
    //! by an additional 26 rows on top of the ~45 from Math 1 (= 71 XEQ rows
    //! crowding a ~15–25-row keyboard reference). The filter is a load-bearing
    //! UX guard, and these tests are its regression sentinel.

    #![allow(clippy::unwrap_used)]

    use hp41_cli::keys::key_ref_entries;

    /// Asserts that `key_ref_entries()` EXCLUDES the Stat 1 Pac `XEQ "ΣNORMD"` entry.
    ///
    /// ΣNORMD is the canonical Stat 1 sentinel (ROADMAP Phase 33 SC §2 uses
    /// `XEQ "ΣNORMD"` as the integration smoke target; Phase 37 STAT-QUAL-11
    /// uses ΣNORMD for the WebdriverIO E2E smoke). A regression that adds
    /// Σ-prefixed entries back to the right-panel surfaces here AND in the
    /// future E2E surface — defense in depth.
    #[test]
    fn key_ref_entries_excludes_stat1_sigma_normd() {
        let entries = key_ref_entries();
        let leaked = entries.iter().any(|(key_path, display)| {
            key_path == "XEQ \"\u{03A3}NORMD\"" && display == "\u{03A3}NORMD"
        });
        assert!(
            !leaked,
            "key_ref_entries() must NOT include (\"XEQ \\\"\u{03A3}NORMD\\\"\", \"\u{03A3}NORMD\") — \
             the post-v3.0 `entry.xrom.is_none()` filter in keys.rs:437 excludes \
             XROM-module functions from the right-panel. Stat 1 functions live in \
             the `?` overlay's \"Stat 1 Pac (XROM 2)\" section per D-34.5, NOT in \
             the keyboard reference."
        );
    }

    /// Asserts that `key_ref_entries()` EXCLUDES the Stat 1 Pac `XEQ "RAND"` entry.
    ///
    /// RAND is an ASCII-named Stat 1 entry (no Σ prefix), confirming the
    /// exclusion is xrom-presence-based and NOT display-name-pattern-based.
    /// If a future bug introduced a heuristic like `display_name.starts_with("Σ")`
    /// in the filter, RAND would leak — this test would catch that drift.
    #[test]
    fn key_ref_entries_excludes_stat1_rand() {
        let entries = key_ref_entries();
        let leaked = entries
            .iter()
            .any(|(key_path, display)| key_path == "XEQ \"RAND\"" && display == "RAND");
        assert!(
            !leaked,
            "key_ref_entries() must NOT include (\"XEQ \\\"RAND\\\"\", \"RAND\") — \
             the post-v3.0 `entry.xrom.is_none()` filter must apply uniformly to \
             every XROM-module function regardless of display_name shape (Σ-prefixed \
             or ASCII). Stat 1 RAND lives in the `?` overlay, NOT the keyboard reference."
        );
    }

    /// Asserts that `key_ref_entries()` still contains v2.2 built-in entries after
    /// the Plan 34-01 third-pool extension.
    ///
    /// Negative-regression test: the merged accessor's three-pool chain must not
    /// accidentally drop v2.2 entries (whose `xrom` field is `None`). The v2.2 Add
    /// op (`("+", "+")`) is the canonical sentinel from `phase29_key_ref_includes_math1.rs`.
    #[test]
    fn key_ref_entries_preserves_v22_entries() {
        let entries = key_ref_entries();
        let found = entries
            .iter()
            .any(|(key_path, display)| key_path == "+" && display == "+");
        assert!(
            found,
            "key_ref_entries() must still include (\"+\", \"+\") after the \
             Plan 34-01 three-pool chain extension — negative-regression guard \
             ensuring v2.2 built-in entries (with `xrom: None`) are NOT accidentally \
             caught by the filter. The filter drops ONLY entries where `xrom.is_some()`."
        );
    }
    ```

    Run `cargo test -p hp41-cli --test phase34_key_ref_includes_stat1` and confirm `3 passed; 0 failed`.

    Per STAT-CLI-04 (the `?` overlay surfaces a `Stat 1 Pac (XROM 2)` section — which IS where Stat 1 functions live; the right-panel exclusion is the corollary), D-34.5 (section header naming), D-25.18 (KEY_REF_TABLE derives from JSON via `key_ref_entries` — the post-v3.0 filter is the only modification to that derivation), post-v3.0 right-panel UX revert (`phase29_key_ref_includes_math1.rs` header comment).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase34_key_ref_includes_stat1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase34_key_ref_includes_stat1` reports `3 passed; 0 failed`
    - Source assertion (test file exists): `test -f hp41-cli/tests/phase34_key_ref_includes_stat1.rs` succeeds
    - Source assertion (3 test fns): `grep -c "^fn key_ref_entries_" hp41-cli/tests/phase34_key_ref_includes_stat1.rs` outputs `3`
    - Source assertion (ΣNORMD sentinel uses Unicode escape): `grep -c "\\\\u{03A3}NORMD" hp41-cli/tests/phase34_key_ref_includes_stat1.rs` outputs at least `1`
    - Source assertion (RAND non-Σ sentinel present): `grep -c "RAND" hp41-cli/tests/phase34_key_ref_includes_stat1.rs` outputs at least `1`
    - Source assertion (v2.2 + sentinel preserved): `grep -c "v2.2 built-in entries" hp41-cli/tests/phase34_key_ref_includes_stat1.rs` outputs at least `1`
    - No code change to `hp41-cli/src/keys.rs` (the filter is already in place from v3.0): `git diff --name-only hp41-cli/src/keys.rs` (run from repo root after the edit) returns empty
    - Sibling Math 1 exclusion tests still green: `cargo test -p hp41-cli --test phase29_key_ref_includes_math1` reports `3 passed; 0 failed`
  </acceptance_criteria>
  <done>3 right-panel exclusion regression guards green; Stat 1 Pac functions confirmed excluded from `key_ref_entries()` (they live in the `?` overlay's `Stat 1 Pac (XROM 2)` section per D-34.5); v2.2 built-in `+` sentinel preserved; sibling Phase 29 Math 1 tests still green; `hp41-cli/src/keys.rs` untouched (the v3.0 filter handles both Math 1 + Stat 1 uniformly).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Full-suite CI verification — `just ci` exits 0; STAT-CLI-01..05 sealed</name>
  <files></files>
  <read_first>
    - .planning/phases/34-hp41-cli-cli-integration/34-CONTEXT.md §"Critical Invariant" (`just ci` exit code as primary acceptance criterion for plan-checker)
    - CLAUDE.md `## Quality Gates` table (the gates `just ci` enforces)
  </read_first>
  <action>
    Run the full CI surface for the CLI path and confirm green:

    1. `just ci` — exits 0. This runs `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test --workspace`, license-audit, and the Free42 contamination guard. The expected state at the end of Plan 34-02: CLI surface green (4-way invariant item 3 sealed); GUI surface NOT exercised (the `non-exhaustive patterns` break in `hp41-gui/src-tauri/src/prgm_display.rs` is intentional and Phase 36 closes it; `just ci` is configured to NOT include `gui-ci`).

    2. `cargo test -p hp41-cli` — all CLI tests pass, including the 5 new tests added in this plan (Task 2: 4 new tests in `function_matrix_parity.rs`; Task 3: existing test extended; Task 4: 5 new tests in `phase34_modal_flow.rs`; Task 5: 3 new tests in `phase34_key_ref_includes_stat1.rs`; Plan 34-01 Task 2: 12 tests in `phase34_help_data_stat1.rs`).

    3. `cargo check -p hp41-cli` — exits 0; no `non-exhaustive patterns` warning anywhere in `hp41-cli`.

    4. `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` — outputs `0` (the pre-existing break is gone; no new break introduced).

    5. **DO NOT** run `just gui-ci` — the GUI's analogous CI break is intentionally open until Phase 36 per CONTEXT §"Out of scope". Running `gui-ci` would surface the expected GUI break and fail; Phase 34's verification surface is CLI-only.

    6. Optional but recommended: `cargo test -p hp41-cli --test key_coverage` — confirms FN-CLI-01 verifiable-closure test (xrom-tagged entries are excluded from the keyboard-reachability closure, so the 3-pool merged accessor change in Plan 34-01 does NOT affect this test).

    No source-file modifications in this task. This is a CI verification gate and SUMMARY-input collection step.

    Per CONTEXT §"Critical Invariant" + STAT-CLI-01 + STAT-CLI-03 + STAT-CLI-04 + STAT-CLI-05 (all four sealed by Tasks 1–5; this task is the closing verification).
  </action>
  <verify>
    <automated>just ci && cargo test -p hp41-cli --tests && cargo check -p hp41-cli</automated>
  </verify>
  <acceptance_criteria>
    - `just ci` exits 0 (full CLI CI surface green — Phase 34 closure achieved for the CLI dimension)
    - `cargo test -p hp41-cli --tests` exits 0 (every test file green, including the 4 new Phase 34 test files)
    - `cargo check -p hp41-cli` exits 0 (no `non-exhaustive patterns` warning)
    - `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `0` (Plan 34-02 Task 1 closure verified)
    - `cargo test -p hp41-cli --test key_coverage` exits 0 (FN-CLI-01 invariant preserved — xrom-tagged entries excluded from keyboard-reachability closure)
    - GUI break still present (sanity check that this plan did NOT touch GUI): `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | grep -c "non-exhaustive patterns"` outputs `1` (Phase 36 closure scope — DO NOT close in Phase 34)
    - No untracked file additions outside the planned set: `git status --porcelain | grep -v "^??.*\\.planning/" | grep -v "^.M.*\\.planning/" | grep -E "^\\?\\? "` returns only the expected new test files (`phase34_modal_flow.rs`, `phase34_key_ref_includes_stat1.rs`) and the new JSON file (`docs/hp41-stat1-functions.json` from Plan 34-01)
  </acceptance_criteria>
  <done>`just ci` exits 0; STAT-CLI-01..05 all sealed; CLI's 4-way invariant item 3 closed; GUI's item 4 remains intentionally open (Phase 36); no source-file modifications in this task — verification gate + SUMMARY input only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → CLI App | Stat 1 Pac mnemonics enter via XeqByName modal accumulator; the resolver chain (`xeq_by_name_local_resolve` → `xrom_resolve`) is the security perimeter. Task 3 tests this perimeter at the 0b0000_0011 / 0b0000_0010 / 0b0000_0000 bitfield states. |
| ModalProgram::Stat1 dispatch | The carrier-enum dispatch in `hp41-core/src/ops/math1/modal.rs` routes `current_prompt` + `requires_alpha_label` calls to `stat1::modal::*`. Task 4 tests this dispatch level (NOT the inner-fn level) to catch carrier-enum drift. |
| JSON ↔ Op enum boundary | `function_matrix_parity.rs` is the bidirectional invariant: every JSON entry's `display_name` must resolve via `xrom_resolve`, and every Op variant must have a JSON entry. Task 2 extends this guarantee to the third pool. |
| Right-panel filter boundary | `key_ref_entries::xrom.is_some()` filter is a load-bearing UX guard (post-v3.0 revert). Task 5 is the regression sentinel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Tampering | `prgm_display.rs` `_ =>` catch-all sneaks in (silences `non-exhaustive patterns` but breaks FN-CLI-04) | mitigate | Task 1 acceptance_criteria asserts `grep -c "^\s*_ =>" hp41-cli/src/prgm_display.rs` outputs `0`. The compile-time exhaustiveness IS the invariant — closing the CI break with a catch-all would silently degrade program-listing display when future Op variants land. |
| T-34-02-02 | Tampering | Mnemonic-string drift between `prgm_display.rs` arm and `STAT_1.ops` display_name | mitigate | Task 2 reverse parity test (`test_every_stat1_json_entry_has_xrom_resolver_match`) catches this — the JSON display_name must resolve via xrom_resolve, which uses STAT_1.ops as its source. The forward parity test catches missing JSON rows. |
| T-34-02-03 | Tampering | Carrier-enum dispatch bug in `ModalProgram::current_prompt`/`requires_alpha_label` arms (wrong Stat1Step routing) | mitigate | Task 4 tests at the CARRIER level, not the inner-fn level. A copy-paste bug in `math1/modal.rs` that returns the wrong Stat1Step's prompt would fire here. |
| T-34-02-04 | Tampering | Bitfield drift — `default_xrom_modules()` regresses from 0b11 to 0b01 (Stat 1 unloaded by default) | mitigate | Task 3 bit-1-only test (`0b0000_0010`) proves bit-1 routing is independent of bit-0; Task 2 reverse parity uses `0b0000_0011` (the v3.1 default) so a default regression would fail. |
| T-34-02-05 | Information disclosure | Right-panel ballooning by 26 Stat 1 rows (UX regression, not security) | mitigate | Task 5 regression guards assert exclusion; sibling Math 1 tests' header comment documents the rationale for future contributors. |
| T-34-02-06 | Spoofing | Stat 1 Op variant added in a future plan without `prgm_display.rs` arm (4-way invariant item 3 reopens) | mitigate | Compile-time exhaustive match is the gate — Rust's `non-exhaustive patterns` error fires on the next build. Task 1 doc-comment update ("Adding a new Op variant in hp41-core forces a compile error here until the arm lands") makes the invariant readable. |
| T-34-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure source edits + test scaffolding. Zero new runtime dependencies. CLAUDE.md Tech Stack section unchanged. |
</threat_model>

<verification>
- `just ci` exits 0 (Task 6 primary gate — CLI dimension Phase 34 closure)
- `cargo test -p hp41-cli --tests` exits 0 (all CLI tests, including 4 new Phase 34 test files)
- `cargo check -p hp41-cli` exits 0 (no `non-exhaustive patterns` warning)
- `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `0` (the Phase 33 → Phase 34 CI break is closed)
- All 4 new test files green: `phase34_help_data_stat1.rs` (Plan 34-01, 12 tests), `phase34_modal_flow.rs` (5 tests), `phase34_key_ref_includes_stat1.rs` (3 tests), extended `function_matrix_parity.rs` (4 new + 7 existing = 11 tests), extended `phase25_xeq_by_name.rs::cli_resolver_matches_core_resolver` (1 test, extended block)
- GUI's 4-way invariant item 4 still open (Phase 36 scope — verified via `cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | grep -c "non-exhaustive patterns"` outputs `1`)
- `cargo test -p hp41-cli --test key_coverage` exits 0 (FN-CLI-01 verifiable closure preserved)
</verification>

<success_criteria>
- STAT-CLI-01: `xeq_by_name_local_resolve("ΣNORMD", 0b0000_0011) == Some(Op::SigmaNormdWorkflow)` AND `(_, 0b0000_0010) == Some(_)` AND `(_, 0b0000_0000) == None` — proven by Task 3's `cli_resolver_matches_core_resolver` extension; no `hp41-cli/src/keys.rs` code change (verification-only per CONTEXT §"Already locked")
- STAT-CLI-03: 26 new `op_display_name` arms in `hp41-cli/src/prgm_display.rs` — every Phase-33 Op variant (`Op::SigmaBstat` through `Op::SigmaChisqdWorkflow`, `Op::Rand`, `Op::Seed`) has an exhaustive-match arm; NO `_ =>` catch-all (FN-CLI-04); proven by Task 1's `cargo check -p hp41-cli` exit 0 + 26 grep assertions
- STAT-CLI-04 (a): `function_matrix_parity.rs` 3-pool walk via partition-by-`xrom.module_id` covers every Stat 1 entry; bidirectional Op ↔ JSON parity proven — Task 2's 4 new tests
- STAT-CLI-04 (b): right-panel `key_ref_entries()` EXCLUDES Stat 1 entries per the post-v3.0 xrom-presence filter; Stat 1 lives in the `?` overlay's `Stat 1 Pac (XROM 2)` section per D-34.5 — Task 5's 3 regression guards
- STAT-CLI-05: all 5 Stat1Step variants surface their OM-cited modal prompts via `ModalProgram::Stat1(_).current_prompt()` (carrier-enum dispatch) AND `requires_alpha_label()` returns `false` for all 5 — Task 4's 5 modal-flow tests
- CI break closure: `cargo check -p hp41-cli` exits 0; `cargo build -p hp41-cli --tests --no-run 2>&1 | grep -c "non-exhaustive patterns"` outputs `0` — Task 1 closure verified by Task 6
- 4-way invariant item 3 sealed; item 4 (GUI) remains intentionally open (Phase 36 scope)
- D-25.6 CLI ↔ GUI parity preserved at the SHARED-CORE level: every Stat 1 modal behavior routes through shared `hp41-core` code (`xrom_resolve`, `ModalProgram::Stat1` dispatch); Phase 36 will mirror the CLI's `op_display_name` arms verbatim in GUI
- No `hp41-core` source modifications (Phase 33 freeze + math1/ freeze D-33.3 preserved)
- No `hp41-gui` source modifications (Phase 36 scope per CONTEXT §"Out of scope")
- No new runtime dependencies; MSRV 1.88 unchanged
</success_criteria>

<output>
Create `.planning/phases/34-hp41-cli-cli-integration/34-02-SUMMARY.md` when done. Include:
- Task 1 verification: `cargo check -p hp41-cli` exit code + the 26 grep assertion outputs
- Task 2 verification: `cargo test -p hp41-cli --test function_matrix_parity` pass count (expected `11 passed; 0 failed`)
- Task 3 verification: `cargo test -p hp41-cli --test phase25_xeq_by_name cli_resolver_matches_core_resolver` exit + the count of `Op::Sigma*`/`Op::Rand`/`Op::Seed` references in the extension block
- Task 4 verification: `cargo test -p hp41-cli --test phase34_modal_flow` pass count (expected `5 passed; 0 failed`)
- Task 5 verification: `cargo test -p hp41-cli --test phase34_key_ref_includes_stat1` pass count (expected `3 passed; 0 failed`)
- Task 6 verification: `just ci` exit code + the GUI-still-broken sentinel (`cargo build --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | grep -c "non-exhaustive patterns"` output = `1`)
- "Phase 34 sealed" summary: STAT-CLI-01..05 status (all 5 → ✅); 4-way invariant item 3 sealed; item 4 deferred to Phase 36; CLI's intentional sanctioned CI break from Phase 33's ship is now closed
</output>
