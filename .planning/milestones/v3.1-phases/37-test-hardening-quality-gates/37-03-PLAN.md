---
phase: 37-test-hardening-quality-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - hp41-core/tests/stat1_anova_coverage.rs
  - hp41-core/tests/stat1_backward_compat.rs
  - hp41-core/tests/fixtures/v30-autosave.json
  - docs/hp41-stat1-divergences.md
autonomous: true
requirements:
  - STAT-QUAL-03
  - STAT-QUAL-09
  - STAT-QUAL-10
  - STAT-GUI-05

must_haves:
  truths:
    - "stat1/anova.rs line coverage rises from 86.50% to >= 90%"
    - "v3.0 save file with xrom_modules=1 loads and migrates to bit 1 set"
    - "Free42 contamination guard exits 0 over final stat1 tree"
    - "STAT-GUI-05 is formally resolved with bounded-iter waiver documented"
  artifacts:
    - path: "hp41-core/tests/stat1_anova_coverage.rs"
      provides: "Coverage-gap closure tests for stat1/anova.rs"
      contains: "fn aovone"
    - path: "hp41-core/tests/stat1_backward_compat.rs"
      provides: "v3.0 save-file migration test"
      contains: "fn v30_save_loads_with_stat1_migration"
    - path: "hp41-core/tests/fixtures/v30-autosave.json"
      provides: "v3.0 CalcState fixture with xrom_modules=1"
      contains: "xrom_modules"
    - path: "docs/hp41-stat1-divergences.md"
      provides: "D-35-13 behavioral-policy entry for bounded-iter waiver"
      contains: "D-35-13"
  key_links:
    - from: "hp41-core/tests/stat1_backward_compat.rs"
      to: "hp41-core/src/state.rs"
      via: "migrate_after_load call"
      pattern: "migrate_after_load"
    - from: "hp41-core/tests/stat1_backward_compat.rs"
      to: "hp41-core/tests/fixtures/v30-autosave.json"
      via: "include_str! fixture load"
      pattern: "include_str.*v30"
---

<objective>
Close coverage gaps, verify backward compatibility, resolve STAT-GUI-05, and re-verify Free42 contamination.

Purpose: This plan handles three independent requirements that share Wave 2 timing: (1) anova.rs coverage gap closure (86.50% -> >= 90%), (2) v3.0 save-file backward-compat migration test (STAT-QUAL-10), (3) STAT-GUI-05 formal resolution via documented waiver (D-37.9), and (4) Free42 contamination guard re-verification (STAT-QUAL-09). These are grouped because they all need the Wave 1 meta-gates in place and produce non-overlapping files.

Output: Two new test files, one fixture, one divergence-doc entry, and Free42 attestation.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-hardening-quality-gates/37-CONTEXT.md
@.planning/phases/37-test-hardening-quality-gates/37-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From hp41-core/src/ops/stat1/anova.rs (509 LOC, 8 inline tests):
- op_sigma_aovone(state) — one-way ANOVA F-ratio; uses STAT1_AOV_*_REG register consts
- op_sigma_aovtwo(state) — two-way ANOVA row+column F-ratios
- op_sigma_anocov(state) — one-way ANCOVA with covariate adjustment
- SIZE-guard: checks register count >= required minimum; returns HpError if insufficient

From hp41-core/src/state.rs:
- pub fn migrate_after_load(&mut self) — sets xrom_modules bit 1 if clear (line 386)
- CalcState::new() — default state constructor
- xrom_modules: u8 field with #[serde(default)]

From hp41-core/tests/fixtures/v20-autosave.json (structural template):
- Minimal CalcState JSON with default values; stack, regs (64 zeros), display_mode, etc.

From hp41-core/tests/synthetic_tests.rs:
- test_calcstate_loads_without_new_fields — fixture load + serde_json::from_str + field assertions

From docs/hp41-stat1-divergences.md:
- D-35-01 through D-35-12 existing entries
- Bucket 3 "Behavioral Policies" section where D-35-13 will be appended
- Five-field entry format: OM citation / Our behavior / OM behavior / Rationale / See

From scripts/check-free42-contamination.sh:
- 18-token grep pattern; scans both math1/ and stat1/ trees; exit 0 = clean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stat1_anova_coverage.rs coverage-gap tests (STAT-QUAL-03)</name>
  <files>hp41-core/tests/stat1_anova_coverage.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/anova.rs (the coverage target — read in full, 509 lines)
    - hp41-core/src/ops/stat1/mod.rs (register constants: STAT1_AOV_*_REG named consts)
    - hp41-core/tests/stat1_cancellation.rs (dispatch + assert pattern reference)
  </read_first>
  <action>
    Run `cargo llvm-cov --html -p hp41-core` and inspect the HTML report for `stat1/anova.rs` to identify exact uncovered line ranges (98 missed lines, 19 missed regions). Then create `hp41-core/tests/stat1_anova_coverage.rs` targeting those branches.

    File structure: Free42 disclaim header, `#![allow(clippy::unwrap_used)]`, imports from hp41_core (CalcState, HpNum, Op, dispatch, HpError, ops::stat1 register consts), rust_decimal.

    Target the following branch families:

    1. ΣAOVONE SIZE-guard error path: dispatch with default-zero state (insufficient data) — must return error. Test with exactly enough data for a minimal valid one-way ANOVA.

    2. ΣAOVTWO error paths: two-way ANOVA with insufficient rows/columns, zero-cell error (division by zero in SS calculations). Test minimal valid 2x2 layout.

    3. ΣANOCOV covariate-adjustment edge cases: covariate with zero variance (degenerate case), ANCOVA with exactly 2 groups.

    4. Group-count register paths: test that group iteration counts are correctly decremented and that the within-group vs between-group decomposition handles boundary cases.

    5. op_sigma_minus correction key interaction: if the anova [C] correction-key path has uncovered branches, test accumulate-then-correct sequences.

    Each test uses CalcState::new(), populates the required Sigma registers (R11..R22 etc. per named consts in mod.rs), then dispatches the Op and asserts on the result (either HpError or stack X value). Use `approx::assert_relative_eq!` for computed results (1e-9 tolerance for closed-form paths).

    After writing tests, re-run coverage to verify anova.rs >= 90%.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test stat1_anova_coverage -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/stat1_anova_coverage.rs exists with >= 8 test functions
    - All tests pass: `cargo test -p hp41-core --test stat1_anova_coverage` exits 0
    - Tests cover ΣAOVONE, ΣAOVTWO, and ΣANOCOV error paths
    - Tests cover at least 2 SIZE-guard error paths
    - stat1/anova.rs line coverage >= 90% (up from 86.50%)
    - File contains `#![allow(clippy::unwrap_used)]` and Free42 disclaim header
  </acceptance_criteria>
  <done>stat1/anova.rs coverage at >= 90%; STAT-QUAL-03 per-file floor met for anova.rs</done>
</task>

<task type="auto">
  <name>Task 2: Create v3.0 backward-compat fixture and migration test (STAT-QUAL-10)</name>
  <files>hp41-core/tests/fixtures/v30-autosave.json, hp41-core/tests/stat1_backward_compat.rs</files>
  <read_first>
    - hp41-core/tests/fixtures/v20-autosave.json (structural template for fixture format)
    - hp41-core/tests/synthetic_tests.rs (backward-compat test pattern — lines 263-297)
    - hp41-core/src/state.rs (CalcState struct fields, serde attributes, migrate_after_load at line 386)
  </read_first>
  <action>
    Step 1: Create `hp41-core/tests/fixtures/v30-autosave.json`. This is a minimal CalcState JSON matching the v3.0 state shape:
    - Same base fields as v20-autosave.json (stack with all zeros, 64 register zeros, default display/alpha/angle modes)
    - Add `"xrom_modules": 1` (v3.0 default — only Math 1 bit 0 set)
    - Intentionally OMIT `rand_seed` field — tests that `#[serde(default)]` provides HpNum::zero() (Pitfall 20 / STAT-RNG-03)
    - Omit all `#[serde(skip)]` transient fields (modal_program, modal_prompt, integ_state, solve_state, cancel_requested, print_buffer, pending_chisqd_nu) — they are absent from persisted state
    - The fixture must be deserializable by `serde_json::from_str::<CalcState>()` without error

    Step 2: Create `hp41-core/tests/stat1_backward_compat.rs` with the following tests:

    Test 1: `v30_save_loads_with_stat1_migration` — load fixture via `include_str!`, deserialize, assert `state.xrom_modules == 1` (pre-migration), call `hp41_core::state::migrate_after_load(&mut state)`, assert `state.xrom_modules == 0b0000_0011` (post-migration bit 1 set).

    Test 2: `v30_save_rand_seed_defaults_to_zero` — load fixture, call migrate_after_load, assert `state.rand_seed` inner Decimal == Decimal::ZERO. This verifies `#[serde(default)]` WITHOUT `#[serde(skip)]` works correctly for absent field.

    Test 3: `v30_save_stat1_op_resolves_after_migration` — load fixture, migrate, then dispatch `Op::SigmaNormdWorkflow` and verify it opens the modal (state.modal_prompt is Some) — confirming the Stat 1 module is functional after migration.

    File header: Free42 disclaim + `#![allow(clippy::unwrap_used)]`. Imports: hp41_core (CalcState, state::migrate_after_load, Op, dispatch, HpNum), rust_decimal::Decimal, serde_json.

    Add LINT-EXEMPT annotation on the `assert_eq!(state.rand_seed.inner(), Decimal::ZERO)` line since it compares Decimal values (will be scanned by lint_stat1_assertions.rs from Plan 37-01).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test stat1_backward_compat -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/fixtures/v30-autosave.json exists with `"xrom_modules": 1` and NO `rand_seed` field
    - hp41-core/tests/stat1_backward_compat.rs exists with 3 test functions
    - `cargo test -p hp41-core --test stat1_backward_compat` passes
    - Pre-migration xrom_modules == 1 assertion passes
    - Post-migration xrom_modules == 0b0000_0011 assertion passes
    - rand_seed defaults to Decimal::ZERO when absent from fixture
    - ΣNORMD resolves after migration (modal_prompt is Some)
    - File contains LINT-EXEMPT annotation on Decimal equality check
  </acceptance_criteria>
  <done>v3.0 backward-compat test passing; migrate_after_load activates Stat 1 (bit 1); STAT-QUAL-10 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: STAT-GUI-05 waiver documentation + Free42 re-verification (STAT-GUI-05, STAT-QUAL-09)</name>
  <files>docs/hp41-stat1-divergences.md</files>
  <read_first>
    - docs/hp41-stat1-divergences.md (the file being modified — current state, find last D-35-NN entry number)
    - .planning/phases/36-hp41-gui-gui-integration/36-CONTEXT.md (D-36.2 bounded-iter rationale)
    - scripts/check-free42-contamination.sh (the script to re-verify)
  </read_first>
  <action>
    Step 1 (STAT-GUI-05): Append a D-35-13 entry to `docs/hp41-stat1-divergences.md` in bucket 3 (Behavioral Policies), after the last existing entry. Use the five-field format per D-37.9 / 37-CONTEXT.md specifics block:

    Title: "D-35-13: Bounded-Iteration Distribution Primitives Do Not Wire cancel_requested"
    OM citation: N/A (emulator-internal design choice)
    Our behavior: Distribution primitives (norm_cdf_inv_f64, gamma_regularized_f64, beta_regularized_f64) bounded at ITER_CAP=50, complete in microseconds, no per-loop cancel_requested check
    OM behavior: N/A (HP-41 hardware has no cancellation mechanism for built-in math)
    Rationale: Phase 31 cancel_requested designed for user-driven open-ended iterative paths (INTG/SOLVE/DIFEQ); bounded 50-iter f64 primitives complete faster than R/S press; see D-36.2 + ADR-v3.1-002
    See: hp41-core/src/ops/stat1/distributions.rs (ITER_CAP constant), 36-CONTEXT.md D-36.2, docs/adr/v3.1-002-distribution-primitives-policy.md

    Step 2 (STAT-QUAL-09): Run `bash scripts/check-free42-contamination.sh` and verify exit code 0. Document the result (18-token pattern, both math1/ and stat1/ trees scanned, exit 0) in the plan summary. No code changes needed — the guard was already extended in Phase 33 Plan 33-00 per D-33.8.
  </action>
  <verify>
    <automated>grep -c "D-35-13" docs/hp41-stat1-divergences.md && bash scripts/check-free42-contamination.sh && echo "FREE42_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - docs/hp41-stat1-divergences.md contains a D-35-13 entry in bucket 3 (Behavioral Policies)
    - D-35-13 entry uses the five-field format (OM citation / Our behavior / OM behavior / Rationale / See)
    - D-35-13 references D-36.2, ADR-v3.1-002, and hp41-core/src/ops/stat1/distributions.rs
    - `bash scripts/check-free42-contamination.sh` exits 0 (no contamination detected)
    - STAT-GUI-05 is formally resolved by the documented waiver (no code changes needed)
  </acceptance_criteria>
  <done>STAT-GUI-05 resolved via D-35-13 behavioral-policy entry; Free42 contamination guard re-verified at 18 tokens, exit 0; STAT-QUAL-09 attested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test files + documentation only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
cargo test -p hp41-core --test stat1_anova_coverage --test stat1_backward_compat -- --nocapture && bash scripts/check-free42-contamination.sh
</verification>

<success_criteria>
- stat1/anova.rs coverage >= 90%
- v3.0 backward-compat migration test passes (xrom_modules=1 -> 0b11)
- D-35-13 entry in divergences doc
- Free42 contamination guard exits 0
</success_criteria>

<output>
Create `.planning/phases/37-test-hardening-quality-gates/37-03-SUMMARY.md` when done
</output>
