---
phase: 37-test-hardening-quality-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - hp41-core/tests/stat1_modal_coverage.rs
autonomous: true
requirements:
  - STAT-QUAL-03

must_haves:
  truths:
    - "stat1/modal.rs line coverage rises from 74.21% to >= 90%"
    - "All 5 Stat1Step submit_step branches are exercised by external tests"
    - "Cancel and error paths in modal.rs are covered by targeted tests"
  artifacts:
    - path: "hp41-core/tests/stat1_modal_coverage.rs"
      provides: "Coverage-gap closure tests for stat1/modal.rs"
      contains: "fn normd_mode"
      min_lines: 80
  key_links:
    - from: "hp41-core/tests/stat1_modal_coverage.rs"
      to: "hp41-core/src/ops/stat1/modal.rs"
      via: "dispatch + submit_step calls"
      pattern: "dispatch.*Op::Sigma"
---

<objective>
Close the coverage gap on stat1/modal.rs (74.21% lines / 79.01% regions -> >= 90%).

Purpose: modal.rs is the CRITICAL GAP file identified by research — 65 missed lines, 38 missed regions. The gap is in submit_step branches for each Stat1Step variant, cancel paths, error paths in modal initialization, and multi-step modal sequences (ΣCHISQD nu-prompt -> mode-choice chain, ΣPOLYP degree prompt). Closing this gap is the highest-leverage coverage action for STAT-QUAL-03 (per-file floor) and contributes to STAT-QUAL-01 (aggregate).

Output: stat1_modal_coverage.rs with targeted tests covering submit_step error paths, cancel semantics, and edge cases per Stat1Step variant.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-hardening-quality-gates/37-CONTEXT.md
@.planning/phases/37-test-hardening-quality-gates/37-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From hp41-core/src/ops/stat1/modal.rs:
- pub enum Stat1Step { NormdModeChoice, ChisqdNuPrompt, ChisqdModeChoice, PolypDegreePrompt(u32), SeedPrompt }
- pub fn submit_step(state: &mut CalcState, step: Stat1Step) -> Result<(), HpError>
- NormdModeChoice: reads X as mode index (1=CDF, 2=PDF, 3=inverse); drops X, dispatches eval fn
- ChisqdNuPrompt: reads X as positive integer nu; stores in state.pending_chisqd_nu; advances to ChisqdModeChoice
- ChisqdModeChoice: reads X as mode (1=PDF, 2=CDF); recovers nu from pending_chisqd_nu; dispatches eval fn
- PolypDegreePrompt(n): reads X as degree d; validates range; stores coefficients
- SeedPrompt: reads X as seed value; stores in state.rand_seed

From hp41-core/src/ops/mod.rs:
- pub fn dispatch(state: &mut CalcState, op: Op) -> Result<(), HpError>

From hp41-core/src/state.rs:
- pub struct CalcState — stack, modal_program, modal_prompt, pending_chisqd_nu, rand_seed fields
- ModalProgram::Stat1(Stat1Step) variant

From hp41-core/src/error.rs:
- pub enum HpError { Domain, ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current modal.rs coverage and identify uncovered branches</name>
  <files>hp41-core/tests/stat1_modal_coverage.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/modal.rs (the coverage target — read in full, 532 lines)
    - hp41-core/tests/stat1_rand_determinism.rs (existing tests that partially cover SeedPrompt path)
    - hp41-core/tests/stat1_cancellation.rs (existing tests that partially cover cancel paths)
  </read_first>
  <action>
    Run `cargo llvm-cov --html -p hp41-core` and inspect the HTML report for `stat1/modal.rs` to identify the exact uncovered line ranges. Then create `hp41-core/tests/stat1_modal_coverage.rs` targeting the uncovered branches.

    File structure: Free42 disclaim header, `#![allow(clippy::unwrap_used)]`, imports from hp41_core (CalcState, HpNum, Op, dispatch, HpError), rust_decimal.

    Target the following branch families (65 missed lines, research-identified gaps):

    1. NormdModeChoice error paths: dispatch ΣNORMD workflow, then submit with invalid mode index (0, 4, negative) — each must return HpError::Domain. Also test mode 2 (PDF) and mode 3 (inverse) happy paths if not already covered by inline tests.

    2. ChisqdNuPrompt error paths: dispatch ΣCHISQD workflow, submit with nu=0 (returns Domain), nu=-1 (returns Domain), nu=fractional (truncates to integer). Test the two-step chain: ChisqdNuPrompt succeeds -> advances modal to ChisqdModeChoice -> submit mode 1 (PDF) and mode 2 (CDF).

    3. ChisqdModeChoice error paths: invoke ChisqdModeChoice without prior ChisqdNuPrompt (pending_chisqd_nu is None -> Domain). Invalid mode index (0, 3) after valid nu submission.

    4. PolypDegreePrompt error paths: dispatch ΣPOLYP, submit with degree=0 (invalid), degree too large, fractional degree. Happy path with degree=2 (polynomial fit).

    5. SeedPrompt: dispatch SEED, submit with various values. Cancel path if applicable. The seed prompt reads X and stores in rand_seed — verify the modal clears after submit.

    6. Cancel paths: for each Stat1Step, verify that Esc (modal cancellation) restores clean state — modal_program=None, modal_prompt=None, no side effects.

    Use the dispatch + assert pattern from stat1_rand_determinism.rs. Each test function gets a `/// Catches:` doc comment per the established convention.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test stat1_modal_coverage -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/stat1_modal_coverage.rs exists with >= 15 test functions
    - All tests pass: `cargo test -p hp41-core --test stat1_modal_coverage` exits 0
    - Tests cover all 5 Stat1Step variants in submit_step
    - Tests cover at least 3 error paths (invalid mode, invalid nu, out-of-sequence ChisqdModeChoice)
    - Tests cover the ΣCHISQD two-step modal chain (ChisqdNuPrompt -> ChisqdModeChoice)
    - File contains `#![allow(clippy::unwrap_used)]` and Free42 disclaim header
  </acceptance_criteria>
  <done>stat1_modal_coverage.rs installed with >= 15 targeted tests covering submit_step error paths, cancel paths, and edge cases across all 5 Stat1Step variants</done>
</task>

<task type="auto">
  <name>Task 2: Verify modal.rs coverage >= 90% after gap closure</name>
  <files>hp41-core/tests/stat1_modal_coverage.rs</files>
  <read_first>
    - hp41-core/tests/stat1_modal_coverage.rs (just created in Task 1)
  </read_first>
  <action>
    Run `cargo llvm-cov --html -p hp41-core` and check the HTML report for `stat1/modal.rs` line coverage. The target is >= 90% lines.

    If modal.rs is still below 90% after Task 1, identify the remaining uncovered lines from the HTML report and add additional targeted tests to stat1_modal_coverage.rs. Common remaining gaps:

    - The `to_i32_safe` trait impl and error path (line 317-330 range)
    - The `cancel_step` function if it exists separately from submit_step
    - Any `cfg` conditional code paths
    - Error message formatting branches

    After adding tests, re-run coverage and verify >= 90%. Also verify that `stat1_op_test_count.rs` (from Plan 37-01) still passes — the new test file should add test mentions for variants that were potentially below the >= 5 threshold.

    Also run `just test` to verify no regressions across the full test suite.
  </action>
  <verify>
    <automated>cargo llvm-cov -p hp41-core --html 2>&1 | grep "stat1/modal" || echo "check HTML report manually"</automated>
  </verify>
  <acceptance_criteria>
    - stat1/modal.rs line coverage >= 90% (up from 74.21%)
    - `cargo test -p hp41-core --test stat1_modal_coverage` passes
    - `cargo test -p hp41-core --test stat1_op_test_count` passes (or reports progress — new tests add variant mentions)
    - `just test` passes with no regressions
  </acceptance_criteria>
  <done>stat1/modal.rs coverage verified at >= 90% lines; per-file STAT-QUAL-03 floor met for modal.rs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages — zero new dependencies |
</threat_model>

<verification>
cargo test -p hp41-core --test stat1_modal_coverage -- --nocapture && cargo llvm-cov -p hp41-core --html
</verification>

<success_criteria>
- stat1/modal.rs line coverage >= 90%
- All stat1_modal_coverage.rs tests pass
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/37-test-hardening-quality-gates/37-02-SUMMARY.md` when done
</output>
