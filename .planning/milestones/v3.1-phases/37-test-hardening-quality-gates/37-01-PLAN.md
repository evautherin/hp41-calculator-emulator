---
phase: 37-test-hardening-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/stat1_rand_determinism.rs
  - hp41-core/tests/stat1_op_test_count.rs
  - hp41-core/tests/lint_stat1_assertions.rs
autonomous: true
requirements:
  - STAT-QUAL-06
  - STAT-QUAL-07
  - STAT-QUAL-08

must_haves:
  truths:
    - "stat1_op_test_count.rs asserts every STAT_1 Op variant has >= 5 test mentions across external + inline tests"
    - "lint_stat1_assertions.rs blocks assert_eq!(decimal, decimal) on iterated results in stat1 test files"
    - "xrom_shadowing.rs already passes 4 STAT_1 tests (Phase 33 attestation)"
  artifacts:
    - path: "hp41-core/tests/stat1_op_test_count.rs"
      provides: "Per-Op test count meta-gate for 26 Stat 1 variants"
      contains: "fn each_stat1_op_has_at_least_5_tests"
    - path: "hp41-core/tests/lint_stat1_assertions.rs"
      provides: "Assertion-discipline lint over stat1 test files"
      contains: "fn no_decimal_assert_eq_in_stat1_tests"
    - path: "hp41-core/tests/stat1_rand_determinism.rs"
      provides: "LINT-EXEMPT annotations on exact HpNum assert_eq calls"
      contains: "LINT-EXEMPT"
  key_links:
    - from: "hp41-core/tests/stat1_op_test_count.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "include_str! scan of stat1_resolve scope"
      pattern: "fn stat1_resolve"
    - from: "hp41-core/tests/lint_stat1_assertions.rs"
      to: "hp41-core/tests/stat1_*.rs"
      via: "runtime glob scan of test files for forbidden patterns"
      pattern: "starts_with.*stat1_"
---

<objective>
Install the Wave 1 meta-gate infrastructure for Stat 1 Pac quality enforcement.

Purpose: These meta-gates must land BEFORE any Wave 2 coverage-gap test files are written, ensuring new test files are immediately scanned by the per-Op count gate and the assertion-discipline lint. Mirrors the v3.0 Phase 32 ordering where math1_op_test_count.rs and lint_math1_assertions.rs landed first.

Output: Three test files (one modified, two new) that enforce Pitfall 14/16/17 discipline over the entire stat1 test surface. STAT-QUAL-08 (xrom_shadowing) is formally attested as already complete from Phase 33.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-hardening-quality-gates/37-CONTEXT.md
@.planning/phases/37-test-hardening-quality-gates/37-PATTERNS.md

<interfaces>
<!-- Source files the executor needs to understand -->

From hp41-core/tests/math1_op_test_count.rs (229 lines — direct structural template):
- fn collect_math1_variant_names() -> Vec<String> — brace-depth scan of xrom.rs `fn math1_resolve` scope
- fn line_mentions_variant(line, variant_name) -> bool — word-boundary matching (WR-03)
- fn count_math1_test_mentions(variant_name, tests_dir) -> usize — glob tests/math1_*.rs, count #[test] blocks mentioning the variant
- fn each_math1_op_has_at_least_5_tests() — main assertion

From hp41-core/tests/lint_math1_assertions.rs (326 lines — direct structural template):
- const LINT_EXEMPT_TOKEN: &str = "LINT-EXEMPT:"
- fn format_offender(path, line_no, line) -> String
- fn preceding_block_has_lint_exempt(lines, idx) -> bool — 3-line lookback
- fn line_is_forbidden_assert_eq(line) -> bool — detects `assert_eq!` with Decimal/HpNum operands
- fn line_is_forbidden_manual_tolerance(line) -> bool — detects `(a - b).abs() < eps` pattern
- fn no_decimal_assert_eq_in_math1_tests() — main Pitfall 17 gate
- fn no_manual_tolerance_pattern_in_math1_tests() — main Pitfall 14 gate

From hp41-core/src/ops/math1/xrom.rs (STAT_1 block lines 141-186):
- STAT_1.ops slice contains 26 entries with `=> Some(Op::SigmaBstat)` etc.
- fn stat1_resolve(&str) -> Option<Op> at line 313

From hp41-core/tests/stat1_rand_determinism.rs:
- Lines 38-43: `assert_eq!(state.rand_seed, expected)` and `assert_eq!(state.stack.x, expected)` — exact HpNum equality, needs LINT-EXEMPT
- Lines 116, 120: `assert_eq!(state.rand_seed, hp(...))` — same pattern, needs LINT-EXEMPT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LINT-EXEMPT annotations to stat1_rand_determinism.rs</name>
  <files>hp41-core/tests/stat1_rand_determinism.rs</files>
  <read_first>
    - hp41-core/tests/stat1_rand_determinism.rs (the file being modified — current state)
    - hp41-core/tests/lint_math1_assertions.rs (LINT-EXEMPT convention reference — see how annotations are placed on lines preceding assert_eq!)
  </read_first>
  <action>
    Add `// LINT-EXEMPT: exact HpNum equality via Decimal::new construction — no f64 bridge; rand_seed is a Decimal-exact LCG accumulator` comment on the line immediately preceding each of the 5 `assert_eq!` calls that compare HpNum/Decimal values (lines 38, 43, 73, 87, 116, 120 — verify current line numbers). The LINT-EXEMPT comment must be on the line BEFORE the `assert_eq!` macro invocation (or on the same line if single-line). This is required per Pitfall 2 in 37-RESEARCH.md: without these annotations, the Wave 1 lint_stat1_assertions.rs will fire false positives on exact-integer HpNum equality assertions that are semantically correct. Do NOT modify any assertion logic — only add annotation comments. Per D-32.1, the LINT-EXEMPT token is `LINT-EXEMPT:` (with colon) and the preceding_block_has_lint_exempt helper uses a 3-line lookback window.
  </action>
  <verify>
    <automated>grep -c "LINT-EXEMPT" hp41-core/tests/stat1_rand_determinism.rs</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/stat1_rand_determinism.rs contains at least 5 lines matching `LINT-EXEMPT:`
    - Every `assert_eq!` that compares HpNum or Decimal values has a LINT-EXEMPT annotation within the 3-line lookback window
    - `cargo test -p hp41-core --test stat1_rand_determinism` still passes (no behavioral change)
  </acceptance_criteria>
  <done>All exact-HpNum assert_eq! calls in stat1_rand_determinism.rs have LINT-EXEMPT annotations; existing tests pass unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create stat1_op_test_count.rs meta-gate (STAT-QUAL-07)</name>
  <files>hp41-core/tests/stat1_op_test_count.rs</files>
  <read_first>
    - hp41-core/tests/math1_op_test_count.rs (direct structural template — read in full)
    - hp41-core/src/ops/math1/xrom.rs (lines 300-350 for stat1_resolve scope; lines 141-186 for STAT_1.ops variant names)
    - hp41-core/tests/stat1_rand_determinism.rs (existing external stat1 test file — verify it would be scanned)
    - hp41-core/tests/stat1_cancellation.rs (existing external stat1 test file — verify it would be scanned)
  </read_first>
  <action>
    Create `hp41-core/tests/stat1_op_test_count.rs` mirroring the `math1_op_test_count.rs` structure with these KEY ADAPTATIONS per D-37.7:

    1. File header: Free42 disclaim + doc comment explaining Wave-1 meta-test for Stat 1 Pac Op variants + `#![allow(clippy::unwrap_used)]`.

    2. `collect_stat1_variant_names()`: Use `include_str!("../src/ops/math1/xrom.rs")`, scan for `fn stat1_resolve` (NOT `fn math1_resolve`), use identical brace-depth tracking, extract variant names from `=> Some(Op::...)` patterns. Must produce exactly 26 variants.

    3. `line_mentions_variant()`: Reuse verbatim from math1 analog — word-boundary matching with `Op::{variant_name}` token + post-boundary check (WR-03).

    4. `count_stat1_test_mentions()`: TWO-PASS scan (the KEY adaptation from math1 which only does Pass 1):
       - Pass 1: external `tests/stat1_*.rs` files via `std::fs::read_dir(tests_dir)` — filter by `filename.starts_with("stat1_")`, split by `#[test]`, count mentions.
       - Pass 2: inline `#[cfg(test)]` modules in `src/ops/stat1/*.rs` via `CARGO_MANIFEST_DIR` join `src/ops/stat1` — for each `.rs` file, split by `#[cfg(test)]`, then split by `#[test]`, count mentions. This captures the 189 inline tests.

    5. `each_stat1_op_has_at_least_5_tests()`: main `#[test]` fn — collect variants, iterate, assert `count >= 5` for each, collect failures, assert failures is empty with Pitfall 16 violation message.

    Expected: 26 variants detected. Most should have >= 5 test mentions given 189 inline tests + 2 external test files. If any variant is below threshold, that surfaces at Wave 1 and gets addressed by Wave 2 coverage-gap plans.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test stat1_op_test_count -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/stat1_op_test_count.rs exists and compiles
    - `cargo test -p hp41-core --test stat1_op_test_count` runs (may pass or fail — if it fails, the failure message lists specific variants below threshold, which is the meta-gate working as designed)
    - `collect_stat1_variant_names()` returns exactly 26 variants (not 45 — which would mean the scanner leaked into math1_resolve scope)
    - The file contains `#![allow(clippy::unwrap_used)]` and the Free42 disclaim header
  </acceptance_criteria>
  <done>stat1_op_test_count.rs meta-gate installed; scans 26 STAT_1 variants across both external and inline test files; reports any variants with fewer than 5 test mentions</done>
</task>

<task type="auto">
  <name>Task 3: Create lint_stat1_assertions.rs assertion-discipline lint (STAT-QUAL-06)</name>
  <files>hp41-core/tests/lint_stat1_assertions.rs</files>
  <read_first>
    - hp41-core/tests/lint_math1_assertions.rs (direct structural template — read in full, 326 lines)
    - hp41-core/tests/stat1_rand_determinism.rs (verify LINT-EXEMPT annotations from Task 1 are present)
  </read_first>
  <action>
    Create `hp41-core/tests/lint_stat1_assertions.rs` mirroring the `lint_math1_assertions.rs` structure with these KEY ADAPTATIONS per D-37.8:

    1. File header: Free42 disclaim + doc comment referencing Phase 37 Wave 1, Pitfall 14/17 discipline, D-37.8 scope, note about LINT-EXEMPT pre-annotations on stat1_rand_determinism.rs + `#![allow(clippy::unwrap_used)]`.

    2. Constants: `LINT_EXEMPT_TOKEN = "LINT-EXEMPT:"` (unchanged from math1 analog).

    3. `collect_stat1_test_content(tests_dir: &Path) -> Vec<(PathBuf, String)>`: TWO-PASS file collector (KEY adaptation):
       - Pass 1: external `tests/stat1_*.rs` via `read_dir`, filter `starts_with("stat1_")`, read full content.
       - Pass 2: inline `#[cfg(test)]` blocks from `src/ops/stat1/*.rs` via `CARGO_MANIFEST_DIR`. For each source file, split by `#[cfg(test)]`, skip(1), collect test content. Push `(path, test_content)` pairs.

    4. Four lint helper functions: copy VERBATIM from math1 analog — `format_offender`, `preceding_block_has_lint_exempt`, `line_is_forbidden_assert_eq`, `line_is_forbidden_manual_tolerance`. These are content-agnostic.

    5. `no_decimal_assert_eq_in_stat1_tests()`: main Pitfall 17 gate. Same body as math1 analog but using `collect_stat1_test_content` instead of math1-scoped collector. Assert no offenders found.

    6. `no_manual_tolerance_pattern_in_stat1_tests()`: main Pitfall 14 gate. Same body as math1 analog.

    The LINT-EXEMPT annotations added in Task 1 to stat1_rand_determinism.rs ensure this lint does not false-positive on the exact-HpNum assert_eq! calls in the RNG determinism tests.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test lint_stat1_assertions -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/lint_stat1_assertions.rs exists and compiles
    - `cargo test -p hp41-core --test lint_stat1_assertions` passes (no Pitfall 14/17 violations in current stat1 test files)
    - The lint scans BOTH external `tests/stat1_*.rs` AND inline `#[cfg(test)]` modules in `src/ops/stat1/*.rs`
    - The file contains `#![allow(clippy::unwrap_used)]` and the Free42 disclaim header
    - stat1_rand_determinism.rs LINT-EXEMPT annotations prevent false positives on exact HpNum equality
  </acceptance_criteria>
  <done>lint_stat1_assertions.rs installed; no_decimal_assert_eq and no_manual_tolerance_pattern gates pass over all stat1 test files; STAT-QUAL-06 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 37 is a pure-test phase. No new IPC endpoints, user inputs, authentication, or data persistence changes.

| Boundary | Description |
|----------|-------------|
| (none) | No new trust boundaries — test files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 37 — zero new runtime or dev-dependencies |

No actionable threats for a test-only plan.
</threat_model>

<verification>
cargo test -p hp41-core --test stat1_op_test_count --test lint_stat1_assertions --test xrom_shadowing -- --nocapture
</verification>

<success_criteria>
- stat1_op_test_count.rs detects all 26 STAT_1 Op variants and reports test coverage per variant
- lint_stat1_assertions.rs passes both Pitfall 14 and Pitfall 17 gates over all stat1 test content
- xrom_shadowing.rs STAT_1 tests pass (attesting STAT-QUAL-08)
- stat1_rand_determinism.rs has LINT-EXEMPT annotations and still passes
</success_criteria>

<output>
Create `.planning/phases/37-test-hardening-quality-gates/37-01-SUMMARY.md` when done
</output>
