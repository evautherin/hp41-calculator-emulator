---
phase: 37-test-hardening-quality-gates
plan: 05
type: execute
wave: 4
depends_on:
  - 37-04
files_modified:
  - hp41-gui/e2e/smoke.spec.js
  - README.md
  - CLAUDE.md
autonomous: false
requirements:
  - STAT-QUAL-01
  - STAT-QUAL-02
  - STAT-QUAL-11

must_haves:
  truths:
    - "E2E smoke includes at least one Stat 1 Pac workflow on Ubuntu"
    - "hp41-core line coverage >= 95.39% (or gap analysis documents the shortfall)"
    - "hp41-core region coverage >= 94.26%"
    - "README.md contains feature-complete hard-claim for Stat 1 Pac"
  artifacts:
    - path: "hp41-gui/e2e/smoke.spec.js"
      provides: "ΣNORMD E2E smoke test"
      contains: "ΣNORMD"
    - path: "README.md"
      provides: "v3.1 Stat 1 Pac hard-claim graduation"
      contains: "feature-complete per Owner"
  key_links:
    - from: "hp41-gui/e2e/smoke.spec.js"
      to: "hp41-gui/src-tauri/src/key_map.rs"
      via: "invokeBackend dispatch_op with xeq_ΣNORMD key ID"
      pattern: "xeq_ΣNORMD"
---

<objective>
Complete Phase 37 with E2E smoke extension, quality-gate graduation, and README hard-claim.

Purpose: This is the FINAL plan of v3.1 milestone. It extends the E2E smoke spec with a Stat 1 Pac workflow (STAT-QUAL-11), verifies all quality gates hold (STAT-QUAL-01/02), and graduates the README soft-claim to a hard-claim (D-35.3 / D-37.11). The hard-claim mirrors the v3.0 D-32.5 graduation pattern where the phase that verified quality gates also graduated the README.

Output: Extended E2E spec, verified quality gates, graduated README, updated CLAUDE.md Phase 37 entry.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-hardening-quality-gates/37-CONTEXT.md
@.planning/phases/37-test-hardening-quality-gates/37-PATTERNS.md
@.planning/phases/37-test-hardening-quality-gates/37-RESEARCH.md (Pattern 5: E2E Smoke)

<interfaces>
<!-- Key interfaces the executor needs -->

From hp41-gui/e2e/smoke.spec.js:
- async function invokeBackend(cmd, args) — calls __TAURI_INTERNALS__.invoke, returns CalcStateView
- async function clickKey(keyId) — clicks SVG keyboard button by data-key-id
- describe('HP-41 Calculator Emulator Smoke', ...) — existing test suite
- it('XEQ "SINH" ...') — invokeBackend dispatch_op fallback pattern (line 214)
- it('XEQ "MATRIX" DET ...') — multi-step modal pipeline pattern (line 267)
- beforeEach: CLX reset via invokeBackend dispatch_op keyId:'clx'
- view.display_str assertion pattern (D-11 no polling)

From hp41-core/src/ops/stat1/modal.rs:
- Stat1Step::NormdModeChoice — mode 1=CDF (upper-tail Q), 2=PDF, 3=inverse
- Modal flow: dispatch Op::SigmaNormdWorkflow -> modal_prompt="ΣNORMD MODE?" -> user enters mode index -> R/S -> submit_step -> result in stack X

From hp41-gui/src-tauri/src/key_map.rs:
- xeq_ prefix pattern: "xeq_SINH" -> Op::Xeq("SINH") -> xrom_resolve -> Op::Sinh
- "xeq_ΣNORMD" -> Op::Xeq("ΣNORMD") -> xrom_resolve -> Op::SigmaNormdWorkflow

From README.md (current soft-claim at line 53-54):
- "Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points, ..."
- Hard-claim graduation adds "feature-complete per Owner's Manual HP 00041-90030"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend E2E smoke.spec.js with ΣNORMD workflow (STAT-QUAL-11)</name>
  <files>hp41-gui/e2e/smoke.spec.js</files>
  <read_first>
    - hp41-gui/e2e/smoke.spec.js (the file being modified — read in full to understand structure, invokeBackend helper, clickKey helper, beforeEach reset)
    - hp41-core/src/ops/stat1/modal.rs (lines 140-177 for NormdModeChoice submit_step semantics — mode 1=CDF, 2=PDF, 3=inverse)
    - hp41-core/src/ops/stat1/normd.rs (op_sigma_normd_eval_cdf — upper-tail Q(x) = 1 - Phi(x))
    - hp41-gui/src-tauri/src/key_map.rs (verify xeq_ prefix pattern for XROM ops, find the exact handling of multi-character xeq_ names containing Unicode)
  </read_first>
  <action>
    Add a new `it()` block to `hp41-gui/e2e/smoke.spec.js` inside the existing `describe(...)` block, AFTER the MATRIX DET test (line 341). The test verifies ΣNORMD upper-tail CDF evaluation of x = 1.96 per D-37.2 (Ubuntu-only).

    The ΣNORMD E2E workflow per modal.rs reconnaissance:

    1. Push z-score 1.96 to stack Y via digit clicks: clickKey('1'), clickKey('decimal'), clickKey('9'), clickKey('6'), clickKey('enter'). The ENTER duplicates X to Y and lifts the stack.

    2. Dispatch ΣNORMD via magic-prefix: `await invokeBackend('dispatch_op', { keyId: 'xeq_ΣNORMD' })`. This opens the NormdModeChoice modal (modal_prompt = "ΣNORMD MODE?"). The z-score 1.96 is now in Y.

    3. Enter mode index 1 (CDF = upper-tail Q). The mode selection through the GUI requires the user to type "1" and press R/S while the modal is active. In the E2E context with invokeBackend, the most reliable path is: `await clickKey('1')` then `const view = await invokeBackend('dispatch_op', { keyId: 'r_s' })`. The R/S key triggers the modal submit (App.tsx invokeForKey R/S 3-way routing). The mode index 1 goes into stack X, submit_step reads it, drops X, pulls z-score 1.96 from Y into X, and evaluates Q(1.96).

    4. Assert `view.display_str` starts with '0.0250' (Q(1.96) = 0.024998... formatted in FIX 4 as 0.0250). Use the same assertion pattern as the SINH test: if `!view.display_str.startsWith('0.0250')` throw Error.

    Important: Between step 2 and step 3, add a brief `browser.waitUntil` timing guard (same pattern as MATRIX DET intermediate steps) to ensure the modal state has settled before entering the mode digit.

    Add the test with a block comment explaining the click-strategy (BROWSER.EXECUTE FALLBACK for xeq_ prefix, REAL CLICKS for digit entry and R/S), the ΣNORMD modal flow, and the D-37.2 Ubuntu-only scope. Include the `// Catches:` annotation.

    FALLBACK: If the `clickKey('1') + invokeBackend('r_s')` path fails because the modal state machine does not properly receive the digit entry, use a pure invokeBackend approach: after dispatching xeq_ΣNORMD, directly set the mode by calling `invokeBackend('dispatch_op', { keyId: '1' })` followed by `invokeBackend('dispatch_op', { keyId: 'r_s' })`. Document whichever approach works.
  </action>
  <verify>
    <automated>grep -c "ΣNORMD" hp41-gui/e2e/smoke.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/e2e/smoke.spec.js contains a new `it()` block referencing ΣNORMD
    - The test pushes 1.96, dispatches xeq_ΣNORMD, enters mode 1 (CDF), and asserts display_str starts with '0.0250'
    - The test uses the invokeBackend fallback pattern for the XEQ dispatch (consistent with SINH/MATRIX DET)
    - The test has a block comment explaining the click-strategy and modal flow
    - `just gui-build` succeeds (the spec compiles/loads without syntax errors)
    - STAT-QUAL-11 gating condition met (at least one Stat 1 Pac workflow in E2E spec)
  </acceptance_criteria>
  <done>E2E smoke extended with ΣNORMD Q(1.96) workflow; STAT-QUAL-11 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Run quality-gate verification and measure final coverage (STAT-QUAL-01, STAT-QUAL-02)</name>
  <files></files>
  <read_first>
    - .planning/phases/37-test-hardening-quality-gates/37-02-SUMMARY.md (modal.rs coverage result)
    - .planning/phases/37-test-hardening-quality-gates/37-03-SUMMARY.md (anova.rs coverage + backward-compat result)
    - .planning/phases/37-test-hardening-quality-gates/37-04-SUMMARY.md (accuracy suite result)
  </read_first>
  <action>
    Run the full quality-gate verification suite:

    1. `just ci` — must exit 0 (lint + test + coverage + license-audit). This runs all hp41-core tests including the new meta-gates, coverage-gap tests, accuracy suite, and backward-compat test.

    2. `just gui-ci` — must exit 0 (GUI build + test + clippy). This covers the hp41-gui side.

    3. `cargo llvm-cov -p hp41-core` — measure aggregate coverage. Record: line coverage %, region coverage %. Target: >= 95.39% lines / >= 94.26% regions (v3.0 baseline).

    4. Per-file stat1/*.rs coverage check: verify every file in `hp41-core/src/ops/stat1/` is >= 90% lines. Use `cargo llvm-cov --html -p hp41-core` and inspect the report.

    5. Document the final metrics in the plan summary:
       - Aggregate hp41-core coverage (lines/regions)
       - Per-file stat1/*.rs coverage table
       - Numerical accuracy pass rate (N/M cases, percentage)
       - v1.x sub-gate result
       - Free42 contamination guard status

    CRITICAL — Aggregate coverage assessment (Pitfall 5 from 37-RESEARCH.md):
    The current aggregate is 93.53% lines. Closing stat1 gaps eliminates ~311 missed lines from a total miss of ~1,571. Even perfect stat1 coverage only reaches ~94.81% lines — below the 95.39% target. The gap is dominated by `ops/program.rs` (82.09%, 366 missed lines) and `ops/stats.rs` (85.80%, 75 missed lines).

    If the aggregate is below 95.39% after all Phase 37 work:
    - Document the measured value and the root cause (non-stat1 files)
    - Check whether the v3.0 baseline of 95.39% was measured with a different code surface (before stat1 was added — adding ~6,824 lines of stat1 source dilutes the denominator)
    - The per-file stat1/*.rs >= 90% floor (STAT-QUAL-03) may be the achievable gate; the aggregate gate may need re-baselining at `/gsd-complete-milestone`
    - Record findings in summary for milestone-completion decision
  </action>
  <verify>
    <automated>just ci 2>&1 | tail -3 && echo "CI_PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `just ci` exits 0 (all hp41-core tests pass including new Phase 37 tests)
    - `just gui-ci` exits 0 (GUI tests pass)
    - Aggregate hp41-core coverage measured and documented
    - Per-file stat1/*.rs coverage all >= 90% (STAT-QUAL-03 met)
    - Numerical accuracy suite passes at >= 98% (STAT-QUAL-04 met)
    - Coverage analysis documented in summary with root-cause if aggregate < 95.39%
  </acceptance_criteria>
  <done>Quality gates verified; final metrics documented; STAT-QUAL-01/02 assessed with aggregate coverage measurement</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 37 quality gates verified. All test infrastructure installed:
    - stat1_op_test_count.rs meta-gate (26 variants, >= 5 tests each)
    - lint_stat1_assertions.rs (Pitfall 14/17 discipline)
    - stat1_modal_coverage.rs (modal.rs 74% -> >= 90%)
    - stat1_anova_coverage.rs (anova.rs 86% -> >= 90%)
    - stat1_backward_compat.rs (v3.0 migration test)
    - numerical_accuracy.rs (~30 Stat 1 cases, >= 98%)
    - smoke.spec.js ΣNORMD E2E workflow
    - D-35-13 STAT-GUI-05 waiver documented
    - Free42 contamination guard re-verified
    - README hard-claim graduation ready

    Coverage metrics and accuracy pass rate documented in Task 2 summary.
  </what-built>
  <how-to-verify>
    1. Review the coverage metrics from Task 2 — do the aggregate numbers satisfy the v3.0 baseline?
    2. If aggregate coverage is below 95.39%: is the shortfall acceptable given the stat1 source addition diluting the denominator?
    3. Confirm README hard-claim graduation is appropriate given the quality-gate results.
    4. Type "graduate" to proceed with README hard-claim, or "defer" if quality gates are not satisfactory.
  </how-to-verify>
  <resume-signal>Type "graduate" to proceed with hard-claim, or "defer" with rationale</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Graduate README soft-claim to hard-claim (D-35.3 / D-37.11)</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (lines 50-55 for current soft-claim text and surrounding context)
    - CLAUDE.md (find the Phase 37 stub "(in progress)" in the v3.1 additions block)
  </read_first>
  <action>
    Step 1 — README.md hard-claim graduation (per D-35.3):
    Replace the current Stat 1 soft-claim line (line 53-54) with the hard-claim version. The current text is:
    `- Stat 1 Pac behavioral emulation (13 programs, 26 XEQ entry points, RAND/SEED extension, [documented divergences](docs/hp41-stat1-divergences.md)) — see [Stat 1 Pac Function Matrix](docs/hp41-stat1-function-matrix.md)`

    Replace with (mirrors the Math Pac I hard-claim at line 51):
    `- Stat 1 Pac behavioral emulation, feature-complete per Owner's Manual HP 00041-90030 (13 programs, 26 XEQ entry points, RAND/SEED extension, [documented divergences](docs/hp41-stat1-divergences.md)) — see [Stat 1 Pac Function Matrix](docs/hp41-stat1-function-matrix.md)`

    The ONLY change is inserting `, feature-complete per Owner's Manual HP 00041-90030` after "behavioral emulation".

    Step 2 — CLAUDE.md Phase 37 entry:
    Find `#### Phase 37 — Test Hardening & Quality Gates (in progress)` in the `### v3.1 additions` block and update:
    - Change "(in progress)" to "(shipped YYYY-MM-DD)" with today's date
    - Add bullet points summarizing Phase 37 deliverables: stat1_op_test_count.rs meta-gate, lint_stat1_assertions.rs, coverage gap closure (modal.rs + anova.rs), backward-compat migration test, numerical accuracy suite extension (~30 cases), E2E smoke ΣNORMD workflow, D-35-13 bounded-iter waiver, Free42 re-verification, README hard-claim graduation
    - Update the header `#### Phase 36 — GUI Integration (in progress)` to "(shipped 2026-05-24)" if not already updated
    - Update "Phases 33-35 -- 36-37 IN PROGRESS" header to "Phases 33-37" without the IN PROGRESS suffix

    Step 3 — Also update the `**Frozen invariants preserved across v3.1 (so far):**` suffix to remove "(so far)" since v3.1 is now complete.
  </action>
  <verify>
    <automated>grep -c "feature-complete per Owner.*00041-90030" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 53-54 contains "feature-complete per Owner's Manual HP 00041-90030"
    - README.md still contains the Math Pac I hard-claim on line 51 (no regression)
    - CLAUDE.md Phase 37 entry shows "(shipped YYYY-MM-DD)" with correct date
    - CLAUDE.md v3.1 header no longer says "IN PROGRESS"
    - CLAUDE.md frozen-invariants block no longer says "(so far)"
    - `grep -c "feature-complete" README.md` returns 3 (HP-41CV + Math 1 + Stat 1)
  </acceptance_criteria>
  <done>README hard-claim graduated; CLAUDE.md Phase 37 entry finalized; v3.1 milestone quality-gated and documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | E2E spec + documentation only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
grep "feature-complete per Owner.*00041-90030" README.md && grep "ΣNORMD" hp41-gui/e2e/smoke.spec.js && just ci
</verification>

<success_criteria>
- E2E smoke spec contains ΣNORMD workflow
- Quality gates verified with documented metrics
- README hard-claim graduated from soft-claim
- CLAUDE.md v3.1 Phase 37 entry finalized
- `just ci` passes
</success_criteria>

<output>
Create `.planning/phases/37-test-hardening-quality-gates/37-05-SUMMARY.md` when done
</output>
