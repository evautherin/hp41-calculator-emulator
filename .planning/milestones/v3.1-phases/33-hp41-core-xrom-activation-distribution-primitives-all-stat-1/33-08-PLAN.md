---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 08
plan_id: 33-08
type: execute
wave: 3
depends_on: ["33-01", "33-05"]
files_modified:
  - hp41-core/src/ops/stat1/regression.rs
  - hp41-core/src/ops/stat1/rand.rs
  - hp41-core/src/ops/stat1/modal.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/num.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/tests/stat1_rand_determinism.rs
autonomous: true
requirements:
  - STAT-REG-05
  - STAT-REG-06
  - STAT-REG-07
  - STAT-REG-08
  - STAT-REG-09
  - STAT-RNG-01
  - STAT-RNG-02
  - STAT-RNG-04

must_haves:
  truths:
    - "XEQ \"ΣMLRXY\" with 2-predictor oracle dataset returns partial coefficients within 1e-7"
    - "XEQ \"ΣMLRXYZ\" with 3-predictor oracle dataset returns partial coefficients within 1e-7"
    - "XEQ \"ΣPOLYP\" opens a modal degree prompt; submit d → accumulate (x,y) → compute (d+1)×(d+1) coefficients via LOCAL Gauss elimination"
    - "XEQ \"ΣPOLYC\" Horner-evaluates the most-recent ΣPOLYP coefficients at stack X"
    - "grep -n \"ops::math1::matrix\" hp41-core/src/ops/stat1/ returns empty (no cross-XROM coupling)"
    - "XEQ \"RAND\" with rand_seed=0.5 returns FRC(9821·0.5 + 0.211327) = 0.711327"
    - "XEQ \"SEED\" opens an ALPHA prompt SEED?; submit value updates state.rand_seed"
    - "state.rand_seed survives serde save/load round-trip; second-instance replay yields identical RAND sequence"
    - "Op::Stat1Stub is DELETED from the Op enum; all 26 STAT_1.ops entries point to real Sigma*/Rand/Seed variants"
  artifacts:
    - path: "hp41-core/src/ops/stat1/regression.rs"
      provides: "ΣMLRXY + ΣMLRXYZ + ΣPOLYP + ΣPOLYC with local Gauss elimination (no math1::matrix imports)"
      contains: "fn solve_normal_equations"
    - path: "hp41-core/src/ops/stat1/rand.rs"
      provides: "RAND (LCG) + SEED (ALPHA-prompt) using state.rand_seed"
      contains: "pub fn op_rand"
    - path: "hp41-core/tests/stat1_rand_determinism.rs"
      provides: "Save/load round-trip + sequence determinism integration test"
      contains: "rand_sequence_deterministic_after_save_load"
  key_links:
    - from: "hp41-core/src/ops/stat1/regression.rs"
      to: "fn solve_normal_equations (private)"
      via: "self-contained Gauss elimination with partial pivoting — NO math1::matrix imports"
      pattern: "Gauss"
    - from: "hp41-core/src/ops/stat1/rand.rs"
      to: "state.rand_seed"
      via: "LCG step + FRC + write-back"
      pattern: "rand_seed"
---

<objective>
Wave 3 — FINAL Stat 1 Pac Op cluster. Implements ΣMLRXY (2-predictor) + ΣMLRXYZ (3-predictor) multiple regression with SELF-CONTAINED Gauss elimination (REJECTING math1::matrix per SPEC.md Req. 21); ΣPOLYP polynomial regression with modal degree-prompt + (d+1)×(d+1) normal-equation solve; ΣPOLYC Horner evaluation; RAND/SEED LCG-based RNG using `state.rand_seed` shipped in Plan 33-01. Adds the `Stat1Step::PolypDegreePrompt` + `Stat1Step::SeedPrompt` modal variants. Adds `HpNum::trunc_int` helper if absent. **Final clean-up: DELETE `Op::Stat1Stub` from the Op enum + all dispatch/execute_op/stat1/mod.rs references; all 26 STAT_1.ops entries now point to real Sigma*/Rand/Seed variants.**

Purpose: Six Ops complete the Stat 1 Pac feature set. SPEC.md Req. 19, 20, 21 (multiple regression with local Gauss + matrix-reuse forbidden), 22, 23 (polynomial), 35, 36, 37 (RAND/SEED determinism + serde) all green. The Stat 1 Pac module is feature-complete in hp41-core; Phases 34/36 wire CLI + GUI; Phase 35 docs; Phase 37 hardens.

Output: hp41-core compiles green WITHOUT Op::Stat1Stub; regression.rs ≤300 LOC (extended for 4 more Ops + Gauss helper); rand.rs ≤300 LOC; stat1_rand_determinism integration test passes; 6 final stub swaps + Stub variant deletion; xrom.rs STAT_1.ops 26-of-26 real-variant coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-03-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-05-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-07-SUMMARY.md
@CLAUDE.md

<interfaces>
From hp41-core/src/state.rs (post-Plan-33-01):
- `pub rand_seed: HpNum` with `#[serde(default)]` (NOT skip) — used by RAND/SEED.

From hp41-core/src/ops/stat1/modal.rs (post-Plan-33-03):
- `pub enum Stat1Step { NormdModeChoice, ChisqdNuPrompt, ChisqdModeChoice }` — extended in this plan with PolypDegreePrompt(u8), SeedPrompt.

From hp41-core/src/ops/stat1/regression.rs (post-Plan-33-05):
- ΣLIN/EXP/LOGI/POW already shipped; this plan extends with ΣMLRXY, ΣMLRXYZ, ΣPOLYP, ΣPOLYC + private `solve_normal_equations` Gauss helper.

From hp41-core/src/num.rs:
- Per RESEARCH.md Open Q 1: may need to ADD `HpNum::trunc_int` (Decimal::trunc wrapper) for the FRC operation in RAND.

From PATTERNS.md §"hp41-core/src/ops/stat1/regression.rs" + §"hp41-core/src/ops/stat1/rand.rs" (line 504–632):
- Self-contained Gauss elimination shell signature + full LCG body.

From SPEC.md Req. 21:
- CI gate: `grep -n "ops::math1::matrix" hp41-core/src/ops/stat1/` returns empty.

From RESEARCH.md §"RAND / SEED — Emulator Extension" (line 854–905):
- LCG formula: r_{n+1} = FRC(9821 · r_n + 0.211327)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ΣMLRXY + ΣMLRXYZ + ΣPOLYP + ΣPOLYC with self-contained Gauss elimination (no math1::matrix imports)</name>
  <files>hp41-core/src/ops/stat1/regression.rs, hp41-core/src/ops/stat1/modal.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/regression.rs (post-Plan-33-05; extend with 4 new Ops + Gauss helper; verify LOC budget allows extension)
    - hp41-core/src/ops/stat1/mod.rs (verify STAT1_MLRXY_*_REG, STAT1_MLRXYZ_*_REG, STAT1_POLYP_COEF_BASE_REG named consts exist; ADD with OM page citations if absent)
    - hp41-core/src/ops/stat1/modal.rs (post-Plan-33-03; extend Stat1Step with PolypDegreePrompt(u8))
    - hp41-core/src/ops/math1/poly.rs (lines 80–90 — op_poly_workflow modal opener template; DEGREE=? precedent for ΣPOLYP)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/regression.rs" §"Analog 2" (line 518–537)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Regression Family — ΣMLRXY / ΣMLRXYZ" (line 813–836) + §"ΣPOLYP / ΣPOLYC" (line 841–848)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 19, 20, 21, 22, 23 (line 150–173)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣMLRXY/ΣMLRXYZ/ΣPOLYP/ΣPOLYC
  </read_first>
  <action>
    1. Extend `hp41-core/src/ops/stat1/regression.rs` (post-Plan-33-05) with 4 new Ops + 1 private helper.

    2. Add OM-traceable named consts to `stat1/mod.rs` if absent: `STAT1_MLRXY_*_REG` (9 registers for 2-predictor per RESEARCH.md line 514), `STAT1_MLRXYZ_*_REG` (14 minimum for 3-predictor), `STAT1_POLYP_COEF_BASE_REG` + `STAT1_POLYP_DEGREE_REG`.

    3. Add 4 Op variants: `Op::SigmaMlrxy`, `Op::SigmaMlrxyz`, `Op::SigmaPolypWorkflow`, `Op::SigmaPolyc`.

    4. Implement `fn solve_normal_equations(matrix: &mut Vec<Vec<HpNum>>, rhs: &mut Vec<HpNum>) -> Result<Vec<HpNum>, HpError>` — private, standard Gauss elimination with partial pivoting on the augmented matrix; returns the solution vector; returns Err(HpError::Domain) for singular matrices (pivot < eps after elimination step). Use HpNum arithmetic throughout; no f64 conversion. Use Vec rather than const generics to avoid MSRV-1.88 const-generic constraints.

    5. Implement `pub fn op_sigma_mlrxy(state: &mut CalcState) -> Result<(), HpError>`:
       - SIZE-floor guard with STAT1_MAX_REG.
       - Read 9 named-const registers per OM ΣMLRXY layout (Σx₁, Σx₂, Σy, Σx₁², Σx₂², Σx₁x₂, Σx₁y, Σx₂y, n).
       - Build 3×3 augmented matrix per RESEARCH.md line 814–818.
       - Call solve_normal_equations to get (b₀, b₁, b₂).
       - Push results per OM (likely b₂ to X, b₁ to Y, b₀ to Z). LiftEffect::Enable.
       - Tolerance 1e-7 iterative per SPEC.md Req. 19.

    6. Implement `pub fn op_sigma_mlrxyz(state: &mut CalcState) -> Result<(), HpError>` — same shape with 4×4 normal-equation matrix (14 named-const register reads). Tolerance 1e-7.

    7. Implement `pub fn op_sigma_polyp_workflow(state: &mut CalcState) -> Result<(), HpError>` — modal opener per math1/poly.rs op_poly_workflow:
       - `state.modal_program = Some(ModalProgram::Stat1(Stat1Step::PolypDegreePrompt(0)));`
       - Prompt string: tentative `"DEGREE=?"` per SPEC.md Req. 22 OM override clause; confirm via OM read; if different, use OM string and document divergence inline.
       - `state.modal_prompt = Some(prompt_string.to_string());` + `state.print_buffer.push(prompt_string.to_string());`

    8. Extend `hp41-core/src/ops/stat1/modal.rs::Stat1Step` enum with `PolypDegreePrompt(u8)`. Update three dispatch impls — when PolypDegreePrompt(0) submit, consume stack X as degree d (cast to u8, error if d < 1 or d > 5 per Math Pac I POLY precedent), store d in STAT1_POLYP_DEGREE_REG. Per OM ΣPOLYP user flow: PolypDegreePrompt(d) then acts as "accumulation-ready" state; user uses Σ+ for each (x,y) pair; the compute step is triggered (per OM-specified mechanism — possibly via ΣPOLYP being re-invoked, or via a separate submit). Read OM §ΣPOLYP for the user-flow specification. The SPEC.md Req. 22 acceptance only requires that with d=2 and y=x², coefficients are (a₀=0, a₁=0, a₂=1) within 1e-7 — this can be tested by pre-populating the accumulated registers and calling the compute step directly, bypassing the modal flow.

    9. Implement `pub fn op_sigma_polyc(state: &mut CalcState) -> Result<(), HpError>`:
       - SIZE-floor guard.
       - Read d from STAT1_POLYP_DEGREE_REG; read coefficients from STAT1_POLYP_COEF_BASE_REG..+d+1.
       - Read x_eval from stack X.
       - Horner: `let mut acc = coef[d]; for i in (0..d).rev() { acc = acc.checked_mul(&x_eval)?.checked_add(&coef[i])?; }`.
       - Push acc to stack X; LiftEffect::Enable.
       - Tolerance 1e-9 closed-form per SPEC.md Req. 23.

    10. Dispatch + execute_op + xrom.rs stub swaps for all 4 Ops (16 sites).

    11. Tests in regression.rs:
        - `mlrxy_two_predictor_oracle`: oracle dataset from scipy least-squares; coefficients within 1e-7.
        - `mlrxyz_three_predictor_oracle`: scipy reference; coefficients within 1e-7.
        - `polyp_d2_y_eq_x_sq_oracle` (SPEC.md Req. 22): with d=2 and (x,y) pairs from [(1,1),(2,4),(3,9),(4,16),(5,25)], pre-populate the Σ-register state, call the compute step directly; assert coefficients (a₀=0, a₁=0, a₂=1) within 1e-7.
        - `polyc_d2_at_x_6` (SPEC.md Req. 23): chained after polyp test, evaluate at x=6 → ŷ = 36.0 within 1e-9.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::regression::tests -- --quiet && grep -nE "ops::math1::matrix" hp41-core/src/ops/stat1/; test $? -ne 0  # grep returns 1 (no matches) — SPEC Req. 21 acceptance gate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'pub fn op_sigma_(mlrxy|mlrxyz|polyp_workflow|polyc)' hp41-core/src/ops/stat1/regression.rs` returns 4.
    - `grep -cE 'fn solve_normal_equations|Gauss' hp41-core/src/ops/stat1/regression.rs` returns >=1.
    - `grep -nE 'ops::math1::matrix' hp41-core/src/ops/stat1/` returns 0 lines (SPEC.md Req. 21 CI gate).
    - `grep -cE 'Op::SigmaMlrxy|Op::SigmaMlrxyz|Op::SigmaPolypWorkflow|Op::SigmaPolyc' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=16.
    - `grep -c 'PolypDegreePrompt' hp41-core/src/ops/stat1/modal.rs` returns >=2.
    - `cargo test -p hp41-core --lib ops::stat1::regression::tests` exits 0 (Plan-33-05 ΣLIN/EXP/LOGI/POW tests still green + new MLR/POLYP/POLYC tests green).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (cumulative 24 of 26 stubs swapped).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/regression.rs | wc -l` ≤ 300.
  </acceptance_criteria>
  <done>ΣMLRXY/ΣMLRXYZ/ΣPOLYP/ΣPOLYC ship with self-contained Gauss elimination (no math1::matrix imports per SPEC.md Req. 21 CI gate); modal-prompt degree workflow operational; SPEC.md Reqs 19, 20, 21, 22, 23 acceptance green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RAND + SEED + add HpNum::trunc_int helper if absent + ship stat1_rand_determinism integration test + DELETE Op::Stat1Stub scaffolding</name>
  <files>hp41-core/src/num.rs, hp41-core/src/ops/stat1/rand.rs, hp41-core/src/ops/stat1/modal.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs, hp41-core/tests/stat1_rand_determinism.rs</files>
  <read_first>
    - hp41-core/src/num.rs (lines 1–260; grep for `checked_int|trunc_int` — add helper if absent per RESEARCH.md Open Q 1)
    - hp41-core/src/ops/math.rs (`op_frc` function for FRC pattern reference)
    - hp41-core/src/state.rs (post-Plan-33-01; `rand_seed: HpNum` field in place)
    - hp41-core/src/ops/stat1/modal.rs (post-Task-1; extend Stat1Step with SeedPrompt)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/rand.rs" (line 587–632)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"RAND / SEED — Emulator Extension" (line 854–905)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 35, 36, 37, 38 (line 248–266)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md §D-33.4 + D-33.4a (line 67–72)
  </read_first>
  <action>
    1. Check `hp41-core/src/num.rs` for `HpNum::checked_int` or `HpNum::trunc_int`. If absent, add:
       ```
       /// Truncate toward zero (integer part of self). Mirrors rust_decimal::Decimal::trunc.
       /// Used by FRC operations: FRC(x) = x − trunc(x).
       pub fn trunc_int(&self) -> HpNum {
           HpNum::rounded(self.0.trunc())
       }
       ```
       Verify rust_decimal::Decimal 1.42 has `trunc()` method (it does; check docs.rs if uncertain).

    2. Per D-33.4 / SPEC.md Req. 38: read OM 00041-90030 ROM listing to confirm whether RAND/SEED ARE top-level Stat 1 Pac ROM entries (per RESEARCH.md A5: QRC does NOT list them but community sources confirm). Record the OM read result in this plan's SUMMARY.md for Phase 35 divergences-catalog routing (D-33.4 → bucket "emulator extension"; D-33.4a → bucket "OM-feature-complete").

    3. Create `hp41-core/src/ops/stat1/rand.rs`. Disclaim header per PATTERNS.md.

    4. Add `pub mod rand;` to `stat1/mod.rs`.

    5. Add `Op::Rand` and `Op::Seed` to the `Op` enum. Doc-comments cite D-33.4 (emulator extension).

    6. Implement `pub fn op_rand(state: &mut CalcState) -> Result<(), HpError>` per PATTERNS.md line 617–631:
       - `let multiplier = HpNum::from_str("9821").map_err(|_| HpError::Domain)?;`
       - `let increment = HpNum::from_str("0.211327").map_err(|_| HpError::Domain)?;`
       - `let stepped = state.rand_seed.checked_mul(&multiplier)?.checked_add(&increment)?;`
       - `let int_part = stepped.trunc_int(); let new_seed = stepped.checked_sub(&int_part)?;`
       - `state.rand_seed = new_seed.clone();`
       - Push new_seed to stack X using the project's stack-lift helpers (verify exact API by reading hp41-core/src/ops/math1/poly.rs or similar Math Pac I file that pushes results).
       - LiftEffect::Enable.

    7. Implement `pub fn op_seed(state: &mut CalcState) -> Result<(), HpError>` per PATTERNS.md line 615:
       - `state.modal_program = Some(ModalProgram::Stat1(Stat1Step::SeedPrompt));`
       - `state.modal_prompt = Some("SEED?".to_string());`
       - `state.print_buffer.push("SEED?".to_string());`

    8. Extend `stat1/modal.rs::Stat1Step` enum with `SeedPrompt`. Update dispatch impls:
       - `submit_step` arm for SeedPrompt → `state.rand_seed = state.stack.x.clone();` → clear modal_program and modal_prompt → return Ok(()).
       - `current_prompt` arm for SeedPrompt → `Some("SEED?".to_string())`.
       - `requires_alpha_label` arm for SeedPrompt → `false`.
       - Update PolypDegreePrompt(u8) arms from Task 1 similarly: current_prompt returns the OM-confirmed prompt string per the collection index; requires_alpha_label returns false.

    9. Dispatch + execute_op + xrom.rs stub swaps for RAND and SEED (8 sites). After this step, ALL 26 STAT_1.ops entries point to real variants.

    10. **STUB CLEANUP** (final Phase 33 deliverable):
        - Verify all 26 STAT_1.ops entries point to real Sigma*/Rand/Seed variants via `grep -c 'Op::Stat1Stub' hp41-core/src/ops/math1/xrom.rs` returning 0.
        - DELETE the `Op::Stat1Stub` enum variant from `hp41-core/src/ops/mod.rs`.
        - DELETE the `Op::Stat1Stub => op_stat1_stub(state)` dispatch arm in `ops/mod.rs::dispatch()`.
        - DELETE the parallel arm in `ops/program.rs::execute_op()`.
        - DELETE the `pub fn op_stat1_stub` function in `ops/stat1/mod.rs`.
        - Verify `cargo check -p hp41-core` clean (no orphan references).

    11. Create `hp41-core/tests/stat1_rand_determinism.rs` integration test (Wave 0 deliverable per VALIDATION.md). Byte-for-byte disclaim header.
        - Test `rand_sequence_deterministic_after_save_load` (SPEC.md Req. 37):
          - Construct CalcState; `state.rand_seed = HpNum::from_str("0.7").unwrap();`.
          - Call op_rand 3 times; record results.
          - Serialize state to JSON; deserialize into a fresh CalcState.
          - Call op_rand 3 times on restored state; record results.
          - Assert the two 3-tuples are IDENTICAL via HpNum equality (decimal-exact, no tolerance per Req. 35).
        - Test `rand_lcg_formula_first_iter` (SPEC.md Req. 35):
          - rand_seed = 0.5; call op_rand once; expected result FRC(9821·0.5 + 0.211327) = FRC(4910.711327) = 0.711327 EXACTLY in HpNum.
        - Test `seed_modal_round_trip` (SPEC.md Req. 36):
          - Construct CalcState; call op_seed → assert modal_program/modal_prompt are set as expected.
          - Push 0.5 to stack X; call the modal-submit entry point (`crate::ops::math1::submit_step(state)` or equivalent — confirm exact symbol via grep on hp41-core/src/ops/math1/mod.rs); assert state.rand_seed equals HpNum::from_str("0.5")?; assert modal cleared.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::rand::tests -- --quiet && cargo test -p hp41-core --test stat1_rand_determinism -- --quiet && cargo check -p hp41-core && bash scripts/check-free42-contamination.sh && cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/rand.rs` succeeds.
    - `test -f hp41-core/tests/stat1_rand_determinism.rs` succeeds.
    - `grep -cE 'pub fn op_rand|pub fn op_seed' hp41-core/src/ops/stat1/rand.rs` returns 2.
    - `grep -c 'state.rand_seed' hp41-core/src/ops/stat1/rand.rs` returns >=2 (read + write).
    - `grep -c 'trunc_int\|checked_int' hp41-core/src/ops/stat1/rand.rs hp41-core/src/num.rs` returns >=2 (helper used in rand.rs and declared in num.rs).
    - `grep -cE 'Op::Rand|Op::Seed' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=8.
    - `grep -c 'SeedPrompt' hp41-core/src/ops/stat1/modal.rs` returns >=2 (variant + dispatch arms).
    - `grep -c 'Op::Stat1Stub' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs hp41-core/src/ops/stat1/mod.rs` returns 0 (STUB CLEANUP successful).
    - `grep -c 'op_stat1_stub' hp41-core/src/ops/stat1/mod.rs` returns 0 (function deleted).
    - `cargo test -p hp41-core --lib ops::stat1::rand::tests` exits 0.
    - `cargo test -p hp41-core --test stat1_rand_determinism` exits 0 (all 3 integration tests).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (final 26 of 26 stubs swapped — full STAT_1.ops coverage).
    - `cargo test -p hp41-core --test xrom_shadowing` exits 0.
    - `cargo test -p hp41-core --test v3_save_compat` exits 0 (no backward-compat regression).
    - `cargo check -p hp41-core` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - `bash scripts/check-free42-contamination.sh` exits 0.
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/rand.rs | wc -l` ≤ 300.
  </acceptance_criteria>
  <done>RAND + SEED ship using state.rand_seed (with the unique #[serde(default)]-no-skip serde shape from Plan 33-01); RAND determinism survives save/load round-trip via integration test; SEED modal round-trip works; Op::Stat1Stub scaffolding fully removed from the codebase; all 26 STAT_1.ops entries point to real variants; SPEC.md Reqs 35, 36, 37 acceptance green; STAT-RNG-04 emulator-extension status confirmed for Phase 35 docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| math1/matrix ↔ stat1/regression boundary | SPEC.md Req. 21 LOCKS local Gauss elimination — cross-XROM coupling via `ops::math1::matrix::*` imports would silently couple Stat 1 to Math 1's loaded-state, breaking the bit-1-only-loaded scenario. |
| state.rand_seed ↔ serde JSON | Plan 33-01 shipped the field with `#[serde(default)]` WITHOUT skip; the integration test in this plan PROVES the round-trip empirically — failure would silently break determinism for users with reproducible-simulation workflows. |
| LCG arithmetic ↔ HpNum decimal-exact | The LCG formula `r_{n+1} = FRC(9821·r_n + 0.211327)` MUST run in HpNum (rust_decimal) not f64 — converting through f64 would silently introduce drift; the SPEC.md Req. 35 acceptance requires HpNum-equality (no tolerance). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering | ΣMLRXY/MLRXYZ/POLYP normal-equation Gauss elimination | mitigate | Local implementation in stat1/regression.rs; partial pivoting prevents silent-singularity passes (Err(HpError::Domain) on small pivots); oracle tests against scipy reference catch any algorithmic drift within 1e-7 iterative tolerance. |
| T-33-numerical-precision | Tampering (cross-XROM coupling SPEC.md Req. 21) | hp41-core/src/ops/stat1/regression.rs | mitigate | Task 1 acceptance includes `grep -nE 'ops::math1::matrix' hp41-core/src/ops/stat1/` returning empty (zero matches) — CI gate per SPEC.md Req. 21. |
| T-33-state-persistence | Tampering (rand_seed serde shape) | hp41-core/tests/stat1_rand_determinism.rs | mitigate | Integration test `rand_sequence_deterministic_after_save_load` empirically proves the round-trip works after Plan 33-01's field declaration; failure here would also signal a Plan 33-01 regression (the Plan-33-01 `rand_seed_serde_round_trip` lib test catches it earlier). |
| T-33-numerical-precision | Tampering (decimal-exact LCG drift) | RAND LCG body | mitigate | The 9821 multiplier and 0.211327 increment are constructed via HpNum::from_str — exact-decimal parse, not f64 conversion. The test `rand_lcg_formula_first_iter` asserts HpNum equality (no tolerance) for the first iteration; any drift surfaces immediately. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/xrom.rs (final 6 stub swaps + Op::Stat1Stub deletion repercussions) | mitigate | After this plan: `git diff develop -- hp41-core/src/ops/math1/` lists ONLY `xrom.rs` (D-33.3) and `modal.rs` (D-33.3b); all 26 STAT_1.ops entries point to real variants; `Op::Stat1Stub` is fully removed (no scaffolding remains for Phase 34/36 to inherit). |
| T-33-free42-contamination | Tampering | regression.rs (Gauss) + rand.rs (LCG) + stat1_rand_determinism.rs | mitigate | Disclaim headers present in all new files; algorithm citations: Gauss elimination is standard textbook (cite Numerical Recipes 3e §2.1 if needed; do NOT cite Free42); LCG cites NPS p. 21 + Don Malm + HP-41C Standard Applications (RESEARCH.md §"RAND / SEED" line 862–865). `bash scripts/check-free42-contamination.sh` exits 0. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1` exits 0 (full suite green: distributions + normd + chisqd + basic_stats + moments + anova + regression + hypothesis + nonparam + rand).
- `cargo test -p hp41-core --test stat1_rand_determinism` exits 0.
- `cargo test -p hp41-core --test stat1_cancellation` exits 0.
- `cargo test -p hp41-core --test xrom_shadowing` exits 0.
- `cargo test -p hp41-core --test v3_save_compat` exits 0.
- `bash scripts/check-free42-contamination.sh` exits 0.
- `grep -nE 'ops::math1::matrix' hp41-core/src/ops/stat1/` returns 0 lines (SPEC.md Req. 21 CI gate).
- `grep -c 'Op::Stat1Stub' hp41-core/src/` returns 0 (scaffolding fully removed).
- `grep -c 'STAT_1.ops' hp41-core/src/ops/math1/xrom.rs` returns >=1 AND the STAT_1.ops slice contains 26 real variant references (no Op::Stat1Stub).
- File-level audit: regression.rs ≤300 LOC; rand.rs ≤300 LOC.
- Expected: `cargo check -p hp41-cli` and `cargo check -p hp41-gui` STILL fail on missing op_display_name arms for ~24 new Sigma*/Rand/Seed variants (Phase 34 + 36 fix). This proves the 4-way invariant is enforced.
- `git diff develop -- hp41-core/src/ops/math1/ | grep '+++ b/' | sort -u` lists ONLY `math1/modal.rs` and `math1/xrom.rs`.
</verification>

<success_criteria>
PHASE 33 COMPLETE. All 39 STAT requirements covered; all 46 SPEC.md acceptance criteria green; the math1/ freeze is preserved (only xrom.rs + modal.rs touched per D-33.3 / D-33.3b); the Free42 contamination guard runs clean across math1/ + stat1/; `rand_seed` survives serde; XROM ID 2 STAT_1 resolves end-to-end; 26 STAT_1.ops entries all point to real, OM-traceable Sigma*/Rand/Seed variants. Phase 34 (CLI integration) can now begin by adding op_display_name arms for ~24 new Op variants.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-08-SUMMARY.md` when done summarizing:
- The OM ROM-listing read result for RAND/SEED (D-33.4 vs D-33.4a routing for Phase 35 divergences catalog).
- The HpNum::trunc_int decision (added or pre-existing).
- The Op::Stat1Stub cleanup result (final 0 references confirmed).
- The final SPEC.md Acceptance Criteria scorecard (25 criteria → 25 green).
- The full set of D-33.x divergences/discrepancies surfaced across all 9 plans (ΣSPEAR 0.7 vs 0.8, ΣEFXSQ 1.667 vs scipy, ΣBSTAT CV variance, etc.) — handoff bundle for Phase 35 divergences catalog.
- The cumulative LOC count for each stat1/*.rs file (verify all ≤300 excluding tests per D-33.5).
- Confirmation that `git diff develop -- hp41-core/src/ops/math1/` lists exactly two files (xrom.rs + modal.rs) — math1/ freeze preserved.
- Handoff notes for Phase 34: list of all ~24 Sigma*/Rand/Seed Op variants needing op_display_name arms in hp41-cli/src/prgm_display.rs; pointer to docs/hp41-stat1-functions.json schema (Phase 34 creates the file).
</output>
