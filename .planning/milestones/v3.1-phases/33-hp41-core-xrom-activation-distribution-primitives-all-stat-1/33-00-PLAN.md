---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 00
plan_id: 33-00
type: execute
wave: 0
depends_on: []
files_modified:
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - scripts/check-free42-contamination.sh
autonomous: true
requirements:
  - STAT-UNI-03
  - STAT-QUAL-09

must_haves:
  truths:
    - "Stat 1 Pac OM Storage Registers layout is the single in-repo source of truth before any Stat 1 Op reads from registers"
    - "Free42 stats-domain identifiers are caught by check-free42-contamination.sh BEFORE the first stat1/*.rs algorithm file lands"
    - "The hp41-core/src/ops/stat1/ directory exists and is mounted in ops/mod.rs"
  artifacts:
    - path: "hp41-core/src/ops/stat1/mod.rs"
      provides: "Stat 1 module hub + OM `//!` Storage Registers transcription + STAT1_MAX_REG const + STAT1_*_REG named consts"
      contains: "STAT1_MAX_REG"
      min_lines: 60
    - path: "scripts/check-free42-contamination.sh"
      provides: "Extended PATTERN (>=18 tokens) + STAT1_DIR scan loop"
      contains: "STAT1_DIR"
  key_links:
    - from: "hp41-core/src/ops/mod.rs"
      to: "hp41-core/src/ops/stat1/mod.rs"
      via: "pub mod stat1;"
      pattern: "pub mod stat1;"
    - from: "scripts/check-free42-contamination.sh"
      to: "hp41-core/src/ops/stat1/"
      via: "for dir in MATH1_DIR STAT1_DIR loop"
      pattern: "STAT1_DIR=.hp41-core/src/ops/stat1"
---

<objective>
Wave 0 foundation plan: extend the Free42 contamination guard with stats-domain identifiers AND ship the `hp41-core/src/ops/stat1/mod.rs` skeleton containing the verbatim OM 00041-90030 Storage Registers transcription as a `//!` doc-comment header plus the `STAT1_MAX_REG` and per-domain `STAT1_*_REG` named constants. NO Op code in this plan — pure infrastructure that subsequent plans depend on.

Purpose: D-33.8 ordered STAT-QUAL-09 ahead of any `stat1/*.rs` algorithm file so the contamination guard catches anything BEFORE it lands. SPEC.md Req. 9 (P21 mitigation) makes `mod.rs` the single source of truth for register indices used by ANOVA, moments, regression, contingency.

Output: extended `scripts/check-free42-contamination.sh` + new `hp41-core/src/ops/stat1/mod.rs` + `pub mod stat1;` in `hp41-core/src/ops/mod.rs`. CI green; `bash scripts/check-free42-contamination.sh` exits 0; probe-token in stat1/ exits 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing check-free42-contamination.sh shape (12 tokens, math1/-scoped). -->
<!-- Plan 33-00 extends PATTERN by >=6 stats-domain tokens AND adds STAT1_DIR scan loop. -->

From scripts/check-free42-contamination.sh:
- `MATH1_DIR="hp41-core/src/ops/math1"`
- `DISCLAIM_LINE='Free42 source consulted only as sanity-check oracle'`
- `PATTERN='phloat|Phloat|bid128_|decNumber|decContext|vartype|arg_struct|prgm_lines|bcd_t|Thomas Okken|AGPL|GNU General Public License'`
- Loop body: `grep -rn -E "$PATTERN" "$MATH1_DIR" | grep -v "$DISCLAIM_LINE"`

From hp41-core/src/ops/mod.rs (existing module declarations):
- `pub mod math1;` (line 17) — Stat 1 sibling mounts directly below or grouped.

From PATTERNS.md §"mod.rs module-hub" + §"OM disclaim header" + §"Free42 contamination guard":
- Disclaim header text MUST be byte-for-byte: "Algorithm independently re-derived from HP Stat 1 Pac Owner's Manual 00041-90030 (1979); Free42 source consulted only as sanity-check oracle, not copied."
- DISCLAIM_LINE substring stays identical so existing allow-list logic continues to work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-free42-contamination.sh with stats-domain tokens and STAT1_DIR scan loop</name>
  <files>scripts/check-free42-contamination.sh</files>
  <read_first>
    - scripts/check-free42-contamination.sh (full file — 37 lines; verify current PATTERN/MATH1_DIR/DISCLAIM_LINE structure before editing)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"scripts/check-free42-contamination.sh (PLAN 33-00 — STAT-QUAL-09 reassigned per D-33.8)" (line 1199–1269 — full extended-script template)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Pitfall 5: Free42 distinctive symbol copy-paste from core_math2.cc (P27 carry-forward)" (line 954–965 — candidate identifier list)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md §"Free42 Contamination Guard (D-33.8 reassignment)" Req. 39 (line 270–278 — acceptance contract)
  </read_first>
  <action>
    Open `github.com/thomasokken/free42/blob/master/common/core_math2.cc` (via WebFetch or browser) and finalize a concrete list of >=6 stats-domain tokens from PATTERNS.md candidates: prefer (a) function-name conventions actually present in core_math2.cc (e.g. tokens matching `math_normal_`, `math_chi2_`, `math_t_dist_`, `math_F_dist_`, `math_gamma_`, `math_beta_inc` regex prefixes), (b) decNumber distinctive idioms not already in the 12-symbol base. If a candidate is not present in core_math2.cc, drop it and substitute the next strongest concrete identifier from the file — record the final list in the file header comment for review.

    Edit `scripts/check-free42-contamination.sh` as follows:
    1. Add `STAT1_DIR="hp41-core/src/ops/stat1"` immediately after the existing `MATH1_DIR=` line.
    2. Replace the single-directory existence check with a loop iterating both `$MATH1_DIR` and `$STAT1_DIR` (exit 2 on either missing).
    3. Replace `PATTERN=` with the extended pattern: 12 existing tokens + >=6 finalized stats-domain tokens, joined by `|` in a single quoted string. Total token count >=18 (each `|`-delimited segment counts as 1 token).
    4. Replace the single `grep -rn -E "$PATTERN" "$MATH1_DIR"` block with a loop iterating both directories; same `grep -v "$DISCLAIM_LINE"` filter; same exit-1 behavior on contamination match.
    5. Update the success message (`echo "OK: ..."`) to enumerate both directories.
    6. Update the header comment (D-32.7 line, currently ~line 7) to D-33.8 amendment note: "Extended in Plan 33-00 per D-33.8 (STAT-QUAL-09 reassignment); 12 math1-era symbols + N stats-domain symbols sourced from core_math2.cc on 2026-05-22."

    The DISCLAIM_LINE substring MUST remain byte-for-byte `'Free42 source consulted only as sanity-check oracle'` so existing math1/ allow-list logic stays valid AND so the new stat1/ disclaim headers (added in Plan 33-02 onward) inherit the same allow-list policy.

    Per D-33.8: pattern grows to >=18 tokens; both directories scanned; script must exit 0 on the Plan-33-00 mod.rs skeleton (which contains only the disclaim line + OM transcription, no Free42 tokens).
  </action>
  <verify>
    <automated>bash scripts/check-free42-contamination.sh; test $? -eq 2  # exits 2 because STAT1_DIR does not exist yet — Task 2 creates it. Re-verified by Task 3.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[^#]' scripts/check-free42-contamination.sh` reports more non-comment lines than the pre-edit baseline (script grew).
    - `grep -oE '[^|]+' scripts/check-free42-contamination.sh | grep -v '^#' | grep -c '^math_\|^phloat\|^Phloat\|^bid128\|^decNumber\|^decContext\|^vartype\|^arg_struct\|^prgm_lines\|^bcd_t\|^Thomas Okken\|^AGPL\|^GNU General Public License'` returns a value >=18 (12 original + >=6 new), counted on the PATTERN line.
    - `grep -c 'STAT1_DIR=' scripts/check-free42-contamination.sh` returns 1.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' scripts/check-free42-contamination.sh` returns >=1 (DISCLAIM_LINE substring preserved).
    - `bash scripts/check-free42-contamination.sh; echo $?` returns either `2` (STAT1_DIR missing — this task only; Task 2 fixes) or `0` (if Task 2 has already run).
  </acceptance_criteria>
  <done>The script PATTERN has >=18 `|`-delimited tokens, scans both `MATH1_DIR` and `STAT1_DIR`, preserves the DISCLAIM_LINE substring, and the header comment cites D-33.8 plus the core_math2.cc consultation date.</done>
</task>

<task type="auto">
  <name>Task 2: Create hp41-core/src/ops/stat1/mod.rs skeleton with verbatim OM Storage Registers transcription and named consts</name>
  <files>hp41-core/src/ops/stat1/mod.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/mod.rs (lines 1–30 — module-hub shape: disclaim header, `//!` block, exhaustive `pub mod` re-exports; this is the analog template)
    - hp41-core/src/ops/stats.rs (lines 1–30 — existing R01–R06 layout AND the `if state.regs.len() < 7` SIZE-floor guard pattern that STAT1_MAX_REG mirrors)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/mod.rs (module-hub, request-response)" (line 54–135 — the full module-hub template + STAT1_MAX_REG placement)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Σ-Register Layout" + §"Extended Stat 1 Pac registers (Plan 33-00 transcribes from OM)" (line 496–530 — what the OM layout MUST cover)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 9 (line 96–99 — acceptance contract for `mod.rs` shape)
    - HP-41C Stat 1 Pac Owner's Manual 00041-90030, "Storage Registers" section — locate via `literature.hpcalc.org` item 800 PDF; transcribe verbatim
  </read_first>
  <action>
    Read OM 00041-90030 "Storage Registers" section in full (it spans the per-program tables for ΣBSTAT, ΣMMTUG/ΣMMTGD, ΣAOVONE/ΣAOVTWO/ΣANOCOV, ΣLIN/ΣEXP/ΣLOGI/ΣPOW, ΣMLRXY/ΣMLRXYZ, ΣPOLYP/ΣPOLYC, ΣXSQEV/ΣEFXSQ, ΣCTKKK/ΣCTKK, ΣSPEAR; cross-reference NPS NPS55-84-003 §"Register Layout" for any OM ambiguity). For each program, record the register index range (R-low..R-high) and the role of every slot.

    Create `hp41-core/src/ops/stat1/mod.rs` with:
    1. Three-line disclaim header (verbatim per PATTERNS.md "OM disclaim header — byte-for-byte template"): `// Algorithm independently re-derived from HP Stat 1 Pac Owner's Manual 00041-90030 (1979);` / `// Free42 source consulted only as sanity-check oracle, not copied.` / `//` — DISCLAIM_LINE substring must match the existing math1/ files exactly so the contamination guard allow-list logic continues to work.
    2. `//!` block containing the verbatim OM "Storage Registers" transcription (one fenced table per program; cite OM page number on each table). This is the SINGLE SOURCE OF TRUTH per SPEC.md Req. 9.
    3. `pub const STAT1_MAX_REG: usize = N;` where N is the highest 0-indexed register slot touched by ANY Stat 1 accumulator (computed from the OM tables above). Doc-comment cites the specific OM page+program that drives the max.
    4. Named per-program register constants — e.g. `pub const STAT1_AOV_SSB_REG: usize = ...;`, `pub const STAT1_MMTUG_CUBE_REG: usize = ...;`, `pub const STAT1_MLRXY_S_X1_X2_REG: usize = ...;`, etc. One named const per non-Σ-baseline register slot used by ANOVA, moments, regression, contingency, nonparam. Naming convention: `STAT1_<PROGRAM>_<ROLE>_REG`. Each const carries a doc-comment with the OM page citation and the role description.
    5. `pub mod` declarations for the 10 sibling submodules listed in D-33.5: `anova`, `basic_stats`, `chisqd`, `distributions`, `hypothesis`, `moments`, `nonparam`, `normd`, `rand`, `regression`. These declarations remain in `mod.rs` even before the sibling files exist — Plan 33-01 onward creates the files. If the build fails until sibling files are stubbed, prefer adding the `pub mod` lines as `// pub mod <name>; // Plan 33-NN` commented placeholders, and uncomment them per plan as the sibling files land.

    Per CLAUDE.md the disclaim header must be byte-for-byte identical to the math1/ pattern so the contamination guard's DISCLAIM_LINE allow-list continues to work. Do NOT use `println!`/`eprintln!`; this is a hp41-core file (per CLAUDE.md "Frozen Invariants — Core engine").
  </action>
  <verify>
    <automated>cargo check -p hp41-core 2>&1 | grep -v "^warning" | grep -E "(error|cannot find)" | head -5; test ${PIPESTATUS[3]} -ne 0  # expects no errors — cargo check must succeed</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/mod.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/mod.rs` returns 1 (DISCLAIM_LINE byte-for-byte match).
    - `grep -c 'pub const STAT1_MAX_REG: usize' hp41-core/src/ops/stat1/mod.rs` returns 1.
    - `grep -cE 'pub const STAT1_[A-Z_]+_REG: usize' hp41-core/src/ops/stat1/mod.rs` returns >=4 (at least four named per-program register consts — ANOVA, moments, regression, nonparam).
    - `grep -c '00041-90030' hp41-core/src/ops/stat1/mod.rs` returns >=3 (OM citation appears in multiple places: header disclaim, `//!` block, individual const doc-comments).
    - `grep -c 'OM' hp41-core/src/ops/stat1/mod.rs` returns >=5 (multiple OM references; ensures the transcription is real, not a placeholder).
    - `wc -l hp41-core/src/ops/stat1/mod.rs` reports >=60 lines.
    - `cargo check -p hp41-core` exits 0.
  </acceptance_criteria>
  <done>The `mod.rs` file exists with the verbatim OM Storage Registers transcription, `STAT1_MAX_REG` const, >=4 named per-program register consts, the byte-for-byte disclaim header, and at least 10 `pub mod`/commented-placeholder declarations for the sibling submodules. `cargo check -p hp41-core` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the stat1 module in hp41-core/src/ops/mod.rs and verify contamination guard runs green</name>
  <files>hp41-core/src/ops/mod.rs</files>
  <read_first>
    - hp41-core/src/ops/mod.rs (lines 9–25 — existing `pub mod` declarations; `pub mod math1;` is the sibling pattern at line 17)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/mod.rs (modify — Op enum + dispatch)" (line 718–767 — confirms `pub mod stat1;` insertion site is alongside `pub mod math1;`)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-VALIDATION.md §"Wave 0 Requirements" (line 56–63 — confirms `pub mod stat1;` is a Wave 0 deliverable)
  </read_first>
  <action>
    Add a single line `pub mod stat1;` to `hp41-core/src/ops/mod.rs` in alphabetical order with the existing `pub mod` declarations (between `pub mod sound;` and `pub mod stack_ops;` per alphabetical ordering — verify exact site by reading the file). No other edits to `ops/mod.rs` in this plan (the Op enum + dispatch arm additions are Plan 33-01..33-08 work).

    Then re-run the contamination guard to confirm the loop now scans both directories and exits 0 on the skeleton (the stat1/mod.rs file from Task 2 contains the OM disclaim line and no Free42 tokens, so it MUST pass cleanly).

    Per CLAUDE.md §"Frozen Invariants — Workspace structure": `hp41-core` must compile clean before any caller; this task gates the rest of Wave 1.
  </action>
  <verify>
    <automated>cargo check -p hp41-core && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pub mod stat1;' hp41-core/src/ops/mod.rs` returns 1.
    - `cargo check -p hp41-core` exits 0 (the stat1 module mounts cleanly).
    - `bash scripts/check-free42-contamination.sh` exits 0 (script scans both `hp41-core/src/ops/math1` and `hp41-core/src/ops/stat1`, finds the byte-for-byte DISCLAIM_LINE in both, reports zero contamination matches).
    - Probe test: `echo 'math_normal_cdf_probe' > /tmp/stat1_probe.txt; cp /tmp/stat1_probe.txt hp41-core/src/ops/stat1/_probe.tmp; bash scripts/check-free42-contamination.sh; PROBE_RC=$?; rm hp41-core/src/ops/stat1/_probe.tmp; test $PROBE_RC -eq 1` succeeds (proves the extended PATTERN actually catches a stats-domain token in the new directory).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`pub mod stat1;` is mounted; `cargo check -p hp41-core` clean; `bash scripts/check-free42-contamination.sh` exits 0 on the clean skeleton and exits 1 on a probe-token insertion — proving the extended PATTERN actively guards stat1/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script ↔ source tree | check-free42-contamination.sh greps source files; untrusted regex content could theoretically alter exit codes — mitigated by the script being entirely repo-local and CI-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-free42-contamination | Tampering | scripts/check-free42-contamination.sh + hp41-core/src/ops/stat1/ | mitigate | Extend PATTERN with >=6 stats-domain tokens BEFORE first stat1/*.rs algorithm file lands (D-33.8 ordering); CI gate `bash scripts/check-free42-contamination.sh` returns 0 on clean skeleton AND 1 on probe-token insertion (Task 3 acceptance criterion). |
| T-33-numerical-precision | Tampering | hp41-core/src/ops/stat1/mod.rs STAT1_*_REG named consts | mitigate | Every named register constant carries an OM 00041-90030 page citation in its doc-comment; Plan 33-06 / 33-08 enforce that NO literal-integer register indices appear in stat1/*.rs (code-review gate; downstream Phase 37 lint extension). |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/ | accept (no math1/ edits in this plan) | This plan touches only `ops/mod.rs` (sibling mount) and `ops/stat1/mod.rs` (new file) — no math1/ edits. The freeze exceptions D-33.3 / D-33.3a / D-33.3b are not triggered by this plan. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `bash scripts/check-free42-contamination.sh` exits 0.
- Probe-token round-trip test exits 1 when a stats-domain token is injected into `stat1/`.
- `grep -c 'pub const STAT1_MAX_REG' hp41-core/src/ops/stat1/mod.rs` returns 1; `grep -c 'pub const STAT1_[A-Z_]\+_REG' hp41-core/src/ops/stat1/mod.rs` returns >=4.
- `grep -c 'pub mod stat1' hp41-core/src/ops/mod.rs` returns 1.
- `grep -c '00041-90030' hp41-core/src/ops/stat1/mod.rs` returns >=3.
</verification>

<success_criteria>
The Wave 0 foundation is established: every algorithm file landing in Plans 33-02..33-08 will scan against an extended Free42 token pattern AND every register-indexing Op landing in Plans 33-04..33-08 will resolve its R-indices via a named const traceable to an OM page citation. No Op code yet; no math1/ edits; no risk of silently disabling Stat 1 Pac.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-00-SUMMARY.md` when done summarizing: the finalized stats-domain token list with the core_math2.cc consultation evidence, the OM page citations used for STAT1_MAX_REG and per-program register constants, the named consts created, the probe-token round-trip result.
</output>
