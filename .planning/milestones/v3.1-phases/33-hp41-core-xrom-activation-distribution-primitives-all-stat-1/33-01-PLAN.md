---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 01
plan_id: 33-01
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - hp41-core/src/state.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/stat1/modal.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/xrom_shadowing.rs
autonomous: true
requirements:
  - STAT-FW-01
  - STAT-FW-02
  - STAT-FW-03
  - STAT-FW-04
  - STAT-RNG-03

must_haves:
  truths:
    - "STAT_1: XromModule { id: 2, name: \"STAT 1B\" } is registered alongside MATH_1 and resolves via the bit-1 arm of xrom_resolve"
    - "default_xrom_modules() returns 0b0000_0011 — Math 1 AND Stat 1 both pre-loaded by default"
    - "CalcState::migrate_after_load() sets bit 1 unconditionally on v3.0 save files (idempotent on already-migrated state)"
    - "CalcState::rand_seed: HpNum field carries #[serde(default)] WITHOUT #[serde(skip)] and round-trips through serde unchanged"
    - "ModalProgram::Stat1(stat1::modal::Stat1Step) variant exists; submit_step / current_prompt / requires_alpha_label dispatch arms exist for it"
    - "Every STAT_1.ops mnemonic is disjoint from MATH_1.ops AND from builtin_card_op keys (xrom_shadowing.rs gate extended)"
  artifacts:
    - path: "hp41-core/src/state.rs"
      provides: "rand_seed field + default_xrom_modules() flipped + migrate_after_load() method"
      contains: "rand_seed"
    - path: "hp41-core/src/ops/math1/xrom.rs"
      provides: "STAT_1 const + stat1_resolve() + bit-1 arm in xrom_resolve()"
      contains: "pub const STAT_1: XromModule"
    - path: "hp41-core/src/ops/math1/modal.rs"
      provides: "ModalProgram::Stat1(...) variant + 3 dispatch arms"
      contains: "Stat1(stat1::modal"
    - path: "hp41-core/src/ops/stat1/modal.rs"
      provides: "Stat1Step enum + submit_step/current_prompt/requires_alpha_label impls"
      contains: "pub enum Stat1Step"
    - path: "hp41-core/tests/xrom_shadowing.rs"
      provides: "STAT_1.ops disjointness gate"
      contains: "STAT_1"
  key_links:
    - from: "hp41-core/src/ops/math1/xrom.rs::xrom_resolve"
      to: "stat1_resolve"
      via: "bit-1 arm at line 134"
      pattern: "modules & 0b0000_0010 != 0"
    - from: "hp41-core/src/ops/math1/modal.rs::ModalProgram"
      to: "hp41-core/src/ops/stat1/modal.rs::Stat1Step"
      via: "Stat1(stat1::modal::Stat1Step) variant"
      pattern: "ModalProgram::Stat1"
    - from: "hp41-core/src/state.rs::CalcState::migrate_after_load"
      to: "self.xrom_modules"
      via: "bit-1 set if previously clear"
      pattern: "xrom_modules \\|= 0b0000_0010"
---

<objective>
Wave 1 — XROM framework activation. Land the SOLE math1/ freeze exceptions per D-33.3 (xrom.rs) and D-33.3b (modal.rs); flip default_xrom_modules to 0b0000_0011; add `rand_seed` field with the unique serde shape (`#[serde(default)]` WITHOUT `skip`); add `migrate_after_load()` to CalcState; ship the empty `Stat1Step` enum + dispatch hooks so subsequent plans can add modal-prompt-driven Stat 1 Pac Ops (ΣCHISQD ν=?, ΣPOLYP DEGREE=?, SEED?) without touching frozen math1/ files again.

Purpose: This is the structural fork-point of v3.1. Without it, no Stat 1 mnemonic can resolve via XEQ-by-name; without `rand_seed` serde shape correct, RAND determinism breaks silently across save/load (P20 trap); without `migrate_after_load()`, v3.0 users open v3.1 binaries with Stat 1 silently disabled (P24 trap); without `ModalProgram::Stat1`, ΣCHISQD/ΣPOLYP/SEED have nowhere to route their modal state.

Output: hp41-core compiles green with the new `STAT_1` const + bit-1 arm + serde-roundtrip-safe `rand_seed` + `Stat1Step` enum stub. `cargo test -p hp41-core` green. NO Stat 1 Op variants in `Op` enum yet — Plan 33-03..33-08 add those. The `STAT_1.ops` slice initially carries STUB entries (e.g. `("\u{03A3}NORMD", Op::Noop)` — see action notes for the stub strategy) so the disjointness CI gate runs immediately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-00-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing CalcState shape — extend with rand_seed alongside existing fields. -->

From hp41-core/src/state.rs (lines 159–230):
- `xrom_modules: u8` with `#[serde(default = "default_xrom_modules")]` at line 163
- `fn default_xrom_modules() -> u8 { 0b0000_0001 }` at line 228 — flip to `0b0000_0011`
- Existing `#[serde(default)]` (no skip) precedent: `complex_mode: bool` at line 169–170
- Existing `#[serde(default, skip)]` precedent (NOT the pattern for rand_seed): print_buffer / modal_program / etc.

From hp41-core/src/ops/math1/xrom.rs (lines 43–226):
- `pub const MATH_1: XromModule { id: 7, name: "MATH 1A", ops: &[...52 entries] }` at line 43
- `xrom_resolve(name, modules)` at line 127 — bit-1 stub at line 133–134
- `math1_resolve(name)` at line 159 — exhaustive match with `_ => None`
- Tests at lines 230–340 — `resolve_returns_none_for_unknown_name_with_module_loaded`, `resolve_returns_none_when_module_not_loaded`, `resolve_uses_bit_0_only_for_math1`, `math1_const_id_and_name`

From hp41-core/src/ops/math1/modal.rs (lines 23–84):
- `pub enum ModalProgram { Matrix(MatrixInputStep), Solve(SolveInputStep), Poly(PolyInputStep), Integ(IntegInputStep), Difeq(DifeqInputStep), Four(FourInputStep), Trans(TransInputStep) }` at line 23–39
- `impl ModalProgram { pub fn current_prompt(&self) -> Option<String> { ... } pub fn requires_alpha_label(&self) -> bool { ... } }` at line 41–84

From hp41-core/src/ops/math1/mod.rs (per RESEARCH.md §"Pitfall 8"):
- `submit_step()` dispatch table at line 54–81 — gains one arm `ModalProgram::Stat1(step) => stat1::modal::submit_step(state, step)`

From hp41-core/tests/xrom_shadowing.rs (existing 3.5K — covers MATH_1.ops × builtin_card_op disjointness):
- Pattern: iterate `MATH_1.ops` mnemonics, assert each is not in `builtin_card_op` key set, assert `xrom_resolve` returns matching Op.

From PATTERNS.md §"hp41-core/src/ops/math1/xrom.rs" (line 138–250):
- Exact bit-1-arm replacement + `STAT_1` const template + `stat1_resolve` match-arm pattern.

From PATTERNS.md §"hp41-core/src/state.rs (modify — `rand_seed` field + `migrate_after_load()`)" (line 636–714):
- Verbatim field declaration with the loud serde-shape doc-comment + migration method + tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rand_seed field, flip default_xrom_modules, add migrate_after_load() to CalcState (and 3 unit tests)</name>
  <files>hp41-core/src/state.rs</files>
  <read_first>
    - hp41-core/src/state.rs (lines 155–250 — CalcState fields, default_xrom_modules helper, CalcState::new initialization)
    - hp41-core/src/state.rs (lines 374–429 — existing serde_roundtrip test for the xrom_modules field — analog for new tests)
    - hp41-core/src/num.rs (lines 1–60 — HpNum::zero, HpNum::from_str signatures used by the new field initialization and tests)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/state.rs (modify)" (line 636–714 — full verbatim patterns including loud doc-comment, migrate_after_load body, three round-trip tests)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"State Migration & RNG Seed Persistence" (line 595–682 — exact migration method body and three test snippets)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 3, 4, 37, 40 (lines 29–37, 258–267, 282–286 — acceptance contracts)
  </read_first>
  <action>
    Edit `hp41-core/src/state.rs`:
    1. Flip `default_xrom_modules()` body from `0b0000_0001` to `0b0000_0011`; update the doc-comment to "bit 0 = Math 1, bit 1 = Stat 1, both pre-loaded per v3.1 scope" with D-33.2 citation.
    2. Add a new field `rand_seed: HpNum` immediately after the existing `complex_mode` field block (so v3.1 additions cluster). Annotation: `#[serde(default)]` ONLY — explicitly NO `skip`. The doc-comment MUST SHOUT the unique serde shape per RESEARCH.md template at line 631–642 and PATTERNS.md "Serde discipline" line 1410–1448: state that this is the ONLY new v3.1 CalcState field with this serde shape; cite STAT-RNG-03 + P20 trap; warn that `#[serde(skip)]` would silently break determinism.
    3. Initialize `rand_seed: HpNum::zero()` in `CalcState::new()` at the field-init block (per RESEARCH.md line 644–648).
    4. Add `impl CalcState { pub fn migrate_after_load(&mut self) { ... } }` with the body from RESEARCH.md line 599–620 (verbatim): unconditional `if self.xrom_modules & 0b0000_0010 == 0 { self.xrom_modules |= 0b0000_0010; }`. Doc-comment enumerates the two wiring sites (CLI Phase 34, GUI Phase 36) per D-33.7 + RESEARCH.md.
    5. Add three tests in the existing `#[cfg(test)] mod tests` block (verbatim from RESEARCH.md line 657–681):
       - `xrom_modules_default_is_three` — asserts `CalcState::new().xrom_modules == 0b0000_0011`.
       - `v3_0_save_loads_with_stat_1_after_migration` — synthetic v3.0 JSON blob (mirror an existing v2-compat test in `hp41-core/tests/v3_save_compat.rs` for the blob shape) with `"xrom_modules": 1`; deserialize; assert pre-migration value `0b0000_0001`; call `migrate_after_load()`; assert post-migration value `0b0000_0011`.
       - `migrate_after_load_idempotent` — fresh `CalcState::new()` (already has bit 1 set by the new default); call `migrate_after_load()`; assert unchanged at `0b0000_0011`.
       - `rand_seed_serde_round_trip` — set `state.rand_seed = HpNum::from_str("0.7")?`; serialize; deserialize; assert restored equal to original. Proves serde NOT skipped.

    Per CLAUDE.md §"Save-file backward compat": every new CalcState field carries `#[serde(default)]`; for `rand_seed` specifically, ONLY `default` (no `skip`).
    Per CLAUDE.md §"Frozen Invariants — Core engine": no `println!`/`eprintln!`; use `.expect("…")` or `?` (test modules carry `#[allow(clippy::unwrap_used)]`).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib state::tests::xrom_modules_default_is_three state::tests::v3_0_save_loads_with_stat_1_after_migration state::tests::migrate_after_load_idempotent state::tests::rand_seed_serde_round_trip -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub rand_seed: HpNum' hp41-core/src/state.rs` returns 1.
    - `grep -B1 'pub rand_seed: HpNum' hp41-core/src/state.rs | grep -c 'serde(default)' ` returns 1 (annotation present).
    - `grep -B1 'pub rand_seed: HpNum' hp41-core/src/state.rs | grep -c 'skip'` returns 0 (verifies NO `#[serde(skip)]` near the field).
    - `grep -c '0b0000_0011' hp41-core/src/state.rs` returns >=1 (new default_xrom_modules value present).
    - `grep -c 'pub fn migrate_after_load' hp41-core/src/state.rs` returns 1.
    - `grep -c 'xrom_modules |= 0b0000_0010' hp41-core/src/state.rs` returns 1.
    - `cargo test -p hp41-core --lib state::tests::xrom_modules_default_is_three` exits 0.
    - `cargo test -p hp41-core --lib state::tests::v3_0_save_loads_with_stat_1_after_migration` exits 0.
    - `cargo test -p hp41-core --lib state::tests::migrate_after_load_idempotent` exits 0.
    - `cargo test -p hp41-core --lib state::tests::rand_seed_serde_round_trip` exits 0.
    - `cargo test -p hp41-core --test v3_save_compat` exits 0 (existing backward-compat test still green — proves no regression for v2.x/v3.0 save files).
  </acceptance_criteria>
  <done>The `rand_seed` field exists with the unique serde shape; `default_xrom_modules()` returns `0b0000_0011`; `migrate_after_load()` is a callable public method; all four new tests are green; the existing v3_save_compat test remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Land math1/ freeze exception D-33.3b — extend math1/modal.rs with ModalProgram::Stat1 variant + create stat1/modal.rs with Stat1Step enum + 3 dispatch arms</name>
  <files>hp41-core/src/ops/math1/modal.rs, hp41-core/src/ops/stat1/modal.rs, hp41-core/src/ops/math1/mod.rs, hp41-core/src/ops/stat1/mod.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/modal.rs (lines 23–84 — `ModalProgram` enum + `current_prompt` + `requires_alpha_label` dispatch impls)
    - hp41-core/src/ops/math1/mod.rs (lines 50–90 — `submit_step()` dispatch table; per RESEARCH.md it has one match arm per ModalProgram variant)
    - hp41-core/src/ops/stat1/mod.rs (existing file from Plan 33-00 — the new `pub mod modal;` declaration will be added/uncommented here)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md §D-33.3b (line 59–64 — user-confirmed amendment authorizing second math1/ freeze exception)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"Pitfall 8: ModalProgram::Stat1 extension" (line 1476–1518 — the verbatim 1-line variant extension + 3 dispatch arms)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Pattern 3: Modal-Prompt Workflow" (line 314–331 — design rationale for the additive variant approach)
  </read_first>
  <action>
    SECOND math1/ FREEZE EXCEPTION per D-33.3b (user-confirmed 2026-05-22). The math1/ freeze invariant per CLAUDE.md is broken ONLY for these two files: `xrom.rs` (D-33.3) and `modal.rs` (D-33.3b). Every other math1/ file remains strictly frozen.

    1. Create `hp41-core/src/ops/stat1/modal.rs` with:
       - Byte-for-byte disclaim header (per PATTERNS.md OM disclaim template) so the contamination guard allow-list logic continues to work: `// Algorithm independently re-derived from HP Stat 1 Pac Owner's Manual 00041-90030 (1979);` / `// Free42 source consulted only as sanity-check oracle, not copied.` / `//`.
       - `pub enum Stat1Step { /* empty in this plan — Plan 33-03 adds NormdModeChoice, ChisqdNuPrompt; Plan 33-08 adds PolypDegreePrompt, SeedPrompt */ }` — initially declared as `#[derive(Debug, Clone, PartialEq)] pub enum Stat1Step {}` so the dispatch arms compile. To make `match step { }` exhaustive against an empty enum, the dispatch impls below use `match *step {}` (Rust accepts empty match on uninhabited enum).
       - **Alternative if the empty-enum match pattern is awkward:** ship ONE placeholder variant `pub enum Stat1Step { Placeholder }` and have the three dispatch fns return `None` / `Ok(())` / `false` for `Placeholder`. Plan 33-03 immediately replaces `Placeholder` with the real variants (NormdModeChoice, ChisqdNuPrompt). Choose the cleaner option that survives `cargo clippy -- -D warnings`.
       - `pub fn submit_step(state: &mut CalcState, step: Stat1Step) -> Result<(), HpError>` — placeholder body matching on `step`; returns `Ok(())` for the empty/placeholder case. Plan 33-03 fills the body for the real variants.
       - `pub fn current_prompt(step: &Stat1Step) -> Option<String>` — placeholder returns `None`; Plan 33-03 fills.
       - `pub fn requires_alpha_label(step: &Stat1Step) -> bool` — placeholder returns `false`; Plan 33-08 sets `true` for SeedPrompt.

    2. Edit `hp41-core/src/ops/math1/modal.rs`:
       - Add one variant to the `ModalProgram` enum (line 24–39): `Stat1(crate::ops::stat1::modal::Stat1Step),` — positioned at the end of the variant list, after `Trans(TransInputStep)`. Doc-comment cites D-33.3b user-confirmed freeze exception.
       - Add one arm to `current_prompt()` (line 52–62): `ModalProgram::Stat1(step) => crate::ops::stat1::modal::current_prompt(step),`.
       - Add the variant to `requires_alpha_label()` (line 76–83): extend the `matches!` macro with `| ModalProgram::Stat1(step) if crate::ops::stat1::modal::requires_alpha_label(step)`. NOTE: this requires changing the body shape because `matches!` does not support guard clauses with full call expressions for `if`-guards in the simple pattern; the cleaner refactor is to convert the body to an explicit `match self { ... }` block with the four FunctionNamePrompt arms + the new Stat1 guard arm returning the function-call result. Use the explicit-match form.

    3. Edit `hp41-core/src/ops/math1/mod.rs::submit_step()` dispatch table (line 50–90 area): add one match arm `ModalProgram::Stat1(step) => crate::ops::stat1::modal::submit_step(state, step),` at the end of the table, in parity with the existing 7 arms.

    4. Edit `hp41-core/src/ops/stat1/mod.rs` (from Plan 33-00): uncomment or add `pub mod modal;` declaration so the new file mounts.

    Per CLAUDE.md §"Frozen Invariants — Core engine": math1/ remains frozen EXCEPT for `xrom.rs` (D-33.3) and now `modal.rs` (D-33.3b). The math1/modal.rs edit is the minimum-viable single-line variant addition + three dispatch arms, exactly per the rationale recorded in D-33.3b. Update CLAUDE.md "Frozen Invariants → Core engine" carve-out paragraph in Phase 35 (STAT-DOC-05) — NOT in this plan. This plan only ships the code; the docs amendment is downstream.
  </action>
  <verify>
    <automated>cargo check -p hp41-core && cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/modal.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/modal.rs` returns 1 (DISCLAIM_LINE).
    - `grep -c 'pub enum Stat1Step' hp41-core/src/ops/stat1/modal.rs` returns 1.
    - `grep -c 'pub fn submit_step' hp41-core/src/ops/stat1/modal.rs` returns 1.
    - `grep -c 'pub fn current_prompt' hp41-core/src/ops/stat1/modal.rs` returns 1.
    - `grep -c 'pub fn requires_alpha_label' hp41-core/src/ops/stat1/modal.rs` returns 1.
    - `grep -c 'ModalProgram::Stat1' hp41-core/src/ops/math1/modal.rs` returns >=2 (variant declaration + current_prompt arm).
    - `grep -c 'D-33.3b' hp41-core/src/ops/math1/modal.rs` returns >=1 (doc-comment cites the user-confirmed amendment for traceability).
    - `grep -c 'ModalProgram::Stat1' hp41-core/src/ops/math1/mod.rs` returns >=1 (submit_step dispatch arm exists).
    - `grep -c 'pub mod modal' hp41-core/src/ops/stat1/mod.rs` returns 1.
    - `cargo check -p hp41-core` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - `git diff develop -- hp41-core/src/ops/math1/ | grep -E '^\+\+\+ b/' | sort -u` lists ONLY `math1/modal.rs` and `math1/xrom.rs` (which is Task 3 + 4 below) — proving NO other math1/ files touched.
  </acceptance_criteria>
  <done>The `ModalProgram::Stat1(Stat1Step)` variant exists in `math1/modal.rs`; the three dispatch arms compile; the new `stat1/modal.rs` file ships with the placeholder Stat1Step shape; no math1/ file other than modal.rs (here) and xrom.rs (next task) is touched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend math1/xrom.rs with STAT_1 const, stat1_resolve(), and bit-1 arm in xrom_resolve (with 3 unit tests)</name>
  <files>hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/math1/xrom.rs (FULL FILE — lines 1–end; understand MATH_1 const shape, xrom_resolve body, math1_resolve match arms, existing test patterns at lines 230–340+)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/math1/xrom.rs (FREEZE EXCEPTION per D-33.3)" (line 139–250 — full verbatim STAT_1 const + bit-1 arm + stat1_resolve match-arm pattern + tests)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Pattern 1: XROM Module Registration" (line 252–292 — STAT_1 const shape with Σ as \u{03A3} convention)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md §"Stat 1 Pac Mnemonics (14 entry points)" (line 49–82 — the locked mnemonic strings) + Req. 1, 2 (line 19–27 — acceptance contract)
    - hp41-core/src/ops/mod.rs (lines 100–200 — confirm `Op::Noop` exists as a no-op variant; if not, use any existing stable no-op-ish Op for the stub entries — see action notes)
  </read_first>
  <action>
    FIRST math1/ FREEZE EXCEPTION per D-33.3. ONLY `xrom.rs` and `modal.rs` (Task 2) may be edited in math1/ during Phase 33. All other math1/ files remain frozen.

    1. Add `pub const STAT_1: XromModule { id: 2, name: "STAT 1B", ops: &[...] }` to `hp41-core/src/ops/math1/xrom.rs`, positioned immediately after the `MATH_1` const definition (around line 118). The `ops` slice contains 26 entries — one per SPEC.md §"Stat 1 Pac Mnemonics" row — using Unicode Σ via `\u{03A3}` (matching the existing `\u{00D7}` × convention at line 171). The mnemonic strings are LOCKED in SPEC.md.

       **Stub Op problem:** the right-hand side `Op::SigmaNormdWorkflow`, `Op::SigmaSpear`, etc. do NOT yet exist in the `Op` enum (Plans 33-03..33-08 add them). To allow this plan to compile while still landing the XROM registration AND the disjointness CI gate, use one of these strategies — choose whichever passes `cargo clippy -- -D warnings`:
       - **Strategy A (preferred):** ship `STAT_1` with all 26 entries pointing to a placeholder Op variant. If `Op::Noop` exists, use it. If not, add ONE temporary variant `Op::Stat1Stub` to the `Op` enum in `hp41-core/src/ops/mod.rs` with an `op_stat1_stub(_state) -> Result<(), HpError> { Err(HpError::InvalidOp) }` implementation in `hp41-core/src/ops/stat1/mod.rs` (returns an error so any accidental dispatch surfaces clearly); add the 4-way-invariant arms (dispatch in `ops/mod.rs`, execute_op in `program.rs`, CLI/GUI prgm_display arms are Phase 34/36); Plan 33-03..33-08 then DELETE the stub variant once all real Sigma* variants exist. Document Op::Stat1Stub in code with a comment block flagging it as Plan-33-01 scaffolding to be removed by end of Phase 33.
       - **Strategy B (alternative):** ship the `STAT_1` const empty initially: `ops: &[]`. The disjointness test then has nothing to check. NOT RECOMMENDED because it defers the disjointness gate to Plan 33-08.

       Choose Strategy A. Document the Op::Stat1Stub variant prominently so executors of Plan 33-03..33-08 know to replace stub references with real variants and to delete Op::Stat1Stub at the end of Phase 33.

       The list of 26 STAT_1.ops entries must include every mnemonic from SPEC.md §"Stat 1 Pac Mnemonics" — 24 standard + RAND + SEED = 26 strings. Use the U+03A3 Unicode codepoint for Σ.

    2. Replace the bit-1 stub comment at `xrom_resolve()` (lines 133–134) with the real arm:
       ```
       if modules & 0b0000_0010 != 0 {
           if let Some(op) = stat1_resolve(name) {
               return Some(op);
           }
       }
       ```
       The math1 arm at lines 128–132 stays unchanged. The `None` return at the end stays. This preserves the resolver-LAST invariant (xrom_resolve still fires after `builtin_card_op` at both `program.rs` insertion sites — see read_first context for the call sites — no `program.rs` change needed for THIS task; Task 4 only touches `program.rs::execute_op` dispatch arms for Op::Stat1Stub).

    3. Add `fn stat1_resolve(name: &str) -> Option<Op>` below `math1_resolve` (around line 226), exhaustively matching all 26 Stat 1 mnemonics to the placeholder Op (per Strategy A). Use the same `match name { "..." => Some(Op::Stat1Stub), ..., _ => None }` shape as `math1_resolve`.

    4. Add 4 new tests inside the existing `#[cfg(test)] mod tests` block at the file end:
       - `stat1_const_id_and_name` — asserts `STAT_1.id == 2`, `STAT_1.name == "STAT 1B"`.
       - `stat1_ops_has_correct_entry_count` — asserts `STAT_1.ops.len() == 26`.
       - `stat1_ops_mnemonics_resolve_consistently` — iterates `STAT_1.ops`, asserts each `xrom_resolve(name, 0b0000_0011)` returns `Some(expected_op)`.
       - `resolve_uses_bit_1_for_stat1` — asserts `xrom_resolve("\u{03A3}NORMD", 0b0000_0010) == Some(Op::Stat1Stub)` AND `xrom_resolve("\u{03A3}NORMD", 0b0000_0001) == None` (bit-1 isolation per SPEC.md Req. 2).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::math1::xrom::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub const STAT_1: XromModule' hp41-core/src/ops/math1/xrom.rs` returns 1.
    - `grep -A2 'pub const STAT_1: XromModule' hp41-core/src/ops/math1/xrom.rs | grep -c 'id: 2'` returns 1.
    - `grep -A3 'pub const STAT_1: XromModule' hp41-core/src/ops/math1/xrom.rs | grep -c 'STAT 1B'` returns 1.
    - `grep -c 'fn stat1_resolve' hp41-core/src/ops/math1/xrom.rs` returns 1.
    - `grep -c 'modules & 0b0000_0010' hp41-core/src/ops/math1/xrom.rs` returns >=1 (bit-1 arm present).
    - `grep -cE '"[\u{03A3}\\u\{03A3\}]NORMD"' hp41-core/src/ops/math1/xrom.rs` returns >=2 (mnemonic in both STAT_1.ops and stat1_resolve match arm — use `grep -cE '\\\\u\\{03A3\\}NORMD'` or `grep -cE 'NORMD'` depending on shell escaping; the goal is to verify presence in both places).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_const_id_and_name` exits 0.
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_has_correct_entry_count` exits 0.
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::resolve_uses_bit_1_for_stat1` exits 0.
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::resolve_uses_bit_0_only_for_math1` exits 0 (existing test still green — proves bit-0 isolation preserved).
  </acceptance_criteria>
  <done>The `STAT_1` const carries 26 mnemonic entries; `stat1_resolve` exhaustively matches them; `xrom_resolve` bit-1 arm fires after `math1_resolve` (LAST-fires invariant preserved); all four new unit tests are green; the existing math1 bit-0-isolation test stays green.</done>
</task>

<task type="auto">
  <name>Task 4: Add Op::Stat1Stub variant + 4-way invariant items 1+2 (dispatch + execute_op) + extend xrom_shadowing.rs</name>
  <files>hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/tests/xrom_shadowing.rs</files>
  <read_first>
    - hp41-core/src/ops/mod.rs (lines 100–700 — `pub enum Op { ... }`; lines 950–1250 — `pub fn dispatch(state, op) -> Result<(), HpError>` exhaustive match)
    - hp41-core/src/ops/program.rs (lines 800–1100 — `execute_op()` exhaustive match; lines 74–82 and 532–551 — already-wired xrom_resolve insertion sites; READ-ONLY for this task)
    - hp41-core/tests/xrom_shadowing.rs (full 3.5K file — current shape: iterates MATH_1.ops mnemonics, asserts disjoint from builtin_card_op keys, asserts xrom_resolve returns Some for each)
    - hp41-core/src/ops/program.rs (`builtin_card_op` function — typically lines 50–80; understand the keys to grep against)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"Resolver-chain shadow-prevention" (line 577–592 — extension blueprint)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 5, 44 (line 39–42, 321–324 — 4-way invariant + shadow CI gate contracts)
  </read_first>
  <action>
    1. In `hp41-core/src/ops/mod.rs::Op` enum (around lines 589–662 for math1 cluster — pattern-match where to add):
       Add a single new variant `Stat1Stub` to the `Op` enum, grouped at the bottom of the enum with a comment block: "── Phase 33 Plan 33-01 scaffolding: placeholder Op for all STAT_1.ops entries until Plans 33-03..33-08 land the real Sigma* variants and DELETE this Stub. Do NOT use directly. ─". `Stat1Stub` carries NO data fields.

    2. In `hp41-core/src/ops/mod.rs::dispatch()` exhaustive match (around line 1152–1214 for math1 arms):
       Add one arm `Op::Stat1Stub => crate::ops::stat1::op_stat1_stub(state),`. NO `_ =>` catch-all (matches the existing exhaustive-match invariant per SPEC.md Req. 5).

    3. In `hp41-core/src/ops/program.rs::execute_op()` exhaustive match (around line 900 for math1 arms):
       Add the parallel arm `Op::Stat1Stub => crate::ops::stat1::op_stat1_stub(state),`.

    4. In `hp41-core/src/ops/stat1/mod.rs` (extend the file from Plan 33-00):
       Add `pub fn op_stat1_stub(_state: &mut CalcState) -> Result<(), HpError> { Err(HpError::InvalidOp) }` with a doc-comment block clearly marking it as Plan-33-01 scaffolding to be removed at the end of Phase 33. Per CLAUDE.md `#![deny(clippy::unwrap_used)]` — use `Err(...)` propagation, never panic.

    5. Extend `hp41-core/tests/xrom_shadowing.rs` with two new tests:
       - `stat1_ops_disjoint_from_math1_ops` — iterate `STAT_1.ops` mnemonics; assert each is NOT in the `MATH_1.ops` mnemonic set (set built once at test top).
       - `stat1_ops_disjoint_from_builtin_card_op` — iterate `STAT_1.ops` mnemonics; for each name, assert `builtin_card_op(name).is_none()` (or the equivalent check pattern used by the existing math1 test in this file).
       - `stat1_ops_resolve_via_xrom_resolve` — iterate `STAT_1.ops`; for each `(name, expected_op)`, assert `xrom_resolve(name, 0b0000_0011)` returns `Some(expected_op)` (currently Op::Stat1Stub for every name — the test still proves the bidirectional consistency of the slice vs the match arm).

    Per CLAUDE.md §"4-way exhaustive-match invariant": items 1 (`dispatch`) and 2 (`execute_op`) MUST land in the SAME wave because they share an exhaustive match. Adding `Op::Stat1Stub` to the enum without updating both dispatch sites would be a compile error. Items 3 (CLI prgm_display) and 4 (GUI prgm_display) are Phase 34/36 — `cargo check -p hp41-cli` and `-p hp41-gui` WILL fail after this plan; that is expected per RESEARCH.md §"4-Way Exhaustive-Match".
  </action>
  <verify>
    <automated>cargo check -p hp41-core && cargo test -p hp41-core --lib && cargo test -p hp41-core --test xrom_shadowing -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Stat1Stub' hp41-core/src/ops/mod.rs` returns >=2 (enum variant + dispatch arm).
    - `grep -c 'Stat1Stub' hp41-core/src/ops/program.rs` returns >=1 (execute_op arm).
    - `grep -c 'pub fn op_stat1_stub' hp41-core/src/ops/stat1/mod.rs` returns 1.
    - `grep -c 'Plan 33-01 scaffolding\|scaffolding.*Phase 33\|TO BE REMOVED' hp41-core/src/ops/mod.rs hp41-core/src/ops/stat1/mod.rs` returns >=2 (the temporary nature of Op::Stat1Stub is explicitly documented at both sites).
    - `cargo check -p hp41-core` exits 0.
    - `cargo test -p hp41-core --test xrom_shadowing` exits 0 (existing math1 tests + new stat1 disjointness tests all green).
    - `cargo test -p hp41-core --lib` exits 0 (all existing lib tests + new ones from Task 1 + Task 3 green).
    - Expected non-acceptance failures (per 4-way invariant items 3+4 deferred): `cargo check -p hp41-cli` and `cargo check -p hp41-gui` fail with exhaustive-match errors on `op_display_name`. These are EXPECTED per RESEARCH.md line 575 and resolved in Phase 34 + 36.
  </acceptance_criteria>
  <done>The `Op::Stat1Stub` variant lands in all required hp41-core sites; `cargo check -p hp41-core` clean; the extended `xrom_shadowing.rs` proves STAT_1.ops mnemonics are disjoint from MATH_1.ops AND from builtin_card_op; the test for end-to-end bidirectional consistency (slice ↔ match arm) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serde JSON ↔ in-memory CalcState | Save files round-trip through serde; field omission or wrong serde shape silently breaks determinism (rand_seed) or feature visibility (xrom_modules migration). |
| math1/ freeze invariant ↔ xrom.rs + modal.rs edits | Two carve-outs (D-33.3, D-33.3b) are now active; any subsequent edit to a math1/ file other than these two violates the frozen-invariant contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-state-persistence | Information Disclosure | hp41-core/src/state.rs::rand_seed | mitigate | Field declared with `#[serde(default)]` WITHOUT `skip`; Task 1 acceptance criteria include a `grep` that proves NO `skip` annotation near the field; the unit test `rand_seed_serde_round_trip` asserts the value survives serialize→deserialize; the doc-comment SHOUTS the unique shape per P20 trap mitigation. |
| T-33-state-persistence | Tampering (silent feature disablement) | CalcState::migrate_after_load | mitigate | Test `v3_0_save_loads_with_stat_1_after_migration` proves the migration sets bit 1; `migrate_after_load_idempotent` proves no double-application damage. CLI/GUI wiring (Phase 34/36) is out of this plan but the method MUST exist so downstream plans can call it without modifying core. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/ | mitigate | This plan edits ONLY `math1/xrom.rs` (D-33.3) and `math1/modal.rs` (D-33.3b). Task 2 acceptance criterion runs `git diff develop -- hp41-core/src/ops/math1/` and asserts only these two files appear; doc-comments on the variant additions cite D-33.3b for traceability; CLAUDE.md amendment listing `modal.rs` alongside `xrom.rs` is queued for Phase 35 (STAT-DOC-05). Any future plan that touches a different math1/ file requires a new user-confirmed D-33.3x amendment. |
| T-33-numerical-precision | Tampering | hp41-core/src/ops/math1/xrom.rs::stat1_resolve | mitigate | Test `stat1_ops_mnemonics_resolve_consistently` runs bidirectional consistency between the `STAT_1.ops` slice and the `stat1_resolve` match arms — any drift (slice gains an entry without match arm or vice versa) is a CI failure. The Op::Stat1Stub placeholder makes this test green TODAY; Plans 33-03..33-08 each must preserve consistency as they swap stubs for real Op variants. |
| T-33-free42-contamination | Tampering | scripts/check-free42-contamination.sh + hp41-core/src/ops/stat1/modal.rs | accept | Task 2 ships `stat1/modal.rs` with the byte-for-byte DISCLAIM_LINE so the allow-list logic continues to work. No Free42 algorithm code in this plan. The extended PATTERN from Plan 33-00 catches any future contamination. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib` exits 0 (all unit tests green including 4 new state.rs tests and 4 new xrom.rs tests).
- `cargo test -p hp41-core --test xrom_shadowing` exits 0 (math1 disjointness + 3 new stat1 disjointness tests green).
- `cargo test -p hp41-core --test v3_save_compat` exits 0 (no backward-compat regression).
- `bash scripts/check-free42-contamination.sh` exits 0 (no contamination tokens in new files).
- `git diff develop -- hp41-core/src/ops/math1/ | grep '+++ b/' | sort -u` lists ONLY `math1/modal.rs` and `math1/xrom.rs`.
- Expected (NOT a failure): `cargo check -p hp41-cli` and `cargo check -p hp41-gui` fail until Phase 34/36 add the 4-way-invariant items 3+4 for `Op::Stat1Stub`. Verify the failure is on `op_display_name`'s exhaustive match — that proves the invariant is enforced and Phase 34/36 will surface the missing arm immediately.
</verification>

<success_criteria>
After this plan: a `XEQ "ΣNORMD"` invocation reaches `xrom_resolve` which routes through the bit-1 arm and returns `Op::Stat1Stub`, whose execute_op arm returns `HpError::InvalidOp` — proving the resolver chain is wired end-to-end. v3.0 save files load and have bit 1 set after one call to `migrate_after_load()`. The new `rand_seed` field round-trips through serde. The `ModalProgram::Stat1(Stat1Step)` enum is ready to receive real variants in Plan 33-03 + 33-08 without further math1/ edits.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md` when done summarizing: the STAT_1 const final entry count, the 26 mnemonics shipped, the Op::Stat1Stub scaffolding plan (to be deleted at end of Phase 33), the strategy used for the empty Stat1Step enum (placeholder variant vs uninhabited), and a callout that `cargo check -p hp41-cli` is EXPECTED to fail until Phase 34.
</output>
