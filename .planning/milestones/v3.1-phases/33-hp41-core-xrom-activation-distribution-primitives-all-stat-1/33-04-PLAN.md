---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 04
plan_id: 33-04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - hp41-core/src/ops/stat1/nonparam.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
autonomous: true
requirements:
  - STAT-HYP-03
  - STAT-HYP-04
  - STAT-HYP-07

must_haves:
  truths:
    - "XEQ \"ΣSPEAR\" with ranks_x=[1,2,3,4,5] / ranks_y=[2,1,3,5,4] returns ρ_s matching scipy.stats.spearmanr within 1e-9"
    - "XEQ \"ΣXSQEV\" with observed [10,20,30] / expected [15,20,25] returns χ² ≈ 2.667 within 1e-9"
    - "XEQ \"ΣEFXSQ\" with proportions [0.2,0.3,0.5] vs observed [10,30,60] returns χ² ≈ 1.667 within 1e-9"
    - "All three Ops are closed-form — no iterative loops, no distribution-function calls, no modal prompts"
  artifacts:
    - path: "hp41-core/src/ops/stat1/nonparam.rs"
      provides: "ΣSPEAR + ΣXSQEV + ΣEFXSQ closed-form chi-square + Spearman implementations"
      contains: "pub fn op_sigma_spear"
  key_links:
    - from: "hp41-core/src/ops/stat1/nonparam.rs"
      to: "hp41-core/src/ops/stat1/mod.rs"
      via: "STAT1_MAX_REG SIZE-floor guard + STAT1_*_REG named consts for goodness-of-fit register layout"
      pattern: "STAT1_MAX_REG\\|STAT1_NONPARAM"
---

<objective>
Wave 2 — implement the three closed-form non-parametric Ops: ΣSPEAR (Spearman rank correlation), ΣXSQEV (chi-square goodness-of-fit from observed + expected counts), ΣEFXSQ (chi-square with expected-as-proportions entry). All three are closed-form (no iteration, no distribution-function calls, no modal prompts), consume the existing R01–R06 Σ-register block, and follow the named-const register-indexing discipline established in Plan 33-00.

Purpose: ΣSPEAR is "the simplest of all 13 programs" per RESEARCH.md — closed-form ρ_s on existing Σ-register state. ΣXSQEV / ΣEFXSQ are also closed-form chi-square computations. Shipping them in parallel with Plan 33-03 (which handles the iterative paths) gets 3 user-facing Ops live in Wave 2 with minimal blast radius. Open Q 4 (ΣSPEAR oracle value 0.7 vs 0.8) is resolved IN this plan by running scipy and amending SPEC.md if needed.

Output: hp41-core compiles green with 3 new Sigma* Op variants; xrom.rs STAT_1.ops entries for ΣSPEAR, ΣXSQEV, ΣEFXSQ point to real variants; nonparam.rs ≤300 LOC excluding tests; oracle tests for all three Ops green within 1e-9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From hp41-core/src/ops/stats.rs (lines 1–80):
- `pub fn op_sigma_plus(state: &mut CalcState) -> Result<(), HpError>` — R01–R06 accumulator at lines 22–53; SIZE floor `if state.regs.len() < 7`
- `pub fn op_sigma_minus(state: &mut CalcState) -> Result<(), HpError>` — reversal at lines 58–85
- R01–R06 layout: R1=Σx², R2=Σx, R3=n, R4=Σy², R5=Σy, R6=Σxy

From hp41-core/src/ops/stat1/mod.rs (post-Plan-33-00):
- `pub const STAT1_MAX_REG: usize = N` (transcribed from OM Storage Registers)
- Per-program named consts: e.g. `STAT1_NONPARAM_*_REG`, `STAT1_CTKKK_*_REG` (if Plan 33-00 transcription included them)

From PATTERNS.md §"hp41-core/src/ops/stat1/nonparam.rs" (line 566–584):
- ΣSPEAR closed-form ρ_s = 1 − 6·Σd²/(n·(n²−1))

From RESEARCH.md §"Hypothesis Tests" (line 740–793):
- ΣSPEAR oracle DISCREPANCY: SPEC.md says ρ_s = 0.7, manual + scipy say 0.8 — resolved in this plan via scipy verification (Open Q 4)
- ΣXSQEV: χ² = Σ(O − E)²/E
- ΣEFXSQ: convert proportions to expected counts via Σf, then χ² = Σ(O − E)²/E

From CLAUDE.md §"Frozen Invariants — Core engine":
- ISG/DSE counter string-split: NEVER `floor()`/`fmod()` on f64 for register indexing — use named consts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve ΣSPEAR oracle value discrepancy via scipy + implement ΣSPEAR + add OM-traceable register consts if missing</name>
  <files>hp41-core/src/ops/stat1/nonparam.rs, hp41-core/src/ops/stat1/mod.rs</files>
  <read_first>
    - hp41-core/src/ops/stats.rs (lines 1–80 — Σ-register read pattern + SIZE-floor guard analog)
    - hp41-core/src/ops/stat1/mod.rs (post-Plan-33-00 — verify whether STAT1_NONPARAM_* consts exist; if Plan 33-00 transcription did not cover ΣSPEAR/ΣXSQEV/ΣEFXSQ registers, add them in THIS task with OM page citations)
    - hp41-core/src/ops/stat1/modal.rs (post-Plan-33-01; understand ModalProgram::Stat1 dispatch — ΣSPEAR does NOT need a modal, but verify the pattern in case OM specifies one)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/nonparam.rs" (line 566–584 — closed-form ρ_s template)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ΣSPEAR — Spearman Rank Correlation" (line 786–793 — algorithm + Open Q 4 oracle discrepancy)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 30 (line 207–210 — ρ_s = 0.7 acceptance value; may be wrong per Open Q 4)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣSPEAR — confirm formula + register layout
  </read_first>
  <action>
    1. Run scipy oracle FIRST (e.g. via local Python or by deriving manually): `scipy.stats.spearmanr([1,2,3,4,5], [2,1,3,5,4]).statistic` returns approximately 0.7999999999999999 (i.e. 0.8). RESEARCH.md A8 + Open Q 4 explicitly flagged this. Confirm: manual Σd² = 1+1+0+1+1 = 4; ρ_s = 1 − 6·4/(5·24) = 1 − 0.2 = 0.8. So the OM-correct answer is 0.8, NOT 0.7 as SPEC.md Req. 30 states.

       Record the discrepancy in this plan's SUMMARY.md AND in the inline test comment. Use the scipy-confirmed value 0.8 for the oracle assertion. The SPEC.md amendment is documentation-only — gated to Phase 35 (STAT-DOC entries); this plan implements the mathematically correct behavior.

    2. Create `hp41-core/src/ops/stat1/nonparam.rs`. Disclaim header (3 lines) per PATTERNS.md OM template.

    3. Add `pub mod nonparam;` to `hp41-core/src/ops/stat1/mod.rs` if absent.

    4. If `hp41-core/src/ops/stat1/mod.rs` does not yet define `STAT1_NONPARAM_*_REG` named consts (or equivalent — Plan 33-00 may have used different naming), add them now per OM 00041-90030 §Storage Registers for ΣSPEAR/ΣXSQEV/ΣEFXSQ. ΣSPEAR specifically: per OM, accumulates Σd² into a designated register (often R1 if reusing Σ-block, or a Stat-1-specific slot — confirm by reading OM). ΣXSQEV / ΣEFXSQ register layout: observed/expected pairs per OM. Each new const carries an OM page citation in its doc-comment per P21 mitigation.

    5. Add `Op::SigmaSpear` to the `Op` enum in `hp41-core/src/ops/mod.rs` (after Op::SigmaChisqdWorkflow from Plan 33-03).

    6. Implement `pub fn op_sigma_spear(state: &mut CalcState) -> Result<(), HpError>` in `nonparam.rs`:
       - SIZE-floor guard `if state.regs.len() < stat1::STAT1_MAX_REG + 1 { return Err(HpError::InvalidOp); }` per PATTERNS.md line 460–462 — use STAT1_MAX_REG, not the bare 7.
       - Read n from `state.regs[3]` (existing Σ-register R3 per CLAUDE.md / stats.rs convention) AND Σd² from the OM-specified register slot (per Task 4 added const).
       - Compute ρ_s = HpNum::one() − (HpNum::from(6) · sum_d_sq) / (n · (n.checked_sq()? − HpNum::one())) using rust_decimal arithmetic; propagate any `?` errors.
       - Push result to stack X; apply `LiftEffect::Enable`; return Ok(()).
       - No iteration. No cancel-check. No modal.

    7. Add the dispatch arm `Op::SigmaSpear => crate::ops::stat1::nonparam::op_sigma_spear(state)` in BOTH `ops/mod.rs::dispatch()` and `ops/program.rs::execute_op()`.

    8. Replace the ΣSPEAR entry in `STAT_1.ops` slice + `stat1_resolve` match arm in `hp41-core/src/ops/math1/xrom.rs`: swap `Op::Stat1Stub` to `Op::SigmaSpear` (both places).

    9. Add `#[cfg(test)] mod tests` in `nonparam.rs` for ΣSPEAR:
       - Test `spear_basic_5_pairs`: construct CalcState; set R[3] = 5, set the Σd² register to 4 (per OM layout); call op_sigma_spear; assert `state.stack.x` equals 0.8 within `approx::assert_relative_eq!(..., max_relative = 1e-9)`. Comment cites `scipy.stats.spearmanr([1,2,3,4,5], [2,1,3,5,4]).statistic = 0.7999999999999999` and the SPEC.md Req. 30 discrepancy.
       - Test `spear_perfect_positive_correlation`: ranks identical → Σd² = 0 → ρ_s = 1.0 exactly.
       - Test `spear_perfect_negative_correlation`: ranks reversed → ρ_s = -1.0 exactly.
       - Test `spear_size_floor_guard`: state.regs truncated below STAT1_MAX_REG → returns Err(HpError::InvalidOp).

    Per CLAUDE.md `#![deny(clippy::unwrap_used)]`: use `?` propagation, no `.unwrap()` in production code.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::nonparam::tests::spear -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/nonparam.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/nonparam.rs` returns 1.
    - `grep -c 'pub fn op_sigma_spear' hp41-core/src/ops/stat1/nonparam.rs` returns 1.
    - `grep -c 'STAT1_MAX_REG\|state.regs.len()' hp41-core/src/ops/stat1/nonparam.rs` returns >=1 (SIZE-floor guard present).
    - `grep -c 'Op::SigmaSpear' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=4 (enum + dispatch + execute_op + STAT_1.ops + stat1_resolve).
    - `grep -c 'scipy.stats.spearmanr' hp41-core/src/ops/stat1/nonparam.rs` returns >=1 (oracle citation).
    - `grep -c 'SPEC.md Req. 30\|D-33\|discrepancy' hp41-core/src/ops/stat1/nonparam.rs` returns >=1 (the 0.7-vs-0.8 discrepancy is documented inline so future readers see the rationale).
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests::spear` exits 0 (all 4 spear-related tests green).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (bidirectional consistency preserved after stub swap).
    - `grep -E 'state.regs\\[[0-9]+\\]' hp41-core/src/ops/stat1/nonparam.rs` returns 0 lines (NO literal-integer register indices — all named consts per P21 mitigation; bare `state.regs[3]` IS allowed per CLAUDE.md stats convention because R1–R6 are existing v1.x baseline — but anything beyond R6 MUST use a STAT1_* named const).
  </acceptance_criteria>
  <done>ΣSPEAR ships with the scipy-confirmed oracle value 0.8; the 0.7-vs-0.8 SPEC.md discrepancy is documented inline + in SUMMARY for Phase 35 amendment; 4 unit tests green; STAT_1.ops bidirectional consistency preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ΣXSQEV (chi-square goodness-of-fit with observed + expected counts)</name>
  <files>hp41-core/src/ops/stat1/nonparam.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/nonparam.rs (post-Task-1; extend, do not overwrite — verify the file LOC budget stays ≤300 excluding tests)
    - hp41-core/src/ops/stat1/mod.rs (verify STAT1_XSQEV_* register consts exist or add in this task per OM 00041-90030 ΣXSQEV section)
    - hp41-core/src/ops/stats.rs (lines 1–80 — Σ-register read pattern reused)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ΣXSQEV / ΣEFXSQ — Chi-Square Goodness-of-Fit" (line 770–775)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 26 (line 188–191 — acceptance: obs=[10,20,30], exp=[15,20,25] → χ²=2.667 within 1e-9)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣXSQEV — confirm input format (observed/expected pair entry, or pre-accumulated registers)
  </read_first>
  <action>
    1. In `hp41-core/src/ops/stat1/mod.rs`, add `STAT1_XSQEV_OBS_BASE_REG`, `STAT1_XSQEV_EXP_BASE_REG`, `STAT1_XSQEV_COUNT_REG` named consts if Plan 33-00 transcription did not cover the ΣXSQEV register layout. Each carries an OM page citation. The OM specifies whether observed/expected counts are interleaved (R_base, R_base+1, R_base+2, ...) or contiguous-block (R_obs_base..R_obs_base+k for observed, separate block for expected) — read OM ΣXSQEV section to confirm.

    2. Add `Op::SigmaXsqev` to the `Op` enum.

    3. Implement `pub fn op_sigma_xsqev(state: &mut CalcState) -> Result<(), HpError>` in `nonparam.rs`:
       - SIZE-floor guard `if state.regs.len() < stat1::STAT1_MAX_REG + 1 { return Err(HpError::InvalidOp); }`.
       - Read count k from `state.regs[STAT1_XSQEV_COUNT_REG]` (truncate to integer; error if k <= 0).
       - Loop i from 0 to k-1 (using a fixed iteration count derived from k, NOT a `while` loop): `let obs = state.regs[STAT1_XSQEV_OBS_BASE_REG + i].clone(); let exp = state.regs[STAT1_XSQEV_EXP_BASE_REG + i].clone(); if exp == HpNum::zero() { return Err(HpError::Domain); }; chi_sq = chi_sq.checked_add(&(obs - exp).checked_sq()?.checked_div(&exp)?)?;`. Use `?` for every fallible operation. NO `.unwrap()`.
       - Push result to stack X; apply `LiftEffect::Enable`; return `Ok(())`.
       - No iteration cap (the loop bound is k from the user's data — typically << 100 cells; if pathological, defensive `if k > 100 { return Err(HpError::InvalidOp); }` is acceptable).
       - No cancel-check (this is a fixed-iteration loop bounded by user data, not a convergence loop).
       - No modal prompt.

    4. Dispatch + execute_op + xrom.rs stub swap (4 sites) — same pattern as Task 1.

    5. Add `#[cfg(test)] mod tests::xsqev`:
       - Test `xsqev_basic`: load observed=[10,20,30], expected=[15,20,25] into the OM-specified registers; set count=3; call op; assert `state.stack.x` within `assert_relative_eq!(..., 2.667, max_relative=1e-9)`. Manual computation: (10-15)²/15 + (20-20)²/20 + (30-25)²/25 = 25/15 + 0 + 25/25 = 1.6667 + 0 + 1.0 = 2.6667. Cite scipy.stats.chisquare in comment.
       - Test `xsqev_zero_expected_returns_domain_error`.
       - Test `xsqev_size_floor_guard`.
       - Test `xsqev_perfect_fit`: observed == expected → χ² = 0.0.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::nonparam::tests::xsqev -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn op_sigma_xsqev' hp41-core/src/ops/stat1/nonparam.rs` returns 1.
    - `grep -c 'STAT1_XSQEV' hp41-core/src/ops/stat1/nonparam.rs hp41-core/src/ops/stat1/mod.rs` returns >=4 (named consts referenced from nonparam.rs and declared in mod.rs).
    - `grep -c 'Op::SigmaXsqev' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=4.
    - `grep -c 'scipy.stats.chisquare\|chi.*square\|χ²' hp41-core/src/ops/stat1/nonparam.rs` returns >=1 (oracle citation OR result name in comment).
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests::xsqev` exits 0 (4 tests green).
    - File-level: `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/nonparam.rs | wc -l` ≤ 300.
  </acceptance_criteria>
  <done>ΣXSQEV ships with goodness-of-fit chi-square computation, named-const register access (P21), domain-error on zero expected, ≥4 unit tests including the SPEC.md Req. 26 oracle.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ΣEFXSQ (chi-square with expected-as-proportion entry) + final nonparam.rs file audit</name>
  <files>hp41-core/src/ops/stat1/nonparam.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/nonparam.rs (post-Tasks 1–2; extend; verify LOC budget ≤300 excluding tests)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ΣXSQEV / ΣEFXSQ" (line 770–775 — proportions to expected counts via Σf)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 27 (line 192–195 — acceptance: proportions [0.2,0.3,0.5], observed [10,30,60] → χ² ≈ 1.667 within 1e-9)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣEFXSQ — confirm proportions storage location (likely separate register block from ΣXSQEV) and Σf source (likely the same Σ-register R3 = n that v1.x ops/stats.rs maintains)
  </read_first>
  <action>
    1. Add `Op::SigmaEfxsq` to the `Op` enum.

    2. Add `STAT1_EFXSQ_*_REG` named consts to `stat1/mod.rs` if absent (or reuse the ΣXSQEV block if OM specifies sharing — confirm via OM read).

    3. Implement `pub fn op_sigma_efxsq(state: &mut CalcState) -> Result<(), HpError>` in `nonparam.rs`:
       - SIZE-floor guard.
       - Read count k from the OM-specified register; read Σf (total observed) from the OM-specified register (likely R3 = n).
       - Loop i from 0 to k-1: `obs = regs[obs_base + i]; prop = regs[prop_base + i]; exp = sum_f * prop`. Then `chi_sq += (obs - exp)² / exp`. Domain error if exp == 0 or if prop sum does NOT equal 1.0 within tolerance (validate sum-to-1 at the start of the loop per OM convention; reject if violated).
       - Push result to X; LiftEffect::Enable; return Ok(()).

    4. Dispatch + execute_op + xrom.rs stub swap.

    5. Add `#[cfg(test)] mod tests::efxsq`:
       - Test `efxsq_basic`: proportions [0.2, 0.3, 0.5], observed [10, 30, 60], Σf = 100; manual: expected = [20, 30, 50]; χ² = (10-20)²/20 + (30-30)²/30 + (60-50)²/50 = 100/20 + 0 + 100/50 = 5.0 + 0 + 2.0 = 7.0. Wait — SPEC.md says 1.667. Re-verify the formula with scipy: `scipy.stats.chisquare([10, 30, 60], f_exp=[20, 30, 50]).statistic = 7.0`. SPEC.md Req. 27 value 1.667 appears wrong — flag as a second SPEC.md discrepancy similar to ΣSPEAR. Use the scipy-confirmed value 7.0 in the test; document the discrepancy inline. If SPEC.md Req. 27's 1.667 is in fact correct under a different sum-to-1 convention (e.g. with Σf = 50 not 100), confirm via OM read and use the OM-confirmed convention.
       - Test `efxsq_proportions_not_sum_to_one_returns_domain_error`.
       - Test `efxsq_zero_expected_returns_domain_error`.
       - Test `efxsq_size_floor_guard`.

    6. Final audit: re-run `wc -l` on nonparam.rs production-code section (above `#[cfg(test)]`); confirm ≤300. If exceeded, extract shared helpers (e.g. a `chi_sq_accumulate(obs_reg, exp_reg, k)` private fn) to keep ΣCTKKK / ΣCTKK additions in Plan 33-06 within budget.

    Note: ΣSPEAR, ΣXSQEV, ΣEFXSQ are this plan's deliverables. ΣCTKKK + ΣCTKK are deferred to Plan 33-06 (where they cluster with ANOVA per D-33.2 because both depend on OM-extended register layouts).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::nonparam::tests -- --quiet && bash scripts/check-free42-contamination.sh && cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn op_sigma_efxsq' hp41-core/src/ops/stat1/nonparam.rs` returns 1.
    - `grep -c 'Op::SigmaEfxsq' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=4.
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests::efxsq` exits 0 (≥4 efxsq tests).
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests` exits 0 (all nonparam tests for spear + xsqev + efxsq green — total ≥12).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (slice ↔ resolver consistency preserved after 3 more stub swaps; total 5 of 26 swapped after this plan).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/nonparam.rs | wc -l` ≤ 300.
    - `bash scripts/check-free42-contamination.sh` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - `grep -c 'STAT1_EFXSQ\|STAT1_XSQEV\|STAT1_SPEAR' hp41-core/src/ops/stat1/mod.rs` returns >=3 (each program has at least one named-const declared, OM-traceable).
  </acceptance_criteria>
  <done>ΣEFXSQ ships with proportion-to-count conversion + chi-square computation + sum-to-1 validation; the SPEC.md Req. 27 value (1.667 vs scipy 7.0) is verified against OM and either confirmed or flagged for amendment; nonparam.rs production-code stays ≤300 LOC; all three Ops in this plan operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user data ↔ Σ-register slot | User accumulates ranked pairs / observed counts / expected counts into specific registers; if Op reads from the wrong index, computation silently uses the wrong values — Pitfall 1 (P21). |
| SPEC.md acceptance value ↔ scipy oracle | Two SPEC.md oracle values (Req. 30 = 0.7 for ΣSPEAR; Req. 27 = 1.667 for ΣEFXSQ) appear inconsistent with scipy / manual computation; this plan resolves both by running scipy + OM cross-check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering | ΣSPEAR / ΣXSQEV / ΣEFXSQ register access | mitigate | All register reads use named consts (`STAT1_XSQEV_OBS_BASE_REG`, etc.) traceable to OM 00041-90030 page citations declared in `stat1/mod.rs`; NO literal-integer register indices in `nonparam.rs` beyond R1-R6 baseline. Code-review gate: `grep -nE 'state.regs\\[[7-9]\\]|state.regs\\[1[0-9]\\]' hp41-core/src/ops/stat1/nonparam.rs` returns empty (literal integers ≥7 forbidden). |
| T-33-numerical-precision | Tampering | SPEC.md oracle value drift (Open Q 4 + new ΣEFXSQ flag) | mitigate | Both discrepancies are documented inline in the test comments AND in the SUMMARY for handoff to Phase 35 (STAT-DOC entries amend SPEC.md). This plan ships the scipy/OM-confirmed values; SPEC.md is updated downstream. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/xrom.rs | mitigate | This plan edits xrom.rs only to swap 3 of 26 `Op::Stat1Stub` references for real Sigma* variants (Plans 33-01 → 33-03 → 33-04 cumulative: 5 swapped, 21 remaining). All edits within D-33.3 carve-out. |
| T-33-free42-contamination | Tampering | hp41-core/src/ops/stat1/nonparam.rs | mitigate | Disclaim header present; algorithm citations point to manual computations + scipy.stats verification + OM 00041-90030. No Free42 references. `bash scripts/check-free42-contamination.sh` exits 0. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1::nonparam::tests` exits 0 (≥12 unit tests across 3 Ops).
- `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
- `cargo test -p hp41-core --test xrom_shadowing` exits 0.
- `bash scripts/check-free42-contamination.sh` exits 0.
- File-level: nonparam.rs production-code ≤300 LOC (D-33.5).
- No literal-integer register indices ≥7 in nonparam.rs (P21 mitigation).
- Expected: `cargo check -p hp41-cli` still fails on missing op_display_name arms for the 3 new Sigma* variants (Phase 34 fix).
</verification>

<success_criteria>
SPEC.md Acceptance criteria 26, 27, 30 GREEN with scipy-confirmed oracle values (any SPEC.md value drift documented for Phase 35 amendment). ΣSPEAR, ΣXSQEV, ΣEFXSQ all callable end-to-end through dispatch; bidirectional xrom slice↔resolver consistency preserved.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-04-SUMMARY.md` when done summarizing: the resolved ΣSPEAR oracle value (0.8 confirmed); the resolved ΣEFXSQ value vs SPEC.md Req. 27 (1.667 vs scipy 7.0 — final answer); the named-const register layout chosen for each Op; the total Op::Stat1Stub references remaining (should be 21 of 26 — Plan 33-01 placed 26 stubs, Plans 33-03/04 swapped 5).
</output>
