---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 05
plan_id: 33-05
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - hp41-core/src/ops/stat1/basic_stats.rs
  - hp41-core/src/ops/stat1/regression.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
autonomous: true
requirements:
  - STAT-UNI-01
  - STAT-REG-01
  - STAT-REG-02
  - STAT-REG-03
  - STAT-REG-04

must_haves:
  truths:
    - "XEQ \"ΣBSTAT\" reads R01–R06 Σ-register state and returns weighted mean + CV per OM Results section"
    - "XEQ \"ΣBSTG\" returns the bivariate summary variant of ΣBSTAT per OM"
    - "XEQ \"ΣLIN\" with x=[1..5], y=[2,4,6,8,10] returns a=0, b=2 within 1e-9"
    - "XEQ \"ΣEXP\" / ΣLOGI / ΣPOW transforms x and/or y in place then delegates to existing op_sigma_plus, then extracts a, b via the existing linear-regression formula"
    - "No duplication of Σ arithmetic — every curve-fit Op delegates to ops::stats::op_sigma_plus"
  artifacts:
    - path: "hp41-core/src/ops/stat1/basic_stats.rs"
      provides: "ΣBSTAT + ΣBSTG univariate extended summaries reading R01–R06"
      contains: "pub fn op_sigma_bstat"
    - path: "hp41-core/src/ops/stat1/regression.rs"
      provides: "ΣLIN + ΣEXP + ΣLOGI + ΣPOW curve fits using log-linearization + op_sigma_plus delegate"
      contains: "pub fn op_sigma_lin"
  key_links:
    - from: "hp41-core/src/ops/stat1/regression.rs"
      to: "hp41-core/src/ops/stats.rs::op_sigma_plus"
      via: "transform Y then delegate"
      pattern: "ops::stats::op_sigma_plus"
---

<objective>
Wave 2 — implement ΣBSTAT/ΣBSTG (univariate extended summaries) + the four log-linearizing curve fits (ΣLIN/ΣEXP/ΣLOGI/ΣPOW). All six Ops consume existing R01–R06 Σ-register state (NO new register slots; ΣMMTUG/ΣMMTGD's extended Σx³/Σx⁴ block belongs to Plan 33-06). ΣLIN/EXP/LOGI/POW follow the delegate pattern: transform x and/or y in place via ln, then call `crate::ops::stats::op_sigma_plus` — never duplicate Σ arithmetic.

Purpose: Six low-risk Ops shipped in parallel with Plans 33-03 / 33-04 / 33-06 / 33-07. Closed-form throughout. The delegate pattern from PATTERNS.md "Pattern 4" guarantees the regression family inherits all v1.x Σ-register accumulator correctness without re-implementation. ΣBSTAT/ΣBSTG complete the STAT-UNI-01 acceptance contract.

Output: hp41-core compiles green with 6 new Sigma* Op variants; 6 stub swaps in STAT_1.ops + stat1_resolve; basic_stats.rs + regression.rs each ≤300 LOC excluding tests; oracle tests for all six Ops green within 1e-9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From hp41-core/src/ops/stats.rs (lines 1–80):
- `pub fn op_sigma_plus(state: &mut CalcState) -> Result<(), HpError>` — R01–R06 accumulator
- `pub fn op_mean(state: &mut CalcState) -> Result<(), HpError>` — computes x̄ from R01–R06
- `pub fn op_stddev(state: &mut CalcState) -> Result<(), HpError>` — computes s from R01–R06
- `pub fn op_linear_reg(...)` — computes a, b from R01–R06 (v1.x XROM-by-name; verify exact symbol via grep — may be split into op_lr_a / op_lr_b)

From HpNum / rust_decimal::MathematicalOps:
- `HpNum::checked_ln() -> Result<HpNum, HpError>` — natural log; used for log-linearization of EXP/LOGI/POW
- `HpNum::checked_exp() -> Result<HpNum, HpError>` — used to convert back from ln(a) to a in EXP/POW results
- `HpNum::checked_div`, `checked_sub`, `checked_mul`, `checked_sq` — arithmetic primitives with `?` propagation

From PATTERNS.md §"hp41-core/src/ops/stat1/basic_stats.rs" + §"Pattern 4: Σ-Register Delegate" (line 405–448, 334–346):
- Read-only consume of R01–R06; SIZE-floor guard `if state.regs.len() < 7`.
- ΣEXP example body: `let ln_y = state.stack.y.checked_ln()?; state.stack.y = ln_y; crate::ops::stats::op_sigma_plus(state)`.

From RESEARCH.md §"Regression Family" (line 796–810):
- ΣLIN: identity transform.
- ΣEXP: y ← ln y; a' = e^a, b unchanged.
- ΣLOGI: x ← ln x; a, b direct.
- ΣPOW: x ← ln x, y ← ln y; a' = e^a, b unchanged.

From RESEARCH.md §"Univariate Stats" (line 86–94):
- ΣBSTAT extended summary: weighted mean, CV = σ/μ + bivariate summary variants.
- ΣBSTG: bivariate summary variant of ΣBSTAT per OM Results.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ΣBSTAT + ΣBSTG (extended univariate summaries reading R01–R06)</name>
  <files>hp41-core/src/ops/stat1/basic_stats.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stats.rs (FULL FILE — confirm exact public-fn names for mean/stddev/linear-regression; understand R01–R06 layout convention; SIZE-floor guard pattern at line 25)
    - hp41-core/src/ops/stat1/mod.rs (verify whether `pub mod basic_stats;` is declared; add if missing)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/basic_stats.rs" (line 405–448 — ΣBSTAT template reading R01–R06 read-only)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 7 (line 86–89 — acceptance: data=[(1,2,3,4,5),(10,20,30,40,50)] → weighted mean of x with weights y = 3.6667, CV = 0.4083, both within 1e-9)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Univariate Stats" (line 86–94 — algorithm spec)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣBSTAT and §ΣBSTG — confirm result-output channel (stack push vs print buffer per CONTEXT.md Claude's Discretion); default to stack push following Math Pac I POLY precedent if OM is silent
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/basic_stats.rs`. Disclaim header per PATTERNS.md OM template.

    2. Add `pub mod basic_stats;` to `hp41-core/src/ops/stat1/mod.rs` if missing.

    3. Add `Op::SigmaBstat` and `Op::SigmaBstg` to the `Op` enum in `hp41-core/src/ops/mod.rs`.

    4. Implement `pub fn op_sigma_bstat(state: &mut CalcState) -> Result<(), HpError>`:
       - SIZE-floor guard `if state.regs.len() < 7 { return Err(HpError::InvalidOp); }` (R01–R06 baseline, no Stat-1 extension needed for this Op per RESEARCH.md line 91).
       - Read n=R3, Σx=R2, Σx²=R1, Σy=R5, Σy²=R4, Σxy=R6 from existing v1.x Σ-block.
       - Per OM §ΣBSTAT compute the extended summary results:
         - Weighted mean: `μ_w = Σ(x_i · w_i) / Σ(w_i)` — interpret y as weights per SPEC.md Req. 7 acceptance. In Σ-register form: `μ_w = R6 / R5` (Σxy / Σy when y is the weight; verify with OM).
         - Coefficient of variation: `CV = σ_x / μ_x` where `σ_x = sqrt((R1 - R2²/R3) / (R3 - 1))` (Bessel-corrected sample standard deviation per v1.x convention) and `μ_x = R2/R3`. Use rust_decimal `sqrt` from MathematicalOps.
       - Output channel (per CONTEXT.md Claude's Discretion): push primary results to stack — weighted mean to X, CV to Y, σ_x to Z, μ_x to T per OM Results convention. Apply `LiftEffect::Enable`.
       - All arithmetic uses `?` propagation; no `.unwrap()` in production code.

    5. Implement `pub fn op_sigma_bstg(state: &mut CalcState) -> Result<(), HpError>` — bivariate summary variant per OM ΣBSTG. Likely returns covariance / correlation / regression coefficients in addition to means. Read OM §ΣBSTG for exact result list; follow stack-push convention from ΣBSTAT.

    6. Add dispatch + execute_op + xrom.rs stub swaps for both Ops (8 sites total: 2 Ops × 4 sites each).

    7. Add `#[cfg(test)] mod tests::bstat` and `mod tests::bstg`:
       - `bstat_weighted_mean_and_cv`: load Σ-registers from data=[(1,2,3,4,5),(10,20,30,40,50)] (x = [1..5], y/weight = [10,20,30,40,50]); n=5, Σx=15, Σx²=55, Σy=150, Σy²=5500, Σxy=550. Manual: μ_w = 550/150 = 3.6667; σ_x = sqrt((55 - 225/5)/(5-1)) = sqrt(10/4) = 1.5811; μ_x = 15/5 = 3.0; CV = 1.5811/3.0 = 0.5270 ... wait, SPEC.md says CV = 0.4083. Re-check the OM CV definition — may use σ_y/μ_y or σ_w_corrected. Run scipy to confirm: if SPEC.md value disagrees with scipy, document the discrepancy inline as in Plan 33-04 ΣSPEAR. Use the OM-confirmed formula and value.
       - `bstg_basic`: similar oracle from OM ΣBSTG example.
       - `bstat_size_floor_guard`: regs len < 7 → InvalidOp.

    Per CLAUDE.md `#![deny(clippy::unwrap_used)]`: use `.expect("…")` or `?` only.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::basic_stats::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/basic_stats.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/basic_stats.rs` returns 1.
    - `grep -c 'pub fn op_sigma_bstat\|pub fn op_sigma_bstg' hp41-core/src/ops/stat1/basic_stats.rs` returns 2.
    - `grep -c 'Op::SigmaBstat\|Op::SigmaBstg' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=8 (2 Ops × 4 sites).
    - `grep -c 'state.regs.len() < 7\|STAT1_MAX_REG' hp41-core/src/ops/stat1/basic_stats.rs` returns >=1 (SIZE-floor guard).
    - `cargo test -p hp41-core --lib ops::stat1::basic_stats::tests` exits 0 (≥3 tests: bstat oracle + bstg oracle + size-floor).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (slice ↔ resolver consistency preserved after 2 stub swaps).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/basic_stats.rs | wc -l` ≤ 300.
  </acceptance_criteria>
  <done>ΣBSTAT and ΣBSTG ship as read-only consumers of R01–R06; both Op-level acceptance criteria green; bidirectional xrom consistency preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ΣLIN + ΣEXP + ΣLOGI + ΣPOW curve fits using log-linearization + op_sigma_plus delegate</name>
  <files>hp41-core/src/ops/stat1/regression.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/regression.rs (post-Plan-33-01; this Plan creates the file if absent; Plan 33-08 will later extend it with ΣMLRXY/MLRXYZ/POLYP/POLYC — keep the file structure organized so 33-08 can land without conflict)
    - hp41-core/src/ops/stats.rs (FULL FILE — confirm op_sigma_plus signature + how to retrieve a, b from R01–R06; verify whether op_linear_reg or op_lr_a / op_lr_b exists)
    - hp41-core/src/num.rs (HpNum::checked_ln + checked_exp signatures)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"Pattern 4: Σ-Register Delegate (curve fits → existing op_sigma_plus)" (line 334–346 — verbatim ΣEXP body)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/regression.rs" §"Analog 1" (line 506–516 — delegate pattern for ΣEXP)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ΣLIN / ΣEXP / ΣLOGI / ΣPOW" (line 798–810 — table of transforms + a, b extraction conventions)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 15, 16, 17, 18 (line 130–148 — four acceptance oracle tuples)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣLIN/ΣEXP/ΣLOGI/ΣPOW — confirm whether these are ACCUMULATE ops (each call adds one (x,y) pair via Σ+) or COMPUTE ops (compute a, b from already-accumulated R01–R06)
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/regression.rs`. Disclaim header per PATTERNS.md.

    2. Add `pub mod regression;` to `hp41-core/src/ops/stat1/mod.rs` if missing.

    3. Add 4 Op variants to the `Op` enum: `SigmaLin`, `SigmaExp`, `SigmaLogi`, `SigmaPow`.

    4. CRITICAL OM read needed: clarify ΣLIN/EXP/LOGI/POW semantics. Two possible interpretations:
       - **(A) Accumulator semantics:** each call transforms x/y then delegates to `op_sigma_plus` to accumulate ONE point. The user calls XEQ "ΣLIN" once per data point. Final a, b retrieved via existing v1.x `op_lr_a` / `op_lr_b` (or whatever the linear-regression-coefficient extractor is named in stats.rs).
       - **(B) Compute semantics:** ΣLIN computes a, b from an already-fully-populated R01–R06. The user uses Σ+ for each point then calls XEQ "ΣLIN" once.
       The PATTERNS.md "Pattern 4" excerpt (line 337–345) suggests interpretation (A) — ΣEXP transforms y in place then delegates to op_sigma_plus. Confirm via OM §ΣLIN read; if OM specifies (B), implement compute-only versions that read R01–R06 and push a, b to stack.

       Implementation guidance (assume A unless OM disconfirms):
       - `pub fn op_sigma_lin(state: &mut CalcState) -> Result<(), HpError>` — identity transform; just calls `crate::ops::stats::op_sigma_plus(state)`. (If interpretation B, then read R01–R06 + push a = Σy/n - b·Σx/n and b = (n·Σxy - Σx·Σy)/(n·Σx² - (Σx)²) per standard linear-regression formulas; cite OM page.)
       - `pub fn op_sigma_exp(state: &mut CalcState) -> Result<(), HpError>` per PATTERNS.md line 339–345:
         ```
         let y = state.stack.y.clone();
         let ln_y = y.checked_ln()?;
         state.stack.y = ln_y;
         crate::ops::stats::op_sigma_plus(state)
         ```
       - `pub fn op_sigma_logi(state: &mut CalcState) -> Result<(), HpError>`: same pattern but transform `state.stack.x = state.stack.x.clone().checked_ln()?` before delegating.
       - `pub fn op_sigma_pow(state: &mut CalcState) -> Result<(), HpError>`: transform BOTH X and Y by ln before delegating.

       If interpretation (B) is OM-confirmed: implement `op_sigma_<name>_compute` that reads R01–R06 + applies the inverse transform on a (e.g. `a_final = a_raw.checked_exp()?` for EXP/POW) and pushes a, b to stack X/Y.

    5. Dispatch + execute_op + xrom.rs stub swaps for all 4 Ops (16 sites total).

    6. Add `#[cfg(test)] mod tests`:
       - `lin_x_eq_2y` (SPEC.md Req. 15): accumulate (1,2), (2,4), (3,6), (4,8), (5,10) via ΣLIN; extract a, b via the existing op_lr_a/op_lr_b call (or whatever the extractor is); assert a within 1e-9 of 0.0 (absolute, since 0) and b within 1e-9 of 2.0.
       - `exp_y_eq_e_x` (SPEC.md Req. 16): accumulate (1, e), (2, e²), (3, e³) via ΣEXP; extract a, b; apply `e^a_raw` to get final a; assert a ≈ 1.0 and b ≈ 1.0 within 1e-9.
       - `logi_y_eq_2_plus_ln_x` (SPEC.md Req. 17): accumulate (1, 2), (e, 3), (e², 4) via ΣLOGI; extract a, b directly; assert a ≈ 2.0, b ≈ 1.0.
       - `pow_y_eq_x_sq` (SPEC.md Req. 18): accumulate (1, 1), (2, 4), (4, 16), (8, 64) via ΣPOW; extract a, b; apply `e^a_raw`; assert a ≈ 1.0, b ≈ 2.0.
       - Edge: `lin_size_floor_guard`.

    Per CLAUDE.md `#![deny(clippy::unwrap_used)]` and "anti-patterns to avoid" in PATTERNS.md §"Anti-Patterns" (line 348–352): DO NOT hand-roll Σ arithmetic in regression.rs — every Op that adds to Σ-block calls op_sigma_plus.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::regression::tests -- --quiet && bash scripts/check-free42-contamination.sh && cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/regression.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/regression.rs` returns 1.
    - `grep -cE 'pub fn op_sigma_(lin|exp|logi|pow)' hp41-core/src/ops/stat1/regression.rs` returns 4.
    - `grep -c 'crate::ops::stats::op_sigma_plus' hp41-core/src/ops/stat1/regression.rs` returns >=4 (delegate pattern used by ΣLIN/EXP/LOGI/POW — interpretation A); OR if interpretation B confirmed via OM, this grep returns 0 and a different verification applies (compute-only versions read R01–R06).
    - `grep -c 'Op::SigmaLin\|Op::SigmaExp\|Op::SigmaLogi\|Op::SigmaPow' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=16 (4 Ops × 4 sites).
    - `cargo test -p hp41-core --lib ops::stat1::regression::tests` exits 0 (≥5 tests: lin + exp + logi + pow oracle + size-floor).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
    - `cargo test -p hp41-core --lib ops::stat1::basic_stats::tests` exits 0 (basic_stats tests from Task 1 still green — no regression).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/regression.rs | wc -l` ≤ 300 (Plan 33-08 will add ~3 more functions to regression.rs; the current budget MUST leave headroom for ΣMLRXY/MLRXYZ + Gauss elimination — aim for ≤100 LOC in this plan).
    - `bash scripts/check-free42-contamination.sh` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The four log-linearizing curve fits ship as thin delegates to op_sigma_plus (interpretation A) OR as direct R01–R06 readers + transform-on-output (interpretation B); 4 SPEC.md Acceptance criteria (15–18) green; regression.rs stays small enough for Plan 33-08 to extend without splitting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log transform domain ↔ user data | Negative or zero values in y for ΣEXP / x for ΣLOGI / either for ΣPOW cause ln domain errors; the delegate-pattern `?` propagates HpError::Domain cleanly without panic. |
| stats.rs Σ-block ↔ stat1/regression.rs | Curve fits MUST NOT duplicate Σ arithmetic — every accumulation goes through `op_sigma_plus`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering | log-linearization | mitigate | All transforms use HpNum::checked_ln + checked_exp (rust_decimal-backed); no f64 bridge in this plan; tolerance 1e-9 closed-form per SPEC.md Req. 46. Domain errors surface as HpError::Domain via `?`. |
| T-33-numerical-precision | Tampering (silent variance error) | basic_stats.rs CV computation | mitigate | CV computed via stddev from R01–R06 which were accumulated one HpNum at a time with 10-digit rounding — Welford NOT required per PATTERNS.md Pitfall 4 ("Functions that consume the EXISTING Σ-register block are SAFE"). The "naive" formula `(R1 - R2²/R3) / (R3-1)` is acceptable here because R1, R2, R3 are already accumulated with BCD rounding. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/xrom.rs | mitigate | 6 more stub swaps in xrom.rs (cumulative: Plan 33-01 placed 26 stubs; Plans 33-03/04/05 swapped 2+3+6 = 11; remaining stubs after this plan = 15; Plans 33-06/07/08 swap the rest). |
| T-33-free42-contamination | Tampering | hp41-core/src/ops/stat1/basic_stats.rs + regression.rs | mitigate | Disclaim headers present; algorithm citations point to OM 00041-90030 (in-house re-derivation) + existing v1.x stats.rs delegate. No Free42 references. `bash scripts/check-free42-contamination.sh` exits 0. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1::basic_stats::tests ops::stat1::regression::tests` exits 0.
- `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
- `bash scripts/check-free42-contamination.sh` exits 0.
- File-level: basic_stats.rs production-code ≤300 LOC; regression.rs ≤100 LOC (leaving room for Plan 33-08).
- 6 Op::Stat1Stub references swapped to real variants (cumulative 11 of 26 after this plan + Plans 33-03/04).
</verification>

<success_criteria>
SPEC.md Reqs 7 (ΣBSTAT/BSTG), 15, 16, 17, 18 (ΣLIN/EXP/LOGI/POW) GREEN. The delegate pattern is established for Plan 33-08 to follow for ΣMLRXY/ΣMLRXYZ (multiple regression also delegates to op_sigma_plus per RESEARCH.md).
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-05-SUMMARY.md` when done summarizing: the OM-confirmed interpretation (A accumulator vs B compute) for ΣLIN/EXP/LOGI/POW; the exact CV formula used in ΣBSTAT (and whether it matched SPEC.md Req. 7 value 0.4083 or required a documented discrepancy); the cumulative count of swapped stubs.
</output>
