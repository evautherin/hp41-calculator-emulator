---
phase: 36-hp41-gui-gui-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
files_modified:
  - hp41-gui/src/HelpOverlay.test.tsx
autonomous: true
requirements:
  - STAT-GUI-03
  - STAT-GUI-04

must_haves:
  truths:
    - "vitest tests assert three section headings including Stat 1 Pac (XROM 2)"
    - "helpEntriesStat1() returns 26 entries all with xrom.module === 'Stat 1'"
    - "helpEntriesAll() returns the sum of built-in + Math 1 + Stat 1 entry counts"
    - "Clicking the Stat 1 section heading toggles aria-expanded correctly"
    - "Searching for 'NORMD' returns Stat 1 entries"
    - "Existing sectionButtons.length assertion updated from 2 to 3"
  artifacts:
    - path: "hp41-gui/src/HelpOverlay.test.tsx"
      provides: "Stat 1 section tests + updated existing assertions + data-layer tests"
      contains: "Stat 1 Pac (XROM 2)"
  key_links:
    - from: "hp41-gui/src/HelpOverlay.test.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "helpEntriesStat1 import"
      pattern: "helpEntriesStat1"
    - from: "hp41-gui/src/HelpOverlay.test.tsx"
      to: "hp41-gui/src/HelpOverlay.tsx"
      via: "render(<HelpOverlay .../>)"
      pattern: "HelpOverlay"
---

<objective>
Extend the GUI vitest suite with Stat 1 section tests for HelpOverlay and help_data,
covering section rendering, collapse/expand, search filtering, and data-layer 3-pool
integrity.

Purpose: Provides test coverage for STAT-GUI-03 (overlay third section) and strengthens
the CI gate so that future changes to the help data pipeline or section rendering will
fail visibly if they break Stat 1 entries.

Output: All vitest tests pass including updated existing assertions and new Stat 1 block.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-hp41-gui-gui-integration/36-CONTEXT.md
@.planning/phases/36-hp41-gui-gui-integration/36-RESEARCH.md
@.planning/phases/36-hp41-gui-gui-integration/36-PATTERNS.md
@.planning/phases/36-hp41-gui-gui-integration/36-01-SUMMARY.md
@.planning/phases/36-hp41-gui-gui-integration/36-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hp41-gui/src/help_data.ts (after Plan 36-02):
  export function helpEntries(): readonly HelpEntry[]
  export function helpEntriesMath1(): readonly HelpEntry[]
  export function helpEntriesStat1(): readonly HelpEntry[]    // NEW in Plan 36-02
  export function helpEntriesAll(): readonly HelpEntry[]      // 3-pool after Plan 36-02

From hp41-gui/src/HelpOverlay.tsx (after Plan 36-02):
  3 SECTIONS entries: hp41cv, math1, stat1
  expanded state: { hp41cv: boolean; math1: boolean; stat1: boolean }

From docs/hp41-stat1-functions.json:
  26 entries, each with xrom.module === "Stat 1", xrom.module_id === 2

From hp41-gui/src/HelpOverlay.test.tsx (existing, to be extended):
  - describe('help_data', ...) — data-layer tests
  - describe('HelpOverlay', ...) — component tests
  - Line 19: import of help_data functions
  - Line 21: import of sourceJson, math1Json
  - Line 47-58: helpEntriesAll test (2-pool, must update to 3-pool)
  - Line 227: expect(sectionButtons.length).toBe(2) — MUST update to 3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HelpOverlay.test.tsx with Stat 1 data-layer tests + section tests</name>
  <files>hp41-gui/src/HelpOverlay.test.tsx</files>
  <read_first>
    - hp41-gui/src/HelpOverlay.test.tsx (full file — see existing imports at line 19-21, help_data tests at lines 23-58, HelpOverlay tests at lines 131-245, sectionButtons.length at line 227)
    - hp41-gui/src/help_data.ts (after Plan 36-02 — confirm helpEntriesStat1 export exists)
    - hp41-gui/src/HelpOverlay.tsx (after Plan 36-02 — confirm 3 SECTIONS entries)
    - docs/hp41-stat1-functions.json (first 3 entries — confirm xrom.module value, entry count)
  </read_first>
  <action>
    **Part A — Imports (top of file):**

    1. Add stat1Json import alongside existing sourceJson and math1Json (line 21 area):
       import stat1Json from '../../docs/hp41-stat1-functions.json';

    2. Add helpEntriesStat1 to the destructured import from './help_data' (line 19):
       import { helpEntries, helpOverlayRows, filterHelpEntries, helpEntriesMath1, helpEntriesAll, helpEntriesStat1 } from './help_data';

    **Part B — Data-layer tests (add to existing describe('help_data', ...)):**

    3. Add test: `helpEntriesStat1 returns all entries from docs/hp41-stat1-functions.json (drift-catch)`. Assert helpEntriesStat1().length equals stat1Json.length. Sanity floor: assert length >= 26 (the Phase 33-34 scope).

    4. Add test: `helpEntriesStat1 entries all have xrom field with module "Stat 1"`. Loop over helpEntriesStat1() and assert each entry.xrom is truthy, entry.xrom.module === 'Stat 1', entry.xrom.module_id === 2.

    5. UPDATE the existing test `helpEntriesAll returns concatenation of built-in + Math 1 entries` (line 47-58):
       - Change the test name to `helpEntriesAll returns concatenation of built-in + Math 1 + Stat 1 entries`
       - Change the length assertion from `helpEntries().length + helpEntriesMath1().length` to `helpEntries().length + helpEntriesMath1().length + helpEntriesStat1().length`
       - Update the existing loop assertions: built-in entries (0..hp41cvCount) have no xrom, Math 1 entries (hp41cvCount..hp41cvCount+math1Count) have xrom.module === 'Math 1', Stat 1 entries (hp41cvCount+math1Count..end) have xrom.module === 'Stat 1'. This replaces the existing 2-partition loop.

    **Part C — CRITICAL existing test update (Pitfall 2):**

    6. At line 227, change `expect(sectionButtons.length).toBe(2)` to `expect(sectionButtons.length).toBe(3)`. This is the ONLY existing test that hard-codes the section count. Without this update, vitest WILL fail after Plan 36-02 lands the third section.

    **Part D — New Stat 1 section tests (add to existing describe('HelpOverlay', ...)):**

    7. Add test: `renders three top-level sections including Stat 1 Pac (XROM 2)`. Render HelpOverlay open=true. Query all .help-overlay-section-heading buttons. Map to text content. Assert buttonTexts includes 'HP-41CV (built-in)', 'Math 1 Pac (XROM 7)', and 'Stat 1 Pac (XROM 2)'.

    8. Add test: `Stat 1 Pac section contains a Stat 1 category heading (D-34.1)`. Render HelpOverlay open=true. Query all .help-overlay-category-heading elements. Assert at least one heading text includes a known Stat 1 category substring (e.g., 'univariate', 'distributions', or 'anova' — case-insensitive check).

    9. Add test: `clicking Stat 1 Pac section heading toggles aria-expanded (D-31.8)`. Render HelpOverlay open=true. Find the section button containing 'Stat 1 Pac'. Assert initial aria-expanded is 'true'. Click it, assert 'false'. Click again, assert 'true'. Mirror the existing Math 1 section toggle test structure (lines 222-244).

    10. Add test: `search for "NORMD" returns Stat 1 entries`. Render HelpOverlay open=true. Set the search input value to 'NORMD'. Assert at least one .help-overlay-row exists. Assert at least one row contains 'NORMD' in its text content.

    **Part E — Final verification:**

    11. Run the full vitest suite: `cd hp41-gui && npm test`. All tests must pass including the updated existing tests and the new Stat 1 tests.

    12. Run `just gui-ci` to confirm the full GUI CI pipeline is green (TypeScript compile + Rust compile + vitest + cargo test).
  </action>
  <verify>
    <automated>cd hp41-gui && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Import line includes helpEntriesStat1 from './help_data'
    - Import line includes stat1Json from '../../docs/hp41-stat1-functions.json'
    - helpEntriesStat1 drift-catch test asserts length equals stat1Json.length AND >= 26
    - helpEntriesStat1 xrom-field test loops over all entries asserting module === 'Stat 1' and module_id === 2
    - helpEntriesAll test name updated to mention Stat 1; length assertion includes helpEntriesStat1().length
    - Line 227 (or its equivalent) asserts sectionButtons.length === 3 (NOT 2)
    - "renders three top-level sections" test asserts all three section headings present
    - "Stat 1 category heading" test asserts at least one Stat 1 category is rendered
    - "clicking Stat 1 section heading toggles aria-expanded" test asserts true -> false -> true
    - "search for NORMD" test asserts at least one result row containing 'NORMD'
    - `cd hp41-gui && npm test` exits 0 with all tests passing
    - `just gui-ci` exits 0 (full pipeline green)
  </acceptance_criteria>
  <done>All vitest tests pass including updated 3-section assertions and new Stat 1 section test block; just gui-ci green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only changes; no production code modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Tampering | Test assertions | accept | Tests are CI-gated; false passes caught by section-count and xrom-field assertions |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed |
</threat_model>

<verification>
cd hp41-gui && npm test
just gui-ci
</verification>

<success_criteria>
- `cd hp41-gui && npm test` exits 0 — all vitest tests pass
- `just gui-ci` exits 0 — full GUI CI pipeline green (TypeScript + Rust + vitest + cargo test)
- No existing test regressed (sectionButtons.length updated from 2 to 3)
</success_criteria>

<output>
Create `.planning/phases/36-hp41-gui-gui-integration/36-03-SUMMARY.md` when done
</output>
