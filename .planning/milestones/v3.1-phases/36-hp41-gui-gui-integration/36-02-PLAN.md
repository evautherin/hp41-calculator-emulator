---
phase: 36-hp41-gui-gui-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/HelpOverlay.tsx
  - hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt_stat1.rs
autonomous: true
requirements:
  - STAT-GUI-03
  - STAT-GUI-04

must_haves:
  truths:
    - "The GUI ? overlay shows three collapsible sections: HP-41CV (built-in), Math 1 Pac (XROM 7), and Stat 1 Pac (XROM 2)"
    - "helpEntriesAll() returns the 3-pool concatenation of built-in + Math 1 + Stat 1 entries"
    - "All 5 Stat1Step modal prompts route through CalcStateView correctly — including ΣCHISQD MODE? truncation to ΣCHISQD MOD≡"
    - "Search in the ? overlay returns Stat 1 entries alongside built-in and Math 1 results"
  artifacts:
    - path: "hp41-gui/src/help_data.ts"
      provides: "Third Vite static import + helpEntriesStat1() + 3-pool helpEntriesAll()"
      contains: "helpEntriesStat1"
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "Third SECTIONS entry for Stat 1 Pac (XROM 2)"
      contains: "Stat 1 Pac (XROM 2)"
    - path: "hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt_stat1.rs"
      provides: "5 integration tests for Stat1Step modal prompt routing through CalcStateView"
      contains: "normd_mode_choice_prompt_renders_verbatim"
  key_links:
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/hp41-stat1-functions.json"
      via: "Vite static JSON import"
      pattern: "import stat1Functions from"
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "helpEntriesAll() call"
      pattern: "helpEntriesAll"
    - from: "hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt_stat1.rs"
      to: "hp41-core/src/ops/stat1/modal.rs"
      via: "Stat1Step enum import + CalcStateView::from_state assertion"
      pattern: "Stat1Step::NormdModeChoice"
---

<objective>
Wire the Stat 1 Pac help data into the GUI overlay and verify modal-prompt routing for
all 5 Stat1Step variants via Rust integration tests.

Purpose: Makes all 26 Stat 1 functions discoverable from the GUI `?` overlay (STAT-GUI-03)
and verifies that the Stat 1 modal-prompt pipeline routes OM strings correctly through
CalcStateView (STAT-GUI-04).

Output: HelpOverlay shows 3 sections; help_data.ts serves 3-pool entries; 5 LCD-alternation
modal prompt tests pass.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-hp41-gui-gui-integration/36-CONTEXT.md
@.planning/phases/36-hp41-gui-gui-integration/36-RESEARCH.md
@.planning/phases/36-hp41-gui-gui-integration/36-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hp41-gui/src/help_data.ts:
  export interface HelpEntry { op_variant, display_name, category, status, phase, key_path, description, divergences?, xrom?: XromEntry }
  export interface XromEntry { module: string, module_id: number, function_id: number }
  export function helpEntries(): readonly HelpEntry[]
  export function helpEntriesMath1(): readonly HelpEntry[]
  export function helpEntriesAll(): readonly HelpEntry[]   // MUST update in-place to 3-pool

From hp41-gui/src/HelpOverlay.tsx:
  interface SectionDef { id: 'hp41cv' | 'math1'; heading: string; predicate: (e: HelpEntry) => boolean }
  const SECTIONS: SectionDef[]  (currently 2 entries)
  const [expanded, setExpanded] = useState<{ hp41cv: boolean; math1: boolean }>
  const toggleSection = (id: 'hp41cv' | 'math1') => ...

From docs/hp41-stat1-functions.json (26 entries):
  Each entry has: { "xrom": { "module": "Stat 1", "module_id": 2, "function_id": N }, ... }

From hp41-core/src/ops/stat1/modal.rs (current_prompt strings):
  Stat1Step::NormdModeChoice  -> "\u{03A3}NORMD MODE?"   (12 chars, no truncation)
  Stat1Step::ChisqdNuPrompt   -> "\u{03BD}=?"             (3 chars, no truncation)
  Stat1Step::ChisqdModeChoice -> "\u{03A3}CHISQD MODE?"   (13 chars, TRUNCATES to "\u{03A3}CHISQD MOD\u{2261}")
  Stat1Step::PolypDegreePrompt(0) -> "DEGREE=?"           (8 chars, no truncation)
  Stat1Step::SeedPrompt       -> "SEED?"                  (5 chars, no truncation)

From hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs (template):
  CalcStateView::from_state(&calc, vec![], vec![])  // (state, print_lines, program_steps)
  LCD_WIDTH = 12; prompts > 12 chars truncate to take(11) + "≡" (U+2261)

From hp41-core (imports for integration test):
  hp41_core::ops::math1::modal::ModalProgram
  hp41_core::ops::stat1::modal::Stat1Step
  hp41_core::CalcState
  hp41_gui_lib::types::CalcStateView
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend help_data.ts with third Vite import + 3-pool helpEntriesAll()</name>
  <files>hp41-gui/src/help_data.ts</files>
  <read_first>
    - hp41-gui/src/help_data.ts (full file — see existing import at line 18, helpEntriesMath1() at line 143, helpEntriesAll() at line 152)
    - docs/hp41-stat1-functions.json (first entry — confirm xrom.module value is "Stat 1")
  </read_first>
  <action>
    1. Add a third Vite static JSON import immediately after line 18 (alongside existing math1Functions import):
       import stat1Functions from '../../docs/hp41-stat1-functions.json';

    2. Add a new accessor function after the existing helpEntriesMath1() function (after line 145). Name it helpEntriesStat1(). Return stat1Functions cast as readonly HelpEntry[]. Include a Phase 36 Plan 36-02 doc-comment explaining the source is docs/hp41-stat1-functions.json and the Vite build-time gate semantics (mirrors the math1 comment block at lines 137-142).

    3. UPDATE (not duplicate) the existing helpEntriesAll() function at line 152 to return the 3-pool concatenation: [...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1()]. Update its doc-comment from "built-in + Math Pac I entries" to "built-in + Math Pac I + Stat 1 Pac entries". Reference Phase 36 Plan 36-02 in the comment. Pitfall 5: do NOT create a new helpEntriesAll3() function — update in-place so all existing callers automatically pick up Stat 1 entries.

    4. Add helpEntriesStat1 to the module's exports (it is already exported via export function syntax).
  </action>
  <verify>
    <automated>cd hp41-gui && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/src/help_data.ts contains `import stat1Functions from '../../docs/hp41-stat1-functions.json';`
    - helpEntriesStat1() function exists and returns `stat1Functions as readonly HelpEntry[]`
    - helpEntriesAll() returns `[...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1()]` (3-pool)
    - TypeScript compiles without errors: `cd hp41-gui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>help_data.ts serves 3-pool entries; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Extend HelpOverlay.tsx with Stat 1 Pac section + LCD modal prompt integration test</name>
  <files>hp41-gui/src/HelpOverlay.tsx, hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt_stat1.rs</files>
  <read_first>
    - hp41-gui/src/HelpOverlay.tsx (full file — SectionDef at line 32, SECTIONS at line 39, expanded state at line 56, useEffect reset at line 65, toggleSection at line 130)
    - hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt.rs (full file — template for the new test)
    - hp41-core/src/ops/stat1/modal.rs (lines 333-343 — current_prompt return strings for all 5 Stat1Step variants)
  </read_first>
  <action>
    **Part A — HelpOverlay.tsx (4 surgical edits, ~15 LOC delta):**

    1. Widen SectionDef.id union type (line 33) from `'hp41cv' | 'math1'` to `'hp41cv' | 'math1' | 'stat1'`.

    2. Add third SECTIONS array entry (after the existing math1 entry at line 49). The new entry is: `{ id: 'stat1', heading: 'Stat 1 Pac (XROM 2)', predicate: (e: HelpEntry) => e.xrom?.module === 'Stat 1' }`. Order is: hp41cv, math1, stat1 per D-34.6.

    3. Widen expanded useState type (line 56) from `{ hp41cv: boolean; math1: boolean }` to `{ hp41cv: boolean; math1: boolean; stat1: boolean }`. Add `stat1: true` to the initial state object. Update the useEffect reset at line 65 from `setExpanded({ hp41cv: true, math1: true })` to `setExpanded({ hp41cv: true, math1: true, stat1: true })`.

    4. Widen toggleSection id parameter (line 130) from `'hp41cv' | 'math1'` to `'hp41cv' | 'math1' | 'stat1'`.

    No JSX changes needed — the existing sectionGroups.map() render loop at lines 160-192 iterates over SECTIONS generically and the new third section renders automatically.

    **Part B — lcd_alternation_modal_prompt_stat1.rs (new file, 5 tests):**

    5. Create `hp41-gui/src-tauri/tests/lcd_alternation_modal_prompt_stat1.rs` using `lcd_alternation_modal_prompt.rs` as the direct template.

    6. File header: Phase 36 Plan 36-02 comment explaining this tests CalcStateView LCD-alternation routing for all 5 Stat1Step variants (STAT-GUI-04 verification).

    7. Add `#![allow(clippy::unwrap_used)]` at file top.

    8. Imports: `hp41_core::ops::math1::modal::ModalProgram`, `hp41_core::ops::stat1::modal::Stat1Step`, `hp41_core::CalcState`, `hp41_gui_lib::types::CalcStateView`.

    9. Write 5 test functions, each constructing a fresh CalcState with modal_program set to ModalProgram::Stat1(step) and modal_prompt set to the OM string, then asserting CalcStateView::from_state().display_str:
       - normd_mode_choice_prompt_renders_verbatim: "ΣNORMD MODE?" (12 chars, no truncation) -> assert display_str == "\u{03A3}NORMD MODE?"
       - chisqd_nu_prompt_renders_verbatim: "ν=?" (3 chars) -> assert display_str == "\u{03BD}=?"
       - chisqd_mode_choice_prompt_truncates: "ΣCHISQD MODE?" (13 chars) -> assert display_str == "\u{03A3}CHISQD MOD\u{2261}" (Pitfall 1: LCD truncation to 11 chars + ≡ continuation marker)
       - polyp_degree_prompt_renders_verbatim: "DEGREE=?" (8 chars) -> assert display_str == "DEGREE=?"
       - seed_prompt_renders_verbatim: "SEED?" (5 chars) -> assert display_str == "SEED?"
       Each test also asserts entry_buf.is_empty() before calling from_state (the LCD-alternation branch only fires when entry_buf is empty).

    10. For the ΣCHISQD MODE? truncation test, also assert display_str.chars().count() == 12 to verify the truncated result is exactly LCD width.
  </action>
  <verify>
    <automated>cd hp41-gui && npx tsc --noEmit && cargo test --manifest-path src-tauri/Cargo.toml lcd_alternation_modal_prompt_stat1 -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - HelpOverlay.tsx SectionDef.id includes 'stat1' in the union
    - SECTIONS array has exactly 3 entries in order: hp41cv, math1, stat1
    - SECTIONS[2].heading is "Stat 1 Pac (XROM 2)"
    - SECTIONS[2].predicate filters on e.xrom?.module === 'Stat 1'
    - expanded state type includes stat1: boolean with default true
    - useEffect reset includes stat1: true
    - toggleSection id parameter includes 'stat1'
    - lcd_alternation_modal_prompt_stat1.rs contains 5 test functions covering all Stat1Step variants
    - ΣCHISQD MODE? test asserts the TRUNCATED form "\u{03A3}CHISQD MOD\u{2261}" (not the raw prompt)
    - `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml lcd_alternation_modal_prompt_stat1` — all 5 pass
    - TypeScript compiles clean: `cd hp41-gui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HelpOverlay shows 3 sections with Stat 1 Pac (XROM 2); all 5 LCD-alternation modal prompt tests pass including ΣCHISQD MODE? truncation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite JSON import | docs/hp41-stat1-functions.json is imported at build time; malformed JSON fails the build (hard gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | hp41-stat1-functions.json | accept | Read-only consumption; JSON authored in Phase 34 and CI-validated by parity tests |
| T-36-04 | Information Disclosure | HelpOverlay section data | accept | Public function reference data; no secrets |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in Phase 36 |
</threat_model>

<verification>
cd hp41-gui && npx tsc --noEmit
cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml lcd_alternation_modal_prompt_stat1
</verification>

<success_criteria>
- TypeScript compiles without errors
- All 5 LCD-alternation modal prompt integration tests pass
- HelpOverlay has 3 sections visible in `just gui-dev`
</success_criteria>

<output>
Create `.planning/phases/36-hp41-gui-gui-integration/36-02-SUMMARY.md` when done
</output>
