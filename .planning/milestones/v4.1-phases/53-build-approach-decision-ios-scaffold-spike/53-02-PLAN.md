---
phase: 53-build-approach-decision-ios-scaffold-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BUILD-04]
user_setup:
  - service: apple-app-store-connect
    why: "Bundle ID must be registered as an explicit App ID + app record before any provisioning/signing can succeed (P-iOS-14)"
    dashboard_config:
      - task: "Register explicit App ID ch.talent-factory.hp41 under Identifiers"
        location: "Apple Developer portal → Certificates, Identifiers & Profiles → Identifiers"
      - task: "Create the app record for the bundle ID"
        location: "App Store Connect → Apps → New App"
must_haves:
  truths:
    - "Bundle ID ch.talent-factory.hp41 is registered as an explicit App ID in the Apple Developer portal"
    - "An App Store Connect app record exists for that bundle ID"
    - "Provisioning-profile creation for ch.talent-factory.hp41 is now unblocked"
  artifacts:
    - path: ".planning/phases/53-build-approach-decision-ios-scaffold-spike/53-02-SUMMARY.md"
      provides: "record of the registered App ID + app record (confirmation, not secrets)"
  key_links:
    - from: "App Store Connect app record"
      to: "tauri.conf.json identifier ch.talent-factory.hp41"
      via: "matching bundle identifier"
      pattern: "ch.talent-factory.hp41"
---

<objective>
Register the bundle ID `ch.talent-factory.hp41` in the Apple Developer portal as an
explicit App ID and create the matching App Store Connect app record. This is a
~5-minute human-only web-portal task that unblocks all future provisioning-profile
and signing work (Plan 04 device install + Phase 57 TestFlight).

Purpose: Remove the P-iOS-14 blocker (missing App ID → opaque 403s during signing).
Output: a registered App ID + app record (no files changed, no secrets stored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Reference only — DO NOT EDIT tauri.conf.json (D-53.3). -->
hp41-gui/src-tauri/tauri.conf.json already has:
  "identifier": "ch.talent-factory.hp41"   (hyphen-free; sidesteps the Tauri bundle-ID bug)
The bundle ID is NOT changed in this phase — it is REGISTERED in App Store Connect.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Register App ID + app record in App Store Connect</name>
  <read_first>
    - .planning/research/PITFALLS.md (P-iOS-14: bundle ID not registered before first build)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md (specifics: "register ch.talent-factory.hp41 as an explicit App ID + create the app record")
  </read_first>
  <action>
    HUMAN-ONLY checkpoint (no Apple CLI/API path is provisioned this phase). Follow the
    click-by-click steps in <how-to-verify> to register the explicit App ID
    `ch.talent-factory.hp41` under Apple Developer → Identifiers and create the matching
    App Store Connect app record. Do NOT generate, download, or commit any certificate,
    provisioning profile, API key, or `.p12` (Phase 57 owns distribution signing).
  </action>
  <what-built>
    Nothing to automate here — this is a human-only Apple web-portal action (no Apple
    CLI/API path is provisioned in this phase). The Apple Developer Program is already
    enrolled and active (D-53.10).
  </what-built>
  <how-to-verify>
    1. Sign in at https://developer.apple.com/account → "Certificates, Identifiers & Profiles".
    2. Go to "Identifiers" → click the "+" to register a new identifier → choose "App IDs" → "App".
    3. Set Description "HP-41 Calculator". Set Bundle ID to **Explicit**:
       `ch.talent-factory.hp41` (must match `tauri.conf.json` identifier exactly — do NOT use a wildcard).
    4. Leave Capabilities at defaults (none required for this foundation spike). Register.
    5. Go to https://appstoreconnect.apple.com → "Apps" → "+" → "New App" → platform iOS →
       select the `ch.talent-factory.hp41` bundle ID from the dropdown → name the app →
       create the app record.
    6. Confirm: back in "Identifiers", `ch.talent-factory.hp41` is listed as an explicit
       App ID; in App Store Connect "Apps", the new app record appears.
    Do NOT generate or download any certificate or provisioning profile in this phase
    (that is Phase 57). Do NOT paste any secret, API key, or .p12 into the repo.
  </how-to-verify>
  <verify>
    <human-check>The explicit App ID ch.talent-factory.hp41 is listed under Apple Developer → Identifiers AND an App Store Connect app record for that bundle ID exists; no secret was committed.</human-check>
  </verify>
  <acceptance_criteria>
    - The explicit App ID `ch.talent-factory.hp41` appears under Identifiers (not a wildcard `*` identifier)
    - An App Store Connect app record for `ch.talent-factory.hp41` exists
    - No signing certificate, provisioning profile, API key, or `.p12` was committed to the repo (none should exist yet)
    - The SUMMARY records confirmation of registration WITHOUT recording any secret value
  </acceptance_criteria>
  <done>Explicit App ID registered + app record created for ch.talent-factory.hp41; provisioning unblocked; no secrets committed.</done>
  <resume-signal>Type "registered" once the explicit App ID and the app record both exist, or describe any blocker (e.g., name collision, team-role permission).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Apple Developer portal | authenticated human session creating an App ID + app record |
| Apple portal identifiers namespace | bundle-ID ownership / squatting surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Spoofing | App ID `ch.talent-factory.hp41` | mitigate | Register the bundle ID under the owned `ch.talent-factory` reverse-domain namespace immediately, claiming ownership before any third party can squat the identifier. |
| T-53-05 | Information disclosure | signing material / API keys | mitigate | This plan deliberately does NOT create or download certs, profiles, or API keys (deferred to Phase 57). Acceptance criteria assert no `.p12` / provisioning / API-key file enters the repo; SUMMARY records confirmation only, never secret values. |
| T-53-06 | Elevation of privilege | Apple Developer team role | accept | Action performed by the enrolled account holder (D-53.10); team-role scoping is Apple-managed and outside repo control. |
</threat_model>

<verification>
- Explicit App ID `ch.talent-factory.hp41` visible under Apple Developer → Identifiers.
- App Store Connect app record exists for the bundle ID.
- No secret material committed.
</verification>

<success_criteria>
Bundle ID `ch.talent-factory.hp41` is registered as an explicit App ID and an app
record exists — provisioning-profile creation is unblocked for Plan 04 and Phase 57.
</success_criteria>

<output>
Create `.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-02-SUMMARY.md` when done.
Record registration confirmation only — NEVER any secret, key, or profile value.
</output>
