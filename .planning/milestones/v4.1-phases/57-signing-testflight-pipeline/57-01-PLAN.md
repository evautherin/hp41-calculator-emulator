---
phase: 57-signing-testflight-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy
  - hp41-gui/src-tauri/gen/apple/project.yml
  - hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist
  - hp41-gui/src-tauri/gen/apple/ExportOptions.plist
  - justfile
autonomous: true
requirements: [SHIP-01, SHIP-02]
must_haves:
  truths:
    - "PrivacyInfo.xcprivacy declares NSPrivacyAccessedAPICategoryFileTimestamp with reason C617.1"
    - "PrivacyInfo.xcprivacy is listed as a source in the iOS target so it ships inside the bundle"
    - "ExportOptions.plist uses method app-store-connect (the Xcode 26 canonical value, not deprecated app-store)"
    - "The marketing version (CFBundleShortVersionString) is 4.1 in both Info.plist and project.yml"
    - "A just ios-build-release recipe exists that drives a signed app-store-connect export"
  artifacts:
    - path: "hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy"
      provides: "Apple privacy manifest (file-timestamp accessed-API reason)"
      contains: "NSPrivacyAccessedAPICategoryFileTimestamp"
    - path: "hp41-gui/src-tauri/gen/apple/ExportOptions.plist"
      provides: "Distribution export configuration"
      contains: "app-store-connect"
    - path: "justfile"
      provides: "ios-build-release recipe"
      contains: "ios-build-release"
  key_links:
    - from: "hp41-gui/src-tauri/gen/apple/project.yml"
      to: "PrivacyInfo.xcprivacy"
      via: "sources entry in hp41-gui_iOS target"
      pattern: "PrivacyInfo\\.xcprivacy"
    - from: "justfile ios-build-release"
      to: "npm run tauri ios build"
      via: "--export-method app-store-connect --ci"
      pattern: "export-method app-store-connect"
---

<objective>
Lay the signing-config + privacy-manifest foundation that every later plan in this phase
builds on: create the `PrivacyInfo.xcprivacy` manifest and wire it into the Xcode project
(SHIP-02), switch the distribution export config to `app-store-connect`, bump the marketing
version to 4.1, and add the `just ios-build-release` recipe that the CI workflow (Plan 03)
will invoke (SHIP-01 build-config).

Purpose: These are pure-config artifacts with zero device/cloud dependency — they can be
verified locally with `plutil`/`yamllint` long before any signing secret exists. Getting
them right first de-risks the CI plan and prevents ITMS-91053 on the first upload.

Output: `PrivacyInfo.xcprivacy` (new), edited `project.yml`, `Info.plist`,
`ExportOptions.plist`, and a new `ios-build-release` recipe in `justfile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signing-testflight-pipeline/57-CONTEXT.md
@.planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md
@.planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md

<interfaces>
<!-- Current iOS target sources list (project.yml hp41-gui_iOS target) — append PrivacyInfo.xcprivacy as last entry -->
sources:
  - path: Sources
  - path: Assets.xcassets
  - path: Externals
  - path: hp41-gui_iOS
  - path: assets   (buildPhase: resources, type: folder)
  - path: LaunchScreen.storyboard

<!-- Current version keys (project.yml info.properties + Info.plist) -->
CFBundleShortVersionString: 1.0.0   -> target: 4.1   (D-57.6)
CFBundleVersion: "1.0.0"            -> LEAVE as baseline (CI PlistBuddy overwrites at build time, D-57.5)

<!-- DEVELOPMENT_TEAM already configured in project.yml settingGroups.app.base -->
DEVELOPMENT_TEAM: 2P4R8QSWT4
CODE_SIGN_STYLE: Automatic
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyInfo.xcprivacy and wire it into the Xcode project (SHIP-02)</name>
  <files>hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy, hp41-gui/src-tauri/gen/apple/project.yml</files>
  <read_first>
    - .planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md (Pattern 6 — exact PrivacyInfo.xcprivacy XML; Pattern 7 — project.yml sources entry; Pitfall 6 — file-on-disk is NOT enough, must be a source)
    - .planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md (PrivacyInfo.xcprivacy section + project.yml section — exact source-list before/after)
    - hp41-gui/src-tauri/gen/apple/project.yml (the file being edited — hp41-gui_iOS target sources list at the `targets:` block)
  </read_first>
  <action>
    Create `hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy` as a plist declaring a single
    `NSPrivacyAccessedAPITypes` array with one dict: `NSPrivacyAccessedAPIType` =
    `NSPrivacyAccessedAPICategoryFileTimestamp` and `NSPrivacyAccessedAPITypeReasons` = `[C617.1]`
    (D-57.8 — file-timestamp access from autosave I/O at `Library/Application Support/ch.talent-factory.hp41/autosave.json`).
    Use the exact XML structure from 57-RESEARCH.md Pattern 6.
    Then edit `project.yml`: in the `hp41-gui_iOS` target `sources:` list, append a final entry
    `- path: PrivacyInfo.xcprivacy` with an inline comment noting SHIP-02 / C617.1. XcodeGen
    (per 57-RESEARCH.md Pattern 7) handles `.xcprivacy` natively. Do NOT add it under
    `buildPhase: resources` (Pitfall: XcodeGen #1459 double-add) — a bare `path:` entry is correct.
    If the executor cannot regenerate the .pbxproj in this task, that is fine — the project.yml
    source entry is the source of truth; Plan 03 CI does not regenerate, so document in the SUMMARY
    whether the committed .pbxproj already references the manifest (fallback per 57-RESEARCH.md Open Q2).
  </action>
  <verify>
    <automated>plutil -lint hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy` exits 0 (OK)
    - `grep -q NSPrivacyAccessedAPICategoryFileTimestamp hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy` exits 0
    - `grep -q 'C617.1' hp41-gui/src-tauri/gen/apple/PrivacyInfo.xcprivacy` exits 0
    - `grep -q 'PrivacyInfo.xcprivacy' hp41-gui/src-tauri/gen/apple/project.yml` exits 0 (source entry present)
    - The PrivacyInfo source entry is NOT under a `buildPhase: resources` line
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy exists, is valid plist with C617.1, and is referenced as a source in the iOS target.</done>
</task>

<task type="auto">
  <name>Task 2: Switch ExportOptions to app-store-connect and bump marketing version to 4.1 (SHIP-01 config)</name>
  <files>hp41-gui/src-tauri/gen/apple/ExportOptions.plist, hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist, hp41-gui/src-tauri/gen/apple/project.yml</files>
  <read_first>
    - .planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md (Pattern 5 — ExportOptions target XML; Pitfall 1 — app-store is deprecated, app-store-connect is canonical for Xcode 26; D-57.5/D-57.6 build/marketing version split)
    - .planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md (ExportOptions.plist + Info.plist + project.yml sections — exact before/after)
    - hp41-gui/src-tauri/gen/apple/ExportOptions.plist (current: method=debugging)
    - hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist (current: CFBundleShortVersionString 1.0.0)
  </read_first>
  <action>
    Edit `ExportOptions.plist`: change `method` from `debugging` to `app-store-connect`
    (Xcode 26 canonical, per 57-RESEARCH.md Pitfall 1), and add `signingStyle` = `automatic`
    and `teamID` = `2P4R8QSWT4`. Use the exact target XML from 57-PATTERNS.md ExportOptions section.
    Edit `Info.plist`: set `CFBundleShortVersionString` to `4.1` (D-57.6). LEAVE `CFBundleVersion`
    at `1.0.0` — the CI runner injects the real build number via PlistBuddy (D-57.5); do NOT
    hardcode a build number here.
    Edit `project.yml` `info.properties`: set `CFBundleShortVersionString: 4.1` to keep it in
    sync with Info.plist (XcodeGen merges these). Leave `CFBundleVersion: "1.0.0"`.
    Note for SUMMARY: Tauri CLI overwrites ExportOptions.plist at build time when
    `--export-method app-store-connect` is passed (57-RESEARCH.md Pattern 5), so the committed
    file is documentation + fallback for direct xcodebuild runs.
  </action>
  <verify>
    <automated>plutil -lint hp41-gui/src-tauri/gen/apple/ExportOptions.plist && plutil -lint hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint` exits 0 for both `ExportOptions.plist` and `Info.plist`
    - `ExportOptions.plist` contains `<key>method</key>` followed by value `app-store-connect`
    - `ExportOptions.plist` does NOT contain the bare string `debugging`
    - `/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist` prints `4.1`
    - `/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" hp41-gui/src-tauri/gen/apple/hp41-gui_iOS/Info.plist` prints `1.0.0` (baseline preserved)
    - `grep -q 'CFBundleShortVersionString: 4.1' hp41-gui/src-tauri/gen/apple/project.yml` exits 0
  </acceptance_criteria>
  <done>ExportOptions uses app-store-connect/automatic/team; marketing version is 4.1 in Info.plist + project.yml; build number baseline untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the just ios-build-release recipe (SHIP-01 build entry point)</name>
  <files>justfile</files>
  <read_first>
    - .planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md (Pattern 8 — ios-build-release recipe body; "just is sole task runner" constraint)
    - .planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md (justfile section — placement after ios-build, npm ci + dist flags)
    - justfile (lines 257-276 — existing ios-build / ios-sim recipes + iOS section header conventions: [group('ios')], cd hp41-gui first, npm run tauri)
  </read_first>
  <action>
    Add a new recipe `ios-build-release` to `justfile`, inserted immediately after the existing
    `ios-build` recipe (currently ending at the `cd hp41-gui && npm run tauri ios build` line)
    and before `ios-sim`. Follow the existing iOS recipe conventions: a `[group('ios')]`
    attribute, a doc comment, `cd hp41-gui && npm ci` first (lockfile-strict, mirroring gui-build),
    then `cd hp41-gui && npm run tauri ios build -- --export-method app-store-connect --ci`.
    The doc comment must state it requires `APPLE_API_KEY`, `APPLE_API_ISSUER`,
    `APPLE_API_KEY_PATH` env vars for signing (consumed by Tauri CLI per 57-RESEARCH.md Pattern 1).
    Do NOT call cargo or xcodebuild directly — `just` is the sole task runner (CLAUDE.md).
  </action>
  <verify>
    <automated>just --list 2>/dev/null | grep -q 'ios-build-release'</automated>
  </verify>
  <acceptance_criteria>
    - `just --list` (or `just --summary`) includes `ios-build-release`
    - `just --dry-run ios-build-release` exits 0 (recipe parses) OR `just --list` lists it (if dry-run requires env)
    - The recipe body contains `--export-method app-store-connect` and `--ci`
    - The recipe body contains no raw `cargo` or `xcodebuild` invocation (`grep -A4 'ios-build-release:' justfile | grep -Eq 'cargo |xcodebuild' && exit 1 || exit 0`)
    - The recipe is annotated `[group('ios')]`
  </acceptance_criteria>
  <done>`just ios-build-release` is a parseable recipe that drives a signed app-store-connect export via npm run tauri, with no raw cargo/xcodebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → bundled IPA | PrivacyInfo.xcprivacy + version metadata become part of the shipped app; a missing/incorrect manifest is rejected by Apple (ITMS-91053). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | PrivacyInfo.xcprivacy over-declaration | accept | Declares ONLY the file-timestamp API actually used (C617.1, autosave I/O). No tracking domains, no other reason codes. Verified by grep that only C617.1 is present. |
| T-57-02 | Tampering | Hardcoded build number drifting from CI | mitigate | CFBundleVersion stays at the 1.0.0 baseline in committed files; CI PlistBuddy is the single writer (D-57.5). Acceptance asserts the committed baseline is unchanged so a stray hardcoded number cannot break TestFlight's strictly-increasing rule. |

(No secrets are handled in this plan — the secrets/signing threat surface lives in Plan 03's threat model.)
</threat_model>

<verification>
- `plutil -lint` passes for PrivacyInfo.xcprivacy, ExportOptions.plist, Info.plist.
- `just --list` shows `ios-build-release`.
- `just gui-ci` still green (no Rust/engine change — sanity that nothing else broke).
- Note: `just gui-ci` is a host-target check and cannot see `#[cfg(mobile)]` errors; this plan touches no mobile Rust, so that blind spot is not in play here.
</verification>

<success_criteria>
- SHIP-02 config artifact complete: valid PrivacyInfo.xcprivacy referenced in project.yml.
- SHIP-01 build-config complete: ExportOptions=app-store-connect, marketing version 4.1, `ios-build-release` recipe present.
- No secrets touched; no engine/Rust change; Frozen Invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/57-signing-testflight-pipeline/57-01-SUMMARY.md` when done.
Record: whether the committed `.pbxproj` already references PrivacyInfo.xcprivacy (or whether a
regenerate/manual-edit is still owed before Plan 04's upload), and that Tauri overwrites
ExportOptions.plist at build time.
</output>
