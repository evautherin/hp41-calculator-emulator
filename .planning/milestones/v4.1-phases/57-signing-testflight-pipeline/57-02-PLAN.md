---
phase: 57-signing-testflight-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app-icon-1024.png
  - hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset
  - hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard
autonomous: false
requirements: [SHIP-03]
must_haves:
  truths:
    - "A real HP-41-styled 1024x1024 app-icon master exists (not the auto-generated placeholder)"
    - "The founder has approved the icon/launch-screen art direction BEFORE the generated slots are committed"
    - "All required iOS AppIcon slots are present (>= 18 PNG slots, including the 1024 marketing slot)"
    - "The launch screen has HP-41 branding (dark faceplate background, not blank systemBackground white)"
  artifacts:
    - path: "hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "Icon slot manifest"
      contains: "ios-marketing"
    - path: "hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard"
      provides: "Branded launch screen"
      contains: "backgroundColor"
  key_links:
    - from: "app-icon-1024.png"
      to: "AppIcon.appiconset"
      via: "cargo tauri icon generation"
      pattern: "AppIcon-512@2x"
---

<objective>
Replace the auto-generated placeholder iOS app icon with a real HP-41-styled icon (regenerated
from a 1024×1024 master) and brand the launch screen with the HP-41 dark-faceplate aesthetic
(SHIP-03). A founder approval checkpoint gates committing the generated art so the visual
direction is signed off before it lands.

Purpose: D-57.7 — a placeholder icon is NOT acceptable for the TestFlight foundation build; the
founder wants the app to look like a real product (gold-on-dark / brushed-faceplate aesthetic).
The phase UI gate was deliberately skipped (no interactive React UI here), so this checkpoint is
the art-direction approval instrument instead of a UI-SPEC.

Output: `app-icon-1024.png` master, regenerated `AppIcon.appiconset` slots, branded
`LaunchScreen.storyboard`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signing-testflight-pipeline/57-CONTEXT.md
@.planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md
@.planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md

<interfaces>
<!-- Existing AppIcon slots (18 PNG + Contents.json) — cargo tauri icon overwrites all -->
Marketing 1024: AppIcon-512@2x.png   (idiom: ios-marketing, scale: 1x)
iPhone:  20@2x 20@3x 29@2x 29@3x 40@2x 40@3x 60@2x 60@3x
iPad:    20@1x 20@2x 29@1x 29@2x 40@1x 40@2x 76@1x 76@2x 83.5@2x

<!-- LaunchScreen current background — replace with HP-41 dark tone -->
current: <color key="backgroundColor" systemColor="systemBackgroundColor"/>
target HP-41 dark: red=0.102 green=0.102 blue=0.102 alpha=1 (custom sRGB)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the HP-41 app-icon + launch-screen art direction before the master is finalized and slots are committed</decision>
  <context>
    D-57.7 mandates a real HP-41-styled icon (gold-on-dark / brushed-faceplate aesthetic), not a
    placeholder. RESEARCH Open Question #4 flags the exact visual elements as unconfirmed (calculator
    face? logo? stylized "41"?). This gate captures the founder's choice so the 1024×1024 master and
    the generated slots are not committed against the wrong direction. The phase deliberately skipped
    a UI-SPEC because there is no interactive UI here — this is the substitute approval gate.
  </context>
  <options>
    <option id="faceplate">
      <name>Stylized HP-41 faceplate / keyboard crop</name>
      <pros>Most recognizable as "the HP-41"; matches the emulator's own brushed-faceplate look</pros>
      <cons>Busy at small (40px) sizes; fine detail can muddy</cons>
    </option>
    <option id="numeral-41">
      <name>Gold "41" numeral on dark brushed background</name>
      <pros>Crisp at every size incl. 20pt; clean product feel</pros>
      <cons>Less literally "a calculator"</cons>
    </option>
    <option id="display-glyph">
      <name>14-segment display glyph (e.g. "HP-41") on dark</name>
      <pros>Evokes the LCD; distinctive</pros>
      <cons>Segment font may be hard to read at small sizes</cons>
    </option>
  </options>
  <resume-signal>Select: faceplate, numeral-41, display-glyph, or describe a different direction. Also confirm the dark background hex (default #1A1A1A) and whether the launch screen should show the icon, an "HP-41" label, or both.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Produce the 1024x1024 master and regenerate all AppIcon slots (SHIP-03)</name>
  <files>app-icon-1024.png, hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset</files>
  <read_first>
    - .planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md (Pattern 9 — `cargo tauri icon` invocation, --ios-color, no transparency, issue #11578 slot-miss + manual verification)
    - .planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md (AppIcon.appiconset section — 18 slot filenames + slot-count verification command)
    - hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/Contents.json (existing slot manifest being regenerated)
  </read_first>
  <action>
    Create the 1024×1024 master at repo root as `app-icon-1024.png`, realizing the art direction
    approved in Task 1 (gold-on-dark per D-57.7). The icon MUST be fully opaque — App Store Connect
    rejects transparent iOS icons (57-RESEARCH.md Pattern 9). Then regenerate the slots:
    `cd hp41-gui && npm run tauri icon -- ../app-icon-1024.png --ios-color "<approved-hex, default #1A1A1A>"`.
    Because of Tauri issue #11578 (some iOS slots can be missed), after generation manually verify
    every slot in `Contents.json` has a matching PNG and fill any gap by re-running or copying the
    correct size. Do NOT hand-edit Contents.json structure — the tool regenerates it.
    `cargo tauri icon` is the only sanctioned resizer (do not hand-roll ImageMagick slot scripts).
  </action>
  <verify>
    <automated>test "$(ls hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/*.png | wc -l | tr -d ' ')" -ge 18</automated>
  </verify>
  <acceptance_criteria>
    - `app-icon-1024.png` exists at repo root and is 1024×1024 (`sips -g pixelWidth -g pixelHeight app-icon-1024.png` reports 1024 × 1024)
    - `app-icon-1024.png` has no alpha channel / is opaque (`sips -g hasAlpha app-icon-1024.png` reports `no`, OR the marketing slot is opaque)
    - `ls hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/*.png | wc -l` >= 18
    - Every `filename` in `Contents.json` resolves to an existing PNG (no missing slot — verify each listed filename exists)
    - `grep -q ios-marketing hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/Contents.json` (1024 marketing slot present)
    - `plutil -lint hp41-gui/src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/Contents.json` exits 0
  </acceptance_criteria>
  <done>A real HP-41-styled 1024 master exists; all >=18 AppIcon slots (incl. marketing) are regenerated, opaque, and every Contents.json filename resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Brand the LaunchScreen.storyboard (SHIP-03)</name>
  <files>hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard</files>
  <read_first>
    - .planning/phases/57-signing-testflight-pipeline/57-RESEARCH.md (Pattern 10 — storyboard background color + optional centered label/image)
    - .planning/phases/57-signing-testflight-pipeline/57-PATTERNS.md (LaunchScreen.storyboard section — exact current viewController/view XML + target background color replacement + remove unused systemColor resources block)
    - hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard (the file being edited — currently systemBackgroundColor)
  </read_first>
  <action>
    Edit `LaunchScreen.storyboard`: replace the view's `<color key="backgroundColor"
    systemColor="systemBackgroundColor"/>` with an explicit HP-41 dark faceplate color
    (the approved hex; default red=0.102 green=0.102 blue=0.102 alpha=1, custom sRGB) per
    57-PATTERNS.md. Remove the now-unused `<resources><systemColor>` block so the storyboard
    stays valid. If the founder approved showing the icon and/or an "HP-41" label on launch
    (Task 1 resume-signal), add a centered `<imageView>` (using the AppIcon asset) and/or a
    centered `<label>` as `<subviews>` with safe-area-anchored constraints, following standard
    storyboard XML — no Xcode Interface Builder required. Otherwise the dark background alone is
    the minimum viable branding (not a blank white screen).
  </action>
  <verify>
    <automated>plutil -lint hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard 2>/dev/null || xmllint --noout hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard</automated>
  </verify>
  <acceptance_criteria>
    - `xmllint --noout hp41-gui/src-tauri/gen/apple/LaunchScreen.storyboard` exits 0 (well-formed XML)
    - The storyboard no longer contains `systemColor="systemBackgroundColor"` for the root view background
    - The root view `backgroundColor` is an explicit (non-system) dark color
    - No dangling reference to a removed `<systemColor>` resource remains
  </acceptance_criteria>
  <done>Launch screen shows the HP-41 dark-faceplate branding (not blank white); storyboard is well-formed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design asset → shipped bundle | The icon/launch art ships in the IPA; a transparent or malformed icon is rejected by App Store Connect. No secrets in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering | Wrong/placeholder icon shipped to testers | mitigate | Founder approval checkpoint (blocking) before slots are committed; opacity + slot-count + marketing-slot acceptance assertions prevent a malformed asset reaching upload. |

(No signing secrets handled in this plan — secret/CI threats live in Plan 03.)
</threat_model>

<verification>
- Icon slot count >= 18; every Contents.json filename resolves; marketing slot present; opaque.
- LaunchScreen storyboard well-formed, dark-branded, no system-color background.
- Founder has signed off the art direction (Task 1 checkpoint).
</verification>

<success_criteria>
- SHIP-03 complete: real HP-41-styled icon in all slots + branded launch screen, both approved.
- No engine/Rust change; no secrets; Frozen Invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/57-signing-testflight-pipeline/57-02-SUMMARY.md` when done.
Record: the approved art direction (which option + hex), the final slot count, and any slot the
tool missed that needed a manual fill (issue #11578).
</output>
