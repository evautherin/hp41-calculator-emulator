---
phase: 40-documentation-adrs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - docs/architecture-history.md
autonomous: true
requirements:
  - TIME-DOC-04
  - TIME-DOC-05
  - TIME-DOC-06
must_haves:
  truths:
    - "README carries a v3.2 soft-claim bullet for Time Pac behavioral emulation under Features"
    - "CLAUDE.md has a `### v3.2 additions (Time Pac Emulation, Phases 38-42)` block with per-phase subsections for Phases 38, 39, 40 and stubs for 41-42"
    - "`docs/architecture-history.md` has a `## v3.2 additions` section with per-phase narratives for Phases 38-39 and stubs for 40-42"
  artifacts:
    - path: "README.md"
      provides: "v3.2 soft-claim bullet in Features section"
      contains: "Time Pac"
    - path: "CLAUDE.md"
      provides: "v3.2 additions block with per-phase summaries"
      contains: "### v3.2 additions"
    - path: "docs/architecture-history.md"
      provides: "v3.2 narrative section"
      contains: "## v3.2 additions"
  key_links:
    - from: "README.md"
      to: "docs/hp41-time-divergences.md"
      via: "Markdown link in soft-claim bullet"
      pattern: "hp41-time-divergences"
    - from: "README.md"
      to: "docs/hp41-time-function-matrix.md"
      via: "Markdown link in soft-claim bullet"
      pattern: "hp41-time-function-matrix"
    - from: "CLAUDE.md"
      to: "docs/architecture-history.md"
      via: "Origin cross-reference"
      pattern: "architecture-history.md"
---

<objective>
Update project-level documentation with v3.2 Time Pac content: README soft-claim, CLAUDE.md v3.2 additions block, and architecture-history.md v3.2 narrative section.

Purpose: TIME-DOC-04 requires a README soft-claim. TIME-DOC-05 requires CLAUDE.md v3.2 additions. TIME-DOC-06 requires architecture-history.md v3.2 narrative. These capture the Phase 38-39 decisions and implementation at the project documentation level.

Output: Updated README.md, CLAUDE.md, and docs/architecture-history.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-documentation-adrs/40-CONTEXT.md
@.planning/phases/40-documentation-adrs/40-PATTERNS.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-CONTEXT.md
@.planning/phases/39-hp41-cli-cli-integration-live-display/39-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README v3.2 soft-claim bullet</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/phases/40-documentation-adrs/40-CONTEXT.md
  </read_first>
  <action>
    Per D-40.7: Add a v3.2 soft-claim bullet in the `## Features` section of `README.md`, immediately after the v3.1 bullet (currently at lines 54-55). Follow the v3.1 bullet tone and format. The bullet must:
    - Start with `- v3.2 ships Time Pac behavioral emulation`
    - Mention 35 XEQ entry points and real-time clock/stopwatch/alarm capability
    - Link to `docs/hp41-time-divergences.md` as "documented divergences"
    - Link to `docs/hp41-time-function-matrix.md` as "Time Pac Function Matrix"
    - NOT include "feature-complete per Owner's Manual" -- that is the hard-claim deferred to Phase 42

    Per D-40.8: Do NOT add a v3.2 row to the Releases table -- that is deferred to Phase 42 ship.
  </action>
  <verify>
    <automated>grep -c "v3.2 ships Time Pac" /Users/daniel/GitRepository/hp41-calculator-emulator/README.md && grep -c "hp41-time-divergences" /Users/daniel/GitRepository/hp41-calculator-emulator/README.md && grep -c "hp41-time-function-matrix" /Users/daniel/GitRepository/hp41-calculator-emulator/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a bullet starting with `- v3.2 ships Time Pac behavioral emulation`
    - The bullet appears after the v3.1 bullet and before `**Terminal UI (`hp41-cli`)**`
    - The bullet contains `[documented divergences](docs/hp41-time-divergences.md)` link
    - The bullet contains `[Time Pac Function Matrix](docs/hp41-time-function-matrix.md)` link
    - The bullet does NOT contain "feature-complete per Owner's Manual"
    - The Releases table is unchanged (no v3.2 row added)
  </acceptance_criteria>
  <done>README v3.2 soft-claim bullet added with divergences and function matrix links</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md v3.2 additions block and architecture-history.md v3.2 narrative</name>
  <files>CLAUDE.md, docs/architecture-history.md</files>
  <read_first>
    CLAUDE.md
    docs/architecture-history.md
    .planning/phases/40-documentation-adrs/40-CONTEXT.md
    .planning/phases/40-documentation-adrs/40-PATTERNS.md
    .planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-CONTEXT.md
    .planning/phases/39-hp41-cli-cli-integration-live-display/39-CONTEXT.md
    .planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-01-SUMMARY.md
    .planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-03-SUMMARY.md
    .planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-05-SUMMARY.md
    .planning/phases/39-hp41-cli-cli-integration-live-display/39-01-SUMMARY.md
    .planning/phases/39-hp41-cli-cli-integration-live-display/39-02-SUMMARY.md
  </read_first>
  <action>
    **CLAUDE.md (D-40.9, D-40.10):**

    Add a new `### v3.2 additions (Time Pac Emulation, Phases 38--42)` block after the `### v3.1 additions` block (which ends around the `**v3.1 file landmarks**` section, before `## Tech Stack`). Follow the exact structural pattern of the v3.1 block.

    Opening italics line: reference `docs/architecture-history.md` section v3.2 additions; note this is the SECOND `### v3.x additions` block.

    Per-phase subsections to include (use D-NN.x decision IDs, bold subjects, parenthetical decision refs):

    `#### Phase 38 -- XROM Framework + Clock/Date/Stopwatch/Alarm Core (shipped 2026-05-24)` -- ~10-15 bullets covering:
    - TIME_MODULE XROM ID 26 registration (D-carried.5), xrom_resolve bit-2 arm fires LAST
    - `default_xrom_modules() = 0b0000_0111` + `migrate_after_load()` (D-carried.5)
    - Direct `SystemTime::now()` in hp41-core (D-38.1) + `time_offset_secs: i64` (D-38.2)
    - OS local time via `libc::localtime_r` / `GetLocalTime` (D-38.3)
    - Stopwatch fields with split serde shapes (D-38.6, D-38.7): `stopwatch_mode`, `stopwatch_accumulated`, `stopwatch_start`, `stopwatch_split`
    - `AlarmType` enum + `Vec<AlarmEntry>` on CalcState (D-38.8-11)
    - Interrupting control alarm DEFERRED (D-38.4), documented divergence
    - JDN Fliegel-Van Flandern date arithmetic, string-split-at-decimal parsing (D-carried.2)
    - `ModalProgram::Time(TimeStep)` variant (D-carried.4)
    - Zero new runtime deps (D-carried.1)
    - 35 new `Op` variants in `dispatch()` + `execute_op()` (4-way invariant items 1+2)
    - Quality gates: Free42 contamination guard extended to `time/` tree

    `#### Phase 39 -- CLI Integration + Live Display (shipped 2026-05-25)` -- ~8-10 bullets covering:
    - `docs/hp41-time-functions.json` authored (D-39.9), 35-entry, 7-category
    - Fourth `OnceLock<Vec<HelpEntry>>` in `help_data.rs` (D-39.12), 4-pool chain
    - 35 new `op_display_name` arms in `prgm_display.rs` (4-way invariant item 3)
    - `?` help overlay "Time Pac (XROM 26)" section (D-39.13)
    - Live clock/stopwatch display, pull-on-redraw (D-39.1-2)
    - Stopwatch keyboard mode (D-39.4-5)
    - Alarm event draining via event_buffer (D-39.6-8)
    - XROM shadowing extended to TIME_MODULE.ops (D-39.14)

    `#### Phase 40 -- Documentation & ADRs (shipped 2026-05-25)` -- ~5 bullets (brief):
    - `docs/hp41-time-divergences.md` authored (D-40.3)
    - 3 ADRs: v3.2-001 (clock access), v3.2-002 (live display), v3.2-003 (alarm catalog) (D-40.5)
    - `scripts/docs-matrix` fourth invocation (D-40.1-2)
    - README v3.2 soft-claim (D-40.7)
    - CLAUDE.md v3.2 additions block authored (D-40.9)

    `#### Phase 41 -- GUI Integration (IN PROGRESS)` -- single-line stub.
    `#### Phase 42 -- Test Hardening & Quality Gates (TBD)` -- single-line stub.

    D-40.10 frozen invariants addendum: add a brief paragraph after the per-phase subsections documenting Phase 38 CalcState additions with their serde shapes: `time_offset_secs: i64` (`#[serde(default)]`), `stopwatch_mode: StopwatchMode` (`#[serde(default)]`), `stopwatch_accumulated: f64` (`#[serde(default)]`), `stopwatch_split: f64` (`#[serde(default)]`), `stopwatch_start: Option<Instant>` (`#[serde(default, skip)]` -- transient), `alarms: Vec<AlarmEntry>` (`#[serde(default)]`), `clock_display_mode` and related flags. XROM bit-2 arm = TIME_MODULE (XROM 26).

    **docs/architecture-history.md (D-40.11):**

    Add a new `## v3.2 additions (Time Pac Emulation, Phases 38--42 -- 41--42 IN PROGRESS)` section at the end of the file, BEFORE the `## Quality Gate History` table. Follow the exact structural pattern of the v3.1 section (~80 lines total).

    Opening paragraph: describe Time Pac as third XROM module, HP 82182A (HP 00041-90035, 1982), 35 Op variants across clock/date/stopwatch/alarm families. Mention the novel additions: SystemTime clock access, pull-on-redraw live display, Vec<AlarmEntry> alarm catalog, time_offset_secs field. This is the FIRST module introducing real-time behavior.

    Per-phase narrative subsections:
    - `### Phase 38 -- XROM Framework + Clock/Date/Stopwatch/Alarm Core (shipped 2026-05-24)` -- ~20-40 lines, dense prose covering all key decisions and implementation details. Calibrate to v3.1 Phase 33 depth.
    - `### Phase 39 -- CLI Integration + Live Display (shipped 2026-05-25)` -- ~15-25 lines covering JSON, help data, live display, stopwatch mode, alarm draining.
    - `### Phase 40 -- Documentation & ADRs (shipped 2026-05-25)` -- brief stub, filled after this plan executes.
    - `### Phase 41 -- GUI Integration (IN PROGRESS)` -- stub.
    - `### Phase 42 -- Test Hardening & Quality Gates (TBD)` -- stub.

    Closing block: `**Frozen invariants preserved across v3.2:**` -- stub with placeholder bullets for SC-4, 4-way exhaustive match, deny(clippy::unwrap_used), save-file backward compat, MSRV, Free42 guard. Mark as "(to be completed after Phase 42)".

    Also update the Quality Gate History table at the end of the file to add a `v3.2 (Phase 42)` column header (leave cells as "TBD" -- Phase 42 fills them).
  </action>
  <verify>
    <automated>grep -c "### v3.2 additions" /Users/daniel/GitRepository/hp41-calculator-emulator/CLAUDE.md && grep -c "## v3.2 additions" /Users/daniel/GitRepository/hp41-calculator-emulator/docs/architecture-history.md && grep -c "#### Phase 38" /Users/daniel/GitRepository/hp41-calculator-emulator/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `### v3.2 additions (Time Pac Emulation, Phases 38` heading
    - CLAUDE.md contains `#### Phase 38` and `#### Phase 39` and `#### Phase 40` subsections under v3.2 additions
    - CLAUDE.md contains `#### Phase 41` and `#### Phase 42` stubs
    - CLAUDE.md v3.2 block appears after v3.1 block and before `## Tech Stack`
    - CLAUDE.md contains D-40.10 frozen invariants addendum mentioning `time_offset_secs`, `stopwatch_mode`, `alarms: Vec<AlarmEntry>`, `clock_display_mode`, XROM bit-2
    - `docs/architecture-history.md` contains `## v3.2 additions (Time Pac Emulation, Phases 38` heading
    - `docs/architecture-history.md` contains `### Phase 38` and `### Phase 39` per-phase narratives
    - `docs/architecture-history.md` v3.2 section appears before `## Quality Gate History`
    - `docs/architecture-history.md` Quality Gate History table has a v3.2 column header
  </acceptance_criteria>
  <done>CLAUDE.md v3.2 additions block and architecture-history.md v3.2 narrative section authored with per-phase detail for Phases 38-40 and stubs for 41-42</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan -- documentation-only changes to project-level Markdown files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Information Disclosure | README/CLAUDE.md | accept | Public repo; documentation is intended to be public |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- README.md has v3.2 soft-claim bullet with divergences + matrix links
- CLAUDE.md has v3.2 additions block with Phase 38-40 subsections
- `docs/architecture-history.md` has v3.2 narrative with Phase 38-39 detail
</verification>

<success_criteria>
- TIME-DOC-04 satisfied: README v3.2 soft-claim present
- TIME-DOC-05 satisfied: CLAUDE.md v3.2 additions block complete
- TIME-DOC-06 satisfied: architecture-history.md v3.2 narrative section present
</success_criteria>

<output>
Create `.planning/phases/40-documentation-adrs/40-03-SUMMARY.md` when done
</output>
