---
phase: 40-documentation-adrs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/docs-matrix/src/main.rs
  - justfile
  - docs/hp41-time-function-matrix.md
  - docs/hp41-time-divergences.md
autonomous: true
requirements:
  - TIME-DOC-01
  - TIME-DOC-02
must_haves:
  truths:
    - "`just docs-matrix` generates `docs/hp41-time-function-matrix.md` from `docs/hp41-time-functions.json`"
    - "`just docs-matrix-check` passes with zero diff on all four matrices"
    - "`docs/hp41-time-divergences.md` documents accuracy-factor no-op, host-clock backing, stopwatch freeze-on-save, interrupting-alarm deferral, and centisecond stopwatch resolution with 5-field entries"
  artifacts:
    - path: "docs/hp41-time-function-matrix.md"
      provides: "Generated Time Pac function matrix from JSON"
    - path: "docs/hp41-time-divergences.md"
      provides: "Time Pac divergences catalog (3-bucket, D-40-NN numbering)"
      contains: "D-40-01"
  key_links:
    - from: "scripts/docs-matrix/src/main.rs"
      to: "docs/hp41-time-functions.json"
      via: "basename dispatch branch"
      pattern: "hp41-time-functions.json"
    - from: "justfile"
      to: "docs/hp41-time-function-matrix.md"
      via: "docs-matrix recipe invocation"
      pattern: "hp41-time-function"
---

<objective>
Extend the docs-matrix pipeline to generate the fourth function matrix (Time Pac) and author the Time Pac divergences catalog.

Purpose: TIME-DOC-01 requires `docs/hp41-time-function-matrix.md` generated via `just docs-matrix` alongside the existing cv/math1/stat1 matrices. TIME-DOC-02 requires `docs/hp41-time-divergences.md` documenting emulator divergences from OM 00041-90035.

Output: `docs/hp41-time-function-matrix.md` (generated), `docs/hp41-time-divergences.md` (authored), updated `scripts/docs-matrix/src/main.rs` and `justfile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-documentation-adrs/40-CONTEXT.md
@.planning/phases/40-documentation-adrs/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs-matrix pipeline for Time Pac (4th invocation)</name>
  <files>scripts/docs-matrix/src/main.rs, justfile, docs/hp41-time-function-matrix.md</files>
  <read_first>
    scripts/docs-matrix/src/main.rs
    justfile
    docs/hp41-time-functions.json
    docs/hp41-stat1-function-matrix.md
  </read_first>
  <action>
    Per D-40.1: In `scripts/docs-matrix/src/main.rs`, insert a new `else if` branch in `render_markdown()` BEFORE the final `else` fallback (currently at line 76). The new branch matches `basename.ends_with("hp41-time-functions.json")` and returns the tuple `("# HP-41C Time Pac Function Matrix", "docs/hp41-time-functions.json")`. The insertion point is between the `hp41-stat1-functions.json` arm and the `else` fallback. No other changes to the file.

    Per D-40.2: In `justfile`, extend the `docs-matrix` recipe (starting at line 188) by appending a 4th `cargo run` invocation after the stat1 line: input `docs/hp41-time-functions.json`, output `docs/hp41-time-function-matrix.md`. Update the comment above the recipe to mention four (not three) function matrices and add the 4th source line. Also extend the `docs-matrix-check` recipe (starting at line 199) by appending a 4th `cargo run` invocation writing to `/tmp/hp41-time-function-matrix-check.md` followed by a `diff -u docs/hp41-time-function-matrix.md /tmp/hp41-time-function-matrix-check.md` line.

    After both edits, run `just docs-matrix` to generate `docs/hp41-time-function-matrix.md`. Then run `just docs-matrix-check` to verify all four matrices pass the drift check with zero diff.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && just docs-matrix && just docs-matrix-check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/docs-matrix/src/main.rs` contains `hp41-time-functions.json` in a basename dispatch branch
    - `justfile` `docs-matrix` recipe has 4 `cargo run` invocations (cv, math1, stat1, time)
    - `justfile` `docs-matrix-check` recipe has 4 `cargo run` + 4 `diff -u` blocks
    - `docs/hp41-time-function-matrix.md` exists and contains `# HP-41C Time Pac Function Matrix`
    - `just docs-matrix-check` exits 0 (all four matrices match)
    - Existing cv/math1/stat1 matrices are bit-for-bit unchanged after regeneration
  </acceptance_criteria>
  <done>Fourth function matrix generated and CI drift-check passes for all four matrices</done>
</task>

<task type="auto">
  <name>Task 2: Author Time Pac divergences catalog</name>
  <files>docs/hp41-time-divergences.md</files>
  <read_first>
    docs/hp41-stat1-divergences.md
    docs/hp41-math1-divergences.md
    .planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-CONTEXT.md
    .planning/phases/39-hp41-cli-cli-integration-live-display/39-CONTEXT.md
    hp41-core/src/ops/time/clock.rs
    hp41-core/src/ops/time/stopwatch.rs
    hp41-core/src/ops/time/alarm.rs
  </read_first>
  <action>
    Per D-40.3 and D-40.4: Create `docs/hp41-time-divergences.md` following the exact structure of `docs/hp41-stat1-divergences.md` (3-bucket catalog, D-40-NN numbering, 5-field entry shape per D-30.5).

    Header: substitute "Stat 1 Pac" with "Time Pac", OM part number "HP 00041-90030, 1979" with "HP 00041-90035, 1982" (Time Module OM), "Phase 35 / Plan 35-02 (STAT-DOC-03)" with "Phase 40 / Plan 40-01 (TIME-DOC-02)", and all `D-35-NN` references with `D-40-NN`.

    "How to Use This Document" block: substitute identifiers as above, note that Time Pac is the third XROM module.

    Three-bucket structure (identical section headers to stat1-divergences.md):
    - `## 1. OM Divergences` -- include any entries discovered from Phase 38/39 source files. If none, state "No OM numerical divergences identified in Phase 38-39 implementation." (same pattern as stat1 bucket 1).
    - `## 2. Emulator Extensions` -- include any non-OM features added (check Phase 38/39 context for extensions).
    - `## 3. Behavioral Policies` -- known entries per D-40.4:
      - D-40-01: CORRECT/SETAF accuracy factor no-op (D-38.1 -- host NTP clock makes crystal correction meaningless; SETAF stores value, RCLAF recalls it, CORRECT is documented no-op)
      - D-40-02: Host system clock backing (D-38.1 -- SystemTime::now() vs. crystal oscillator; all TIME/DATE/ATIME/ADATE/SETIME/SETDATE use system clock + offset)
      - D-40-03: Stopwatch freeze-on-save (D-38.6 -- Instant cannot serialize; accumulated time persists, running state does not; user must RUNSW to resume after load)
      - D-40-04: Interrupting control alarm deferral (D-38.4 -- re-entrancy against 4-level call stack not supported; message + non-interrupting control alarms work; interrupting control alarms stored but not executed)
      - D-40-05: Centisecond stopwatch resolution (D-38.7 -- matches OM resolution but uses Instant::elapsed() instead of hardware timer; effective resolution is sub-microsecond but display truncates to centiseconds)

    Each entry uses the 5-field shape: OM citation (reference HP 00041-90035 section or "N/A -- emulator policy"), Our behavior, OM behavior, Rationale, See (cross-reference D-38.x decision IDs, ADR links when they exist from Plan 40-02, source file paths). Pitfall 18 citation discipline enforced.

    Also check `hp41-core/src/ops/time/clock.rs`, `stopwatch.rs`, `alarm.rs` for any additional divergences worth documenting (e.g., date range limits, alarm catalog size limit).
  </action>
  <verify>
    <automated>grep -c "^### D-40-" /Users/daniel/GitRepository/hp41-calculator-emulator/docs/hp41-time-divergences.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hp41-time-divergences.md` exists with `# HP-41C Time Pac Emulator Divergences` heading
    - Contains "How to Use This Document" section with `D-40-NN` numbering convention
    - Contains three bucket sections: `## 1. OM Divergences`, `## 2. Emulator Extensions`, `## 3. Behavioral Policies`
    - Contains at least 5 D-40-NN entries (D-40-01 through D-40-05 minimum) covering accuracy-factor no-op, host clock backing, stopwatch freeze-on-save, interrupting alarm deferral, centisecond stopwatch resolution
    - Every entry has all 5 fields: OM citation, Our behavior, OM behavior, Rationale, See
    - Every OM citation references HP 00041-90035 (1982) or carries "N/A -- emulator policy/extension"
    - Every See field cross-references at least one D-38.x or D-39.x decision ID or source file path
  </acceptance_criteria>
  <done>Time Pac divergences catalog authored with numbered entries covering all known divergences from Phase 38/39</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan -- documentation-only changes with no runtime code, no user input processing, no network access.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | docs-matrix binary | accept | Binary is developer-side only, not shipped; CI drift-check validates output |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `just docs-matrix` succeeds and generates `docs/hp41-time-function-matrix.md`
- `just docs-matrix-check` exits 0 (all four matrices pass drift check)
- `docs/hp41-time-divergences.md` contains at least 5 numbered entries with valid 5-field shape
</verification>

<success_criteria>
- TIME-DOC-01 satisfied: function matrix generated and CI-checkable
- TIME-DOC-02 satisfied: divergences catalog authored with 3-bucket structure
</success_criteria>

<output>
Create `.planning/phases/40-documentation-adrs/40-01-SUMMARY.md` when done
</output>
