---
phase: 42-test-hardening-quality-gates
plan: 04
type: execute
wave: 4
depends_on:
  - 42-03
files_modified:
  - hp41-core/tests/time_backward_compat.rs
  - hp41-core/tests/fixtures/v31-autosave.json
  - hp41-gui/e2e/smoke.spec.js
  - README.md
autonomous: true
requirements:
  - TIME-QUAL-04
  - TIME-QUAL-06
  - TIME-QUAL-07
  - TIME-QUAL-09

must_haves:
  truths:
    - "v3.1 save file (xrom_modules: 3) loads in v3.2 with automatic migration to xrom_modules: 7, clock/alarm/stopwatch state defaulting cleanly"
    - "E2E smoke test executes at least one Time Pac workflow (DDAYS) end-to-end in the GUI on Ubuntu"
    - "README v3.2 line graduated to OM-cited hard-claim: feature-complete per Owner's Manual 00041-90035"
    - "All quality gates pass: cargo test, coverage >= 93% regions, Free42 clean, function_matrix_parity 4-pool"
  artifacts:
    - path: "hp41-core/tests/time_backward_compat.rs"
      provides: "v3.1-to-v3.2 save-file migration test"
      contains: "v31_save_loads_with_time_migration"
    - path: "hp41-core/tests/fixtures/v31-autosave.json"
      provides: "v3.1 CalcState fixture with xrom_modules: 3"
      contains: "\"xrom_modules\": 3"
    - path: "hp41-gui/e2e/smoke.spec.js"
      provides: "Time Pac DDAYS E2E smoke test"
      contains: "DDAYS"
    - path: "README.md"
      provides: "v3.2 hard-claim graduation"
      contains: "feature-complete per Owner's Manual 00041-90035"
  key_links:
    - from: "hp41-core/tests/time_backward_compat.rs"
      to: "hp41-core/src/state.rs"
      via: "serde_json::from_str + migrate_after_load"
      pattern: "migrate_after_load"
    - from: "hp41-gui/e2e/smoke.spec.js"
      to: "hp41-gui/src-tauri/src/key_map.rs"
      via: "invokeBackend dispatch_op with xeq_DDAYS"
      pattern: "xeq_DDAYS"
---

<objective>
Complete the Phase 42 quality gates: (1) backward-compat migration test for v3.1 save files per TIME-QUAL-04, (2) E2E smoke with a Time Pac DDAYS workflow per TIME-QUAL-06, (3) final quality-gate verification per TIME-QUAL-07, and (4) README hard-claim graduation per TIME-QUAL-09 / D-carried.2.

Purpose: This is the final plan in the test-hardening phase, mirroring the Phase 32 (v3.0) and Phase 37 (v3.1) graduation pattern. After this plan, v3.2 Time Pac is fully quality-gated and the README can carry the "feature-complete" claim.

Output: Backward-compat test, E2E smoke extension, README hard-claim; all quality gates green.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-test-hardening-quality-gates/42-CONTEXT.md
@.planning/phases/42-test-hardening-quality-gates/42-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v3.1 backward-compat migration test</name>
  <files>hp41-core/tests/time_backward_compat.rs, hp41-core/tests/fixtures/v31-autosave.json</files>
  <read_first>
    - hp41-core/tests/stat1_backward_compat.rs (direct structural template: fixture loading, migrate_after_load, xrom_modules bit assertion, op resolution smoke test)
    - hp41-core/tests/fixtures/v30-autosave.json (fixture template: CalcState JSON with xrom_modules: 1)
    - hp41-core/src/state.rs (CalcState struct definition, migrate_after_load function, serde attributes on time-related fields)
  </read_first>
  <action>
    Create two files per D-42.8:

    **hp41-core/tests/fixtures/v31-autosave.json:**
    A CalcState JSON snapshot representing a v3.1 save file. Must:
    1. Copy the structure from v30-autosave.json as base.
    2. Set `"xrom_modules": 3` (v3.1 default: bit 0 = Math 1, bit 1 = Stat 1, bit 2 NOT set for Time).
    3. Include `"rand_seed": "0.5"` — a non-zero value to verify rand_seed survives migration (STAT-RNG-03 / Pitfall 20 regression guard).
    4. OMIT all Time-related fields (time_offset_secs, alarms, stopwatch_*, clock_*, accuracy_factor) — they should default via #[serde(default)].
    5. OMIT all #[serde(skip)] transient fields (modal_program, modal_prompt, integ_state, solve_state, cancel_requested, print_buffer, etc.).

    **hp41-core/tests/time_backward_compat.rs:**
    Mirrors stat1_backward_compat.rs structure. Must carry `#![allow(clippy::unwrap_used)]` per D-carried.3.

    Three tests:
    1. `v31_save_loads_with_time_migration` — deserialize v31 fixture, assert pre-migration `state.xrom_modules == 3`, call `state.migrate_after_load()`, assert post-migration `state.xrom_modules == 0b0000_0111` (all 3 modules enabled). Mark xrom_modules assertions with `// LINT-EXEMPT: integer-equality -- xrom_modules is u8, not HpNum`.
    2. `v31_save_time_fields_default_cleanly` — after deserialization + migration, assert:
       - `state.time_offset_secs == 0` (default)
       - `state.alarms.is_empty()` (default)
       - `state.stopwatch_mode == StopwatchMode::Idle` (default, import from hp41_core)
       - `state.stopwatch_accumulated == 0.0` (default)
       - `state.clock_12h == false` (default)
    3. `v31_save_rand_seed_preserved` — assert `state.rand_seed.inner() == Decimal::from_str("0.5").unwrap()` confirming the v3.1 rand_seed field survives v3.2 migration. Mark with `// LINT-EXEMPT: exact Decimal equality -- rand_seed is constructed via Decimal, not f64`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test time_backward_compat -- --nocapture 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/fixtures/v31-autosave.json exists with "xrom_modules": 3 and "rand_seed": "0.5"
    - hp41-core/tests/time_backward_compat.rs exists with 3 test functions
    - cargo test -p hp41-core --test time_backward_compat exits 0
    - Migration: xrom_modules 3 -> 7 confirmed
    - Time fields default: time_offset_secs=0, alarms empty, stopwatch idle
    - rand_seed "0.5" preserved through migration
  </acceptance_criteria>
  <done>v3.1 backward-compat migration test passes; fixture validates xrom_modules upgrade and clean field defaults</done>
</task>

<task type="auto">
  <name>Task 2: Extend E2E smoke with Time Pac DDAYS workflow</name>
  <files>hp41-gui/e2e/smoke.spec.js</files>
  <read_first>
    - hp41-gui/e2e/smoke.spec.js (existing 4 E2E tests: 2+3=5, SINH, MATRIX DET, NORMD; invokeBackend helper, clickKey helper, beforeEach CLX reset pattern)
    - hp41-gui/src-tauri/src/key_map.rs (xeq_ prefix dispatch pattern for direct XROM invocation)
  </read_first>
  <action>
    Add a 5th E2E test to hp41-gui/e2e/smoke.spec.js for the Time Pac DDAYS workflow per TIME-QUAL-06 and D-42.15 (DDAYS recommended for exercising date parsing + JDN arithmetic with clear numeric assertion).

    The new `it()` block must:
    1. Be placed after the existing NORMD test, inside the existing `describe` block.
    2. Title: `'XEQ "DDAYS" between Jan 1 2000 and Feb 1 2000 displays 31 (Time Pac date arithmetic)'`
    3. Strategy: BROWSER.EXECUTE FALLBACK for XEQ-by-name (same pattern as SINH/DET/NORMD tests), REAL CLICKS for digit entry.
    4. Setup:
       - Ensure MDY mode: dispatch `xeq_MDY` via invokeBackend (clears Flag 31).
       - Enter first date Jan 1 2000 = `1.012000` via real clicks: 1, decimal, 0, 1, 2, 0, 0, 0, then ENTER (lifts to Y).
       - Enter second date Feb 1 2000 = `2.012000` via real clicks: 2, decimal, 0, 1, 2, 0, 0, 0.
       - Stack state: Y = 1.012000, X = 2.012000.
    5. Dispatch: `invokeBackend('dispatch_op', { keyId: 'xeq_DDAYS' })`.
    6. Assert: `view.display_str` equals `'-31.0000'` (DDAYS = Y - X = Jan 1 - Feb 1 = -31 days; FIX 4 format).
       Note: DDAYS returns Y_date - X_date per HP 82182A OM, so Jan 1 minus Feb 1 = -31.
    7. Comment header referencing Plan 42-04 / D-42.15 / TIME-QUAL-06.
    8. Per D-carried.1: this test runs Ubuntu-only via ci-gui.yml::e2e-linux.

    Important: verify the DDAYS sign convention by reading the date_arith.rs op_ddays implementation — it computes jdn_y - jdn_x. With Y=1.012000 and X=2.012000, the result is JDN(Jan 1) - JDN(Feb 1) = negative. Adjust the expected value accordingly.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && grep -c "DDAYS" hp41-gui/e2e/smoke.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/e2e/smoke.spec.js contains a new it() block with "DDAYS" in the title
    - The test dispatches xeq_MDY + enters two dates via real clicks + dispatches xeq_DDAYS
    - Assert on view.display_str for the expected day count in FIX 4 format
    - No changes to existing 4 tests (2+3=5, SINH, MATRIX DET, NORMD)
    - File has valid JavaScript syntax (no parse errors)
  </acceptance_criteria>
  <done>E2E smoke extended with Time Pac DDAYS workflow; 5 E2E tests total</done>
</task>

<task type="auto">
  <name>Task 3: README hard-claim graduation and final quality-gate verification</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 50-60 for the current v3.2 soft-claim line)
    - hp41-core/tests/stat1_backward_compat.rs (Phase 37 README graduation pattern reference)
  </read_first>
  <action>
    Graduate the README v3.2 Time Pac line from soft-claim to hard-claim per TIME-QUAL-09 / D-carried.2, mirroring the D-30.9 -> D-32.5 -> D-37.11 graduation pattern.

    In README.md, change the v3.2 line from the current soft-claim:
      `- v3.2 ships Time Pac behavioral emulation (35 XEQ entry points, real-time clock/stopwatch/alarm backed by the host system clock, ...`
    to the hard-claim format:
      `- v3.2 ships Time Pac behavioral emulation, feature-complete per Owner's Manual 00041-90035 (35 XEQ entry points, real-time clock/stopwatch/alarm backed by the host system clock, ...`

    The key change is inserting "feature-complete per Owner's Manual 00041-90035" after "behavioral emulation," — same placement as the v3.0 Math Pac I and v3.1 Stat 1 Pac lines.

    After the README edit, run the final quality-gate verification:
    1. `cargo test -p hp41-core` — full core test suite including all new Phase 42 tests
    2. `cargo test -p hp41-cli` — CLI tests including function_matrix_parity 4-pool
    3. `bash scripts/check-free42-contamination.sh` — Free42 guard
    4. `cargo clippy -p hp41-core -- -D warnings` — clippy clean
    5. Record all gate results in the summary.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && grep "feature-complete per Owner's Manual 00041-90035" README.md && cargo test -p hp41-core 2>&amp;1 | tail -5 && cargo test -p hp41-cli 2>&amp;1 | tail -5 && bash scripts/check-free42-contamination.sh 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "feature-complete per Owner's Manual 00041-90035" on the v3.2 line
    - The v3.0 and v3.1 hard-claim lines are unchanged
    - cargo test -p hp41-core exits 0 (all Phase 42 tests pass)
    - cargo test -p hp41-cli exits 0 (function_matrix_parity 4-pool passes)
    - bash scripts/check-free42-contamination.sh exits 0
    - cargo clippy -p hp41-core -- -D warnings exits 0
  </acceptance_criteria>
  <done>README hard-claim graduated; all quality gates pass; Phase 42 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infrastructure-only | Phase 42 has zero user-facing changes beyond README text |
| E2E smoke | Runs on Ubuntu-only CI runner; no cross-platform exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Tampering | v31-autosave.json fixture | mitigate | Fixture is a minimal CalcState snapshot; no secrets; verified by 3 specific assertions in time_backward_compat.rs |
| T-42-07 | Repudiation | README hard-claim | mitigate | Hard-claim only added after ALL quality gates pass (coverage, accuracy, E2E, contamination); graduation is conditional |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (D-carried.4) |
</threat_model>

<verification>
cargo test -p hp41-core passes (all new + existing tests)
cargo test -p hp41-cli passes (function_matrix_parity 4-pool)
bash scripts/check-free42-contamination.sh exits 0
README.md contains "feature-complete per Owner's Manual 00041-90035"
</verification>

<success_criteria>
- v3.1 save file migrates cleanly to v3.2 (xrom_modules 3 -> 7, time fields default, rand_seed preserved)
- E2E smoke has 5 tests including DDAYS Time Pac workflow
- README carries v3.2 hard-claim after quality gates pass
- All quality gates green: test suite, coverage, contamination, clippy
</success_criteria>

<output>
Create `.planning/phases/42-test-hardening-quality-gates/42-04-SUMMARY.md` when done
</output>
