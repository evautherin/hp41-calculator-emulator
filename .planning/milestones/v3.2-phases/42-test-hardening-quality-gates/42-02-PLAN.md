---
phase: 42-test-hardening-quality-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - hp41-core/tests/time_coverage_supplement.rs
autonomous: true
requirements:
  - TIME-QUAL-01

must_haves:
  truths:
    - "hp41-core region coverage remains >= 93% after Time Pac additions"
    - "All time/*.rs source files below 90% region coverage have targeted tests closing the gap"
  artifacts:
    - path: "hp41-core/tests/time_coverage_supplement.rs"
      provides: "Targeted coverage-gap tests for time module files below 90%"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/time_coverage_supplement.rs"
      to: "hp41-core/src/ops/time/*.rs"
      via: "targeted tests for uncovered error paths, edge cases, and branch conditions"
      pattern: "use hp41_core"
---

<objective>
Measure per-file region coverage for all 7 time/*.rs source files, identify any below 90%, and write targeted tests to close coverage gaps, per D-carried.8 (Phase 32/37 precedent: measure first, then targeted closure).

Purpose: TIME-QUAL-01 requires hp41-core region coverage >= 93%. The Time Pac added ~4636 LOC / 35 ops, causing denominator dilution. Targeted tests for low-coverage files maintain the gate.

Output: Per-file coverage measurements documented; time_coverage_supplement.rs with targeted tests for any files below 90%.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-test-hardening-quality-gates/42-CONTEXT.md
@.planning/phases/42-test-hardening-quality-gates/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure per-file region coverage for time module</name>
  <files></files>
  <read_first>
    - hp41-core/src/ops/time/mod.rs (module dispatch + SETAF/RCLAF)
    - hp41-core/src/ops/time/alarm.rs (alarm catalog, check_alarms, event_buffer)
    - hp41-core/src/ops/time/clock.rs (TIME/DATE/SETIME/SETDATE and display ops)
    - hp41-core/src/ops/time/stopwatch.rs (RUNSW/STOPSW/RCLSW/SETSW/SW/SWPT/STPW)
    - hp41-core/src/ops/time/modal.rs (TimeStep enum + modal dispatch)
    - hp41-core/src/ops/time/alpha_time.rs (ATIME/ATIME24/ADATE)
    - hp41-core/src/ops/time/date_arith.rs (DATE+/DDAYS/DOW/DMY/MDY + JDN)
  </read_first>
  <action>
    Run cargo-llvm-cov with per-file region coverage reporting to identify time/*.rs files below 90% region coverage. Use the command:
    `cargo llvm-cov --no-report test -p hp41-core` followed by `cargo llvm-cov report --summary-only` for aggregate, plus per-file detail via `cargo llvm-cov report` to extract region coverage per time module file.

    Target files to measure (all 7):
    - hp41-core/src/ops/time/alarm.rs
    - hp41-core/src/ops/time/alpha_time.rs
    - hp41-core/src/ops/time/clock.rs
    - hp41-core/src/ops/time/date_arith.rs
    - hp41-core/src/ops/time/mod.rs
    - hp41-core/src/ops/time/modal.rs
    - hp41-core/src/ops/time/stopwatch.rs

    Record the per-file region coverage numbers. Identify which files fall below 90% region coverage. These are the targets for Task 2.

    Also record the aggregate hp41-core region coverage to confirm the >= 93% gate status (TIME-QUAL-01).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo llvm-cov --no-report test -p hp41-core 2>&amp;1 | tail -5 && cargo llvm-cov report --summary-only 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Per-file region coverage numbers recorded for all 7 time/*.rs files
    - Aggregate hp41-core region coverage number recorded
    - Files below 90% region coverage identified as targets for Task 2
  </acceptance_criteria>
  <done>Coverage measurement complete; gap targets identified; baseline documented</done>
</task>

<task type="auto">
  <name>Task 2: Write targeted coverage-gap tests for time module files below 90%</name>
  <files>hp41-core/tests/time_coverage_supplement.rs</files>
  <read_first>
    - (the specific time/*.rs source files identified as below 90% in Task 1)
    - hp41-core/tests/stat1_coverage_supplement.rs (Phase 37 structural template for coverage-gap tests)
  </read_first>
  <action>
    Create hp41-core/tests/time_coverage_supplement.rs with targeted tests for the time module files identified as below 90% region coverage in Task 1, per D-carried.8 (Phase 32/37 precedent).

    The file must:
    1. Carry `#![allow(clippy::unwrap_used)]` per D-carried.3.
    2. Carry the Free42 disclaim header per the time module convention.
    3. Target UNCOVERED BRANCHES in the below-90% files. Typical coverage gaps include:
       - Error paths (invalid input, overflow, out-of-range values)
       - Edge-case branches in match arms (alarm types, stopwatch modes, modal steps)
       - Boundary conditions (empty alarm catalog, max capacity 253, stopwatch not running, clock display inactive)
       - Rarely-exercised code paths (CORRECT no-op, T+X, ALMNOW, CLALMA/CLALMX with invalid indices)
    4. Use `use hp41_core::{ops::{dispatch, Op}, state::CalcState}` import pattern.
    5. Each test function should target a specific uncovered code path, named descriptively (e.g., `alarm_clear_empty_catalog`, `stopwatch_rclsw_when_idle`).
    6. If coverage is already >= 90% for all files, this task creates a minimal file with a comment explaining all files meet the threshold and a few supplementary edge-case tests for the lowest-coverage file.

    After writing the tests, re-run coverage to verify improvement toward the 93% aggregate gate.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test time_coverage_supplement 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/time_coverage_supplement.rs exists and compiles
    - cargo test -p hp41-core --test time_coverage_supplement exits 0
    - Re-measured aggregate hp41-core region coverage >= 93% (TIME-QUAL-01)
    - Previously below-90% files show measurable improvement
  </acceptance_criteria>
  <done>Coverage-gap tests written; all time module files at or approaching 90%; aggregate hp41-core region coverage >= 93%</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infrastructure-only | Coverage tests exercise existing code paths; no user-facing changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | time_coverage_supplement.rs | mitigate | Tests target specific uncovered branches (not padding with trivial assertions); coverage tool output is the arbiter |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (D-carried.4) |
</threat_model>

<verification>
cargo test -p hp41-core --test time_coverage_supplement passes
Aggregate hp41-core region coverage >= 93%
</verification>

<success_criteria>
- Per-file coverage measured for all 7 time/*.rs source files
- Targeted tests written for files below 90%
- Aggregate hp41-core region coverage >= 93% (TIME-QUAL-01 met)
</success_criteria>

<output>
Create `.planning/phases/42-test-hardening-quality-gates/42-02-SUMMARY.md` when done
</output>
