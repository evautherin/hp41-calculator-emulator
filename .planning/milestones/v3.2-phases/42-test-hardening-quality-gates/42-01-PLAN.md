---
phase: 42-test-hardening-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/xrom_op_test_count.rs
  - hp41-core/tests/lint_xrom_assertions.rs
autonomous: true
requirements:
  - TIME-QUAL-03
  - TIME-QUAL-05
  - TIME-QUAL-08

must_haves:
  truths:
    - "Unified xrom_op_test_count.rs scans all 3 XROM modules (Math 1 + Stat 1 + Time) and asserts >= 5 test mentions per variant"
    - "Unified lint_xrom_assertions.rs scans all 3 module test trees for Pitfall 14/17 assertion discipline"
    - "Old per-module meta-gate files (math1_op_test_count.rs, stat1_op_test_count.rs, lint_math1_assertions.rs, lint_stat1_assertions.rs) are deleted"
    - "Free42 contamination guard still passes (re-verify TIME-QUAL-05)"
    - "XROM shadowing test still passes across all 3 modules (re-verify TIME-QUAL-08)"
  artifacts:
    - path: "hp41-core/tests/xrom_op_test_count.rs"
      provides: "Unified per-Op test-count meta-gate for all 3 XROM modules"
      contains: "each_xrom_op_has_at_least_5_tests"
    - path: "hp41-core/tests/lint_xrom_assertions.rs"
      provides: "Unified assertion-discipline lint for all 3 XROM module test surfaces"
      contains: "no_decimal_assert_eq_in_xrom_tests"
  key_links:
    - from: "hp41-core/tests/xrom_op_test_count.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "include_str! variant extraction from math1_resolve + stat1_resolve + time_resolve"
      pattern: "collect_.*_variant_names"
    - from: "hp41-core/tests/lint_xrom_assertions.rs"
      to: "hp41-core/src/ops/{math1,stat1,time}/*.rs"
      via: "three-pass file collection for inline test scanning"
      pattern: "collect_xrom_test_content"
---

<objective>
Unify the per-module meta-gate infrastructure into two files that scan all 3 XROM modules (Math 1 + Stat 1 + Time), per D-42.1. Delete the 4 old per-module files. Re-verify that XROM shadowing (D-42.2 / TIME-QUAL-08) and Free42 contamination (D-carried.6 / TIME-QUAL-05) gates still pass.

Purpose: Meta-gates must land BEFORE coverage-gap tests (Wave 2+) so that new test files are immediately counted and linted. The unification also fulfills the Phase 37 deferred commitment ("if a third XROM module lands, consider unifying") and makes future Advantage Pac (v3.3) integration free.

Output: Two unified test files replace four old ones; CI passes with all meta-gates green.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-test-hardening-quality-gates/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified xrom_op_test_count.rs and delete old per-module files</name>
  <files>hp41-core/tests/xrom_op_test_count.rs, hp41-core/tests/math1_op_test_count.rs, hp41-core/tests/stat1_op_test_count.rs</files>
  <read_first>
    - hp41-core/tests/math1_op_test_count.rs (structural template for math1 variant detection and test counting)
    - hp41-core/tests/stat1_op_test_count.rs (structural template for stat1 — TWO-PASS scan + dual-token matching + variant_to_fn_name converter)
    - hp41-core/src/ops/math1/xrom.rs (lines 420-476 for time_resolve function with 35 Time Op variant names)
  </read_first>
  <action>
    Create hp41-core/tests/xrom_op_test_count.rs that unifies math1_op_test_count.rs and stat1_op_test_count.rs into a single file scanning all 3 XROM modules, per D-42.1 and D-42.10 (dual-scan inline + external).

    The file must:
    1. Carry the `#![allow(clippy::unwrap_used)]` crate-level attribute per D-carried.3.
    2. Implement three collector functions — `collect_math1_variant_names()`, `collect_stat1_variant_names()`, `collect_time_variant_names()` — each scoped to its respective `fn math1_resolve` / `fn stat1_resolve` / `fn time_resolve` body in xrom.rs via brace-depth tracking (exactly as the existing files do; prevents cross-contamination between resolver functions).
    3. Retain the `variant_to_fn_name()` PascalCase-to-snake_case converter from stat1_op_test_count.rs (needed for inline test matching).
    4. Retain `line_mentions_variant` (word-boundary WR-03 matching) and `line_mentions_variant_or_fn` (dual-token).
    5. Implement a unified `count_xrom_test_mentions()` that does a THREE-PASS scan:
       - Pass 1: external test files matching the module prefix (math1_*.rs / stat1_*.rs for the respective module; plus numerical_accuracy.rs for stat1). For Time module: there are currently 0 external time_*.rs files, so this pass produces 0 for Time variants initially (the coverage-gap tests added in Wave 2+ will be counted once they exist).
       - Pass 2: inline `#[cfg(test)]` blocks from src/ops/{math1,stat1,time}/*.rs. This is critical for the Time module which has 209 inline tests and 0 external test files.
       - Pass 3: (Pass 1 and 2 are combined per-module, not separate physical passes — the function takes tests_dir + src_module_dir arguments.)
    6. Implement one `#[test] fn each_xrom_op_has_at_least_5_tests()` that iterates all 3 modules, collects variants, counts test mentions (dual-scan per D-42.10), and asserts >= 5 per variant. Report per-module failures with the module name prefix for diagnostic clarity.
    7. Expected variant counts: Math 1 = 45, Stat 1 = 26, Time = 35 (total 106). Include a sanity assertion on each module's variant count to catch scope leakage between resolver functions.
    8. Include the stat1 unit_tests module for variant_to_fn_name coverage (the existing unit tests from stat1_op_test_count.rs).

    After creating the unified file, DELETE hp41-core/tests/math1_op_test_count.rs and hp41-core/tests/stat1_op_test_count.rs (per D-42.14 — atomic replacement).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test xrom_op_test_count -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/xrom_op_test_count.rs exists and contains fn each_xrom_op_has_at_least_5_tests
    - hp41-core/tests/math1_op_test_count.rs does NOT exist (deleted)
    - hp41-core/tests/stat1_op_test_count.rs does NOT exist (deleted)
    - cargo test -p hp41-core --test xrom_op_test_count exits 0
    - The test scans 45 + 26 + 35 = 106 variants and all pass >= 5 mentions
    - Sanity assertions verify exactly 45 Math 1, 26 Stat 1, and 35 Time variants detected
  </acceptance_criteria>
  <done>Unified xrom_op_test_count.rs passes with all 106 variants at >= 5 test mentions; old files deleted; cargo test -p hp41-core passes</done>
</task>

<task type="auto">
  <name>Task 2: Create unified lint_xrom_assertions.rs and delete old per-module files</name>
  <files>hp41-core/tests/lint_xrom_assertions.rs, hp41-core/tests/lint_math1_assertions.rs, hp41-core/tests/lint_stat1_assertions.rs</files>
  <read_first>
    - hp41-core/tests/lint_math1_assertions.rs (structural template for Pitfall 14/17 lint heuristics, LINT-EXEMPT token, multi-line assert_eq detection WR-02, preceding_block_has_lint_exempt lookback)
    - hp41-core/tests/lint_stat1_assertions.rs (TWO-PASS file collection spanning external tests + inline #[cfg(test)] blocks)
  </read_first>
  <action>
    Create hp41-core/tests/lint_xrom_assertions.rs that unifies lint_math1_assertions.rs and lint_stat1_assertions.rs into a single file scanning all 3 XROM module test surfaces, per D-42.1.

    The file must:
    1. Carry `#![allow(clippy::unwrap_used)]` per D-carried.3.
    2. Implement a unified `collect_xrom_test_content(tests_dir, manifest_dir)` that does a THREE-MODULE collection:
       - Module 1 (math1): external tests/math1_*.rs files (full content; the whole file is test code)
       - Module 2 (stat1): external tests/stat1_*.rs files (full content) + inline #[cfg(test)] blocks from src/ops/stat1/*.rs
       - Module 3 (time): external tests/time_*.rs files (if any exist) + inline #[cfg(test)] blocks from src/ops/time/*.rs
       Returns Vec<(PathBuf, String)> of all collected test content.
    3. Retain all helper functions VERBATIM from lint_math1_assertions.rs: LINT_EXEMPT_TOKEN const, format_offender, preceding_block_has_lint_exempt, line_is_forbidden_assert_eq (with WR-02 multi-line lookahead), line_is_forbidden_manual_tolerance.
    4. Implement two #[test] functions:
       - `no_decimal_assert_eq_in_xrom_tests` (Pitfall 17)
       - `no_manual_tolerance_pattern_in_xrom_tests` (Pitfall 14)
       Each scans ALL collected test content from all 3 modules.
    5. Comment header must reference Phase 42 D-42.1 unification and preserve the Pitfall 14/17 T-32-04 rationale.

    After creating the unified file, DELETE hp41-core/tests/lint_math1_assertions.rs and hp41-core/tests/lint_stat1_assertions.rs (per D-42.14).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test lint_xrom_assertions -- --nocapture 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/lint_xrom_assertions.rs exists and contains fn no_decimal_assert_eq_in_xrom_tests and fn no_manual_tolerance_pattern_in_xrom_tests
    - hp41-core/tests/lint_math1_assertions.rs does NOT exist (deleted)
    - hp41-core/tests/lint_stat1_assertions.rs does NOT exist (deleted)
    - cargo test -p hp41-core --test lint_xrom_assertions exits 0
    - The lint scans math1, stat1, AND time test surfaces (all three module trees)
  </acceptance_criteria>
  <done>Unified lint_xrom_assertions.rs passes both Pitfall 14 and Pitfall 17 lint gates across all 3 XROM modules; old files deleted; cargo test -p hp41-core passes</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify XROM shadowing and Free42 contamination gates</name>
  <files></files>
  <read_first>
    - scripts/check-free42-contamination.sh (18-token grep covering math1/, stat1/, time/ trees)
    - hp41-core/tests/xrom_shadowing.rs (3-module cross-check per D-39.14)
  </read_first>
  <action>
    Re-verify that existing quality gates still pass after the meta-gate unification, per D-42.2, D-42.3, D-carried.6, and D-carried.7.

    Run in sequence:
    1. `cargo test -p hp41-core --test xrom_shadowing` — verifies all 3 XROM modules have disjoint mnemonics (TIME-QUAL-08).
    2. `cargo test -p hp41-cli --test function_matrix_parity` — verifies the 4-pool partition test (cv + math1 + stat1 + time) passes (TIME-QUAL-07).
    3. `bash scripts/check-free42-contamination.sh` — verifies the 18-token Free42 guard exits 0 covering math1/, stat1/, and time/ trees (TIME-QUAL-05).
    4. Full `cargo test -p hp41-core` — verifies no regressions from deleting old meta-gate files.

    No code changes needed — this is a re-verification task. If any gate fails, diagnose and report.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test xrom_shadowing 2>&amp;1 | tail -3 && cargo test -p hp41-cli --test function_matrix_parity 2>&amp;1 | tail -3 && bash scripts/check-free42-contamination.sh 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p hp41-core --test xrom_shadowing exits 0
    - cargo test -p hp41-cli --test function_matrix_parity exits 0
    - bash scripts/check-free42-contamination.sh exits 0
    - cargo test -p hp41-core exits 0 (full suite, no regressions)
  </acceptance_criteria>
  <done>All 3 re-verification gates pass; full hp41-core test suite passes without regressions from meta-gate unification</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infrastructure-only | Phase 42 has zero user-facing changes; all deliverables are tests, CI gates, and documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | xrom_op_test_count.rs | mitigate | Word-boundary matching (WR-03) prevents substring inflation; sanity assertions on per-module variant counts (45/26/35) catch scope leakage |
| T-42-02 | Tampering | lint_xrom_assertions.rs | mitigate | LINT-EXEMPT requires inline rationale per T-32-04; preceding_block_has_lint_exempt enforces adjacency |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this phase (D-carried.4); zero package installs |
</threat_model>

<verification>
cargo test -p hp41-core passes (all unified meta-gates + existing tests)
cargo test -p hp41-cli --test function_matrix_parity passes
bash scripts/check-free42-contamination.sh exits 0
</verification>

<success_criteria>
- Unified xrom_op_test_count.rs scans 106 variants across 3 modules, all >= 5 test mentions
- Unified lint_xrom_assertions.rs scans all 3 module test trees, zero offenders
- 4 old per-module meta-gate files deleted
- XROM shadowing, function_matrix_parity, Free42 contamination gates pass
- Full cargo test -p hp41-core passes without regressions
</success_criteria>

<output>
Create `.planning/phases/42-test-hardening-quality-gates/42-01-SUMMARY.md` when done
</output>
