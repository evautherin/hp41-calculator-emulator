---
phase: 39-hp41-cli-cli-integration-live-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41-time-functions.json
  - hp41-cli/src/help_data.rs
  - hp41-cli/tests/function_matrix_parity.rs
  - hp41-core/tests/xrom_shadowing.rs
autonomous: true
requirements:
  - TIME-CLI-01
  - TIME-CLI-02
  - TIME-CLI-04
  - TIME-CLI-08

must_haves:
  truths:
    - "User can press ? and see a 'Time Pac (XROM 26)' section listing all 35 Time functions"
    - "Incremental substring search in ? overlay spans all four JSON pools (cv + math1 + stat1 + time)"
    - "All Time Pac mnemonics are disjoint from Math 1, Stat 1, and built-in card op names"
  artifacts:
    - path: "docs/hp41-time-functions.json"
      provides: "35-entry canonical JSON for Time Pac"
      contains: "module_id"
    - path: "hp41-cli/src/help_data.rs"
      provides: "TIME_HELP_ENTRIES OnceLock + help_entries_time() + 4-pool help_entries_all()"
      exports: ["help_entries_time", "help_entries_all"]
    - path: "hp41-core/tests/xrom_shadowing.rs"
      provides: "TIME_MODULE shadowing tests"
      contains: "TIME_MODULE"
  key_links:
    - from: "hp41-cli/src/help_data.rs"
      to: "docs/hp41-time-functions.json"
      via: "include_str! compile-time embed"
      pattern: "include_str!.*hp41-time-functions"
    - from: "hp41-cli/src/help_data.rs"
      to: "help_overlay_rows()"
      via: "help_entries_all() 4-pool chain"
      pattern: "help_entries_time"
---

<objective>
Create the fourth JSON canonical data source for Time Pac functions and wire it into the CLI help system, extending the OnceLock pattern from 3 pools to 4. Update all parity/shadowing tests to recognize the 35 new Time Op variants.

Purpose: Establishes the data foundation for Time Pac CLI discoverability -- the ? help overlay, function matrix parity tests, and XROM shadowing guards all depend on this JSON file existing and being properly loaded.

Output: `docs/hp41-time-functions.json` (35 entries), extended `help_data.rs` (4th OnceLock), updated `function_matrix_parity.rs` (165-count + partition test + ALL_OP_VARIANT_NAMES), extended `xrom_shadowing.rs` (TIME_MODULE tests).
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hp41-cli/src/help_data.rs:
- `pub struct HelpEntry` with fields: op_variant, display_name, category, status, phase, key_path, description, divergences, xrom
- `pub struct XromEntry` with fields: module (String), module_id (u8), function_id (u16)
- `pub fn help_entries() -> &'static [HelpEntry]` (v2.2 pool, 130 entries)
- `pub fn help_entries_math1() -> &'static [HelpEntry]` (Math 1 pool, 45 entries)
- `pub fn help_entries_stat1() -> &'static [HelpEntry]` (Stat 1 pool, 26 entries)
- `pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry>` (currently 3-pool chain)
- `pub fn help_overlay_rows() -> Vec<HelpRow>` (calls help_entries_all internally)

From hp41-core/src/ops/math1/xrom.rs:
- `pub const TIME_MODULE: XromModule` with id=26, name="TIME 2C", 35 ops entries
- `pub const MATH_1: XromModule` with id=7
- `pub const STAT_1: XromModule` with id=2
- `pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>`

From hp41-cli/tests/function_matrix_parity.rs:
- `const ALL_OP_VARIANT_NAMES: &[&str]` -- currently 130 entries, needs 35 Time variants appended (total 165)
- `test_pool_partition_is_exhaustive()` -- currently routes None/Some(7)/Some(2); needs Some(26) arm
- `test_op_inventory_count_matches_enum()` -- asserts len == 130; needs update to 165

From hp41-core/tests/xrom_shadowing.rs:
- Import line: `use hp41_core::ops::math1::xrom::{xrom_resolve, MATH_1, STAT_1};` -- needs TIME_MODULE added
- `const BUILTIN_CARD_OP_NAMES: &[&str]` -- 18-entry allowlist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author hp41-time-functions.json and wire fourth OnceLock in help_data.rs</name>
  <files>docs/hp41-time-functions.json, hp41-cli/src/help_data.rs</files>
  <read_first>
    - docs/hp41-stat1-functions.json (JSON schema template -- copy exact field structure)
    - hp41-cli/src/help_data.rs (current 3-pool OnceLock pattern + help_entries_all chain)
    - hp41-core/src/ops/math1/xrom.rs lines 199-244 (TIME_MODULE.ops -- canonical 35 entries with exact Op variant names and mnemonic strings)
  </read_first>
  <action>
    Create docs/hp41-time-functions.json with 35 entries following the exact schema from hp41-stat1-functions.json. Each entry has fields: op_variant, display_name, category, status, phase, key_path, description, xrom, and optionally divergences.

    Field values per D-39.9/D-39.10/D-39.11:
    - op_variant: PascalCase Op enum variant name (e.g. "TimeTime", "TimeDate", "TimeDatePlus")
    - display_name: mnemonic string from TIME_MODULE.ops (e.g. "TIME", "DATE", "DATE+")
    - category: one of 7 values per D-39.9: "Time Clock", "Time Date Arithmetic", "Time Display", "Time Format", "Time Alpha", "Time Stopwatch", "Time Alarm"
    - status: "implemented" for all 35
    - phase: "38" for all (first implemented in Phase 38 core)
    - key_path: "XEQ \"MNEMONIC\"" for all 35 (XEQ-by-name only, no physical key assignments)
    - xrom: { "module": "Time", "module_id": 26, "function_id": N } where N is 1-indexed sequential within the TIME_MODULE.ops slice
    - divergences: inline array ONLY for SETAF ("Accuracy factor stored but has no effect in emulation"), RCLAF ("Recalls stored accuracy factor; no crystal to adjust"), CORRECT ("No-op in emulation; system clock is authoritative"), SW ("Extended interactive stopwatch keyboard mode -- emulator extension"). All other entries omit the divergences field entirely.

    Category assignments (per D-39.9, using PATTERNS.md function_id mapping):
    - "Time Clock": TIME (1), DATE (2), T+X (11), CLOCK (9)
    - "Time Date Arithmetic": DATE+ (12), DDAYS (13), DOW (14), DMY (15), MDY (16)
    - "Time Display": CLKT (7), CLKTD (8), SETIME (3), SETDATE (4)
    - "Time Format": CLK12 (5), CLK24 (6), CORRECT (10), SETAF (32), RCLAF (31)
    - "Time Alpha": ATIME (17), ATIME24 (18), ADATE (19)
    - "Time Stopwatch": RUNSW (20), STOPSW (21), SETSW (23), RCLSW (22), SWPT (25), STPW (26), SW (24)
    - "Time Alarm": XYZALM (28), RCLALM (30), ALMCAT (29), CLALMA (33), CLALMX (34), CLRALMS (35), ALMNOW (27)

    function_id values must match the 1-indexed position within TIME_MODULE.ops slice (xrom.rs lines 202-243): TIME=1, DATE=2, SETIME=3, SETDATE=4, CLK12=5, CLK24=6, CLKT=7, CLKTD=8, CLOCK=9, CORRECT=10, T+X=11, DATE+=12, DDAYS=13, DOW=14, DMY=15, MDY=16, ATIME=17, ATIME24=18, ADATE=19, RUNSW=20, STOPSW=21, RCLSW=22, SETSW=23, SW=24, SWPT=25, STPW=26, XYZALM=27 (note: the 27th op is XYZALM, then ALMCAT=28, ALMNOW=29, RCLALM=30, RCLAF=31, SETAF=32, CLALMA=33, CLALMX=34, CLRALMS=35). Cross-reference the exact ordering from TIME_MODULE.ops slice to get correct function_id values.

    In help_data.rs, add the 4th OnceLock after the STAT1 block (after line 147):
    - const TIME_FUNCTIONS_JSON: &str = include_str!("../../docs/hp41-time-functions.json");
    - static TIME_HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();
    - pub fn help_entries_time() -> &'static [HelpEntry] with .expect("hp41-time-functions.json is malformed -- fix the JSON")
    - Update help_entries_all() to chain 4 pools: .chain(help_entries_time().iter()) as 4th arm per D-39.12
    - Update the doc-comment on help_entries_all() from "three" to "four" pools and add help_entries_time to the narrow-accessor retention note
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo build -p hp41-cli 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/hp41-time-functions.json exists with exactly 35 JSON entries (verify: python3 -c "import json; d=json.load(open('docs/hp41-time-functions.json')); assert len(d)==35, f'got {len(d)}'")
    - Every entry has xrom.module_id == 26
    - Every entry has key_path starting with "XEQ \""
    - Exactly 4 entries have non-empty divergences arrays (SETAF, RCLAF, CORRECT, SW)
    - help_data.rs contains `pub fn help_entries_time()` and `help_entries_time().iter()` in help_entries_all()
    - cargo build -p hp41-cli compiles without errors (include_str! resolves the JSON path)
  </acceptance_criteria>
  <done>35-entry hp41-time-functions.json authored with correct schema; 4th OnceLock wired in help_data.rs; help_entries_all() returns 4-pool chain; CLI compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Update function_matrix_parity.rs and extend xrom_shadowing.rs</name>
  <files>hp41-cli/tests/function_matrix_parity.rs, hp41-core/tests/xrom_shadowing.rs</files>
  <read_first>
    - hp41-cli/tests/function_matrix_parity.rs (ALL_OP_VARIANT_NAMES array, test_op_inventory_count_matches_enum assertion at line 202, test_pool_partition_is_exhaustive at line 511)
    - hp41-core/tests/xrom_shadowing.rs (STAT_1 section at lines 93-160 as template; import line 26)
    - hp41-cli/src/prgm_display.rs lines 328-365 (canonical list of 35 Time Op variant name strings)
  </read_first>
  <action>
    In function_matrix_parity.rs:
    1. Append all 35 Time* variant name strings to ALL_OP_VARIANT_NAMES const array. The exact names from prgm_display.rs: TimeAdate, TimeAlmcat, TimeAlmnow, TimeAtime, TimeAtime24, TimeClalma, TimeClalmx, TimeClk12, TimeClk24, TimeClkt, TimeClktd, TimeClock, TimeClralms, TimeCorrect, TimeDate, TimeDatePlus, TimeDdays, TimeDmy, TimeDow, TimeMdy, TimeRclaf, TimeRclalm, TimeRclsw, TimeRunsw, TimeSetaf, TimeSetdate, TimeSetime, TimeSetsw, TimeStopsw, TimeStpw, TimeSw, TimeSwpt, TimeTime, TimeTplusx, TimeXyzalm. Add a comment "// Phase 38 Time Pac" above the block.
    2. Update test_op_inventory_count_matches_enum assertion from 130 to 165.
    3. In test_pool_partition_is_exhaustive: add `let mut time_count = 0usize;` alongside existing counters. Add `Some(26) => time_count += 1,` match arm before the `Some(other) => rogue` catch-all. Add assertion `assert_eq!(time_count, 35, "Time pool count drift: {time_count}");` after the rogue assertion. Update the error message string to mention "module_id in {None, 7, 2, 26}".

    In xrom_shadowing.rs:
    1. Update import line 26 from `use hp41_core::ops::math1::xrom::{xrom_resolve, MATH_1, STAT_1};` to `use hp41_core::ops::math1::xrom::{xrom_resolve, MATH_1, STAT_1, TIME_MODULE};`
    2. Add 4 new tests mirroring the STAT_1 section pattern (lines 93-160):
       - time_names_do_not_shadow_builtins: iterate TIME_MODULE.ops, assert each name not in BUILTIN_CARD_OP_NAMES
       - time_ops_disjoint_from_math1_and_stat1: collect MATH_1.ops and STAT_1.ops name HashSets, assert TIME_MODULE.ops names not in either
       - time_ops_resolve_via_xrom_resolve: for each (name, expected_op) in TIME_MODULE.ops, assert xrom_resolve(name, 0b0000_0111) == Some(expected_op). Bit mask 0b0000_0111 = all three modules loaded.
       - time_const_fields: assert TIME_MODULE.id == 26, TIME_MODULE.name == "TIME 2C"
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test xrom_shadowing 2>&1 | tail -10 && cargo test -p hp41-cli --test function_matrix_parity 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ALL_OP_VARIANT_NAMES contains exactly 165 entries (130 existing + 35 Time)
    - test_op_inventory_count_matches_enum asserts == 165 and passes
    - test_pool_partition_is_exhaustive routes module_id 26 to time_count and asserts == 35
    - xrom_shadowing.rs imports TIME_MODULE and has 4 new test functions prefixed with time_
    - All 4 xrom_shadowing time tests pass (no shadowing collisions, correct xrom_resolve)
    - cargo test -p hp41-core --test xrom_shadowing exits 0
    - cargo test -p hp41-cli --test function_matrix_parity exits 0
  </acceptance_criteria>
  <done>function_matrix_parity.rs recognizes 165 Op variants including 35 Time entries; xrom_shadowing.rs verifies TIME_MODULE mnemonics are disjoint from Math 1, Stat 1, and built-ins; all parity and shadowing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON compile-time embed | hp41-time-functions.json is embedded at compile time via include_str! -- no runtime file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | hp41-time-functions.json | accept | Compile-time embed; malformed JSON panics at first access (D-25.17 hard-build-blocker); no runtime modification possible |
| T-39-02 | Tampering | XROM mnemonic collisions | mitigate | xrom_shadowing.rs tests assert TIME_MODULE mnemonics disjoint from MATH_1 + STAT_1 + BUILTIN_CARD_OP_NAMES; CI-gated |
</threat_model>

<verification>
cargo build -p hp41-cli completes without errors
cargo test -p hp41-cli --test function_matrix_parity passes (165-count + partition)
cargo test -p hp41-core --test xrom_shadowing passes (TIME_MODULE tests)
python3 -c "import json; d=json.load(open('docs/hp41-time-functions.json')); assert len(d)==35"
</verification>

<success_criteria>
- hp41-time-functions.json has exactly 35 entries with correct schema
- help_entries_time() returns 35 entries; help_entries_all() returns 201 (130+45+26) + 35 = 236 entries total (if all pools parse)
- ? help overlay auto-generates "Time Pac (XROM 26)" section from JSON categories
- All Time mnemonics confirmed disjoint from Math 1 + Stat 1 + built-in card ops
- ALL_OP_VARIANT_NAMES = 165; partition test recognizes module_id 26
</success_criteria>

<output>
Create `.planning/phases/39-hp41-cli-cli-integration-live-display/39-01-SUMMARY.md` when done
</output>
