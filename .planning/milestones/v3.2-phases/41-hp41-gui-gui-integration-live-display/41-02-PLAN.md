---
phase: 41-hp41-gui-gui-integration-live-display
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/HelpOverlay.tsx
  - hp41-gui/src/HelpOverlay.test.tsx
autonomous: true
requirements:
  - TIME-GUI-03

must_haves:
  truths:
    - "help_data.ts exports helpEntriesTime() returning 35 entries from docs/hp41-time-functions.json"
    - "helpEntriesAll() returns 4-pool concatenation (cv + math1 + stat1 + time)"
    - "HelpOverlay renders 4 collapsible sections including Time Pac (XROM 26)"
    - "Vitest suite validates Time Pac data layer integrity and section rendering"
  artifacts:
    - path: "hp41-gui/src/help_data.ts"
      provides: "helpEntriesTime() accessor + 4-pool helpEntriesAll()"
      exports: ["helpEntriesTime", "helpEntriesAll"]
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "4-section SECTIONS array with Time Pac (XROM 26)"
      contains: "Time Pac (XROM 26)"
    - path: "hp41-gui/src/HelpOverlay.test.tsx"
      provides: "Vitest assertions for Time Pac data and section rendering"
      contains: "helpEntriesTime"
  key_links:
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/hp41-time-functions.json"
      via: "Vite static JSON import"
      pattern: "import timeFunctions from"
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "helpEntriesAll() call"
      pattern: "helpEntriesAll"
---

<objective>
Wire the fourth help data pool (Time Pac) into the GUI's help overlay system: add the Vite static import for docs/hp41-time-functions.json, extend helpEntriesAll() to 4-pool concatenation, add the "Time Pac (XROM 26)" section to HelpOverlay.tsx, and extend the vitest suite with drift-catch and section rendering assertions.

Purpose: Enables GUI users to discover all 35 Time Pac functions via the `?` help overlay, completing TIME-GUI-03.
Output: HelpOverlay with 4 sections, help_data.ts with 4-pool chain, vitest suite updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-hp41-gui-gui-integration-live-display/41-CONTEXT.md
@.planning/phases/41-hp41-gui-gui-integration-live-display/41-PATTERNS.md

<interfaces>
<!-- Existing patterns to mirror exactly. Extracted from codebase. -->

From hp41-gui/src/help_data.ts (3-pool pattern, lines 17-19, 144-168):
  import functions from '../../docs/hp41cv-functions.json';
  import math1Functions from '../../docs/hp41-math1-functions.json';
  import stat1Functions from '../../docs/hp41-stat1-functions.json';
  export function helpEntriesStat1(): readonly HelpEntry[] { return stat1Functions as readonly HelpEntry[]; }
  export function helpEntriesAll(): readonly HelpEntry[] { return [...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1()]; }

From hp41-gui/src/HelpOverlay.tsx (3-section pattern):
  interface SectionDef { id: 'hp41cv' | 'math1' | 'stat1'; heading: string; predicate: (e: HelpEntry) => boolean; }
  const SECTIONS: SectionDef[] = [3 entries]
  expanded state: { hp41cv: boolean; math1: boolean; stat1: boolean }
  toggleSection(id: 'hp41cv' | 'math1' | 'stat1')

From docs/hp41-time-functions.json (Phase 39 D-39.9):
  35 entries, each with xrom: { module: "Time", module_id: 26, function_id: N }
  CRITICAL: xrom.module value is "Time" — NOT "TIME", "Time Pac", or "TIME 2C"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: help_data.ts 4th Vite import + helpEntriesTime() + 4-pool helpEntriesAll()</name>
  <files>hp41-gui/src/help_data.ts</files>
  <read_first>
    - hp41-gui/src/help_data.ts (full file — understand import section at lines 17-19, stat1 accessor at lines 148-157, helpEntriesAll at lines 159-169)
    - docs/hp41-time-functions.json (first 3 entries — confirm xrom.module is "Time" and structure matches HelpEntry interface)
  </read_first>
  <action>
    Add the fourth Vite static JSON import at line 20: `import timeFunctions from '../../docs/hp41-time-functions.json';` per D-carried.8.

    Add `helpEntriesTime()` accessor after `helpEntriesStat1()` (after line 157). Mirror the stat1 pattern exactly: export function returning `timeFunctions as readonly HelpEntry[]`. Add a doc comment referencing Phase 41, D-carried.8, the 35-entry count per D-39.9, and the Vite hard-build-blocker semantics per D-25.17.

    Update `helpEntriesAll()` from 3-pool to 4-pool: return `[...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1(), ...helpEntriesTime()]`. Update the existing doc comment from "3-pool" to "4-pool" and reference Phase 41 / Phase 39 D-39.12 fourth OnceLock parallel. Per Pitfall 5: update in-place — do NOT create a parallel `helpEntriesAll4()`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - help_data.ts line 20 contains `import timeFunctions from '../../docs/hp41-time-functions.json'`
    - help_data.ts exports `helpEntriesTime()` returning `timeFunctions as readonly HelpEntry[]`
    - help_data.ts `helpEntriesAll()` returns 4-pool spread: `[...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1(), ...helpEntriesTime()]`
    - `npx tsc --noEmit` exits 0 (TypeScript compiles clean)
  </acceptance_criteria>
  <done>help_data.ts has 4th Vite import, helpEntriesTime() accessor, and 4-pool helpEntriesAll(). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: HelpOverlay 4th section + vitest extensions</name>
  <files>hp41-gui/src/HelpOverlay.tsx, hp41-gui/src/HelpOverlay.test.tsx</files>
  <read_first>
    - hp41-gui/src/HelpOverlay.tsx (full file — understand SectionDef interface at line 35, SECTIONS array at line 43, expanded state at line 66, toggleSection at line 141, useEffect reset near line 76)
    - hp41-gui/src/HelpOverlay.test.tsx (full file — understand describe structure, stat1 drift-catch pattern at lines 74-88, sectionButtons.length assertion, search test, import structure)
    - docs/hp41-time-functions.json (first 2 entries to confirm xrom.module === "Time" and xrom.module_id === 26)
  </read_first>
  <action>
    A) HelpOverlay.tsx:
    Widen `SectionDef.id` union from `'hp41cv' | 'math1' | 'stat1'` to `'hp41cv' | 'math1' | 'stat1' | 'time'`.

    Add 4th entry to SECTIONS array after the stat1 entry: `{ id: 'time', heading: 'Time Pac (XROM 26)', predicate: (e: HelpEntry) => e.xrom?.module === 'Time' }`. The predicate value MUST be the string `'Time'` — not `'TIME'` or `'Time Pac'` or `'TIME 2C'` (Pitfall 6 from RESEARCH.md; confirmed from docs/hp41-time-functions.json xrom.module field).

    Widen `expanded` state type to `{ hp41cv: boolean; math1: boolean; stat1: boolean; time: boolean }`. Update the initial state to include `time: true`. Update the `useEffect` reset to include `time: true`. Widen `toggleSection` parameter type to `'hp41cv' | 'math1' | 'stat1' | 'time'`.

    No JSX changes needed — the existing `sectionGroups.map()` render loop handles the 4th section automatically.

    B) HelpOverlay.test.tsx:
    Add import for `helpEntriesTime` from `./help_data` alongside existing imports.
    Add import for `import timeJson from '../../docs/hp41-time-functions.json'`.

    Add data-layer tests mirroring the stat1 drift-catch pattern:
    - `helpEntriesTime returns all entries from docs/hp41-time-functions.json (drift-catch)` — assert `.length === timeJson.length` and `.length >= 35`
    - `helpEntriesTime entries all have xrom field with module "Time"` — loop asserting `entry.xrom!.module === 'Time'` and `entry.xrom!.module_id === 26`

    Update `helpEntriesAll` test to 4-pool:
    - Rename from "built-in + Math 1 + Stat 1" to "built-in + Math 1 + Stat 1 + Time"
    - Update length assertion to include `helpEntriesTime().length`
    - Extend the partition loop to include a 4th segment asserting `xrom.module === 'Time'`

    Update the existing `sectionButtons.length` assertion from `.toBe(3)` to `.toBe(4)` (Pitfall 2 from Phase 36 Plan 36-03 — this is a critical existing test break if not updated).

    Add section tests mirroring stat1 section tests:
    - `renders four top-level sections including Time Pac (XROM 26)` — assert all 4 headings present
    - `Time Pac section contains a Time category heading` — check for a Time-specific category like "Time Clock" or "Time Stopwatch"
    - `clicking Time Pac section heading toggles aria-expanded` — true to false to true cycle
    - `search for "SETIME" returns Time entries` — fireEvent.change + row text assertion
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && node_modules/.bin/vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - HelpOverlay.tsx SectionDef.id union includes `'time'`
    - HelpOverlay.tsx SECTIONS array has 4 entries, 4th is `{ id: 'time', heading: 'Time Pac (XROM 26)', predicate uses 'Time' }`
    - HelpOverlay.tsx expanded state type includes `time: boolean`, initial value `time: true`, reset includes `time: true`
    - HelpOverlay.tsx toggleSection parameter type includes `'time'`
    - HelpOverlay.test.tsx imports `helpEntriesTime` and `timeJson`
    - HelpOverlay.test.tsx has drift-catch test asserting `helpEntriesTime().length === timeJson.length`
    - HelpOverlay.test.tsx has xrom field test asserting `module === 'Time'` and `module_id === 26`
    - HelpOverlay.test.tsx helpEntriesAll test uses 4-pool sum
    - HelpOverlay.test.tsx sectionButtons.length assertion is `.toBe(4)`
    - HelpOverlay.test.tsx has "Time Pac (XROM 26)" heading presence test
    - HelpOverlay.test.tsx has toggle and search tests for Time section
    - `vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>HelpOverlay renders 4 sections including "Time Pac (XROM 26)" per TIME-GUI-03. Vitest suite extended with drift-catch + section + toggle + search tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite build -> docs/hp41-time-functions.json | JSON consumed at build time via static import; malformed JSON breaks the build (hard-build-blocker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Tampering | hp41-time-functions.json content | accept | File is a checked-in source-of-truth authored by Phase 39; Vite build-time type checking catches malformed data |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 41 (D-carried.1) |
</threat_model>

<verification>
cd hp41-gui && npx tsc --noEmit exits 0 (TypeScript clean)
cd hp41-gui && node_modules/.bin/vitest run exits 0 (all tests pass)
grep -c "helpEntriesTime" hp41-gui/src/help_data.ts returns >= 2 (definition + usage in helpEntriesAll)
grep "Time Pac (XROM 26)" hp41-gui/src/HelpOverlay.tsx returns match
grep -c "sectionButtons.*toBe(4)" hp41-gui/src/HelpOverlay.test.tsx returns >= 1
</verification>

<success_criteria>
1. TIME-GUI-03: Help overlay has "Time Pac (XROM 26)" 4th section with 35 Time entries
2. help_data.ts exports helpEntriesTime() and 4-pool helpEntriesAll()
3. Vitest suite passes with data-layer drift-catch + section rendering tests
4. TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/41-hp41-gui-gui-integration-live-display/41-02-SUMMARY.md` when done
</output>
