---
phase: 38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/state.rs
  - hp41-core/src/ops/time/mod.rs
  - hp41-core/src/ops/time/clock.rs
  - hp41-core/src/ops/time/date_arith.rs
  - hp41-core/src/ops/time/alpha_time.rs
  - hp41-core/src/ops/time/stopwatch.rs
  - hp41-core/src/ops/time/alarm.rs
  - hp41-core/src/ops/time/modal.rs
  - scripts/check-free42-contamination.sh
autonomous: true
requirements:
  - TIME-FW-01
  - TIME-FW-02
  - TIME-FW-03
  - TIME-FW-04
  - TIME-FW-05
  - TIME-FW-06

must_haves:
  truths:
    - "All 35 Time Op variants compile in dispatch() and execute_op()"
    - "xrom_resolve with bit-2 set resolves all Time Pac function names to their Op variants"
    - "v3.1 save files (xrom_modules=3) load cleanly with auto-migration to xrom_modules=7"
    - "CalcState serialization round-trips all new persistent Time fields with correct defaults"
    - "Free42 contamination guard script scans the new time/ directory"
  artifacts:
    - path: "hp41-core/src/ops/time/mod.rs"
      provides: "Time module hub with pub re-exports"
    - path: "hp41-core/src/ops/time/clock.rs"
      provides: "Clock op function stubs"
    - path: "hp41-core/src/ops/time/date_arith.rs"
      provides: "Date arithmetic op function stubs"
    - path: "hp41-core/src/ops/time/alpha_time.rs"
      provides: "Alpha time op function stubs"
    - path: "hp41-core/src/ops/time/stopwatch.rs"
      provides: "Stopwatch op function stubs"
    - path: "hp41-core/src/ops/time/alarm.rs"
      provides: "Alarm op function stubs + AlarmEntry/AlarmType types"
    - path: "hp41-core/src/ops/time/modal.rs"
      provides: "TimeStep enum + dispatch stub functions"
  key_links:
    - from: "hp41-core/src/ops/math1/xrom.rs"
      to: "hp41-core/src/ops/time/mod.rs"
      via: "time_resolve() function resolving names to Op::Time* variants"
      pattern: "time_resolve"
    - from: "hp41-core/src/ops/mod.rs"
      to: "hp41-core/src/ops/time/"
      via: "dispatch() arms routing Op::Time* to time::* functions"
      pattern: "crate::ops::time::"
    - from: "hp41-core/src/state.rs"
      to: "hp41-core/src/ops/time/stopwatch.rs"
      via: "StopwatchMode enum imported for CalcState field type"
      pattern: "StopwatchMode"
---

<objective>
Phase 38 framework scaffolding: register Time Module (XROM 26) as the third XROM application module, add all 35 Op variants to the enum with stub implementations, extend CalcState with 12 new fields (8 persistent + 4 transient), wire dispatch()/execute_op() routing, extend the Free42 contamination guard, and update save-file migration.

Purpose: Establish the complete compile-time structure so subsequent Wave 2-3 plans can implement individual op functions independently and in parallel without touching shared infrastructure files.
Output: Compiling hp41-core with 35 new Op variants routing to stub functions; `cargo test -p hp41-core` passes including XROM registration and migration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-CONTEXT.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-RESEARCH.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-PATTERNS.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From hp41-core/src/ops/math1/xrom.rs:
- pub struct XromModule { pub id: u8, pub name: &'static str, pub ops: &'static [(&'static str, Op)] }
- pub const MATH_1: XromModule
- pub const STAT_1: XromModule
- pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>
- fn math1_resolve(name: &str) -> Option<Op>
- fn stat1_resolve(name: &str) -> Option<Op>

From hp41-core/src/ops/math1/modal.rs:
- pub enum ModalProgram { ..., Stat1(Stat1Step), /* add: Time(TimeStep) */ }
- impl ModalProgram { pub fn current_prompt(&self) -> Option<String>; pub fn requires_alpha_label(&self) -> bool; }

From hp41-core/src/state.rs:
- pub struct CalcState { ..., pub xrom_modules: u8, ... }
- fn default_xrom_modules() -> u8 { 0b0000_0011 }
- pub fn migrate_after_load(&mut self)

From hp41-core/src/ops/mod.rs:
- pub enum Op { ..., Rand, Seed }  (last Stat 1 variants)
- pub fn dispatch(state: &mut CalcState, op: Op) -> Result<(), HpError>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Time module directory + types + CalcState fields + migration + Free42 guard</name>
  <files>
    hp41-core/src/ops/time/mod.rs,
    hp41-core/src/ops/time/clock.rs,
    hp41-core/src/ops/time/date_arith.rs,
    hp41-core/src/ops/time/alpha_time.rs,
    hp41-core/src/ops/time/stopwatch.rs,
    hp41-core/src/ops/time/alarm.rs,
    hp41-core/src/ops/time/modal.rs,
    hp41-core/src/state.rs,
    scripts/check-free42-contamination.sh
  </files>
  <read_first>
    hp41-core/src/ops/stat1/mod.rs,
    hp41-core/src/state.rs,
    hp41-core/src/ops/stat1/modal.rs,
    scripts/check-free42-contamination.sh
  </read_first>
  <action>
    Create directory `hp41-core/src/ops/time/` with 7 source files. Every file carries the Free42 disclaim header per D-carried.6: "Algorithm independently re-derived from HP Time Module Owner's Manual (HP 82182A); Free42 source consulted only as sanity-check oracle, not copied."

    **time/mod.rs:** Module hub. Declare `pub mod clock; pub mod date_arith; pub mod alpha_time; pub mod stopwatch; pub mod alarm; pub mod modal;`. Add module docstring noting XROM ID 26, bit 2, Phase 38 activation. Re-export all pub `op_*` functions and types (AlarmEntry, AlarmType, StopwatchMode, ClockDisplayMode, TimeStep) needed by ops/program.rs and state.rs.

    **time/alarm.rs:** Define `AlarmEntry` struct (per D-38.8/D-38.10): fields `trigger_unix: i64`, `repeat_secs: i64`, `alarm_type: AlarmType`, `past_due: bool`. Derive `Debug, Clone, Serialize, Deserialize`. Define `AlarmType` enum: `Message(String)`, `Control { label: String, interrupting: bool }`. Derive same. Define stub `pub fn op_xyzalm(state: &mut CalcState) -> Result<(), HpError>` returning `Ok(())` for now (7 alarm ops + `check_alarms`). Import `crate::error::HpError` and `crate::state::CalcState`.

    **time/stopwatch.rs:** Define `StopwatchMode` enum: `Idle, Running, Stopped`. Derive `Debug, Clone, Copy, PartialEq, Serialize, Deserialize`. Implement `Default` returning `Idle`. Define stub pub functions for all 7 stopwatch ops.

    **time/clock.rs:** Define `ClockDisplayMode` enum: `Off, TimeOnly, TimeAndDate`. Derive `Debug, Clone, Copy, PartialEq, Serialize, Deserialize`. Implement `Default` returning `Off`. Define stub pub functions for all 13 clock/format ops.

    **time/date_arith.rs:** Define pub helper functions `parse_date_hpnum`, `parse_time_hpnum`, `secs_to_hpnum_time`, `date_to_jdn`, `jdn_to_date`, `jdn_to_dow` as stubs. Define stub pub op functions: `op_date_plus`, `op_ddays`, `op_dow`, `op_dmy`, `op_mdy`.

    **time/alpha_time.rs:** Define stub pub functions: `op_adate`, `op_atime`, `op_atime24`.

    **time/modal.rs:** Define `TimeStep` enum with variants: `SetTimePrompt`, `SetDatePrompt`, `XyzalmTimePrompt`. Derive `Debug, Clone, PartialEq`. Define stub pub functions: `current_prompt(step: &TimeStep) -> Option<String>`, `requires_alpha_label(step: &TimeStep) -> bool`, `submit_step(state: &mut CalcState, step: TimeStep) -> Result<(), HpError>`. The `current_prompt` stub returns `Some("TIME?".to_string())` etc. per variant. The `requires_alpha_label` stub returns `false`. The `submit_step` stub clears `state.modal_program = None; state.modal_prompt = None;` and returns `Ok(())`.

    **state.rs modifications (per D-38.2, D-38.7, D-38.10, Serde Decision Matrix):**
    - Add import: `use crate::ops::time::{StopwatchMode, ClockDisplayMode, alarm::AlarmEntry};`
    - Add 8 persistent fields with `#[serde(default)]`: `time_offset_secs: i64`, `clock_12h: bool`, `clock_display_mode: ClockDisplayMode`, `accuracy_factor: HpNum`, `alarms: Vec<AlarmEntry>`, `stopwatch_mode: StopwatchMode`, `stopwatch_accumulated: f64`, `stopwatch_split: f64`
    - Add 4 transient fields with `#[serde(default, skip)]`: `stopwatch_start: Option<std::time::Instant>`, `clock_active: bool`, `stopwatch_keyboard_mode: bool`, `alarm_catalog_mode: bool`
    - Update `default_xrom_modules()` to return `0b0000_0111` per D-carried.5
    - Extend `migrate_after_load()`: add v3.1-to-v3.2 bit-2 migration (same pattern as existing bit-1 migration); add stopwatch Running-to-Stopped transition per D-38.6 (if `self.stopwatch_mode == StopwatchMode::Running { self.stopwatch_mode = StopwatchMode::Stopped; self.stopwatch_start = None; }`)
    - Update `CalcState::new()`: initialize all 12 new fields with their defaults (0, false, Default::default(), Vec::new(), None)

    **scripts/check-free42-contamination.sh:** Add `TIME_DIR="hp41-core/src/ops/time"` variable. Extend the directory-existence-check loop to include `"$TIME_DIR"`. Extend the `PATTERN` variable with 3 additional Free42 time-module identifiers: `core_commands7`, `date2j`, `j2date`. Extend the grep scan loop to include `"$TIME_DIR"`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo check -p hp41-core 2>&1 | tail -5 && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/ops/time/` directory exists with 7 .rs files
    - Every file in `time/` starts with the Free42 disclaim header
    - `state.rs` contains `time_offset_secs: i64` with `#[serde(default)]`
    - `state.rs` contains `stopwatch_start: Option<std::time::Instant>` with `#[serde(default, skip)]`
    - `default_xrom_modules()` returns `0b0000_0111`
    - `migrate_after_load()` contains `self.xrom_modules |= 0b0000_0100` and stopwatch Running-to-Stopped transition
    - `StopwatchMode` enum has 3 variants: Idle, Running, Stopped
    - `ClockDisplayMode` enum has 3 variants: Off, TimeOnly, TimeAndDate
    - `AlarmEntry` struct has 4 fields: trigger_unix, repeat_secs, alarm_type, past_due
    - `AlarmType` enum has 2 variants: Message(String), Control { label, interrupting }
    - `TimeStep` enum has 3 variants: SetTimePrompt, SetDatePrompt, XyzalmTimePrompt
    - `scripts/check-free42-contamination.sh` contains `TIME_DIR="hp41-core/src/ops/time"` and `core_commands7|date2j|j2date` in PATTERN
    - `cargo check -p hp41-core` exits 0
    - `bash scripts/check-free42-contamination.sh` exits 0
  </acceptance_criteria>
  <done>All 7 time module files exist with correct types defined; CalcState has 12 new fields with correct serde annotations; migration handles v3.1 to v3.2; Free42 guard covers time/ directory; cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Op enum variants + XROM registration + dispatch/execute_op wiring</name>
  <files>
    hp41-core/src/ops/mod.rs,
    hp41-core/src/ops/program.rs,
    hp41-core/src/ops/math1/xrom.rs,
    hp41-core/src/ops/math1/modal.rs
  </files>
  <read_first>
    hp41-core/src/ops/mod.rs,
    hp41-core/src/ops/program.rs,
    hp41-core/src/ops/math1/xrom.rs,
    hp41-core/src/ops/math1/modal.rs
  </read_first>
  <action>
    **ops/mod.rs:** Add `pub mod time;` to module declarations (after `pub mod stat1;`). Add 35 new variants to the `Op` enum under a `// -- Phase 38 (v3.2): Time Module (XROM 26) --` comment block. Variant names per PATTERNS.md: TimeAdate, TimeAlmcat, TimeAlmnow, TimeAtime, TimeAtime24, TimeClk12, TimeClk24, TimeClkt, TimeClktd, TimeClock, TimeCorrect, TimeDate, TimeDatePlus, TimeDdays, TimeDmy, TimeDow, TimeMdy, TimeRclaf, TimeRclalm, TimeRclsw, TimeRunsw, TimeSetaf, TimeSetdate, TimeSetime, TimeSetsw, TimeStopsw, TimeSw, TimeTplusx, TimeTime, TimeXyzalm, TimeClalma, TimeClalmx, TimeClralms, TimeSwpt, TimeStpw.

    Add 35 dispatch() match arms under a `// -- Phase 38 (v3.2): Time Module (XROM 26) --` comment. Route each variant to its stub function in the time/ module using the exact paths from PATTERNS.md (e.g., `Op::TimeTime => crate::ops::time::clock::op_time(state)`). Add comment: "// Phase 38 sanctioned CI break -- Phase 39 closes item 3, Phase 41 closes item 4."

    **ops/program.rs:** Add 35 execute_op() match arms following the Stat 1 precedent pattern: `Op::TimeTime => crate::ops::dispatch(state, Op::TimeTime)`. All Time Module ops route through dispatch (same as Stat 1 ops pattern).

    **math1/xrom.rs:** Add `TIME_MODULE` const (XromModule with id: 26, name: "TIME 2C", ops: 35-entry array mapping mnemonic strings to Op::Time* variants). Add `fn time_resolve(name: &str) -> Option<Op>` with a match block for all 35 mnemonics. Extend `xrom_resolve()` with bit-2 arm AFTER the bit-1 (Stat 1) arm per D-carried.5: `if modules & 0b0000_0100 != 0 { if let Some(op) = time_resolve(name) { return Some(op); } }`. Add 3 unit tests: `time_module_const_id_and_name` (asserts id=26, name="TIME 2C"), `time_module_ops_has_correct_entry_count` (asserts len=35), `resolve_uses_bit_2_for_time_module` (asserts TIME resolves with bit-2 set, does not resolve with bit-0 only).

    **math1/modal.rs:** Add `Time(crate::ops::time::modal::TimeStep)` variant to `ModalProgram` enum with doc-comment referencing D-carried.4 freeze exception. Add dispatch arm in `current_prompt` match: `ModalProgram::Time(step) => crate::ops::time::modal::current_prompt(step)`. Add dispatch arm in `requires_alpha_label` match: `ModalProgram::Time(step) => crate::ops::time::modal::requires_alpha_label(step)`. Add dispatch arm in any other match blocks that cover ModalProgram variants (check for `submit_modal_input` or similar).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core -- time_module 2>&1 | tail -10 && cargo test -p hp41-core -- resolve_uses_bit_2 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ops/mod.rs` contains `pub mod time;`
    - `Op` enum contains all 35 `Time*` variants (grep -c "Time" ops/mod.rs >= 35)
    - `dispatch()` contains 35 arms routing to `crate::ops::time::` functions
    - `execute_op()` contains 35 arms routing through `crate::ops::dispatch(state, Op::Time*)`
    - `math1/xrom.rs` contains `pub const TIME_MODULE: XromModule` with id 26
    - `math1/xrom.rs` contains `fn time_resolve(name: &str) -> Option<Op>` with 35 match arms
    - `xrom_resolve()` contains `modules & 0b0000_0100` arm after the bit-1 arm
    - `math1/modal.rs` ModalProgram enum contains `Time(crate::ops::time::modal::TimeStep)` variant
    - `cargo test -p hp41-core` passes (all existing + 3 new xrom tests)
    - `cargo test -p hp41-core -- time_module_const_id_and_name` passes
    - `cargo test -p hp41-core -- time_module_ops_has_correct_entry_count` passes (35 entries)
    - `cargo test -p hp41-core -- resolve_uses_bit_2_for_time_module` passes
  </acceptance_criteria>
  <done>All 35 Op::Time* variants declared, dispatch/execute_op fully wired to stub functions, XROM registration complete with bit-2 resolver, ModalProgram::Time variant added, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: CalcState serde round-trip + migration integration tests</name>
  <files>
    hp41-core/src/state.rs
  </files>
  <read_first>
    hp41-core/src/state.rs
  </read_first>
  <action>
    Add integration tests to `state.rs` test module (or create `hp41-core/tests/time_serde_compat.rs` if the test module would become too large). Tests to add:

    1. `time_fields_serde_round_trip`: Create CalcState::new(), set `time_offset_secs = 3600`, `clock_12h = true`, `clock_display_mode = ClockDisplayMode::TimeAndDate`, `accuracy_factor = HpNum::from_str("1.5")`, push an AlarmEntry into `alarms`, set `stopwatch_mode = StopwatchMode::Stopped`, `stopwatch_accumulated = 123.45`, `stopwatch_split = 10.5`. Serialize to JSON, deserialize back, assert all persistent fields match. Assert transient fields (`stopwatch_start`, `clock_active`, `stopwatch_keyboard_mode`, `alarm_catalog_mode`) are at defaults after deserialization.

    2. `time_v31_save_migration`: Construct a JSON string representing a v3.1 save file (contains `"xrom_modules": 3` and NO time-related fields). Deserialize into CalcState. Call `migrate_after_load()`. Assert `xrom_modules == 7` (bit-2 set). Assert all time fields at defaults. Assert `stopwatch_mode == StopwatchMode::Idle`.

    3. `time_stopwatch_running_freeze_on_load`: Construct a CalcState with `stopwatch_mode = StopwatchMode::Running`, serialize to JSON (note: Running persists since it has serde(default) not skip). Deserialize and call `migrate_after_load()`. Assert `stopwatch_mode == StopwatchMode::Stopped` (D-38.6 freeze semantics).

    4. `default_xrom_modules_returns_0b111`: Assert `default_xrom_modules()` returns 7 (0b0000_0111). Complements the existing test that checked for 0b11.

    Update the existing `default_xrom_modules_regression` test (currently asserts `0b0000_0011`) to assert `0b0000_0111`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core -- time_fields_serde time_v31_save time_stopwatch_running default_xrom_modules 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test `time_fields_serde_round_trip` passes (persistent fields survive, transient fields reset)
    - Test `time_v31_save_migration` passes (xrom_modules migrated to 7)
    - Test `time_stopwatch_running_freeze_on_load` passes (Running becomes Stopped)
    - Test `default_xrom_modules_returns_0b111` passes (returns 7)
    - Existing `default_xrom_modules` test updated to assert 7 instead of 3
    - `cargo test -p hp41-core` all pass (no regressions in existing tests)
  </acceptance_criteria>
  <done>CalcState serde contract validated for all 12 new fields; v3.1 backward compat proven; stopwatch freeze-on-load semantics tested; default_xrom_modules updated across all test assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CalcState deserialization | Untrusted JSON from autosave.json loaded into CalcState struct |
| xrom_resolve name input | User-typed function name passed to resolver chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | CalcState deserialization (new fields) | mitigate | All new fields have `#[serde(default)]`; invalid enum values rejected by serde; Instant field is `skip` |
| T-38-02 | Denial of Service | alarms Vec unbounded | accept | Cap enforcement deferred to Plan 05 (op_xyzalm implementation); empty Vec is harmless in scaffold |
| T-38-03 | Tampering | xrom_resolve name matching | mitigate | Exact string match only (no regex/eval); match returns None for unknown names |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in Phase 38 (D-carried.1); no install tasks |
</threat_model>

<verification>
After all three tasks complete:
- `cargo test -p hp41-core` passes with 0 failures
- `cargo check -p hp41-core` exits 0 (no warnings from new code)
- `bash scripts/check-free42-contamination.sh` exits 0
- `grep -c "Time" hp41-core/src/ops/mod.rs` returns >= 35
- `grep "0b0000_0111" hp41-core/src/state.rs` returns matches
</verification>

<success_criteria>
Complete framework scaffolding: 35 Op variants compile, XROM 26 resolves, CalcState fields serialize/deserialize correctly, migration handles v3.1 saves, Free42 guard scans time/ directory, all existing tests pass unmodified.
</success_criteria>

<output>
Create `.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-01-SUMMARY.md` when done
</output>
