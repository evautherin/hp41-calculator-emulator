---
phase: 61-quality-gates
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - hp41-cli/tests/phase61_help_search_aliases.rs
autonomous: true
requirements: [HSQUAL-01, HSQUAL-02]
user_setup: []

must_haves:
  truths:
    - "Rust real-data tests prove all four scoring tiers (exact > prefix > substring > fuzzy) against the loaded JSON pools"
    - "Rust tests prove DE+EN alias resolution: 'zeitwert des geldes'->TVM, 'zinseszins'->TVM, 'square root'->SQRT, 'quadratwurzel'->SQRT, 'wurzel'->SQRT, all top-1"
    - "Rust test proves at least one fuzzy-hit case top-1 (e.g. 'sqirt'->SQRT)"
    - "Rust test proves whitespace-only query yields no ranked results (empty-after-trim passthrough contract)"
    - "Rust test loads docs/fixtures/help_search_parity.json via include_str! and asserts every query's top-1 matches"
  artifacts:
    - path: "hp41-cli/tests/phase61_help_search_aliases.rs"
      provides: "Rust integration tests over real loaded JSON + Rust side of the parity fixture"
      min_lines: 60
      contains: "ranked_help_entries"
  key_links:
    - from: "hp41-cli/tests/phase61_help_search_aliases.rs"
      to: "hp41_cli::help_data::ranked_help_entries"
      via: "use hp41_cli::help_data::{ranked_help_entries, HelpRow}"
      pattern: "ranked_help_entries"
    - from: "hp41-cli/tests/phase61_help_search_aliases.rs"
      to: "docs/fixtures/help_search_parity.json"
      via: "include_str!(\"../../../docs/fixtures/help_search_parity.json\")"
      pattern: "include_str.*help_search_parity"
---

<objective>
Add the Rust real-data verification suite (HSQUAL-01 Rust + HSQUAL-02 Rust side) in a NEW
integration test file `hp41-cli/tests/phase61_help_search_aliases.rs`. Unlike the existing
Phase-59 tests (which inject synthetic `make_entry()` data), these tests call
`ranked_help_entries()` against the REAL loaded six-pool JSON data, proving the Phase-60
aliases plus the Plan-61-01 hand-edits resolve as intended. The file also loads the shared
parity fixture via `include_str!` and asserts the Rust matcher produces the expected top-1
for every canonical query — the Rust half of the CLI<->GUI drift guard.

Purpose: lock real-data scorer behavior + the Rust side of parity in one fast cargo test.
Output: one new Rust integration test file; `cargo test -p hp41-cli --test phase61_help_search_aliases` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-quality-gates/61-RESEARCH.md
@.planning/phases/61-quality-gates/61-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Confirmed against hp41-cli/src/help_data.rs on 2026-06-05. -->

Public API (re-exported from hp41_cli::help_data):
  pub fn ranked_help_entries(query: &str) -> Vec<HelpRow>
      // lowercases + trims `query` internally; pass raw queries.
      // CONTRACT (doc comment at help_data.rs:480-482): empty-string callers must use
      //   filter_help_rows / help_overlay_rows instead. A debug_assert!(!query.is_empty(), …)
      //   at help_data.rs:483-495 ABORTS the test on a literal "" in debug builds.
      // A whitespace-only query trims to empty and returns Vec::new() (the guard this plan tests).
      // Sorts (score DESC, then display_name ASC) over status=="implemented" entries
      // across all six pools (help_entries_all()).
  pub fn score_entry(entry: &HelpEntry, q: &str) -> u8   // q must be pre-lowercased
  pub struct HelpRow { pub key: String, pub op: String, pub desc: String }
      // HelpRow.op == the entry's display_name (e.g. "TVM", "SQRT", "PI", "EMDIR").

Existing precedent: hp41-cli/tests/phase59_help_search.rs
  use line:  use hp41_cli::help_data::{filter_help_rows, ranked_help_entries, score_entry, HelpEntry, HelpRow};
  Phase 59 tests are SYNTHETIC (make_entry). Phase 61 must use ranked_help_entries() on REAL data.

serde_json is already a workspace dependency — use it to parse the fixture.

Verified top-1 results against live data (RESEARCH "Verified canonical queries"):
  "tvm"->TVM(40), "pi"->PI(40), "emdir"->EMDIR(40),
  "zeitwert des geldes"->TVM(35), "quadratwurzel"->SQRT(35),
  "square root"->SQRT(35), "financial solver"->TVM(35), "sqirt"->SQRT(8 fuzzy).
After Plan 61-01 hand-edit: "zinseszins"->TVM(35), "compound interest"->TVM(35), "wurzel"->SQRT(35).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-data tier + alias + fuzzy + empty-query tests</name>
  <files>hp41-cli/tests/phase61_help_search_aliases.rs</files>
  <read_first>
    - hp41-cli/tests/phase59_help_search.rs (use line, test idioms, helper imports — mirror its style; do NOT reuse its synthetic make_entry)
    - hp41-cli/src/help_data.rs lines 480-530 (ranked_help_entries: the doc comment at 480-482 documents the empty-string contract — empty-string callers must use filter_help_rows/help_overlay_rows; the debug_assert!(!query.is_empty(), "ranked_help_entries called with empty query") at 483-495 ABORTS on a literal "" in debug builds, so tests must NOT call ranked_help_entries(""); a whitespace-only query trims to empty and returns Vec::new(); HelpRow.op == display_name, sort order)
    - .planning/phases/61-quality-gates/61-RESEARCH.md (Pattern 1 "Rust Integration Test Against Real JSON Data"; Pitfalls 3/4/5; the "Phase 61 MUST ADD" list under Validation Architecture)
    - docs/hp41-advantage-functions.json + docs/hp41cv-functions.json (post-61-01 aliases — confirm "zinseszins"/"wurzel" are present)
  </read_first>
  <behavior>
    - exact-name tier: ranked_help_entries("tvm")[0].op == "TVM"; ("pi")[0].op == "PI"; ("emdir")[0].op == "EMDIR"
    - exact-alias DE tier: ("zeitwert des geldes")[0].op == "TVM"; ("quadratwurzel")[0].op == "SQRT"
    - exact-alias EN tier: ("square root")[0].op == "SQRT"; ("financial solver")[0].op == "TVM"
    - spec-example DE aliases (depend on 61-01 hand-edit): ("zinseszins")[0].op == "TVM"; ("compound interest")[0].op == "TVM"; ("wurzel")[0].op == "SQRT"
    - prefix tier: a multi-entry prefix query (e.g. "sq") returns SQRT among results and a name-prefix beats a substring-only hit (assert relative ordering, not a brittle absolute index)
    - substring tier: a substring-only query still returns the intended entry (non-empty result)
    - fuzzy tier: ("sqirt")[0].op == "SQRT" (1-typo on the display_name)
    - empty-after-trim contract: ranked_help_entries("   ") (whitespace-only) is empty. Do NOT assert ranked_help_entries("") — a literal "" trips the debug_assert! guard and aborts the test; the pure empty-string passthrough is the contract of filter_help_rows and is already covered by Phase-59.
  </behavior>
  <action>
    Create hp41-cli/tests/phase61_help_search_aliases.rs. Import the real API
    (`use hp41_cli::help_data::{ranked_help_entries, score_entry, HelpEntry, HelpRow};`
    — drop unused imports to satisfy `#[deny(warnings)]`/clippy). Add a top-of-file
    `#![allow(clippy::unwrap_used)]` (test files may unwrap, per CLAUDE.md). Write one
    `#[test]` fn per behavior above, each calling `ranked_help_entries()` on the REAL
    loaded pools (NOT a synthetic pool) and asserting `results[0].op` equals the expected
    display_name, with a descriptive failure message naming the query and the actual
    `results[0].op`. For the empty-after-trim test assert
    `ranked_help_entries("   ").is_empty()` (whitespace-only — exercises the real
    empty-after-trim guard). Do NOT call `ranked_help_entries("")`: a literal empty string
    trips the `debug_assert!(!query.is_empty(), …)` and aborts the test in debug builds. The
    pure empty-string passthrough is the contract of `filter_help_rows`/`help_overlay_rows`
    and is already covered by the Phase-59 test — do not duplicate it here.
    Do NOT replicate Phase-59 synthetic coverage; these are real-data assertions only. No
    fenced code beyond what the executor writes; name exact queries and expected ops from
    <behavior>. Touch NO file outside this test file; never touch hp41-core/.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase61_help_search_aliases</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase61_help_search_aliases` exits 0
    - The run shows a test asserting ("zinseszins")[0].op == "TVM" passing (proves 61-01 hand-edit is wired)
    - The run shows a test asserting ("wurzel")[0].op == "SQRT" passing
    - The run shows a test asserting ("sqirt")[0].op == "SQRT" passing (fuzzy tier)
    - The run shows a whitespace-only-query test asserting `ranked_help_entries("   ").is_empty()`
    - No test calls `ranked_help_entries("")` (would abort on the debug_assert!): `grep -n 'ranked_help_entries("")' hp41-cli/tests/phase61_help_search_aliases.rs` returns empty
    - `cargo clippy -p hp41-cli --tests` produces no new warnings for this file
    - `grep -n "make_entry" hp41-cli/tests/phase61_help_search_aliases.rs` returns empty (real data, not synthetic)
    - `grep -n "hp41-core" hp41-cli/tests/phase61_help_search_aliases.rs` returns empty
  </acceptance_criteria>
  <done>All four tiers, DE+EN alias resolution (incl. the 61-01 spec examples), a fuzzy hit, and the whitespace-only empty-after-trim passthrough are proven against the real loaded JSON via cargo test.</done>
</task>

<task type="auto">
  <name>Task 2: Rust side of the CLI<->GUI parity fixture</name>
  <files>hp41-cli/tests/phase61_help_search_aliases.rs</files>
  <read_first>
    - docs/fixtures/help_search_parity.json (the fixture created in 61-01: object with `queries` array of {query, top_n, expected_top, note})
    - hp41-cli/tests/function_matrix_parity.rs (precedent for a fixture-driven parity test loop in this repo)
    - .planning/phases/61-quality-gates/61-RESEARCH.md (Pattern 2 "Rust test pattern"; Pitfall 7 — include_str! path is RELATIVE to the test source file: from hp41-cli/tests/ the path is "../../../docs/fixtures/help_search_parity.json")
  </read_first>
  <action>
    Append to hp41-cli/tests/phase61_help_search_aliases.rs a `#[test] fn parity_fixture_rust()`
    that embeds the fixture at compile time via
    `include_str!("../../../docs/fixtures/help_search_parity.json")`, parses it with
    `serde_json::from_str::<serde_json::Value>`, iterates `fixture["queries"]`, and for each
    case reads `query` (str), `top_n` (u64), and `expected_top` (array of str), calls
    `ranked_help_entries(query)`, and asserts that for each i in 0..top_n,
    `results.get(i).map(|r| r.op.as_str())` equals `expected_top[i]`, with a failure message
    naming the query, position, and expected/actual op. Verify the include_str! path resolves
    at compile time (a wrong path is a compile error). Touch only this test file.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase61_help_search_aliases parity_fixture_rust</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase61_help_search_aliases parity_fixture_rust` exits 0
    - `grep -n "include_str!(\"../../../docs/fixtures/help_search_parity.json\")" hp41-cli/tests/phase61_help_search_aliases.rs` matches (correct relative path)
    - Temporarily corrupting one `expected_top` value in the fixture makes `parity_fixture_rust` FAIL (proves the loop actually asserts; revert after) — record this manual check in the SUMMARY
    - `cargo test -p hp41-cli --test phase61_help_search_aliases` (whole file) exits 0
  </acceptance_criteria>
  <done>The Rust matcher's top-N output matches the committed parity fixture for every canonical query; the include_str! path is correct and compile-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant surface. This plan adds a Rust integration test file that reads
committed static JSON (the six pools via the library + the parity fixture via include_str!).
No authentication, authorization, cryptographic, network, or untrusted-input surface; the
test code does not execute or eval JSON content, only parses and compares strings.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-NA | N/A | hp41-cli/tests/phase61_help_search_aliases.rs | accept | No security-relevant surface (tests over committed static data); ASVS L1 N/A. |
</threat_model>

<verification>
- `cargo test -p hp41-cli --test phase61_help_search_aliases` green (per-task quick run)
- `just test` green (per-wave merge)
- No synthetic make_entry data; all assertions go through `ranked_help_entries()` real data
</verification>

<success_criteria>
- New file hp41-cli/tests/phase61_help_search_aliases.rs covers four tiers, DE+EN alias resolution, a fuzzy hit, whitespace-only empty-after-trim passthrough, and the parity fixture
- All tests green via `cargo test -p hp41-cli --test phase61_help_search_aliases`
- hp41-core untouched; no new deps
</success_criteria>

<output>
Create `.planning/phases/61-quality-gates/61-02-SUMMARY.md` when done.
</output>
