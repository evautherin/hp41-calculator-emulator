---
phase: 59-runtime-matcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-cli/tests/phase59_help_search.rs
  - hp41-gui/src/help_data.test.ts
  - hp41-gui/src/HelpOverlay.test.tsx
autonomous: true
requirements: [HSMATCH-01, HSMATCH-02, HSMATCH-03, HSMATCH-04, HSMATCH-05, HSUX-01, HSUX-02]
user_setup: []

must_haves:
  truths:
    - "Three RED test files exist describing the full scorer contract before any scorer code is written"
    - "Rust integration test asserts tier order, fuzzy typo hits (Zineszins->TVM, Wurzel->SQRT), DE+EN alias resolution, and empty-query invariance"
    - "TS scoreEntry/rankedEntries tests mirror the Rust assertions tier-for-tier"
    - "HelpOverlay render test asserts flat-ranked output for non-empty query and grouped output for empty query"
  artifacts:
    - path: "hp41-cli/tests/phase59_help_search.rs"
      provides: "RED Rust scorer contract (score_entry, ranked_help_entries) over synthetic HelpEntry fixtures"
      contains: "fn make_entry"
      min_lines: 80
    - path: "hp41-gui/src/help_data.test.ts"
      provides: "RED TS scorer contract (scoreEntry, rankedEntries)"
      contains: "scoreEntry"
    - path: "hp41-gui/src/HelpOverlay.test.tsx"
      provides: "RED render-branch contract — flat-ranked vs section-grouped"
      contains: "help-overlay-category-heading"
  key_links:
    - from: "hp41-cli/tests/phase59_help_search.rs"
      to: "hp41_cli::help_data"
      via: "use import of score_entry, ranked_help_entries, HelpEntry"
      pattern: "use hp41_cli::help_data::"
    - from: "hp41-gui/src/help_data.test.ts"
      to: "./help_data"
      via: "import of scoreEntry, rankedEntries"
      pattern: "from './help_data'"
---

<objective>
Write the failing (RED) test contract for the Phase 59 runtime matcher BEFORE any scorer implementation exists. This is the Nyquist Wave 0 mandated by 59-VALIDATION.md: the three test files encode every scorer behavior (tier order, fuzzy typo tolerance, DE+EN alias resolution, empty-query invariance, flat-ranked render branch) as executable assertions. The two Wave-1 implementation plans (CLI scorer 59-02, GUI scorer 59-03) turn these RED tests GREEN.

Purpose: Tests-first locks the scoring contract so the two mirrored implementations are byte-equivalent in behavior. The scoring logic is "business logic with defined I/O" — exactly the case that benefits from tests-first even with full TDD mode off.
Output: Three RED test files (one new Rust integration test, two extended TS test files), all referencing not-yet-existing scorer symbols so they fail until Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-runtime-matcher/59-VALIDATION.md
@.planning/phases/59-runtime-matcher/59-RESEARCH.md
@.planning/phases/59-runtime-matcher/59-PATTERNS.md

<interfaces>
<!-- Symbols the test files reference. These are the contracts Wave-1 plans implement. -->
<!-- Rust (hp41-cli/src/help_data.rs — added by plan 59-02): -->
<!--   pub fn score_entry(entry: &HelpEntry, q: &str) -> u8     (0 = no match; higher = better) -->
<!--   pub fn ranked_help_entries(query: &str) -> Vec<HelpRow>  (flat, score DESC, then display_name ASC; no headers) -->
<!--   HelpEntry already exists (Phase 58) with field: pub search_aliases: Vec<String> -->
<!--   score_entry expects a PRE-LOWERCASED query q (caller lowercases once). -->
<!-- TS (hp41-gui/src/help_data.ts — added by plan 59-03): -->
<!--   export function scoreEntry(entry: HelpEntry, q: string): number   (0 = no match) -->
<!--   export function rankedEntries(entries: readonly HelpEntry[], q: string): HelpEntry[] -->
<!--   q is the lowercased+trimmed query. -->
<!-- Tier constants (RESEARCH.md §Tiered Scoring — EXPECTED relative orderings the tests assert): -->
<!--   name:  exact 40 > prefix 32 > substr 24 > fuzzy 8 -->
<!--   alias: exact 35 > prefix 28 > substr 21 > fuzzy 7 -->
<!--   desc:  exact 30 > prefix 24 > substr 18 > fuzzy 6 -->
<!--   cat:   exact 20 > prefix 16 > substr 12 > fuzzy 4 -->
<!-- Fuzzy threshold: max(1, query.len()/4); fuzzy only when query.len() >= 2 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED Rust integration test phase59_help_search.rs</name>
  <read_first>
    - hp41-cli/tests/phase25_help_data.rs (analog: file header, `#![allow(clippy::unwrap_used)]`, `use hp41_cli::help_data::{...}` import, `#[test]` structure — 59-PATTERNS.md lines 281-323)
    - hp41-cli/src/help_data.rs (the `HelpEntry` struct field list at lines 64-88 — make_entry must construct EXACTLY: op_variant, display_name, category, status, phase, key_path, description, divergences, xrom, search_aliases; the `HelpRow` struct at lines 358-365; the existing `empty_query_returns_all_rows` test at lines 367-430 — 59-PATTERNS.md lines 27-118)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Tiered Scoring (lines 220-249) + §Scorer skeleton Rust (lines 486-567) — tier constants and the Zineszins/Wurzel worked examples
    - .planning/phases/59-runtime-matcher/59-VALIDATION.md §Per-Task Verification Map + §Wave 0 Requirements — the test-name filter substrings the documented `cargo test ... -- <filter>` commands select
  </read_first>
  <action>
    Create hp41-cli/tests/phase59_help_search.rs as a new integration test. Open with a `//! Phase 59 Wave 0 — RED scorer contract; impl lands in plan 59-02` doc comment, then `#![allow(clippy::unwrap_used)]`. Import `use hp41_cli::help_data::{score_entry, ranked_help_entries, filter_help_rows, HelpEntry, HelpRow};`. The scorer symbols do NOT yet exist — the file is intended to fail to compile until 59-02 supplies them; that is the RED Wave-0 state.

    Add a private `make_entry(display_name, description, category, aliases: &[&str]) -> HelpEntry` helper constructing a HelpEntry literal with all current fields (op_variant = display_name, status = "implemented", phase = None, key_path = None, xrom = None, divergences = vec![], search_aliases collected from the slice), per the 59-PATTERNS.md fixture pattern.

    Write `#[test]` functions whose names contain the VALIDATION.md filter substrings:
    - `tier_order_exact_prefix_substring_fuzzy` (HSMATCH-02): entry display_name "TVM"; assert score_entry("tvm") == 40 (exact-name) and exact > word-prefix > substring > fuzzy band, fuzzy lowest non-zero. Isolate each tier with distinct fields/entries.
    - `fuzzy_typo_zineszins_resolves_tvm` (HSMATCH-03, HSUX-02): entry "TVM" alias "Zinseszins"; assert `score_entry(&tvm, "zineszins") > 0` (1 typo within threshold max(1,9/4)=2); a control entry scores 0.
    - `fuzzy_wurzel_resolves_sqrt` (HSUX-02): entry "SQRT" alias "Wurzel"; assert `score_entry(&sqrt, "wurzel") == 35` (alias-exact band) and sqrt is the max-scoring entry across a small fixture vec.
    - `alias_de_en_both_resolve` (HSMATCH-05, HSMATCH-01): entry "TVM" aliases ["Zinseszins","compound interest"]; assert both queries score > 0; an alias-less entry scores 0 for both.
    - `empty_query_returns_all_grouped` (HSMATCH-04): build a small `Vec<HelpRow>` literal fixture (mirror the existing `empty_query_returns_all_rows` test) and assert `filter_help_rows(&rows, "")` returns `rows.len()` items in original order — the regression guard that 59-02 leaves the empty-query guard untouched.
    - `score_over_all_four_fields` (HSMATCH-01): four entries each matching only via display_name / description / category / alias; assert all four score > 0.

    Do NOT implement the scorer here and do NOT add scorer code to help_data.rs in this plan. The file is RED on purpose. Never inline a fenced implementation — only assertions.
  </action>
  <acceptance_criteria>
    - hp41-cli/tests/phase59_help_search.rs contains `fn make_entry` and test fns with substrings: tier_order, fuzzy_typo_zineszins, fuzzy_wurzel, alias_de_en, empty_query_returns_all_grouped, score_over_all_four_fields
    - The file imports filter_help_rows and HelpRow in addition to the scorer symbols
    - `grep -c "#\[test\]"` returns >= 6
    - The file references but never DEFINES score_entry / ranked_help_entries
    - `cargo test -p hp41-cli --test phase59_help_search` fails to compile (RED, symbols unresolved) — expected Wave-0 state, recorded in the SUMMARY, not a defect
  </acceptance_criteria>
  <verify>
    <automated>grep -q "fn make_entry" hp41-cli/tests/phase59_help_search.rs && grep -Eq "tier_order|fuzzy_typo_zineszins|fuzzy_wurzel|alias_de_en|empty_query_returns_all_grouped" hp41-cli/tests/phase59_help_search.rs && [ "$(grep -c '#\[test\]' hp41-cli/tests/phase59_help_search.rs)" -ge 6 ]</automated>
  </verify>
  <done>phase59_help_search.rs exists with make_entry fixture + >=6 RED tests covering all six named cases; references (never defines) the scorer fns; compiles RED until 59-02.</done>
</task>

<task type="auto">
  <name>Task 2: RED TS scorer tests in help_data.test.ts</name>
  <read_first>
    - hp41-gui/src/help_data.test.ts (existing test file — two-level `describe`/`it` nesting, the Phase 58 `search_aliases backward-compat` describe block; `globals: false` requires explicit `import { describe, it, expect } from 'vitest';` — 59-PATTERNS.md lines 352-376)
    - hp41-gui/src/help_data.ts (the `HelpEntry` interface at lines 59-94 incl. `search_aliases?: string[]`; the `filterHelpEntries` analog at lines 155-167; `helpEntriesAll()` — 59-PATTERNS.md lines 122-173)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Scorer skeleton TypeScript (lines 570-618) — the exact scoreEntry/rankedEntries signatures and the `?? []` alias guard
  </read_first>
  <action>
    Append `describe('Phase 59 scorer — scoreEntry', ...)` and `describe('Phase 59 ranking — rankedEntries', ...)` blocks to hp41-gui/src/help_data.test.ts. Extend the existing import to also pull `scoreEntry, rankedEntries` from `'./help_data'` (these exports do not yet exist — RED until 59-03). Keep explicit Vitest imports since `globals: false`.

    Build inline literal `HelpEntry` objects (search_aliases as `string[]`) — the TS twin of the Rust make_entry fixture. Write `it(...)` cases mirroring the Rust tests one-for-one for CLI↔GUI parity:
    - tier order: scoreEntry exact==40 on display_name "tvm"; exact > prefix > substring > fuzzy; fuzzy lowest non-zero band.
    - fuzzy typo: scoreEntry(tvm, "zineszins") > 0 with alias "Zinseszins"; control entry scores 0.
    - fuzzy wurzel: scoreEntry(sqrt, "wurzel") == 35 with alias "Wurzel".
    - alias DE+EN: scoreEntry(tvm, "zinseszins") > 0 AND scoreEntry(tvm, "compound interest") > 0; alias-less entry scores 0 for both.
    - all four fields: four entries matching only name/description/category/alias each score > 0.
    - rankedEntries: given a small entry array, returns entries sorted score DESC then display_name ASC, filtering score==0; assert top entry for "wurzel" is SQRT and zero-score entries absent.

    Mirror the Rust numeric expectations exactly (exact name 40, alias exact 35). Do NOT implement scoreEntry/rankedEntries. No DOM render here, so no `afterEach(cleanup)` in this file.
  </action>
  <acceptance_criteria>
    - help_data.test.ts imports scoreEntry and rankedEntries from './help_data'
    - New describe blocks name scoreEntry and rankedEntries with it-cases for tier order, fuzzy typo, fuzzy wurzel, alias DE+EN, all-four-fields, ranked sort
    - Assertions encode the same numeric expectations as the Rust twin (exact name 40, alias exact 35)
    - `cd hp41-gui && npm test -- help_data` fails (RED, scoreEntry/rankedEntries unresolved) — expected Wave-0 state
  </acceptance_criteria>
  <verify>
    <automated>grep -q "scoreEntry" hp41-gui/src/help_data.test.ts && grep -q "rankedEntries" hp41-gui/src/help_data.test.ts && grep -Eiq "wurzel|zineszins|compound interest" hp41-gui/src/help_data.test.ts</automated>
  </verify>
  <done>help_data.test.ts has Phase-59 scoreEntry + rankedEntries describe blocks mirroring the Rust assertions; imports the not-yet-existing exports; RED until 59-03.</done>
</task>

<task type="auto">
  <name>Task 3: RED HelpOverlay render-branch tests in HelpOverlay.test.tsx</name>
  <read_first>
    - hp41-gui/src/HelpOverlay.test.tsx (existing render tests — the CSS selectors `.help-overlay-category-heading`, `.help-overlay-row`, `.help-overlay-search`, `.help-overlay-empty` at lines 237-253; the `afterEach(cleanup)` portal pattern — 59-PATTERNS.md lines 225-232, §afterEach lines 360-370)
    - hp41-gui/src/HelpOverlay.tsx (the two filter useMemos `filtered`/`filteredAllFn` at lines 222-241 and the `sectionGroups` grouping at lines 244-291, the empty-query guard `if (q === '') return allEntries` — 59-PATTERNS.md lines 177-223; these are what 59-03 changes and what these tests pin)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Pattern 4 GUI Rendering (lines 338-361) + §Pitfall 4 (lines 454-459) — the flat-vs-grouped render-branch contract these tests assert
  </read_first>
  <action>
    Append a new `describe('Phase 59 — flat-ranked render branch', ...)` block to hp41-gui/src/HelpOverlay.test.tsx, using explicit Vitest + Testing-Library imports and the existing `afterEach(cleanup)` pattern (this file renders the overlay into a portal). Write `it(...)` cases asserting the render-branch contract (these will pass only once 59-03 wires the flat-ranked branch — RED until then):
    - Empty-query regression (HSMATCH-04): render HelpOverlay, leave the search input empty; assert `.help-overlay-category-heading` count is non-zero (grouped view present) — the bit-for-bit-unchanged grouped path.
    - Non-empty-query flat ranked (HSMATCH-04, HSUX-01): render, type a query into `.help-overlay-search` (use `fireEvent.change`) that matches at least one entry; assert `.help-overlay-category-heading` count is ZERO (no section headers in flat-ranked mode) and `.help-overlay-row` count is non-zero (results render). Drive the query from real loaded help data (e.g. an existing display_name substring) so the test does not depend on Phase-60 alias content.
    - Both tabs (HSMATCH-01, Pitfall 5): if the component exposes a tab control, switch to the All Functions tab and repeat the non-empty assertion so both filter memos are covered; if a single render path covers both, assert once and note it.
    - No-match empty state: type a query that matches nothing; assert `.help-overlay-empty` (or zero `.help-overlay-row`) is present.

    Do NOT modify HelpOverlay.tsx in this plan — only the test file. The tests are RED until 59-03 adds the flat-ranked branch. Never inline the component implementation in the test.
  </action>
  <acceptance_criteria>
    - HelpOverlay.test.tsx contains a new describe block referencing `.help-overlay-category-heading` and `.help-overlay-row`
    - It asserts: empty query → category headings present; non-empty matching query → zero category headings AND non-zero rows
    - The new block uses `afterEach(cleanup)` (portal hygiene) and explicit Vitest imports (globals:false)
    - `cd hp41-gui && npm test -- HelpOverlay` shows the new cases RED (flat-ranked branch not yet wired) — expected Wave-0 state
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 59" hp41-gui/src/HelpOverlay.test.tsx && grep -q "help-overlay-category-heading" hp41-gui/src/HelpOverlay.test.tsx && grep -q "help-overlay-row" hp41-gui/src/HelpOverlay.test.tsx && grep -q "afterEach" hp41-gui/src/HelpOverlay.test.tsx</automated>
  </verify>
  <done>HelpOverlay.test.tsx has a Phase-59 render-branch describe asserting grouped-on-empty / flat-on-query via the heading+row selectors; RED until 59-03 wires the branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only test files. No runtime code, no input parsing, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | Test fixtures (synthetic HelpEntry literals) | accept | Fixtures are inert literals with no secrets; severity LOW |

No `{high}` severity threats. Test-only plan; passes the block-on-high gate.
</threat_model>

<verification>
- `grep -c "#\[test\]" hp41-cli/tests/phase59_help_search.rs` >= 6
- `grep -Eiq "scoreEntry|rankedEntries" hp41-gui/src/help_data.test.ts` matches
- `grep -q "help-overlay-category-heading" hp41-gui/src/HelpOverlay.test.tsx` matches
- The three files reference scorer symbols that do not yet exist → suites are RED (expected). Wave 1 (59-02, 59-03) turns them GREEN.
</verification>

<success_criteria>
- Three RED test files created/extended, encoding tier order, fuzzy typo (Zineszins→TVM, Wurzel→SQRT), DE+EN alias resolution, empty-query invariance, and the flat-vs-grouped render branch
- Rust and TS scoring assertions mirror each other one-for-one (CLI↔GUI parity at the unit level)
- No scorer implementation added in this plan (RED Wave 0); zero new dependencies introduced (test files only)
</success_criteria>

<output>
Create `.planning/phases/59-runtime-matcher/59-01-SUMMARY.md` when done. Record the RED state of all three suites explicitly (this is expected, not a failure).
</output>
