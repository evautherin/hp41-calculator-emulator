---
phase: 59-runtime-matcher
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - hp41-cli/src/help_data.rs
  - hp41-cli/src/ui.rs
autonomous: true
requirements: [HSMATCH-01, HSMATCH-02, HSMATCH-03, HSMATCH-04, HSMATCH-05, HSUX-01, HSUX-02]
user_setup: []

must_haves:
  truths:
    - "A query matching a search_aliases value surfaces the corresponding entry in the CLI matcher"
    - "Non-empty CLI query results appear in relevance order: exact > prefix > substring > fuzzy"
    - "A misspelled CLI query (Zineszins, Wurzel) resolves to the intended entry via hand-rolled fuzzy distance"
    - "An empty CLI query leaves the overlay in the existing category-grouped view, bit-for-bit unchanged"
    - "No new Cargo dependency is added; the fuzzy distance is hand-rolled pure integer arithmetic"
  artifacts:
    - path: "hp41-cli/src/help_data.rs"
      provides: "levenshtein_bounded, tier_score, score_entry, ranked_help_entries; filter_help_rows empty guard untouched"
      contains: "fn score_entry"
    - path: "hp41-cli/src/ui.rs"
      provides: "render_help_overlay flat-ranked branch for non-empty query"
      contains: "ranked_help_entries"
  key_links:
    - from: "hp41-cli/src/ui.rs"
      to: "hp41-cli/src/help_data.rs"
      via: "ranked_help_entries(&app.help_search_query) in the non-empty branch"
      pattern: "ranked_help_entries"
    - from: "hp41-cli/src/help_data.rs score_entry"
      to: "HelpEntry.search_aliases"
      via: "iterating entry.search_aliases for alias tier scoring"
      pattern: "search_aliases"
---

<objective>
Implement the CLI (Rust) half of the Phase 59 runtime matcher: a hand-rolled bounded Levenshtein, a tiered per-field scorer over `HelpEntry` (display_name + description + category + search_aliases), and a `ranked_help_entries` accessor that returns a flat, relevance-ordered `Vec<HelpRow>` for non-empty queries. Wire `ui.rs` to render the flat ranked list when the query is non-empty while leaving the empty-query category-grouped path bit-for-bit unchanged. This turns the RED Rust tests from plan 59-01 GREEN.

Purpose: SC-1/2/3 (alias surfacing, relevance order, typo tolerance) for the CLI frontend, with SC-4 (empty-query invariance) protected by an untouched guard and a regression test.
Output: scorer + accessor in help_data.rs, a render branch in ui.rs, all 59-01 Rust tests passing, zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-runtime-matcher/59-RESEARCH.md
@.planning/phases/59-runtime-matcher/59-PATTERNS.md
@.planning/phases/59-runtime-matcher/59-VALIDATION.md
@hp41-cli/tests/phase59_help_search.rs

<interfaces>
<!-- Existing contracts the new code consumes (hp41-cli/src/help_data.rs, verified in 59-PATTERNS.md): -->
<!--   pub struct HelpEntry { op_variant, display_name, category, status, phase, key_path, -->
<!--                          description, divergences, xrom, search_aliases: Vec<String> } -->
<!--   pub struct HelpRow { pub key: String, pub op: String, pub desc: String } -->
<!--   pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry>   (six-pool chain order) -->
<!--   pub fn filter_help_rows<'a>(rows: &'a [HelpRow], query: &str) -> Vec<&'a HelpRow>  (empty-guard at top) -->
<!--   pub fn help_overlay_rows() -> Vec<HelpRow>  (category-header-interleaved, empty-query path) -->
<!-- New contracts this plan ADDS (consumed by 59-01 tests + ui.rs): -->
<!--   pub fn score_entry(entry: &HelpEntry, q: &str) -> u8     (0 = no match; q pre-lowercased) -->
<!--   pub fn ranked_help_entries(query: &str) -> Vec<HelpRow>  (score DESC, then display_name ASC; no headers) -->
<!-- Tier constants + threshold: RESEARCH.md §Scorer skeleton Rust lines 486-567 (use verbatim, tunable). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-rolled scorer in help_data.rs (levenshtein_bounded, tier_score, score_entry, ranked_help_entries)</name>
  <read_first>
    - hp41-cli/src/help_data.rs (the `HelpEntry`/`HelpRow` structs at lines 64-88 + 358-365, `help_entries_all`, `help_overlay_rows` key-projection at lines 276-315, `filter_help_rows` empty-guard at lines 331-340, the `#[cfg(test)] mod tests` block at lines 367-430 — 59-PATTERNS.md lines 23-118)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Pattern 2 Bounded Levenshtein (lines 250-312) — the exact Rust algorithm to mirror; §Scorer skeleton Rust (lines 486-567) — tier_score, score_entry, ranked_help_entries bodies; §Pitfall 7 (lines 476-480) — `.unwrap_or(...)` not `.unwrap()`
    - hp41-cli/tests/phase59_help_search.rs (the RED contract this code must satisfy — expected scores 40 exact-name, 35 exact-alias, fuzzy thresholds)
  </read_first>
  <action>
    Add to hp41-cli/src/help_data.rs (alongside `filter_help_rows`, NOT in hp41-core): the tier-score constants; a private `levenshtein_bounded(a, b, max_dist) -> usize` (classic DP, char-vector, early-exit when the row min exceeds max_dist; use `.chars().collect::<Vec<char>>()` for Unicode-correct umlaut handling; never `.unwrap()` — use `.unwrap_or(usize::MAX)` on the early-exit `.min()`); a private `tier_score(field, q, exact, prefix, substr, fuzzy) -> u8` (lowercase the field; exact==field / word-prefix via `starts_with` or any `split_whitespace().any(|w| w.starts_with(q))` / substring via `contains` / fuzzy only when `q.len() >= 2` with `max_dist = (q.len()/4).max(1)`, comparing the whole field when `field.len() <= 20` else min over split words); a `pub fn score_entry(entry: &HelpEntry, q: &str) -> u8` taking the max over name/desc/category tier-scores and the max alias tier (`entry.search_aliases.iter()...max().unwrap_or(0)`); and a `pub fn ranked_help_entries(query: &str) -> Vec<HelpRow>` that lowercases the query once, iterates `help_entries_all().filter(|e| e.status == "implemented")`, keeps score>0, stable-sorts by `(score DESC, display_name ASC)`, and projects to `HelpRow` reusing the EXACT key-display logic from `help_overlay_rows` (key_path else `XEQ "NAME"` for non-XROM keyless, else empty). Use the tier constants and threshold from RESEARCH.md §Scorer skeleton Rust verbatim; add `// Tier constants are tunable — see 59-RESEARCH.md §Open Questions Q1` above them.

    Add a provenance comment at the top of the scorer region: `// Mirrored scorer — keep byte-equivalent in behavior with hp41-gui/src/help_data.ts per Phase 59 (CLI<->GUI parity). Parity fixture lands in Phase 61.` (mirror the `prgm_display.rs` provenance-comment idiom from 59-PATTERNS.md §Mirror discipline).

    Do NOT modify the `HelpRow` struct (D-58.5). Do NOT modify the `if query.is_empty()` guard or the body of `filter_help_rows`. Do NOT add anything to hp41-core. Do NOT add any entry under `[dependencies]` in any Cargo.toml.
  </action>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase59_help_search` passes (all 59-01 Rust tests GREEN: tier_order, fuzzy_typo_zineszins, fuzzy_wurzel, alias_de_en, empty_query_returns_all_grouped, score_over_all_four_fields)
    - `just test` exits 0 (full Rust workspace, no regressions)
    - help_data.rs contains `fn levenshtein_bounded` with an early-exit `return max_dist + 1` and NO bare `.unwrap()` in the new scorer code (only `.unwrap_or`/`.unwrap_or_else`)
    - help_data.rs contains a provenance comment naming hp41-gui/src/help_data.ts as the mirror
    - The `pub struct HelpRow` definition and the `filter_help_rows` empty-query guard lines are unchanged in the diff
    - No line is added under `[dependencies]` in hp41-cli/Cargo.toml or the workspace Cargo.toml
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p hp41-cli --test phase59_help_search && grep -q "fn levenshtein_bounded" hp41-cli/src/help_data.rs && grep -q "help_data.ts" hp41-cli/src/help_data.rs</automated>
  </verify>
  <done>Scorer + ranked accessor live in help_data.rs, all 59-01 Rust tests GREEN, HelpRow + empty-guard unchanged, no new dependency, provenance comment present.</done>
</task>

<task type="auto">
  <name>Task 2: Flat-ranked render branch in ui.rs render_help_overlay</name>
  <read_first>
    - hp41-cli/src/ui.rs (the `render_help_overlay` function at lines 447-498 — the `help_overlay_rows()` + `filter_help_rows` call, the `match_count` non-header filter, the header `starts_with("===")` row styling, the existing empty/non-empty title branch — 59-PATTERNS.md lines 236-275)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Pattern 3 CLI Rendering (lines 314-336) + §Empty-Query Preservation CLI branch (lines 378-405) — the exact if/else branch shape
    - hp41-cli/src/help_data.rs (the `ranked_help_entries` signature added in Task 1 — returns owned `Vec<HelpRow>` with no headers)
  </read_first>
  <action>
    In `render_help_overlay` (ui.rs), replace the single `overlay_rows`/`filtered` acquisition with a branch on `app.help_search_query.is_empty()`. The empty branch keeps the EXISTING code path verbatim — `help_data::help_overlay_rows()` then `help_data::filter_help_rows(&overlay_rows, "")`, rendered as today (headers via the `===` arm preserved). The non-empty branch calls `help_data::ranked_help_entries(&app.help_search_query)` to get an owned `Vec<HelpRow>` (no headers) and feeds it through the SAME row-building loop — since ranked rows never start with `===`, the header arm is simply never hit. Compute `match_count` from the rendered data rows (ranked path: all rows are data rows; empty path: keep the existing non-header count). Keep the existing title branch (match count in the non-empty title).

    Normalize the two branches so the downstream render loop is shared (e.g. produce a single `Vec<HelpRow>` the loop consumes) — do not duplicate the table-building code. Do NOT change the empty-query rendering output in any way (SC-4). Do NOT touch help_data.rs in this task.
  </action>
  <acceptance_criteria>
    - ui.rs `render_help_overlay` branches on `app.help_search_query.is_empty()`; the non-empty branch calls `ranked_help_entries`
    - The empty branch still calls `help_overlay_rows()` + `filter_help_rows(..., "")` and renders identically (category headers preserved)
    - `just test` exits 0; `cargo build -p hp41-cli` succeeds clippy-clean (no `unwrap()` added)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ranked_help_entries" hp41-cli/src/ui.rs && grep -q "help_search_query.is_empty()" hp41-cli/src/ui.rs && just test</automated>
  </verify>
  <done>render_help_overlay branches empty vs non-empty; ranked flat list renders for queries, grouped view unchanged for empty; just test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → CLI help filter | A short, user-typed help-query string crosses into the scorer. The scorer matches it against static `include_str!` JSON data — no network, auth, DB, filesystem, code execution, or untrusted deserialization at query time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02-DOS | Denial of Service | `levenshtein_bounded` over a long/pathological query | mitigate | Distance is BOUNDED: early-exit `return max_dist + 1` once the row minimum exceeds `max_dist`; fuzzy is gated by `q.len() >= 2`; worst case is O(n·m) over a tiny n (≤ ~364 entries) and short m. No unbounded work, no ReDoS-class blowup (no regex backtracking — it is integer DP). |
| T-59-02-INFO | Information Disclosure | scorer output | accept | Output is only the public help-function reference already shown in the overlay; no new data exposed. Severity LOW. |

Surface is LOW (offline string scoring over static compile-time data). No `{high}` severity threats → passes the block-on-high gate.
</threat_model>

<verification>
- `cargo test -p hp41-cli --test phase59_help_search` — all 59-01 Rust scorer tests GREEN
- `just test` — full Rust workspace green, no regressions
- `git diff` confirms HelpRow struct + filter_help_rows empty-guard unchanged, no new `[dependencies]` line
- Manual spot-check (optional, per 59-VALIDATION.md Manual-Only): open the CLI `?` overlay, type "Zineszins" / "Wurzel" — intended entry is the top hit; clear query — grouped view returns unchanged
</verification>

<success_criteria>
- CLI matcher scores over display_name + description + category + search_aliases (HSMATCH-01, HSMATCH-05)
- Non-empty query results ranked exact > prefix > substring > fuzzy (HSMATCH-02)
- Misspelled "Zineszins"/"Wurzel" resolve via hand-rolled bounded Levenshtein, no new dependency (HSMATCH-03, HSUX-02)
- Empty query renders the existing category-grouped view bit-for-bit unchanged (HSMATCH-04)
- Existing `?` overlay input reused; no new view (HSUX-01)
- Provenance comment documents the GUI mirror (SC-5 / CLI↔GUI parity discipline)
</success_criteria>

<output>
Create `.planning/phases/59-runtime-matcher/59-02-SUMMARY.md` when done.
</output>
