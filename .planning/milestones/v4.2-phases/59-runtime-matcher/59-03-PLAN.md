---
phase: 59-runtime-matcher
plan: 03
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/HelpOverlay.tsx
autonomous: true
requirements: [HSMATCH-01, HSMATCH-02, HSMATCH-03, HSMATCH-04, HSMATCH-05, HSUX-01, HSUX-02]
user_setup: []

must_haves:
  truths:
    - "A query matching a search_aliases value surfaces the corresponding entry in the GUI overlay filter (both tabs)"
    - "Non-empty GUI query results appear in relevance order: exact > prefix > substring > fuzzy"
    - "A misspelled GUI query (Zineszins, Wurzel) resolves to the intended entry via the TS-mirror fuzzy distance"
    - "An empty GUI query preserves the existing category-grouped section view unchanged"
    - "No new npm dependency is added; the fuzzy distance is the hand-rolled TS mirror of the Rust scorer"
  artifacts:
    - path: "hp41-gui/src/help_data.ts"
      provides: "levenshteinBounded, tierScore, scoreEntry, rankedEntries; filterHelpEntries upgraded; empty guard untouched"
      contains: "export function scoreEntry"
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "both filter memos scored+ranked for non-empty query; flat-ranked render branch bypassing sectionGroups"
      contains: "rankedEntries"
  key_links:
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "rankedEntries(entries, q) in the non-empty branch of both filter memos"
      pattern: "rankedEntries"
    - from: "hp41-gui/src/help_data.ts scoreEntry"
      to: "HelpEntry.search_aliases"
      via: "(entry.search_aliases ?? []).map(...) alias tier scoring"
      pattern: "search_aliases"
---

<objective>
Implement the GUI (TypeScript) half of the Phase 59 runtime matcher as a byte-equivalent-in-behavior mirror of the Rust CLI scorer (59-02): a hand-rolled bounded Levenshtein, a tiered per-field scorer over `HelpEntry`, `scoreEntry`/`rankedEntries` exports, and an upgrade of both `HelpOverlay.tsx` filter memos (Keyboard Shortcuts `filtered` AND All Functions `filteredAllFn`) plus a flat-ranked render branch that bypasses `sectionGroups` when the query is non-empty. The empty-query grouped path stays bit-for-bit unchanged. This turns the RED TS tests from plan 59-01 GREEN.

Purpose: SC-1/2/3 for the GUI frontend across both tabs, SC-4 (empty-query invariance), and SC-5 (CLI↔GUI mirror discipline).
Output: scorer + ranking in help_data.ts, both filter memos + render branch upgraded in HelpOverlay.tsx, all 59-01 TS tests passing, no new npm dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-runtime-matcher/59-RESEARCH.md
@.planning/phases/59-runtime-matcher/59-PATTERNS.md
@.planning/phases/59-runtime-matcher/59-VALIDATION.md
@hp41-gui/src/help_data.test.ts
@hp41-gui/src/HelpOverlay.test.tsx

<interfaces>
<!-- Existing contracts the new code consumes (hp41-gui/src/help_data.ts + HelpOverlay.tsx, verified in 59-PATTERNS.md): -->
<!--   interface HelpEntry { ...; search_aliases?: string[] }   (optional — guard with ?? []) -->
<!--   function helpEntriesAll(): readonly HelpEntry[]   (six-pool chain, mirrors Rust help_entries_all) -->
<!--   filterHelpEntries(query): readonly HelpEntry[]    (empty-guard returns helpEntries().filter(key_path !== null)) -->
<!--   HelpOverlay.tsx: `filtered` memo (Keyboard Shortcuts, key_path !== null) line 222; -->
<!--                    `filteredAllFn` memo (All Functions) line 233; both: if (q==='') return all... -->
<!--                    `sectionGroups`/`allFnSectionGroups` group memos downstream (lines 244-291) -->
<!--   CSS selectors in tests: .help-overlay-category-heading, .help-overlay-row, .help-overlay-search, .help-overlay-empty -->
<!-- New exports this plan ADDS (consumed by 59-01 tests + HelpOverlay.tsx): -->
<!--   export function scoreEntry(entry: HelpEntry, q: string): number   (mirror of Rust score_entry; 0 = no match) -->
<!--   export function rankedEntries(entries: readonly HelpEntry[], q: string): HelpEntry[]  (score DESC, name ASC) -->
<!-- Tier constants + threshold: RESEARCH.md §Scorer skeleton TypeScript lines 570-618 (use verbatim, must equal the Rust constants). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS mirror scorer in help_data.ts (levenshteinBounded, tierScore, scoreEntry, rankedEntries) + upgrade filterHelpEntries</name>
  <read_first>
    - hp41-gui/src/help_data.ts (the `HelpEntry` interface at lines 59-94 incl. `search_aliases?: string[]`; `filterHelpEntries` empty-guard at lines 155-167; `helpEntriesAll()` — 59-PATTERNS.md lines 122-173)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Pattern 2 Bounded Levenshtein TypeScript mirror (lines 284-312) — the exact TS algorithm (`[...str]` Unicode-aware split); §Scorer skeleton TypeScript (lines 570-618) — tierScore, scoreEntry, rankedEntries bodies and the SCORE constant table
    - hp41-cli/src/help_data.rs (the Rust scorer from 59-02 — this TS code must be byte-equivalent in behavior: same tier constants, same threshold, same word-prefix/substring/fuzzy logic; read it to mirror exactly)
    - hp41-gui/src/help_data.test.ts (the RED contract this code must satisfy — expected scores 40 exact-name, 35 exact-alias)
  </read_first>
  <action>
    Add to hp41-gui/src/help_data.ts: a `levenshteinBounded(a, b, maxDist): number` (DP, `[...a]`/`[...b]` Unicode-aware char arrays, early-exit when `Math.min(...curr) > maxDist`); a `tierScore(field, q, exact, prefix, substr, fuzzy): number` (lowercase field; `f === q` exact / `f.startsWith(q) || f.split(/\s+/).some(w => w.startsWith(q))` prefix / `f.includes(q)` substring / fuzzy only when `q.length >= 2` with `maxDist = Math.max(1, Math.floor(q.length/4))`, whole field when `f.length <= 20` else min over split words); an `export function scoreEntry(entry: HelpEntry, q: string): number` taking the max over name/desc/category and the max alias tier using `(entry.search_aliases ?? []).map(...).reduce((a,b)=>Math.max(a,b),0)`; and an `export function rankedEntries(entries: readonly HelpEntry[], q: string): HelpEntry[]` mapping to `{e,s}`, filtering `s > 0`, sorting `b.s - a.s || a.e.display_name.localeCompare(b.e.display_name)`, mapping back to entries. Use the SCORE constant table from RESEARCH.md §Scorer skeleton TypeScript verbatim — the values MUST equal the Rust constants (exact name 40, alias 35, etc.). Add a `// Tunable — see 59-RESEARCH.md §Open Questions Q1` comment.

    Upgrade `filterHelpEntries` so its non-empty branch delegates to the new scorer over `helpEntriesAll()` (keep the `key_path !== null` Keyboard-Shortcuts predicate and the EXACT empty-guard `if (q === '') return helpEntries().filter(e => e.key_path !== null)` unchanged — 59-PATTERNS.md §Pitfall 6).

    Add a provenance comment: `// Mirrored scorer — keep byte-equivalent in behavior with hp41-cli/src/help_data.rs per Phase 59 (CLI<->GUI parity). Parity fixture lands in Phase 61.` (mirror the `prgm_display.rs` provenance idiom). Do NOT add any npm dependency. Do NOT touch HelpOverlay.tsx in this task.
  </action>
  <acceptance_criteria>
    - `cd hp41-gui && npm test -- help_data` passes (all 59-01 scoreEntry/rankedEntries tests GREEN)
    - help_data.ts exports `scoreEntry` and `rankedEntries`; contains `levenshteinBounded` with an early-exit and the `[...` Unicode-aware split
    - The TS SCORE constants equal the Rust tier constants (exact name 40, alias exact 35, etc.)
    - `filterHelpEntries` empty-guard line (`if (q === '') ...`) is unchanged in the diff
    - help_data.ts contains a provenance comment naming hp41-cli/src/help_data.rs as the mirror
    - `git diff hp41-gui/package.json` shows NO added dependency
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function scoreEntry" hp41-gui/src/help_data.ts && grep -q "export function rankedEntries" hp41-gui/src/help_data.ts && grep -q "levenshteinBounded" hp41-gui/src/help_data.ts && grep -q "help_data.rs" hp41-gui/src/help_data.ts && cd hp41-gui && npm test -- help_data</automated>
  </verify>
  <done>TS mirror scorer + rankedEntries exported, filterHelpEntries upgraded, 59-01 help_data tests GREEN, constants equal Rust, empty-guard unchanged, no npm dep, provenance comment present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both filter memos + flat-ranked render branch in HelpOverlay.tsx</name>
  <read_first>
    - hp41-gui/src/HelpOverlay.tsx (the `filtered` memo lines 222-230 and `filteredAllFn` memo lines 233-241 with their `if (q === '') return ...` guards; the `sectionGroups` memo lines 244-267 and `allFnSectionGroups` lines 270-291; the JSX render entry at line 335+ and the existing `activeTab` conditional — 59-PATTERNS.md lines 177-223)
    - .planning/phases/59-runtime-matcher/59-RESEARCH.md §Pattern 4 GUI Rendering (lines 338-361) + §Pitfall 4 sectionGroups bypass (lines 454-459) + §Pitfall 5 both tabs (lines 461-466)
    - hp41-gui/src/help_data.ts (the `rankedEntries` export added in Task 1)
    - hp41-gui/src/HelpOverlay.test.tsx (the render-branch assertions from 59-01: empty → category headings present; non-empty → zero headings + non-zero rows)
  </read_first>
  <action>
    In HelpOverlay.tsx, upgrade BOTH filter memos: keep each `if (q === '') return allEntries` / `return allFnEntries` empty-guard EXACTLY as-is; replace each non-empty branch (`allEntries.filter(...)` / `allFnEntries.filter(...)`) with `rankedEntries(allEntries, q)` / `rankedEntries(allFnEntries, q)` imported from `./help_data` (59-PATTERNS.md §Pitfall 5 — both tabs must be upgraded). In the render JSX, branch on `query !== ''`: when non-empty, render a FLAT list of the ranked entries using the existing `.help-overlay-row` row markup and WITHOUT emitting any `.help-overlay-category-heading` (bypass `sectionGroups`/`allFnSectionGroups` — 59-PATTERNS.md §Pitfall 4); when empty, render the existing section-grouped path unchanged (sectionGroups / allFnSectionGroups). Apply the same flat-vs-grouped branch per active tab. Preserve the existing `.help-overlay-empty` no-match state and the `.help-overlay-search` input (HSUX-01 — input reused, no new view).

    Do NOT change the empty-query grouped rendering output (SC-4). Do NOT touch help_data.ts in this task. Do NOT add an npm dependency.
  </action>
  <acceptance_criteria>
    - HelpOverlay.tsx imports `rankedEntries` from './help_data' and calls it in BOTH the `filtered` and `filteredAllFn` non-empty branches
    - The two `if (q === '') return ...` guards are unchanged in the diff
    - The render branches on `query !== ''`: non-empty → flat list (no `.help-overlay-category-heading`), empty → section-grouped path
    - `cd hp41-gui && npm test -- HelpOverlay` passes (59-01 render-branch tests GREEN: empty → headings present, non-empty → zero headings + non-zero rows)
    - `cd hp41-gui && npm test` passes in full (no regressions); `cd hp41-gui && npx tsc --noEmit` is clean
  </acceptance_criteria>
  <verify>
    <automated>grep -q "rankedEntries" hp41-gui/src/HelpOverlay.tsx && cd hp41-gui && npx tsc --noEmit && npm test -- HelpOverlay</automated>
  </verify>
  <done>Both filter memos call rankedEntries for non-empty queries; render flat-ranked (no headings) on query, grouped on empty; both tabs covered; 59-01 render tests + full Vitest GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (WKWebView) → GUI help filter | A short, user-typed help-query string crosses into the TS scorer. The scorer matches it against static `import`ed JSON data — no network, auth, DB, filesystem, code execution, or untrusted deserialization at query time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-DOS | Denial of Service | `levenshteinBounded` over a long/pathological query | mitigate | Distance is BOUNDED: early-exit when `Math.min(...curr) > maxDist`; fuzzy gated by `q.length >= 2`; worst case O(n·m) over a tiny n (≤ ~364 entries) and short m. No unbounded work, no ReDoS-class blowup (integer DP, not regex). |
| T-59-03-INFO | Information Disclosure | scorer output | accept | Output is only the public help-function reference already shown in the overlay; no new data exposed. Severity LOW. |

Surface is LOW (offline string scoring over static imported data). No `{high}` severity threats → passes the block-on-high gate.
</threat_model>

<verification>
- `cd hp41-gui && npm test -- help_data` — scoreEntry/rankedEntries tests GREEN
- `cd hp41-gui && npm test -- HelpOverlay` — render-branch tests GREEN (flat on query, grouped on empty)
- `cd hp41-gui && npm test` + `cd hp41-gui && npx tsc --noEmit` — full Vitest green, types clean
- TS SCORE constants equal the Rust constants (manual diff vs 59-02 help_data.rs); no new npm dependency
- Manual spot-check (optional, 59-VALIDATION.md Manual-Only): open GUI `?` overlay, type "Zineszins"/"Wurzel"/"compound interest" on both tabs — intended entry top; clear query — grouped view returns unchanged
</verification>

<success_criteria>
- GUI matcher scores over display_name + description + category + search_aliases in BOTH tabs (HSMATCH-01, HSMATCH-05, Pitfall 5)
- Non-empty query results ranked exact > prefix > substring > fuzzy (HSMATCH-02)
- Misspelled "Zineszins"/"Wurzel" resolve via the TS-mirror bounded Levenshtein, no new dependency (HSMATCH-03, HSUX-02)
- Empty query preserves the existing category-grouped section view unchanged (HSMATCH-04, Pitfall 4)
- Existing `?` overlay input reused; no new view (HSUX-01)
- TS scorer is a behavior-equivalent mirror of the Rust scorer; provenance comment documents the pair (SC-5)
</success_criteria>

<output>
Create `.planning/phases/59-runtime-matcher/59-03-SUMMARY.md` when done.
</output>
