---
phase: 60-alias-authoring-pipeline
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - docs/hp41cv-functions.json
  - docs/hp41-math1-functions.json
  - docs/hp41-stat1-functions.json
  - docs/hp41-time-functions.json
  - docs/hp41-advantage-functions.json
  - docs/hp41-xmem-functions.json
autonomous: false
requirements: [HSDATA-03, HSGEN-03]

must_haves:
  truths:
    - "Before the full run, claude -p auth + the chosen flags (--bare / --dangerously-skip-permissions / --output-format json) are confirmed working with a one-line probe; if --bare breaks local OAuth it is dropped (D-60.1, RESEARCH Open Question 2)"
    - "After just help-aliases, every status:\"implemented\" entry across all six pools has search_aliases.len() >= 1 (D-60.3 / HSDATA-03)"
    - "Non-implemented entries (status != \"implemented\") are left untouched — no aliases (D-60.3)"
    - "The git diff of the data commit is alias-only: no key reordering, no escaping of existing Σ/—/umlauts, no whitespace churn (D-60.4)"
    - "A human reviewed the alias diff per pool and corrected/trimmed any misleading aliases by hand before commit (D-60.5)"
    - "Both frontends still build with the larger include_str!/Vite payload; the new total pool size is noted and no pool exceeds 200 KB (D-60.7 / P-HS-05)"
    - "The aliases are committed as static, PR-reviewable data — there is NO regenerate-and-diff CI gate (HSGEN-03)"
  artifacts:
    - path: "docs/hp41cv-functions.json"
      provides: "DE+EN search_aliases for all 136 implemented hp41cv entries"
      contains: "search_aliases"
    - path: "docs/hp41-math1-functions.json"
      provides: "DE+EN search_aliases for all 45 implemented math1 entries"
      contains: "search_aliases"
    - path: "docs/hp41-stat1-functions.json"
      provides: "DE+EN search_aliases for all 26 implemented stat1 entries"
      contains: "search_aliases"
    - path: "docs/hp41-time-functions.json"
      provides: "DE+EN search_aliases for all 35 implemented time entries"
      contains: "search_aliases"
    - path: "docs/hp41-advantage-functions.json"
      provides: "DE+EN search_aliases for all 114 implemented advantage entries"
      contains: "search_aliases"
    - path: "docs/hp41-xmem-functions.json"
      provides: "DE+EN search_aliases for all 8 implemented xmem entries"
      contains: "search_aliases"
  key_links:
    - from: "just help-aliases (60-01 generator)"
      to: "docs/hp41-*-functions.json search_aliases population"
      via: "claude -p per-category batches, fill-only writeback"
      pattern: "search_aliases"
    - from: "populated pools"
      to: "hp41-cli include_str! + hp41-gui Vite static import"
      via: "cargo check -p hp41-cli && just gui-ci compile against larger payload"
      pattern: "include_str"
---

<objective>
Run the Wave-1 generator to populate `search_aliases` with DE+EN content for every `status:"implemented"` entry across all six `docs/hp41-*-functions.json` pools, human-review the alias diffs, verify both frontends still build with the larger payload, and commit the populated JSON as static, PR-reviewable data.

Purpose: This is the data deliverable of Phase 60 (D-60.3 / HSDATA-03). The previously-deferred DE/typo queries (Zineszins, Wurzel, compound interest) gain the alias data they need — but PROVING they resolve is Phase-61 verification, not this plan. Phase 60 stops at "data committed + generator runnable".

Output: Six pools with full implemented-entry alias coverage, an alias-only minimal diff (D-60.4), a recorded post-population total size (D-60.7), and a commit. The alias content is LLM-generated and non-deterministic, so this plan's "test" is the run-summary coverage report + the minimal-diff human review — NOT a regenerate-and-diff assertion (explicitly rejected, HSGEN-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-alias-authoring-pipeline/60-CONTEXT.md
@.planning/phases/60-alias-authoring-pipeline/60-RESEARCH.md
@.planning/phases/60-alias-authoring-pipeline/60-VALIDATION.md
@.planning/phases/60-alias-authoring-pipeline/60-01-SUMMARY.md

<interfaces>
<!-- The generator built in 60-01. This plan RUNS it; it does not modify the crate. -->

Generator entry point: `just help-aliases` (in-place, six pools, fill-only).
Run summary per pool (from 60-01 main.rs): "<path>: N scanned, S skipped, P populated, A aliases added"
  + an end-of-run "needs manual review" list for any op_variant the LLM omitted after retries.

Coverage target (VERIFIED counts from 60-CONTEXT.md / 60-RESEARCH.md):
  hp41cv 136 implemented, math1 45, stat1 26, time 35, advantage 114, xmem 8  -> ~364 implemented entries total.
  Exit bar: EVERY implemented entry ends with search_aliases.len() >= 1 (D-60.3).

Minimal-diff contract (D-60.4, enforced by 60-01 pool.rs): the only change vs HEAD is added search_aliases
  arrays — key order preserved, Σ/—/umlauts literal, 4-space/8-space indent, trailing newline intact.

Current total pool size ~= 131 KB; estimated post-population ~= 175 KB (+34%). Flag if any single pool > 200 KB (P-HS-05).

Worked example shape (spec, mixed DE+EN): TVM ->
  ["Zinseszins","Annuität","Tilgung","Kredit","compound interest","time value of money","loan payment","mortgage"].

Build-smoke (D-60.7): `cargo check -p hp41-cli` (six include_str! recompile; hard build blocker if any pool is
  malformed JSON) and `just gui-ci` (Vite static imports against the larger payload).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-run claude probe — confirm auth + flags before the full ~30-batch run</name>
  <what-built>
    The 60-01 generator is built and unit-tested, but its live claude -p path was never exercised
    (tests are fixture-only). Before spending ~30 LLM batches across six pools, confirm the chosen flags
    actually authenticate and return the expected envelope on THIS dev machine. This resolves RESEARCH
    Open Question 2 (does --bare break local OAuth?) and A3/A5 (flag + model-alias validity).
  </what-built>
  <how-to-verify>
    1. Run a one-line probe with the exact flags the generator uses:
       claude -p --output-format json --max-turns 1 --bare --dangerously-skip-permissions --model sonnet "reply with the JSON: {\"ok\":true}"
    2. Confirm stdout is a {"type":"result","result":"..."} envelope and the .result string parses to JSON.
       - If it errors on auth under --bare: drop --bare in scripts/help-aliases/src/claude.rs (RESEARCH Open Question 2 fallback), rebuild, re-probe.
       - If --model sonnet is unrecognized: switch to a full model name (e.g. claude-sonnet-4-5) per RESEARCH A5.
    3. (Optional smoke) run the generator on the SMALLEST pool only as a confidence check:
       cargo run --quiet --manifest-path scripts/help-aliases/Cargo.toml -- docs/hp41-xmem-functions.json
       then inspect: git diff docs/hp41-xmem-functions.json — confirm it is alias-only (no key reorder,
       umlauts literal). If clean you may keep it or git checkout it and re-run in Task 2; if it shows
       reordering/escaping, STOP — the D-60.4 writeback regressed and 60-01 needs a fix.
  </how-to-verify>
  <resume-signal>Type "probe ok" once the envelope parses and any flag fallback is applied, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the generator across all six pools + assert coverage + record size</name>
  <files>docs/hp41cv-functions.json, docs/hp41-math1-functions.json, docs/hp41-stat1-functions.json, docs/hp41-time-functions.json, docs/hp41-advantage-functions.json, docs/hp41-xmem-functions.json</files>
  <read_first>
    - 60-01-SUMMARY.md — the final generator behavior, the exact `just help-aliases` invocation, and any flag
      change applied in Task 1's probe.
    - 60-RESEARCH.md §"Bundle-size Sanity Check" (lines 433-457) — the size table + 200 KB flag threshold.
    - 60-CONTEXT.md D-60.3 (coverage exit bar) + §Specifics (run-summary fields).
  </read_first>
  <action>
    Run `just help-aliases` to populate all six pools in-place (fill-only). Capture the per-pool run summary
    (scanned / skipped / populated / aliases added + any "needs manual review" list). If any pool reports
    omitted op_variants after retries, re-run `just help-aliases` (fill-only means already-populated entries are
    skipped, so only the gaps get re-attempted); repeat until the needs-review list is empty OR hand-author the
    handful of stragglers directly into the JSON (still >=1 alias each). The exit bar is D-60.3: EVERY
    status:"implemented" entry in all six pools has search_aliases.len() >= 1.
    Then assert coverage with the verify command below (must report 0 uncovered implemented entries). Record the
    post-population byte size of each pool and the new total; if any single pool exceeds 200 KB, note it
    prominently (P-HS-05). Confirm the diff is alias-only by spot-checking `git diff` on two pools (one with
    non-ASCII: hp41cv has Σ, time has —/…) — no key reordering, no Unicode-escaping.
    Do NOT touch non-implemented entries (D-60.3). Do NOT overwrite any entry that already had aliases (fill-only,
    D-60.2 — enforced by the generator, but verify the diff shows no churn on pre-populated entries). Do NOT
    modify the generator crate in this task (that is 60-01's scope); if the writeback is wrong, STOP and flag.
  </action>
  <verify>
    <automated>python3 -c "import json,glob,sys; bad=[(f,e['op_variant']) for f in glob.glob('docs/hp41*functions.json') for e in json.load(open(f)) if e.get('status')=='implemented' and not e.get('search_aliases')]; print('uncovered implemented entries:', len(bad)); [print('  ',f,v) for f,v in bad[:20]]; sys.exit(1 if bad else 0)"</automated>
  </verify>
  <done>
    `just help-aliases` has run; the coverage assertion reports 0 uncovered implemented entries across all six
    pools (D-60.3 / HSDATA-03); per-pool and total post-population sizes are recorded with any >200 KB flagged;
    spot-checked diffs are alias-only (D-60.4); pre-populated and non-implemented entries are untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human alias-diff review (D-60.5) + frontend build-smoke (D-60.7) before commit</name>
  <what-built>
    All six pools are populated with LLM-generated DE+EN aliases and pass the automated coverage assertion.
    Before committing, a human must (a) review the alias diff so a bad alias cannot silently pull the wrong
    function to the top of search (D-60.5 — the LLM has no quality judge but the reviewer does), and (b) confirm
    both frontends still compile with the larger include_str!/Vite payload (D-60.7 / P-HS-05).
  </what-built>
  <how-to-verify>
    1. Review the alias diff per pool — `git diff docs/hp41cv-functions.json` (and the other five). Confirm:
       - The diff is ALIAS-ONLY: every changed hunk adds a `"search_aliases": [...]` array and nothing else
         moved (no key reorder, no `\uXXXX` for Σ/—/umlauts, no whitespace churn). D-60.4.
       - Spot-check the spec's worked example (a TVM entry in the advantage pool) reads sensibly in DE+EN.
       - Trim/correct any misleading alias by hand directly in the JSON (fill-only means hand edits survive
         future re-runs). Keep umlauts as real characters.
    2. Build-smoke both frontends with the larger payload:
       - `cargo check -p hp41-cli` — exits 0 (all six include_str! recompile; hard build blocker if any pool
         is malformed JSON).
       - `just gui-ci` — exits 0 (Vite static imports succeed with the larger payload).
    3. Confirm the recorded total size is reasonable (~175 KB expected) and no pool > 200 KB.
    4. Once satisfied, the executor commits the six populated pools as static data (HSGEN-03). NOTE: this is a
       data-only commit — no regenerate-and-diff CI gate is added (that is rejected; the Phase-61 schema gate is
       a separate, later deliverable).
  </how-to-verify>
  <resume-signal>Type "aliases approved" once the diff is reviewed/cleaned and both `cargo check -p hp41-cli` and `just gui-ci` are green, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
## Plan-level checks (goal-backward)

Full coverage (D-60.3 / HSDATA-03):
- The Task 2 python assertion reports 0 uncovered implemented entries across all six pools.

Minimal-diff, alias-only (D-60.4):
- `git diff docs/hp41cv-functions.json docs/hp41-time-functions.json` shows only added `search_aliases` arrays;
  no key reordering and Σ/—/umlauts remain literal (not `\uXXXX`).

Non-implemented + pre-populated untouched (D-60.3 / D-60.2):
- No diff hunk modifies an entry whose status != "implemented" or one that already carried aliases.

Frontends build with larger payload (D-60.7 / P-HS-05):
- `cargo check -p hp41-cli` exits 0; `just gui-ci` exits 0; recorded total ~175 KB; no pool > 200 KB.

Static data, no regenerate gate (HSGEN-03):
- The six pools are committed as data; `grep -c 'help-aliases-check' justfile` remains 0 (no diff gate added here or in 60-01).
</verification>

<success_criteria>
Plan 60-02 is complete when ALL are true:
1. `claude -p` auth + flags were probed before the full run; any --bare/model fallback is applied (D-60.1).
2. Every status:"implemented" entry in all six pools has search_aliases.len() >= 1 — the Task 2 assertion reports 0 uncovered (D-60.3 / HSDATA-03).
3. Non-implemented and pre-populated entries are untouched (D-60.2 / D-60.3).
4. The data diff is alias-only: preserved key order, literal non-ASCII, no whitespace churn (D-60.4).
5. A human reviewed and cleaned the alias diff per pool (D-60.5).
6. `cargo check -p hp41-cli` and `just gui-ci` are green with the larger payload; total size recorded, no pool > 200 KB (D-60.7 / P-HS-05).
7. The populated pools are committed as static, PR-reviewable data; no regenerate-and-diff gate added (HSGEN-03).
8. `hp41-core`/`CalcState`/save-file untouched; the generator crate is unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/60-alias-authoring-pipeline/60-02-SUMMARY.md` when done.
</output>
