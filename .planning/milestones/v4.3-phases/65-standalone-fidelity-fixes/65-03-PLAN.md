---
phase: 65-standalone-fidelity-fixes
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - hp41-cli/src/app.rs
autonomous: true
requirements: [DISP-02]

must_haves:
  truths:
    - "Pressing CHS while entering a mantissa (non-empty entry_buf, no 'e') toggles a leading '-' in the entry buffer in place — no flush, no dispatch, no stack lift"
    - "Pressing CHS with an empty entry buffer still dispatches Op::Chs to negate X (unchanged)"
    - "Pressing CHS during EEX entry (entry_buf contains 'e') still toggles the exponent sign (unchanged)"
  artifacts:
    - path: "hp41-cli/src/app.rs"
      provides: "CHS mantissa in-buffer toggle, gated on non-empty entry_buf without 'e', placed before the EEX-CHS block"
      contains: "entry_buf"
  key_links:
    - from: "hp41-cli/src/app.rs"
      to: "self.state.entry_buf"
      via: "in-place leading-'-' toggle on 'n' key when no 'e'"
      pattern: "entry_buf"
---

<objective>
Make CHS during mantissa entry toggle the sign of the entry buffer in place, mirroring the existing EEX-CHS in-buffer handling.

Implements DISP-02 (D-07, D-08). Light plan, independent of the other three (only file: app.rs).

Purpose: Hardware fidelity — on a real HP-41 CHS during number entry flips the mantissa sign without flushing or lifting the stack. The fix is purely additive; the empty-buffer and EEX cases are unchanged.
Output: One early-return block in app.rs before the EEX-CHS block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-standalone-fidelity-fixes/65-CONTEXT.md
@.planning/phases/65-standalone-fidelity-fixes/65-PATTERNS.md
@.planning/phases/65-standalone-fidelity-fixes/65-VALIDATION.md

<interfaces>
EEX-CHS analog (app.rs ~848-864): `if c == 'n' && self.state.entry_buf.contains('e') { /* toggle exponent sign in-place */ self.message = None; return; }`.
CHS key binding: keys.rs line ~117 `KeyCode::Char('n') => Some(Op::Chs)` — so the gate is `c == 'n'`.
DISP-02 block (insert BEFORE the EEX-CHS block): gate `c == 'n' && !entry_buf.is_empty() && !entry_buf.contains('e')` -> toggle leading '-' (`starts_with('-')` ? remove(0) : insert(0,'-')); `self.message = None; return;` (D-07).
Empty-buffer path: falls through to `call_dispatch(Op::Chs)` unchanged (D-08).
In-buffer edit shape (PATTERNS Shared Patterns): check -> mutate entry_buf -> `self.message = None;` -> `return;`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert CHS mantissa in-buffer toggle before the EEX-CHS block</name>
  <read_first>
    - hp41-cli/src/app.rs lines 820-880 (the EEX-CHS block ~848-864 and surrounding key handling, the `c == 'n'` dispatch path)
    - hp41-cli/src/keys.rs line ~117 (CHS binding `KeyCode::Char('n') => Some(Op::Chs)`) — confirm exact key/char before writing the condition (RESEARCH Open Question 2 / A4)
    - 65-PATTERNS.md §"hp41-cli/src/app.rs (DISP-02)" + §"early-return in-buffer editing pattern"
    - 65-CONTEXT.md D-07, D-08
  </read_first>
  <action>
    Immediately BEFORE the existing EEX-CHS block (`if c == 'n' && self.state.entry_buf.contains('e')`), insert the mantissa-toggle block: `if c == 'n' && !self.state.entry_buf.is_empty() && !self.state.entry_buf.contains('e') { if self.state.entry_buf.starts_with('-') { self.state.entry_buf.remove(0); } else { self.state.entry_buf.insert(0, '-'); } self.message = None; return; }`. No flush_entry_buf, no call_dispatch, no stack lift (D-07). The EEX case keeps its branch (entry_buf with 'e'); the empty-buffer case keeps falling through to `call_dispatch(Op::Chs)` (D-08). Follow the established in-buffer edit shape exactly so it does not fall through.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli && cargo build -p hp41-cli</automated>
  </verify>
  <done>Mantissa CHS toggles leading '-' in place; EEX and empty-buffer paths unchanged; CLI builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — DISP-02 CHS mantissa toggle integration test</name>
  <read_first>
    - hp41-cli/src/app.rs the EEX-CHS regression test (~line 2683 per VALIDATION) as the model
    - 65-VALIDATION.md (DISP-02 Wave 0 gap + existing EEX-CHS regression at ~2683)
  </read_first>
  <action>
    Add an integration test mirroring the EEX-CHS regression test: feed digits to build entry_buf = "123", send the CHS key ('n'), assert entry_buf == "-123" and that X / stack lift state is unchanged (no flush, no dispatch); send CHS again, assert entry_buf == "123". Add a regression assertion that with entry_buf containing "1e2", CHS still toggles the exponent sign (existing behavior), and that with an empty buffer CHS dispatches Op::Chs (X negated). `#[allow(clippy::unwrap_used)]` on the test module. Cover the edge case noted in RESEARCH: entry_buf "0" -> "-0" round-trips (display verbatim).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli</automated>
  </verify>
  <done>Test asserts "123"->"-123"->"123" with no flush/lift, EEX-CHS still toggles exponent, empty-buffer still dispatches Op::Chs; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend-only entry-buffer string edit on a local keystroke. No authentication, session, network, or new untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Tampering | entry_buf string mutation | accept | entry_buf is a bounded local input string; the toggle only inserts/removes a leading '-'. Flush-time Decimal parsing already validates the buffer; no new parse path. |

Per RESEARCH §Security Domain: NOT APPLICABLE beyond the above — no HIGH/MEDIUM threats; local calculator entry logic. No package installs.
</threat_model>

<verification>
- `cargo test -p hp41-cli` green.
- CHS mantissa toggle, EEX-CHS, and empty-buffer Op::Chs all behave per D-07/D-08.
- `just test` green at wave merge.
</verification>

<success_criteria>
Pressing CHS while entering a mantissa flips the entry-buffer sign in place with no flush/dispatch/lift (ROADMAP SC-2); EEX and empty-buffer cases unchanged.
</success_criteria>

<output>
Create `.planning/phases/65-standalone-fidelity-fixes/65-03-SUMMARY.md` when done.
</output>
