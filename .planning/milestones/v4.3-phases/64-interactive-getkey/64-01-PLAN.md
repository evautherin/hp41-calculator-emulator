---
phase: 64-interactive-getkey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/state.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/registers.rs
  - hp41-core/tests/phase_64_getkey.rs
autonomous: true
requirements: [PRGM-03]

must_haves:
  truths:
    - "When a running program executes GETKEY, run_loop breaks and pending_yield = WaitForKey; the program does not continue until resumed."
    - "After resume_program_with_key(code), the row×col code is placed in X (LiftEffect::Enable) and the program resumes at the step after GETKEY."
    - "resume_program_with_key(0) places the no-key sentinel 0 in X (cancel path); no spurious sentinel otherwise."
    - "The suspend/resume reuses the Phase 63 yield path with transient #[serde(default, skip)] fields and no new Op variant; v1.0–v3.3 save files still load."
  artifacts:
    - path: "hp41-core/src/state.rs"
      provides: "YieldKind::WaitForKey variant + getkey_captured_code: Option<u8> transient field"
      contains: "WaitForKey"
    - path: "hp41-core/src/ops/program.rs"
      provides: "Op::GetKey run_loop yield arm + resume_program_with_key entry point"
      contains: "resume_program_with_key"
    - path: "hp41-core/src/ops/registers.rs"
      provides: "op_getkey reworked to consume getkey_captured_code with last_key_code fallback"
      contains: "getkey_captured_code"
    - path: "hp41-core/tests/phase_64_getkey.rs"
      provides: "PRGM-03-a..i core test scaffolding"
      contains: "getkey_mid_run_breaks_and_sets_wait_for_key"
  key_links:
    - from: "hp41-core/src/ops/program.rs (run_loop Op::GetKey arm)"
      to: "hp41-core/src/state.rs (YieldKind::WaitForKey)"
      via: "pending_yield = Some(YieldState { kind: YieldKind::WaitForKey, .. })"
      pattern: "YieldKind::WaitForKey"
    - from: "hp41-core/src/ops/program.rs (resume_program_with_key)"
      to: "hp41-core/src/ops/registers.rs (op_getkey)"
      via: "state.getkey_captured_code set before re-entering run_loop, consumed by op_getkey.take()"
      pattern: "getkey_captured_code"
---

<objective>
Build the hp41-core foundation for interactive GETKEY: the event-driven WaitForKey yield kind, the captured-keycode transient field, the run_loop suspend arm, the resume_program_with_key re-entry point (with the load-bearing pending_interrupt* preservation), the reworked op_getkey, and the Wave 0 core test scaffolding that every later plan depends on.

Purpose: This is the single core deliverable other plans (CLI, GUI-Rust, GUI-TS, docs) build on. It closes PRGM-03's engine half and establishes the test contract (PRGM-03-a..i).
Output: WaitForKey YieldKind, getkey_captured_code field, run_loop arm, resume_program_with_key, reworked op_getkey, hp41-core/tests/phase_64_getkey.rs (9 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-interactive-getkey/64-RESEARCH.md
@.planning/phases/64-interactive-getkey/64-PATTERNS.md
@.planning/phases/64-interactive-getkey/64-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Concrete identifiers + line refs the executor needs. Read the files for current code; PATTERNS.md has the excerpt to mirror. -->

hp41-core/src/state.rs:
- `pub enum YieldKind { Pse, View, Aview }` at line 58 — add `WaitForKey` after `Aview` (line ~63).
- `pub struct YieldState { kind, text, resume_ms }` at line 79; `pub const PSE_RESUME_MS: u64 = 1000;` at line 94.
- `pub last_key_code: u8` at line 162 (row×10+col, 1-indexed, 0 = none).
- transient field block: `pending_interrupt` (498), `pending_interrupt_alarm_index` (506), `pending_interrupt_depth` (522), `pending_yield: Option<YieldState>` (530) — all `#[serde(default, skip)]`. Add `getkey_captured_code` after `pending_yield`.
- CalcState::new() initializers at lines 625–628 (`pending_yield: None`). Add `getkey_captured_code: None`.
- serde-skip test block at lines 820–829 (asserts `pending_yield` absent from JSON + None after restore). Extend with `getkey_captured_code`.

hp41-core/src/ops/program.rs:
- `use crate::state::{CalcState, YieldKind, YieldState, PSE_RESUME_MS};` at line 22.
- `pub fn resume_program(state: &mut CalcState) -> Result<(), HpError>` at line 475; it clears `pending_interrupt` (482), `pending_interrupt_alarm_index` (483), `pending_interrupt_depth` (484), `pending_yield` (485) before re-entering run_loop.
- PSE/VIEW/AVIEW yield arms in run_loop (the `Op::Pse`/`Op::View`/`Op::AView` arms) fire BEFORE the `other => execute_op(...)` catch-all. ack-after-RTN gate uses `pending_interrupt_alarm_index.is_some()` + `Some(call_stack.len()) == pending_interrupt_depth` (lines 567–570).

hp41-core/src/ops/registers.rs:
- `pub fn op_getkey(state: &mut CalcState) -> Result<(), HpError>` at lines 145–151 — reads `state.last_key_code`, `enter_number`, `apply_lift_effect(state, LiftEffect::Enable)`.

Test analog: hp41-core/tests/phase_63_yield_engine.rs (file header, imports, load_program helper, deterministic-program test pattern — copy verbatim).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WaitForKey YieldKind + getkey_captured_code field + Wave 0 core test scaffold (RED)</name>
  <read_first>
    - hp41-core/src/state.rs (YieldKind enum line 58, YieldState line 79, transient field block 490–530, CalcState::new initializers 625–628, serde-skip test block 820–829)
    - hp41-core/tests/phase_63_yield_engine.rs (full file — header, imports, load_program helper, deterministic-program test pattern to copy)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (state.rs section + phase_64_getkey.rs test scaffold section)
    - .planning/phases/64-interactive-getkey/64-VALIDATION.md (Per-Requirement Verification Map — exact test names a..i)
  </read_first>
  <behavior>
    - Test getkey_mid_run_breaks_and_sets_wait_for_key (PRGM-03-a): program [Lbl, GetKey, StoReg(0), Rtn]; after run_program the pending_yield is Some with kind WaitForKey, resume_ms 0, text "", display_override is None, reg 0 unchanged.
    - Test resume_with_key_pushes_code_to_x (PRGM-03-b): after suspend, resume_program_with_key(state, 71); pending_yield is None, is_running false, the value 71 reached X (e.g. captured into reg 1 by a StoReg(1) after GetKey).
    - Test resume_with_key_zero_pushes_sentinel (PRGM-03-c): resume_program_with_key(state, 0); X holds 0.
    - Test getkey_resume_continues_to_next_step (PRGM-03-d): the step after GetKey executes after resume.
    - Test getkey_does_not_write_display_override (PRGM-03-e): display_override stays None across suspend.
    - Test getkey_inside_alarm_handler_preserves_interrupt_state (PRGM-03-f): with pending_interrupt_alarm_index/pending_interrupt_depth set before resume, resume_program_with_key does NOT prematurely zero them (assert via a program that does not RTN so the fields are observable post-resume).
    - Test getkey_then_pse_sequential_yields (PRGM-03-g): program [Lbl, GetKey, Pse, Rtn]; first yield WaitForKey, after resume the next yield is Pse.
    - Test getkey_captured_code_cleared_on_error (PRGM-03-i): if run_loop returns Err, getkey_captured_code is None afterward.
  </behavior>
  <action>
    In hp41-core/src/state.rs: add variant `WaitForKey` to `pub enum YieldKind` after `Aview` (doc-comment: Op::GetKey, event-driven, resume_ms unused/0, resumed via resume_program_with_key). Add field `getkey_captured_code: Option<u8>` to `CalcState` after `pending_yield`, annotated `#[serde(default, skip)]` (doc-comment: keycode captured during a WaitForKey yield; set by resume_program_with_key before re-entering run_loop; consumed via .take() by op_getkey; transient, never persisted). Add `getkey_captured_code: None` to `CalcState::new()` after `pending_yield: None`. Extend the serde-skip test block (820–829) with two assertions mirroring pending_yield: JSON must NOT contain "getkey_captured_code", and restored.getkey_captured_code.is_none().
    Create hp41-core/tests/phase_64_getkey.rs: copy the header/`#![allow(clippy::unwrap_used)]`/imports/`load_program` helper from phase_63_yield_engine.rs; add `use hp41_core::ops::program::{resume_program_with_key, run_program};` and `use hp41_core::state::YieldKind;`. Write the 9 tests named exactly per VALIDATION.md (a..i; h is the existing synthetic test — do not duplicate it here, only a,b,c,d,e,f,g,i go in this file). The tests reference resume_program_with_key + op_getkey behavior that Tasks 2/3 implement — this is the RED step (compile may fail until Task 2/3 land; that is expected). Use deterministic programs and surface the captured keycode as register data (no real keyboard input) per Claude's-discretion D-05.
  </action>
  <verify>
    <automated>cargo build -p hp41-core 2>&1 | rtk grep -q "WaitForKey" && echo "variant referenced" ; rtk grep -c "WaitForKey" hp41-core/src/state.rs</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/state.rs` contains enum variant `WaitForKey` in `YieldKind` and field `getkey_captured_code: Option<u8>` annotated `#[serde(default, skip)]`.
    - `CalcState::new()` initializes `getkey_captured_code: None`.
    - The serde-skip test block asserts `getkey_captured_code` is absent from serialized JSON and is None after deserialization (save-file backward compat: v1.0–v3.3 saves load — covered by the existing migrate/skip assertions plus the new None assertion).
    - `hp41-core/tests/phase_64_getkey.rs` exists with the 7 named tests a,b,c,d,e,f,g and the i test, matching the names in VALIDATION.md exactly.
    - `rtk grep -c "WaitForKey" hp41-core/src/state.rs` returns ≥ 1.
  </acceptance_criteria>
  <done>WaitForKey variant + getkey_captured_code field + CalcState::new init + serde-skip assertions land; phase_64_getkey.rs scaffold exists with the named tests (RED — they will go GREEN after Task 2/3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_loop GetKey yield arm + resume_program_with_key entry point (GREEN, engine)</name>
  <read_first>
    - hp41-core/src/ops/program.rs (resume_program line 475–490; the PSE/VIEW/AVIEW yield arms in run_loop; the ack-after-RTN gate 567–570; use line 22)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (program.rs section: Op::GetKey arm + resume_program_with_key with the two divergences)
    - .planning/phases/64-interactive-getkey/64-RESEARCH.md (Pitfall 1, Pitfall 2, Pitfall 6; D-06 Resolution nested-yield section)
    - hp41-core/tests/phase_64_getkey.rs (the tests from Task 1 that this satisfies)
  </read_first>
  <action>
    In hp41-core/src/ops/program.rs add the `Op::GetKey` arm to run_loop, placed AFTER the `Op::AView` arm and BEFORE the `other => execute_op(...)` catch-all (mirror the PSE arm): set `state.pending_yield = Some(YieldState { kind: YieldKind::WaitForKey, text: String::new(), resume_ms: 0 })` then `break`. Do NOT call apply_lift_effect here (op_getkey applies LiftEffect::Enable on resume). Do NOT touch display_override (D-03).
    Add `pub fn resume_program_with_key(state: &mut CalcState, keycode: u8) -> Result<(), HpError>` after `resume_program`. It MUST: (1) return `Err(HpError::InvalidOp)` if `state.pc >= state.program.len()`; (2) clone the program; (3) set `state.getkey_captured_code = Some(keycode)` BEFORE clearing pending_yield; (4) set `state.pending_yield = None`; (5) NOT clear `pending_interrupt_alarm_index` or `pending_interrupt_depth` (load-bearing divergence from resume_program — preserves alarm-handler context for ack-after-RTN, Pitfall 1); (6) set `state.is_running = true`; (7) capture `let result = run_loop(state, &program);` (never `?` directly — Pitfall 2); (8) set `state.is_running = false`; (9) set `state.getkey_captured_code = None` on BOTH Ok and Err paths before returning (Pitfall 6); (10) return `result`.
  </action>
  <verify>
    <automated>just test-core --test phase_64_getkey -- getkey_mid_run_breaks_and_sets_wait_for_key resume_with_key_pushes_code_to_x resume_with_key_zero_pushes_sentinel getkey_then_pse_sequential_yields getkey_captured_code_cleared_on_error</automated>
  </verify>
  <acceptance_criteria>
    - `program.rs` run_loop has an `Op::GetKey` arm setting `pending_yield = Some(YieldState { kind: YieldKind::WaitForKey, text: String::new(), resume_ms: 0 })` then `break`, placed before the `other =>` catch-all.
    - `resume_program_with_key` does NOT assign `pending_interrupt_alarm_index` or `pending_interrupt_depth` (verify by absence — these two identifiers must not be assigned inside the new fn body).
    - `resume_program_with_key` sets `getkey_captured_code = None` after run_loop on both Ok and Err (Pitfall 6).
    - `just test-core --test phase_64_getkey -- getkey_mid_run_breaks_and_sets_wait_for_key resume_with_key_pushes_code_to_x resume_with_key_zero_pushes_sentinel getkey_then_pse_sequential_yields getkey_captured_code_cleared_on_error` exits 0.
    - No `println!`/`eprintln!` added to hp41-core; no `.unwrap()` (clippy::unwrap_used denied).
  </acceptance_criteria>
  <done>GETKEY suspends via WaitForKey; resume_program_with_key threads the keycode, preserves pending_interrupt*, and cleans getkey_captured_code on all paths; PRGM-03-a/b/c/g/i tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rework op_getkey (interactive path + last_key_code fallback) + close PRGM-03-d/e/f/h</name>
  <read_first>
    - hp41-core/src/ops/registers.rs (op_getkey lines 145–151)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (registers.rs section — reworked op_getkey, keep LiftEffect verbatim)
    - hp41-core/tests/synthetic_tests.rs (test_getkey_pushes_last_key_code — PRGM-03-h, must stay green)
    - hp41-core/tests/phase_64_getkey.rs (the d/e/f tests)
  </read_first>
  <action>
    In hp41-core/src/ops/registers.rs rework `op_getkey`: source `code` from `state.getkey_captured_code.take()` if Some (interactive program path), else fall back to `HpNum::from(state.last_key_code as i32)` (non-program interactive dispatch, preserves PRGM-03-h backward compat). Keep the existing `state.stack.lift_enabled = true; enter_number(state, code); apply_lift_effect(state, LiftEffect::Enable); Ok(())` tail verbatim. Update the doc-comment to note the interactive vs fallback paths.
    Confirm PRGM-03-d/e/f pass with the engine from Task 2: getkey_resume_continues_to_next_step, getkey_does_not_write_display_override, getkey_inside_alarm_handler_preserves_interrupt_state.
  </action>
  <verify>
    <automated>just test-core --test phase_64_getkey && just test-core --test synthetic_tests -- test_getkey_pushes_last_key_code</automated>
  </verify>
  <acceptance_criteria>
    - `op_getkey` consumes `state.getkey_captured_code.take()` when Some and falls back to `state.last_key_code` when None.
    - `op_getkey` keeps `apply_lift_effect(state, LiftEffect::Enable)` and never writes `display_override` (D-03).
    - `just test-core --test phase_64_getkey` exits 0 (all of PRGM-03-a..g,i green).
    - `just test-core --test synthetic_tests -- test_getkey_pushes_last_key_code` exits 0 (PRGM-03-h backward compat preserved).
  </acceptance_criteria>
  <done>op_getkey interactive-with-fallback lands; full phase_64_getkey suite green; PRGM-03-h still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → core (resume_program_with_key) | A `u8` keycode crosses from the untrusted-input side (key event) into core state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | resume_program_with_key keycode | accept | Key code is a `u8` (0–255); HP-41 row×col codes are 11–85; the value is only pushed to X as a number — no indexing/dispatch on it, so out-of-range is harmless and bounded by the type. No bounds check needed. |
| T-64-02 | Tampering | double resume (resume_program_with_key called twice) | mitigate | `getkey_captured_code.take()` consumes the field once; a second call sees None and falls back to `last_key_code` (harmless), and `pc >= program.len()` returns Err(InvalidOp). |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `just test-core --test phase_64_getkey` exits 0 (PRGM-03-a,b,c,d,e,f,g,i).
- `just test-core --test synthetic_tests -- test_getkey_pushes_last_key_code` exits 0 (PRGM-03-h).
- `just ci` green (lint + test + coverage ≥ 95% lines / ≥ 93% regions + license-audit) at wave merge.
- No new runtime deps; no println!/eprintln! in hp41-core; no .unwrap() in core.
</verification>

<success_criteria>
- WaitForKey YieldKind + getkey_captured_code transient field exist with `#[serde(default, skip)]`.
- run_loop breaks on Op::GetKey; resume_program_with_key threads the keycode to X (LiftEffect::Enable) and resumes at the next step.
- resume_program_with_key preserves pending_interrupt_alarm_index/depth (alarm-handler context) and cleans getkey_captured_code on all paths.
- No new Op variant; save-file backward compat preserved.
</success_criteria>

<output>
Create `.planning/phases/64-interactive-getkey/64-01-SUMMARY.md` when done.
</output>
