---
phase: 64-interactive-getkey
plan: 05
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - docs/hp41cv-divergences.md
autonomous: true
requirements: [PRGM-03]

must_haves:
  truths:
    - "The divergence documentation records interactive GETKEY (FGAP-04 / SYNT-06) as implemented in v4.3."
    - "The documentation records that CONTEXT.md D-02's '84' for R/S was a documentation error, corrected to 31 (R/S's true row 3 col 1 position); 84 is the ENTER key's position."
  artifacts:
    - path: "docs/hp41cv-divergences.md"
      provides: "GETKEY interactive-implemented divergence entry (FGAP-04/SYNT-06 -> implemented v4.3) + the R/S 84->31 correction note"
      contains: "GETKEY"
  key_links: []
---

<objective>
Update the divergence documentation to reflect that interactive GETKEY (FGAP-04, subsumes FGAP-08; SYNT-06) is now implemented in v4.3, mirroring how Phase 63 flipped the time-divergences §D-40-04 entry. Record the R/S keycode correction (D-02's "84" was a documentation error; the true value is 31, R/S's row 3 col 1 position; 84 is ENTER's position).

Purpose: Keeps the divergence record truthful after the behavior ships (CONTEXT.md specifics + orchestrator-locked R/S=31 decision). Doc-only; no code.
Output: a GETKEY implemented-divergence entry in docs/hp41cv-divergences.md with the 84->31 correction note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-interactive-getkey/64-CONTEXT.md
@.planning/phases/64-interactive-getkey/64-RESEARCH.md
@.planning/phases/64-interactive-getkey/64-PATTERNS.md
@CLAUDE.md

<interfaces>
docs/hp41cv-divergences.md:
- Existing entries use D-CV-NN ids (D-CV-01 lenient mnemonic aliases, D-CV-02 iOS ALPHA keys-only, ...). Add a new D-CV-05 (next free id) GETKEY entry.
- Header notes module-pac divergences live in docs/hp41-{math1,stat1,time,advantage,xmem}-divergences.md — GETKEY is a CX OS built-in, not a module pac, so the CV doc is the correct home (PATTERNS.md maps this file as the target).

Analog flip pattern (read for tone/structure):
- docs/hp41-time-divergences.md — the §D-40-04 entry flipped to "implemented" (Phase 63 / Plan 63-05) at lines ~265 and the changelog line ~383 ("§D-40-04 flipped to implemented — Phase 63 / Plan 63-05").

Source facts to encode:
- FGAP-04: interactive GETKEY = pause execution, wait for next key, push row×col code to X, resume. Subsumes FGAP-08 (no-key sentinel 0 returned only on cancel). SYNT-06 deferral history closed.
- R/S during GETKEY returns 31 (row 3 col 1), NOT 84. 84 is ENTER's position (row 8 col 4). D-02's literal "84" was a documentation error corrected to 31 (RESEARCH Pitfall 4 / Open Question 1 / orchestrator lock).
- No emulator-side timeout: GETKEY waits indefinitely until a key or cancel (GETKEYX deferred — D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GETKEY implemented-divergence entry + R/S 84->31 correction</name>
  <read_first>
    - docs/hp41cv-divergences.md (full file — existing D-CV-NN entry structure + header)
    - docs/hp41-time-divergences.md (the §D-40-04 flip entry ~line 265 + changelog line ~383 — tone/structure to mirror)
    - .planning/phases/64-interactive-getkey/64-CONTEXT.md (D-02; the "flip FGAP-04/SYNT-06 to implemented v4.3" specific idea)
    - .planning/phases/64-interactive-getkey/64-RESEARCH.md (Pitfall 4, Open Question 1, D-01 Resolution — R/S=31, no timeout)
  </read_first>
  <action>
    In docs/hp41cv-divergences.md add a new divergence entry (next free D-CV id, e.g. D-CV-05) titled for interactive GETKEY. Mirror the structure of the existing D-CV entries and the time-doc D-40-04 flip: state the Authentic behavior (HP-41 GETKEY pauses, waits for a key, returns its row×col code in X, resumes; no-key sentinel 0 only on cancel/idle), and the Emulator behavior marked Implemented (v4.3): WaitForKey yield + resume_program_with_key; R/S returns 31; Esc/cancel pushes sentinel 0; waits indefinitely (GETKEYX deferred — no emulator timeout). Include an explicit note: CONTEXT.md D-02's "R/S captured as keycode 84" was a documentation error — the correct value is 31 (R/S = row 3, col 1); 84 is the ENTER key's position (row 8, col 4); corrected per Phase 64 research + orchestrator decision. Reference FGAP-04 (subsumes FGAP-08) and SYNT-06 as now closed/implemented. Add a changelog/status line at the bottom mirroring the time-doc pattern (date + "GETKEY interactive implemented — Phase 64 / Plan 64-05"). English only (project convention).
  </action>
  <verify>
    <automated>rtk grep -c "GETKEY" docs/hp41cv-divergences.md && rtk grep -c "31" docs/hp41cv-divergences.md && rtk grep -ci "implemented" docs/hp41cv-divergences.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/hp41cv-divergences.md contains a new entry describing interactive GETKEY as Implemented (v4.3), referencing FGAP-04 (subsumes FGAP-08) and SYNT-06.
    - The entry states R/S returns 31 (not 84) and explicitly records the D-02 "84" -> 31 correction with the reason (84 = ENTER's position; 31 = R/S row 3 col 1).
    - The entry notes the cancel-path sentinel 0 and the indefinite wait (GETKEYX deferred, no emulator timeout).
    - A dated changelog/status line mirrors the time-doc flip pattern.
    - Prose is English only.
  </acceptance_criteria>
  <done>Divergence doc records interactive GETKEY as implemented v4.3 with the R/S 84->31 correction; FGAP-04/SYNT-06 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary crossed — this plan modifies documentation only (no code, no input handling, no IPC).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-DOC-01 | Information Disclosure | docs/hp41cv-divergences.md | accept | Public-facing behavioral documentation; no secrets; no PII; no executable content. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- docs/hp41cv-divergences.md contains the GETKEY implemented entry + the 84->31 correction.
- English-only prose (project convention / MEMORY.md feedback_project_doc_language).
</verification>

<success_criteria>
- Interactive GETKEY (FGAP-04/SYNT-06) recorded as implemented v4.3.
- R/S=31 correction (D-02 "84" documentation error) recorded with rationale.
</success_criteria>

<output>
Create `.planning/phases/64-interactive-getkey/64-05-SUMMARY.md` when done.
</output>
