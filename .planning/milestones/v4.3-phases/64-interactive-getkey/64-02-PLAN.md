---
phase: 64-interactive-getkey
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - hp41-cli/src/app.rs
autonomous: true
requirements: [PRGM-03]

must_haves:
  truths:
    - "While a CLI program is suspended on GETKEY, the TUI keeps redrawing (no freeze) and waits for the next physical keypress."
    - "Pressing an HP-41 key during the suspend resumes the program with that key's row×col code; R/S resumes with code 31."
    - "Pressing Esc during the suspend cancels GETKEY: pushes sentinel 0 and resumes (program ends/returns). Ctrl+C still hard-quits the app."
    - "Keys with no HP-41 equivalent (F5/F7/F8/unknown) are ignored — the wait continues."
  artifacts:
    - path: "hp41-cli/src/app.rs"
      provides: "WaitForKey branch in drain_pending_yields (non-blocking poll + redraw + resume_program_with_key) and a GETKEY guard in handle_key"
      contains: "WaitForKey"
  key_links:
    - from: "hp41-cli/src/app.rs (drain_pending_yields WaitForKey branch)"
      to: "hp41-core resume_program_with_key"
      via: "keys::keycode_to_hp41_code(key.code) → hp41_core::ops::program::resume_program_with_key"
      pattern: "resume_program_with_key"
---

<objective>
Wire the CLI to capture the next keypress during a GETKEY (WaitForKey) suspend and resume the program with its HP-41 row×col code, with Esc=cancel→sentinel 0 and Ctrl+C=quit, using a non-blocking poll+redraw loop so the TUI never freezes.

Purpose: Delivers the CLI half of PRGM-03's frontend (D-04). Reuses the existing keycode_to_hp41_code map and the Phase 63 drain_pending_yields loop.
Output: WaitForKey branch in drain_pending_yields + a defensive GETKEY guard in handle_key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-interactive-getkey/64-RESEARCH.md
@.planning/phases/64-interactive-getkey/64-PATTERNS.md
@.planning/phases/64-interactive-getkey/64-01-SUMMARY.md
@CLAUDE.md

<interfaces>
hp41-cli/src/app.rs:
- imports (line 11): `use crossterm::event::{self, Event, KeyCode, KeyEvent, KeyEventKind, KeyModifiers};` (already present).
- `drain_pending_yields` (lines 332–369): `while self.state.pending_yield.is_some()` loop; currently reads text/resume_ms then `std::thread::sleep` (timer) then `resume_program`. The WaitForKey branch must run BEFORE the sleep path.
- `handle_key` (lines 382–396): `KeyEventKind::Press` filter then the `keycode_to_hp41_code` + `last_key_code` update block.
- redraw call shape: `terminal.draw(|frame| self.draw(frame))?;` (pattern used in the main run() loop).
- self.exit, self.message fields; self.drain_and_show_print_output(None).

hp41-cli/src/keys.rs (read-only):
- `pub fn keycode_to_hp41_code(code: crossterm::event::KeyCode) -> Option<u8>` — returns None for F5/F7/F8/unknown; R/S (F5 / R-S key) maps to 31; Enter→84.

hp41-core (from Plan 01):
- `hp41_core::ops::program::resume_program_with_key(&mut self.state, code: u8) -> Result<(), HpError>`.
- `hp41_core::state::YieldKind::WaitForKey`.
</interfaces>

LOCKED DECISIONS (orchestrator):
- R/S during GETKEY = code 31 (via keycode_to_hp41_code, no special-casing). 84 is ENTER's position.
- Esc during GETKEY = cancel → resume_program_with_key(0). Ctrl+C still hard-quits.
- Use crossterm::event::poll(16ms) + terminal.draw(...) per iteration — never blocking event::read() (Pitfall 5).
- Skip ALPHA/shift/modal routing during the suspend; route raw keycode_to_hp41_code directly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WaitForKey branch in drain_pending_yields (poll + redraw + resume; Esc=0, Ctrl+C=quit)</name>
  <read_first>
    - hp41-cli/src/app.rs (drain_pending_yields 332–369; the main run() poll+draw loop; imports line 11; self.exit/self.message/self.drain_and_show_print_output)
    - hp41-cli/src/keys.rs (keycode_to_hp41_code 452–528 — caller contract: None = ignore)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (app.rs section — drain_pending_yields WaitForKey extension + Crossterm Event Pattern)
    - .planning/phases/64-interactive-getkey/64-RESEARCH.md (CLI Capture Path; Pitfall 5; Open Question 2 — Esc resolution)
  </read_first>
  <action>
    In hp41-cli/src/app.rs, inside the `drain_pending_yields` `while self.state.pending_yield.is_some()` loop, add a branch that fires when the current `pending_yield`'s kind is `hp41_core::state::YieldKind::WaitForKey`, BEFORE the existing timer/sleep path. The branch enters an inner `loop`: each iteration calls `terminal.draw(|frame| self.draw(frame))?;` then `if crossterm::event::poll(Duration::from_millis(16))?` reads the next `Event::Key(key)`, skipping non-`KeyEventKind::Press`. Routing within the loop: (a) `key.code == KeyCode::Esc` → call `hp41_core::ops::program::resume_program_with_key(&mut self.state, 0)`, set self.message per Ok/Err, drain_and_show_print_output(None), `break`; (b) `KeyCode::Char('c')` with CONTROL modifier → `self.exit = true; return Ok(());`; (c) `keys::keycode_to_hp41_code(key.code)` Some(code) AND not CONTROL-modified → resume_program_with_key(&mut self.state, code), set message per Ok/Err, drain_and_show_print_output(None), `break`; (d) otherwise (None code, or modified) → continue the inner loop (ignore — hardware faithful). After the inner loop breaks, `continue;` the outer while so a chained next yield (e.g. PSE) is handled by the existing path. Do NOT add ALPHA/shift/modal handling — raw keycode only.
  </action>
  <verify>
    <automated>cargo build -p hp41-cli 2>&1 | rtk grep -qi "error" && echo BUILD_FAIL || echo BUILD_OK ; rtk grep -c "WaitForKey\|resume_program_with_key" hp41-cli/src/app.rs</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-cli/src/app.rs` has a `YieldKind::WaitForKey` branch in `drain_pending_yields` that uses `crossterm::event::poll(Duration::from_millis(16))` and `terminal.draw(...)` per iteration (no blocking `event::read()` without a poll guard, no `std::thread::sleep` in this branch — Pitfall 5).
    - Esc routes to `resume_program_with_key(&mut self.state, 0)` (cancel → sentinel 0); Ctrl+C sets `self.exit = true` and returns.
    - HP-41 keys route via `keys::keycode_to_hp41_code` to `resume_program_with_key`; `None` codes and CONTROL-modified keys are ignored (inner loop continues).
    - `cargo build -p hp41-cli` succeeds.
    - `rtk grep -c "resume_program_with_key" hp41-cli/src/app.rs` returns ≥ 1.
  </acceptance_criteria>
  <done>CLI captures a keypress during GETKEY, resumes with its code (R/S=31 via the existing map), Esc cancels with sentinel 0, Ctrl+C quits, TUI redraws during the wait.</done>
</task>

<task type="auto">
  <name>Task 2: Defensive GETKEY guard in handle_key + CLI acceptance assertion (R/S=31, Esc=0)</name>
  <read_first>
    - hp41-cli/src/app.rs (handle_key 382–396; the new WaitForKey branch from Task 1; existing CLI test module / integration tests in hp41-cli)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (app.rs handle_key guard section)
    - .planning/phases/64-interactive-getkey/64-VALIDATION.md (Manual-Only Verifications — CLI R/S=31 / Esc=0 expectations to encode as a unit assertion where possible)
  </read_first>
  <action>
    Add a guard at the top of `handle_key` (immediately after the `KeyEventKind::Press` filter, BEFORE the `last_key_code` update block): if `self.state.pending_yield` is `Some(YieldState { kind: YieldKind::WaitForKey, .. })`, return early without processing the key here (drain_pending_yields owns the event during the wait; this prevents double-processing of a stale event). Add a CLI-side test (in hp41-cli's test module or a new integration test) that asserts the contract at the core boundary the CLI relies on: keycode_to_hp41_code for the R/S key returns 31 (NOT 84), and that resume_program_with_key(&mut state, 0) on a GETKEY-suspended program ends/returns with X = 0. If a pure unit test of the live TUI key loop is not feasible, assert the two pieces (keys::keycode_to_hp41_code(R/S keycode) == Some(31)) and the core resume(0) X==0) directly — record the live-keyboard end-to-end check as the manual verification from VALIDATION.md.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli 2>&1 | rtk grep -E "test result|error" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `handle_key` early-returns when `pending_yield.kind == YieldKind::WaitForKey` (no double-processing).
    - A CLI test asserts `keys::keycode_to_hp41_code` for the R/S key == `Some(31)` (locked decision: R/S=31, not 84).
    - A CLI/core test asserts `resume_program_with_key(&mut state, 0)` on a GETKEY-suspended program leaves X = 0 and the program ends/returns (Esc-cancel acceptance).
    - `cargo test -p hp41-cli` exits 0.
  </acceptance_criteria>
  <done>handle_key guarded against double-processing; R/S=31 and Esc-cancel→0 encoded as automated assertions; live-keyboard path recorded as the VALIDATION.md manual check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal keyboard → CLI → core | crossterm key events cross into resume_program_with_key as a bounded u8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-CLI-01 | Tampering | keycode_to_hp41_code output | accept | Returns `Option<u8>`; None is ignored, Some is bounded 11–85; pushed to X only. No dispatch on the value. |
| T-64-CLI-02 | DoS | WaitForKey poll loop | mitigate | `event::poll(16ms)` + redraw per iteration avoids freeze (Pitfall 5); Esc/Ctrl+C always provide an exit. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `cargo build -p hp41-cli` and `cargo test -p hp41-cli` succeed.
- `just ci` green at wave merge (lint + test + coverage + license-audit).
- Manual (VALIDATION.md): run a GETKEY program in hp41-cli; press a key → X = its row×col code; R/S → 31; Esc → sentinel 0 + program ends; Ctrl+C → app quits.
</verification>

<success_criteria>
- CLI suspends on GETKEY without freezing, captures the next physical keypress, resumes with the correct HP-41 code.
- R/S resumes with 31; Esc cancels with sentinel 0 + resume; Ctrl+C quits; unmapped keys ignored.
</success_criteria>

<output>
Create `.planning/phases/64-interactive-getkey/64-02-SUMMARY.md` when done.
</output>
