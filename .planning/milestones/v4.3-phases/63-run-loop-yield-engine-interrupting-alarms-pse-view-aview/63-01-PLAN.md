---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/state.rs
  - hp41-core/src/ops/time/alarm.rs
  - hp41-core/tests/phase_63_interrupting_alarms.rs
  - hp41-core/tests/phase_63_yield_engine.rs
autonomous: true
requirements: [ALARM-02, ALARM-03, PRGM-01, PRGM-02]

must_haves:
  truths:
    - "An interrupting control alarm coming due while a program runs sets pending_interrupt (not the dead-end deferred stub) when no solver/modal is active and no interrupt is already pending."
    - "An interrupting control alarm coming due while idle (is_running == false) is queued as alarm:xeq:{label} on event_buffer (criterion 3 routing)."
    - "An interrupting alarm with a solver/modal active is demoted to alarm:xeq:{label} on event_buffer (D-10)."
    - "Non-interrupting and message alarms route exactly as before (DNT-05 unchanged)."
    - "New transient fields (pending_interrupt, pending_interrupt_alarm_index, pending_interrupt_depth, pending_yield) default cleanly and never appear in serialized JSON (v4.2 autosave backward compat)."
  artifacts:
    - path: "hp41-core/src/state.rs"
      provides: "pending_interrupt, pending_interrupt_alarm_index, pending_interrupt_depth, pending_yield fields + YieldKind/YieldState types"
      contains: "pending_yield"
    - path: "hp41-core/src/ops/time/alarm.rs"
      provides: "dispatch_alarm_event interrupting-arm routing + D-10 demotion guard"
      contains: "pending_interrupt"
    - path: "hp41-core/tests/phase_63_interrupting_alarms.rs"
      provides: "Wave 0 test scaffold: deterministic alarm helpers + 12 alarm/edge scenarios"
      contains: "make_past_due_interrupting_alarm"
    - path: "hp41-core/tests/phase_63_yield_engine.rs"
      provides: "Wave 0 test scaffold: PSE/VIEW/AVIEW yield scenarios"
      contains: "pending_yield"
  key_links:
    - from: "hp41-core/src/ops/time/alarm.rs"
      to: "state.pending_interrupt"
      via: "dispatch_alarm_event interrupting arm"
      pattern: "pending_interrupt = Some"
    - from: "hp41-core/src/state.rs"
      to: "serde transient discipline"
      via: "#[serde(default, skip)] on all four new fields"
      pattern: "serde\\(default, skip\\)"
---

<objective>
Phase A of the run-loop yield engine: author the two Wave-0 test files FIRST (so the TDD tasks have a live RED signal), then add the four transient control fields to `CalcState`, define the typed yield channel, and rewire `dispatch_alarm_event`'s interrupting-alarm arm to set `pending_interrupt` (running) / queue `alarm:xeq` (idle or demoted) instead of pushing the dead-end `"alarm:interrupting:deferred"` stub.

Purpose: Establishes the state contract every downstream plan consumes (run_loop interrupt in 63-02, frontends in 63-03/63-04/63-06). Isolating `state.rs` + `alarm.rs` here keeps `program.rs` untouched so plan 63-02 owns it exclusively (no shared-index race per reference_parallel_wave_index_contention). The synthetic-frame injection-depth disambiguator field (`pending_interrupt_depth`) is declared HERE in 63-01 (wave 1, owns state.rs) so that 63-02 (wave 2, program.rs-only) can READ/WRITE it without touching state.rs and breaking wave isolation.
Output: Two new test files (scaffolds + all 16 scenarios; run_loop-dependent assertions land RED until 63-02), four new `CalcState` fields, `YieldKind`/`YieldState` types, rewired alarm routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-PATTERNS.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-VALIDATION.md
@hp41-core/src/state.rs
@hp41-core/src/ops/time/alarm.rs

<interfaces>
<!-- Anchors verified against develop 2026-06-06. Use directly; no exploration needed. -->

state.rs:
- `call_stack: Vec<usize>` (line 79), `is_running: bool` (82)
- `event_buffer: Vec<String>` (152, `#[serde(default, skip)]` at 151)
- `display_override: Option<String>` (144, `#[serde(default, skip)]` at 143) — LEAVE UNTOUCHED (D-04)
- solver/modal state: `modal_program` (219), `integ_state` (233), `solve_state` (239), `difeq_state` (246)
- `CalcState::new()` init block ~466–537 (existing transient inits near 489–490)

alarm.rs:
- `dispatch_alarm_event` (493–512). Message arm: 495–498 (`alarm:message:{msg}` + print_buffer). Non-interrupting arm: 508 (`alarm:xeq:{label}`). Interrupting arm currently pushes `"alarm:interrupting:deferred"` at 506 — THIS is the dead-end to replace.
- `check_alarms` (448): sets `past_due = true` (453), calls `dispatch_alarm_event` (455).
- `acknowledge_alarm` (474): repeat reschedule / remove (consumed by 63-02, do not call here).
- `parse_alarm_type` (~78): `>>` => interrupting:true. DO NOT TOUCH (D-11 / ADR v4.3-003).

Test analogs (copy conventions verbatim):
- `hp41-core/tests/time_alarm_latency.rs` lines 1–19 (file-level `#![allow(clippy::unwrap_used)]` + Free42-oracle header) and 58–68 (deterministic `CalcState` fixture: `trigger_unix = 0`, `time_offset_secs = 0`).
- `hp41-core/tests/program_tests.rs` for the `state.program = vec![Op::Lbl…]` load idiom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Wave-0 test scaffolds FIRST (both new test files)</name>
  <files>hp41-core/tests/phase_63_interrupting_alarms.rs, hp41-core/tests/phase_63_yield_engine.rs</files>
  <action>
    Authored FIRST so Tasks 2–3's `tdd="true"` work has a live RED signal (the two files ARE the RED target). Create both files with file-level `#![allow(clippy::unwrap_used)]` (copy header convention from time_alarm_latency.rs:1–19).
    Add deterministic helpers: `make_past_due_interrupting_alarm(label: &str)` building an alarm with `trigger_unix = 0`, `interrupting: true`, `past_due` not yet set; a program-load helper setting `state.program = vec![Op::Lbl(...), ...]`; `state.time_offset_secs = 0`. Never call `SystemTime::now()` in assertions; never `sleep`.
    In `phase_63_interrupting_alarms.rs`, author ALL 12 scenarios from 63-VALIDATION.md (assertions per the RESEARCH § Validation Architecture tables): `interrupting_alarm_halts_running_program_and_resumes`, `interrupt_preserves_stack_x_y_z_t_and_lift_state`, `interrupt_blocked_when_call_stack_at_4_level_cap`, `interrupt_nesting_blocked_when_already_in_alarm_program`, `interrupting_alarm_fires_when_no_program_running`, `non_interrupting_alarm_still_fires_as_event_not_inline`, `message_alarm_still_fires_to_event_buffer_not_executed`, `interrupt_demoted_when_solver_or_modal_active`, `missing_handler_label_surfaces_event`, `pending_interrupt_cleared_on_resume_after_stop`, `v4_3_interrupt_backward_compat`, `repeating_interrupting_alarm_reschedules_after_handler`.
    In `phase_63_yield_engine.rs`, author the 4 yield scenarios: `pse_mid_run_breaks_and_records_resume_ms`, `pse_resume_continues_to_next_step`, `view_mid_run_captures_formatted_register_into_yield`, `aview_mid_run_captures_alpha_into_yield`. Assert `pending_yield.resume_ms == 1000` (PSE_RESUME_MS) as DATA; assert `pending_yield.text` equals the expected `format_hpnum`/alpha string; assert `display_override` is NOT written by the yield path.
    These scenarios reference the four new `CalcState` fields and `YieldState`/`YieldKind` from Task 2 — author the test BODIES now referencing those names; they will not COMPILE until Task 2 adds the fields/types (expected — Task 2's verify is the first green-compile gate). The scenarios that depend on the run_loop interrupt/yield arms (built in 63-02) WILL fail RED even after compile — that is expected; 63-02 turns them green. Mark run_loop-dependent tests clearly (a comment `// GREEN after 63-02`) so the executor of 63-02 knows the target set. Do NOT `#[ignore]` them — they must run RED so 63-02 has a live signal.
  </action>
  <verify>
    <automated>test -f hp41-core/tests/phase_63_interrupting_alarms.rs && test -f hp41-core/tests/phase_63_yield_engine.rs && grep -q "make_past_due_interrupting_alarm" hp41-core/tests/phase_63_interrupting_alarms.rs && grep -q "resume_ms" hp41-core/tests/phase_63_yield_engine.rs && echo SCAFFOLD_OK</automated>
  </verify>
  <done>Both test files exist with file-level allow header, deterministic helpers, all 16 scenarios authored (bodies reference the 63-01 fields/types). Compile is deferred to Task 2 (the fields don't exist yet); no `#[ignore]`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transient yield/interrupt fields and types to CalcState</name>
  <files>hp41-core/src/state.rs</files>
  <behavior>
    - A fresh `CalcState::new()` has `pending_interrupt == None`, `pending_interrupt_alarm_index == None`, `pending_interrupt_depth == None`, `pending_yield == None`.
    - Serializing a `CalcState` produces JSON that does NOT contain the substrings "pending_interrupt", "pending_interrupt_alarm_index", "pending_interrupt_depth", or "pending_yield" (transient discipline; extend the existing serde-skip assertion test in state.rs).
    - Deserializing a v4.2-era autosave JSON (lacking these fields) succeeds and all four fields default to None.
    - Both Wave-0 test files (authored in Task 1) now COMPILE (the field/type names they reference resolve).
  </behavior>
  <action>
    Define the yield channel types near the top of state.rs (alongside existing public types, NOT inside an impl):
    `pub enum YieldKind { Pse, View, Aview }` and `pub struct YieldState { pub kind: YieldKind, pub text: String, pub resume_ms: u64 }`. Derive `Debug, Clone, PartialEq` on both, plus `Serialize, Deserialize` to match the crate's struct conventions (the fields carrying them are `skip`, so the derives only satisfy any nested-clone needs — mirror how peer transient structs in state.rs derive).
    Add a `pub const PSE_RESUME_MS: u64 = 1000;` constant (single source of truth for PSE/VIEW/AVIEW duration per Claude's-discretion D-04; all three reuse 1000 ms per RESEARCH recommendation).
    Add FOUR fields to `CalcState`, each immediately preceded by `#[serde(default, skip)]` (the transient pattern at lines 108/110, 143/144, 151/152 — NOT the `rand_seed`/`adv_tvm_state` no-skip exception):
    `pending_interrupt: Option<String>` (D-12), `pending_interrupt_alarm_index: Option<usize>` (D-06a), `pending_interrupt_depth: Option<usize>` (synthetic-frame injection-depth disambiguator — declared here in 63-01 so 63-02's program.rs-only plan can read/write it without touching state.rs and breaking wave-2 file isolation; see revision note), `pending_yield: Option<YieldState>` (D-04). Use the doc comments from 63-PATTERNS.md; document `pending_interrupt_depth` as "call_stack depth captured at synthetic-frame injection; 63-02's Op::Rtn ack gate compares against it to ack ONLY when the handler (which may XEQ deeper) returns to the injection depth."
    Initialize all four to `None` in `CalcState::new()` alongside the existing `display_override: None,` / `event_buffer: Vec::new(),` inits (~489–490).
    Review `migrate_after_load()` — confirm no active migration is needed (fields default cleanly via serde; I-03). Do not add migration code.
    Extend the existing serde-skip assertion test (the one asserting `!json.contains("modal_prompt")` / `integ_state`) with `assert!(!json.contains("pending_interrupt"))`, `assert!(!json.contains("pending_interrupt_depth"))`, `assert!(!json.contains("pending_yield"))`.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test phase_63_interrupting_alarms --test phase_63_yield_engine --no-run 2>&1 | tail -10 && just test-core 2>&1 | tail -20 && cargo +1.88 clippy -p hp41-core --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Four transient fields exist with `#[serde(default, skip)]`, init to None in new(), YieldKind/YieldState/PSE_RESUME_MS defined; both Wave-0 test files compile (`--no-run` succeeds); serde-skip assertion passes; clippy -D warnings clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire dispatch_alarm_event interrupting arm (set pending_interrupt / demote / idle-queue)</name>
  <files>hp41-core/src/ops/time/alarm.rs</files>
  <behavior>
    - `interrupting_alarm_fires_when_no_program_running`: with `is_running == false`, dispatching a past-due interrupting alarm pushes `alarm:xeq:{label}` to `event_buffer`; `pending_interrupt` stays None (criterion 3).
    - Running + no solver/modal + no pending: sets `pending_interrupt = Some(label)` and `pending_interrupt_alarm_index = Some(idx)`; does NOT push `alarm:interrupting:deferred`.
    - `interrupt_demoted_when_solver_or_modal_active` (D-10): with any of `integ_state`/`solve_state`/`difeq_state` Some OR `modal_program` Some → pushes `alarm:xeq:{label}`; `pending_interrupt` stays None.
    - Already-pending (`pending_interrupt.is_some()`): second interrupting alarm demoted to `alarm:xeq:{label}` (nesting guard).
    - `non_interrupting_alarm_still_fires_as_event_not_inline` / `message_alarm_*`: routing at the non-interrupting (508) and message (495–498) arms is byte-for-byte unchanged (DNT-05).
  </behavior>
  <action>
    In `dispatch_alarm_event` (alarm.rs:503–509), replace the interrupting-arm body that pushes `"alarm:interrupting:deferred"` (506). New logic, gated in this order:
    1. If `state.is_running == true` AND `state.pending_interrupt.is_none()` AND no solver/modal active (all of `integ_state`/`solve_state`/`difeq_state` are None AND `modal_program.is_none()`) → `state.pending_interrupt = Some(label.clone()); state.pending_interrupt_alarm_index = Some(index);` (the alarm's index — `check_alarms` already iterates with the index; thread it through to `dispatch_alarm_event` if not already a parameter — extend the signature and update its single caller at alarm.rs:455). Do NOT set `pending_interrupt_depth` here (that is captured at the run_loop injection point in 63-02).
    2. Else (idle D-13, already-pending nesting, or solver/modal demotion D-10) → `state.event_buffer.push(format!("alarm:xeq:{label}"));` (same string the non-interrupting arm and idle path use).
    DO NOT touch the message arm (495–498) or the non-interrupting `alarm:xeq` arm (508) — DNT-05. DO NOT call `acknowledge_alarm` here (that is 63-02's run_loop ack point, D-06). DO NOT touch `parse_alarm_type` (D-11).
    If `dispatch_alarm_event` does not currently receive the alarm index, add an `index: usize` parameter and pass `i` from the `check_alarms` enumerate loop (455).
    SECOND CALLER — `op_almnow` (alarm.rs:364) also calls `dispatch_alarm_event(state, &alarm)` and is program-executable (`Op::TimeAlmnow` → runs with `is_running == true` mid-program), so the new `index` parameter breaks its compile too. Pass its local `idx` (alarm.rs:351) at line 364. BUT `op_almnow` immediately self-acks (reschedule/remove at 366–372) right after dispatch — if the new running+interrupting arm sets `pending_interrupt`/`pending_interrupt_alarm_index` here, you get a double-ack + stale-index hazard (op_almnow removes the alarm at `idx`, then 63-02's run_loop RTN-ack (D-06) re-acks a now-invalid index). ALMNOW is an explicit synchronous "fire the alarm now" command that owns its own ack and is NOT one of the 16 validation scenarios. Resolve by ensuring the `op_almnow` call site does NOT take the deferred `pending_interrupt` path: add a `defer_to_run_loop: bool` parameter to `dispatch_alarm_event` (`check_alarms` passes `true`, `op_almnow` passes `false`); the running+interrupting arm sets `pending_interrupt` ONLY when `defer_to_run_loop == true`, otherwise it falls through to the `alarm:xeq:{label}` event push so `op_almnow`'s own ack stays authoritative. Add a one-line comment at the op_almnow call site recording why it passes `false`. (Keep this minimal — it is a compile-correctness fix, not a behavior change for the common interactive ALMNOW path where `is_running == false` already routes to the idle branch.)
  </action>
  <verify>
    <automated>just test-core --test phase_63_interrupting_alarms 2>&1 | tail -25</automated>
  </verify>
  <done>Interrupting arm sets pending_interrupt (running, clear) / queues alarm:xeq (idle, demoted, nested); demotion + idle + nesting scenarios green; DNT-05 scenarios (non-interrupting, message) green; no `alarm:interrupting:deferred` string remains in the codebase (`grep -rn 'alarm:interrupting' hp41-core/src/` empty).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alarm scheduler → run_loop | A past-due alarm injects a label to execute; an attacker-authored alarm label is user-trusted (single-user emulator) but must not corrupt solver state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | dispatch_alarm_event solver/modal demotion | mitigate | D-10 guard demotes to event_buffer when integ/solve/difeq/modal active — synthetic frame never corrupts solver re-entrancy state. |
| T-63-02 | DoS | repeated interrupting alarms | accept | nesting guard (pending_interrupt.is_some() → demote) caps inline depth at 1; deeper re-interrupt out of scope (ALARM-03). |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | No new deps (I-06); `git diff HEAD -- hp41-core/Cargo.toml` must be empty. No install tasks → no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `just test-core` green; Phase-A scenarios GREEN, run_loop scenarios RED (expected).
- `grep -rn 'alarm:interrupting' hp41-core/src/` → empty (dead-end stub removed).
- `grep -rn 'unwrap()' hp41-core/src/state.rs hp41-core/src/ops/time/alarm.rs | grep -v '#\[allow'` → empty.
- `git diff HEAD -- hp41-core/Cargo.toml hp41-core/src/ops/math1/` → empty (no new deps, math1 freeze).
- All four new fields serde-skipped (assertion test passes).
</verification>

<success_criteria>
- pending_interrupt / pending_interrupt_alarm_index / pending_interrupt_depth / pending_yield fields exist, transient, default None.
- YieldKind/YieldState/PSE_RESUME_MS defined.
- dispatch_alarm_event routes interrupting alarms to pending_interrupt (running) / alarm:xeq (idle, demoted, nested); DNT-05 paths unchanged.
- Both test files authored with all 16 scenarios; Phase-A subset GREEN, run_loop subset RED.
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
