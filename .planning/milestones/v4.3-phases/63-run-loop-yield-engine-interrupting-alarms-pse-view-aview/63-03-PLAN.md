---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 03
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - hp41-cli/src/app.rs
autonomous: true
requirements: [ALARM-02, ALARM-03, PRGM-01, PRGM-02]

must_haves:
  truths:
    - "After a running program yields (PSE/VIEW/AVIEW), the CLI renders pending_yield.text on the display, waits ~1 s, then resumes — the value is visible before the next step (criteria 4–5, CLI side of D-25.6)."
    - "A missing alarm-handler label surfaces on the CLI status line (alarm:missing:{label} → self.message)."
    - "Interrupting-alarm handler output (PRX/PRA/PRSTK) drains via drain_and_show_print_output after the program call returns (I-07)."
  artifacts:
    - path: "hp41-cli/src/app.rs"
      provides: "yield render+sleep+resume loop; alarm:missing arm; print drain at the run call site"
      contains: "pending_yield"
  key_links:
    - from: "R/S keystroke handler (run_program call site)"
      to: "resume_program"
      via: "while pending_yield.is_some(): render text, sleep resume_ms, resume_program"
      pattern: "resume_program"
    - from: "drain_event_buffer"
      to: "self.message"
      via: "alarm:missing arm"
      pattern: "alarm:missing"
---

<objective>
CLI frontend wiring for the yield engine (D-25.6 parity, CLI half): a render → sleep → resume loop so PSE/VIEW/AVIEW pauses are visible in the TUI, plus a `drain_event_buffer` arm surfacing `alarm:missing:{label}` on the status line, and ensuring alarm-handler print output drains. Lands in the SAME phase as the GUI half (63-04).

Purpose: Criteria 4–5 require the pause to be visible in the CLI; criterion 2's missing-label surfacing (D-08) needs a CLI status line. The running interrupting-alarm case needs NO CLI branch — it executes entirely inside `run_program`'s `run_loop`.
Output: CLI renders yields and surfaces missing labels; print discipline preserved.

Touches `hp41-cli/src/app.rs` ONLY — disjoint from 63-04 (GUI) and 63-05 (docs); safe to run parallel in Wave 3 (pathspec-scope commits to app.rs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-PATTERNS.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-02-SUMMARY.md
@hp41-cli/src/app.rs

<interfaces>
<!-- Anchors verified against develop 2026-06-06. CLI may sleep (no-sleep rule is hp41-core only). -->

app.rs:
- `drain_event_buffer` (1778): `alarm:message:` → self.message (1781); `alarm:xeq:` → run_program + drain_pending_card_op + drain_and_show_print_output (1783–1795); `alarm:interrupting:…` SILENTLY IGNORED at 1797 (comment to remove). Add `alarm:missing:{label}` arm here.
- `drain_and_show_print_output` (1865) — I-07 print discipline.
- `call_dispatch_and_drain` (~1936–1957) — wraps a dispatch + drains print/card/event.
- The R/S keystroke handler calls `run_program`; after it returns with `state.pending_yield` set, the CLI must render + sleep + resume.

From 63-02 (state.rs / program.rs):
- `state.pending_yield: Option<YieldState{kind, text, resume_ms}>` — text is the display string; resume_ms == 1000.
- `resume_program(state)` continues at the already-advanced pc and clears pending_yield/pending_interrupt on entry.
- Running interrupting alarms execute inside run_program (no CLI branch). Idle/demoted arrive as `alarm:xeq:` (existing 1783 branch — unchanged).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Yield render + sleep + resume loop at the run call site</name>
  <files>hp41-cli/src/app.rs</files>
  <action>
    At the R/S keystroke handler's `run_program` call site (and any other site that runs a program to completion), after the call returns, add a loop: `while self.state.pending_yield.is_some() { … }`:
    - Take/clone the `YieldState`; render `yield.text` on the main display line (set the same display field the CLI uses for the program-running display — match how the existing display path renders during a stopped program; the carried text is the sole source per D-04, do NOT read display_override).
    - Force a redraw so the value is visible, then `std::thread::sleep(Duration::from_millis(yield.resume_ms))` (CLI may sleep — the no-sleep rule is hp41-core-only).
    - Call `resume_program(&mut self.state)` (which clears pending_yield on entry and continues at the next step). Drain print/card/event output after the resume (reuse drain_and_show_print_output + the existing card/event drains).
    - Loop until pending_yield is None AND the program has stopped (is_running false or program ended). This handles consecutive yields (e.g. PSE then VIEW).
    Ensure the interrupting-alarm handler's staged print output (PRX/PRA/PRSTK) drains via `drain_and_show_print_output()` after the enclosing run_program returns — the 1791–1792 pattern already does this for `alarm:xeq`; confirm the R/S run-call site also drains (I-07).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli 2>&1 | tail -15 && cargo +1.88 clippy -p hp41-cli --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>CLI loops render→sleep(resume_ms)→resume until the program stops; consecutive yields handled; print output drains; cli crate builds + clippy -D warnings clean.</done>
</task>

<task type="auto">
  <name>Task 2: Surface alarm:missing on the CLI status line + remove interrupting silent-ignore</name>
  <files>hp41-cli/src/app.rs</files>
  <action>
    In `drain_event_buffer` (1778), add an arm: `else if line.starts_with("alarm:missing:") { let label = line.trim_start_matches("alarm:missing:"); self.message = Some(format!("Alarm XEQ {label}: label not found")); }` (D-08 — status-line surface; matches CLAUDE.md D-07 never-silently-swallow).
    Remove or replace the `alarm:interrupting:…` "silently ignored" comment/arm at 1797 — interrupting alarms no longer emit that string (63-01 removed the deferred stub; the running case is handled inline in run_loop, the idle/demoted case arrives as `alarm:xeq:`). If any `alarm:interrupting:` prefix arm remains, delete it (dead code) and update the doc comment at the top of drain_event_buffer (1163-style listing) to reflect `alarm:missing:` instead.
    Add a targeted CLI test (in `hp41-cli/src/app.rs` test module or a sibling test) asserting that draining an `alarm:missing:FOO` event sets `self.message` to the not-found string (hp41-cli is NOT coverage-gated, so this targeted test is the backstop per VALIDATION § Manual-Only).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli 2>&1 | grep -E "test result|missing" | tail -8</automated>
  </verify>
  <done>alarm:missing arm sets self.message; no `alarm:interrupting:` silent-ignore remains; targeted CLI test green; `grep -n "silently ignored" hp41-cli/src/app.rs` empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core run result → CLI render | The CLI renders core-carried yield text and event strings; no untrusted external input — single-user TUI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-06 | DoS | yield resume loop | accept | bounded by program termination (MAX_STEPS in core); each yield sleeps a fixed resume_ms then resumes — no unbounded spin. |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | No new deps; `git diff HEAD -- hp41-cli/Cargo.toml` empty. |
</threat_model>

<verification>
- `cargo test -p hp41-cli` green; targeted alarm:missing test green.
- `cargo +1.88 clippy -p hp41-cli --all-targets -- -D warnings` clean (P-43-06 divergence).
- `grep -n "silently ignored\|alarm:interrupting" hp41-cli/src/app.rs` → empty.
- `just ci` green (CLI is part of the root workspace test).
</verification>

<success_criteria>
- CLI renders yield text, pauses ~1 s, resumes (criteria 4–5, CLI half).
- Missing label surfaces on the status line (criterion 2 / D-08).
- Print discipline preserved (I-07).
- D-25.6 parity: CLI half landed alongside the GUI half (63-04).
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-03-SUMMARY.md` when done.
</output>
