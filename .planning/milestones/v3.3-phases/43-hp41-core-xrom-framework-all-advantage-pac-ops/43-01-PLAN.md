---
phase: 43-hp41-core-xrom-framework-all-advantage-pac-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/advantage/mod.rs
  - hp41-core/src/ops/advantage/modal.rs
  - hp41-core/src/ops/advantage/tvm.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/ops/math1/modal.rs
  - hp41-core/src/ops/math1/complex.rs
  - hp41-core/src/state.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - scripts/check-free42-contamination.sh
autonomous: true
requirements:
  - ADV-FW-01
  - ADV-FW-02
  - ADV-FW-03
  - ADV-FW-04
  - ADV-FW-05
must_haves:
  truths:
    - "ADV_MATH_A (XROM 22) and ADV_MATH_B (XROM 24) constants exist with correct hardware IDs"
    - "xrom_resolve() resolves Advantage Pac mnemonics via bit-3 and bit-4 arms"
    - "v3.2 save files auto-migrate to xrom_modules 0b0001_1111 via migrate_after_load()"
    - "adv_matrices Vec<AdvMatrix> defaults to empty on fresh and migrated state"
    - "ModalProgram::Advantage(AdvantageStep) variant dispatches prompts correctly"
    - "complex_atan2 is callable from ops/advantage/ via pub(crate) visibility"
    - "Free42 contamination guard covers advantage/ directory"
  artifacts:
    - path: "hp41-core/src/ops/advantage/mod.rs"
      provides: "Module hub with AdvMatrix struct, constants, sub-module declarations"
    - path: "hp41-core/src/ops/advantage/modal.rs"
      provides: "AdvantageStep enum + current_prompt() + requires_alpha_label() + submit_step()"
    - path: "hp41-core/src/ops/advantage/tvm.rs"
      provides: "TvmState struct (persistent, no serde skip)"
  key_links:
    - from: "hp41-core/src/ops/math1/xrom.rs"
      to: "hp41-core/src/ops/advantage/mod.rs"
      via: "ADV_MATH_A.ops and ADV_MATH_B.ops reference Op::Adv* variants"
      pattern: "ADV_MATH_A|ADV_MATH_B"
    - from: "hp41-core/src/ops/math1/modal.rs"
      to: "hp41-core/src/ops/advantage/modal.rs"
      via: "ModalProgram::Advantage delegates to advantage::modal functions"
      pattern: "Advantage.*AdvantageStep"
    - from: "hp41-core/src/state.rs"
      to: "hp41-core/src/ops/advantage/mod.rs"
      via: "CalcState fields reference AdvMatrix and TvmState types"
      pattern: "adv_matrices|adv_tvm_state"
---

<objective>
XROM framework registration, CalcState field additions, AdvMatrix/TvmState data model, ModalProgram variant, complex_atan2 visibility promotion, Free42 contamination guard extension, and the full ~117-entry Op enum variant block with ALL dispatch() arms pointing to stub functions. This plan establishes the entire Advantage Pac skeleton so subsequent plans can implement algorithms without touching framework files.

Purpose: Every subsequent plan in Phase 43 depends on the XROM resolver arms, CalcState fields, Op variants, and dispatch wiring being in place. Concentrating all framework + wiring + stubs here prevents file-ownership conflicts across parallel waves.

Output: Compilable hp41-core with all ~117 Op::Adv* variants registered, stub-dispatched, AdvMatrix/TvmState types on CalcState, and XROM bit-3/bit-4 resolution working end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-CONTEXT.md
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-RESEARCH.md
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hp41-core/src/ops/math1/xrom.rs:
- `pub struct XromModule { pub id: u8, pub name: &'static str, pub ops: &'static [(&'static str, Op)] }`
- `pub const MATH_1: XromModule` (id=7), `pub const STAT_1: XromModule` (id=2), `pub const TIME_MODULE: XromModule` (id=26)
- `pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>` — bit-0/1/2 arms exist; add bit-3/bit-4
- `fn math1_resolve(name: &str) -> Option<Op>`, `fn stat1_resolve(...)`, `fn time_resolve(...)` — per-module resolvers

From hp41-core/src/ops/math1/modal.rs:
- `pub enum ModalProgram { Matrix(..), Solve(..), Poly(..), Integ(..), Difeq(..), Four(..), Trans(..), Stat1(..), Time(..) }` — add `Advantage(..)`
- `pub fn current_prompt(&self) -> Option<String>` — exhaustive match, add Advantage arm
- `pub fn requires_alpha_label(&self) -> bool` — exhaustive match, add Advantage arm

From hp41-core/src/state.rs:
- `pub struct CalcState { ... }` — add adv_matrices, adv_matrix_i, adv_matrix_j, adv_tvm_state, adv_current_matrix, adv_froot_state, adv_fintg_state, adv_fsolve_state, adv_fdifeq_state
- `fn default_xrom_modules() -> u8` — change from `0b0000_0111` to `0b0001_1111`
- `pub fn migrate_after_load(&mut self)` — add bit-3 and bit-4 OR-assign guards
- `pub fn new() -> Self` — add initializers for all new fields

From hp41-core/src/ops/math1/complex.rs line 52:
- `pub(super) fn complex_atan2(im: HpNum, re: HpNum) -> HpNum` — promote to `pub(crate)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create advantage/ module hub, data types, modal.rs, and tvm.rs with stub op functions for all ~117 ops</name>
  <files>
    hp41-core/src/ops/advantage/mod.rs,
    hp41-core/src/ops/advantage/modal.rs,
    hp41-core/src/ops/advantage/tvm.rs,
    hp41-core/src/ops/advantage/conv.rs,
    hp41-core/src/ops/advantage/matrix_ops.rs,
    hp41-core/src/ops/advantage/matrix_linalg.rs,
    hp41-core/src/ops/advantage/matrix_complex.rs,
    hp41-core/src/ops/advantage/complex_ext.rs,
    hp41-core/src/ops/advantage/solvers.rs,
    hp41-core/src/ops/advantage/poly.rs,
    hp41-core/src/ops/advantage/matrix_workflow.rs,
    hp41-core/src/ops/advantage/curve_fit.rs,
    hp41-core/src/ops/advantage/vectors.rs
  </files>
  <read_first>
    hp41-core/src/ops/stat1/mod.rs (lines 1-110 — module hub pattern, OM constants header, sub-module declarations),
    hp41-core/src/ops/time/modal.rs (lines 1-120 — AdvantageStep enum pattern, current_prompt/requires_alpha_label/submit_step free functions),
    hp41-core/src/ops/stat1/modal.rs (lines 1-60 — Stat1Step enum for structural reference),
    hp41-core/src/ops/math1/integ.rs (lines 55-85 — IntegState struct pattern for solver transient state),
    hp41-core/src/ops/math1/solve.rs (lines 70-105 — SolveState struct pattern),
    hp41-core/src/ops/time/clock.rs (lines 1-40 — Free42 disclaim header + imports + op function signature pattern)
  </read_first>
  <action>
    Create the `hp41-core/src/ops/advantage/` directory with 13 files. Every file carries the verbatim Free42 disclaim header: "Algorithm independently re-derived from HP Advantage Pac Owner's Manual 00041-90482 (1985); Free42 source consulted only as sanity-check oracle, not copied."

    **mod.rs**: Module hub following stat1/mod.rs pattern. Declare all 12 sub-modules (`pub mod modal/conv/matrix_ops/matrix_linalg/matrix_complex/complex_ext/solvers/poly/matrix_workflow/curve_fit/vectors/tvm`). Define and export: `AdvMatrix` struct (fields: `name: String`, `rows: u8`, `cols: u8`, `is_complex: bool`, `data: Vec<HpNum>`; derive Debug, Clone, Serialize, Deserialize, Default); `ADV_MATRIX_MAX_ROWS: u8 = 255`; `ADV_MATRIX_MAX_COLS: u8 = 255`; `ADV_WORD_MASK: u64 = 0x0000_000F_FFFF_FFFF` (36-bit mask per D-43.9). Re-export `AdvantageStep` from modal and `TvmState` from tvm. Re-export all `pub fn op_adv_*` functions from sub-modules.

    **modal.rs**: Define `AdvantageStep` enum with variants for every modal workflow: `TvmN`, `TvmI`, `TvmPv`, `TvmPmt`, `TvmFv`, `TvmBeginEnd`, `MatrixNamePrompt`, `MatrixDimRowPrompt`, `MatrixDimColPrompt`, `MeditElementPrompt(u8,u8)`, `CmeditElementPrompt(u8,u8)`, `VeComponentPrompt(u8)`, `MatrxOperationChoice`, `MtrNamePrompt`, `FdifeqOrderPrompt`, `FdifeqFunctionNamePrompt`. Derive Debug, Clone, PartialEq. Implement three free functions: `current_prompt(step: &AdvantageStep) -> Option<String>`, `requires_alpha_label(step: &AdvantageStep) -> bool`, `submit_step(state: &mut CalcState, step: AdvantageStep) -> Result<(), HpError>`. All matches exhaustive, no `_ =>` catch-all. submit_step stubs return `Err(HpError::InvalidOp)` for now (Plans 43-06/43-08 implement).

    **tvm.rs**: Define `TvmState` struct (fields: `n: HpNum`, `i: HpNum`, `pv: HpNum`, `pmt: HpNum`, `fv: HpNum`, `begin_mode: bool`; derive Debug, Clone, Serialize, Deserialize, Default per D-43.11). Define `TVM_MAX_ITERATIONS: u8 = 100` and `TVM_CONVERGENCE_THRESHOLD: f64 = 1e-9` constants. Create stub pub functions: `op_adv_tvm`, `op_adv_tvm_n`, `op_adv_tvm_pv`, `op_adv_tvm_pmt`, `op_adv_tvm_fv`, `op_adv_tvm_star_i` — all return `Err(HpError::InvalidOp)` as stubs.

    **solvers.rs**: Define solver state structs: `FrootState` (degree, coeffs: Vec of f64, roots_found: Vec of (f64,f64), current_index), `AdvFintegState` (user_label, a, b as HpNum, tolerance as f64, iteration as u8, result as HpNum), `AdvFsolveState` (user_label, x1, x2, fx1, fx2 as HpNum, iteration as u8), `AdvFdifeqState` (user_label, x, y, y_prime as HpNum, order as u8, step_size as HpNum, iteration as u32). All derive Debug, Clone, Default. Create stub pub functions: `op_adv_fsolve`, `op_adv_fintg`, `op_adv_fdifeq`, `op_adv_froot`, `op_adv_fsolve_run_loop`, `op_adv_fintg_run_loop`, `op_adv_fdifeq_run_loop` — all stubs.

    **conv.rs**: Create 12 stub pub functions: `op_adv_binin`, `op_adv_binview`, `op_adv_octin`, `op_adv_cvtview`, `op_adv_hexin`, `op_adv_hexview`, `op_adv_not`, `op_adv_and`, `op_adv_or`, `op_adv_xor`, `op_adv_rotxy`, `op_adv_bit_test` — all stubs returning `Err(HpError::InvalidOp)`.

    **matrix_ops.rs**: Create stub pub functions for all element access + lifecycle + reduction ops: `op_adv_i_plus`, `op_adv_i_minus`, `op_adv_j_plus`, `op_adv_j_minus`, `op_adv_mr`, `op_adv_ms`, `op_adv_mrij`, `op_adv_msij`, `op_adv_mrc_plus`, `op_adv_mrc_minus`, `op_adv_mrr_plus`, `op_adv_mrr_minus`, `op_adv_msc_plus`, `op_adv_msr_plus`, `op_adv_mrijr`, `op_adv_msijr`, `op_adv_matdim`, `op_adv_dim_query`, `op_adv_mname_query`, `op_adv_mswap`, `op_adv_r_exchange_r`, `op_adv_r_gt_r_query`, `op_adv_piv`, `op_adv_mp`, `op_adv_sum`, `op_adv_sumab`, `op_adv_max`, `op_adv_min`, `op_adv_maxab`, `op_adv_rmaxab`, `op_adv_fnrm`, `op_adv_rnrm`, `op_adv_rsum` — all stubs.

    **matrix_linalg.rs**: Stubs for: `op_adv_mdet`, `op_adv_minv`, `op_adv_msys`, `op_adv_m_mul_m`, `op_adv_mat_plus`, `op_adv_mat_minus`, `op_adv_mat_scalar_mul`, `op_adv_mat_scalar_div`, `op_adv_trnps`, `op_adv_mmove`.

    **matrix_complex.rs**: Stubs for: `op_adv_c_exchange_c`, `op_adv_cmaxab`, `op_adv_cnrm`, `op_adv_csum`, `op_adv_yc_plus_c`.

    **complex_ext.rs**: Stubs for: `op_adv_magz`, `op_adv_exp_z`, `op_adv_ln_z`, `op_adv_z_pow_n`, `op_adv_z_pow_1n`, `op_adv_sin_z`, `op_adv_cos_z`, `op_adv_tan_z`, `op_adv_a_pow_z`, `op_adv_log_z`, `op_adv_z_pow_1w`, `op_adv_z_pow_w`, `op_adv_aip`, `op_adv_c_plus`, `op_adv_c_minus`, `op_adv_cinv`, `op_adv_c_mul`, `op_adv_c_div`.

    **poly.rs**: Stubs for: `op_adv_ply`, `op_adv_rts`.

    **matrix_workflow.rs**: Stubs for: `op_adv_matrx`, `op_adv_mtr`, `op_adv_medit`, `op_adv_cmedit`.

    **curve_fit.rs**: Stubs for: `op_adv_cfit`, `op_adv_as`, `op_adv_ds`, `op_adv_bfit`, `op_adv_fit`, `op_adv_y_query_x`, `op_adv_sz_query`.

    **vectors.rs**: Stubs for: `op_adv_v_plus`, `op_adv_v_minus`, `op_adv_dot`, `op_adv_cross`, `op_adv_vc`, `op_adv_vs`, `op_adv_vr`, `op_adv_ve`, `op_adv_vxy`, `op_adv_uv`, `op_adv_v_mag`, `op_adv_vd`, `op_adv_v_star`, `op_adv_tr`.

    Every stub function has signature `pub fn op_adv_xxx(state: &mut CalcState) -> Result<(), HpError>` and body `Err(HpError::InvalidOp)`. Add the D-43.5 isolation guard comment at the top of every matrix file: "D-43.5 ISOLATION INVARIANT: This file MUST NOT reference state.matrix_dim or state.matrix_active_reg."
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo check -p hp41-core 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/ops/advantage/mod.rs` exists with `pub struct AdvMatrix` + `ADV_WORD_MASK` + all 12 sub-module declarations
    - `hp41-core/src/ops/advantage/modal.rs` has `pub enum AdvantageStep` with >= 10 variants and exhaustive `current_prompt` match
    - `hp41-core/src/ops/advantage/tvm.rs` has `pub struct TvmState` with `#[derive(Serialize, Deserialize)]` and NO `#[serde(skip)]`
    - `hp41-core/src/ops/advantage/solvers.rs` has `pub struct FrootState`, `pub struct AdvFintegState`, `pub struct AdvFsolveState`, `pub struct AdvFdifeqState`
    - Every file under `advantage/` starts with the Free42 disclaim header
    - All 13 files create compilable stub functions
    - `grep -rn "matrix_dim\|matrix_active_reg" hp41-core/src/ops/advantage/` returns empty (D-43.5)
  </acceptance_criteria>
  <done>All advantage/ files exist with correct types, stubs compile, Free42 headers present, D-43.5 isolation holds</done>
</task>

<task type="auto">
  <name>Task 2: Wire XROM framework (xrom.rs bits 3+4, modal.rs Advantage variant, complex.rs pub(crate), state.rs fields + migration, Op enum + dispatch stubs, contamination guard)</name>
  <files>
    hp41-core/src/ops/math1/xrom.rs,
    hp41-core/src/ops/math1/modal.rs,
    hp41-core/src/ops/math1/complex.rs,
    hp41-core/src/state.rs,
    hp41-core/src/ops/mod.rs,
    hp41-core/src/ops/program.rs,
    scripts/check-free42-contamination.sh
  </files>
  <read_first>
    hp41-core/src/ops/math1/xrom.rs (full file — understand existing MATH_1/STAT_1/TIME_MODULE constants, xrom_resolve bit arms, resolver functions, test patterns),
    hp41-core/src/ops/math1/modal.rs (lines 1-116 — ModalProgram enum variants + current_prompt + requires_alpha_label),
    hp41-core/src/ops/math1/complex.rs (line 51-52 — pub(super) fn complex_atan2),
    hp41-core/src/state.rs (lines 160-486 — CalcState fields + default_xrom_modules + migrate_after_load + CalcState::new),
    hp41-core/src/ops/mod.rs (lines 117-500 — Op enum; lines 1320-1760 — dispatch function),
    hp41-core/src/ops/program.rs (grep for execute_op function and its exhaustive match),
    scripts/check-free42-contamination.sh (full file)
  </read_first>
  <action>
    **math1/xrom.rs** (freeze carve-out): Add two new `XromModule` constants after `TIME_MODULE`:
    - `pub const ADV_MATH_A: XromModule` with `id: 22`, `name: "ADV 22A"`, `ops` slice containing all ~62 entries for ADV CONV (12) + ADV MTRX (50) mapped to their `Op::Adv*` variants. Use section comments matching the existing `// ── Plan 28-02: Hyperbolics ──` style, organized as `// ── ADV CONV ──`, `// ── ADV MTRX: Element Access ──`, etc.
    - `pub const ADV_MATH_B: XromModule` with `id: 24`, `name: "ADV 24B"`, `ops` slice containing all ~53 entries for ADV MATH (47) + ADV TVM (6) mapped to their `Op::Adv*` variants.

    Add `adv_a_resolve(name: &str) -> Option<Op>` and `adv_b_resolve(name: &str) -> Option<Op>` resolver functions following the `stat1_resolve`/`time_resolve` pattern. Exhaustive match, `_ => None` terminal.

    Extend `xrom_resolve()` with bit-3 arm (`modules & 0b0000_1000 != 0` -> `adv_a_resolve`) and bit-4 arm (`modules & 0b0001_0000 != 0` -> `adv_b_resolve`) after the existing bit-2 (time) arm, before the terminal `None`.

    Add in-file tests following the TIME_MODULE test block pattern: `adv_math_a_const_id_and_name`, `adv_math_b_const_id_and_name`, `adv_math_a_ops_has_correct_entry_count` (assert 62 or actual count), `adv_math_b_ops_has_correct_entry_count`, `resolve_uses_bit_3_for_adv_a`, `resolve_uses_bit_4_for_adv_b`, `adv_a_ops_mnemonics_resolve_consistently`, `adv_b_ops_mnemonics_resolve_consistently`.

    Mnemonic strings for the ops slice: use the exact REQUIREMENTS.md mnemonic for each op. For ADV MATH complex variants that duplicate Math Pac I names (C+, C-, CINV, C*, C/), prefix with "ADV" to avoid shadowing: "ADVC+", "ADVC-", "ADVCINV", "ADVC*", "ADVC/". For ops with special characters: "M*M", "MAT+", "MAT-", "MAT*", "MAT/", "R<>R", "R>R?", "DIM?", "MNAME?", "C<>C", "SZ?", "Y?X", "V+", "V-", "V<", "V*", "*I", "e^Z" (use "E^Z" ASCII per Math Pac I precedent), "Z^N", "Z^1/N", "Z^1/W", "Z^W", "a^Z" (use "A^Z"), "T+X" style. "BIT?" for bit test. Keep "I+" / "I-" / "J+" / "J-" for index ops.

    **math1/modal.rs** (freeze carve-out): Add `Advantage(crate::ops::advantage::modal::AdvantageStep)` variant to `ModalProgram` enum after `Time(..)`. Add dispatch arms to `current_prompt()` and `requires_alpha_label()` delegating to `crate::ops::advantage::modal::current_prompt(step)` and `crate::ops::advantage::modal::requires_alpha_label(step)`.

    **math1/complex.rs** (freeze carve-out): Line 52 — change `pub(super)` to `pub(crate)` on `complex_atan2`. Remove `#[allow(dead_code)]` from line 51.

    **state.rs**: Update `default_xrom_modules()` from `0b0000_0111` to `0b0001_1111`. Add `migrate_after_load` guards for bit-3 and bit-4 (same idempotent OR-assign pattern, BEFORE the stopwatch freeze logic). Add all new CalcState fields per RESEARCH.md CalcState Additions section: persistent (`#[serde(default)]`): `adv_matrices: Vec<AdvMatrix>`, `adv_matrix_i: u8`, `adv_matrix_j: u8`, `adv_tvm_state: Option<TvmState>` (D-43.11: NO skip); transient (`#[serde(default, skip)]`): `adv_current_matrix: Option<String>`, `adv_froot_state: Option<FrootState>`, `adv_fintg_state: Option<AdvFintegState>`, `adv_fsolve_state: Option<AdvFsolveState>`, `adv_fdifeq_state: Option<AdvFdifeqState>`. Update `CalcState::new()` with zero-value initializers for all new fields.

    **ops/mod.rs**: Add all ~117 `Op::Adv*` variants to the `Op` enum after the Time Module block. Two section comments: `// ── Phase 43 (v3.3): Advantage Pac XROM 22 (ADV CONV + ADV MTRX) ──` and `// ── Phase 43 (v3.3): Advantage Pac XROM 24 (ADV MATH + ADV TVM) ──`. Each variant gets a doc comment with requirement ID (e.g., `/// BININ — binary string in ALPHA to integer (ADV-CONV-01, ADV-FW-06).`). Add all corresponding `dispatch()` arms routing to the stub functions in `advantage/` sub-modules.

    **ops/program.rs**: Add all ~117 `execute_op()` arms matching the `dispatch()` arms. For solver ops (FSOLVE/FINTG/FDIFEQ), the execute_op arm should route to the `_run_loop` variant when available.

    **scripts/check-free42-contamination.sh**: Add `ADV_DIR="hp41-core/src/ops/advantage"`. Extend both `for dir in` loops to include `"$ADV_DIR"`. Update the success echo message.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core -- adv_math_a_const adv_math_b_const resolve_uses_bit_3 resolve_uses_bit_4 adv_a_ops_mnemonics adv_b_ops_mnemonics 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p hp41-core` succeeds with all ~117 Op::Adv* variants in dispatch() and execute_op() (4-way invariant items 1+2 for Phase 43)
    - `cargo test -p hp41-core -- adv_math_a_const_id_and_name` passes (ADV_MATH_A.id == 22)
    - `cargo test -p hp41-core -- adv_math_b_const_id_and_name` passes (ADV_MATH_B.id == 24)
    - `cargo test -p hp41-core -- resolve_uses_bit_3` passes (bit-3 isolation for ADV_MATH_A)
    - `cargo test -p hp41-core -- resolve_uses_bit_4` passes (bit-4 isolation for ADV_MATH_B)
    - `cargo test -p hp41-core -- adv_a_ops_mnemonics_resolve_consistently` passes
    - `cargo test -p hp41-core -- adv_b_ops_mnemonics_resolve_consistently` passes
    - `xrom_resolve("BININ", 0b0000_1000)` returns `Some(Op::AdvBinin)` and `xrom_resolve("BININ", 0b0000_0111)` returns `None`
    - `default_xrom_modules()` returns `0b0001_1111` (state.rs test)
    - `migrate_after_load` sets bits 3+4 on a state with `xrom_modules = 0b0000_0111`
    - `ModalProgram::Advantage(AdvantageStep::TvmN).current_prompt()` returns a non-None string
    - `complex_atan2` is callable as `crate::ops::math1::complex::complex_atan2`
    - `scripts/check-free42-contamination.sh` contains `ADV_DIR` and exits 0 when run from repo root
    - hp41-cli and hp41-gui produce expected `non-exhaustive patterns` compile errors (sanctioned break for items 3+4)
  </acceptance_criteria>
  <done>Full XROM framework wired: bit-3/bit-4 resolution, all Op variants registered, state fields added, migration working, contamination guard extended, all framework tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CalcState input validation | Matrix dimensions from stack (MATDIM Y=rows, X=cols) must be bounded |
| ALPHA string parsing | Base conversion ops parse ALPHA register content — reject non-digit chars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | MATDIM dimension input | mitigate | Validate rows/cols against ADV_MATRIX_MAX_ROWS/COLS before Vec allocation |
| T-43-02 | Denial of Service | Named matrix Vec growth | accept | D-43.1 allows unlimited matrices; Vec growth is bounded by heap; no external network exposure |
| T-43-03 | Tampering | BININ/HEXIN/OCTIN ALPHA parsing | mitigate | Use Rust from_str_radix which returns Err on invalid chars; map to HpError::Domain |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in Phase 43; zero-new-runtime-deps invariant (ADR-v3.1-002) |
</threat_model>

<verification>
```bash
# Full test suite
cargo test -p hp41-core

# XROM framework tests specifically
cargo test -p hp41-core -- adv_math_a adv_math_b resolve_uses_bit

# State migration test
cargo test -p hp41-core -- migrate

# Contamination guard
bash scripts/check-free42-contamination.sh

# D-43.5 isolation check
grep -rn "matrix_dim\|matrix_active_reg" hp41-core/src/ops/advantage/ && echo "FAIL: D-43.5 violation" || echo "OK: D-43.5 isolation holds"
```
</verification>

<success_criteria>
- cargo build -p hp41-core succeeds with all ~117 Op variants
- All XROM framework tests pass (ADV_MATH_A/B constants, bit-3/bit-4 resolution, bidirectional consistency)
- State migration tests pass (0b0000_0111 -> 0b0001_1111)
- Free42 contamination guard exits 0
- D-43.5 isolation grep returns empty
- hp41-cli/hp41-gui produce only the expected non-exhaustive patterns compile errors
</success_criteria>

<output>
Create `.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-01-SUMMARY.md` when done
</output>
