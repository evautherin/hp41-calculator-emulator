---
phase: 44-hp41-cli-cli-integration
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - hp41-cli/tests/function_matrix_parity.rs
  - hp41-core/tests/xrom_shadowing.rs
  - hp41-cli/tests/phase44_help_data_adv.rs
  - hp41-cli/tests/phase44_modal_flow.rs
  - hp41-cli/tests/key_coverage.rs
autonomous: true
requirements:
  - ADV-CLI-05
  - ADV-CLI-06
  - ADV-CLI-07

must_haves:
  truths:
    - "function_matrix_parity partition guard accepts module_ids 22 and 24 without rogue entries"
    - "ADV_A and ADV_B inventory constants match 63 and 51 entries respectively"
    - "Forward parity: every inventory Op variant has a matching JSON entry"
    - "Reverse parity: every JSON display_name resolves via xrom_resolve(name, 0b0001_1111)"
    - "All ADV_MATH_A + ADV_MATH_B mnemonics are disjoint from all prior XROM modules and builtins"
    - "AdvantageStep modal prompts route correctly through ModalProgram::Advantage carrier enum"
    - "key_coverage probes >= 269 entries with 0b0001_1111 bitfield"
  artifacts:
    - path: "hp41-cli/tests/function_matrix_parity.rs"
      provides: "5-pool partition guard + ADV inventory constants + forward/reverse parity tests"
      contains: "ADV_A_OP_VARIANT_NAMES"
    - path: "hp41-core/tests/xrom_shadowing.rs"
      provides: "ADV_MATH_A + ADV_MATH_B disjointness + resolver tests (8 new tests)"
      contains: "adv_a_ops_disjoint_from_prior_modules"
    - path: "hp41-cli/tests/phase44_help_data_adv.rs"
      provides: "Smoke tests for Advantage JSON pool (12 tests)"
      contains: "adv_help_entries_count_meets_114_target"
    - path: "hp41-cli/tests/phase44_modal_flow.rs"
      provides: "Modal routing verification for AdvantageStep prompts"
      contains: "advantage_tvm_n_prompt"
    - path: "hp41-cli/tests/key_coverage.rs"
      provides: "Updated bitfield + Advantage sub-loop + raised floor assertion"
      contains: "0b0001_1111"
  key_links:
    - from: "hp41-cli/tests/function_matrix_parity.rs"
      to: "docs/hp41-advantage-functions.json"
      via: "help_entries_adv() accessor"
      pattern: "help_entries_adv"
    - from: "hp41-core/tests/xrom_shadowing.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "ADV_MATH_A + ADV_MATH_B imports"
      pattern: "ADV_MATH_A.*ADV_MATH_B"
    - from: "hp41-cli/tests/phase44_modal_flow.rs"
      to: "hp41-core/src/ops/advantage/modal.rs"
      via: "AdvantageStep enum import"
      pattern: "ModalProgram::Advantage"
---

<objective>
Extend all CI test gates to cover the Advantage Pac's dual-XROM structure: partition parity, xrom shadowing, JSON smoke tests, modal routing verification, and key coverage.

Purpose: Without these tests, JSON typos, mnemonic collisions, and modal routing bugs would go undetected. The test suite is the quality gate that converts "it compiles" into "it is correct".

Output: Updated function_matrix_parity.rs, xrom_shadowing.rs, key_coverage.rs; new phase44_help_data_adv.rs, phase44_modal_flow.rs.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hp41-cli-cli-integration/44-RESEARCH.md
@.planning/phases/44-hp41-cli-cli-integration/44-PATTERNS.md
@.planning/phases/44-hp41-cli-cli-integration/44-01-SUMMARY.md

<interfaces>
From hp41-cli/src/help_data.rs (after Plan 01):
```rust
pub fn help_entries_adv() -> &'static [HelpEntry];
pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry>;
// 5-pool chain: cv -> math1 -> stat1 -> time -> adv
```

From hp41-core/src/ops/math1/xrom.rs:
```rust
pub static ADV_MATH_A: XromModule;  // id=22, name="ADV CONV", 63 ops
pub static ADV_MATH_B: XromModule;  // id=24, name="ADV MATH", 51 ops
pub fn xrom_resolve(name: &str, modules: u8) -> Option<Op>;
// bit-3 = ADV_MATH_A, bit-4 = ADV_MATH_B
// 0b0001_1111 = all 5 modules loaded (v3.3 default)
```

From hp41-core/src/ops/advantage/modal.rs:
```rust
pub enum AdvantageStep {
    TvmN, TvmI, TvmPv, TvmPmt, TvmFv, TvmBeginEnd,
    MatrixNamePrompt, MatrixDimRowPrompt, MatrixDimColPrompt,
    MatrxOperationChoice, MtrNamePrompt,
    FdifeqFunctionNamePrompt, FdifeqOrderPrompt,
    MeditElementPrompt(usize, usize),
    CmeditElementPrompt(usize, usize),
    VeComponentPrompt(usize),
}
pub fn current_prompt(step: &AdvantageStep) -> Option<String>;
pub fn requires_alpha_label(step: &AdvantageStep) -> bool;
```

From hp41-core/src/ops/math1/modal.rs:
```rust
pub enum ModalProgram {
    // ... prior variants ...
    Advantage(AdvantageStep),
}
impl ModalProgram {
    pub fn current_prompt(&self) -> Option<String>;
    pub fn requires_alpha_label(&self) -> bool;
}
```

Inventory constants needed (63 + 51 Op variant names):
ADV_A_OP_VARIANT_NAMES: 63 entries (XROM 22) — no run-loop variants
ADV_B_OP_VARIANT_NAMES: 51 entries (XROM 24) — no run-loop variants
Run-loop variants (AdvFsolveRunLoop, AdvFintgRunLoop, AdvFdifeqRunLoop) are EXCLUDED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend function_matrix_parity.rs + xrom_shadowing.rs + key_coverage.rs</name>
  <files>hp41-cli/tests/function_matrix_parity.rs, hp41-core/tests/xrom_shadowing.rs, hp41-cli/tests/key_coverage.rs</files>
  <action>
Part A — function_matrix_parity.rs:

1. Add `help_entries_adv` to the import list at the top of the file.

2. Add two inventory constants after `TIME_OP_VARIANT_NAMES`:
   - `ADV_A_OP_VARIANT_NAMES: &[&str]` — 63 entries matching ADV_MATH_A.ops. Listed by group: ADV CONV (12: AdvBinin through AdvBitTest) then ADV MTRX (51: AdvIPlus through AdvCmedit). Does NOT include AdvFsolveRunLoop/AdvFintgRunLoop/AdvFdifeqRunLoop.
   - `ADV_B_OP_VARIANT_NAMES: &[&str]` — 51 entries matching ADV_MATH_B.ops. Listed by group: ADV MATH (45: AdvExpZ through AdvTr) then ADV TVM (6: AdvTvm through AdvTvmStarI). Does NOT include run-loop variants.

3. Add inventory count sentinels:
   - `test_adv_a_op_inventory_count` asserts len == 63
   - `test_adv_b_op_inventory_count` asserts len == 51

4. Update `test_pool_partition_is_exhaustive`:
   - Add `let mut adv_a_count = 0usize;` and `let mut adv_b_count = 0usize;` before the loop
   - Add two match arms: `Some(22) => adv_a_count += 1,` and `Some(24) => adv_b_count += 1,` in the module_id match
   - Add assertions after the loop: `assert_eq!(adv_a_count, 63, ...)` and `assert_eq!(adv_b_count, 51, ...)`
   - Update the rogue assertion error message to include module_ids 22 and 24 in the supported set, and change "v3.2" to "v3.3"

5. Add forward parity tests (one per module):
   - `test_every_adv_a_rom_op_has_adv_a_json_entry`: filter help_entries_adv() by module_id==22, collect op_variants into HashSet, assert every ADV_A_OP_VARIANT_NAMES entry is present
   - `test_every_adv_b_rom_op_has_adv_b_json_entry`: same for module_id==24 and ADV_B_OP_VARIANT_NAMES

6. Add reverse parity tests (one per module):
   - `test_every_adv_a_json_entry_has_xrom_resolver_match`: for each help_entries_adv() entry with module_id==22, call `xrom_resolve(entry.display_name, 0b0001_1111)` and assert Some
   - `test_every_adv_b_json_entry_has_xrom_resolver_match`: same for module_id==24

Part B — xrom_shadowing.rs:

1. Update the import to add `ADV_MATH_A, ADV_MATH_B` to the use statement.

2. Add ADV_MATH_A test block (4 tests, following TIME_MODULE pattern):
   - `adv_a_names_do_not_shadow_builtins`: no ADV_MATH_A mnemonic in BUILTIN_CARD_OP_NAMES
   - `adv_a_ops_disjoint_from_prior_modules`: no collision with MATH_1, STAT_1, TIME_MODULE
   - `adv_a_ops_resolve_via_xrom_resolve`: each name resolves via xrom_resolve(name, 0b0001_1111)
   - `adv_a_const_fields`: id==22, name=="ADV CONV"

3. Add ADV_MATH_B test block (4 tests):
   - `adv_b_names_do_not_shadow_builtins`
   - `adv_b_ops_disjoint_from_prior_modules_and_adv_a`: additionally checks disjoint from ADV_MATH_A
   - `adv_b_ops_resolve_via_xrom_resolve`: xrom_resolve(name, 0b0001_1111)
   - `adv_b_const_fields`: id==24, name=="ADV MATH"

Part C — key_coverage.rs:

1. Update the main loop bitfield from `0b0000_0111` to `0b0001_1111` (all 5 modules loaded)
2. Raise the floor assertion from `>= 155` to `>= 269` (155 + 114 Advantage entries)
3. Add an Advantage Pac sub-loop after the Time sub-loop, filtering entries where `xrom.module_id == 22 || xrom.module_id == 24`, probing each via `xeq_by_name_local_resolve(name, 0b0001_1111)`, with floor assertion `>= 100`
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test --package hp41-cli --test function_matrix_parity -- --no-fail-fast 2>&1 | tail -20 && cargo test --package hp41-core --test xrom_shadowing -- --no-fail-fast 2>&1 | tail -20 && cargo test --package hp41-cli --test key_coverage 2>&1 | tail -10</automated>
  </verify>
  <done>All partition, shadowing, and key coverage tests pass. ADV inventory counts match 63+51. No rogue module_ids. All Advantage mnemonics disjoint from prior modules. All JSON display_names resolve via xrom_resolve. Key coverage probes >= 269 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create phase44_help_data_adv.rs + phase44_modal_flow.rs</name>
  <files>hp41-cli/tests/phase44_help_data_adv.rs, hp41-cli/tests/phase44_modal_flow.rs</files>
  <action>
Part A — phase44_help_data_adv.rs (12 tests, mirroring phase39_help_data_time.rs):

Create a new test file with `#![allow(clippy::unwrap_used)]` header. Import `help_entries_adv`, `help_entries_all`, `help_overlay_rows`, and `std::collections::HashSet`.

Tests:
1. `adv_help_entries_is_not_empty`: assert help_entries_adv() is not empty
2. `adv_help_entries_count_meets_114_target`: assert len == 114
3. `adv_every_entry_has_display_name`: assert no empty display_name
4. `adv_every_entry_has_description`: assert no empty description
5. `adv_all_entries_are_implemented`: assert all status == "implemented"
6. `adv_every_entry_has_xrom_module_id_in_22_or_24`: assert xrom.module_id is 22 or 24 for every entry (dual-module check — NOT a single value)
7. `adv_categories_use_adv_prefix`: assert every entry.category.starts_with("Adv ")
8. `adv_function_ids_dense_per_module`: SPLIT into two sub-checks — ADV_MATH_A (module_id=22, function_ids 1..=63) and ADV_MATH_B (module_id=24, function_ids 1..=51) INDEPENDENTLY (Pitfall 1). Sort, check first==1, last==max, len==count.
9. `adv_all_key_paths_are_xeq_form`: assert every key_path starts with "XEQ \""
10. `adv_no_divergence_entries`: assert divergences is empty for all entries (no documented Advantage divergences in Phase 44)
11. `help_entries_all_returns_five_pools`: assert help_entries_all().count() >= 350
12. `help_overlay_rows_includes_adv_pac_sections`: call help_overlay_rows(), verify at least one row has description starting with "=== Adv "

Part B — phase44_modal_flow.rs (ADV-CLI-07 verification):

Create a new test file with `#![allow(clippy::unwrap_used)]` header. Import `AdvantageStep` from `hp41_core::ops::advantage::modal` and `ModalProgram` from `hp41_core::ops::math1::modal`.

Test each AdvantageStep variant via the carrier enum `ModalProgram::Advantage(step)`:

TVM steps:
- `advantage_tvm_n_prompt`: TvmN -> current_prompt() == Some("N=?"), requires_alpha_label() == false
- `advantage_tvm_i_prompt`: TvmI -> Some("I%YR=?"), false
- `advantage_tvm_pv_prompt`: TvmPv -> Some("PV=?"), false
- `advantage_tvm_pmt_prompt`: TvmPmt -> Some("PMT=?"), false
- `advantage_tvm_fv_prompt`: TvmFv -> Some("FV=?"), false
- `advantage_tvm_begin_end_no_prompt`: TvmBeginEnd -> None, false

Alpha-label steps (requires_alpha_label == true):
- `advantage_matrix_name_prompt_requires_alpha_label`: MatrixNamePrompt -> Some("MNAME?"), true
- `advantage_mtr_name_prompt_requires_alpha_label`: MtrNamePrompt -> Some("MTR NAME?"), true
- `advantage_fdifeq_function_name_prompt_requires_alpha_label`: FdifeqFunctionNamePrompt -> Some("F NAME?"), true

Other non-parameterized steps:
- `advantage_matrix_dim_row_prompt`: MatrixDimRowPrompt -> Some("ROWS=?"), false
- `advantage_matrix_dim_col_prompt`: MatrixDimColPrompt -> Some("COLS=?"), false
- `advantage_matrx_operation_choice_prompt`: MatrxOperationChoice -> Some("MATRX OP?"), false
- `advantage_fdifeq_order_prompt`: FdifeqOrderPrompt -> Some("ORDER=?"), false

Parameterized steps (verify prompt contains coordinates):
- `advantage_medit_element_prompt_format`: MeditElementPrompt(3, 7) -> prompt contains '3' and '7', false
- `advantage_cmedit_element_prompt_format`: CmeditElementPrompt(1, 2) -> prompt contains '1' and '2', false
- `advantage_ve_component_prompt_format`: VeComponentPrompt(2) -> prompt contains '2', false

IMPORTANT: The exact prompt strings depend on what Phase 43 implemented in advantage/modal.rs. If any prompt string does not match, adjust the test expectation to match the actual implementation — the test verifies routing through the carrier enum, not the exact wording. Read hp41-core/src/ops/advantage/modal.rs to confirm exact prompt strings before writing assertions.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test --package hp41-cli --test phase44_help_data_adv -- --no-fail-fast 2>&1 | tail -20 && cargo test --package hp41-cli --test phase44_modal_flow -- --no-fail-fast 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 help_data_adv tests pass (114 entries, 5-pool chain >= 350, overlay sections present, dual-module function_ids dense). All ~16 modal_flow tests pass (carrier enum routing verified for every AdvantageStep variant).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are test files — no production code surface added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Information Disclosure | test files | accept | Test files contain no secrets; they verify public API contracts |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no package installs |
</threat_model>

<verification>
```bash
# Full test suite for both packages
cd /Users/daniel/GitRepository/hp41-calculator-emulator
cargo test --package hp41-cli -- --no-fail-fast 2>&1 | tail -20
cargo test --package hp41-core --test xrom_shadowing -- --no-fail-fast
```
</verification>

<success_criteria>
- function_matrix_parity: partition guard accepts module_ids 22+24; inventory counts 63+51; forward+reverse parity pass
- xrom_shadowing: 8 new tests pass; ADV_A+B disjoint from all prior modules + builtins; resolver round-trips clean
- phase44_help_data_adv: 12 tests pass; 114 entries; 5-pool >= 350; overlay sections with "Adv " prefix
- phase44_modal_flow: all AdvantageStep variants route correctly; alpha-label steps return true; TVM prompts correct
- key_coverage: probes >= 269 entries; Advantage sub-loop probes >= 100 entries
- cargo test --package hp41-cli exits 0
- cargo test --package hp41-core --test xrom_shadowing exits 0
</success_criteria>

<output>
Create `.planning/phases/44-hp41-cli-cli-integration/44-02-SUMMARY.md` when done
</output>
