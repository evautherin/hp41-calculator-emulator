---
phase: 44-hp41-cli-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41-advantage-functions.json
  - hp41-cli/src/help_data.rs
  - justfile
  - scripts/docs-matrix/src/main.rs
  - docs/hp41-advantage-function-matrix.md
autonomous: true
requirements:
  - ADV-CLI-01
  - ADV-CLI-02
  - ADV-CLI-04
  - ADV-CLI-08

must_haves:
  truths:
    - "docs/hp41-advantage-functions.json parses without panic via include_str! + serde_json"
    - "help_entries_adv() returns exactly 114 entries (63 module_id=22 + 51 module_id=24)"
    - "help_entries_all() chains 5 pools and returns >= 350 entries"
    - "? overlay auto-derives Advantage Pac sections from Adv category prefixes"
    - "Right-panel excludes Advantage Pac entries via existing entry.xrom.is_none() filter"
    - "docs/hp41-advantage-function-matrix.md generated via just docs-matrix"
  artifacts:
    - path: "docs/hp41-advantage-functions.json"
      provides: "Fifth JSON canonical source (114 entries)"
      contains: "AdvBinin"
    - path: "hp41-cli/src/help_data.rs"
      provides: "Fifth OnceLock pool + 5-chain help_entries_all()"
      exports: ["help_entries_adv", "help_entries_all"]
    - path: "docs/hp41-advantage-function-matrix.md"
      provides: "Generated function matrix (114 entries)"
      contains: "BININ"
  key_links:
    - from: "hp41-cli/src/help_data.rs"
      to: "docs/hp41-advantage-functions.json"
      via: "include_str! at compile time"
      pattern: "include_str.*hp41-advantage-functions"
    - from: "hp41-cli/src/help_data.rs"
      to: "help_entries_adv"
      via: "fifth .chain() arm in help_entries_all()"
      pattern: "chain.*help_entries_adv"
---

<objective>
Author the fifth JSON canonical source for the Advantage Pac (114 entries across XROM 22 + XROM 24), wire it into help_data.rs as the fifth OnceLock pool, extend the docs-matrix tooling, and generate the function matrix.

Purpose: The JSON file is the single source of truth for the ? help overlay, function matrix, and parity tests. Without it, the Advantage Pac ops are callable but not discoverable.

Output: docs/hp41-advantage-functions.json (114 entries), updated help_data.rs (5-pool chain), docs/hp41-advantage-function-matrix.md (generated), updated justfile + docs-matrix script.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hp41-cli-cli-integration/44-RESEARCH.md
@.planning/phases/44-hp41-cli-cli-integration/44-PATTERNS.md

Pre-satisfied: ADV-CLI-03 is DONE (117 op_display_name arms in hp41-cli/src/prgm_display.rs as of commit 7c28d2b). Do NOT touch prgm_display.rs.

ADV-CLI-08 is auto-satisfied: the existing `entry.xrom.is_none()` filter in hp41-cli/src/keys.rs excludes all XROM entries from the right panel. Adding JSON entries with xrom blocks automatically excludes them. No code change needed.

<interfaces>
From hp41-cli/src/help_data.rs (lines 64-80, 189-195):
```rust
pub struct HelpEntry {
    pub op_variant: String,
    pub display_name: String,
    pub category: String,
    pub status: String,
    pub phase: Option<String>,
    pub key_path: Option<String>,
    pub description: String,
    #[serde(default)]
    pub divergences: Vec<String>,
    #[serde(default)]
    pub xrom: Option<XromEntry>,
}

pub struct XromEntry {
    pub module: String,
    pub module_id: u8,
    pub function_id: u16,
}

pub fn help_entries_all() -> impl Iterator<Item = &'static HelpEntry> {
    help_entries()
        .iter()
        .chain(help_entries_math1().iter())
        .chain(help_entries_stat1().iter())
        .chain(help_entries_time().iter())
}
```

From hp41-core/src/ops/math1/xrom.rs — the authoritative mnemonic strings:
ADV_MATH_A (XROM 22, 63 ops): BININ, BINVIEW, OCTIN, HEXIN, HEXVIEW, CVTVIEW, NOT, AND, OR, XOR, ROTXY, BIT?, I+, I-, J+, J-, MR, MS, MRIJ, MSIJ, MSIJR, MRC+, MRC-, MRR+, MRR-, MSR+, MSC+, MSWAP, MNAME?, DIM?, MATDIM, MP, PIV, R<>R, R>R?, SUM, SUMAB, MAX, MAXAB, MIN, RMAXAB, RNRM, RSUM, FNRM, MDET, MINV, MSYS, M*M, MAT+, MAT-, MAT*C, MAT/C, TRNPS, MMOVE, C<>C, CMAXAB, CNRM, CSUM, YC+C, MATRX, MTR, MEDIT, CMEDIT

ADV_MATH_B (XROM 24, 51 ops): E^Z, LNZ, LOGZ, Z^N, Z^1/N, Z^W, Z^1/W, |Z|, SINZ, COSZ, TANZ, A^Z, CADD, CSUB, CINV, CMUL, CDIV, AIP, PLY, RTS, FSOLVE, FINTG, FDIFEQ, FROOT, CFIT, AS, DS, BFIT, FIT, Y?X, SZ?, V+, V-, DOT, CROSS, VC, VS, VR, VE, VXY, UV, |V|, V*, VD, TR, TVM, N, PV, PMT, FV, *I

INTERNAL-ONLY (NOT in JSON): AdvFsolveRunLoop, AdvFintgRunLoop, AdvFdifeqRunLoop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/hp41-advantage-functions.json (114 entries)</name>
  <files>docs/hp41-advantage-functions.json</files>
  <action>
Create `docs/hp41-advantage-functions.json` with exactly 114 HelpEntry objects following the schema established by `docs/hp41-time-functions.json`.

The file contains two groups:

GROUP 1 — ADV_MATH_A (XROM 22, 63 entries, function_ids 1..=63):
- Entries 1-12: category "Adv Conv" (BININ, BINVIEW, OCTIN, HEXIN, HEXVIEW, CVTVIEW, NOT, AND, OR, XOR, ROTXY, BIT?)
- Entries 13-63: category "Adv Mtrx" (I+, I-, J+, J-, MR, MS, MRIJ, MSIJ, MSIJR, MRC+, MRC-, MRR+, MRR-, MSR+, MSC+, MSWAP, MNAME?, DIM?, MATDIM, MP, PIV, R<>R, R>R?, SUM, SUMAB, MAX, MAXAB, MIN, RMAXAB, RNRM, RSUM, FNRM, MDET, MINV, MSYS, M*M, MAT+, MAT-, MAT*C, MAT/C, TRNPS, MMOVE, C<>C, CMAXAB, CNRM, CSUM, YC+C, MATRX, MTR, MEDIT, CMEDIT)

GROUP 2 — ADV_MATH_B (XROM 24, 51 entries, function_ids 1..=51):
- Entries 1-45: category "Adv Math" (E^Z, LNZ, LOGZ, Z^N, Z^1/N, Z^W, Z^1/W, |Z|, SINZ, COSZ, TANZ, A^Z, CADD, CSUB, CINV, CMUL, CDIV, AIP, PLY, RTS, FSOLVE, FINTG, FDIFEQ, FROOT, CFIT, AS, DS, BFIT, FIT, Y?X, SZ?, V+, V-, DOT, CROSS, VC, VS, VR, VE, VXY, UV, |V|, V*, VD, TR)
- Entries 46-51: category "Adv TVM" (TVM, N, PV, PMT, FV, *I)

Per-entry field rules:
- `op_variant`: exact PascalCase Op enum variant name (e.g. "AdvBinin", "AdvExpZ", "AdvTvmStarI")
- `display_name`: copy EXACTLY from `adv_a_resolve`/`adv_b_resolve` match arms in xrom.rs (authoritative source). Watch special chars: |Z|, |V|, *I, BIT?, DIM?, MNAME?, R>R?, SZ?, Y?X, R<>R, M*M, MAT*C, MAT/C, C<>C, YC+C, Z^1/N, Z^1/W, E^Z, A^Z, Z^N, Z^W
- `status`: "implemented" for all 114
- `phase`: "43" for all 114
- `key_path`: XEQ "MNEMONIC" form for all (XROM-only, no dedicated keys)
- `xrom.module`: "Adv Conv" for module_id=22 entries, "Adv Math" for module_id=24 entries
- `xrom.module_id`: 22 for GROUP 1, 24 for GROUP 2
- `xrom.function_id`: 1-based dense independently per module (GROUP 1: 1..=63, GROUP 2: 1..=51 — Pitfall 1: do NOT continue from 64)
- `description`: concise (<= 80 chars) description of each op's behavior
- `divergences`: omit entirely (empty default, no documented Advantage divergences in Phase 44)

CRITICAL: Do NOT include AdvFsolveRunLoop, AdvFintgRunLoop, AdvFdifeqRunLoop (internal run-loop variants, not in ADV_MATH_B.ops). Total must be exactly 114 (63 + 51).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && python3 -c "
import json
data = json.load(open('docs/hp41-advantage-functions.json'))
assert len(data) == 114, f'Expected 114, got {len(data)}'
g1 = [e for e in data if e['xrom']['module_id'] == 22]
g2 = [e for e in data if e['xrom']['module_id'] == 24]
assert len(g1) == 63, f'XROM 22: expected 63, got {len(g1)}'
assert len(g2) == 51, f'XROM 24: expected 51, got {len(g2)}'
fids1 = sorted(e['xrom']['function_id'] for e in g1)
fids2 = sorted(e['xrom']['function_id'] for e in g2)
assert fids1 == list(range(1, 64)), f'XROM 22 fids not 1..63'
assert fids2 == list(range(1, 52)), f'XROM 24 fids not 1..51'
run_loops = [e for e in data if 'RunLoop' in e['op_variant']]
assert len(run_loops) == 0, f'Run-loop variants found: {run_loops}'
print('JSON validation OK: 114 entries (63+51), dense fids, no run-loops')
"</automated>
  </verify>
  <done>docs/hp41-advantage-functions.json exists with 114 valid entries; no run-loop variants; function_ids dense per module</done>
</task>

<task type="auto">
  <name>Task 2: Wire fifth OnceLock pool in help_data.rs + docs-matrix extension</name>
  <files>hp41-cli/src/help_data.rs, justfile, scripts/docs-matrix/src/main.rs, docs/hp41-advantage-function-matrix.md</files>
  <action>
Part A — help_data.rs (fifth OnceLock pool):

After the TIME block (ending at line 172), add:
1. `const ADV_FUNCTIONS_JSON: &str = include_str!("../../docs/hp41-advantage-functions.json");`
2. `static ADV_HELP_ENTRIES: OnceLock<Vec<HelpEntry>> = OnceLock::new();`
3. `pub fn help_entries_adv() -> &'static [HelpEntry]` accessor with `.expect("hp41-advantage-functions.json is malformed — fix the JSON")` per D-25.17
4. Update `help_entries_all()` to append `.chain(help_entries_adv().iter())` as the FIFTH arm (after time, never in middle — per Pitfall 6)
5. Update the doc comments:
   - Line 174: "chains all five JSON pools" (was "four")
   - Line 175: add "Advantage Pac" to the ordering list
   - Line 183: remove the future-tense comment about "Future XROM modules (Advantage Pac)" — it is now present
   - Line 186-188: add `help_entries_adv` to the narrow-accessor list

Part B — docs-matrix (fifth invocation):

1. In `scripts/docs-matrix/src/main.rs`: add a fifth `else if` branch in the basename-dispatch `render_markdown()` function for `"hp41-advantage-functions.json"` input, emitting the title "HP-41 Advantage Pac Function Matrix"
2. In `justfile` recipe `docs-matrix`: add a fifth cargo run invocation: `docs/hp41-advantage-functions.json docs/hp41-advantage-function-matrix.md`
3. In `justfile` recipe `docs-matrix-check`: add a fifth cargo run + diff pair for the advantage matrix
4. Run `just docs-matrix` to generate `docs/hp41-advantage-function-matrix.md`
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo check --package hp41-cli 2>&1 | tail -5 && just docs-matrix && just docs-matrix-check</automated>
  </verify>
  <done>hp41-cli compiles with the fifth OnceLock pool; help_entries_all() chains 5 pools; docs-matrix generates hp41-advantage-function-matrix.md without drift</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Phase 44 is display + test-gating only — no auth, no I/O, no new network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | docs/hp41-advantage-functions.json | accept | JSON is compile-time embedded via include_str!; malformed JSON panics at first access (D-25.17 hard-build-blocker); no runtime file access |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this phase; no package installs |
</threat_model>

<verification>
```bash
# Full check: CLI compiles + docs-matrix passes
cd /Users/daniel/GitRepository/hp41-calculator-emulator
cargo check --package hp41-cli
just docs-matrix-check
```
</verification>

<success_criteria>
- docs/hp41-advantage-functions.json exists with 114 valid entries (63 XROM 22 + 51 XROM 24)
- hp41-cli compiles without errors
- help_entries_adv() is a public accessor returning 114 entries
- help_entries_all() chains 5 pools
- docs/hp41-advantage-function-matrix.md generated by just docs-matrix
- just docs-matrix-check exits 0
</success_criteria>

<output>
Create `.planning/phases/44-hp41-cli-cli-integration/44-01-SUMMARY.md` when done
</output>
