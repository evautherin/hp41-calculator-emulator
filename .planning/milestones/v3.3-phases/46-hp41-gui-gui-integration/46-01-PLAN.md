---
phase: 46-hp41-gui-gui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/program.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
autonomous: true
requirements:
  - ADV-GUI-01
  - ADV-GUI-03
  - ADV-GUI-04
user_setup: []

must_haves:
  truths:
    - "hp41-gui compiles with no non-exhaustive patterns warnings in prgm_display.rs"
    - "CATALOG 2 in the GUI lists XROM 22 (ADV 22A) and XROM 24 (ADV 24B) entries"
    - "Modal prompt LCD alternation works for Advantage Pac modal workflows via existing from_state infrastructure"
  artifacts:
    - path: "hp41-gui/src-tauri/src/prgm_display.rs"
      provides: "117 Advantage Pac op_display_name arms"
      contains: "AdvBinin"
    - path: "hp41-core/src/ops/program.rs"
      provides: "CATALOG 2 xrom_registry with ADV_MATH_A + ADV_MATH_B"
      contains: "ADV_MATH_A"
  key_links:
    - from: "hp41-gui/src-tauri/src/prgm_display.rs"
      to: "hp41-core Op enum"
      via: "exhaustive match"
      pattern: "Op::Adv"
    - from: "hp41-core/src/ops/program.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "ADV_MATH_A + ADV_MATH_B constants"
      pattern: "ADV_MATH_A.*0b0000_1000"
---

<objective>
Seal the 4-way exhaustive-match invariant (item 4) by adding all ~117 Advantage Pac
op_display_name arms to the GUI prgm_display.rs, and extend the CATALOG 2 xrom_registry
with ADV_MATH_A (XROM 22) + ADV_MATH_B (XROM 24) so the GUI lists them.

Purpose: Without these arms, hp41-gui does not compile (non-exhaustive patterns error).
Without the xrom_registry entries, CATALOG 2 does not show Advantage Pac modules.
Modal prompt LCD rendering (ADV-GUI-04) is already handled by the existing
CalcStateView::from_state priority chain (modal_prompt branch at types.rs:140-149)
which delegates to ModalProgram::Advantage(step).current_prompt() -- no new code needed.

Output: hp41-gui compiles; CATALOG 2 shows XROM 22 + XROM 24; modal LCD alternation works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hp41-gui/src-tauri/src/prgm_display.rs
@hp41-cli/src/prgm_display.rs
@hp41-core/src/ops/program.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From hp41-core/src/ops/math1/xrom.rs (line 256 + 340):
The two Advantage Pac XROM module constants:
  pub const ADV_MATH_A: XromModule  (id=22, name="ADV 22A", 63 ops)
  pub const ADV_MATH_B: XromModule  (id=24, name="ADV 24B", 51 ops)

From hp41-core/src/ops/program.rs (line 18):
Current import:
  use crate::ops::math1::xrom::{XromModule, MATH_1, STAT_1, TIME_MODULE};
Must become:
  use crate::ops::math1::xrom::{XromModule, MATH_1, STAT_1, TIME_MODULE, ADV_MATH_A, ADV_MATH_B};

From hp41-core/src/ops/program.rs (lines 348-352):
Current xrom_registry array has 3 entries (MATH_1, STAT_1, TIME_MODULE).
Must gain 2 more entries: ADV_MATH_A (bit-3 = 0b0000_1000), ADV_MATH_B (bit-4 = 0b0001_0000).

From hp41-cli/src/prgm_display.rs (lines 364-482):
All 117 Advantage Pac op_display_name arms exist in the CLI copy. The GUI copy must be
an EXACT byte-for-byte mirror per the SC-4 duplication invariant. The arms begin with
  Op::AdvBinin => "BININ".to_string(),
and end with
  Op::AdvTvmStarI => "*I".to_string(),
No catch-all (_ =>) arm.

From hp41-gui/src-tauri/src/types.rs (lines 136-156):
CalcStateView::from_state display_str priority chain already handles modal_prompt
via truncate_with_continuation when state.modal_program.is_some() && state.entry_buf.is_empty()
&& state.modal_prompt.is_some(). ModalProgram::Advantage(step).current_prompt() is already
wired at modal.rs line 96. No changes to types.rs or App.tsx needed for ADV-GUI-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CATALOG 2 xrom_registry with Advantage Pac modules</name>
  <files>hp41-core/src/ops/program.rs</files>
  <read_first>
    - hp41-core/src/ops/program.rs (lines 1-20 for imports, lines 348-352 for xrom_registry)
    - hp41-core/src/ops/math1/xrom.rs (search for ADV_MATH_A and ADV_MATH_B constant definitions)
  </read_first>
  <action>
    Extend the import on line 18 of hp41-core/src/ops/program.rs to include ADV_MATH_A and ADV_MATH_B:
    the use statement imports from crate::ops::math1::xrom and currently lists {XromModule, MATH_1, STAT_1, TIME_MODULE};
    add ADV_MATH_A, ADV_MATH_B to the import list.

    In the xrom_registry array (line 348), append two entries after the TIME_MODULE entry:
      (&ADV_MATH_A, 0b0000_1000)  with comment: bit 3 -- Advantage Pac ADV CONV+MTRX (XROM 22)
      (&ADV_MATH_B, 0b0001_0000)  with comment: bit 4 -- Advantage Pac ADV MATH+TVM (XROM 24)

    The bit values match CalcState::xrom_modules encoding already set by default_xrom_modules() = 0b0001_1111
    and migrate_after_load() in state.rs.

    Update the comment above the registry that says "Fourth module (Advantage Pac) will extend this
    array automatically" to reflect the extension is now done ("5 XROM modules in registry").
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo check -p hp41-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Line 18 of program.rs imports ADV_MATH_A and ADV_MATH_B from crate::ops::math1::xrom
    - xrom_registry array has exactly 5 entries: MATH_1 (bit 0), STAT_1 (bit 1), TIME_MODULE (bit 2), ADV_MATH_A (bit 3 = 0b0000_1000), ADV_MATH_B (bit 4 = 0b0001_0000)
    - cargo check -p hp41-core succeeds with no errors
    - The existing op_catalog tests still pass (the loop iterates all 5 entries correctly)
  </acceptance_criteria>
  <done>CATALOG 2 xrom_registry contains all 5 XROM modules; hp41-core compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add 117 Advantage Pac op_display_name arms to GUI prgm_display.rs</name>
  <files>hp41-gui/src-tauri/src/prgm_display.rs</files>
  <read_first>
    - hp41-gui/src-tauri/src/prgm_display.rs (current file -- note where Time Module arms end at line 382)
    - hp41-cli/src/prgm_display.rs (lines 364-482 -- the 117 Advantage Pac arms to mirror)
  </read_first>
  <action>
    Copy the 117 Advantage Pac op_display_name arms from hp41-cli/src/prgm_display.rs (lines 364-482)
    into the GUI hp41-gui/src-tauri/src/prgm_display.rs, inserting them after the last Time Module arm
    (Op::TimeStpw line 382) and before the closing brace of the match expression.

    The arms must be BYTE-FOR-BYTE identical to the CLI copy per the SC-4 duplication invariant.
    The block should be preceded by a comment:
      // -- Phase 43 (v3.3): Advantage Pac XROM 22 (ADV CONV + ADV MTRX) --
    and a second section comment:
      // -- Phase 43 (v3.3): Advantage Pac XROM 24 (ADV MATH + ADV TVM) --
    mirroring the CLI structure.

    Also update the doc comment on the fn op_display_name function (line 47) to include
    "Advantage Pac" in the coverage list: change "Time Module" to "Time Module + Advantage Pac".

    Ensure NO catch-all (_ =>) arm exists. The exhaustive match on all Op variants is the
    compile-time enforcement mechanism for the 4-way invariant.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/src-tauri/src/prgm_display.rs contains all 117 Advantage Pac Op::Adv* arms
    - grep -c "Op::Adv" hp41-gui/src-tauri/src/prgm_display.rs returns 117
    - The match expression has NO _ => catch-all arm
    - cargo check in hp41-gui/src-tauri succeeds with no non-exhaustive patterns warnings
    - The doc comment on op_display_name mentions "Advantage Pac"
    - Op::AdvBinin maps to "BININ", Op::AdvTvmStarI maps to "*I" (matching CLI exactly)
  </acceptance_criteria>
  <done>4-way exhaustive-match invariant item 4 is sealed; hp41-gui compiles cleanly with all Advantage Pac op_display_name arms</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI IPC (dispatch_op) | Untrusted string key IDs from frontend cross into Rust backend |
| CATALOG 2 output | Module names displayed to user from xrom_registry constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | op_display_name | accept | Display-only function; no auth context; shows HP-41 mnemonic strings |
| T-46-02 | Tampering | xrom_registry | accept | Compile-time constants from xrom.rs; no user-modifiable data; array is &[(&XromModule, u8)] |
| T-46-03 | Information Disclosure | CATALOG 2 | accept | Module names ("ADV 22A", "ADV 24B") and op names are public documentation from HP Owner's Manual |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies added in this plan; zero new crates |
</threat_model>

<verification>
1. `cd hp41-gui/src-tauri && cargo check` compiles without warnings
2. `grep -c "Op::Adv" hp41-gui/src-tauri/src/prgm_display.rs` returns 117
3. `cargo test -p hp41-core -- op_catalog` passes (CATALOG 2 with 5 modules)
4. `grep -c "ADV_MATH" hp41-core/src/ops/program.rs` returns at least 2 (import + registry entries)
</verification>

<success_criteria>
- hp41-gui compiles with zero non-exhaustive patterns warnings (ADV-GUI-01 satisfied)
- CATALOG 2 lists XROM 22 and XROM 24 entries (ADV-GUI-03 satisfied)
- Modal LCD alternation works for Advantage Pac workflows via existing infrastructure (ADV-GUI-04 satisfied)
</success_criteria>

<output>
Create `.planning/phases/46-hp41-gui-gui-integration/46-01-SUMMARY.md` when done
</output>
