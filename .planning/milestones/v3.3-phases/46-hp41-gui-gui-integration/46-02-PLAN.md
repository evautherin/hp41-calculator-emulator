---
phase: 46-hp41-gui-gui-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/HelpOverlay.tsx
autonomous: true
requirements:
  - ADV-GUI-02
user_setup: []

must_haves:
  truths:
    - "HelpOverlay.tsx displays an 'Advantage Pac (XROM 22)' section for Adv Conv + Adv Mtrx entries"
    - "HelpOverlay.tsx displays an 'Advantage Pac (XROM 24)' section for Adv Math + Adv TVM entries"
    - "Searching across all five JSON pools returns Advantage Pac entries"
    - "All sections expand/collapse independently including the two new Advantage sections"
  artifacts:
    - path: "hp41-gui/src/help_data.ts"
      provides: "Fifth JSON import + helpEntriesAdvantage() + 5-pool helpEntriesAll()"
      contains: "hp41-advantage-functions.json"
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "Two new Advantage Pac section definitions in SECTIONS array"
      contains: "Advantage Pac (XROM 22)"
  key_links:
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/hp41-advantage-functions.json"
      via: "Vite static JSON import"
      pattern: "import.*advantage"
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "helpEntriesAll() 5-pool chain"
      pattern: "helpEntriesAll"
---

<objective>
Add the fifth JSON canonical source (docs/hp41-advantage-functions.json) to the GUI help
overlay system: Vite import + accessor function + 5-pool helpEntriesAll() chain in help_data.ts,
and two new collapsible sections in HelpOverlay.tsx for XROM 22 and XROM 24.

Purpose: Without this, the `?` help overlay does not show any Advantage Pac functions.
The 114-entry JSON file exists from Phase 44; this plan wires it into the frontend.

Output: HelpOverlay shows "Advantage Pac (XROM 22)" and "Advantage Pac (XROM 24)" sections;
search spans all five JSON pools (hp41cv + math1 + stat1 + time + advantage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hp41-gui/src/help_data.ts
@hp41-gui/src/HelpOverlay.tsx
@docs/hp41-advantage-functions.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From docs/hp41-advantage-functions.json (114 entries):
  - xrom.module === "Adv Conv" for XROM 22 entries (module_id: 22, 63 entries)
  - xrom.module === "Adv Math" for XROM 24 entries (module_id: 24, 51 entries)
  - Categories: "Adv Conv", "Adv Mtrx", "Adv Math", "Adv TVM"

From hp41-gui/src/help_data.ts:
  Current 4-pool chain:
    import functions from '../../docs/hp41cv-functions.json';
    import math1Functions from '../../docs/hp41-math1-functions.json';
    import stat1Functions from '../../docs/hp41-stat1-functions.json';
    import timeFunctions from '../../docs/hp41-time-functions.json';

    helpEntriesAll() = [...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1(), ...helpEntriesTime()];

  Must become 5-pool:
    + import advantageFunctions from '../../docs/hp41-advantage-functions.json';
    + helpEntriesAdvantage(): readonly HelpEntry[]
    helpEntriesAll() = [...5 pools...];

From hp41-gui/src/HelpOverlay.tsx:
  SectionDef.id union type: currently 'hp41cv' | 'math1' | 'stat1' | 'time'
  Must become: 'hp41cv' | 'math1' | 'stat1' | 'time' | 'adv22' | 'adv24'

  SECTIONS array: currently 4 entries. Must gain 2 entries:
    { id: 'adv22', heading: 'Advantage Pac (XROM 22)', predicate: e => e.xrom?.module === 'Adv Conv' }
    { id: 'adv24', heading: 'Advantage Pac (XROM 24)', predicate: e => e.xrom?.module === 'Adv Math' }

  expanded state: currently { hp41cv, math1, stat1, time }. Must add adv22: true, adv24: true.
  toggleSection parameter type must widen to include 'adv22' | 'adv24'.
  useEffect reset must include adv22: true, adv24: true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fifth JSON import and 5-pool helpEntriesAll() to help_data.ts</name>
  <files>hp41-gui/src/help_data.ts</files>
  <read_first>
    - hp41-gui/src/help_data.ts (full file -- current 4-pool structure)
    - docs/hp41-advantage-functions.json (first 20 lines -- verify import path and xrom.module values)
  </read_first>
  <action>
    Add the fifth Vite static JSON import after the timeFunctions import (line 25):
      import advantageFunctions from '../../docs/hp41-advantage-functions.json';

    Add the helpEntriesAdvantage() accessor function after helpEntriesTime() (around line 174),
    following the exact same pattern. The doc comment should reference Phase 46 Plan 46-02
    and note 114 entries, with a reference to D-44.1 (Phase 44 JSON authoring decision).
    Mention the hard-build-blocker semantics per D-25.17.

    Update helpEntriesAll() from 4-pool to 5-pool concatenation:
      return [...helpEntries(), ...helpEntriesMath1(), ...helpEntriesStat1(), ...helpEntriesTime(), ...helpEntriesAdvantage()];

    Update the JSDoc comment on helpEntriesAll() to say "5-pool" instead of "4-pool" and reference
    the Advantage Pac addition. Update the "Pitfall 5" note to reference the 5-pool chain and note
    that callers (HelpOverlay.tsx) automatically pick up Advantage entries.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - help_data.ts imports from '../../docs/hp41-advantage-functions.json'
    - helpEntriesAdvantage() function exists and returns advantageFunctions cast as readonly HelpEntry[]
    - helpEntriesAll() concatenates 5 pools (helpEntries + helpEntriesMath1 + helpEntriesStat1 + helpEntriesTime + helpEntriesAdvantage)
    - TypeScript compiles without errors (npx tsc --noEmit)
    - grep -c "advantage" hp41-gui/src/help_data.ts returns at least 3 (import + function + pool reference)
  </acceptance_criteria>
  <done>Fifth JSON pool wired into help_data.ts; helpEntriesAll() returns all 5 pools</done>
</task>

<task type="auto">
  <name>Task 2: Add Advantage Pac sections to HelpOverlay.tsx</name>
  <files>hp41-gui/src/HelpOverlay.tsx</files>
  <read_first>
    - hp41-gui/src/HelpOverlay.tsx (full file -- current 4-section structure)
    - hp41-gui/src/help_data.ts (after Task 1 changes -- verify helpEntriesAll includes advantage)
  </read_first>
  <action>
    Widen the SectionDef id type union at the interface declaration (line 42) from
    'hp41cv' | 'math1' | 'stat1' | 'time' to include 'adv22' | 'adv24'.

    Add two new entries to the SECTIONS array (line 49) after the Time entry:
      { id: 'adv22', heading: 'Advantage Pac (XROM 22)', predicate: (e: HelpEntry) => e.xrom?.module === 'Adv Conv' }
      { id: 'adv24', heading: 'Advantage Pac (XROM 24)', predicate: (e: HelpEntry) => e.xrom?.module === 'Adv Math' }

    CRITICAL: the xrom.module predicate values are "Adv Conv" (NOT "ADV 22A" or "Advantage Conv") for
    XROM 22 entries, and "Adv Math" (NOT "ADV 24B") for XROM 24 entries. These match the xrom.module
    field values in docs/hp41-advantage-functions.json exactly.

    Widen the expanded useState type to include adv22 and adv24 boolean fields, both defaulting to true:
      { hp41cv: boolean; math1: boolean; stat1: boolean; time: boolean; adv22: boolean; adv24: boolean }
    Update the initial state: { hp41cv: true, math1: true, stat1: true, time: true, adv22: true, adv24: true }

    Update the useEffect that resets state on overlay open (line 91) to include adv22: true, adv24: true
    in the setExpanded call.

    Widen the toggleSection parameter type to include 'adv22' | 'adv24'.

    Update the component header comment to mention Phase 46 Plan 46-02 and note the
    fifth+sixth sections for Advantage Pac XROM 22 and XROM 24.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SECTIONS array has exactly 6 entries (hp41cv, math1, stat1, time, adv22, adv24)
    - SectionDef.id union includes 'adv22' and 'adv24'
    - expanded state object has 6 boolean fields (all default to true)
    - grep "Advantage Pac (XROM 22)" hp41-gui/src/HelpOverlay.tsx returns a match
    - grep "Advantage Pac (XROM 24)" hp41-gui/src/HelpOverlay.tsx returns a match
    - grep "Adv Conv" hp41-gui/src/HelpOverlay.tsx returns a match (predicate value)
    - grep "Adv Math" hp41-gui/src/HelpOverlay.tsx returns a match (predicate value, distinct from Math 1)
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>HelpOverlay displays 6 sections including "Advantage Pac (XROM 22)" and "Advantage Pac (XROM 24)"; search spans all 5 JSON pools</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON import (build-time) | docs/hp41-advantage-functions.json loaded via Vite static import; malformed JSON fails the build |
| Help overlay rendering | User-facing display of HP-41 function reference data from JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering | JSON import | accept | JSON file is checked into git; Vite validates at build time; no runtime fetch |
| T-46-05 | Information Disclosure | HelpOverlay | accept | All displayed data is public HP-41 documentation from Owner's Manual |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages added; only a new JSON import from the existing docs/ directory |
</threat_model>

<verification>
1. `cd hp41-gui && npx tsc --noEmit` compiles without TypeScript errors
2. `grep -c "Advantage" hp41-gui/src/HelpOverlay.tsx` returns at least 2 (both section headings)
3. `grep -c "advantage" hp41-gui/src/help_data.ts` returns at least 3 (import + function + pool)
4. `cd hp41-gui && npx vite build 2>&1 | tail -5` completes successfully (JSON import valid)
</verification>

<success_criteria>
- HelpOverlay.tsx renders "Advantage Pac (XROM 22)" and "Advantage Pac (XROM 24)" sections (ADV-GUI-02 satisfied)
- Search across all five JSON pools returns Advantage Pac entries (ADV-GUI-02 satisfied)
- All 6 sections expand/collapse independently
</success_criteria>

<output>
Create `.planning/phases/46-hp41-gui-gui-integration/46-02-SUMMARY.md` when done
</output>
