---
phase: 45-documentation-adrs
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - docs/architecture-history.md
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - ADV-DOC-01
  - ADV-DOC-04
  - ADV-DOC-05
  - ADV-DOC-06
must_haves:
  truths:
    - "architecture-history.md contains a ## v3.3 additions section with Phase 43-45 narratives and Phase 46-47 stubs"
    - "CLAUDE.md contains a ### v3.3 additions block with Phase 43-44 detail, Phase 45 summary, and Phase 46-47 stubs"
    - "README.md contains the v3.3 soft-claim bullet under ## Features without hard-claim language"
    - "just docs-matrix-check exits 0 (ADV-DOC-01 pre-satisfied verification)"
  artifacts:
    - path: "docs/architecture-history.md"
      provides: "v3.3 narrative section appended after v3.2 section"
      contains: "v3.3 additions"
    - path: "CLAUDE.md"
      provides: "v3.3 additions decision-summary block"
      contains: "v3.3 additions"
    - path: "README.md"
      provides: "v3.3 soft-claim bullet"
      contains: "Advantage Pac behavioral emulation"
  key_links:
    - from: "docs/architecture-history.md"
      to: "docs/adr/v3.3-001-named-matrix-storage-model.md"
      via: "ADR cross-reference in Phase 43 narrative"
      pattern: "ADR-v3.3-001"
    - from: "CLAUDE.md"
      to: "docs/architecture-history.md"
      via: "Origin line referencing architecture-history.md"
      pattern: "architecture-history.md"
    - from: "README.md"
      to: "docs/hp41-advantage-divergences.md"
      via: "Markdown link in soft-claim bullet"
      pattern: "hp41-advantage-divergences.md"
---

<objective>
Append the v3.3 narrative to architecture-history.md, add the v3.3 additions block to CLAUDE.md, and add the v3.3 soft-claim bullet to README.md. Verify ADV-DOC-01 (function matrix) is pre-satisfied.

Purpose: ADV-DOC-04 requires the architecture-history narrative documenting what was built and why across Phases 43-47. ADV-DOC-06 requires the CLAUDE.md decision-summary surface for future Claude instances. ADV-DOC-05 requires the README soft-claim bullet. ADV-DOC-01 is verified as already complete from Phase 44.

Output: Three modified files (architecture-history.md appended, CLAUDE.md appended, README.md line inserted) + verification that `just docs-matrix-check` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Plan 45-01 SUMMARY for ADR cross-references -->
@.planning/phases/45-documentation-adrs/45-01-SUMMARY.md

<!-- Analog: v3.2 architecture-history section (lines 277-348) -->
@docs/architecture-history.md

<!-- Analog: v3.2 additions block in CLAUDE.md -->
@CLAUDE.md

<!-- Analog: README soft-claim bullets (lines 53-58) -->
@README.md

<!-- Research: complete source material -->
@.planning/phases/45-documentation-adrs/45-RESEARCH.md
@.planning/phases/45-documentation-adrs/45-PATTERNS.md

<!-- Phase 43 CONTEXT for decision IDs -->
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append v3.3 section to architecture-history.md and add CLAUDE.md block</name>
  <files>docs/architecture-history.md, CLAUDE.md</files>
  <action>
**architecture-history.md:**

Read the current file to find the insertion point (after the v3.2 "Frozen invariants preserved" block and the Quality Gate History table, before the end-of-file). Append a new section following the exact v3.2 section structure (lines 277-348 are the template).

Section heading: `## v3.3 additions (Advantage Pac Emulation, Phases 43-47)`

Intro paragraph (2-3 sentences): Phases 43-47 ship the fourth XROM application module -- the HP-41 Advantage Pac (HP 00041-90482, 1985) -- as a behavioral emulation of ~117 XEQ-by-name entry points across four functional families: base conversion/boolean logic (ADV CONV, XROM 22), named-matrix operations (ADV MTRX, XROM 22), advanced math/complex/solver/curve-fit (ADV MATH, XROM 24), and time-value-of-money (ADV TVM, XROM 24). This is the first XROM module requiring two hardware ROM chip IDs (XROM 22 + XROM 24), introducing a dual-resolver design (ADR-v3.3-003). Three other novel architectural additions -- named-matrix storage isolated from Math Pac I (ADR-v3.3-001), Laguerre polynomial root-finder coexisting with Math Pac I Bairstow (ADR-v3.3-002), and math1/ visibility promotion policy (ADR-v3.3-004) -- each received a standalone ADR. Everything else follows the v3.0/v3.1/v3.2 invariants.

Per-phase subsections:

`### Phase 43 -- XROM Framework + All Advantage Pac Ops (shipped 2026-05-25)` -- ~400 words covering: XROM 22 (ADV_MATH_A, bit-3) + XROM 24 (ADV_MATH_B, bit-4) registration; `default_xrom_modules` from `0b0000_0111` to `0b0001_1111`; `migrate_after_load()` auto-upgrades v3.2 saves; ADV_MATH_A display string "ADV 22A" and ADV_MATH_B "ADV 24B"; named-matrix model (Vec of AdvMatrix, ALPHA naming, global I/J, D-43.5 isolation, ADR-v3.3-001); FROOT Laguerre with (0.4, 0.9) initial guess and quadratic deflation (ADR-v3.3-002); dual XROM 12 intentional alias overlaps (ADR-v3.3-003); math1/ carve-out complex_atan2 pub(crate) + Advantage arm in modal.rs + bits 3+4 in xrom.rs (ADR-v3.3-004); TVM persistence D-43.11; solver cross-nesting D-43.7; 36-bit ADV_WORD_MASK; CalcState field additions (list all 9 fields with serde shapes from RESEARCH.md lines 299-310); ~117 new Op variants (4-way items 1+2 complete, items 3+4 sanctioned-deferred); Free42 contamination guard extended to advantage/ tree; test count from Phase 43 completion.

`### Phase 44 -- CLI Integration (shipped 2026-05-26)` -- ~200 words covering: 114-entry JSON pool (63 XROM 22 + 51 XROM 24); 7-category convention; fifth OnceLock pool in help_data.rs; `help_entries_adv()` + 5-pool `help_entries_all()` chain; 114 new `op_display_name` arms (4-way item 3 complete); xrom_shadowing extension; 12 intentional MATH_1 overlap discovery (ADV_MATH_B disjointness test uses `0b0001_0000` bit-4 isolation); function matrix generated (fifth docs-matrix invocation); `just docs-matrix-check` extended; 421 hp41-cli tests pass; no hp41-core/hp41-gui changes.

`### Phase 45 -- Documentation & ADRs (shipped 2026-05-26)` -- ~100 words covering: divergences catalog (docs/hp41-advantage-divergences.md, 9 D-45-NN entries across 3 buckets), 4 ADRs (v3.3-001 named-matrix, v3.3-002 FROOT Laguerre, v3.3-003 dual-XROM, v3.3-004 math1/ visibility), README v3.3 soft-claim, CLAUDE.md v3.3 additions block, this architecture-history v3.3 narrative section.

`### Phase 46 -- GUI Integration` -- stub: "Phase 46 planned."

`### Phase 47 -- Test Hardening & Quality Gates` -- stub: "Phase 47 planned."

Frozen invariants block (same format as v3.2): SC-4 invariant (Advantage Pac code in ops/advantage/, no leaks to hp41-gui), 4-exhaustive-match (items 1+2 Phase 43, item 3 Phase 44, item 4 deferred Phase 46), deny(clippy::unwrap_used) continued, save-file backward compat (9 new CalcState fields all with serde(default), 5 transient with skip), MSRV 1.88 unchanged, zero new runtime deps, Free42 guard extended to advantage/ tree.

Update the Quality Gate History table: add a `v3.3 (Phase 47)` column with TBD for all rows (quality gates measured at Phase 47).

**CLAUDE.md:**

Read the current file to find the insertion point (after the `### v3.2 additions` block, before `## Tech Stack`). Append a new block following the exact v3.2 additions block structure.

Block header: `### v3.3 additions (Advantage Pac Emulation, Phases 43-47)`

Origin line: "*Origin: see `docs/architecture-history.md` section v3.3 additions for the long-form per-phase narrative; this block is the CLAUDE.md decision-summary surface (the THIRD `### v3.x additions` block; the second was v3.2 per D-40.9).*"

Per-phase subsections:

`#### Phase 43 -- XROM Framework + All Advantage Pac Ops (shipped 2026-05-25)` -- bullet-point decision summary per D-40.10 convention. Include: ADV_MATH_A/ADV_MATH_B XROM registration with IDs and bits; default_xrom_modules migration; named-matrix model (D-43.1, D-43.5, ADR-v3.3-001); FROOT Laguerre (D-43.8, ADR-v3.3-002); dual XROM alias overlaps (ADR-v3.3-003); math1/ carve-out (ADR-v3.3-004); TVM persistence (D-43.11); solver nesting (D-43.7); 36-bit word size (D-43.9, D-43.10); ~117 new Op variants (4-way items 1+2); Free42 guard extended. End with CalcState field table listing all 9 fields with serde shapes (same format as Phase 38 CalcState additions in v3.2 block). XROM bits summary: "ADV_MATH_A = bit-3 (XROM 22), ADV_MATH_B = bit-4 (XROM 24); `default_xrom_modules()` = `0b0001_1111`".

`#### Phase 44 -- CLI Integration (shipped 2026-05-26)` -- bullet-point summary covering JSON pool, OnceLock chain, op_display_name arms, xrom_shadowing, MATH_1 overlap discovery, function matrix generation.

`#### Phase 45 -- Documentation & ADRs (shipped 2026-05-26)` -- brief summary of divergences catalog, 4 ADRs, README soft-claim, CLAUDE.md block, architecture-history narrative.

`#### Phase 46 -- GUI Integration` -- stub: "(in progress -- Phase 46 planned)"

`#### Phase 47 -- Test Hardening & Quality Gates` -- stub: "(Phase 47 planned)"

IMPORTANT: Do NOT use hard-claim language. Do NOT write full content for Phase 46/47 (they are not shipped). Follow the EXACT stub pattern used in v3.2 when Phases 41-42 were stubs.

IMPORTANT for CLAUDE.md CalcState field table: `adv_tvm_state: Option<TvmState>` is `#[serde(default)]` WITHOUT skip -- the Pitfall 20 muscle-memory trap. Include a warning comment in the entry matching the `rand_seed` precedent documentation pattern.
  </action>
  <verify>
    <automated>grep -c "v3.3 additions" docs/architecture-history.md | xargs test 0 -lt && grep -c "v3.3 additions" CLAUDE.md | xargs test 0 -lt</automated>
  </verify>
  <done>architecture-history.md contains "## v3.3 additions" section with Phase 43-45 narratives, Phase 46-47 stubs, frozen invariants block, and updated Quality Gate History table. CLAUDE.md contains "### v3.3 additions" block with Phase 43-44 detail, Phase 45 summary, Phase 46-47 stubs, and CalcState field table with correct serde shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Add README soft-claim bullet and verify ADV-DOC-01</name>
  <files>README.md</files>
  <action>
Read README.md and insert the v3.3 soft-claim bullet immediately after the v3.2 bullet (currently at line 57-58). The new bullet follows the exact pattern of the v3.1 and v3.2 bullets.

Insert this line (soft-claim only -- NO "feature-complete" language per D-30.9 graduation cadence):

```
- v3.3 ships Advantage Pac behavioral emulation (~117 XEQ entry points across base conversion,
  named-matrix operations, advanced math/solvers/complex/curve-fit, and time-value-of-money;
  dual XROM IDs 22 + 24; [documented divergences](docs/hp41-advantage-divergences.md)) --
  see [Advantage Pac Function Matrix](docs/hp41-advantage-function-matrix.md)
```

After inserting, verify ADV-DOC-01 is pre-satisfied by running `just docs-matrix-check` (should exit 0 -- the fifth invocation was added in Phase 44 Plan 01). Also run `cargo build -p hp41-core` to verify no code regressions from doc changes (should be clean since this plan only modifies documentation files).
  </action>
  <verify>
    <automated>grep -q "Advantage Pac behavioral emulation" README.md && just docs-matrix-check</automated>
  </verify>
  <done>README.md contains v3.3 soft-claim bullet under "## Features" without hard-claim language. `just docs-matrix-check` exits 0 confirming ADV-DOC-01 is pre-satisfied (function matrix generated in Phase 44). No "feature-complete per Owner's Manual 00041-90482" language present (hard-claim deferred to Phase 47).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply -- this plan modifies documentation files only (architecture-history.md, CLAUDE.md, README.md). No code execution paths are changed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Information Disclosure | CLAUDE.md, README.md | accept | Public documentation; no secrets disclosed |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this documentation-only plan |
</threat_model>

<verification>
1. `grep "v3.3 additions" docs/architecture-history.md` returns at least 1 match
2. `grep "v3.3 additions" CLAUDE.md` returns at least 1 match
3. `grep "Advantage Pac behavioral emulation" README.md` returns at least 1 match
4. `grep "feature-complete per Owner's Manual 00041-90482" README.md` returns 0 matches (hard-claim NOT present)
5. `just docs-matrix-check` exits 0
</verification>

<success_criteria>
1. `docs/architecture-history.md` contains `## v3.3 additions` section with Phase 43-45 narratives and Phase 46-47 stubs
2. `CLAUDE.md` contains `### v3.3 additions` block with all Phase 43 CalcState fields documented with correct serde shapes
3. `README.md` contains the v3.3 soft-claim bullet linked to divergences and function matrix docs
4. `just docs-matrix-check` exits 0 (ADV-DOC-01 verified as pre-satisfied)
5. No hard-claim language ("feature-complete per Owner's Manual 00041-90482") appears in README
</success_criteria>

<output>
Create `.planning/phases/45-documentation-adrs/45-02-SUMMARY.md` when done
</output>
