---
phase: 47-test-hardening-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/tests/xrom_op_test_count.rs
  - hp41-core/tests/lint_xrom_assertions.rs
  - hp41-core/tests/adv_backward_compat.rs
  - hp41-gui/e2e/smoke.spec.js
  - README.md
autonomous: true
requirements:
  - ADV-QUAL-01
  - ADV-QUAL-02
  - ADV-QUAL-05
  - ADV-QUAL-06
  - ADV-QUAL-07
  - ADV-QUAL-09

must_haves:
  truths:
    - "Unified xrom_op_test_count.rs scans all 5 XROM modules (Math 1 + Stat 1 + Time + ADV_MATH_A + ADV_MATH_B) and asserts >= 5 test mentions per variant"
    - "Unified lint_xrom_assertions.rs scans all 5 module test trees for Pitfall 14/17 assertion discipline including advantage/ inline tests"
    - "adv_backward_compat.rs confirms v3.2 save files (xrom_modules: 7) load and migrate to 0b11111 without data loss; adv_matrices defaults to empty; existing rand_seed and time_offset_secs are preserved"
    - "E2E smoke test exercises an Advantage Pac workflow (MDET) end-to-end in the GUI"
    - "Free42 contamination guard covers advantage/ directory and exits 0"
    - "README hard-claim reads feature-complete per Owner's Manual 00041-90482"
  artifacts:
    - path: "hp41-core/tests/xrom_op_test_count.rs"
      provides: "Unified per-Op test-count meta-gate for all 5 XROM modules"
      contains: "adv_a_variant_to_fn_name"
    - path: "hp41-core/tests/lint_xrom_assertions.rs"
      provides: "Unified assertion-discipline lint for all 5 XROM module test surfaces"
      contains: "advantage"
    - path: "hp41-core/tests/adv_backward_compat.rs"
      provides: "v3.2-to-v3.3 save-file migration test"
      contains: "v32_save_loads_with_advantage_migration"
    - path: "hp41-gui/e2e/smoke.spec.js"
      provides: "Advantage Pac MDET E2E smoke test"
      contains: "MDET"
    - path: "README.md"
      provides: "v3.3 hard-claim graduation"
      contains: "feature-complete per Owner's Manual 00041-90482"
  key_links:
    - from: "hp41-core/tests/xrom_op_test_count.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "include_str! variant extraction from adv_a_resolve + adv_b_resolve"
      pattern: "collect_adv_a_variant_names"
    - from: "hp41-core/tests/lint_xrom_assertions.rs"
      to: "hp41-core/src/ops/advantage/*.rs"
      via: "five-module file collection for inline test scanning"
      pattern: "collect_xrom_test_content"
    - from: "hp41-core/tests/adv_backward_compat.rs"
      to: "hp41-core/src/state.rs"
      via: "serde_json::from_str + migrate_after_load"
      pattern: "migrate_after_load"
---

<objective>
Extend the unified meta-gate infrastructure to cover all 5 XROM modules (adding ADV_MATH_A and ADV_MATH_B), verify backward compatibility, add E2E smoke coverage, verify Free42 contamination guard, and graduate the README hard-claim for v3.3.

Purpose: Meta-gates must be extended to the Advantage Pac variants (ADV-QUAL-01/02) so the coverage gap closure in Wave 2 is immediately counted and linted. Backward compat (ADV-QUAL-05) confirms save-file migration. E2E smoke (ADV-QUAL-06) confirms GUI end-to-end path. Free42 guard re-verification (ADV-QUAL-07) confirms the Phase 43 extension holds. README graduation (ADV-QUAL-09) conditionally ships the hard-claim after all gates pass.

Output: Extended meta-gate files, enhanced backward-compat tests, E2E smoke extension, README hard-claim.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key structures the executor needs from existing test files -->

From hp41-core/tests/xrom_op_test_count.rs:
- `collect_variants_in_fn(src: &str, fn_sig: &str) -> Vec<String>` — generic variant collector
- `pascal_to_op_snake(pascal: &str) -> String` — PascalCase to op_snake converter
- `count_xrom_test_mentions_dual(variant, fn_name, tests_dir, prefix, extra_file, src_module_dir) -> usize`
- Time module uses `time_variant_to_fn_name()` which strips "Time" prefix before conversion
- Advantage Pac does NOT strip "Adv" prefix — `AdvBinin` -> `op_adv_binin` directly via `pascal_to_op_snake`

From hp41-core/tests/lint_xrom_assertions.rs:
- `collect_xrom_test_content(tests_dir, manifest_dir) -> Vec<(PathBuf, String)>`
- `collect_external_files(tests_dir, prefix, extra_file, files)` — collects tests/<prefix>*.rs
- `collect_inline_cfg_test_blocks(src_dir, files)` — collects #[cfg(test)] blocks from source

From hp41-core/src/ops/math1/xrom.rs:
- `fn adv_a_resolve(name: &str) -> Option<Op>` — 63 variants (XROM 22, bit 3)
- `fn adv_b_resolve(name: &str) -> Option<Op>` — 51 variants (XROM 24, bit 4)
- Total Advantage variants: 114 (63 + 51)

From hp41-core/tests/adv_backward_compat.rs (existing):
- Already has 3 tests: `v32_save_loads_with_advantage_migration`, `v32_save_advantage_fields_default_cleanly`, `v32_save_time_fields_preserved`
- Fixture: `hp41-core/tests/fixtures/v32-autosave.json` with `"xrom_modules": 7`

From hp41-gui/e2e/smoke.spec.js:
- `invokeBackend(command, args)` — direct Tauri IPC invocation
- `clickKey(keyId)` — DOM click dispatch on SVG key
- Existing tests: 2+3=5, SINH, MATRIX DET, DDAYS, NORMD (5 total)
- Assertion pattern: `view.display_str` from CalcStateView response
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend xrom_op_test_count.rs and lint_xrom_assertions.rs for Advantage Pac</name>
  <files>hp41-core/tests/xrom_op_test_count.rs, hp41-core/tests/lint_xrom_assertions.rs</files>
  <action>
    Extend hp41-core/tests/xrom_op_test_count.rs to scan the two Advantage Pac resolver functions (`adv_a_resolve` and `adv_b_resolve`) in xrom.rs. The Advantage Pac has inline tests only (322 tests across 13 files in `src/ops/advantage/*.rs`, 0 external `tests/adv_*.rs` test files besides `adv_backward_compat.rs` which is not module-specific).

    Additions to xrom_op_test_count.rs:

    1. Add `collect_adv_a_variant_names()` calling `collect_variants_in_fn(xrom_src, "fn adv_a_resolve")`.
    2. Add `collect_adv_b_variant_names()` calling `collect_variants_in_fn(xrom_src, "fn adv_b_resolve")`.
    3. Add `adv_variant_to_fn_name(variant: &str) -> String` — for Advantage Pac, the "Adv" prefix is NOT stripped (unlike Time which strips "Time"). `AdvBinin` converts to `op_adv_binin` via the existing `pascal_to_op_snake()`. This function simply delegates to `pascal_to_op_snake(variant)` (same as `stat1_variant_to_fn_name`).
    4. Add `count_adv_a_test_mentions()` and `count_adv_b_test_mentions()` calling `count_xrom_test_mentions_dual()` with:
       - `tests_dir` + prefix `"adv_"` (picks up `adv_backward_compat.rs` as external)
       - `src_module_dir` = `src/ops/advantage` (picks up 322 inline tests)
       - No `extra_file` (no numerical_accuracy.rs dependency)
    5. Extend `each_xrom_op_has_at_least_5_tests()` with two new module sections:
       - ADV_MATH_A: sanity assert `adv_a_variants.len() == 63`, iterate + count + push failures as `[AdvA]`
       - ADV_MATH_B: sanity assert `adv_b_variants.len() == 51`, iterate + count + push failures as `[AdvB]`
    6. Update the doc comment header to mention 5 XROM modules and total 220 variants (45 + 26 + 35 + 63 + 51).
    7. Add unit tests for `adv_variant_to_fn_name` in the `unit_tests` module: `AdvBinin` -> `op_adv_binin`, `AdvExpZ` -> `op_adv_exp_z`, `AdvZPowN` -> `op_adv_z_pow_n`, `AdvMMulM` -> `op_adv_m_mul_m`, `AdvTvmStarI` -> `op_adv_tvm_star_i`.

    Extend hp41-core/tests/lint_xrom_assertions.rs to include Advantage Pac test surfaces:

    1. In `collect_xrom_test_content()`, add a fourth module section after Time:
       - External `tests/adv_*.rs` files via `collect_external_files(tests_dir, "adv_", None, &mut files)`
       - Inline `#[cfg(test)]` blocks from `src/ops/advantage/*.rs` via `collect_inline_cfg_test_blocks(&src_ops_dir.join("advantage"), &mut files)`
    2. Update the doc comment header to reference 5 XROM modules (Math 1 + Stat 1 + Time + ADV MATH A + ADV MATH B) and mention Phase 47 / ADV-QUAL-02.
    3. Update the scan scope comments on both `#[test]` functions to list the 5th module surface.

    IMPORTANT: The existing 3-module scan code for Math 1, Stat 1, and Time MUST remain unchanged. Only ADD the Advantage Pac sections.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test xrom_op_test_count -- --nocapture 2>&1 | tail -20 && cargo test -p hp41-core --test lint_xrom_assertions -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <done>Unified xrom_op_test_count.rs scans 220 variants across 5 modules (45+26+35+63+51), all >= 5 test mentions; lint_xrom_assertions.rs scans all 5 module test trees with zero offenders</done>
</task>

<task type="auto">
  <name>Task 2: Verify backward compat, E2E smoke, Free42 guard, and README hard-claim</name>
  <files>hp41-core/tests/adv_backward_compat.rs, hp41-gui/e2e/smoke.spec.js, README.md</files>
  <action>
    Four deliverables in this task:

    **1. Verify/extend adv_backward_compat.rs (ADV-QUAL-05):**
    The existing file at `hp41-core/tests/adv_backward_compat.rs` already has 3 tests covering xrom_modules migration (7 -> 0b11111), field defaults (adv_matrices empty, adv_matrix_i/j = 0, adv_tvm_state = None), and time fields preserved (time_offset_secs = 3600, clock_12h = true). Review these against the ADV-QUAL-05 requirement: "v3.2 save files (xrom_modules = 0b0111) load and migrate to 0b11111 without data loss; adv_matrices defaults to empty; existing rand_seed and time_offset_secs are preserved."

    The existing tests already cover xrom_modules migration, adv_matrices defaulting, and time_offset_secs preservation. However, the requirement explicitly asks that `rand_seed` is also verified as preserved. Add a 4th test `v32_save_rand_seed_preserved` that asserts `state.rand_seed.inner() == Decimal::from_str("0.5").unwrap()` (the v32-autosave.json fixture has `"rand_seed": "0.5"`). Add necessary `use` imports for `Decimal` and `FromStr`. Mark with `// LINT-EXEMPT: exact Decimal equality -- rand_seed is constructed via Decimal, not f64`.

    **2. Extend E2E smoke with Advantage Pac workflow (ADV-QUAL-06):**
    Add a 6th E2E test to `hp41-gui/e2e/smoke.spec.js` for an Advantage Pac MDET workflow. This mirrors the Phase 42 DDAYS test pattern (pure-backend invokeBackend path).

    The new `it()` block:
    - Title: `'XEQ "MDET" on 2x2 named matrix displays -2.0000 (Advantage Pac linear algebra)'`
    - Strategy: pure invokeBackend path (no clickKey mixing to avoid race conditions on CI).
    - Setup:
      a. Create a 2x2 named matrix "A": set ALPHA to "A" via `xeq_CLA` then append "A" via `dispatch_op` with `keyId: 'xeq_AIP'` after entering 65 (ASCII 'A') to X. Actually, simpler: dispatch `xeq_ALPHA_A` if available, or use the direct alpha approach. The simplest path: use invokeBackend to dispatch op sequences that create a named matrix with MATDIM (rows=2, cols=2 in X/Y, "A" in ALPHA), then store elements via MS with index manipulation.
      b. Alternative simpler approach for E2E: use the MATRX modal workflow. BUT modal workflows require specific step sequencing. The cleanest approach for a deterministic E2E test is:
         - Set up ALPHA register to "A": dispatch `dispatch_op` with `keyId: 'alpha_mode'` to enter alpha mode, then dispatch `dispatch_op` with `keyId: 'alpha_A'` to type "A", then dispatch `dispatch_op` with `keyId: 'alpha_mode'` to exit alpha mode.
         - Enter rows=2 in Y: `dispatch_op('2')`, `dispatch_op('enter')`.
         - Enter cols=2 in X: `dispatch_op('2')`.
         - Dispatch `xeq_MATDIM` to create the matrix.
         - Set elements: dispatch xeq_MSIJ style, or use I+/J+/MS sequences. The simplest: use dispatch_op sequences: Enter 1, dispatch `xeq_MS` (stores 1 at [1,1]); dispatch `xeq_J+` (advance col); enter 2, dispatch `xeq_MS` (stores 2 at [1,2]); dispatch `xeq_I+` and reset j: enter 3, dispatch `xeq_MSIJ` after entering i=2,j=1; etc.
         - Actually, the CLEANEST approach given the E2E constraints: use `xeq_MDET` on the matrix after populating it via MSIJ calls. Each MSIJ takes Y=i, X=j from the stack, with the value already stored... wait, MSIJ stores X to element at explicit I,J coordinates. Check the convention: MSIJ reads row from Y, col from T, and value from... no. Let me think about the actual Advantage Pac MSIJ semantics: it reads element indices from the stack.

         Actually, the simplest E2E test that exercises the Advantage Pac path would be to test something non-matrix, like a base conversion. `BININ` converts a binary string in ALPHA to integer in X. Example: ALPHA="101" -> BININ -> X=5.

         Revised approach for simplicity and CI robustness:
         - Use BININ as the E2E test: set ALPHA to "1010" (binary 10), dispatch xeq_BININ, assert display_str = "10.0000".
         - This is deterministic, requires no matrix setup, and exercises the Advantage Pac XROM resolution path.

      Revised test spec:
      - Title: `'XEQ "BININ" converts binary "1010" to 10 (Advantage Pac base conversion)'`
      - Steps:
        a. Enter alpha mode, type "1010", exit alpha mode.
        b. `invokeBackend('dispatch_op', { keyId: 'xeq_BININ' })`.
        c. Assert `view.display_str === '10.0000'`.
      - Alpha mode entry via `dispatch_op` with keyId `'alpha_mode'`, then individual characters via `dispatch_op` with `'alpha_1'`, `'alpha_0'`, etc., then exit with `'alpha_mode'` again.
      - Check that the key IDs for alpha digits exist. The key_map should handle `alpha_1` and `alpha_0`. If not, use `dispatch_op` with the actual key id convention from key_map.rs.

      Read `hp41-gui/src-tauri/src/key_map.rs` to confirm the alpha key id convention before writing the test. The pattern from existing code: `'alpha_mode'` toggles ALPHA mode. Within ALPHA mode, keys produce their alpha character. So to enter "1010" in ALPHA, enter alpha mode, then dispatch keys '1', '0', '1', '0' (which in alpha mode append their alphaChar to the ALPHA register), then exit alpha mode.

      However, the IPC `dispatch_op` goes through key_map::resolve which may not handle alpha mode digit entry the same way. The safer approach: enter characters via the AIP (Alpha Integer Prepend/append) operation from the Advantage Pac itself. `AIP` takes X register value (ASCII code) and appends that character to ALPHA. So:
        a. Dispatch CLA (clear ALPHA).
        b. Enter 49 (ASCII '1'), dispatch xeq_AIP. Enter 48 (ASCII '0'), dispatch xeq_AIP. Enter 49, dispatch xeq_AIP. Enter 48, dispatch xeq_AIP. ALPHA now = "1010".
        c. Dispatch xeq_BININ. Assert display_str = "10.0000".
      This is clean, deterministic, and exercises TWO Advantage Pac ops (AIP + BININ).

    Place the new test after the existing NORMD test, inside the existing `describe` block. Comment header referencing Plan 47-01 / ADV-QUAL-06.

    **3. Verify Free42 contamination guard (ADV-QUAL-07):**
    Run `bash scripts/check-free42-contamination.sh` and confirm it exits 0. The Phase 43 execution already extended the script to cover `advantage/`. This is a re-verification task, not a code change. If the guard fails, diagnose and fix.

    **4. README hard-claim graduation (ADV-QUAL-09):**
    In README.md, find the current v3.3 soft-claim line (line ~59):
    `- v3.3 ships Advantage Pac behavioral emulation (~117 XEQ entry points across...`
    Change it to include the hard-claim:
    `- v3.3 ships Advantage Pac behavioral emulation, feature-complete per Owner's Manual 00041-90482 (~117 XEQ entry points across...`
    The key insertion is "feature-complete per Owner's Manual 00041-90482" after "behavioral emulation," — same placement as v3.0 Math Pac I, v3.1 Stat 1 Pac, and v3.2 Time Pac lines.

    After all changes, run final quality-gate verification:
    1. `cargo test -p hp41-core` — full core test suite
    2. `cargo test -p hp41-cli` — CLI tests including function_matrix_parity 5-pool
    3. `bash scripts/check-free42-contamination.sh` — Free42 guard
    4. `cargo clippy -p hp41-core -- -D warnings` — clippy clean
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test adv_backward_compat -- --nocapture 2>&1 | tail -10 && grep "feature-complete per Owner's Manual 00041-90482" README.md && grep -c "BININ" hp41-gui/e2e/smoke.spec.js && bash scripts/check-free42-contamination.sh 2>&1 | tail -3</automated>
  </verify>
  <done>Backward compat test passes (4 tests including rand_seed preservation); E2E smoke extended with Advantage Pac BININ workflow (6 tests total); Free42 guard exits 0; README carries v3.3 hard-claim</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infrastructure-only | Phase 47 has zero user-facing changes beyond README text; all deliverables are tests, CI gates, and documentation |
| E2E smoke | Runs on Ubuntu-only CI runner; no cross-platform exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | xrom_op_test_count.rs | mitigate | Word-boundary matching (WR-03) prevents substring inflation; sanity assertions on per-module variant counts (63/51) catch scope leakage |
| T-47-02 | Tampering | lint_xrom_assertions.rs | mitigate | LINT-EXEMPT requires inline rationale per T-32-04; preceding_block_has_lint_exempt enforces adjacency |
| T-47-03 | Tampering | v32-autosave.json fixture | mitigate | Fixture is a minimal CalcState snapshot; no secrets; verified by 4 specific assertions in adv_backward_compat.rs |
| T-47-04 | Repudiation | README hard-claim | mitigate | Hard-claim only added after ALL quality gates pass (coverage, accuracy, E2E, contamination); graduation is conditional |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; zero package installs |
</threat_model>

<verification>
cargo test -p hp41-core passes (all extended meta-gates + backward compat + existing tests)
cargo test -p hp41-cli passes (function_matrix_parity 5-pool)
bash scripts/check-free42-contamination.sh exits 0
README.md contains "feature-complete per Owner's Manual 00041-90482"
grep -c "BININ" hp41-gui/e2e/smoke.spec.js returns >= 1
</verification>

<success_criteria>
- Unified xrom_op_test_count.rs scans 220 variants across 5 modules, all >= 5 test mentions
- Unified lint_xrom_assertions.rs scans all 5 module test trees, zero offenders
- adv_backward_compat.rs has 4 tests including rand_seed preservation
- E2E smoke has 6 tests including Advantage Pac BININ workflow
- Free42 contamination guard exits 0 covering advantage/ directory
- README carries v3.3 hard-claim "feature-complete per Owner's Manual 00041-90482"
- Full cargo test -p hp41-core and cargo test -p hp41-cli pass without regressions
</success_criteria>

<output>
Create `.planning/phases/47-test-hardening-quality-gates/47-01-SUMMARY.md` when done
</output>
