---
phase: 42-test-hardening-quality-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 42-02
files_modified:
  - hp41-core/tests/time_date_accuracy.rs
  - hp41-core/tests/time_stopwatch_timing.rs
  - hp41-core/tests/time_alarm_latency.rs
autonomous: true
requirements:
  - TIME-QUAL-02
  - TIME-QUAL-10
  - TIME-QUAL-11

must_haves:
  truths:
    - "Date arithmetic accuracy suite covers leap years (Feb 29 2000, 2100, 2400), century boundaries, Gregorian start (Oct 15 1582), and the DATE+/DDAYS/DOW trio with oracle-verified values"
    - "Stopwatch timing test verifies Instant-based elapsed time accuracy within +/-10ms over a short CI-safe test window"
    - "Alarm past-due detection fires within one dispatch cycle"
  artifacts:
    - path: "hp41-core/tests/time_date_accuracy.rs"
      provides: "Oracle-verified date arithmetic edge cases for DATE+/DDAYS/DOW"
      contains: "#[test]"
    - path: "hp41-core/tests/time_stopwatch_timing.rs"
      provides: "Stopwatch timing accuracy test using Instant monotonic clock"
      contains: "#[test]"
    - path: "hp41-core/tests/time_alarm_latency.rs"
      provides: "Alarm past-due detection fires within one dispatch cycle"
      contains: "#[test]"
  key_links:
    - from: "hp41-core/tests/time_date_accuracy.rs"
      to: "hp41-core/src/ops/time/date_arith.rs"
      via: "op_date_plus, op_ddays, op_dow exercised with oracle edge cases"
      pattern: "dispatch.*Op::TimeDatePlus"
    - from: "hp41-core/tests/time_stopwatch_timing.rs"
      to: "hp41-core/src/ops/time/stopwatch.rs"
      via: "op_runsw + sleep + op_rclsw timing measurement"
      pattern: "dispatch.*Op::TimeRunsw"
    - from: "hp41-core/tests/time_alarm_latency.rs"
      to: "hp41-core/src/ops/time/alarm.rs"
      via: "check_alarms + event_buffer drain within single dispatch cycle"
      pattern: "check_alarms"
---

<objective>
Create three focused test files: (1) date arithmetic accuracy suite with oracle-verified edge cases per TIME-QUAL-02, (2) stopwatch timing accuracy test per TIME-QUAL-10, (3) alarm past-due detection latency test per TIME-QUAL-11.

Purpose: These are the numerical/behavioral accuracy gates analogous to Phase 32's numerical_accuracy.rs extension and Phase 37's accuracy tests. Date arithmetic is deterministic integer math (exact-match, no tolerance per D-42.4), stopwatch uses Instant for monotonic timing, alarm latency verifies check_alarms fires within one dispatch cycle.

Output: Three test files covering the three remaining quality requirements that involve behavioral verification.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-test-hardening-quality-gates/42-CONTEXT.md
@.planning/phases/42-test-hardening-quality-gates/42-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create date arithmetic accuracy suite</name>
  <files>hp41-core/tests/time_date_accuracy.rs</files>
  <read_first>
    - hp41-core/src/ops/time/date_arith.rs (DATE+/DDAYS/DOW implementations, JDN formula, parse_date_hpnum, date_to_hpnum; existing inline tests for structural reference)
    - hp41-core/tests/numerical_accuracy.rs (case! macro structure — for reference only; D-42.4 says exact-match so the macro's tolerance infrastructure is not needed)
  </read_first>
  <action>
    Create hp41-core/tests/time_date_accuracy.rs with ~20-30 oracle-verified date arithmetic test cases per D-42.4, D-42.5, D-42.6, and D-42.12.

    The file must:
    1. Carry `#![allow(clippy::unwrap_used)]` per D-carried.3.
    2. Carry the Time Module Free42 disclaim header.
    3. Use `use hp41_core::{ops::{dispatch, Op}, state::CalcState, num::HpNum}` and `use rust_decimal::Decimal` + `std::str::FromStr`.
    4. Use EXACT-MATCH assertions (assert_eq!, not approx) since calendar arithmetic is deterministic integer math per D-42.4.
    5. Oracle source per D-42.11: Python datetime/calendar for derivation convenience; published Meeus JDN tables as sanity-check for boundary cases.

    Required edge cases per TIME-QUAL-02:
    - **Leap year Feb 29 2000:** DATE+ (Feb 28 + 1 = Feb 29), DOW (Feb 29 2000 = Tuesday = 2), DDAYS across leap day
    - **Non-leap century 2100:** DATE+ (Feb 28 + 1 = Mar 1), DDAYS across Feb 28/Mar 1
    - **Leap century 2400:** DATE+ (Feb 28 + 1 = Feb 29), confirming 2400 IS a leap year (divisible by 400)
    - **Gregorian start Oct 15 1582:** DOW (Oct 15 1582 = Friday = 5), DATE+ forward/backward from this date
    - **Y2K boundary:** DDAYS from Dec 31 1999 to Jan 1 2000 = 1 day
    - **Large offset:** DATE+ from Jan 1 2000 + 36525 days = Jan 1 2100 (100 years including leap days)
    - **Known DOW historical dates:** July 4 1776 (Thursday = 4), Jan 1 2000 (Saturday = 6), Sep 11 2001 (Tuesday = 2)
    - **DMY mode tests:** At least 2-3 cases in DMY mode (Flag 31 set) verifying the format roundtrip
    - **Negative DDAYS:** X later than Y produces negative count
    - **Same-date DDAYS:** Yields 0
    - **DATE+ negative days:** DATE+ with negative X value going backward in time

    Helper function pattern: create a `setup_date_state(y_date_str: &str, x_val_str: &str, dmy: bool) -> CalcState` helper for concise test setup. For DOW tests: `setup_dow_state(date_str: &str, dmy: bool)`. Parse date strings via `Decimal::from_str` into HpNum.

    Extract result comparison via `parse_date_hpnum` for DATE+ (compare as (year, month, day) tuple) and integer comparison for DDAYS/DOW (extract via `.inner().trunc().to_i64()`).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test time_date_accuracy -- --nocapture 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/time_date_accuracy.rs exists with >= 20 test functions
    - Tests cover all required edges: Feb 29 2000, 2100 non-leap, 2400 leap, Gregorian start Oct 15 1582, Y2K boundary, large offsets, known DOW historical dates, DMY mode, negative DDAYS, same-date DDAYS, DATE+ negative days
    - All assertions are exact-match (assert_eq!, no tolerance)
    - cargo test -p hp41-core --test time_date_accuracy exits 0
  </acceptance_criteria>
  <done>Date arithmetic accuracy suite passes with 20+ oracle-verified edge cases covering all TIME-QUAL-02 requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create stopwatch timing accuracy and alarm latency tests</name>
  <files>hp41-core/tests/time_stopwatch_timing.rs, hp41-core/tests/time_alarm_latency.rs</files>
  <read_first>
    - hp41-core/src/ops/time/stopwatch.rs (op_runsw, op_stopsw, op_rclsw, op_setsw, op_sw, op_swpt, op_stpw, StopwatchMode enum, stopwatch_start/accumulated/split fields)
    - hp41-core/src/ops/time/alarm.rs (check_alarms, event_buffer, AlarmEntry, AlarmType, past-due detection logic)
    - hp41-core/src/state.rs (CalcState fields: stopwatch_mode, stopwatch_accumulated, stopwatch_start, stopwatch_split, alarms, event_buffer)
  </read_first>
  <action>
    Create two test files per D-42.7 and D-42.13:

    **hp41-core/tests/time_stopwatch_timing.rs:**
    1. Carry `#![allow(clippy::unwrap_used)]` per D-carried.3.
    2. Short-window CI test (1-2 seconds, +/-5ms tolerance per D-42.13):
       - Dispatch Op::TimeRunsw to start stopwatch.
       - Sleep for a known duration (e.g., `std::thread::sleep(Duration::from_millis(1000))`).
       - Dispatch Op::TimeStopsw to stop.
       - Dispatch Op::TimeRclsw to recall elapsed time.
       - Parse the recalled HH.MMSScc value via `parse_time_hpnum` or direct Decimal extraction.
       - Assert elapsed centiseconds are within +/-5 of the expected 100 centiseconds (1 second).
    3. A `#[ignore]` 60-second variant for manual validation per D-42.13 (documented that the 60s +/-10ms claim follows from Instant monotonicity + shorter-window proof).
    4. Additional test: stopwatch split (SWPT) records split time while stopwatch continues running.
    5. Additional test: stopwatch reset (STPW) zeros accumulated time.

    **hp41-core/tests/time_alarm_latency.rs:**
    1. Carry `#![allow(clippy::unwrap_used)]` per D-carried.3.
    2. Create an alarm entry with a past-due time (time in the past relative to system clock + offset).
    3. Call `state.check_alarms()` once.
    4. Assert that event_buffer is non-empty (alarm fired within one dispatch cycle).
    5. Additional test: verify that a future alarm does NOT fire when check_alarms is called.
    6. Additional test: verify ALMNOW fires immediately (event_buffer populated after one dispatch of Op::TimeAlmnow).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core --test time_stopwatch_timing -- --nocapture 2>&amp;1 | tail -10 && cargo test -p hp41-core --test time_alarm_latency -- --nocapture 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/time_stopwatch_timing.rs exists and cargo test exits 0 (short-window test passes within +/-5ms tolerance; #[ignore] 60s test present but not run in CI)
    - hp41-core/tests/time_alarm_latency.rs exists and cargo test exits 0
    - Stopwatch timing test verifies 1-second elapsed within +/-5 centiseconds
    - Alarm latency test verifies past-due alarm fires within one check_alarms call
    - Alarm latency test verifies future alarm does NOT fire
  </acceptance_criteria>
  <done>Stopwatch timing accuracy (TIME-QUAL-10) and alarm past-due detection latency (TIME-QUAL-11) tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infrastructure-only | All tests exercise existing core logic; no user-facing changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Information Disclosure | time_date_accuracy.rs | accept | Date arithmetic tests use public calendar dates; no PII |
| T-42-05 | Denial of Service | time_stopwatch_timing.rs | mitigate | Short-window test (1s) is CI-safe; 60s variant is #[ignore]; no infinite loops |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (D-carried.4) |
</threat_model>

<verification>
cargo test -p hp41-core --test time_date_accuracy passes
cargo test -p hp41-core --test time_stopwatch_timing passes (short-window only in CI)
cargo test -p hp41-core --test time_alarm_latency passes
</verification>

<success_criteria>
- Date accuracy suite: 20+ oracle-verified cases covering all TIME-QUAL-02 edges, all exact-match
- Stopwatch timing: 1s window within +/-5cs, #[ignore] 60s variant documented
- Alarm latency: past-due fires in one cycle, future does not fire
</success_criteria>

<output>
Create `.planning/phases/42-test-hardening-quality-gates/42-03-SUMMARY.md` when done
</output>
