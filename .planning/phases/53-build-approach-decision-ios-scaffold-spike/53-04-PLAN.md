---
phase: 53-build-approach-decision-ios-scaffold-spike
plan: 04
type: execute
wave: 3
depends_on: ["53-02", "53-03"]
files_modified: []
autonomous: false
requirements: [BUILD-03]
user_setup:
  - service: apple-developer-signing
    why: "A debug build must be signed with the developer's team before it can install on a physical iPhone (SC-3); the team is selected in Xcode (human-only)"
    dashboard_config:
      - task: "Connect + trust the physical iPhone (iOS 17+) and add it as a network device"
        location: "Xcode → Window → Devices and Simulators"
      - task: "Select the signing team for the ch.talent-factory.hp41 target"
        location: "Xcode → target → Signing & Capabilities → Team"
must_haves:
  truths:
    - "A signed debug build installs and launches on a physical iPhone connected via Xcode"
    - "The RPN smoke sequence 2 ENTER 3 + then SIN dispatches through hp41-core on the device"
    - "The signing team was selected by the human in Xcode for the registered bundle ID"
  artifacts:
    - path: ".planning/phases/53-build-approach-decision-ios-scaffold-spike/53-04-SUMMARY.md"
      provides: "record of the on-device install + smoke result (no secrets)"
  key_links:
    - from: "device touch (key tap)"
      to: "hp41-core dispatch via CalcStateView"
      via: "key_map.resolve() → invoke(dispatch_op) → WKWebView → Rust → React re-render"
      pattern: "dispatch_op"
    - from: "Xcode signing team"
      to: "registered App ID ch.talent-factory.hp41 (Plan 02)"
      via: "automatic signing resolves a development provisioning profile"
      pattern: "ch.talent-factory.hp41"
---

<objective>
Install and run the signed debug build on a physical iPhone (SC-3) and re-prove the
touch→engine loop on real hardware. This requires three human-only steps — connecting +
trusting the device, selecting the signing team in Xcode, and confirming the device
install — modeled as blocking checkpoints (D-53.7). Depends on Plan 02 (bundle ID
registered → signing unblocked) and Plan 03 (approach confirmed, scaffold builds).

Purpose: Satisfy BUILD-03 (the app builds, installs, and runs on a physical iPhone).
Output: an on-device install + verified RPN smoke run (no files changed, no secrets).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-02-SUMMARY.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-03-SUMMARY.md

<interfaces>
<!-- Same data-flow loop as the Simulator, now on real hardware. -->
device touch → key_map.resolve() → invoke("dispatch_op") → WKWebView bridge
  → Rust command → CalcState → CalcStateView → React re-render.

Device-debug pitfalls to honor:
  P-iOS-02 — connect+trust the iPhone in Xcode → Devices and Simulators (network) BEFORE
             running on device; use --force-ip-prompt if the LAN handshake fails.
  P-iOS-09 — Xcode build phase needs ~/.cargo/bin on PATH (gen/apple/.xcode.env.local).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Connect + trust the iPhone and select the signing team in Xcode</name>
  <read_first>
    - .planning/research/PITFALLS.md (P-iOS-02 physical-device debug LAN/Devices setup; P-iOS-09 cargo-on-PATH)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-02-SUMMARY.md (confirms the App ID is registered)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md (D-53.7 — human-only steps; D-53.10 — physical iPhone on iOS 17+ available)
  </read_first>
  <action>
    HUMAN-ONLY checkpoint (no CLI path provisions device-trust or Xcode team selection this
    phase). Follow the click-by-click steps in <how-to-verify>: connect + trust the iPhone
    (iOS 17+), network-enable it in Xcode → Devices and Simulators (P-iOS-02), open the
    generated Xcode project at `hp41-gui/src-tauri/gen/apple/`, and select the Apple Developer
    Team under the app target's Signing & Capabilities so automatic signing resolves a
    development provisioning profile for `ch.talent-factory.hp41`. Do NOT download or commit
    any distribution certificate, provisioning profile, or secret (Phase 57 owns distribution).
  </action>
  <what-built>
    Plan 01 generated `gen/apple/` and Plan 03 confirmed it builds. Opening the generated
    Xcode workspace and selecting a signing team is a human-only Xcode UI action (no CLI
    path provisions the development team selection in this phase).
  </what-built>
  <how-to-verify>
    1. Plug the iPhone (iOS 17+) into the Mac via cable. On the phone, tap "Trust This Computer".
    2. Xcode → Window → Devices and Simulators → select the device → enable "Connect via network"
       so the Tauri dev/build path can reach it over LAN (P-iOS-02).
    3. Open the generated Xcode project at `hp41-gui/src-tauri/gen/apple/` (the `.xcodeproj`).
    4. Select the app target → "Signing & Capabilities" tab → ensure "Automatically manage
       signing" is on → set "Team" to your Apple Developer team. Bundle Identifier must read
       `ch.talent-factory.hp41` (already registered in Plan 02), letting Xcode auto-create a
       development provisioning profile.
    5. Confirm Xcode shows no signing error ("Signing for ... requires a development team" must
       be resolved) and the device appears as a run destination.
    Do NOT download/commit any distribution certificate or provisioning profile (Phase 57
    handles distribution signing). Do NOT paste secrets into the repo.
  </how-to-verify>
  <verify>
    <human-check>iPhone (iOS 17+) is trusted and network-visible in Xcode → Devices and Simulators; the app target shows a selected Team with no unresolved signing error; Bundle Identifier reads ch.talent-factory.hp41; no secret committed.</human-check>
  </verify>
  <acceptance_criteria>
    - The iPhone (iOS 17+) is connected, trusted, and visible in Xcode → Devices and Simulators (network-enabled)
    - The app target's Signing & Capabilities shows a selected Team and no unresolved signing error
    - Bundle Identifier in the target reads `ch.talent-factory.hp41`
    - No certificate, provisioning profile, or secret was committed to the repo
  </acceptance_criteria>
  <done>Device trusted/visible in Xcode; signing team selected for ch.talent-factory.hp41 with no signing error; no secret committed.</done>
  <resume-signal>Type "signed" once the device is trusted/visible and the signing team is selected with no signing error, or describe the blocker (e.g., untrusted device, PATH/cargo error per P-iOS-09).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install on device and run the RPN smoke sequence (SC-3)</name>
  <read_first>
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md (D-53.8 — the smoke sequence and the loop it proves)
    - .planning/research/PITFALLS.md (P-iOS-02 — device-only runtime crashes; use the build→install path)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-03-SUMMARY.md (Simulator smoke baseline to compare against)
  </read_first>
  <action>
    The executor builds + installs the signed debug build onto the connected iPhone (prefer
    Xcode's Run on the device destination for a first install to avoid the P-iOS-02 dev-server
    crash mode; `just ios-dev` targeting the device is acceptable once the LAN handshake is set
    up). The human then confirms on-device behavior per <how-to-verify>: app launches (HP-41 skin
    visible, not blank), the smoke sequence `2 ENTER 3 +` shows `5`, and `SIN` updates X to the
    sine of 5 with the stack dropping — matching the Plan 03 Simulator result and proving the full
    touch → key_map.resolve() → invoke("dispatch_op") → WKWebView → Rust → CalcStateView → React
    loop on real hardware (BUILD-03 / SC-3).
  </action>
  <what-built>
    The build pipeline (`just ios-build` / the generated Xcode project) produces a signed
    debug build for the connected device. The executor runs the build/install; the human
    confirms the on-device behavior.
  </what-built>
  <how-to-verify>
    1. Build + install the debug build onto the connected iPhone (run from Xcode's Run on the
       device destination, or `just ios-dev` targeting the device if the LAN handshake is set up
       per P-iOS-02; prefer the Xcode Run path for a first device install to avoid the
       dev-server crash mode).
    2. The app launches on the iPhone (HP-41 skin renders; not a blank/white screen).
    3. On the device, tap `2`, `ENTER`, `3`, `+` → the display / X register shows `5`.
    4. Tap `SIN` → X updates to the sine of 5 (current angular mode) and the stack drops.
    5. Confirm this matches the Simulator result from Plan 03 — proving the full loop
       touch → key_map.resolve() → invoke("dispatch_op") → WKWebView → Rust → CalcStateView →
       React works on real hardware (BUILD-03 / SC-3).
  </how-to-verify>
  <verify>
    <human-check>On the physical iPhone, the app launches with the HP-41 UI visible; 2 ENTER 3 + shows 5; SIN updates X to sin(5) and the stack drops — matching the Plan 03 Simulator result.</human-check>
  </verify>
  <acceptance_criteria>
    - The signed debug build installs and launches on the physical iPhone (HP-41 UI visible, not blank)
    - `2 ENTER 3 +` shows `5` on the device display / X register
    - `SIN` updates X to the sine of 5 and the stack drops — engine dispatch confirmed on device
    - The on-device behavior matches the Plan 03 Simulator result; recorded in the SUMMARY (screenshot/photo path optional)
  </acceptance_criteria>
  <done>Signed debug build installs + launches on the iPhone; the RPN smoke sequence dispatches through hp41-core on hardware; observation recorded.</done>
  <resume-signal>Type "approved" once the app installs on the iPhone and the RPN smoke sequence dispatches correctly on device, or describe the failure (e.g., installs but crashes — see P-iOS-02 root-cause checklist).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mac ↔ physical iPhone (USB + LAN) | trusted-device pairing + Tauri dev LAN handshake |
| Xcode automatic signing → Apple developer account | development provisioning profile creation |
| device WKWebView → Rust bridge | same IPC surface as desktop (no new network surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-10 | Spoofing | device trust pairing | accept | Human physically trusts a known device over USB; standard Apple pairing flow, developer-controlled hardware. |
| T-53-11 | Information disclosure | development signing material | mitigate | Only Xcode-managed automatic DEVELOPMENT signing is used; no distribution cert/profile is downloaded or committed (Phase 57 scope). Acceptance asserts no secret enters the repo. |
| T-53-12 | Tampering | Tauri dev LAN handshake (P-iOS-02) | accept | Local LAN dev path on a developer network; first device install prefers the Xcode Run path. No exposed service beyond local development. |
| T-53-SC | Tampering | no package installs in this plan | accept | This plan installs no packages; reuses the Plan 01 toolchain. No `[ASSUMED]`/`[SUS]` packages → no blocking legitimacy checkpoint. |
</threat_model>

<verification>
- iPhone trusted + visible in Xcode; signing team selected with no error; bundle ID matches.
- Signed debug build installs and launches on the device.
- Device smoke `2 ENTER 3 +` → 5, then `SIN` updates X + stack (engine dispatch on hardware).
- No signing secret committed.
</verification>

<success_criteria>
The signed debug build installs and runs on a physical iPhone, and the RPN smoke sequence
dispatches through hp41-core on real hardware — BUILD-03 / SC-3 satisfied. With Plans 01–03,
all four Phase-53 success criteria are met.
</success_criteria>

<output>
Create `.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-04-SUMMARY.md` when done.
Record the on-device install + smoke observation. NEVER record signing secrets.
</output>
