---
phase: 53-build-approach-decision-ios-scaffold-spike
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - docs/adr/v4.1-002-build-approach.md
autonomous: true
requirements: [BUILD-01, BUILD-02]
must_haves:
  truths:
    - "cargo tauri ios build was run inside hp41-gui/ and its outcome (clean build vs Xcode-assembly path failure) is recorded"
    - "if the build failed at the Xcode assembly step, a time-boxed set of bounded workarounds was attempted before declaring Approach A dead"
    - "the chosen build approach is decided strictly by the failure STAGE (Rust compile vs Xcode assembly)"
    - "the app launches in the iOS Simulator and the smoke sequence 2 ENTER 3 + then SIN updates the display and stack"
    - "ADR docs/adr/v4.1-002-build-approach.md records the chosen approach and cites the research artifacts"
    - "any diagnostic workspace-flattening was reverted and NOT committed"
  artifacts:
    - path: "docs/adr/v4.1-002-build-approach.md"
      provides: "the build-approach decision record keyed on the observed spike failure stage"
      contains: "Approach"
      min_lines: 40
  key_links:
    - from: "Simulator touch (key tap)"
      to: "hp41-core dispatch via CalcStateView"
      via: "key_map.resolve() → invoke(dispatch_op) → WKWebView → Rust → React re-render"
      pattern: "dispatch_op"
    - from: "docs/adr/v4.1-002-build-approach.md"
      to: ".planning/research/SUMMARY.md and PITFALLS.md P-iOS-03"
      via: "ADR Footnotes / References cite, not re-derive"
      pattern: "research"
---

<objective>
Run the single gating experiment of the milestone — `cargo tauri ios build` inside the
nested `hp41-gui` workspace — and let its FAILURE STAGE decide the build approach
(D-53.4/D-53.5). Boot the resulting build in the iOS Simulator and prove the full
touch→engine loop with the RPN smoke sequence (D-53.8). Record the observed outcome in
ADR `docs/adr/v4.1-002-build-approach.md` (note: **-002**, not -001 — `v4.1-001` is the
shipped macOS menu-bar ADR).

Purpose: Confirm Approach A (Tauri v2 Mobile) or trigger the Approach B fallback, and
unblock Phases 54–57 (which cannot be planned in detail until this ADR is written).
Output: a written ADR + a Simulator-verified one-key engine dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-01-SUMMARY.md
@docs/adr/v4.1-001-macos-menu-bar-mode.md
@docs/adr/v4.0-001-xmem-os-builtin.md

<interfaces>
<!-- Data flow exercised by the smoke test — identical to desktop. -->
Touch → React key_map.resolve(keyId) → invoke("dispatch_op", {keyId})
  → WKWebView bridge → commands::dispatch_op() → handle_op_prepare(&mut CalcState, key_id)
  → CalcStateView serialized → React re-renders display + stack.

ADR section skeleton (from the two analogs):
  # ADR v4.1-002 — iOS Build Approach (Tauri v2 Mobile vs SwiftUI + UniFFI)
  **Status:** Accepted   **Date:** YYYY-MM-DD   **Context:** v4.1 Phase 53 scaffold spike
  ## Context
  ## Decision        (records OBSERVED outcome — written AFTER the spike)
  ## Consequences
  ## Alternatives considered
  ## Footnotes / References   (cite .planning/research/* — do not re-derive A vs B)

Decision criterion (D-53.5):
  clean build                                  → Approach A confirmed
  Rust compiles, Xcode assembly fails on the   → (after ½-day time-box of workarounds) Approach B
    nested-workspace path (#5865)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the decisive spike (cargo tauri ios build) — the A/B oracle</name>
  <read_first>
    - .planning/research/PITFALLS.md (P-iOS-03 nested-workspace #5865 — the gating failure; P-iOS-07 triples; P-iOS-09 cargo-on-PATH)
    - .planning/research/SUMMARY.md (headline recommendation + the A-vs-B cost comparison)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md (D-53.4 time-box, D-53.5 stage criterion, D-53.6 flattening is diagnostic-only)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-01-SUMMARY.md (the scaffold + gitignore policy from Plan 01)
  </read_first>
  <action>
    Run the spike via `just ios-build` (which runs `cargo tauri ios build` inside
    `hp41-gui/`, baking the `aarch64-apple-ios` triple). Classify the result strictly by
    STAGE per D-53.5:
    - CLEAN BUILD → Approach A confirmed. Record stage = "clean".
    - FAILURE during Rust compilation → this is NOT the #5865 signature; treat as an
      ordinary build error to fix (e.g., missing target, PATH); it does not trigger fallback.
    - FAILURE at the Xcode ASSEMBLY step with a nested-workspace path error (#5865, AFTER
      successful Rust compile) → apply a TIME-BOXED (~½ day) set of bounded workarounds per
      D-53.4: try a newer/patched `tauri-cli`, Podfile/path tweaks, an explicit `--target-dir`,
      a temporary symlink, and verify `cargo metadata --manifest-path hp41-gui/src-tauri/Cargo.toml`
      reports the expected `target_directory`. Workspace-FLATTENING is permitted ONLY as a
      throwaway LOCAL diagnostic to confirm nesting is the root cause (D-53.6) — it MUST be
      reverted and NEVER committed; if flattening is the ONLY thing that builds, that is the
      signal Approach A is REJECTED. If still failing after the time-box → Approach A is dead,
      choose Approach B.
    Capture the exact failure stage, error text, and the workarounds attempted for the ADR.
    Confirm the committed tree still has root members `["hp41-core", "hp41-cli"]` (no
    flattening leaked into git).
  </action>
  <verify>
    <automated>grep -qE 'members\s*=\s*\["hp41-core",\s*"hp41-cli"\]' Cargo.toml && git diff --name-only | grep -vq '^Cargo.toml$' || true</automated>
  </verify>
  <acceptance_criteria>
    - `just ios-build` was executed inside `hp41-gui/` and its outcome is captured verbatim (stage + error text if any)
    - The result is classified strictly by STAGE: clean → Approach A; Rust-OK/Xcode-assembly-path-fail (after ½-day time-box) → Approach B
    - If #5865 was hit, the SUMMARY lists each bounded workaround attempted within the time-box
    - Root `Cargo.toml` `members` is still exactly `["hp41-core", "hp41-cli"]` (no diagnostic flattening committed — D-53.6)
    - `git status` shows no staged change to root `Cargo.toml` or to `hp41-gui/src-tauri/Cargo.toml` `[workspace]` header
  </acceptance_criteria>
  <done>Spike run, failure stage classified, time-boxed workarounds applied if #5865 hit, approach chosen by stage, Frozen Invariant verified uncommitted-flattening.</done>
</task>

<task type="auto">
  <name>Task 2: Boot in Simulator and run the RPN smoke sequence (SC-2)</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Data Flow in Approach A (iOS) — the exact touch→engine→render loop)
    - .planning/research/PITFALLS.md (P-iOS-01 — use xcrun simctl to pick a current available device, do not hardcode)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-CONTEXT.md (D-53.8 — the smoke sequence and what it proves)
  </read_first>
  <action>
    Boot the app in the iOS Simulator via `just ios-sim` (which runs the
    `xcrun simctl list devices available` check first, then launches on a CURRENT
    non-hardcoded device per P-iOS-01 — e.g. the newest available iPhone). Perform the
    smoke sequence D-53.8 by tapping keys in the Simulator: `2`, `ENTER`, `3`, `+`
    (display/X should show `5`), then `SIN` (X should become the sine of 5 in the current
    angular mode). Confirm BOTH the display value and the stack registers update — this
    proves the full loop touch → `key_map.resolve()` → `invoke("dispatch_op")` → WKWebView
    bridge → Rust command → `CalcState` → `CalcStateView` → React re-render. If Approach B
    was chosen in Task 1, this Simulator smoke instead targets the minimal SwiftUI shell
    once it exists; for the foundation spike the Tauri-A path is the expected branch.
    Capture a note (and ideally a Simulator screenshot path) of the observed display/stack
    for the ADR and SUMMARY.
  </action>
  <verify>
    <human-check>In the iOS Simulator, tapping 2 ENTER 3 + shows 5 in X/display, then SIN updates X to the sine result and the stack drops correctly — observed and recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - The app launches in the iOS Simulator on a device chosen from `xcrun simctl list devices available` (no hardcoded device name)
    - Smoke sequence `2 ENTER 3 +` produces `5` in the X register / display
    - Subsequent `SIN` updates X to the sine of 5 and the stack drops as expected (proves engine dispatch, not a static UI)
    - The observed display + stack state is recorded in the SUMMARY (screenshot path optional)
  </acceptance_criteria>
  <done>App boots in Simulator; the RPN smoke sequence dispatches through hp41-core and updates display + stack; observation recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Write ADR v4.1-002-build-approach.md recording the observed outcome</name>
  <read_first>
    - docs/adr/v4.1-001-macos-menu-bar-mode.md (concise ADR header + Decision/Consequences/Alternatives shape — best structural match)
    - docs/adr/v4.0-001-xmem-os-builtin.md (Footnotes/References style citing planning artifacts by path)
    - .planning/phases/53-build-approach-decision-ios-scaffold-spike/53-PATTERNS.md (the ADR pattern section: cite-don't-re-derive, record observed outcome, explicit Frozen-Invariant accounting)
    - .planning/research/SUMMARY.md and .planning/research/ARCHITECTURE.md (the A-vs-B comparison to cite, NOT re-derive)
  </read_first>
  <action>
    Create `docs/adr/v4.1-002-build-approach.md` (filename MUST be `v4.1-002` — `v4.1-001` is
    the shipped macOS menu-bar ADR). Use the analog header block (`# ADR v4.1-002 — iOS Build
    Approach (Tauri v2 Mobile vs SwiftUI + UniFFI)`, `**Status:** Accepted`, dated, Context =
    v4.1 Phase 53 scaffold spike). Sections: `## Context`, `## Decision`, `## Consequences`,
    `## Alternatives considered`, `## Footnotes / References`. The Decision section records the
    OBSERVED spike outcome keyed on the failure STAGE from Task 1 (clean build → Approach A
    confirmed; Rust-OK / Xcode-assembly nested-workspace path failure after the ½-day time-box →
    Approach B). CITE `.planning/research/SUMMARY.md`, `STACK.md`, `ARCHITECTURE.md`, and
    `PITFALLS.md` P-iOS-03 for the A-vs-B comparison rather than re-deriving it. Explicitly
    account for the Frozen Invariant: state that BOTH approaches preserve root members
    `["hp41-core", "hp41-cli"]` and confine `tauri`/`tauri-build` to `hp41-gui/src-tauri/`,
    and record that D-53.6 workspace-flattening was diagnostic-only / never committed. Also
    record the `gen/apple/` gitignore policy decided in Plan 01. Note this ADR unblocks
    detailed planning of Phases 54–57 (and that Phase 55 changes to a SwiftUI keyboard if
    Approach B was chosen).
  </action>
  <verify>
    <automated>test -f docs/adr/v4.1-002-build-approach.md && grep -q '## Decision' docs/adr/v4.1-002-build-approach.md && grep -qi 'Approach' docs/adr/v4.1-002-build-approach.md && grep -q 'research' docs/adr/v4.1-002-build-approach.md && ! test -f docs/adr/v4.1-001-build-approach.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/v4.1-002-build-approach.md` exists (and `docs/adr/v4.1-001-build-approach.md` does NOT — no numbering collision)
    - The ADR has `## Context`, `## Decision`, `## Consequences`, `## Alternatives considered`, and a Footnotes/References section
    - The Decision section names the chosen approach AND keys it on the observed failure stage from Task 1
    - The References section cites `.planning/research/` artifacts (SUMMARY/ARCHITECTURE/PITFALLS) rather than re-deriving the A-vs-B comparison
    - The ADR records that both approaches preserve the Frozen Invariant and that D-53.6 flattening was diagnostic-only / never committed
    - The ADR records the chosen `gen/apple/` gitignore policy
  </acceptance_criteria>
  <done>ADR v4.1-002 written, records the observed spike outcome by stage, cites research, accounts for the Frozen Invariant, and unblocks Phases 54–57 planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cargo tauri ios build` toolchain → working tree | the bundler/cargo-mobile2 writes build artifacts; diagnostic workarounds touch the tree |
| diagnostic workspace-flattening → git index | risk of committing a Frozen-Invariant violation |
| Simulator WKWebView → Rust command bridge | the same IPC surface as desktop (no new network surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Tampering | diagnostic workspace-flattening (D-53.6) | mitigate | Flattening is throwaway/local only; Task 1 acceptance asserts root members stay `["hp41-core", "hp41-cli"]` and `git status` stages no `[workspace]`/members change. If flattening is required to build, Approach A is rejected (never committed). |
| T-53-08 | Tampering | newer/patched `tauri-cli` time-box workaround | accept | Workaround tauri-cli is an official first-party tool from the same vendor; pinned `^2`. No third-party crate substitution; `hp41-core` deps untouched. |
| T-53-09 | Information disclosure | ADR + SUMMARY content | mitigate | ADR/SUMMARY record build stages and error text, not secrets; no signing material exists at this stage (Plan 04/Phase 57). |
| T-53-SC | Tampering | no package installs in this plan | accept | This plan installs no new packages (only re-runs the spike toolchain from Plan 01). No `[ASSUMED]`/`[SUS]` packages → no blocking legitimacy checkpoint. |
</threat_model>

<verification>
- `just ios-build` outcome classified by stage; SUMMARY records it verbatim.
- Simulator smoke `2 ENTER 3 +` → 5, then `SIN` updates X + stack (engine dispatch proven).
- `docs/adr/v4.1-002-build-approach.md` exists with required sections, cites research, accounts for Frozen Invariant.
- Root `Cargo.toml` members unchanged; no `v4.1-001-build-approach.md` created.
</verification>

<success_criteria>
The build approach is decided by the spike's failure stage and recorded in ADR v4.1-002;
the app runs in the Simulator with the RPN smoke sequence dispatching through hp41-core;
the Frozen Invariant survived (no committed flattening). Phases 54–57 can now be planned
in detail.
</success_criteria>

<output>
Create `.planning/phases/53-build-approach-decision-ios-scaffold-spike/53-03-SUMMARY.md` when done.
Record the spike outcome (stage + workarounds), the Simulator smoke observation, and the chosen approach.
</output>
