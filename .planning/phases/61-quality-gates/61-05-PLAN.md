---
phase: 61-quality-gates
plan: 05
type: execute
wave: 3
depends_on: ["61-02", "61-03", "61-04"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [HSQUAL-04]
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md's 'JSON canonical data flow' section documents the new search_aliases field (invisible match surface, never rendered, populated by `just help-aliases`)"
    - "CLAUDE.md documents the DE-in-search-input convention exception: German aliases in search_aliases are intentional and exempt from the English-only doc rule"
    - "CLAUDE.md documents the upgraded tiered matcher and names the parity fixture + schema gate as the drift/CI guards"
    - "The opening line is corrected from five to six pools"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated JSON canonical data flow section reflecting v4.2 search enrichment"
      contains: "search_aliases"
  key_links:
    - from: "CLAUDE.md (JSON canonical data flow)"
      to: "docs/fixtures/help_search_parity.json + just schema-aliases-check"
      via: "named drift-guard + CI-gate references"
      pattern: "help_search_parity|schema-aliases-check"
---

<objective>
Close HSQUAL-04: a surgical edit to CLAUDE.md's "JSON canonical data flow" section so future
contributors understand the v4.2 search-enrichment contract. Three additions plus one
correction: (1) document the `search_aliases` field (`Vec<String>` / `string[]`, invisible
match surface never rendered in the `?` overlay, populated dev-only by `just help-aliases`,
schema-validated by `just schema-aliases-check`); (2) document the DE-in-search-input
convention exception (German aliases are intentional and exempt from the English-only doc
rule because they are search INPUT vocabulary, not documentation prose); (3) document the
tiered matcher (name/alias/desc/cat tier weights, non-empty query -> ranked flat list,
empty query -> category-grouped view unchanged) and name the parity fixture + schema gate as
the drift/CI guards; (4) correct the opening line from "Five" pools to "Six".

This plan is LAST (Wave 3) so the documented behavior matches the now-locked tests/gates.

Purpose: accurate, self-consistent project guide for contributors touching help/search.
Output: an edited "JSON canonical data flow" section in CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-quality-gates/61-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Confirmed against CLAUDE.md on 2026-06-05. -->

The "### JSON canonical data flow" section is under the "Frozen Invariants" heading. Its
CURRENT opening line says "Five `docs/hp41-*-functions.json` files (~350 entries total) …"
and it must become "Six … pools (~380 entries total) …". The three existing sub-bullets
(`just docs-matrix`; Op<->JSON parity; right-panel `key_ref_entries()`) stay. The RESEARCH
file (section "CLAUDE.md 'JSON canonical data flow' section") contains the exact current text
AND a ready proposed replacement block — use it as the authoritative wording source.

Facts to state accurately (from 61-RESEARCH.md "Tiered matcher" note):
  - tier weights: name 40/32/24/8; alias 35/28/21/7; desc 30/24/18/6; cat 20/16/12/4
  - non-empty query -> relevance-ranked flat list; empty query -> existing category-grouped view
  - CLI<->GUI parity guarded by docs/fixtures/help_search_parity.json
  - schema validated by `just schema-aliases-check` (wired into ci.yml)
  - search_aliases added v4.2 (HSDATA-01/02); #[serde(default)] from Phase 58
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md JSON canonical data flow section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the "### JSON canonical data flow" section under Frozen Invariants — the current 5-pool wording and the three existing sub-bullets)
    - .planning/phases/61-quality-gates/61-RESEARCH.md (the "CLAUDE.md 'JSON canonical data flow' section — current text" + "Proposed replacement text" — authoritative wording; the "Three things to add" list)
    - docs/fixtures/help_search_parity.json (confirm the path/name to cite) + Justfile (confirm the `schema-aliases-check` recipe name to cite, from plan 61-04)
  </read_first>
  <action>
    Edit ONLY the "### JSON canonical data flow" section of CLAUDE.md. (a) Change the opening
    line from "Five `docs/hp41-*-functions.json` files (~350 entries total)" to "Six
    `docs/hp41*-functions.json` pools (~380 entries total)". (b) Keep the three existing
    sub-bullets unchanged. (c) Append three new sub-bullets, sourced from the RESEARCH
    "Proposed replacement text":
      - a **`search_aliases`** bullet: invisible match surface added in v4.2 (HSDATA-01/02),
        array of DE+EN alternative spellings/abbreviations/synonyms, never rendered in the `?`
        overlay, populated by `just help-aliases` (dev-only, not a CI step), schema-validated by
        `just schema-aliases-check` (wired into ci.yml).
      - a **DE-aliases-are-intentional** bullet: the English-only doc rule applies to
        docs/ADRs/planning files; `search_aliases` is search INPUT vocabulary, not documentation;
        German terms are in-scope by design.
      - a **tiered matcher** bullet (Phase 59): both frontends score display_name / description /
        category / search_aliases with tier weights name 40/32/24/8, alias 35/28/21/7, desc
        30/24/18/6, cat 20/16/12/4; non-empty query -> relevance-ranked flat list; empty query ->
        existing category-grouped view unchanged; CLI<->GUI parity guarded by
        docs/fixtures/help_search_parity.json.
    English prose. Do NOT touch any other CLAUDE.md section. Do NOT edit hp41-core/ or any code.
  </action>
  <verify>
    <automated>grep -q "search_aliases" CLAUDE.md && grep -q "help_search_parity" CLAUDE.md && grep -q "schema-aliases-check" CLAUDE.md && grep -qi "six" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The "### JSON canonical data flow" opening line says "Six" pools (not "Five"); `grep -n "Five .docs/hp41" CLAUDE.md` returns empty
    - CLAUDE.md contains the strings `search_aliases`, `just help-aliases`, `just schema-aliases-check`, and `docs/fixtures/help_search_parity.json`
    - CLAUDE.md contains a sentence stating German aliases in `search_aliases` are intentional / exempt from the English-only doc rule (search input vocabulary)
    - CLAUDE.md contains the tiered-matcher description (tier weights and the non-empty->flat-list / empty->grouped-view behavior)
    - The three pre-existing sub-bullets (`just docs-matrix`, Op<->JSON parity, right-panel `key_ref_entries()`) are still present and unchanged
    - `git diff --name-only` lists ONLY CLAUDE.md (no code/test/script change in this plan)
    - `grep -rn "hp41-core" <(git diff CLAUDE.md)` shows no NEW hp41-core source edit (mentions in prose are fine; no source file touched)
  </acceptance_criteria>
  <done>The JSON-canonical-data-flow section accurately documents the six pools, the search_aliases field, the DE-in-search convention exception, and the tiered matcher with its parity + schema guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant surface. This plan edits a documentation section of CLAUDE.md (prose).
No authentication, authorization, cryptographic, network, untrusted-input, or executable
surface is involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-NA | N/A | CLAUDE.md docs edit | accept | No security-relevant surface (documentation prose); ASVS L1 N/A. |
</threat_model>

<verification>
- Manual review (HSQUAL-04 is a docs deliverable): read the edited section; confirm the four changes are present and accurate against the locked tests/gates
- `git diff --name-only` lists only CLAUDE.md
</verification>

<success_criteria>
- CLAUDE.md "JSON canonical data flow" documents search_aliases, the DE-in-search exception, and the upgraded matcher; opening line corrected to six pools
- Three pre-existing sub-bullets preserved; only CLAUDE.md changed; no code touched
</success_criteria>

<output>
Create `.planning/phases/61-quality-gates/61-05-SUMMARY.md` when done.
</output>
