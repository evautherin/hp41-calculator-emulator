---
phase: 61-quality-gates
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - hp41-gui/src/help_data.test.ts
autonomous: true
requirements: [HSQUAL-01, HSQUAL-02]
user_setup: []

must_haves:
  truths:
    - "TS real-data tests prove all four scoring tiers against the loaded six-pool JSON via rankedEntries(allFunctionsEntries(), q)"
    - "TS tests prove DE+EN alias resolution top-1 mirroring the Rust suite (zeitwert des geldes/zinseszins/compound interest->TVM; square root/quadratwurzel/wurzel->SQRT)"
    - "TS test proves a fuzzy-hit top-1 ('sqirt'->SQRT)"
    - "TS test proves empty-query passthrough: rankedEntries(pool, '') === pool (unchanged), per HSMATCH-04"
    - "TS test loads docs/fixtures/help_search_parity.json and asserts the same top-1 for every query as Rust — the GUI half of the drift guard"
  artifacts:
    - path: "hp41-gui/src/help_data.test.ts"
      provides: "Phase 61 describe block: TS real-data + parity-fixture tests"
      contains: "Phase 61"
  key_links:
    - from: "hp41-gui/src/help_data.test.ts"
      to: "rankedEntries / scoreEntry / allFunctionsEntries (already exported from ./help_data)"
      via: "existing import in help_data.test.ts line 20"
      pattern: "rankedEntries"
    - from: "hp41-gui/src/help_data.test.ts"
      to: "docs/fixtures/help_search_parity.json"
      via: "static JSON import (resolveJsonModule:true, fs.allow repo-root)"
      pattern: "help_search_parity"
---

<objective>
Add the TypeScript real-data verification suite (HSQUAL-01 TS + HSQUAL-02 TS side) as a new
`describe('Phase 61 …')` block APPENDED to the existing `hp41-gui/src/help_data.test.ts`.
These tests call `rankedEntries(allFunctionsEntries(), q)` against the REAL loaded six-pool
JSON (not synthetic data), mirroring the Rust suite (plan 61-02) tier-for-tier and
alias-for-alias, and load the SAME `docs/fixtures/help_search_parity.json` to assert the GUI
matcher produces identical top-1 results — the GUI half of the CLI<->GUI drift guard.

Purpose: prove the duplicated TS matcher resolves real aliases identically to the Rust one.
Output: a Phase-61 block in help_data.test.ts; `cd hp41-gui && npm test -- help_data` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-quality-gates/61-RESEARCH.md
@.planning/phases/61-quality-gates/61-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Confirmed against hp41-gui/src/help_data.ts + help_data.test.ts on 2026-06-05. -->

Already exported and ALREADY IMPORTED at help_data.test.ts line 20:
  import { allFunctionsEntries, xeqToken, helpEntriesAll, OVERLAY_HIDDEN_ALIASES,
           type HelpEntry, scoreEntry, rankedEntries } from './help_data';

Signatures (hp41-gui/src/help_data.ts):
  export function scoreEntry(entry: HelpEntry, q: string): number   // q pre-lowercased
  export function rankedEntries(pool: readonly HelpEntry[], q: string): readonly HelpEntry[]
      // if q === '' returns `pool` UNCHANGED (empty-query passthrough — HSMATCH-04).
      // Non-empty: filters score>0, sorts score DESC then display_name ASC.
      // CALLER lowercases+trims: pass q.toLowerCase().trim() to mirror HelpOverlay.tsx.
  export function allFunctionsEntries(): readonly HelpEntry[]
      // all six pools, status==='implemented', minus OVERLAY_HIDDEN_ALIASES. This is the
      // pool HelpOverlay.tsx feeds rankedEntries — use it (NOT helpEntries(), which is cv-only
      // and would miss TVM in the advantage pool). RESEARCH anti-pattern note.
  result entries expose `.display_name` (assert against "TVM"/"SQRT"/"PI"/"EMDIR").

Test infra (CLAUDE.md + vite.config.ts): Vitest 4.1.6, globals:false (import describe/it/expect
from 'vitest' — already done), jsdom. resolveJsonModule:true and fs.allow=repo-root, so
`import parity from '../../docs/fixtures/help_search_parity.json'` resolves. These Phase-61
tests render NO React component, so afterEach(cleanup) is NOT needed here (it IS required only
in describe blocks that render <HelpOverlay/> — not this one). Run via `vitest run` (no watch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 61 TS real-data tier/alias/fuzzy/empty tests</name>
  <files>hp41-gui/src/help_data.test.ts</files>
  <read_first>
    - hp41-gui/src/help_data.test.ts (existing structure, line-20 import already includes rankedEntries/scoreEntry/allFunctionsEntries; the Phase-59 "ties broken by display_name ASC" + "empty string query returns unranked passthrough" tests near lines 290/304 — mirror their idioms)
    - hp41-gui/src/help_data.ts lines 271-471 (allFunctionsEntries, scoreEntry, rankedEntries semantics; empty-query passthrough returns pool unchanged)
    - .planning/phases/61-quality-gates/61-RESEARCH.md (Pattern 1/2 TS variants; the "Parity fixture on allFunctionsEntries()" anti-pattern; Pitfall 8 cleanup note — not needed here)
    - docs/hp41-advantage-functions.json + docs/hp41cv-functions.json (post-61-01 aliases)
  </read_first>
  <behavior>
    - exact-name: rankedEntries(allFunctionsEntries(), 'tvm')[0].display_name === 'TVM'; 'pi'->'PI'; 'emdir'->'EMDIR'
    - exact-alias DE: 'zeitwert des geldes'->'TVM'; 'quadratwurzel'->'SQRT'
    - exact-alias EN: 'square root'->'SQRT'; 'financial solver'->'TVM'
    - spec-example aliases (depend on 61-01 hand-edit): 'zinseszins'->'TVM'; 'compound interest'->'TVM'; 'wurzel'->'SQRT'
    - fuzzy: 'sqirt'->'SQRT'
    - prefix vs substring ordering: a name-prefix hit ranks at or above a substring-only hit for the same query (assert relative ordering, mirroring the Rust suite)
    - empty-query passthrough: rankedEntries(pool, '') === pool (referential identity — pool returned unchanged), proving HSMATCH-04
  </behavior>
  <action>
    Append a new `describe('Phase 61 — real-data alias resolution', () => { … })` block to
    hp41-gui/src/help_data.test.ts (do NOT modify existing Phase-60/Phase-59 blocks). Reuse
    the existing line-20 import (no new import needed except the JSON fixture in Task 2). In
    each `it(...)` compute `const pool = allFunctionsEntries();` and
    `const results = rankedEntries(pool, q.toLowerCase().trim());` then
    `expect(results[0]?.display_name, \`query "${q}"\`).toBe(<expected>)`. For the empty-query
    test assert `expect(rankedEntries(pool, '')).toBe(pool)` (referential, the passthrough
    contract). Use the exact queries/expected display_names from <behavior>. No React render,
    so no afterEach(cleanup) in this block. Touch only this test file; never touch hp41-core/.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- help_data</automated>
  </verify>
  <acceptance_criteria>
    - `cd hp41-gui && npm test -- help_data` exits 0 (runs via `vitest run`, no watch)
    - The run shows a Phase-61 test asserting 'zinseszins'->'TVM' passing (proves 61-01 hand-edit reaches the TS pool)
    - The run shows 'wurzel'->'SQRT' and 'sqirt'->'SQRT' passing
    - The run shows the empty-query test asserting `rankedEntries(pool,'') === pool`
    - `grep -n "allFunctionsEntries" hp41-gui/src/help_data.test.ts` shows the Phase-61 block uses allFunctionsEntries() (not helpEntries())
    - `grep -n "hp41-core" hp41-gui/src/help_data.test.ts` returns empty
  </acceptance_criteria>
  <done>The TS matcher resolves all four tiers, DE+EN aliases (incl. 61-01 spec examples), a fuzzy hit, and empty-query passthrough against the real loaded six-pool data.</done>
</task>

<task type="auto">
  <name>Task 2: TS side of the CLI<->GUI parity fixture</name>
  <files>hp41-gui/src/help_data.test.ts</files>
  <read_first>
    - docs/fixtures/help_search_parity.json (object with `queries`: array of {query, top_n, expected_top, note})
    - hp41-gui/vite.config.ts (fs.allow = repo root; so `../../docs/fixtures/…` import is permitted) + hp41-gui/tsconfig.json (resolveJsonModule:true)
    - .planning/phases/61-quality-gates/61-RESEARCH.md (Pattern 2 "TS test pattern"; the "TS fixture import path" anti-pattern; the "Parity fixture on allFunctionsEntries()" note)
  </read_first>
  <action>
    Add to hp41-gui/src/help_data.test.ts a static JSON import at the top
    `import parityFixture from '../../docs/fixtures/help_search_parity.json';` (resolveJsonModule
    is on; fs.allow covers the repo root). Append a
    `describe('Phase 61 parity fixture — CLI<->GUI drift guard', () => { … })` block with one
    `it(...)` that iterates `parityFixture.queries`, and for each case computes
    `const results = rankedEntries(allFunctionsEntries(), c.query.toLowerCase().trim());` then for
    i in 0..min(c.top_n, c.expected_top.length) asserts
    `expect(results[i]?.display_name, \`query "${c.query}" position ${i}\`).toBe(c.expected_top[i])`.
    This is the SAME fixture the Rust test (61-02) consumes, so both matchers are pinned to one
    source of truth. No React render → no cleanup. Touch only this test file.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test -- help_data</automated>
  </verify>
  <acceptance_criteria>
    - `cd hp41-gui && npm test -- help_data` exits 0 with the parity describe block present
    - `grep -n "help_search_parity.json" hp41-gui/src/help_data.test.ts` matches the import
    - The TS parity block iterates `parityFixture.queries` and asserts `.display_name` against `expected_top`
    - The same fixture file is referenced by both 61-02 (Rust) and 61-03 (TS) — `grep -rn "help_search_parity.json" hp41-cli/tests hp41-gui/src` shows both consumers
    - Temporarily corrupting one `expected_top` value makes the TS parity test FAIL (proves it asserts; revert after) — record in SUMMARY
  </acceptance_criteria>
  <done>The GUI matcher's top-N matches the committed parity fixture for every canonical query; both frontends are pinned to the same fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant surface. This plan adds Vitest tests that read committed static JSON
(the six pools via the TS library + the parity fixture via a static import). No
authentication, authorization, cryptographic, network, or untrusted-input surface; the test
code parses and string-compares, never executes or evals JSON content.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-NA | N/A | hp41-gui/src/help_data.test.ts (Phase 61 blocks) | accept | No security-relevant surface (tests over committed static data); ASVS L1 N/A. |
</threat_model>

<verification>
- `cd hp41-gui && npm test -- help_data` green (per-task quick run)
- `just gui-ci` green (per-wave merge)
- Parity block uses allFunctionsEntries() (six pools), mirroring HelpOverlay.tsx; no React render so no cleanup leak
</verification>

<success_criteria>
- Phase-61 real-data + parity describe blocks appended to help_data.test.ts
- All TS tests green via `cd hp41-gui && npm test -- help_data`
- Same parity fixture consumed by both Rust (61-02) and TS (61-03); hp41-core untouched; no new deps
</success_criteria>

<output>
Create `.planning/phases/61-quality-gates/61-03-SUMMARY.md` when done.
</output>
