---
phase: 38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core
plan: 05
type: execute
wave: 3
depends_on: ["38-01", "38-02", "38-03"]
files_modified:
  - hp41-core/src/ops/time/alarm.rs
autonomous: true
requirements:
  - TIME-ALM-01
  - TIME-ALM-02
  - TIME-ALM-03
  - TIME-ALM-04
  - TIME-ALM-05
  - TIME-ALM-06
  - TIME-ALM-07
  - TIME-ALM-08
  - TIME-ALM-09
  - TIME-ALM-10
  - TIME-ALM-11
  - TIME-ALM-12

must_haves:
  truths:
    - "XYZALM stores an alarm with time from X, date from Y, repeat from Z, type from ALPHA"
    - "RCLALM recalls alarm N's fields back to the stack and ALPHA register"
    - "CLALMA clears alarm matching ALPHA content; CLALMX clears by number; CLRALMS clears all"
    - "check_alarms() detects past-due alarms and pushes events to event_buffer"
    - "Repeating alarms reschedule (trigger_unix += repeat_secs) after acknowledgment"
    - "Alarm catalog persists across save/load cycles via Vec<AlarmEntry> with serde(default)"
    - "ALPHA prefix parsing: >> = interrupting control, > = non-interrupting control, else = message"
  artifacts:
    - path: "hp41-core/src/ops/time/alarm.rs"
      provides: "Complete alarm catalog system with 7 ops + check_alarms drain function"
      exports: ["op_xyzalm", "op_rclalm", "op_almcat", "op_clalma", "op_clalmx", "op_clralms", "op_almnow", "check_alarms"]
  key_links:
    - from: "hp41-core/src/ops/time/alarm.rs"
      to: "hp41-core/src/state.rs"
      via: "state.alarms Vec<AlarmEntry> + state.event_buffer push"
      pattern: "state.alarms"
    - from: "hp41-core/src/ops/time/alarm.rs"
      to: "hp41-core/src/ops/time/clock.rs"
      via: "current_unix_timestamp() for past-due comparison"
      pattern: "current_unix_timestamp"
---

<objective>
Implement the complete alarm catalog system: XYZALM (set alarm from stack/ALPHA), RCLALM (recall alarm fields), ALMCAT (enter catalog mode), CLALMA/CLALMX/CLRALMS (clear alarms), ALMNOW (trigger immediate alarm), and the check_alarms() drain function that detects past-due alarms after every dispatch.

Purpose: The alarm system is the most complex Time Pac subsystem -- it introduces a persistent event catalog with past-due detection, repeat scheduling, and type-based dispatch (message vs control alarms). The drain pattern (D-38.9) integrates cleanly with the existing print_buffer/event_buffer infrastructure.
Output: Fully-implemented alarm.rs replacing Wave 1 stubs; all alarm operation tests pass including CRUD, past-due detection, and repeat scheduling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-CONTEXT.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-RESEARCH.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-PATTERNS.md
@.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 types and Plans 02-03 helpers -->

From hp41-core/src/ops/time/alarm.rs (Plan 01 stub types -- keep these):
- pub struct AlarmEntry { pub trigger_unix: i64, pub repeat_secs: i64, pub alarm_type: AlarmType, pub past_due: bool }
- pub enum AlarmType { Message(String), Control { label: String, interrupting: bool } }

From hp41-core/src/state.rs:
- pub alarms: Vec<AlarmEntry>  -- persistent, #[serde(default)]
- pub event_buffer: Vec<String>  -- transient drain channel
- pub print_buffer: Vec<String>  -- transient drain channel
- pub alpha_reg: String  -- ALPHA register content
- pub alarm_catalog_mode: bool  -- transient flag for ALMCAT
- pub time_offset_secs: i64  -- clock offset

From hp41-core/src/ops/time/date_arith.rs (Plan 02):
- pub fn parse_date_hpnum(hpnum: &HpNum, dmy: bool) -> Result<(i32, i32, i32), HpError>
- pub fn parse_time_hpnum(hpnum: &HpNum) -> Result<(u8, u8, u8, u8), HpError>
- pub fn date_to_jdn(year: i32, month: i32, day: i32) -> i64

From hp41-core/src/ops/time/clock.rs (Plan 03):
- SystemTime-based current time computation pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XYZALM + RCLALM + alarm utility helpers</name>
  <files>hp41-core/src/ops/time/alarm.rs</files>
  <read_first>
    hp41-core/src/ops/time/alarm.rs,
    hp41-core/src/state.rs,
    hp41-core/src/ops/time/date_arith.rs,
    hp41-core/src/ops/time/clock.rs
  </read_first>
  <behavior>
    - parse_alarm_type("Hello") == AlarmType::Message("Hello")
    - parse_alarm_type(">MYPROG") == AlarmType::Control { label: "MYPROG", interrupting: false }
    - parse_alarm_type(">>MYPROG") == AlarmType::Control { label: "MYPROG", interrupting: true }
    - parse_alarm_type(">") == AlarmType::Control { label: "", interrupting: false } (edge case)
    - op_xyzalm: X=8.000000 (8:00), Y=1.012026 (Jan 1 MDY), Z=0 (no repeat), ALPHA="Wake up" -> stores alarm
    - op_xyzalm: Y=0 means "today's date" (use current date)
    - op_xyzalm with 253 alarms already set -> HpError::Data (cap enforced per OM limit)
    - op_rclalm with X=1: recalls first alarm fields to stack (time->X, date->Y, repeat->Z) and message/label to ALPHA
    - op_rclalm with X out of range -> HpError::Data
  </behavior>
  <action>
    Replace the stub content of `alarm.rs` (keeping `AlarmEntry` and `AlarmType` struct/enum definitions from Plan 01). Keep the Free42 disclaim header.

    Add imports: `std::time::{SystemTime, UNIX_EPOCH}`, `crate::error::HpError`, `crate::num::HpNum`, `crate::stack::{apply_lift_effect, enter_number, LiftEffect}`, `crate::state::CalcState`, `super::date_arith::{parse_date_hpnum, parse_time_hpnum, date_to_jdn}`, `rust_decimal::Decimal`, `std::str::FromStr`.

    Implement private `fn parse_alarm_type(alpha: &str) -> AlarmType`: Check `>>` prefix FIRST (Pitfall 9 -- longer match wins), then `>` prefix, else Message. Use `strip_prefix`.

    Implement private `fn current_unix_secs(offset: i64) -> i64`: `SystemTime::now().duration_since(UNIX_EPOCH).map(|d| d.as_secs() as i64 + offset).unwrap_or(0)`. This gives the "HP-41 time" as Unix seconds for alarm comparison.

    Implement private `fn time_date_to_unix(hours: u8, minutes: u8, seconds: u8, year: i32, month: i32, day: i32) -> i64`: Convert a date+time to Unix-like seconds. Use JDN difference from Unix epoch base (1970-01-01 = JDN 2440588) multiplied by 86400, plus time-of-day seconds.

    Implement `op_xyzalm(state)`: Per XYZALM stack layout from QRC -- X=time (HH.MMSSss), Y=date (per Flag 31; 0=today), Z=repeat interval (HHHH.MMSSss; 0=no repeat). Parse time from X via `parse_time_hpnum`. Parse date from Y: if Y is zero, use current date (call `current_unix_secs` and decompose); else `parse_date_hpnum` per Flag 31. Parse repeat from Z: if zero, `repeat_secs = 0`; else parse as time and convert to total seconds per D-38.11. Parse alarm type from `state.alpha_reg` via `parse_alarm_type`. Enforce catalog cap: if `state.alarms.len() >= 253` return `HpError::Data`. Compute `trigger_unix` via `time_date_to_unix`. Insert alarm into `state.alarms` sorted by `trigger_unix` (chronological). `apply_lift_effect(Neutral)`.

    Implement `op_rclalm(state)`: Read X as 1-indexed alarm number (truncate to integer). Validate 1..=alarms.len(), else `HpError::Data`. Retrieve alarm at index (number - 1). Decompose `trigger_unix` back to time and date components. Push time to X (HH.MMSSss), date to Y, repeat interval to Z. Set `state.alpha_reg` to alarm message or `>label` / `>>label`. Use stack lift/drop as needed for multi-register push. `apply_lift_effect(Enable)`.

    Add comprehensive `#[cfg(test)] mod tests` for `parse_alarm_type`, `op_xyzalm`, `op_rclalm`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core -- alarm 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `parse_alarm_type` checks `>>` before `>` (Pitfall 9)
    - `op_xyzalm` enforces 253 alarm cap per OM limit
    - `op_xyzalm` inserts alarm sorted chronologically
    - `op_xyzalm` with Y=0 uses current date
    - `op_xyzalm` reads alarm type from `state.alpha_reg`
    - `op_rclalm` validates alarm number range
    - `op_rclalm` restores time/date/repeat to stack and message to ALPHA
    - `repeat_secs` stored as i64 seconds per D-38.11
    - All tests pass
  </acceptance_criteria>
  <done>XYZALM stores alarms with correct type detection from ALPHA prefix; RCLALM recalls alarm fields; 253-entry cap enforced; chronological insertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clear ops + check_alarms + ALMNOW + ALMCAT</name>
  <files>hp41-core/src/ops/time/alarm.rs</files>
  <read_first>
    hp41-core/src/ops/time/alarm.rs,
    hp41-core/src/state.rs
  </read_first>
  <behavior>
    - op_clralms: clears all alarms (state.alarms.clear())
    - op_clalmx with X=2: removes alarm at position 2 (1-indexed)
    - op_clalmx with X out of range: HpError::Data
    - op_clalma: removes alarm whose message matches state.alpha_reg content
    - op_clalma with no match: HpError::Data
    - op_almcat: sets alarm_catalog_mode = true, writes first alarm display to print_buffer
    - op_almnow: finds oldest past-due alarm and triggers it (pushes to event_buffer)
    - check_alarms: scans all alarms, marks trigger_unix <= now as past_due, pushes events
    - check_alarms with message alarm: pushes "alarm:message:<text>" to event_buffer + text to print_buffer
    - check_alarms with non-interrupting control: pushes "alarm:xeq:<label>" to event_buffer
    - check_alarms with interrupting control: pushes "alarm:interrupting:deferred" (D-38.4)
    - Repeating alarm: after acknowledgment, trigger_unix += repeat_secs, past_due reset to false
  </behavior>
  <action>
    Add implementations for the remaining 5 alarm ops and `check_alarms` in `alarm.rs` (below Task 1 code).

    Implement `op_clralms(state)`: `state.alarms.clear(); apply_lift_effect(Neutral); Ok(())`.

    Implement `op_clalmx(state)`: Read X as 1-indexed integer. Validate range. Remove at index (number - 1). `apply_lift_effect(Neutral)`.

    Implement `op_clalma(state)`: Find first alarm where `alarm_type` matches `state.alpha_reg` content: for Message type, match if message == alpha_reg; for Control type, match if `>label` or `>>label` form matches alpha_reg. Remove it. If no match found, return `HpError::Data`. `apply_lift_effect(Neutral)`.

    Implement `op_almcat(state)`: Set `state.alarm_catalog_mode = true`. If alarms is not empty, format first alarm's time and message into a display string, push to `state.print_buffer`. `apply_lift_effect(Neutral)`. Frontend handles the actual catalog navigation (Phase 39/41).

    Implement `op_almnow(state)`: Find first alarm with `past_due == true`. If found, trigger it: for Message, push message to event_buffer as `"alarm:message:{msg}"` and to print_buffer. For Control (non-interrupting), push `"alarm:xeq:{label}"` to event_buffer. Mark as acknowledged. If repeating (`repeat_secs > 0`), reschedule: `trigger_unix += repeat_secs`, reset `past_due = false`. If not repeating, remove from catalog. If no past-due alarm, check for the next upcoming and mark it as triggered immediately. `apply_lift_effect(Neutral)`.

    Implement `pub fn check_alarms(state: &mut CalcState)`: Compute `now = current_unix_secs(state.time_offset_secs)`. Iterate all alarms: for any with `!past_due && trigger_unix <= now`, mark `past_due = true` and record index. For each newly past-due alarm, push event to `state.event_buffer`: Message -> `"alarm:message:{msg}"` + push msg to print_buffer; Control(non-interrupting) -> `"alarm:xeq:{label}"`; Control(interrupting) -> `"alarm:interrupting:deferred"` per D-38.4.

    Implement `pub fn acknowledge_alarm(state: &mut CalcState, index: usize)`: If alarm at index has `repeat_secs > 0`, advance `trigger_unix += repeat_secs` and reset `past_due = false`. Otherwise remove the alarm from the Vec. This is a public helper for frontend acknowledge actions (Phase 39/41).

    Add tests for all clear ops, check_alarms behavior (including the repeat scheduling test), and ALMCAT mode activation.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core -- alarm 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `op_clralms` empties `state.alarms`
    - `op_clalmx` removes by 1-indexed number with range validation
    - `op_clalma` matches alarm by ALPHA content (message text or >label form)
    - `op_almcat` sets `alarm_catalog_mode = true`
    - `check_alarms` marks past-due alarms and pushes correct event strings to `event_buffer`
    - `check_alarms` pushes message text to `print_buffer` for message alarms
    - Interrupting control alarms push "alarm:interrupting:deferred" per D-38.4
    - `acknowledge_alarm` with repeat_secs > 0 advances trigger_unix and resets past_due
    - `acknowledge_alarm` with repeat_secs == 0 removes the alarm
    - All tests pass
    - `cargo test -p hp41-core` passes
  </acceptance_criteria>
  <done>Complete alarm system: CRUD ops, past-due detection drain, repeat scheduling, catalog mode activation, acknowledgment with auto-reschedule for repeating alarms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.alpha_reg input to XYZALM | User-controlled ALPHA content determines alarm type via prefix parsing |
| X register (alarm number) | User-entered number indexes into alarm catalog |
| Alarm catalog size | Unbounded growth potential without cap enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Denial of Service | Alarm catalog unbounded growth | mitigate | Cap at 253 entries (OM hardware limit) enforced in op_xyzalm; HpError::Data on overflow |
| T-38-14 | Tampering | ALPHA prefix alarm type injection | accept | parse_alarm_type uses string prefix matching only; no eval/exec of arbitrary content in core |
| T-38-15 | Tampering | event_buffer alarm:xeq format | accept | Frontend parses this format string; core never executes the label directly |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies |
</threat_model>

<verification>
After both tasks:
- `cargo test -p hp41-core -- alarm` passes all alarm tests
- `cargo test -p hp41-core` passes (no regressions)
- `grep "253" hp41-core/src/ops/time/alarm.rs` confirms cap enforcement
- `grep "strip_prefix" hp41-core/src/ops/time/alarm.rs` confirms correct prefix parsing
</verification>

<success_criteria>
Complete alarm catalog system: XYZALM stores with type detection and 253-entry cap, RCLALM recalls to stack/ALPHA, clear ops work by number/name/all, check_alarms drain detects past-due and pushes events, repeating alarms reschedule correctly, ALMCAT activates catalog mode.
</success_criteria>

<output>
Create `.planning/phases/38-hp41-core-xrom-framework-clock-date-stopwatch-alarm-core/38-05-SUMMARY.md` when done
</output>
