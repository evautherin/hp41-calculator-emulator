---
phase: 62-alarm-semantics-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/v4.3-003-alarm-prefix-semantics.md
autonomous: true
requirements: [ALARM-01]

must_haves:
  truths:
    - "Reading ONLY the new ADR, an implementer can state which prefix interrupts a running program without reproducing the >/>> confusion"
    - "The ADR records the audit verdict: parse_alarm_type is confirmed CORRECT (not inverted), so the D-05 conditional correction plan is NOT triggered"
    - "The ADR carries the D-05 correction-plan constraints for the record (OM-correct, unambiguous, save-file back-compat) without pre-committing flip-vs-rename"
    - "The ADR covers all three SC#3 edge cases: 4-level call-stack cap, idle-fire, non-interrupting >label path unchanged"
    - "just ci stays green; zero .rs files changed in this phase"
  artifacts:
    - path: "docs/adr/v4.3-003-alarm-prefix-semantics.md"
      provides: "Locked behavioral contract for >/>> control-alarm prefix semantics"
      contains: "Decision"
      min_lines: 60
  key_links:
    - from: "docs/adr/v4.3-003-alarm-prefix-semantics.md"
      to: "HP 82182A QRC 82182-90002 / HP-41CX QRG 00041-90475"
      via: "primary-OM citation in Decision section"
      pattern: "82182-90002|00041-90475"
    - from: "docs/adr/v4.3-003-alarm-prefix-semantics.md"
      to: "hp41-core/src/ops/time/alarm.rs parse_alarm_type"
      via: "audit section quoting the current flag assignment"
      pattern: "parse_alarm_type"
---

<objective>
Author the locked behavioral contract (ADR `v4.3-003`) for the HP-41 control-alarm
`>` / `>>` prefix semantics, resolving the inversion question the v4.3 audit surfaced.

Purpose: Phase 63 (interrupting-alarm execution) is gated on this contract. The Phase 62
research (62-RESEARCH.md) already resolved the prefix question against two HP primary
sources with HIGH confidence — this plan turns that finding into a permanent, locked
decision record so the `>`/`>>` confusion can never silently recur.

Output: ONE new Markdown file, `docs/adr/v4.3-003-alarm-prefix-semantics.md`. ZERO runtime
code changes. `just ci` stays green throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/62-alarm-semantics-spec/62-CONTEXT.md
@.planning/phases/62-alarm-semantics-spec/62-RESEARCH.md
@.planning/phases/62-alarm-semantics-spec/62-VALIDATION.md
@docs/adr/v4.3-002-macos-global-hotkey.md
@docs/adr/v4.3-001-single-instance-guard.md
@hp41-core/src/ops/time/alarm.rs

<interfaces>
<!-- The current code under audit. The ADR QUOTES these (read-only) — it does NOT modify them. -->
<!-- Source: hp41-core/src/ops/time/alarm.rs (read directly; line numbers approximate). -->

AlarmType enum (alarm.rs ~L56-60):
  pub enum AlarmType {
      Message(String),
      Control { label: String, interrupting: bool },
  }

parse_alarm_type (alarm.rs ~L78-92):
  - alpha.strip_prefix(">>")  -> Control { interrupting: true }   // >> = Interrupting Control Alarm
  - else strip_prefix('>')    -> Control { interrupting: false }  // >  = Conditional / Noninterrupting
  - else                      -> Message(alpha)
  - ">>" is checked BEFORE ">" (Pitfall 9 — longer match wins)

dispatch_alarm_event (alarm.rs ~L499-510):
  - Control { interrupting: true }  -> event_buffer.push("alarm:interrupting:deferred")  // the D-40-04 dead-end Phase 63 fixes
  - Control { interrupting: false } -> event_buffer.push("alarm:xeq:{label}")            // SC#3 path that MUST stay unchanged

alarm_matches_alpha (alarm.rs ~L518-531):
  - interrupting:true  -> format!(">>{label}")
  - interrupting:false -> format!(">{label}")
</interfaces>

<om_verdict>
<!-- The resolved verdict from 62-RESEARCH.md (read it in full for exact citations). DO NOT re-derive. -->
- `>>label` (double arrow) = "Control Alarm" (HP-41CX QRG) = "Interrupting Control Alarm"
  (82182A QRC) = INTERRUPTS a running program  -> code maps to interrupting:true ✓ CORRECT
- `>label` (single arrow)  = "Conditional Alarm" (HP-41CX QRG) = "Noninterrupting Control
  Alarm" (82182A QRC) = does NOT interrupt; fires only when idle/off -> interrupting:false ✓ CORRECT
- Primary sources: HP 82182A Time Module QRC, part 82182-90002 (11/81);
  HP-41CX QRG, part 00041-90475 (8/83), page 32.
- Verdict: code is CORRECT, NOT inverted. D-05 conditional correction plan NOT triggered.
- The CONTROL-ALARMS.md "inversion" was a documentation-reading artifact (prose name
  "control alarm" mis-mapped to the `>` symbol), not a code bug.
</om_verdict>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR v4.3-003 locking the >/>> control-alarm prefix semantics</name>
  <files>docs/adr/v4.3-003-alarm-prefix-semantics.md</files>
  <read_first>
    - .planning/phases/62-alarm-semantics-spec/62-RESEARCH.md (FULL — the OM-confirmed verdict, the exact primary-source citations, the root-cause analysis of the CONTROL-ALARMS.md inversion, and the three edge-case findings; do NOT re-derive any of these)
    - .planning/phases/62-alarm-semantics-spec/62-CONTEXT.md (FULL — locked decisions D-01..D-07; D-06 = ADR is the home, D-07 = do NOT edit D-40-04 here, D-05 = carry the correction-plan constraints for the record, delegate flip-vs-rename to Phase 63)
    - docs/adr/v4.3-002-macos-global-hotkey.md (the ADR FORMAT/frontmatter analog to match: Status / Date / Context line; then ## Context / ## Decision / ## Consequences / ## Alternatives considered)
    - docs/adr/v4.3-001-single-instance-guard.md (second ADR format reference)
    - hp41-core/src/ops/time/alarm.rs (read-only — to QUOTE the exact current parse_alarm_type / AlarmType::Control { interrupting } / dispatch_alarm_event lines in the audit section; do NOT modify this file)
  </read_first>
  <action>
    Create exactly one new file, docs/adr/v4.3-003-alarm-prefix-semantics.md, English-only,
    matching the v4.3-001/002 ADR structure (frontmatter block: title line `# ADR v4.3-003 —
    Control-Alarm Prefix Semantics (>> vs > Resolution)`, then `**Status:** Accepted`,
    `**Date:** 2026-06-06`, a one-line `**Context:**`, then sections `## Context`,
    `## Decision`, `## Consequences`, `## Alternatives considered`).

    The ADR MUST satisfy all four ROADMAP success criteria and honor CONTEXT D-01..D-07:

    SC#1 (Decision section) — State the mapping in OBSERVABLE-BEHAVIOR terms, NOT by leaning on
    the ambiguous `interrupting: bool` field name: "the alarm that interrupts a running program
    is `>>label` (double arrow)"; "the alarm that does NOT interrupt — fires only when idle/off —
    is `>label` (single arrow)"; no-prefix = Message alarm. Cite the primary OM sources by exact
    part number: HP 82182A Time Module QRC part 82182-90002 (11/81) and HP-41CX QRG part
    00041-90475 (8/83) page 32. Record both naming systems (HP-41CX "Control"/"Conditional" vs
    82182A "Interrupting"/"Noninterrupting Control") so the prose names cannot re-invert the
    symbol mapping.

    SC#2 / D-05 (audit + correction-plan-for-the-record) — Quote the ACTUAL current lines from
    alarm.rs: parse_alarm_type (`>>` -> interrupting:true, `>` -> interrupting:false), the
    AlarmType::Control { label, interrupting } definition, and the dispatch_alarm_event routing.
    State the verdict explicitly: the code is CONFIRMED CORRECT / NOT inverted, so the D-05
    conditional correction plan is NOT triggered. Still record the D-05 correction-plan
    constraints for the record (so the constraint set survives even though no fix is needed):
    (1) final mapping must be OM-correct; (2) the representation must be unambiguous so `>`/`>>`
    cannot silently re-invert; (3) save-file back-compat must be preserved because
    AlarmType::Control { interrupting } is serialized in CalcState (D-38.8). Do NOT pre-commit
    flip-vs-rename — explicitly delegate that strategy choice to the Phase 63 planner per D-05.
    Also record the root cause (the CONTROL-ALARMS.md inversion was a documentation-reading
    artifact: the prose name "control alarm" was mis-mapped to the single `>` symbol), so the
    confusion cannot recur.

    SC#3 (Consequences — edge cases) — Cover all three, using the research findings: (a) 4-level
    call-stack cap — when the stack is full at interrupt time the interrupting alarm is suppressed
    and left past-due, running program continues unaffected (note this is "hardware-consistent,
    OM-silent" per research assumption A3, not verbatim-OM-cited); (b) idle-fire — when no program
    is running a `>>` alarm executes its label directly via XEQ; the QRG page-32 behavior that a
    `>` conditional alarm "becomes a control alarm when off/displaying the clock" is noted as a
    Phase 63 implementation detail; (c) the non-interrupting `>label` path (the existing
    `alarm:xeq:{label}` event routing) MUST remain UNCHANGED in Phase 63.

    SC#4 (zero code change) — The ADR text MUST state that this phase makes zero runtime code
    changes and `just ci` stays green. This task creates ONLY the one .md file and edits NO .rs
    file.

    Plus: (a) the prefix-agnostic-mechanism note — per ARCHITECTURE.md/SUMMARY.md the synchronous
    single-threaded interrupt design is correct regardless of which prefix wins; only the routing
    condition matters, and it is already correct — so Phase 63 is unblocked the instant the prefix
    is settled (state this explicitly). (b) D-06: this ADR is the decision home. (c) D-07: state
    that docs/hp41-time-divergences.md §D-40-04 is NOT edited in this phase — its update is a
    Phase 66 success criterion. (d) Save-file back-compat note: since the code is correct and no
    flip is applied, NO migration is needed and no existing v4.x stored alarms are affected.
    (e) Alternatives considered: "rename interrupting:bool to a ControlKind enum" (deferred to
    Phase 63 planner per D-05) and "treat CONTROL-ALARMS.md as authoritative" (rejected —
    overridden by two independent HP primary sources).

    Do NOT edit docs/hp41-time-divergences.md. Do NOT edit any .rs file. Do NOT create an
    docs/adr index/README (none exists — confirmed; do not invent one). Use no fenced-code in
    prose beyond quoting the alarm.rs lines and the prefix/behavior table, matching the existing
    ADR style.
  </action>
  <verify>
    <automated>test -f docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qF '82182-90002' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qF '00041-90475' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qF '>>' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qiF 'parse_alarm_type' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qiE 'interrupt' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qiE '4-level|call.?stack' docs/adr/v4.3-003-alarm-prefix-semantics.md && grep -qiF 'alarm:xeq:' docs/adr/v4.3-003-alarm-prefix-semantics.md && echo ADR_CONTENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - File docs/adr/v4.3-003-alarm-prefix-semantics.md EXISTS and is the ONLY file created.
    - File contains the literal string ">>" described as the prefix that interrupts a running program (SC#1, observable-behavior terms).
    - File cites BOTH primary-source part numbers: "82182-90002" AND "00041-90475" (SC#1 / D-02 resolved).
    - File quotes / names "parse_alarm_type" and records the verdict that the code is CONFIRMED CORRECT / NOT inverted (SC#2 / D-05).
    - File records the three D-05 correction-plan constraints (OM-correct, unambiguous, save-file back-compat) AND explicitly delegates flip-vs-rename to the Phase 63 planner (D-05).
    - File covers all three SC#3 edge cases: "4-level"/call-stack cap, idle-fire, and the unchanged non-interrupting "alarm:xeq:" path.
    - File states zero runtime code change this phase + that §D-40-04 is updated in Phase 66, not here (SC#4 / D-07).
    - Running `git status --porcelain -- '*.rs'` shows NO modified/added .rs file attributable to this phase (zero .rs changes).
    - `just ci` exits 0.
  </acceptance_criteria>
  <done>
    docs/adr/v4.3-003-alarm-prefix-semantics.md exists, satisfies SC#1–SC#4 (content assertions
    above all pass), no .rs file changed, and `just ci` is green.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (docs-only):

1. ADR exists and the SC#1–SC#4 content assertions in Task 1's `<verify>` all pass.
2. Zero `.rs` changes: `git status --porcelain | grep -E '\.rs$'` returns empty (no Rust file
   touched by this phase).
3. `just ci` exits 0 (CI-green guard — proves docs-only, zero code drift).
4. English-only prose throughout the ADR (project convention).
</verification>

<success_criteria>
- [ ] docs/adr/v4.3-003-alarm-prefix-semantics.md created (the single deliverable).
- [ ] SC#1: prefix→behavior mapping stated unambiguously in observable-behavior terms with
      primary-OM citations (82182-90002 and 00041-90475).
- [ ] SC#2: existing parse_alarm_type / interrupting:bool audited; verdict = CONFIRMED CORRECT;
      D-05 correction-plan constraints carried for the record; flip-vs-rename delegated to Phase 63.
- [ ] SC#3: 4-level cap, idle-fire, and unchanged non-interrupting `alarm:xeq:` path all covered.
- [ ] SC#4: zero `.rs` changes; `just ci` green.
- [ ] D-06 (ADR is the home) and D-07 (D-40-04 NOT edited here) honored.
</success_criteria>

<output>
Create `.planning/phases/62-alarm-semantics-spec/62-01-SUMMARY.md` when done.
</output>
