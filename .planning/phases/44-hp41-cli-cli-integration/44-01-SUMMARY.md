---
phase: 44-hp41-cli-cli-integration
plan: 01
subsystem: docs + hp41-cli + scripts
tags: [advantage-pac, json-pipeline, help-overlay, docs-matrix, xrom]
dependency_graph:
  requires: ["43-01 (117 Advantage Pac Op variants in hp41-core)"]
  provides: ["docs/hp41-advantage-functions.json (fifth JSON canonical source)", "help_entries_adv() fifth OnceLock pool", "docs/hp41-advantage-function-matrix.md"]
  affects: ["hp41-cli help overlay (?)", "function_matrix_parity tests", "key_coverage tests", "docs-matrix CI gate"]
tech_stack:
  added: []
  patterns: ["fifth OnceLock JSON pool in help_data.rs", "fifth docs-matrix renderer branch", "fifth justfile invocation pair"]
key_files:
  created: ["docs/hp41-advantage-functions.json", "docs/hp41-advantage-function-matrix.md"]
  modified: ["hp41-cli/src/help_data.rs", "scripts/docs-matrix/src/main.rs", "justfile", "hp41-cli/tests/function_matrix_parity.rs", "hp41-cli/tests/key_coverage.rs"]
decisions:
  - "114 JSON entries split 63 (XROM 22 ADV_MATH_A) + 51 (XROM 24 ADV_MATH_B) with dense 1-based function_ids per module"
  - "xrom.module field: 'Adv Conv' for XROM 22 entries, 'Adv Math' for XROM 24 entries (per ADV_MATH_A.name and ADV_MATH_B.name)"
  - "No run-loop variants (AdvFsolveRunLoop / AdvFintgRunLoop / AdvFdifeqRunLoop) in JSON — internal-only per PLAN context"
  - "function_matrix_parity.rs updated with 114-entry ADV_OP_VARIANT_NAMES + 3 bidirectional parity tests"
  - "key_coverage.rs main dispatch upgraded to 0b0001_1111 (all 5 modules) + probed floor raised 155 -> 270 + Advantage sub-loop"
metrics:
  duration: "9 minutes"
  completed: "2026-05-26"
  tasks_completed: 2
  files_modified: 7
---

# Phase 44 Plan 01: Advantage Pac JSON Canonical Source + CLI Data Pipeline Summary

Fifth JSON canonical source for the HP-41 Advantage Pac (114 entries: XROM 22 ADV_MATH_A + XROM 24 ADV_MATH_B) wired into help_data.rs fifth OnceLock pool, function matrix generated, all parity tests updated.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Author docs/hp41-advantage-functions.json (114 entries) | 2475a99 | docs/hp41-advantage-functions.json (created) |
| 2 | Wire fifth OnceLock pool + docs-matrix extension | 919f1e1 | hp41-cli/src/help_data.rs, justfile, scripts/docs-matrix/src/main.rs, docs/hp41-advantage-function-matrix.md, hp41-cli/tests/function_matrix_parity.rs, hp41-cli/tests/key_coverage.rs |

## Success Criteria Verification

- [x] docs/hp41-advantage-functions.json exists with 114 valid entries (63 XROM 22 + 51 XROM 24)
- [x] hp41-cli compiles without errors (`cargo check --package hp41-cli` clean)
- [x] help_entries_adv() is a public accessor returning 114 entries
- [x] help_entries_all() chains 5 pools (cv -> math1 -> stat1 -> time -> adv)
- [x] docs/hp41-advantage-function-matrix.md generated by `just docs-matrix`
- [x] `just docs-matrix-check` exits 0 (no drift)
- [x] All 386 hp41-cli tests pass

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] function_matrix_parity.rs partition test did not recognize XROM 22/24**
- **Found during:** Task 2 (running `cargo test --package hp41-cli`)
- **Issue:** `test_pool_partition_is_exhaustive` treated module_id 22 and 24 as rogue values, failing with "Unknown xrom.module_id values in help_entries_all(): [("AdvBinin", 22), ...]"
- **Fix:** Updated `test_pool_partition_is_exhaustive` to recognize `Some(22)` (adv_a_count) and `Some(24)` (adv_b_count); added assertions for adv_a_count == 63 and adv_b_count == 51; added 114-entry `ADV_OP_VARIANT_NAMES` constant + 3 bidirectional parity tests (inventory count, forward parity, reverse xrom_resolve)
- **Files modified:** hp41-cli/tests/function_matrix_parity.rs
- **Commit:** 919f1e1

**2. [Rule 2 - Missing Critical] key_coverage.rs dispatch loop used 0b0000_0111 (missing Advantage Pac bits)**
- **Found during:** Task 2 (running `cargo test --package hp41-cli`)
- **Issue:** `key_coverage_implemented_entries_dispatch` failed with "AdvBinin via XEQ "BININ": no resolver matched". The main `XeqByName` arm called `xeq_by_name_local_resolve(&name, 0b0000_0111)` which doesn't have bits 3 or 4 set for the Advantage Pac modules.
- **Fix:** Upgraded `0b0000_0111` to `0b0001_1111` in the main dispatch loop; raised `probed >= 155` to `probed >= 270` to account for 114 new Advantage entries; added Advantage sub-loop (module_id 22 and 24, `adv_probed >= 100`)
- **Files modified:** hp41-cli/tests/key_coverage.rs
- **Commit:** 919f1e1

Both auto-fixes were committed together in Task 2's commit (919f1e1) since they were discovered during the same test run and are logically related to the same deliverable.

## Threat Flags

None. This plan is display and documentation only — no new network surface, auth paths, file access patterns, or schema changes at trust boundaries.

## Known Stubs

None. All 114 entries in docs/hp41-advantage-functions.json have `"status": "implemented"` and reference real Op variants from Phase 43.

## Self-Check: PASSED

- FOUND: docs/hp41-advantage-functions.json
- FOUND: docs/hp41-advantage-function-matrix.md
- FOUND: help_entries_adv in hp41-cli/src/help_data.rs
- FOUND: .chain(help_entries_adv()) in help_entries_all()
- FOUND commit 2475a99 (Task 1)
- FOUND commit 919f1e1 (Task 2)
- All 386 hp41-cli tests pass
- `just docs-matrix-check` exits 0
