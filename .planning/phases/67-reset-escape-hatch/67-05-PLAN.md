---
phase: 67-reset-escape-hatch
plan: 05
type: execute
wave: 3
depends_on: ["67-01", "67-02", "67-03", "67-04"]
files_modified:
  - docs/adr/v4.3-007-reset-escape-hatch.md
  - docs/hp41cv-divergences.md
  - hp41-gui/src/OnboardingWizard.tsx
  - hp41-gui/src/HelpOverlay.tsx
autonomous: false
requirements: [RESET-01]

must_haves:
  truths:
    - "An ADR records the escape-hatch architecture + the intentional divergence from hardware ON semantics"
    - "A divergence-doc entry records: soft reset clears working state (real ON preserves it); full reset maps to ON+left-arrow MEMORY LOST"
    - "A one-line discoverability hint (tap=soft / long-press=full) appears in onboarding and/or the ? overlay"
    - "just ci and just gui-ci are both green; CLI<->GUI parity (D-25.6) holds"
  artifacts:
    - path: "docs/adr/v4.3-007-reset-escape-hatch.md"
      provides: "ADR for the reset escape hatch"
      contains: "reset"
    - path: "docs/hp41cv-divergences.md"
      provides: "divergence entry for soft-reset-vs-ON semantics"
      contains: "reset"
  key_links:
    - from: "docs/adr/v4.3-007-reset-escape-hatch.md"
      to: "hp41-core soft_reset/memory_lost + frontend escape hatches"
      via: "architecture decision record"
      pattern: "soft_reset|memory_lost|escape hatch"
    - from: "OnboardingWizard.tsx or HelpOverlay.tsx"
      to: "the ON-key reset gesture"
      via: "one-line UI hint"
      pattern: "reset"
---

<objective>
Close out Phase 67: record the architecture + intentional ON-semantics divergence
in an ADR and a divergence-doc entry, surface a one-line discoverability hint
(tap=soft / long-press=full) in onboarding and/or the `?` overlay, and confirm the
full quality gate is green with CLI<->GUI parity (D-25.6) intact.

Reset is NOT added to the six `docs/hp41*-functions.json` pools (not an `Op`, not
XEQ-able) — the hint is plain UI copy only.

Purpose: ship-ready documentation + discoverability + green gates. Output: ADR,
divergence entry, UI hint, gate confirmation. Non-autonomous because the final
gate confirmation is a blocking human-verify of `just ci` + `just gui-ci` results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-reset-escape-hatch/67-CONTEXT.md
@docs/superpowers/specs/2026-06-10-reset-escape-hatch-design.md

<interfaces>
<!-- ADR numbering: next free number is v4.3-007 (existing top is v4.3-006-lcd-12cell-scientific.md).
     Follow the format of docs/adr/v4.3-005-hpnum-range-extension.md (Context/Decision/
     Consequences/Status sections).
     Divergence doc: docs/hp41cv-divergences.md is the general (non-module) divergence file;
     follow its existing entry format; use the next free divergence id consistent with that file.
     Discoverability: hp41-gui/src/OnboardingWizard.tsx (wizard steps) and/or
     hp41-gui/src/HelpOverlay.tsx (the ? overlay). Plain copy, e.g.
     "ON: tap = soft reset (unstick), long-press = full reset (MEMORY LOST)".
     English-only prose (MEMORY.md feedback_project_doc_language). Reset is NOT an Op,
     so it is NOT added to docs/hp41*-functions.json.
     Gates: `just ci` (CLI + license-audit + coverage) and `just gui-ci`. D-25.6 parity:
     the CLI Ctrl+R two-tier contract and the GUI ON tap/long-press contract must match
     (soft = clear+preserve, full = MEMORY LOST behind a confirm). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR v4.3-007 + divergence entry + discoverability hint</name>
  <read_first>
    - docs/adr/v4.3-005-hpnum-range-extension.md (ADR section format + Status line)
    - docs/hp41cv-divergences.md (existing entry format; pick the next free divergence id)
    - hp41-gui/src/OnboardingWizard.tsx and hp41-gui/src/HelpOverlay.tsx (where a one-line hint fits; match existing copy style)
    - hp41-gui/src/OnboardingWizard.test.tsx / HelpOverlay.test.tsx (if a text/snapshot assertion needs updating)
  </read_first>
  <action>
    Create `docs/adr/v4.3-007-reset-escape-hatch.md` (English) documenting: the
    problem (a persisted input-blocking state survives restart via the shared
    autosave); the chosen approach (two pure CalcState methods
    `soft_reset`/`memory_lost` invoked OUTSIDE dispatch; frontends persist
    synchronously to overwrite the autosave); rejected alternatives (reset-as-Op —
    self-defeating if dispatch is stuck + forces a 4-way match; frontend-only —
    SC-4 duplication/drift); and the INTENTIONAL DIVERGENCE from hardware ON (real
    ON preserves Continuous Memory via the power toggle; our soft reset deliberately
    clears working state because a faithful ON would not unstick anything). Note the
    CLI Ctrl+R-above-pending_input D-07 exception and the GUI long-press-replaces-
    ON+left-arrow approximation. Set Status: Accepted. Then add a divergence entry
    to `docs/hp41cv-divergences.md` (next free id): hardware ON preserves everything;
    our soft reset clears working state + traps and preserves stored data; full
    reset maps to the authentic ON+left-arrow MEMORY LOST. Finally add a one-line
    discoverability hint in OnboardingWizard.tsx and/or HelpOverlay.tsx (English):
    "ON: tap = soft reset (unstick), long-press = full reset (MEMORY LOST)". Do NOT
    add reset to any docs/hp41*-functions.json pool. Update any onboarding/help test
    that asserts step text or snapshots accordingly.
  </action>
  <verify>
    <automated>test -f docs/adr/v4.3-007-reset-escape-hatch.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/v4.3-007-reset-escape-hatch.md` exists, is English, follows the existing ADR section format, and has Status: Accepted.
    - The ADR names `soft_reset`/`memory_lost`, the outside-dispatch invariant, the rejected alternatives, and the intentional ON-semantics divergence.
    - `docs/hp41cv-divergences.md` gains a new entry describing the soft-reset-vs-ON divergence and the full-reset ON+left-arrow mapping.
    - A one-line tap=soft / long-press=full hint appears in OnboardingWizard.tsx and/or HelpOverlay.tsx (English copy).
    - `grep -ril reset_soft docs/hp41cv-functions.json docs/hp41-functions.json docs/hp41cv-functions.json` returns nothing — reset is NOT in any function-JSON pool (verify by inspection across all six pools).
    - Any onboarding/help vitest assertions touched by the new copy still pass.
  </acceptance_criteria>
  <done>ADR v4.3-007 + divergence entry + discoverability hint authored; reset absent from function-JSON pools.</done>
</task>

<task type="auto">
  <name>Task 2: Full quality-gate run (just ci + just gui-ci) + parity self-check</name>
  <read_first>
    - CLAUDE.md (Quality Gates table: coverage >=95% lines / >=93% regions, numerical accuracy >=98%, 0 panics, 0 Free42 contamination; `just` is the sole runner)
    - MEMORY.md references: reference_msrv_clippy_lint_divergence (run `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings`), reference_executor_commits_fmt_dirty (run `cargo fmt --check` at root before declaring done), reference_gui_ci_no_clippy + reference_gui_crate_not_rustfmt_clean (GUI clippy manual; do NOT run cargo fmt on hp41-gui/src-tauri)
  </read_first>
  <action>
    Run `just ci` and capture the result (lint + test + coverage + license-audit +
    schema-aliases). Run `just gui-ci` and capture the result (3-OS matrix logic +
    E2E smoke as available locally + TS/vitest). Additionally run the MSRV clippy
    check (`cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings`)
    and root `cargo fmt --check` (executor commits can be fmt-dirty). For the GUI
    crate run `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml --all-targets -- -D warnings`
    manually (ungated by CI). Do NOT run `cargo fmt` against hp41-gui/src-tauri
    (reformats pre-existing files). If anything is red, fix it within this phase's
    files (or flag a cross-file issue). Then perform the D-25.6 parity self-check:
    confirm in writing (in the SUMMARY) that the CLI Ctrl+R two-tier contract and
    the GUI ON tap/long-press contract are behaviorally equivalent (soft =
    clear-working+preserve-stored, full = MEMORY LOST behind a confirm), and that
    `shift_armed` (CLI) / `shiftActive` (GUI) are both cleared on reset and neither
    crosses IPC / lives in CalcState.
  </action>
  <verify>
    <automated>just ci</automated>
  </verify>
  <acceptance_criteria>
    - `just ci` exits 0 (lint + test + coverage >=95% lines / >=93% regions + license-audit + schema-aliases).
    - `just gui-ci` exits 0.
    - `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings` clean (MSRV divergence guard).
    - `cargo fmt --check` (root) clean.
    - `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml --all-targets -- -D warnings` introduces no NEW warnings beyond the known pre-existing noise.
    - The SUMMARY records the D-25.6 parity self-check (CLI Ctrl+R contract == GUI ON contract; shift_armed/shiftActive both cleared, neither in CalcState/IPC).
    - 0 panics in hp41-core; 0 Free42 contamination (both still gated by `just ci`).
  </acceptance_criteria>
  <done>just ci + just gui-ci green; MSRV clippy + root fmt clean; D-25.6 parity self-check recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 67 complete: core soft_reset/memory_lost, CLI Ctrl+R two-tier prompt, GUI
    reset commands + ON-key tap/long-press + portaled confirm sheet, ADR +
    divergence doc + discoverability hint, all quality gates green.
  </what-built>
  <how-to-verify>
    1. Review the `just ci` and `just gui-ci` output captured in Task 2 — both
       exit 0.
    2. Confirm the ADR (docs/adr/v4.3-007-reset-escape-hatch.md) and the
       divergence entry read correctly and in English.
    3. Confirm the onboarding/`?`-overlay hint copy is acceptable.
    4. Confirm D-25.6 parity holds: CLI Ctrl+R (s/f) and GUI ON (tap/long-press)
       deliver the same two-tier soft/full behavior.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 67 ready for the v4.3 tag + PR merge, or describe gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (docs only in Task 1) | no runtime boundary; documentation + UI copy |
| CI gate (Task 2) | `just ci`/`just gui-ci` are the trust gate that the runtime changes (Plans 01-04) are safe to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-12 | Repudiation | undocumented divergence from hardware ON semantics (future maintainer "fixes" the intentional clear) | mitigate | ADR v4.3-007 + the hp41cv divergence entry record the decision as intentional with rationale |
| T-67-13 | Tampering | a regression in Plans 01-04 ships unverified | mitigate | `just ci` + `just gui-ci` + MSRV clippy + root fmt-check gate the phase; blocking human-verify of the gate output |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced across Phase 67; nothing to install (zero-new-runtime-deps invariant holds) |
</threat_model>

<verification>
- `just ci` exit 0; `just gui-ci` exit 0.
- `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings` clean.
- `cargo fmt --check` (root) clean.
- ADR + divergence entry present and English; reset absent from function-JSON pools.
- D-25.6 parity self-check recorded in the SUMMARY.
</verification>

<success_criteria>
- ADR + divergence doc record the intentional ON-semantics divergence.
- Discoverability hint surfaced (plain UI copy; not a function-JSON entry).
- All quality gates green; CLI<->GUI parity (D-25.6) confirmed.
- Phase 67 ready for the v4.3 tag + `gh pr merge 26 --merge` (NEVER --squash).
</success_criteria>

<output>
Create `.planning/phases/67-reset-escape-hatch/67-05-SUMMARY.md` when done.
</output>
