---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 06
type: execute
wave: 4
depends_on: ["63-04"]
files_modified:
  - hp41-gui/src/App.tsx
  - hp41-gui/src/App.test.tsx
autonomous: true
requirements: [ALARM-02, ALARM-03, PRGM-01, PRGM-02]

must_haves:
  truths:
    - "R/S on a STOPPED program starts the GUI run loop (invoke run_program); R/S while RUNNING stops it; the existing modal→submit_modal and is_running→request_cancel branches are preserved (R/S routing becomes a 4-way: modal > cancel-long-op > run-loop-stop > run-loop-start)."
    - "The run-loop driver is yield-and-resume: invoke('run_program') → render returned pending_yield.text → if pending_yield set, schedule invoke('resume_program') after pending_yield.resume_ms → repeat until the program ends (is_running false / no pending_yield) — driven by command RETURN VALUES + a scheduled resume, NEVER by polling get_state (D-11)."
    - "When pending_yield is present the main display shows pending_yield.text (precedence: modal preview > pending_yield.text > display_str), without reading display_override (D-04)."
    - "A missing alarm-handler label surfaces as a GUI toast (alarm:missing:{label}); the alarm:interrupting silent-ignore arm is removed; alarm:message / alarm:xeq arms unchanged."
    - "A running no-yield program is handled entirely server-side (one run_program call, Phase-C alarm fire inside it) — no TS branch needed for the compute-loop alarm case."
  artifacts:
    - path: "hp41-gui/src/App.tsx"
      provides: "R/S 4-way start/stop run-loop routing; yield render + scheduled resume_program driver; alarm:missing toast; interrupting silent-ignore removed; CalcStateView.pending_yield field"
      contains: "resume_program"
    - path: "hp41-gui/src/App.test.tsx"
      provides: "Vitest: pending_yield renders + auto-resume; R/S start/stop routing; alarm:missing toast"
      contains: "pending_yield"
  key_links:
    - from: "R/S keystroke handler (invokeForKey r_s branch)"
      to: "run_program / resume_program commands"
      via: "4-way: modal > cancel > stop(running) > start(stopped) + yield-and-resume loop"
      pattern: "run_program"
    - from: "yield driver (scheduled resume)"
      to: "resume_program command"
      via: "setTimeout(pending_yield.resume_ms) → invoke resume_program → repeat"
      pattern: "resume_program"
    - from: "event consumer"
      to: "showToast"
      via: "alarm:missing arm"
      pattern: "alarm:missing"
---

<objective>
GUI run-loop driver (the TS half of the GUI track): wire the new `run_program`/`resume_program` Tauri commands (from 63-04) into App.tsx so the GUI continuously executes user-LBL programs with CLI parity — R/S starts/stops the run loop, PSE/VIEW/AVIEW yields render and auto-resume after `resume_ms`, interrupting alarms fire mid-run (server-side), and a missing handler label toasts. This is what makes GUI criteria 1/4/5 actually reachable in the GUI (criterion 3 already worked idle; 63-02 made the engine; 63-04 exposed it; this plan drives it).

Purpose: D-25.6 CLI↔GUI parity for running programs. Yield-and-resume keeps the Mutex released between yields (D-01/D-11): the driver reacts to command RETURN VALUES and a scheduled resume, never polling get_state. The existing setInterval(tick_time) stays for clock/idle-alarm cadence only.
Output: GUI runs programs continuously; yields render + auto-resume; missing labels toast; interrupting silent-ignore removed.

Touches `hp41-gui/src/App.tsx` + `App.test.tsx` ONLY — disjoint from all wave-3 plans. Depends on 63-04 (the run_program/resume_program commands + pending_yield projection must exist before TS can invoke/read them) → wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-PATTERNS.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-02-SUMMARY.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-04-SUMMARY.md
@hp41-gui/src/App.tsx

<interfaces>
<!-- Anchors verified against develop 2026-06-06. Use directly. -->

App.tsx:
- `interface CalcStateView` (44) — ADD `pending_yield: { kind: string; text: string; resume_ms: number } | null;` (matches 63-04's YieldView projection; kind is the lowercase string).
- `invokeForKey(effectiveId, state)` (99–126): R/S routing today is 3-way (D-31.1):
    1. `state?.modal_program_active` → `invoke('submit_modal')` (116–118)
    2. `state?.is_running` → `await invoke('request_cancel'); invoke('get_state')` (119–122)
    3. else → `invoke('run_stop')` (123)
  EXTEND branch 3: R/S on a STOPPED program should START the run loop. Keep branches 1 + 2 intact (modal advance / cancel long op). The cancel branch (2) already handles "R/S while running stops it" for long math ops; for a running user-LBL program the same request_cancel/run_stop path stops it — preserve that semantics. The new behavior is branch 3: instead of (or in addition to) the bare run_stop toggle, kick off the run-loop driver.
- R/S label selection: the CLI's R/S (app.rs:784) runs `run_program("A")` (hardcoded label "A" for F5; D-16). Mirror that for the GUI start path — start `run_program` with the same entry label the CLI uses for its R/S key ("A"), OR resume from the current pc if mid-program (is_running was toggled but pc advanced). Simplest parity: on R/S-start, if `state.pc` indicates a mid-program resume point use `resume_program`, else `run_program('A')` — mirror how the CLI chooses (check app.rs:784 F5 path uses run_program("A"); the GUI has no separate resume keybind today, so start = run_program('A')). Keep it minimal and document the chosen label.
- event consumer useEffect (1166–1187): `alarm:message:` → showToast (1169–1171); `alarm:xeq:` → dispatch_op xeq_{label} with busyRef guard (1172–1179); `alarm:interrupting:` SILENTLY IGNORED (1180–1181, D-38.4). REMOVE the interrupting arm; ADD `alarm:missing:` → showToast. Update the doc comment block (1162–1165) to list `alarm:missing:` instead of `alarm:interrupting:`.
- display precedence (1266–1268): `pendingInput ? renderModalLcd(pendingInput) : (calcState.display_override ?? calcState.display_str)`. INSERT pending_yield.text ABOVE display_str but BELOW modal preview, and do NOT route through display_override (D-04): e.g. `pendingInput ? renderModalLcd(...) : (calcState.pending_yield?.text ?? calcState.display_override ?? calcState.display_str)`.
- `setInterval(tick_time, 100ms)` (526–544, `liveTickRef`, gated on `needsTick`): the existing cadence — keep it for clock/stopwatch only. The resume scheduler is a SEPARATE setTimeout, not this interval (do NOT add a get_state poll here).
- `busyRef` (single-flight guard, used at 1174/529/1132), `showToast`, `setCalcState`, `setErrorMessage`, `extractErrMessage` — reuse.

From 63-04 (commands.rs / lib.rs):
- `invoke<CalcStateView>('run_program', { label })` — starts a program; returns a view (pending_yield set if it broke at a yield; is_running false when the program ended/stopped).
- `invoke<CalcStateView>('resume_program')` — continues a halted program; returns the next view.
- Running interrupting alarms execute server-side inside run_program/resume_program (Phase-C). Idle/demoted alarms arrive as `alarm:xeq:` (existing branch — unchanged).

Vitest: `globals:false` → portaled nodes need `afterEach(cleanup)` (iOS gotchas memory). Use fake timers (`vi.useFakeTimers()`) to assert the scheduled resume fires after resume_ms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R/S 4-way start/stop routing + yield-and-resume run-loop driver</name>
  <files>hp41-gui/src/App.tsx</files>
  <behavior>
    - R/S on a stopped program (no modal, not is_running) invokes run_program (label "A") and starts the yield-and-resume driver.
    - R/S while running (is_running true, no modal) stops via the existing request_cancel/run_stop path (unchanged) — the driver halts because the next view has is_running false / no pending_yield.
    - R/S with an active modal still invokes submit_modal (branch 1 unchanged).
    - On a returned view with pending_yield set, the driver schedules a single resume_program after pending_yield.resume_ms (via setTimeout, NOT a get_state poll), then repeats with the new view; it stops when pending_yield is null and is_running is false.
    - A no-yield program: run_program returns once with pending_yield null and is_running false (server-side run to completion incl. any Phase-C interrupt) — no resume scheduled.
  </behavior>
  <action>
    1. Add `pending_yield: { kind: string; text: string; resume_ms: number } | null;` to `interface CalcStateView` (44).
    2. R/S routing in `invokeForKey` (the `effectiveId === 'r_s'` branch, 111–124): keep branch 1 (modal → submit_modal) and branch 2 (is_running → request_cancel + get_state) EXACTLY as-is. For branch 3 (stopped, no modal): return `invoke<CalcStateView>('run_program', { label: 'A' })` to START the run loop (mirroring CLI F5 `run_program("A")`, D-16). Document the 'A' label choice inline. The yield-and-resume continuation is driven by the effect in step 3 (which fires whenever a returned view has pending_yield), so invokeForKey just kicks off the first run_program.
    3. Add a yield-driver effect (or fold into the existing post-dispatch handling): when `calcState?.pending_yield` is non-null and no resume is already scheduled, set a `resumeScheduledRef` flag and `setTimeout(() => { invoke<CalcStateView>('resume_program').then(view => { setCalcState(view); setErrorMessage(null); }).catch(err => showToast(extractErrMessage(err))).finally(() => { resumeScheduledRef.current = false; }); }, calcState.pending_yield.resume_ms)`. Guard against double-scheduling with a `useRef` flag (mirror busyRef usage). Because setCalcState re-renders with the new view, a subsequent pending_yield re-arms the timeout → the loop continues until pending_yield is null. Do NOT poll get_state; the loop is driven by the resume_program RETURN VALUE (D-11).
    4. Display precedence (1266–1268): render `calcState.pending_yield?.text` ABOVE `display_str`, BELOW modal preview, NOT via display_override (D-04). Update to `pendingInput ? renderModalLcd(pendingInput) : (calcState.pending_yield?.text ?? calcState.display_override ?? calcState.display_str)`.
    Note the compute-loop alarm case in a comment: a long no-yield program holds the Mutex server-side for one run_program call; Phase-C check_alarms fires the interrupt inside that call — no TS branch needed (accepted, mirrors INTG/SOLVE).
  </action>
  <verify>
    <automated>cd hp41-gui && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>R/S starts run_program('A') when stopped; modal + cancel branches unchanged; pending_yield drives a setTimeout(resume_ms)→resume_program loop with a single-flight ref guard (no get_state poll); display shows pending_yield.text with correct precedence; Vitest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: alarm:missing toast + remove alarm:interrupting silent-ignore</name>
  <files>hp41-gui/src/App.tsx, hp41-gui/src/App.test.tsx</files>
  <behavior>
    - An `alarm:missing:{label}` event line triggers showToast("Alarm XEQ {label}: label not found").
    - The `alarm:interrupting:` arm is gone; `alarm:message:` and `alarm:xeq:` arms unchanged.
    - Vitest: (a) a view with pending_yield renders text on the display and schedules a resume that fires after resume_ms (fake timers); (b) draining an `alarm:missing:FOO` event line calls showToast with the not-found message; (c) R/S on a stopped program invokes run_program('A').
  </behavior>
  <action>
    In the event consumer useEffect (1166–1187): DELETE the `alarm:interrupting:` arm (1180–1181). ADD `else if (line.startsWith('alarm:missing:')) { showToast(\`Alarm XEQ \${line.slice('alarm:missing:'.length)}: label not found\`); }` (D-08; CLAUDE.md never-silently-swallow / D-07). Update the doc comment block (1162–1165) to list `alarm:missing:` instead of `alarm:interrupting:`.
    Add Vitest cases in App.test.tsx (use `afterEach(cleanup)` per globals:false; `vi.useFakeTimers()` for the resume timing assertion):
    (a) Mock invoke so the first `run_program` returns a view with `pending_yield: { kind:'pse', text:'1.0000', resume_ms:1000 }` and `is_running:false`; assert the display renders "1.0000"; advance timers by 1000; assert `resume_program` was invoked.
    (b) Set `calcState.event_buffer = ['alarm:missing:FOO']`; assert showToast called with "Alarm XEQ FOO: label not found".
    (c) With a stopped, non-modal state, simulate R/S; assert `invoke` was called with `'run_program', { label: 'A' }`.
  </action>
  <verify>
    <automated>cd hp41-gui && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>alarm:missing toast added; alarm:interrupting arm removed; `grep -n "alarm:interrupting" hp41-gui/src/App.tsx` empty; all three Vitest cases green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust IPC → TS run-loop driver | CalcStateView (incl. pending_yield) is serialized over Tauri IPC; the TS driver schedules resume_program based on core-carried resume_ms. Single-user desktop/mobile app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-07 | DoS | TS resume scheduler | mitigate | single-flight ref guard prevents double-scheduling; the loop terminates when pending_yield is null (server-side program termination bounds it — MAX_STEPS in core). |
| T-63-08 | Info disclosure | yield text projection | accept | text is the user's own register/ALPHA value — no cross-user exposure. |
| T-63-11 | DoS | R/S start while already running | mitigate | branch 2 (is_running) handles R/S-while-running via cancel; branch 3 only starts when stopped — no concurrent run_program storm. |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm deps; `git diff HEAD -- hp41-gui/package.json` empty. |
</threat_model>

<verification>
- `cd hp41-gui && npm test` green (yield render + auto-resume, alarm:missing toast, R/S start routing).
- `just gui-ci` green.
- `grep -n "alarm:interrupting" hp41-gui/src/App.tsx` → empty.
- `grep -c "resume_program\|run_program" hp41-gui/src/App.tsx` ≥ 2 (driver wires both commands).
- No get_state poll added for the resume loop (driver uses command return values — D-11): the new resume path uses `resume_program`, not `get_state`.
</verification>

<success_criteria>
- Criterion 1 (GUI): R/S runs a user-LBL program; an interrupting alarm fires mid-run (server-side via 63-04 commands + 63-02 Phase-C) and the handler runs, then resumes.
- Criterion 4 (GUI): PSE mid-run renders pending_yield.text, auto-resumes after resume_ms.
- Criterion 5 (GUI): VIEW/AVIEW mid-run render their captured text, auto-resume.
- Criterion 2 (GUI): missing handler label toasts (D-08).
- D-25.6 parity: GUI run-loop half landed alongside the CLI half (63-03); display_override untouched (DISP-01 deferred); no get_state polling (D-11).
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-06-SUMMARY.md` when done.
</output>
</content>
