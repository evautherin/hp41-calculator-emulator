---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 05
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - docs/adr/v4.3-001-interrupt-alarm-pending-field.md
  - docs/hp41-time-divergences.md
autonomous: true
requirements: [ALARM-02, ALARM-03]

must_haves:
  truths:
    - "An ADR records the pending-interrupt run-loop-boundary mechanism (single transient field, synthetic-XEQ resume, 4-level cap, D-10 demotion, D-06 ack-after-RTN)."
    - "docs/hp41-time-divergences.md §D-40-04 is flipped from deferred/not-supported to implemented via pending-interrupt at run-loop boundary (v4.3)."
  artifacts:
    - path: "docs/adr/v4.3-001-interrupt-alarm-pending-field.md"
      provides: "ADR for the interrupt mechanism"
      contains: "pending_interrupt"
    - path: "docs/hp41-time-divergences.md"
      provides: "§D-40-04 flipped to implemented"
      contains: "implemented"
  key_links:
    - from: "docs/hp41-time-divergences.md §D-40-04"
      to: "ADR v4.3-001"
      via: "cross-reference"
      pattern: "v4.3-001"
---

<objective>
Documentation deliverables (Phase G): author ADR `v4.3-001-interrupt-alarm-pending-field.md` recording the pending-interrupt mechanism, and flip `docs/hp41-time-divergences.md` §D-40-04 from "deferred / not supported" to "implemented via pending-interrupt at run-loop boundary (v4.3)".

Purpose: CONTEXT § Specific Ideas + RESEARCH Phase G require both. The §D-40-04 flip is also a Phase 66 verification criterion — Phase 63 flips it now, Phase 66 verifies. English-only (project doc convention).
Output: One new ADR + one updated divergence entry.

Touches `docs/` ONLY — disjoint from 63-03 (CLI) and 63-04 (GUI); safe to run parallel in Wave 3. Depends on 63-02 so the ADR describes the as-built mechanism accurately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-02-SUMMARY.md
@docs/hp41-time-divergences.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR v4.3-001 (interrupt-alarm pending field)</name>
  <files>docs/adr/v4.3-001-interrupt-alarm-pending-field.md</files>
  <action>
    Create the ADR following the format of the existing `docs/adr/v4.3-003-*.md` (status, context, decision, consequences). English only. Record, as-built (cross-check against 63-02-SUMMARY.md):
    - Decision: a single transient control field `pending_interrupt: Option<String>` (+ paired `pending_interrupt_alarm_index`, + typed `pending_yield` channel) on CalcState, all `#[serde(default, skip)]`; NO new Op variant (4-way match not triggered); display_override left untouched (DISP-01 deferred to v4.4).
    - Mechanism: `check_alarms` (running, no solver/modal, none pending) sets pending_interrupt; `run_loop` takes it at the next instruction boundary and injects a synthetic XEQ frame (reuses call_stack push + Op::Rtn pop — no new resume mechanism); Phase-C check_alarms runs ~every 1000 steps so the GUI (Mutex-held run_loop) detects mid-run alarms.
    - Edge handling: 4-level cap → silent suppress, no 5th frame, alarm left past_due, is_running restored on all paths (D-07); missing label → alarm:missing event, no ack (D-08); solver/modal active → demote to alarm:xeq (D-10); idle → alarm:xeq via existing path (D-13); ack/reschedule only after handler RTN (D-06); clear on run_program/resume_program entry, dropping interrupt set just before STOP (D-09).
    - Relationship: builds on ADR v4.3-003 (prefix semantics confirmed correct — `>>`=interrupting); does NOT change parse_alarm_type (D-11).
    - Consequences: known simplifications (interrupt-before-STOP dropped; deeper interrupt nesting out of scope; mid-solver interrupt demoted).
  </action>
  <verify>
    <automated>test -f docs/adr/v4.3-001-interrupt-alarm-pending-field.md && grep -q "pending_interrupt" docs/adr/v4.3-001-interrupt-alarm-pending-field.md && grep -q "v4.3-003" docs/adr/v4.3-001-interrupt-alarm-pending-field.md && echo OK</automated>
  </verify>
  <done>ADR exists, references the pending_interrupt mechanism and ADR v4.3-003, in English, matching the repo's ADR format.</done>
</task>

<task type="auto">
  <name>Task 2: Flip docs/hp41-time-divergences.md §D-40-04 to implemented</name>
  <files>docs/hp41-time-divergences.md</files>
  <action>
    Locate the §D-40-04 "Interrupting Control Alarm Re-Entrancy" entry (currently "deferred / not supported"). Update its status/prose to "implemented via pending-interrupt at run-loop boundary (v4.3)", summarizing: interrupting control alarms (`>>label`) now execute their stored label program mid-run and resume the interrupted program; 4-level cap suppression, idle-fire, solver/modal demotion, and repeating-alarm reschedule are honored. Cross-reference ADR `v4.3-001-interrupt-alarm-pending-field.md`. Keep all other entries untouched. English only. Note inline that Phase 66 verifies this closure.
  </action>
  <verify>
    <automated>grep -n "D-40-04" docs/hp41-time-divergences.md && grep -q "implemented" docs/hp41-time-divergences.md && grep -q "v4.3-001" docs/hp41-time-divergences.md && echo OK</automated>
  </verify>
  <done>§D-40-04 reads "implemented via pending-interrupt at run-loop boundary (v4.3)"; cross-references ADR v4.3-001; other entries unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (docs-only) | No runtime code; no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-SC | Tampering | npm/pip/cargo installs | accept | Docs-only plan; no installs, no code, no deps. |
</threat_model>

<verification>
- ADR file exists, references pending_interrupt + ADR v4.3-003, English.
- §D-40-04 flipped to "implemented", cross-references ADR v4.3-001.
- `just ci` unaffected (docs-only — verify no markdown lint gate breaks if one exists).
</verification>

<success_criteria>
- ADR v4.3-001 authored (Phase G).
- §D-40-04 flipped to implemented (Phase G; verified again in Phase 66).
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-05-SUMMARY.md` when done.
</output>
