---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 04
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - hp41-gui/src-tauri/src/types.rs
  - hp41-gui/src-tauri/src/commands.rs
  - hp41-gui/src/App.tsx
  - hp41-gui/src/App.test.tsx
autonomous: true
requirements: [ALARM-02, ALARM-03, PRGM-01, PRGM-02]

must_haves:
  truths:
    - "pending_yield is projected through CalcStateView (kind/text/resume_ms) so the TS layer can render and schedule resume."
    - "The GUI renders pending_yield.text on the main display, then schedules resume_program via the existing setInterval(tick_time) cadence after resume_ms — Mutex released between yields (D-01/D-11, no get_state polling)."
    - "A missing alarm-handler label surfaces as a GUI toast (alarm:missing:{label})."
    - "The alarm:interrupting silent-ignore in the TS event consumer is removed; running interrupting alarms are handled server-side via Phase-C check_alarms."
  artifacts:
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "CalcStateView.pending_yield projection (kind/text/resume_ms)"
      contains: "pending_yield"
    - path: "hp41-gui/src/App.tsx"
      provides: "yield render + scheduled resume_program; alarm:missing toast; interrupting silent-ignore removed"
      contains: "pending_yield"
  key_links:
    - from: "CalcStateView.from_state"
      to: "state.pending_yield"
      via: "project kind/text/resume_ms triple"
      pattern: "pending_yield"
    - from: "App.tsx setInterval(tick_time)"
      to: "resume_program"
      via: "schedule resume after resume_ms when pending_yield present"
      pattern: "resume_program"
---

<objective>
GUI frontend wiring (D-25.6 parity, GUI half): project the `pending_yield` channel through `CalcStateView`, render its text on the main display in the TS layer, and schedule `resume_program` via the existing `setInterval(tick_time)` cadence (Mutex released between yields — D-01/D-11, no polling). Add an `alarm:missing:{label}` toast and remove the `alarm:interrupting:` silent-ignore. Running interrupting alarms need NO TS branch — Phase-C `check_alarms` inside `run_loop` handles them server-side.

Purpose: Criteria 4–5 require the pause visible in the GUI; criterion 2 / D-08 needs the missing-label toast. Lands in the SAME phase as the CLI half (63-03).
Output: GUI renders yields with scheduled auto-resume; missing labels toast.

Touches GUI files ONLY (`hp41-gui/src-tauri/src/{types.rs,commands.rs}`, `hp41-gui/src/App.tsx`, `App.test.tsx`) — disjoint from 63-03 (CLI) and 63-05 (docs); safe to run parallel in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-PATTERNS.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-02-SUMMARY.md
@hp41-gui/src-tauri/src/types.rs
@hp41-gui/src-tauri/src/commands.rs
@hp41-gui/src/App.tsx

<interfaces>
<!-- Anchors verified against develop 2026-06-06. -->

types.rs:
- `struct CalcStateView` (63); `display_override: Option<String>` projected (88, set at 206/239) — LEAVE UNTOUCHED (D-04, DISP-01 deferral).
- `from_state(state, print_lines, event_lines)` (125) — add pending_yield projection here.

commands.rs:
- `handle_tick_time` (342): check_alarms then drain event_buffer into event_lines, build view (343–348). Idle-fire (D-13) cadence.
- `handle_run_stop` (476): drains event_buffer (479).
- All view builders call `CalcStateView::from_state(calc, print_lines, event_lines)`.

App.tsx:
- `interface CalcStateView` (44) — add the pending_yield triple.
- `display_override: string | null` (59); display precedence `modal preview > display_override > display_str` (1268). The yield text must take precedence over display_str when present (render the carried text), but do NOT route through display_override.
- event consumer (1166–1187): `alarm:message:` → toast (1171); `alarm:xeq:` → dispatch_op xeq_{label} (1172–1179); `alarm:interrupting:` SILENTLY IGNORED (1180–1181). Remove the interrupting arm; add `alarm:missing:` → toast.
- `setInterval(tick_time, 100ms)` (528) — the existing cadence to schedule resume on.

From 63-02:
- `state.pending_yield: Option<YieldState{kind: YieldKind, text: String, resume_ms: u64}>`; resume_ms == 1000.
- `resume_program` is invoked from a Tauri command (or add a `resume_program` command if one is not exposed — check the IPC contract; reuse run_stop/tick_time wiring if `resume_program` is already a command). Mutex released between yields (D-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project pending_yield through CalcStateView (Rust)</name>
  <files>hp41-gui/src-tauri/src/types.rs, hp41-gui/src-tauri/src/commands.rs</files>
  <behavior>
    - from_state projects a Some(pending_yield) into the view as a serializable triple (kind string + text + resume_ms); None when absent.
    - display_override projection is unchanged (D-04).
    - Existing handle_tick_time / handle_run_stop view-build tests still pass; add one asserting pending_yield projects.
  </behavior>
  <action>
    In types.rs: add a serializable field to `CalcStateView`, e.g. `pub pending_yield: Option<YieldView>` where `YieldView { kind: String, text: String, resume_ms: u64 }` (kind as a lowercase string "pse"/"view"/"aview" so the TS layer needs no enum). Project it in `from_state` (125) from `state.pending_yield` — `state.pending_yield.as_ref().map(|y| YieldView { kind: …, text: y.text.clone(), resume_ms: y.resume_ms })`. Do NOT touch the display_override projection (206/239).
    No new Tauri command needed for the projection (rides existing get_state/tick_time/run_stop views). If `resume_program` is not already an exposed Tauri command, the TS scheduler in Task 2 needs one — check `commands.rs`/the IPC contract; if absent, add a thin `resume_program` command + permission TOML (`hp41-gui/src-tauri/permissions/resume-program.toml` referenced in `capabilities/default.json`) following the established Tauri-v2.11 pattern (run `cargo check` first to generate the permission registry). If `resume_program` is already reachable via run_stop/an existing command, reuse it and skip the new command.
    Add a Rust unit test in commands.rs asserting from_state projects pending_yield (mirror handle_tick_time_drains_event_buffer test at 1287).
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | tail -15 && cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>CalcStateView projects pending_yield (kind/text/resume_ms); display_override untouched; GUI-crate tests + clippy -D warnings clean; resume_program reachable from TS (existing command reused or new one added with permission).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GUI yield render + scheduled resume + missing-label toast (TS)</name>
  <files>hp41-gui/src/App.tsx, hp41-gui/src/App.test.tsx</files>
  <behavior>
    - When calcState.pending_yield is present, the main display shows pending_yield.text (precedence over display_str, but below modal preview).
    - The TS layer schedules a single resume_program after pending_yield.resume_ms via the existing setInterval(tick_time) cadence (not a polling get_state); after resume, the next view either yields again or shows the program result.
    - An `alarm:missing:{label}` event line triggers showToast("Alarm XEQ {label}: label not found").
    - The `alarm:interrupting:` arm is gone; `alarm:message:` and `alarm:xeq:` arms unchanged.
  </behavior>
  <action>
    In `interface CalcStateView` (44) add `pending_yield: { kind: string; text: string; resume_ms: number } | null;`.
    Display precedence (1268): when `calcState.pending_yield` is non-null, render `calcState.pending_yield.text` on the main display ABOVE `display_str` but keep modal preview highest. Do NOT read display_override for this (D-04).
    Resume scheduling: in the tick/effect that owns `setInterval(tick_time)` (528), when a returned view has `pending_yield` set and no resume is already scheduled, set a timeout/flag to call the `resume_program` command after `pending_yield.resume_ms`; on resume, take the returned view and repeat if it still yields. Guard against double-scheduling (a ref flag). Mutex is released between yields server-side (D-01) so the 100 ms tick keeps the UI responsive; do NOT add a get_state poll (D-11).
    Event consumer (1166–1187): delete the `alarm:interrupting:` arm (1180–1181); add `else if (line.startsWith('alarm:missing:')) { showToast(\`Alarm XEQ \${line.slice('alarm:missing:'.length)}: label not found\`); }`. Update the doc comment block (1163-style) to list `alarm:missing:` instead of `alarm:interrupting:`.
    Add Vitest cases in App.test.tsx: (a) a view with pending_yield renders text on the display; (b) draining an `alarm:missing:FOO` event line calls showToast with the not-found message. Use `afterEach(cleanup)` (globals:false convention).
  </action>
  <verify>
    <automated>cd hp41-gui && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Display renders pending_yield.text; resume scheduled via existing tick cadence (no polling); alarm:missing toast added; alarm:interrupting arm removed; Vitest cases green.</done>
</task>

<task type="auto">
  <name>Task 3: iOS cfg(mobile) compile check + GUI clippy gate</name>
  <files>hp41-gui/src-tauri/src/commands.rs</files>
  <action>
    No functional change — this task is the mandated GUI verification backstop (commands.rs event-handling was touched in Task 1). Run the iOS target check to catch `#[cfg(mobile)]` compile errors that `just gui-ci` (host target) cannot see (P-43-08), and the ungated GUI clippy (P-43-07). If either surfaces an error introduced by this plan's edits, fix it in commands.rs/types.rs (the 2 pre-existing noise hits at types.rs:148 and commands.rs &*p are acceptable — do not chase them).
  </action>
  <verify>
    <automated>cargo check --target aarch64-apple-ios --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | tail -10 && cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>iOS-target cargo check passes; GUI clippy -D warnings clean (modulo the 2 documented pre-existing noise hits).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust IPC → TS frontend | CalcStateView is serialized over Tauri IPC; TS renders core-carried strings. Single-user desktop/mobile app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-07 | DoS | TS resume scheduler | mitigate | single-flight ref guard prevents double-scheduling; resume bounded by program termination server-side. |
| T-63-08 | Info disclosure | yield text projection | accept | text is the user's own register/ALPHA value — no cross-user exposure. |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime deps; if a `resume_program` permission TOML is added it is a Tauri capability, not a package install. `git diff HEAD -- hp41-gui/package.json hp41-gui/src-tauri/Cargo.toml` empty. |
</threat_model>

<verification>
- `cd hp41-gui && npm test` green (Vitest yield + toast cases).
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml` green.
- `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml --all-targets -- -D warnings` clean (P-43-07).
- `cargo check --target aarch64-apple-ios --manifest-path hp41-gui/src-tauri/Cargo.toml` passes (P-43-08).
- `just gui-ci` green.
- `grep -n "alarm:interrupting" hp41-gui/src/App.tsx` → empty.
</verification>

<success_criteria>
- pending_yield projected + rendered; resume scheduled via existing tick cadence (no polling, D-11).
- Missing-label toast (criterion 2 / D-08).
- D-25.6 parity: GUI half landed alongside CLI half (63-03).
- display_override untouched (DISP-01 stays deferred).
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-04-SUMMARY.md` when done.
</output>
