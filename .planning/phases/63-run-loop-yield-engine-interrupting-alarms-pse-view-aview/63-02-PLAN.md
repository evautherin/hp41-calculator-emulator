---
phase: 63-run-loop-yield-engine-interrupting-alarms-pse-view-aview
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - hp41-core/src/ops/program.rs
autonomous: true
requirements: [ALARM-02, ALARM-03, PRGM-01, PRGM-02]

must_haves:
  truths:
    - "A running program is interrupted at the next instruction boundary when pending_interrupt is set; the alarm label runs in the interrupted program's register environment; the original program resumes at exactly the halted pc."
    - "At the 4-level call-stack cap the interrupt is SILENTLY suppressed (no 5th frame, no CallDepth error), pending_interrupt is dropped, the alarm stays past_due, and is_running is restored on every path including errors."
    - "A repeating interrupting alarm is acknowledged/rescheduled only after its handler RTNs (not on cap-drop or missing-label)."
    - "A missing handler label surfaces alarm:missing:{label} on event_buffer; no panic; no ack."
    - "PSE/VIEW/AVIEW mid-run set pending_yield (kind/text/resume_ms=1000) and break run_loop without writing display_override; resume continues at the next step."
    - "pending_interrupt/pending_interrupt_alarm_index/pending_interrupt_depth/pending_yield are cleared at run_program and resume_program entry (interrupt set just before STOP is dropped on resume)."
  artifacts:
    - path: "hp41-core/src/ops/program.rs"
      provides: "run_loop interrupt check + Phase-C check + ack-after-RTN + PSE/VIEW/AVIEW yield arms + clear-on-entry"
      contains: "pending_interrupt.take()"
  key_links:
    - from: "run_loop interrupt check"
      to: "find_in_program + call_stack.push"
      via: "synthetic XEQ frame (mirrors Op::Xeq); captures pending_interrupt_depth"
      pattern: "call_stack.push"
    - from: "Op::Rtn pop"
      to: "acknowledge_alarm"
      via: "ack after handler returns to pending_interrupt_depth"
      pattern: "acknowledge_alarm"
    - from: "Op::Pse / Op::View / Op::AView run_loop arms"
      to: "state.pending_yield"
      via: "set YieldState + break (no display_override)"
      pattern: "pending_yield = Some"
---

<objective>
Phase B/C/D + yield consumers, all inside `program.rs`: add the `run_loop` interrupt boundary (synthetic XEQ frame), the periodic Phase-C `check_alarms` (~every 1000 steps), the ack-after-RTN reschedule, the clear-on-entry in `run_program`/`resume_program`, and dedicated `run_loop` arms for `Op::Pse`/`Op::View`/`Op::AView` that set `pending_yield` and break. This turns the run_loop-dependent scenarios authored in 63-01 from RED to GREEN.

Purpose: This is the engine. It consumes the state contract from 63-01 and produces the behavior all five Success Criteria require (server-side, frontend-agnostic). The same `run_loop` is what BOTH frontends drive — the CLI via 63-03's render→sleep→resume loop, and the GUI via 63-06's yield-and-resume driver over the new 63-04 commands. By keeping the entire interrupt/yield mechanism inside the core `run_loop`, criteria 1/4/5 become reachable in BOTH frontends with thin glue (SC-4).
Output: A working synchronous yield/interrupt boundary; all 16 VALIDATION core scenarios GREEN.

This plan owns `program.rs` EXCLUSIVELY (no other plan in any wave touches it) — avoids the shared-git-index race (reference_parallel_wave_index_contention). It reads/writes `state.pending_interrupt_depth` (declared by 63-01 in state.rs) but does NOT touch state.rs, preserving wave isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-CONTEXT.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-RESEARCH.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-PATTERNS.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-VALIDATION.md
@.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-01-SUMMARY.md
@hp41-core/src/ops/program.rs
@hp41-core/src/ops/display_ops.rs

<interfaces>
<!-- Anchors verified against develop 2026-06-06. Use directly. -->

program.rs:
- `run_program` (422): `pc = start+1` (444), `call_stack.clear()` (445), `is_running = true` (446), `run_loop` (448), `is_running = false` (450). INSERT the four-field clear right after 445.
- `resume_program` (468): guard `pc >= program.len()` → InvalidOp (469); `is_running = true` (473); `run_loop` (474). INSERT the four-field clear BEFORE re-entering run_loop (474) — D-09.
- `run_loop` (485): `steps >= MAX_STEPS` (~488); `steps += 1` (491); `pc >= program.len()` break (492–495); `op = program[pc].clone(); pc += 1` (496–497). INSERT interrupt `take()` + Phase-C check between `steps += 1` and the `pc >= len` break.
- `MAX_STEPS` (483) = 1_000_000. Phase-C cadence independent (`steps % 1000 == 0`).
- `Op::Rtn` arm (500–504): `call_stack.pop()` → `pc = return_pc`, else break. D-06 ack point: after the pop that restores the INTERRUPTED program's resume address.
- `Op::Stop` arm (657): break.
- `Op::Prompt` arm (660–663): writes display_override + break — TEMPLATE for the yield break.
- `Op::Xeq` arm (538–573): `call_stack.len() >= 4` → Err(CallDepth) (539); push(pc) (548); pc = target+1 (549). The synthetic interrupt frame MIRRORS push+jump but cap-drop is SILENT (no Err).
- `find_in_program` (1320): label→index resolver. Use for the synthetic frame.
- `execute_op` (730): VIEW/AVIEW/PSE currently execute here (884/885/902). The `other =>` catch-all in run_loop (710) routes to execute_op_pub. Adding dedicated run_loop arms for Pse/View/AView means they no longer fall through to execute_op — remove/guard their execute_op bodies so they are not double-executed, OR keep execute_op bodies for the non-program (interactive keystroke) path and ensure run_loop arms are matched BEFORE the `other =>` catch-all. Mirror exactly how Op::Prompt is handled in run_loop yet still listed in execute_op (program.rs:881 comment).

display_ops.rs:
- `op_view` (17): `format_hpnum(regs[reg], display_mode)` → display_override (23). Yield arm captures THIS formatted string.
- `op_aview` (30): `alpha_reg.chars().take(24)` → display_override (32). Yield arm captures this string.

state.rs (from 63-01):
- `pending_interrupt: Option<String>`, `pending_interrupt_alarm_index: Option<usize>`, `pending_interrupt_depth: Option<usize>`, `pending_yield: Option<YieldState>`, `YieldKind{Pse,View,Aview}`, `YieldState{kind,text,resume_ms}`, `PSE_RESUME_MS = 1000`. All four transient + `#[serde(default, skip)]`; `pending_interrupt_depth` is declared by 63-01 expressly so THIS plan can use it without editing state.rs.
- `acknowledge_alarm(state, index)` in time/alarm.rs (474).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_loop interrupt boundary + Phase-C check + clear-on-entry</name>
  <files>hp41-core/src/ops/program.rs</files>
  <behavior>
    - `interrupting_alarm_halts_running_program_and_resumes`: handler runs, then resume continues at the halted step (regs reflect pre-interrupt, handler, and post-resume writes); call_stack empty + is_running false at end.
    - `interrupt_preserves_stack_x_y_z_t_and_lift_state`: stack.lift_enabled and pc at resume match the interrupt point.
    - `interrupt_blocked_when_call_stack_at_4_level_cap`: alarm label NOT run; call_stack stays 4; no CallDepth propagated; is_running unchanged; alarm left past_due; pending_interrupt dropped.
    - `missing_handler_label_surfaces_event`: unknown label → event_buffer has alarm:missing:{label}; no panic; no ack.
    - `interrupting_alarm_fires_when_no_program_running` (regression): idle path still queues alarm:xeq (Phase C does not fire it inline).
    - `pending_interrupt_cleared_on_resume_after_stop`: interrupt set before Op::Stop; resume_program clears it; resume does NOT redirect to handler.
  </behavior>
  <action>
    In `run_loop`, between `steps += 1` (491) and the `pc >= program.len()` break (492):
    1. Interrupt check — `if let Some(label) = state.pending_interrupt.take() { … }`:
       - If `state.call_stack.len() >= 4` → SILENT suppress: do nothing further (label already taken/dropped), leave the alarm `past_due` (do NOT call acknowledge), do NOT push a frame, do NOT return Err (D-07). Clear `pending_interrupt_alarm_index = None;` and `pending_interrupt_depth = None;`. Continue the loop.
       - Else `match find_in_program(program, &label)`:
         - `Ok(target)` → capture injection depth FIRST: `state.pending_interrupt_depth = Some(state.call_stack.len());` then `state.call_stack.push(state.pc); state.pc = target + 1;` (verbatim Op::Xeq push/jump). Leave `pending_interrupt_alarm_index` set (Task 2 acks it after RTN). Continue.
         - `Err(_)` → `state.event_buffer.push(format!("alarm:missing:{label}"));` clear `pending_interrupt_alarm_index = None;` and `pending_interrupt_depth = None;` (D-08, no ack). Continue.
    2. Phase-C check — `if steps % 1000 == 0 { time::alarm::check_alarms(state); }` (D-05). This may set `pending_interrupt`; it is consumed on the NEXT iteration's interrupt check (acceptable — sub-ms granularity).
    Ensure `is_running` safety reset: capture the `run_loop` result into a `let result = …;` in `run_program`/`resume_program`, reset `is_running = false`, then return `result` — never `?`-short-circuit past the cleanup (Pitfall 2; the existing 448–451 / 473–476 pattern already does this — keep it).
    Clear-on-entry: in `run_program` after line 445 add `state.pending_interrupt = None; state.pending_interrupt_alarm_index = None; state.pending_interrupt_depth = None; state.pending_yield = None;`. In `resume_program` before re-entering run_loop (474) add the SAME four-line clear (D-09).
  </action>
  <verify>
    <automated>just test-core --test phase_63_interrupting_alarms 2>&1 | tail -25</automated>
  </verify>
  <done>Interrupt-halts-resumes, cap-drop (silent, no 5th frame, no CallDepth, is_running restored), missing-label, idle-regression, and cleared-on-resume-after-stop scenarios GREEN. No `?`-short-circuit past is_running cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Acknowledge + reschedule repeating alarm after handler RTN</name>
  <files>hp41-core/src/ops/program.rs</files>
  <behavior>
    - `repeating_interrupting_alarm_reschedules_after_handler`: after the handler RTNs back to the interrupted program, acknowledge_alarm advanced trigger_unix (repeat) or removed (one-shot); cap-drop and missing-label do NOT reschedule.
    - `interrupt_nesting_blocked_when_already_in_alarm_program`: a second interrupt while the handler runs is demoted (set by 63-01 routing) — depth never exceeds the single injected frame; ack still fires once for the first alarm.
  </behavior>
  <action>
    In the `Op::Rtn` arm (500–504), at the point where `call_stack.pop()` restores the return pc: gate the alarm ack on the synthetic-frame injection depth recorded in Task 1.
    Use `pending_interrupt_depth` (declared by 63-01) as the disambiguator: the handler may itself XEQ deeper (pushing more frames), so the ack must fire ONLY when the pop returns the call_stack to the depth it had at injection — i.e. `if state.pending_interrupt_alarm_index.is_some() && Some(state.call_stack.len()) == state.pending_interrupt_depth { … }` (compare AFTER the pop). When that condition holds: `let idx = state.pending_interrupt_alarm_index.take().expect("checked is_some"); time::alarm::acknowledge_alarm(state, idx); state.pending_interrupt_depth = None;`. Avoid `unwrap()` — use the `take()` + the matched `is_some` guard, or `if let Some(idx) = state.pending_interrupt_alarm_index { if Some(state.call_stack.len()) == state.pending_interrupt_depth { … } }` (I-04).
    Do NOT ack on the cap-drop or missing-label paths — `pending_interrupt_alarm_index` and `pending_interrupt_depth` were already cleared there in Task 1, so the gate naturally skips them.
    `acknowledge_alarm` (alarm.rs:474) already handles repeat (trigger_unix += repeat_secs, past_due=false) vs one-shot (remove) — just call it.
  </action>
  <verify>
    <automated>just test-core --test phase_63_interrupting_alarms 2>&1 | tail -25</automated>
  </verify>
  <done>Repeating alarm reschedules after handler RTN; cap-drop and missing-label do not reschedule; nesting-blocked scenario GREEN; ack fires exactly once per handled interrupt; no `unwrap()` added (I-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PSE/VIEW/AVIEW yield arms in run_loop</name>
  <files>hp41-core/src/ops/program.rs</files>
  <behavior>
    - `pse_mid_run_breaks_and_records_resume_ms`: Op::Pse sets pending_yield{kind:Pse, text:format_hpnum(X,…), resume_ms:1000}; run_loop breaks; display_override NOT written.
    - `pse_resume_continues_to_next_step`: after resume_program, execution continues at the step after PSE; pending_yield cleared (by resume_program clear-on-entry from Task 1).
    - `view_mid_run_captures_formatted_register_into_yield`: pending_yield.text == format_hpnum(regs[reg],…), kind:View, breaks, resumes.
    - `aview_mid_run_captures_alpha_into_yield`: pending_yield.text == alpha[..24], kind:Aview, breaks, resumes.
  </behavior>
  <action>
    Add dedicated `run_loop` arms for `Op::Pse`, `Op::View(reg)`, `Op::AView`, placed BEFORE the `other =>` catch-all (710) so they no longer fall through to execute_op during program execution (mirror the Op::Prompt arm at 660):
    - `Op::Pse`: compute the same display string as execute_op currently does at 904 (`format_hpnum(state.stack.x, display_mode)`); set `state.pending_yield = Some(YieldState { kind: YieldKind::Pse, text, resume_ms: PSE_RESUME_MS });`; LiftEffect::Neutral semantics preserved (no lift change); then `break`. Do NOT push the legacy `"PAUSE 1000"` event marker (D-04 rejects overloading it) and do NOT write display_override.
    - `Op::View(reg)`: compute `op_view`'s formatted string (the `format_hpnum(regs[reg], display_mode)` from display_ops.rs:23) into `text`; set pending_yield{kind:View,...}; break. Do not write display_override.
    - `Op::AView`: compute `op_aview`'s `alpha_reg.chars().take(24).collect()` (display_ops.rs:32) into `text`; set pending_yield{kind:Aview,...}; break. Do not write display_override.
    `pc` already advanced past the op (496–497), so resume continues at the next step correctly.
    IMPORTANT — interactive path unchanged: the execute_op bodies for VIEW/AVIEW/PSE (884/885/902) drive the NON-program (keystroke) path that writes display_override; keep them intact for that path. Only the run_loop (program-execution) path yields. If the existing exhaustive match in execute_op requires these arms remain present, leave them; just ensure run_loop matches them first so they are not double-executed inside a running program. Verify no `Op` variant exhaustiveness breaks (no new variants — D-12 / I-05).
  </action>
  <verify>
    <automated>just test-core --test phase_63_yield_engine 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 yield scenarios GREEN; pending_yield carries kind/text/resume_ms=1000; run_loop breaks; display_override NOT written by program-path PSE/VIEW/AVIEW; interactive keystroke path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alarm label → run_loop synthetic frame | A past-due alarm injects a label-program into the running interpreter; must respect the 4-level cap and not corrupt the interrupted program's stack/registers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03 | Elevation | synthetic interrupt frame vs 4-level cap | mitigate | cap check `call_stack.len() >= 4` → silent suppress, never push 5th frame, never Err(CallDepth) (D-07); is_running restored on all paths via captured-result cleanup. |
| T-63-04 | DoS | Phase-C check cost | accept | check_alarms runs only `steps % 1000 == 0` — sub-ms at real speed, ~one extra scan per 1000 ops (D-05). |
| T-63-05 | Tampering | repeating-alarm re-arm timing | mitigate | acknowledge_alarm called ONLY after handler RTN at the injection depth (D-06); cap-drop/missing-label never re-arm — no spurious reschedule. |
| T-63-SC | Tampering | npm/pip/cargo installs | mitigate | No new deps (I-06); reuse Vec<usize>/usize/Option. `git diff HEAD -- hp41-core/Cargo.toml` empty. |
</threat_model>

<verification>
- `just test-core` green — ALL 16 VALIDATION scenarios GREEN (both files).
- `git diff HEAD -- hp41-core/src/ops/math1/` empty (math1 freeze, I-01).
- `git diff HEAD -- hp41-core/src/state.rs` empty (wave isolation — this plan does NOT edit state.rs; it only reads/writes 63-01's pending_interrupt_depth).
- `grep -rn 'unwrap()' hp41-core/src/ops/program.rs | grep -v '#\[allow'` → empty (I-04).
- `grep -rn 'println!\|eprintln!' hp41-core/src/` → empty (I-07).
- `grep -rn 'PAUSE 1000' hp41-core/src/` → empty (legacy marker removed).
- No new `Op` variant: 4-way exhaustive match not triggered (I-05).
- `just ci` green (lint + test + coverage ≥95% lines/≥93% regions + license-audit + schema-aliases).
- `just ci-msrv` green + `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings` green (P-43-06).
</verification>

<success_criteria>
- Criterion 1: interrupt halts at next boundary, handler runs in interrupted env, resumes at halt pc.
- Criterion 2: 4-level cap → silent suppress, no 5th frame, is_running restored on all paths.
- Criterion 3: idle interrupting alarm via existing path (regression holds).
- Criterion 4: PSE sets pending_yield + breaks (resume_ms=1000).
- Criterion 5: VIEW/AVIEW set pending_yield + break mid-run.
- All 16 core VALIDATION scenarios GREEN. These behaviors are frontend-agnostic; the CLI (63-03) and GUI (63-04 commands + 63-06 driver) make them visible/runnable in BOTH frontends.
</success_criteria>

<output>
Create `.planning/phases/63-run-loop-yield-engine-interrupting-alarms-pse-view-aview/63-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
