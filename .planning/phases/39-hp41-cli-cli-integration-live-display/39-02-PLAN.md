---
phase: 39-hp41-cli-cli-integration-live-display
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - hp41-cli/src/ui.rs
  - hp41-cli/src/app.rs
autonomous: false
requirements:
  - TIME-CLI-05
  - TIME-CLI-06
  - TIME-CLI-07

must_haves:
  truths:
    - "User activates clock display (XEQ CLKT) and sees live-updating time in TUI LCD at >= 1 Hz without pressing keys"
    - "User activates stopwatch mode (XEQ SW) and sees centisecond-resolution elapsed time updating at >= 10 Hz with Space/Enter toggling start/stop, s for split, r for reset, Esc to exit"
    - "When an alarm comes due, user sees alarm message in status bar without pressing any key"
    - "Any keypress exits clock display mode but the key is still processed normally (not consumed)"
  artifacts:
    - path: "hp41-cli/src/ui.rs"
      provides: "get_display_string() with clock/stopwatch priority above entry_buf"
      contains: "get_clock_display_str"
    - path: "hp41-cli/src/app.rs"
      provides: "handle_stopwatch_mode_key(), drain_event_buffer(), check_alarms in run() loop"
      contains: "drain_event_buffer"
  key_links:
    - from: "hp41-cli/src/ui.rs"
      to: "hp41_core::ops::time::clock::get_clock_display_str"
      via: "function call in get_display_string"
      pattern: "get_clock_display_str"
    - from: "hp41-cli/src/ui.rs"
      to: "hp41_core::ops::time::stopwatch::get_stopwatch_display_str"
      via: "function call in get_display_string"
      pattern: "get_stopwatch_display_str"
    - from: "hp41-cli/src/app.rs"
      to: "hp41_core::ops::time::alarm::check_alarms"
      via: "called on every 16ms tick in run() loop"
      pattern: "check_alarms"
---

<objective>
Wire live-updating clock and stopwatch display into the TUI, add interactive stopwatch keyboard mode with dedicated key bindings, and integrate alarm event draining into the CLI dispatch and main loop.

Purpose: This plan makes the Time Pac a live, interactive experience in the CLI -- the clock updates continuously, the stopwatch runs at centisecond resolution, and alarms fire in real-time without user interaction. These are the first real-time behaviors in the previously event-driven emulator.

Output: Modified ui.rs (display priority), modified app.rs (stopwatch key mode, alarm drain, clock-exit).
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-hp41-cli-cli-integration-live-display/39-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hp41-core/src/ops/time/clock.rs:
- `pub fn get_clock_display_str(state: &CalcState) -> Option<String>` -- returns Some(formatted_time) when state.clock_active is true, None otherwise

From hp41-core/src/ops/time/stopwatch.rs:
- `pub fn get_stopwatch_display_str(state: &CalcState) -> Option<String>` -- returns Some(HH:MM:SS.hh) when state.stopwatch_keyboard_mode is true, None otherwise
- `pub enum StopwatchMode { Idle, Running, Stopped }` -- at stopwatch.rs:48

From hp41-core/src/ops/time/alarm.rs:
- `pub fn check_alarms(state: &mut CalcState)` -- at alarm.rs:479; pushes "alarm:message:TEXT" or "alarm:xeq:LABEL" strings into state.event_buffer

From hp41-core/src/state.rs:
- `pub clock_active: bool` -- transient, serde(default, skip)
- `pub stopwatch_keyboard_mode: bool` -- transient, serde(default, skip)
- `pub stopwatch_mode: StopwatchMode` -- persistent, serde(default)
- `pub event_buffer: Vec<String>` -- transient, serde(default, skip)

From hp41-core/src/ops/mod.rs:
- `Op::TimeRunsw`, `Op::TimeStopsw`, `Op::TimeSwpt`, `Op::TimeStpw` -- relevant Op variants for stopwatch dispatch

From hp41-core/src/ops/program.rs:
- `pub fn run_program(state: &mut CalcState, entry_label: &str) -> Result<(), HpError>` -- for alarm:xeq dispatch

From hp41-cli/src/app.rs:
- `pub fn run()` at line 265 -- main loop: draw -> poll(16ms) -> handle_key -> check_autosave
- `pub fn handle_key()` at line 296 -- key dispatch: Release filter -> last_key_code -> Ctrl+C -> Ctrl+S -> ? -> pending_input -> modal-openers -> alpha_mode -> f-prefix -> ...
- `pub fn call_dispatch()` at line 1769 -- dispatch + maybe_auto_open_collect_for_modal
- `pub(crate) fn call_dispatch_and_drain()` at line 1783 -- dispatch + card drain + print drain + maybe_auto_open_collect_for_modal
- `fn maybe_auto_open_collect_for_modal()` at line 1831 -- called at tail of both dispatch methods
- `self.message: Option<String>` -- status bar text; set on errors, print output, save messages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live display priority + clock exit + stopwatch keyboard mode + alarm drain</name>
  <files>hp41-cli/src/ui.rs, hp41-cli/src/app.rs</files>
  <read_first>
    - hp41-cli/src/ui.rs lines 129-152 (get_display_string current priority chain)
    - hp41-cli/src/app.rs lines 265-283 (run() loop -- draw, poll, check_autosave)
    - hp41-cli/src/app.rs lines 296-455 (handle_key -- Release filter through alpha_mode block -- understand insertion points)
    - hp41-cli/src/app.rs lines 1619-1648 (handle_alpha_mode_key -- structural template for handle_stopwatch_mode_key)
    - hp41-cli/src/app.rs lines 1769-1817 (call_dispatch and call_dispatch_and_drain -- tail sequence where drain_event_buffer appends)
    - hp41-core/src/ops/time/clock.rs line 297 (get_clock_display_str signature)
    - hp41-core/src/ops/time/stopwatch.rs lines 48-53 (StopwatchMode enum) and line 190 (get_stopwatch_display_str signature)
    - hp41-core/src/ops/time/alarm.rs line 479 (check_alarms signature)
  </read_first>
  <action>
    PART A -- ui.rs get_display_string() priority insertion (D-39.1):

    Modify get_display_string() at line 131 to insert clock and stopwatch checks at the TOP, before the existing entry_buf check. Update the doc comment to "Priority: clock_active > stopwatch_keyboard_mode > entry_buf > prgm step > alpha > formatted X".

    Insert two if-let checks at the beginning of the function body, after `let st = &app.state;`:
    1. Call hp41_core::ops::time::clock::get_clock_display_str(st) -- if Some(s), return s immediately
    2. Call hp41_core::ops::time::stopwatch::get_stopwatch_display_str(st) -- if Some(s), return s immediately

    The existing chain (entry_buf -> prgm -> alpha -> formatted X) remains unchanged below these two new checks. Add the necessary use statement at the top of ui.rs if not already present.

    PART B -- app.rs handle_key() clock-exit and stopwatch-mode intercepts (D-39.3, D-39.4):

    In handle_key(), insert two blocks AFTER the alpha_mode check (line 452-455) and BEFORE the f-prefix state machine (line 457). The ordering is: alpha_mode -> clock-exit -> stopwatch-mode -> f-prefix.

    Clock-exit block (D-39.3): if self.state.clock_active is true, set it to false. Do NOT return -- the key falls through for normal processing. This is a mutation-only block, not a routing block. Comment: "D-39.3: any key exits clock display -- clear flag, key still processed normally".

    Stopwatch-mode block (D-39.4): if self.state.stopwatch_keyboard_mode is true, call self.handle_stopwatch_mode_key(key) and return. This IS a routing block (like alpha_mode) -- keys are consumed by the stopwatch handler.

    PART C -- app.rs new handle_stopwatch_mode_key() method (D-39.4, D-39.5):

    Add a new method handle_stopwatch_mode_key(&mut self, key: KeyEvent) following the handle_alpha_mode_key pattern. Key bindings (Claude's Discretion):
    - KeyCode::Esc => set self.state.stopwatch_keyboard_mode = false (exit mode)
    - KeyCode::Char(' ') or KeyCode::Enter => toggle start/stop: if self.state.stopwatch_mode == StopwatchMode::Running, call self.call_dispatch(Op::TimeStopsw); else call self.call_dispatch(Op::TimeRunsw)
    - KeyCode::Char('s') => call self.call_dispatch(Op::TimeSwpt) (split/lap)
    - KeyCode::Char('r') => call self.call_dispatch(Op::TimeStpw) (reset)
    - _ => ignore (all other keys consumed silently in stopwatch mode)

    Import StopwatchMode from hp41_core::ops::time::StopwatchMode. Import the relevant Op variants if not already imported.

    PART D -- app.rs drain_event_buffer() method and alarm check in run() loop (D-39.6, D-39.7):

    Add a new method drain_event_buffer(&mut self) that:
    1. Collects state.event_buffer.drain(..) into a Vec<String>
    2. For each event string: if it starts with "alarm:message:" prefix, set self.message = Some(remainder). If it starts with "alarm:xeq:" prefix, call hp41_core::ops::program::run_program(&mut self.state, remainder) and on error set self.message. BEEP/TONE events are ignored.

    In run() loop (line 265-283), after self.check_autosave() (line 276), add:
    1. hp41_core::ops::time::alarm::check_alarms(&mut self.state); -- on every 16ms tick, not just on keypress (D-39.7)
    2. self.drain_event_buffer(); -- process any alarm events

    In call_dispatch() (line 1769): append self.drain_event_buffer() AFTER self.maybe_auto_open_collect_for_modal() (D-39.6).

    In call_dispatch_and_drain() (line 1783): append self.drain_event_buffer() AFTER self.maybe_auto_open_collect_for_modal() (D-39.6).

    PART E -- app.rs alarm message clear on keypress (D-39.8):

    The existing pattern at call_dispatch line 1771 already clears self.message = None on Ok. This means alarm messages naturally persist until the next successful dispatch clears them. No additional code needed for D-39.8 -- the existing message lifecycle handles it.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo build -p hp41-cli 2>&1 | tail -5 && cargo clippy -p hp41-cli -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ui.rs get_display_string() first two checks are get_clock_display_str and get_stopwatch_display_str (before entry_buf)
    - app.rs handle_key() contains clock_active = false block without a return statement following it
    - app.rs handle_key() contains stopwatch_keyboard_mode check with return (routing block)
    - app.rs contains fn handle_stopwatch_mode_key with match arms for Esc, Space/Enter, 's', 'r'
    - app.rs contains fn drain_event_buffer with strip_prefix("alarm:message:") and strip_prefix("alarm:xeq:") routing
    - app.rs run() loop calls check_alarms and drain_event_buffer after check_autosave
    - app.rs call_dispatch() tail calls drain_event_buffer() after maybe_auto_open_collect_for_modal()
    - app.rs call_dispatch_and_drain() tail calls drain_event_buffer() after maybe_auto_open_collect_for_modal()
    - cargo build -p hp41-cli compiles without errors
    - cargo clippy -p hp41-cli -- -D warnings passes
  </acceptance_criteria>
  <done>Clock display overrides all other display modes in TUI; any keypress exits clock mode without consuming the key; stopwatch mode has dedicated key bindings (Space/Enter=start/stop, s=split, r=reset, Esc=exit); alarm events drain on every 16ms tick and after every dispatch; alarm messages surface in status bar</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify live display and alarm behavior</name>
  <what-built>Live clock display, interactive stopwatch mode, and alarm notification draining in the CLI TUI</what-built>
  <how-to-verify>
    1. Run `cargo run -p hp41-cli` in a terminal (80x24 minimum)
    2. Clock display test: Type the keys to execute XEQ "CLKT" (press 'e', type CLKT, press Enter). Observe the LCD area -- it should show the current time updating at least once per second without pressing any key. Press any key (e.g., '+') -- clock display should stop AND the '+' operation should execute (not be consumed).
    3. Stopwatch test: Type the keys to execute XEQ "SW" (press 'e', type SW, press Enter). The LCD should show "00:00:00.00" (or similar format). Press Space or Enter -- elapsed time should start updating at ~10 Hz (centisecond resolution visible). Press Space/Enter again to stop. Press 's' for split. Press 'r' for reset. Press Esc to exit stopwatch mode.
    4. Alarm test: Execute XEQ "ALMNOW" -- a status-bar message should appear showing the alarm notification.
    5. Ctrl+C to quit.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event_buffer strings | alarm.rs pushes string events; app.rs parses prefixes. Strings are emulator-internal, not user-controlled |
| alarm:xeq label dispatch | Control alarm label is from the alarm catalog (user-stored via XYZALM). run_program validates the label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Spoofing | event_buffer prefix routing | accept | event_buffer is internal Vec<String> on CalcState; no external input path; prefix routing is deterministic |
| T-39-04 | Denial of Service | check_alarms on every 16ms tick | accept | check_alarms is O(n) where n = alarm count (max 253); negligible cost at 62 Hz poll rate |
</threat_model>

<verification>
cargo build -p hp41-cli compiles without errors
cargo clippy -p hp41-cli -- -D warnings passes
Manual verification of live clock, stopwatch, and alarm behavior
</verification>

<success_criteria>
- Clock display updates at >= 1 Hz in TUI when clock_active is true
- Stopwatch display updates at >= 10 Hz with centisecond resolution when stopwatch_keyboard_mode is true
- Alarm messages appear in status bar when alarms come due (even without keypress)
- Clock exit on keypress does not consume the key
- Stopwatch key bindings work (Space/Enter, s, r, Esc)
</success_criteria>

<output>
Create `.planning/phases/39-hp41-cli-cli-integration-live-display/39-02-SUMMARY.md` when done
</output>
