---
phase: 35-documentation-adrs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/docs-matrix/src/main.rs
  - justfile
  - docs/hp41-stat1-function-matrix.md
  - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md
autonomous: true
requirements:
  - STAT-DOC-01
  - STAT-DOC-02
  - STAT-DOC-03
tags:
  - docs
  - tooling
  - matrix-renderer
  - ci-gate
  - spec-amendment
must_haves:
  truths:
    - "Running `just docs-matrix` regenerates ALL THREE function matrices (`docs/hp41cv-function-matrix.md` unchanged, `docs/hp41-math1-function-matrix.md` unchanged, `docs/hp41-stat1-function-matrix.md` newly created from 26-entry `docs/hp41-stat1-functions.json`)."
    - "Running `just docs-matrix-check` exits 0 when all three committed matrix files are in sync with their JSON sources, and exits non-zero when any drifts. CI continuity preserved — `.github/workflows/ci.yml` requires no edits because it already invokes `just docs-matrix-check`."
    - "`docs/hp41-stat1-function-matrix.md` renders an XROM column showing `Stat 1 / 2-N` per entry — D-30.3 conditional column emission fires automatically because every entry in `hp41-stat1-functions.json` carries an `xrom` block per Phase 34 D-34.1; the matrix-renderer's existing line-103 `has_xrom` check requires no code change."
    - "`scripts/docs-matrix/src/main.rs` keeps its 1-in/1-out CLI signature per D-30.1; the change is a single 4-line `else if` branch inserted into the basename dispatch at lines 70–76 — no struct widening, no signature change, no new dependencies."
    - "`33-SPEC-AMENDMENT.md` lands as a SIBLING file (NOT an in-place edit of `33-SPEC.md`) per D-35.1; documents the 6 oracle drifts enumerated in `33-VERIFICATION.md` lines 13–22 + 186–187 as a 6-row markdown table with scipy-correct values + test file:line + drift root cause + `D-35-NN` cross-references."
    - "The hp41cv path remains bit-for-bit unchanged (D-30.2 invariant carried forward); the hp41-math1 matrix also unchanged (D-35 phase 35 does NOT re-render either prior matrix; verified by `git diff --stat docs/hp41cv-function-matrix.md docs/hp41-math1-function-matrix.md` returning empty after the regeneration)."
  artifacts:
    - path: "scripts/docs-matrix/src/main.rs"
      provides: "Three-way basename dispatch (cv + math1 + stat1)"
      contains: "hp41-stat1-functions.json"
    - path: "justfile"
      provides: "Three-input docs-matrix + docs-matrix-check recipes"
      contains: "hp41-stat1-functions.json"
    - path: "docs/hp41-stat1-function-matrix.md"
      provides: "Generated Stat 1 Pac function matrix"
      contains: "Stat 1 / 2-"
      min_lines: 30
    - path: ".planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md"
      provides: "6-row oracle-drift reconciliation table"
      contains: "D-35-"
      min_lines: 30
  key_links:
    - from: "justfile::docs-matrix"
      to: "docs/hp41-stat1-functions.json -> docs/hp41-stat1-function-matrix.md"
      via: "cargo run -- <input> <output>"
      pattern: "hp41-stat1-functions\\.json"
    - from: "justfile::docs-matrix-check"
      to: "/tmp/hp41-stat1-function-matrix-check.md (diff vs committed)"
      via: "cargo run + diff -u"
      pattern: "hp41-stat1-function-matrix"
    - from: "scripts/docs-matrix/src/main.rs::render_markdown"
      to: "stat1 title and src path"
      via: "basename ends_with check (lines 70-76)"
      pattern: "hp41-stat1-functions\\.json"
    - from: ".planning/phases/33-.../33-SPEC-AMENDMENT.md rows"
      to: "docs/hp41-stat1-divergences.md (Plan 35-02 bucket-3 entries)"
      via: "D-35-NN cross-reference column"
      pattern: "D-35-[0-9]{2}"
---

<objective>
Land the three docs-matrix infrastructure deliverables for Phase 35 in a single tight blast-radius plan:

1. Extend `scripts/docs-matrix/src/main.rs` basename dispatch with a third `else if` branch for `hp41-stat1-functions.json` (4-line edit; D-30.1 1-in/1-out signature preserved).
2. Extend justfile `docs-matrix` and `docs-matrix-check` recipes with the third invocation (mirrors the existing math1 invocation that mirrored the cv invocation).
3. Generate `docs/hp41-stat1-function-matrix.md` from the 26-entry `docs/hp41-stat1-functions.json` (authored Phase 34) via the extended pipeline; confirm `just docs-matrix-check` CI gate stays green.
4. Land `.planning/phases/33-.../33-SPEC-AMENDMENT.md` as a SIBLING to `33-SPEC.md` (history-preserving per D-35.1) with the 6-row oracle-drift reconciliation table.

Purpose: ship the structural drift-catch infrastructure for v3.1 documentation. The matrix file becomes the user-facing index for the soft-claim in Plan 35-04 (README link target). The CI gate inherits the v3.0 Phase 30 pattern unchanged. The SPEC amendment closes the planning-archaeology gap surfaced by `33-VERIFICATION.md`'s deferred items.

Output:
- `scripts/docs-matrix/src/main.rs` extended (4-line `else if` branch in lines 70–76 region)
- `justfile` extended (one cargo-run line appended in `docs-matrix`; one cargo-run + one diff line appended in `docs-matrix-check`)
- `docs/hp41-stat1-function-matrix.md` (new, ≥ 30 lines, 8-col with XROM column, 26 entries)
- `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md` (new, ≥ 30 lines, 6-row drift reconciliation table)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-documentation-adrs/35-CONTEXT.md
@.planning/phases/35-documentation-adrs/35-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-VERIFICATION.md
@.planning/milestones/v3.0-phases/30-documentation-adrs/30-01-PLAN.md
@scripts/docs-matrix/src/main.rs
@justfile
@docs/hp41-stat1-functions.json
@docs/hp41-math1-function-matrix.md

<interfaces>
<!-- Current scripts/docs-matrix/src/main.rs basename dispatch (lines 70–76): -->

```rust
let (title, src) = if basename.ends_with("hp41cv-functions.json") {
    ("# HP-41CV ROM Function Matrix", "`docs/hp41cv-functions.json`")
} else if basename.ends_with("hp41-math1-functions.json") {
    ("# HP-41C Math Pac I Function Matrix", "`docs/hp41-math1-functions.json`")
} else {
    ("# Function Matrix", "`{json_path}`")
};
```

Required edit: insert a third `else if` branch BEFORE the fallthrough `else`. New branch:
- predicate: `basename.ends_with("hp41-stat1-functions.json")`
- title: `"# HP-41C Stat 1 Pac Function Matrix"`
- src: `` "`docs/hp41-stat1-functions.json`" ``

<!-- The has_xrom conditional (line 103) and xrom_cell formatter (lines 133-135) require NO changes — every entry in hp41-stat1-functions.json carries `xrom: { module: "Stat 1", module_id: 2, function_id: N }` per Phase 34 D-34.1. Stat 1 rows will render as `"Stat 1 / 2-N"` automatically. -->

<!-- Current justfile docs-matrix recipe (lines 186-191): two cargo-run invocations (cv + math1). -->
<!-- Current justfile docs-matrix-check recipe (lines 195-202): two cargo-run + diff pairs (cv + math1). -->

<!-- Tmp-file naming convention: /tmp/hp41-stat1-function-matrix-check.md — does NOT collide with the existing cv + math1 tmp files. -->

<!-- 33-VERIFICATION.md lines 13-22 + 186-187 enumerate the 6 oracle drifts: -->
<!-- 1. ΣNORMD CDF 1e-9 SPEC.md tolerance band → reality 1e-5 (rust_decimal A&S 6-term approximation ~1.3e-7 abs error) -->
<!-- 2. ΣAOVONE F = 100.0 → scipy.stats.f_oneway returns F = 50.0 (SSB=250, SSW=30, df=2/12) -->
<!-- 3. ΣSPEAR ρ_s = 0.7 → scipy returns ρ_s = 0.8 -->
<!-- 4. ΣEFXSQ χ² calibration drift -->
<!-- 5. ΣBSTAT CV = 0.4083 → scipy returns 0.5270 -->
<!-- 6. ΣTSTAT deep-tail Student-t p (AS 63 precision limit) -->

<!-- Test file:line evidence (from 33-VERIFICATION.md Observable Truths table + lib tests in normd.rs / anova.rs / nonparam.rs / basic_stats.rs / hypothesis.rs / chisqd.rs):
- ΣNORMD CDF band: hp41-core/src/ops/stat1/normd.rs:257 cdf_oracle_q_of_196_within_a_and_s_band
- ΣAOVONE F=50: hp41-core/src/ops/stat1/anova.rs:340-351 aovone_three_groups_of_five_yields_f_50
- ΣSPEAR ρ_s=0.8: hp41-core/src/ops/stat1/nonparam.rs (planner runs grep for "spear_" test on the file at write time)
- ΣEFXSQ χ²: hp41-core/src/ops/stat1/nonparam.rs (planner runs grep for "efxsq_" test)
- ΣBSTAT CV=0.5270: hp41-core/src/ops/stat1/basic_stats.rs (planner runs grep for "bstat_cv" test)
- ΣTSTAT deep-tail p: hp41-core/src/ops/stat1/hypothesis.rs (planner runs grep for "tstat_" test) — assert with 1e-3 band per 33-VERIFICATION.md acknowledgments line 193 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs-matrix renderer with stat1 basename branch (4-line edit)</name>
  <files>scripts/docs-matrix/src/main.rs</files>
  <read_first>
    - scripts/docs-matrix/src/main.rs (current 168-line single-file binary — read in full to confirm the basename dispatch at lines 70-76 and the has_xrom conditional at line 103 are exactly as PATTERNS.md describes)
    - docs/hp41-stat1-functions.json (head 30 lines suffice — confirm every entry carries `xrom: { module: "Stat 1", module_id: 2, function_id: N }` per D-34.1)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md (D-35.1 / D-30.1 carry-forward — re-confirm "binary stays 1-in/1-out")
    - .planning/phases/35-documentation-adrs/35-PATTERNS.md (lines 78-98 — verbatim required-edit specification)
    - .planning/milestones/v3.0-phases/30-documentation-adrs/30-01-PLAN.md Task 1 (analog 4-line edit pattern; Phase 35 mirrors the structure with no struct widening because the `xrom` field already exists)
  </read_first>
  <action>
Make a single 4-line surgical edit to `scripts/docs-matrix/src/main.rs` in the `render_markdown` function (lines 65–76).

Insert a new `else if` branch IMMEDIATELY BEFORE the existing fallthrough `else { ("# Function Matrix", "`{json_path}`") }` and IMMEDIATELY AFTER the existing math1 branch (currently lines 72-73). The new branch:

- Predicate: `basename.ends_with("hp41-stat1-functions.json")`
- Title literal: `"# HP-41C Stat 1 Pac Function Matrix"`
- Src literal: `` "`docs/hp41-stat1-functions.json`" `` (markdown-backticked path string, matches the existing math1 branch shape)

Match the indentation of the existing `else if` branch above it (no leading whitespace change). The total file delta is exactly 2 added lines (the `} else if ... {` line and the `("# HP-41C Stat 1 Pac Function Matrix", "\`docs/hp41-stat1-functions.json\`")` line) — `git diff --numstat scripts/docs-matrix/src/main.rs` shows `2  0`.

Do NOT touch:
- The `Entry` struct (no widening needed; the `xrom: Option<XromRef>` field already exists per Phase 30 / Plan 30-01).
- The `XromRef` struct.
- The `has_xrom` conditional at line 103 (it already evaluates `true` for any input whose entries carry an `xrom` field; Stat 1 inputs all do).
- The `xrom_cell` formatter at lines 133-135 (`format!("{} / {}-{}", x.module, x.module_id, x.function_id)` already renders `"Stat 1 / 2-N"` for Stat 1 rows).
- The status filter at lines 84-95 (`partition` on `status != "deferred-v3"`).
- `escape_pipe` and `render_table` (unchanged).
- `scripts/docs-matrix/Cargo.toml` or `Cargo.lock`.

Rationale: D-30.1 carry-forward + D-35.1 binary-unchanged invariant. The schema is already prepared for arbitrary XROM-carrying JSON inputs; the only change needed for Stat 1 is the title/src dispatch.
  </action>
  <verify>
    <automated>cargo build --quiet --manifest-path scripts/docs-matrix/Cargo.toml 2>&1 | tee /tmp/build-35-01.log; grep -E "^error|warning: unused" /tmp/build-35-01.log; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hp41-stat1-functions.json" scripts/docs-matrix/src/main.rs` returns ≥ 1 (new predicate present in the binary)
    - `grep -c "HP-41C Stat 1 Pac Function Matrix" scripts/docs-matrix/src/main.rs` returns 1 (new title literal present)
    - `grep -c "HP-41C Math Pac I Function Matrix\|HP-41CV ROM Function Matrix" scripts/docs-matrix/src/main.rs` returns 2 (both prior titles UNCHANGED from HEAD)
    - `cargo build --quiet --manifest-path scripts/docs-matrix/Cargo.toml` exits 0 with no errors and no `warning: unused`
    - `git diff --numstat scripts/docs-matrix/src/main.rs | awk '{print $1}'` returns 2 (exactly 2 inserted lines; the surgical 4-line edit per PATTERNS.md actually adds only 2 new source lines because the existing closing brace + opening brace of the next branch are reused; planner verifies the diff and adjusts the criterion to the actual number if it differs by ≤ 4)
    - `git diff --numstat scripts/docs-matrix/src/main.rs | awk '{print $2}'` returns 0 (no deletions — additive change only)
  </acceptance_criteria>
  <done>Renderer accepts stat1 input filename → emits correct title + src path; cv and math1 dispatch unchanged; binary signature still 1-in/1-out; cargo build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend justfile docs-matrix and docs-matrix-check recipes with stat1 invocation</name>
  <files>justfile</files>
  <read_first>
    - justfile (lines 181-203 — the entire `# ─── Docs ───` section)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md (D-35.1 / D-30.1 carry-forward — "justfile invokes the binary three times")
    - .planning/phases/35-documentation-adrs/35-PATTERNS.md (lines 102-143 — verbatim required-edit specification including the `/tmp/hp41-stat1-function-matrix-check.md` tmp-file path)
  </read_first>
  <action>
Edit `justfile` in two recipes within the `# ─── Docs ───` section (lines 181–203 region):

1. **`docs-matrix` recipe (currently lines 186–191):** append ONE cargo-run invocation directly below the existing math1 invocation. The new invocation reads `docs/hp41-stat1-functions.json` and writes `docs/hp41-stat1-function-matrix.md`. Indentation matches the existing recipe body (tab + continuation backslash on the cargo-run line, tab + tab on the wrapped argument line). The shape mirrors the math1 line that mirrors the cv line.

2. **`docs-matrix-check` recipe (currently lines 195–202):** append ONE cargo-run invocation + ONE `diff -u` command directly below the existing math1 cargo-run + diff pair. The cargo-run writes to `/tmp/hp41-stat1-function-matrix-check.md` (distinct tmp file — no collision with the existing cv + math1 tmp paths). The diff compares `docs/hp41-stat1-function-matrix.md` (committed) against the freshly regenerated tmp file. Exit-non-zero-on-mismatch behavior is inherited from just's default error handling.

3. Preserve the existing recipe comments (D-30.1 + D-30.2 carry-forward annotations above the recipe bodies — leave as-is, or extend the comment block with a `# Reads docs/hp41-stat1-functions.json -> docs/hp41-stat1-function-matrix.md (new, D-35.1)` line if the planner judges it adds value; the substance is the invocation lines).

4. The `[group('docs')]` attribute stays on both recipes.

5. Do NOT touch any other recipe in the file. The CI workflow `.github/workflows/ci.yml` calls `just docs-matrix-check` — no workflow edits needed (CONTEXT.md Claude's Discretion line 159 confirms).

6. After the edit, `git diff --stat justfile` should show approximately `+5 to +8 insertions` (2 cargo-run lines × 2-line continuation in docs-matrix + 1 cargo-run line × 2-line continuation + 1 diff line in docs-matrix-check), 0 deletions.
  </action>
  <verify>
    <automated>grep -c "cargo run.*hp41-stat1-functions" justfile | (read n; [ "$n" -ge 2 ] || (echo "FAIL: expected >= 2 matches, got $n"; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cargo run.*hp41-stat1-functions\\.json" justfile` returns ≥ 2 (one in `docs-matrix`, one in `docs-matrix-check`)
    - `grep -c "cargo run.*hp41-math1-functions\\.json" justfile` returns ≥ 2 (math1 invocations UNCHANGED from HEAD)
    - `grep -c "cargo run.*hp41cv-functions\\.json" justfile` returns ≥ 2 (cv invocations UNCHANGED from HEAD)
    - `grep -c "diff -u docs/hp41-stat1-function-matrix.md" justfile` returns 1
    - `grep -c "/tmp/hp41-stat1-function-matrix-check.md" justfile` returns 2 (cargo-run output target + diff input)
    - `just --list 2>&1 | grep -c "docs-matrix"` returns ≥ 2 (both recipes still parsed by `just`; edited file accepted)
    - `git diff --numstat justfile | awk '{print $2}'` returns 0 (no deletions; purely additive)
  </acceptance_criteria>
  <done>Both recipes invoke the binary three times; third invocation targets the Stat 1 Pac JSON/matrix pair; cv + math1 invocations unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Generate docs/hp41-stat1-function-matrix.md and verify hp41cv + math1 paths unchanged</name>
  <files>docs/hp41-stat1-function-matrix.md</files>
  <read_first>
    - docs/hp41-stat1-functions.json (26 entries — confirms input shape, category distribution, every entry has xrom block)
    - docs/hp41-math1-function-matrix.md (structural template — confirms expected 8-column header with XROM column, "Stat 1 / 2-N" cell format analog "Math 1 / 7-N")
    - docs/hp41cv-function-matrix.md HEAD state (`git show HEAD:docs/hp41cv-function-matrix.md | wc -c` captures the baseline; must be byte-identical after regeneration per D-30.2 carry-forward)
    - docs/hp41-math1-function-matrix.md HEAD state (similarly; must be byte-identical — Phase 35 does NOT re-render math1 either)
  </read_first>
  <action>
Run `just docs-matrix` once after Tasks 1 + 2 are complete. This:

1. Regenerates `docs/hp41cv-function-matrix.md` from `docs/hp41cv-functions.json`. Per the D-30.2 carry-forward invariant, output must be byte-identical to HEAD (no `xrom` fields in the hp41cv JSON → `has_xrom = false` in the renderer → no XROM column → identical output structure). Verify with `git diff --stat docs/hp41cv-function-matrix.md` returning empty.

2. Regenerates `docs/hp41-math1-function-matrix.md` from `docs/hp41-math1-functions.json`. Must also be byte-identical to HEAD (Phase 35 does NOT modify the math1 JSON; the matrix is already committed). Verify with `git diff --stat docs/hp41-math1-function-matrix.md` returning empty.

3. Generates `docs/hp41-stat1-function-matrix.md` from `docs/hp41-stat1-functions.json` (new file). Expected shape:
   - Title (first line): `# HP-41C Stat 1 Pac Function Matrix`
   - Generation hint line: `> Generated from \`docs/hp41-stat1-functions.json\` via \`just docs-matrix\`.`
   - `## Implemented (v2.x)` section (the renderer's existing `status != "deferred-v3"` partition routes every Stat 1 entry to the implemented partition since all entries carry an "implemented" status per Phase 34 JSON authoring)
   - Single 8-column table with header: `| Op | Display | XROM | Category | Status | Phase | Key Path | Description |`
   - 26 rows total (one per JSON entry), sorted by (category, op_variant) per the existing renderer's sort logic
   - Each XROM cell shows `Stat 1 / 2-N` where N is the JSON entry's `function_id`
   - The `## v3.x Deferred (Module Pacs)` section will say `_None._` (no entries carry `deferred-v3` status)

If hp41cv-function-matrix.md or hp41-math1-function-matrix.md drift (any byte change), STOP and investigate before committing. Likely cause would be the renderer's title-dispatch logic being mis-ordered (the new `else if` placed BEFORE math1 or cv branches), causing fall-through. Fix and re-run.

4. Stage all three matrix files for git (hp41cv + math1 unchanged stays staged-empty; stat1 is new). Do NOT auto-commit; the orchestrator's commit step runs at end of plan execution.

Note for execute-phase: this task is the first ever generation of `docs/hp41-stat1-function-matrix.md`. After this lands, every subsequent edit to `docs/hp41-stat1-functions.json` MUST be followed by `just docs-matrix` to regenerate the matrix file, or `just docs-matrix-check` will fail in CI.
  </action>
  <verify>
    <automated>just docs-matrix && test -f docs/hp41-stat1-function-matrix.md && [ "$(wc -l < docs/hp41-stat1-function-matrix.md)" -ge 30 ] && [ -z "$(git diff --stat docs/hp41cv-function-matrix.md)" ] && [ -z "$(git diff --stat docs/hp41-math1-function-matrix.md)" ] && just docs-matrix-check</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/hp41-stat1-function-matrix.md` exits 0
    - `wc -l < docs/hp41-stat1-function-matrix.md` returns ≥ 30
    - `grep -c "^| " docs/hp41-stat1-function-matrix.md` returns ≥ 28 (header + separator + 26 entry rows; floor matches the verified 26-entry JSON source)
    - `grep -c "Stat 1 / 2-" docs/hp41-stat1-function-matrix.md` returns ≥ 26 (every implemented row shows its XROM identity)
    - `head -1 docs/hp41-stat1-function-matrix.md` matches `^# HP-41C Stat 1 Pac Function Matrix$`
    - `git diff --stat docs/hp41cv-function-matrix.md` returns empty output (D-30.2 carry-forward: hp41cv path bit-for-bit unchanged)
    - `git diff --stat docs/hp41-math1-function-matrix.md` returns empty output (Phase 35 does NOT re-render math1 either; same byte-identity invariant)
    - `just docs-matrix-check` exits 0 (all three diffs pass cleanly after the regeneration)
  </acceptance_criteria>
  <done>New matrix file exists with 26 entries + XROM column; hp41cv + math1 matrices byte-identical to HEAD; `just docs-matrix-check` exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Land 33-SPEC-AMENDMENT.md sibling with 6-row oracle-drift reconciliation table</name>
  <files>.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md</files>
  <read_first>
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-VERIFICATION.md (lines 13-22 + 186-187: enumerates all 6 oracle drifts; this is the primary source for the table rows)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md (read enough to find the original requirement IDs / row numbers that drift — planner runs `grep -n "F = 100\|ρ_s = 0.7\|CV = 0.4083\|1e-9.*ΣNORMD\|1e-9.*tolerance" .planning/phases/33-.../33-SPEC.md` to identify the SPEC.md row references; if a particular drift is not pinned to a specific SPEC.md row, use the nearest enclosing Req. number, e.g., "Req. 11 (ΣAOVONE)" rather than a line number)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md (D-35.1 lines 102-105 — history-preserving discipline; D-35.1 specifically forbids in-place editing of 33-SPEC.md)
    - .planning/phases/35-documentation-adrs/35-CONTEXT.md `<specifics>` lines 309-314 — 6-row table column shape
  </read_first>
  <action>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md` as a NEW file at the Phase 33 directory (SIBLING to `33-SPEC.md`). Do NOT modify `33-SPEC.md` in any way — the history-preserving choice per D-35.1 requires the original spec to stay frozen as planning-phase archaeology.

**File header (frontmatter or top-of-file metadata block):**

- Title: `# 33-SPEC-AMENDMENT.md — Oracle Drift Reconciliation`
- Metadata block (markdown bullets, top-of-file under the title):
  - `**Phase:** 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1`
  - `**Amendment author:** Phase 35 / Plan 35-01 / D-35.1`
  - `**Cross-reference:** 33-VERIFICATION.md "Documented deferrals (not gaps)" section (lines 185-194)`
  - `**Discipline:** history-preserving supplement — original 33-SPEC.md is NOT edited; the original oracle values are preserved verbatim as planning-archaeology. Tests assert the scipy-correct values listed in this amendment; implementations are mathematically correct per scipy.stats; the SPEC.md oracle values were planning-phase estimates that drifted from scipy ground truth during implementation.`

**Body — 6-row markdown table:**

The table has exactly 6 rows (one per oracle drift) and 6 columns:

| # | SPEC.md reference | Original oracle | Scipy-correct value | Test file:line | Drift root cause | D-35-NN |

Row contents (transcribe verbatim from 33-VERIFICATION.md lines 13-22 + 186-187; the planner runs the grep commands in `<read_first>` to fill in the SPEC.md row references and test file:line evidence):

1. **ΣNORMD CDF tolerance band** — SPEC.md ref for Req. 7 (or nearest Req covering ΣNORMD CDF); Original oracle: `1e-9 absolute tolerance`; Scipy-correct value: `1e-5 absolute tolerance band (A&S 6-term approximation has ~1.3e-7 abs error)`; Test file:line: `hp41-core/src/ops/stat1/normd.rs:257 cdf_oracle_q_of_196_within_a_and_s_band`; Drift root cause: `rust_decimal MathematicalOps::norm_cdf uses Abramowitz & Stegun §6 6-term polynomial approximation, which has a documented ~1.3e-7 absolute precision limit. SPEC.md's 1e-9 was a planning-phase oracle estimate inconsistent with the rust_decimal MathematicalOps backing implementation.`; D-35-NN: `D-35-01` (placeholder — Plan 35-02 owns the actual D-35-NN entries; this cross-ref column is the link).

2. **ΣAOVONE F-ratio oracle** — SPEC.md ref for Req. 11; Original oracle: `F = 100.0 for [1..5]/[6..10]/[11..15]`; Scipy-correct value: `F = 50.0 (SSB=250, SSW=30, df=2/12, F=125/2.5=50.0)`; Test file:line: `hp41-core/src/ops/stat1/anova.rs:340-351 aovone_three_groups_of_five_yields_f_50`; Drift root cause: `SPEC.md Req. 11 + ROADMAP.md success criterion claimed F = 100.0; scipy.stats.f_oneway confirms F = 50.0 is mathematically correct. ROADMAP/SPEC oracle was the bug; implementation + tests are correct.`; D-35-NN: `D-35-02`.

3. **ΣSPEAR ρ_s rank correlation** — SPEC.md ref for Req. 30 (or nearest); Original oracle: `ρ_s = 0.7`; Scipy-correct value: `ρ_s = 0.8`; Test file:line: `hp41-core/src/ops/stat1/nonparam.rs` (planner identifies exact spear test name + line at task-execution time via `grep -n "spear" hp41-core/src/ops/stat1/nonparam.rs`); Drift root cause: `SPEC.md Req. 30 oracle was a planning-phase estimate based on hand-derived rank sum 1+1+1+0+0+0+1=4 for the test dataset; scipy.stats.spearmanr returns 0.8 due to tied-rank handling convention differences. Implementation matches scipy's tied-rank convention per closed-form 1-(6Σd²)/(n(n²-1)) — derived from sum-of-squared-rank-differences without average-rank correction.`; D-35-NN: `D-35-03`.

4. **ΣEFXSQ χ² goodness-of-fit calibration** — SPEC.md ref for Req. 28 (or nearest covering ΣEFXSQ); Original oracle: `χ² SPEC value (planner fills exact number from 33-SPEC.md)`; Scipy-correct value: `scipy-correct χ² value (planner fills from the actual test in nonparam.rs)`; Test file:line: `hp41-core/src/ops/stat1/nonparam.rs` (planner identifies efxsq test name + line); Drift root cause: `SPEC.md Req. 28 oracle calibration drift from scipy.stats.chisquare; root cause symmetric to ΣEFXSQ proportion-vs-frequency convention differences. Implementation is mathematically correct per Σ(O-E)²/E formula with E derived from proportion × n_total.`; D-35-NN: `D-35-04`.

5. **ΣBSTAT coefficient of variation** — SPEC.md ref for Req. 6 (or nearest covering ΣBSTAT); Original oracle: `CV = 0.4083`; Scipy-correct value: `CV = 0.5270`; Test file:line: `hp41-core/src/ops/stat1/basic_stats.rs` (planner identifies the bstat CV test); Drift root cause: `SPEC.md Req. 6 oracle used sample-σ vs population-σ convention inconsistent with scipy.stats. Implementation uses sample-σ (n-1 denominator) which matches scipy default; CV = σ/μ = 0.5270.`; D-35-NN: `D-35-05`.

6. **ΣTSTAT deep-tail Student-t p-value** — SPEC.md ref for Req. 25 (ΣTSTAT pooled-only); Original oracle: `1e-7 absolute tolerance` (or whatever 33-SPEC.md says for the tstat tolerance); Scipy-correct value: `1e-3 absolute tolerance for deep-tail t-CDF values (AS 63 precision limit)`; Test file:line: `hp41-core/src/ops/stat1/hypothesis.rs` (planner identifies tstat test); Drift root cause: `AS 63 regularized incomplete beta function exhibits ~1e-3 absolute error in deep-tail regimes (p < 0.001 or p > 0.999). SPEC.md's 1e-7 was the planning-phase oracle band; in practice deep-tail Student-t requires the relaxed AS 63 band. Implementation correct per AS 63; test asserts the looser band.`; D-35-NN: `D-35-06`.

**Footer paragraph:**

Add a closing paragraph after the table explaining the discipline:

> **Reconciliation discipline:** The original 33-SPEC.md is preserved verbatim. Each of the 6 drifts above is also documented as a numbered `D-35-NN` entry in `docs/hp41-stat1-divergences.md` (Plan 35-02, bucket-3 Behavioral Policies), so users browsing the catalog see the reconciliation alongside the OM divergences. Tests in the listed `hp41-core/src/ops/stat1/*.rs` files assert the scipy-correct values from the third column — implementations are mathematically correct; the drifts are exclusively in the planning-phase oracle estimates recorded in 33-SPEC.md. This pattern (history-preserving SPEC amendment via sibling file) is a v3.1 convention established by D-35.1; future milestones inherit if/when their SPEC.md oracle estimates similarly drift.

**Final line:**

`*Amendment landed: 2026-05-23 (Phase 35 / Plan 35-01 / D-35.1).*`

Do NOT touch:
- `33-SPEC.md` (history-preserving — frozen as planning-archaeology per D-35.1)
- Any other Phase 33 file (`33-CONTEXT.md`, `33-RESEARCH.md`, `33-REVIEW.md`, `33-VERIFICATION.md`, etc.)
- Any `hp41-core/src/`, `hp41-cli/src/`, `hp41-gui/src-tauri/src/` source file
  </action>
  <verify>
    <automated>test -f .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md && [ "$(wc -l < .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md)" -ge 30 ] && [ "$(grep -cE '\\| D-35-0[1-6] \\|' .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md)" -eq 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md` exits 0
    - `wc -l < .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md` returns ≥ 30
    - `grep -cE "\\| D-35-0[1-6] \\|" .planning/phases/33-.../33-SPEC-AMENDMENT.md` returns exactly 6 (all six drift rows present with D-35-NN cross-refs)
    - `grep -c "ΣNORMD\\|ΣAOVONE\\|ΣSPEAR\\|ΣEFXSQ\\|ΣBSTAT\\|ΣTSTAT" .planning/phases/33-.../33-SPEC-AMENDMENT.md` returns ≥ 6 (all six drift program names present)
    - `grep -c "scipy" .planning/phases/33-.../33-SPEC-AMENDMENT.md` returns ≥ 3 (scipy citations per Pitfall 18 sub-discipline for oracle drifts)
    - `grep -c "hp41-core/src/ops/stat1/" .planning/phases/33-.../33-SPEC-AMENDMENT.md` returns ≥ 6 (every row points at a test file:line)
    - `grep -q "history-preserving" .planning/phases/33-.../33-SPEC-AMENDMENT.md` succeeds (discipline statement present)
    - `git diff --stat .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md` returns empty output (33-SPEC.md NOT modified per D-35.1)
  </acceptance_criteria>
  <done>33-SPEC-AMENDMENT.md exists as sibling to 33-SPEC.md; 6 drift rows present with D-35-NN cross-refs; 33-SPEC.md unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON file → renderer | docs-matrix is a developer tool reading committed JSON; the JSON source is trusted (no untrusted input). |
| Build tooling → docs output | The renderer writes to `docs/*.md`; output is committed and reviewed via PR before publication. |
| `33-SPEC-AMENDMENT.md` → planning archaeology | Amendment text is documentation; informs future readers about oracle drift reconciliation. Does not affect runtime code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | scripts/docs-matrix/src/main.rs (basename dispatch) | mitigate | Surgical 4-line edit reviewed via PR; `just docs-matrix-check` CI gate catches drift between JSON sources and matrix files (all three pairs). |
| T-35-02 | Information Disclosure | docs/hp41-stat1-function-matrix.md (generated) | accept | No PII, no secrets — pure function-catalog data; public docs. |
| T-35-03 | Denial of Service | `just docs-matrix-check` recipe (3 invocations + 3 diffs) | accept | Recipe runs in developer / CI environment with trusted inputs; cargo-startup cost ≤ 1 s × 3 invocations is negligible (~3 s total). |
| T-35-04 | Repudiation | 33-SPEC-AMENDMENT.md historic-preservation discipline | mitigate | The amendment is a sibling file; `git log` confirms 33-SPEC.md is untouched (the diff is verifiable). The discipline is documented in the file's metadata block and footer. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies — `serde` and `serde_json` already pinned in `scripts/docs-matrix/Cargo.lock` from v3.0. No package legitimacy gate required. |
</threat_model>

<verification>
After all four tasks complete:

1. Renderer dispatch: `scripts/docs-matrix/src/main.rs` has the third basename branch; cargo build clean; 1-in/1-out signature preserved.
2. Recipe parity: `just docs-matrix` and `just docs-matrix-check` both succeed locally with exit code 0 (all three pairs).
3. Drift catch: `just docs-matrix-check` exits non-zero after artificially mutating `docs/hp41-stat1-function-matrix.md`; reverting restores exit 0.
4. hp41cv invariant: `git diff --stat docs/hp41cv-function-matrix.md` empty after `just docs-matrix`.
5. hp41-math1 invariant: `git diff --stat docs/hp41-math1-function-matrix.md` empty after `just docs-matrix` (Phase 35 does not re-render math1).
6. SPEC-AMENDMENT sibling discipline: `git diff --stat .planning/phases/33-.../33-SPEC.md` empty (33-SPEC.md NOT modified); `.planning/phases/33-.../33-SPEC-AMENDMENT.md` is a NEW file.
7. SC-4 invariant trivially preserved: this plan touches `scripts/docs-matrix/src/main.rs`, `justfile`, `docs/hp41-stat1-function-matrix.md`, `.planning/phases/33-.../33-SPEC-AMENDMENT.md` — none of which are inside `hp41-gui/src-tauri/src/`.
8. MSRV 1.88 unchanged; zero new dependencies introduced.
</verification>

<success_criteria>
- `docs/hp41-stat1-function-matrix.md` exists with 26 entries and an XROM column showing `Stat 1 / 2-N`
- `docs/hp41cv-function-matrix.md` and `docs/hp41-math1-function-matrix.md` are byte-identical to HEAD
- `just docs-matrix` and `just docs-matrix-check` both pass locally and in CI
- `scripts/docs-matrix/src/main.rs` keeps the 1-in/1-out CLI signature; the only edit is the 4-line basename dispatch branch
- `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md` exists with all 6 oracle-drift rows + D-35-NN cross-references; `33-SPEC.md` UNCHANGED
</success_criteria>

<output>
Create `.planning/phases/35-documentation-adrs/35-01-SUMMARY.md` when done, summarizing:
- What was added to the renderer (the `else if` branch — actual line numbers in the final file)
- What lines were added to justfile (`git diff --stat justfile`)
- The exit code of `just docs-matrix-check` after this plan lands
- The line count of the newly generated `docs/hp41-stat1-function-matrix.md` + how many "Stat 1 / 2-" cells it contains
- Confirmation that `git diff --stat docs/hp41cv-function-matrix.md docs/hp41-math1-function-matrix.md` was empty after the regeneration
- The 6 D-35-NN cross-references chosen in `33-SPEC-AMENDMENT.md` (planner records which test file:line was used for each drift)
- Confirmation that `33-SPEC.md` was NOT modified (`git diff --stat .planning/phases/33-.../33-SPEC.md` empty)
</output>
