---
phase: 52-test-hardening-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hp41-xmem-functions.json
  - docs/hp41-xmem-function-matrix.md
  - hp41-cli/src/help_data.rs
  - hp41-gui/src/help_data.ts
  - scripts/docs-matrix/src/main.rs
  - justfile
  - hp41-cli/tests/phase52_help_data_xmem.rs
autonomous: true
requirements: [XMEM-10]

must_haves:
  truths:
    - "All 8 X-MEM functions appear in the `?` help overlay under a dedicated 'Extended Memory' section (CLI + GUI)"
    - "The Phase-49 searchable reference auto-covers X-MEM once the new pool loads"
    - "`just docs-matrix-check` passes with the new X-MEM matrix doc (no drift)"
  artifacts:
    - path: "docs/hp41-xmem-functions.json"
      provides: "8-entry X-MEM help/JSON pool (D-52.1, D-52.3)"
      contains: "EMDIR"
    - path: "docs/hp41-xmem-function-matrix.md"
      provides: "Generated standalone X-MEM matrix doc (D-52.2)"
      contains: "Extended Memory"
    - path: "hp41-cli/src/help_data.rs"
      provides: "Sixth OnceLock pool help_entries_xmem() + help_entries_all() extension (D-52.1)"
      contains: "help_entries_xmem"
    - path: "hp41-gui/src/help_data.ts"
      provides: "GUI sixth pool helpEntriesXmem() + helpEntriesAll() extension (D-52.1)"
      contains: "helpEntriesXmem"
    - path: "hp41-cli/tests/phase52_help_data_xmem.rs"
      provides: "8-entry pool smoke test + six-pool merged chain + overlay section assertion (XMEM-10)"
      contains: "help_entries_xmem"
  key_links:
    - from: "hp41-cli/src/help_data.rs"
      to: "docs/hp41-xmem-functions.json"
      via: "include_str! + OnceLock get_or_init"
      pattern: "include_str!.*hp41-xmem-functions.json"
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/hp41-xmem-functions.json"
      via: "Vite static JSON import"
      pattern: "import xmemFunctions"
    - from: "scripts/docs-matrix/src/main.rs"
      to: "docs/hp41-xmem-functions.json"
      via: "filename title-dispatch arm"
      pattern: "hp41-xmem-functions.json"
---

<objective>
Create the dedicated X-MEM help/JSON pool and wire it everywhere it must surface: the standalone matrix doc + `just docs-matrix`/`docs-matrix-check`, the CLI Rust OnceLock pool, and the GUI TypeScript pool. This makes the 8 X-MEM functions discoverable in the `?` overlay (its own "Extended Memory" section) and in the Phase-49 searchable reference (D-52.1, D-52.2, D-52.3, D-52.5).

Purpose: Satisfies success criterion 3 ("all X-MEM functions appear in the `?` help overlay") for the help-data half. This is the foundation plan — the JSON pool MUST exist before the meta-gate parity tests (Plan 03) can compile.

Output: `docs/hp41-xmem-functions.json` (8 entries), `docs/hp41-xmem-function-matrix.md` (generated), help-pool wiring in CLI + GUI, docs-matrix generator + justfile wiring, pool smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-test-hardening-documentation/52-CONTEXT.md
@.planning/phases/52-test-hardening-documentation/52-RESEARCH.md
@.planning/phases/52-test-hardening-documentation/52-PATTERNS.md

<interfaces>
<!-- Confirmed by RESEARCH.md + codebase grep. Executor uses these directly. -->

HelpEntry struct (hp41-cli/src/help_data.rs) — fields:
  op_variant: String, display_name: String, category: String, status: String,
  phase: Option<String>, key_path: Option<String>, description: String,
  divergences: Vec<String> (#[serde(default)]), xrom: Option<XromEntry>
  NOTE: HelpEntry does NOT declare `example`/`notes` — serde ignores unknown fields (no deny_unknown_fields).
  GUI HelpEntry interface in help_data.ts DOES declare example/notes (lines 82-84 of analog).

Existing pool chain (help_data.rs, confirmed lines 213-220):
  help_entries_all() chains: help_entries() . math1 . stat1 . time . adv  (5 pools)

Existing GUI chain (help_data.ts, confirmed lines 236-238):
  helpEntriesAll() spreads: helpEntries, math1, stat1, time, advantage  (5 pools)

docs-matrix title dispatch (scripts/docs-matrix/src/main.rs ~line 78): filename .ends_with() arms,
  advantage arm immediately before the `else` fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/hp41-xmem-functions.json (8-entry pool) + the docs-matrix generator + justfile wiring</name>
  <files>docs/hp41-xmem-functions.json, scripts/docs-matrix/src/main.rs, justfile</files>
  <read_first>
    - docs/hp41-advantage-functions.json (exact entry-schema analog — copy field shape, including example/notes)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "docs/hp41-xmem-functions.json" — the 8 op_variant/display_name pairs + the worked-example entry skeleton; section "scripts/docs-matrix/src/main.rs"; section "justfile")
    - scripts/docs-matrix/src/main.rs (the advantage filename-dispatch arm at ~line 78, before the `else` fallback)
    - justfile (the `docs-matrix` and `docs-matrix-check` recipes, ~lines 185-220, advantage lines 199-200 and 218-220)
  </read_first>
  <action>
    Create docs/hp41-xmem-functions.json with EXACTLY 8 entries (op_variant / display_name): EmDir/EMDIR, EmRoom/EMROOM, SaveP/SAVEP, GetP/GETP, SaveD/SAVED, GetD/GETD, EmReg/EMREG, SaveRx/SAVERX. Each entry: category "Extended Memory" (drives the dedicated overlay section per D-52.1), status "implemented", phase "52", key_path "XEQ \"EMDIR\"" form (XEQ-by-name only per D-52.4), a <=80-char description, plus full `example` + `notes` fields (D-52.3 — worked examples matching the Phase-49 quality bar: SAVEP/GETP round-trip, SAVED/GETD register transfer, EMREG indexing, EMROOM available-register count, SAVERX store-into-active-file). CRITICAL: NO `xrom` field on any entry — X-MEM ops are HP-41CX OS built-ins, not XROM; an `xrom` key would break test_pool_partition_is_exhaustive (Pitfall 2). JSON must be valid (include_str! + serde panics at first access on malformed JSON).
    Then add ONE title-dispatch arm to scripts/docs-matrix/src/main.rs after the advantage arm: `else if basename.ends_with("hp41-xmem-functions.json")` → title "# HP-41CX Extended Memory Function Matrix", source "`docs/hp41-xmem-functions.json`". No other main.rs change (has_xrom=false renders the non-XROM table format correctly).
    Then add two lines to the `docs-matrix` justfile recipe (generate docs/hp41-xmem-function-matrix.md) and two+diff lines to the `docs-matrix-check` recipe (generate to /tmp and `diff -u`), mirroring the advantage recipe lines verbatim.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('docs/hp41-xmem-functions.json')); assert len(d)==8, len(d); assert all('xrom' not in e for e in d); assert all(e['category']=='Extended Memory' for e in d); names={e['display_name'] for e in d}; assert names=={'EMDIR','EMROOM','SAVEP','GETP','SAVED','GETD','EMREG','SAVERX'}, names; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hp41-xmem-functions.json` parses as valid JSON with exactly 8 entries
    - Every entry has category "Extended Memory", a `key_path` of `XEQ "<MNEMONIC>"` form, and non-empty `example` + `notes`
    - No entry contains an `xrom` field
    - `scripts/docs-matrix/src/main.rs` has exactly one new `hp41-xmem-functions.json` title-dispatch arm
    - `justfile` `docs-matrix` and `docs-matrix-check` recipes each reference `docs/hp41-xmem-functions.json`
  </acceptance_criteria>
  <done>JSON pool exists and is valid; docs-matrix generator + justfile recipes wired for the X-MEM matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the sixth help pool in CLI (help_data.rs) and GUI (help_data.ts), generate the matrix doc</name>
  <files>hp41-cli/src/help_data.rs, hp41-gui/src/help_data.ts, docs/hp41-xmem-function-matrix.md</files>
  <read_first>
    - hp41-cli/src/help_data.rs (the ADV pool block, lines 174-220: const + OnceLock + accessor + help_entries_all chain — the exact analog to copy and rename to XMEM)
    - hp41-gui/src/help_data.ts (the Advantage pool block, lines 192-237: import + helpEntriesAdvantage() accessor + helpEntriesAll() spread; HelpEntry interface lines 82-84; helpOverlayRows() lines 111-138)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (sections "hp41-cli/src/help_data.rs" and "hp41-gui/src/help_data.ts" — exact rename targets)
    - docs/hp41-advantage-function-matrix.md (the generated-doc analog — confirms the non-XROM table format)
  </read_first>
  <action>
    In hp41-cli/src/help_data.rs add the sixth pool mirroring the ADV block: `const XMEM_FUNCTIONS_JSON = include_str!("../../docs/hp41-xmem-functions.json")`, `static XMEM_HELP_ENTRIES: OnceLock<Vec<HelpEntry>>`, `pub fn help_entries_xmem() -> &'static [HelpEntry]` with the malformed-JSON panic message naming hp41-xmem-functions.json (D-25.17 hard-build-blocker). Extend `help_entries_all()` to chain `.chain(help_entries_xmem().iter())` sixth (fixed order: built-ins → Math1 → Stat1 → Time → Adv → X-MEM). Update the `help_entries_all` doc-comment to say "six JSON pools".
    In hp41-gui/src/help_data.ts add `import xmemFunctions from '../../docs/hp41-xmem-functions.json'`, `export function helpEntriesXmem(): readonly HelpEntry[]` returning the import cast, and extend `helpEntriesAll()` to spread `...helpEntriesXmem()` sixth. No change to HelpOverlay.tsx — `helpOverlayRows()` filters `key_path !== null` and X-MEM entries have non-null key_path → they appear as a new "Extended Memory" section automatically.
    Then run `just docs-matrix` to generate docs/hp41-xmem-function-matrix.md and commit it alongside the JSON (CI `just docs-matrix-check` diffs them). Do NOT hand-author the matrix doc.
  </action>
  <verify>
    <automated>grep -c "help_entries_xmem" hp41-cli/src/help_data.rs | grep -qv '^0$' && grep -q "helpEntriesXmem" hp41-gui/src/help_data.ts && test -f docs/hp41-xmem-function-matrix.md && just docs-matrix-check</automated>
  </verify>
  <acceptance_criteria>
    - `help_entries_xmem()` exists in `hp41-cli/src/help_data.rs` and is chained sixth in `help_entries_all()`
    - `helpEntriesXmem()` exists in `hp41-gui/src/help_data.ts` and is spread sixth in `helpEntriesAll()`
    - `docs/hp41-xmem-function-matrix.md` exists and was generated by `just docs-matrix` (not hand-authored)
    - `just docs-matrix-check` passes (no drift between JSON and matrix doc)
  </acceptance_criteria>
  <done>Both help pools (CLI + GUI) load the X-MEM JSON; the generated matrix doc is committed and drift-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pool smoke test phase52_help_data_xmem.rs (count + six-pool chain + overlay section)</name>
  <files>hp41-cli/tests/phase52_help_data_xmem.rs</files>
  <read_first>
    - hp41-cli/tests/phase44_help_data_adv.rs (the exact full-file analog — count test, six-pool-chain test, overlay-section test; swap accessor names + counts)
    - hp41-cli/src/help_data.rs (confirm help_entries_xmem / help_entries_all / help_overlay_rows public signatures)
    - .planning/phases/52-test-hardening-documentation/52-PATTERNS.md (section "hp41-cli/tests/phase52_help_data_xmem.rs" — the four test skeletons with exact assertions)
  </read_first>
  <behavior>
    - help_entries_xmem() returns a non-empty slice
    - help_entries_xmem().len() == 8 (feature-frozen at 8 X-MEM ops)
    - help_entries_all() length >= 358 (130 built-ins + 45 Math1 + 26 Stat1 + 35 Time + 114 Adv + 8 X-MEM)
    - help_overlay_rows() includes an "Extended Memory" category header row
    - every X-MEM entry has category "Extended Memory" and a non-null key_path
  </behavior>
  <action>
    Create hp41-cli/tests/phase52_help_data_xmem.rs with `#![allow(clippy::unwrap_used)]`, importing `hp41_cli::help_data::{help_entries_xmem, help_entries_all, help_overlay_rows}`. Mirror phase44_help_data_adv.rs structure exactly: (1) `xmem_help_entries_is_not_empty`, (2) `xmem_help_entries_count_meets_8_target` asserting `== 8`, (3) `help_entries_all_returns_six_pools` asserting `>= 358`, (4) `help_overlay_rows_includes_xmem_section` asserting a header row whose desc contains "Extended Memory". Add a fifth test asserting every X-MEM entry has category "Extended Memory" and `key_path.is_some()`. Use the exact assertion messages from PATTERNS.md so the failure output points at docs/hp41-xmem-functions.json.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli --test phase52_help_data_xmem</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p hp41-cli --test phase52_help_data_xmem` exits 0
    - The count test asserts exactly 8 entries; the chain test asserts `>= 358`; the overlay test finds the "Extended Memory" header
    - All five tests are present and pass
  </acceptance_criteria>
  <done>`cargo test -p hp41-cli --test phase52_help_data_xmem` is green; the X-MEM pool, six-pool chain, and overlay section are proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds JSON data files, Rust/TypeScript help-pool wiring, a generator arm, and a test file. No trust boundary is crossed: no network, no auth, no user-supplied runtime input, no filesystem writes outside the dev/CI tree. All JSON is compile-time-embedded (`include_str!` / Vite import).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `docs/hp41-xmem-functions.json` (malformed JSON panics at first access — JSON canonical data flow) | mitigate | `cargo test -p hp41-cli --test phase52_help_data_xmem` exercises `help_entries_xmem()` which forces the `OnceLock` parse; `just docs-matrix-check` drift-catch; op↔JSON parity test in Plan 03 |
| T-52-SC | Tampering | npm/cargo installs | accept | No package installs in this plan (zero new deps, VERIFIED in RESEARCH.md). No `[ASSUMED]`/`[SUS]` packages — Package Legitimacy Gate N/A |

Per RESEARCH.md "Security Domain": this phase makes NO changes to authentication, session management, access control, cryptography, or network-facing code. No ASVS categories apply (Security domain: N/A). The single real integrity concern is malformed canonical JSON panicking at first access — mitigated above.
</threat_model>

<verification>
- `python3 -c "import json; assert len(json.load(open('docs/hp41-xmem-functions.json')))==8"` — exactly 8 entries
- `just docs-matrix-check` — matrix doc has no drift
- `cargo test -p hp41-cli --test phase52_help_data_xmem` — pool wiring proven
- `just lint` — no clippy regressions in help_data.rs
</verification>

<success_criteria>
- 8-entry X-MEM JSON pool exists, valid, no `xrom` field, full worked examples (D-52.1, D-52.3)
- CLI + GUI help pools both load X-MEM; `?` overlay shows a dedicated "Extended Memory" section (D-52.1, D-52.5; success criterion 3 help-half)
- Standalone matrix doc generated + wired into docs-matrix / docs-matrix-check (D-52.2)
- Pool smoke test green
</success_criteria>

<output>
Create `.planning/phases/52-test-hardening-documentation/52-01-SUMMARY.md` when done
</output>
