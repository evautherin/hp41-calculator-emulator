---
phase: 41-hp41-gui-gui-integration-live-display
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/op_catalog_xrom.rs
  - hp41-gui/src-tauri/src/commands.rs
  - hp41-gui/src-tauri/src/types.rs
  - hp41-gui/src-tauri/src/lib.rs
  - hp41-gui/src-tauri/src/prgm_display.rs
  - hp41-gui/src-tauri/permissions/tick-time.toml
  - hp41-gui/src-tauri/capabilities/default.json
autonomous: true
requirements:
  - TIME-GUI-01
  - TIME-GUI-02
  - TIME-GUI-04
  - TIME-GUI-05
  - TIME-GUI-06
  - TIME-GUI-07

must_haves:
  truths:
    - "cargo check -p hp41-gui exits 0 with all 35 Time Op arms in prgm_display.rs"
    - "CATALOG 2 enumerates all 3 XROM modules (Math 1, Stat 1, Time) via a generic loop"
    - "CalcStateView projects clock_active and stopwatch_keyboard_mode from CalcState"
    - "tick_time Tauri command calls check_alarms, drains both buffers, returns CalcStateView"
    - "tick_time is registered in generate_handler and has Tauri permission TOML + capability"
  artifacts:
    - path: "hp41-gui/src-tauri/permissions/tick-time.toml"
      provides: "Tauri v2.11 permission for tick_time command"
      contains: "allow-tick-time"
    - path: "hp41-gui/src-tauri/src/commands.rs"
      provides: "tick_time + handle_tick_time functions"
      exports: ["tick_time", "handle_tick_time"]
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "clock_active + stopwatch_keyboard_mode fields in CalcStateView"
      contains: "clock_active"
  key_links:
    - from: "hp41-gui/src-tauri/src/commands.rs"
      to: "hp41_core::ops::time::alarm::check_alarms"
      via: "function call in handle_tick_time"
      pattern: "check_alarms"
    - from: "hp41-gui/src-tauri/src/lib.rs"
      to: "hp41-gui/src-tauri/src/commands.rs"
      via: "generate_handler! macro"
      pattern: "commands::tick_time"
    - from: "hp41-gui/src-tauri/capabilities/default.json"
      to: "hp41-gui/src-tauri/permissions/tick-time.toml"
      via: "permission identifier reference"
      pattern: "allow-tick-time"
---

<objective>
Wire the Rust backend for Time Pac GUI integration: verify prgm_display.rs completeness (TIME-GUI-01), refactor op_catalog to a generic 3-module loop (D-41.5 / TIME-GUI-02), add clock_active + stopwatch_keyboard_mode to CalcStateView (D-41.3), create the tick_time Tauri command (D-41.1 / D-41.4), and register it with Tauri permissions (TIME-GUI-04/05/06).

Purpose: Establishes the entire Rust-side infrastructure that the React frontend (Plan 41-02 and 41-03) consumes via IPC. The tick_time command is the novel element enabling live clock/stopwatch display in the GUI.
Output: Compilable hp41-gui crate with tick_time command, CalcStateView projections, and generic op_catalog loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-hp41-gui-gui-integration-live-display/41-CONTEXT.md
@.planning/phases/41-hp41-gui-gui-integration-live-display/41-RESEARCH.md
@.planning/phases/41-hp41-gui-gui-integration-live-display/41-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hp41-gui/src-tauri/src/commands.rs (handle_get_state pattern, lines 228-232):
  pub fn handle_get_state(calc: &mut CalcState) -> Result<CalcStateView, GuiError>
  // drains print_buffer + event_buffer, then CalcStateView::from_state

From hp41-gui/src-tauri/src/types.rs (CalcStateView struct, line 61):
  pub struct CalcStateView — fields ending at line 105, from_state at line 114
  // Takes &CalcState + print_lines: Vec<String> + event_lines: Vec<String>

From hp41-core/src/ops/time/alarm.rs (line 479):
  pub fn check_alarms(state: &mut CalcState)

From hp41-core/src/ops/math1/xrom.rs:
  pub static MATH_1: XromModule (id=7)
  pub static STAT_1: XromModule (id=2)
  pub static TIME_MODULE: XromModule (id=26)
  // XromModule has: .id, .name, .ops (slice of (name, Op) tuples)

From hp41-core/src/state.rs:
  pub clock_active: bool            // #[serde(default, skip)] — transient
  pub stopwatch_keyboard_mode: bool  // #[serde(default, skip)] — transient

From hp41-gui/src-tauri/src/lib.rs (generate_handler!, lines 88-98):
  .invoke_handler(tauri::generate_handler![commands::dispatch_op, commands::get_state, ...])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify prgm_display.rs + op_catalog generic loop refactor + catalog test</name>
  <files>hp41-gui/src-tauri/src/prgm_display.rs, hp41-core/src/ops/program.rs, hp41-core/tests/op_catalog_xrom.rs</files>
  <read_first>
    - hp41-gui/src-tauri/src/prgm_display.rs (verify 35 Time arms at lines 346-382; update stale doc comment at line 46)
    - hp41-core/src/ops/program.rs (lines 338-378 — current op_catalog CATALOG 2 block with latent else-if bug at line 364; import at line 18)
    - hp41-core/tests/op_catalog_xrom.rs (existing 4 tests — catalog_2_with_math1_loaded, catalog_2_without_xrom, catalog_3_and_4_still_not_available, catalog_2_lists_stat1_when_bit1_set)
    - hp41-core/src/ops/math1/xrom.rs (MATH_1, STAT_1, TIME_MODULE consts — verify pub, field names .id, .name, .ops)
  </read_first>
  <action>
    First: run `cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml` to confirm TIME-GUI-01 is already satisfied (all 35 Time arms present, no non-exhaustive patterns warning). If it passes, only update the stale doc comment in prgm_display.rs line 46 from "v2.2 built-ins + Math Pac I + Stat 1 Pac" to include "+ Time Module". Do NOT add any arms — they already exist at lines 346-382.

    Second: refactor op_catalog CATALOG 2 block in hp41-core/src/ops/program.rs per D-41.5. Add `TIME_MODULE` to the import at line 18 (alongside existing `MATH_1, STAT_1`). Replace lines 345-368 (the two parallel if-blocks + latent else-if bug) with a generic loop: define `xrom_registry: &[(&XromModule, u8)]` array containing `(&MATH_1, 0b0000_0001)`, `(&STAT_1, 0b0000_0010)`, `(&TIME_MODULE, 0b0000_0100)`. Use `xrom_registry.iter().any(|(_, bit)| state.xrom_modules & bit != 0)` to gate the "NO XROM" fallback. Then iterate with `for (module, bit) in xrom_registry` to enumerate each loaded module's header + ops. The `XromModule` type path is `crate::ops::math1::xrom::XromModule`. Preserve the existing `{:<24}` format string padding. Preserve the `"XROM {} {}"` header format using `module.id` and `module.name`. Preserve the `"NO XROM"` fallback. Per D-carried.6 use `state.print_buffer.push(format!(...))` — no println/eprintln.

    Third: add `catalog_2_lists_time_when_bit2_set` test to hp41-core/tests/op_catalog_xrom.rs. Mirror the existing `catalog_2_lists_stat1_when_bit1_set` pattern. Set `xrom_modules = 0b0000_0111` (all 3 modules). Assert that the print_buffer contains "XROM 7 MATH 1A", "XROM 2 STAT 1B", "XROM 26 TIME 2C" headers plus spot-check "POLY" (Math 1), a known Stat 1 op like "\u{03A3}BSTAT", and a Time op like "TIME" or "ADATE". Assert "-- END --" is the last line. Also add a test `catalog_2_time_only` with `xrom_modules = 0b0000_0100` (only Time) to verify the Time module enumerates alone without "NO XROM".
  </action>
  <verify>
    <automated>cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | grep -v "^$" | tail -5 && cargo test -p hp41-core --test op_catalog_xrom -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-gui` exits 0 with zero `non-exhaustive patterns` warnings (TIME-GUI-01 verified)
    - prgm_display.rs doc comment at line 46 includes "Time Module"
    - program.rs line 18 imports `MATH_1, STAT_1, TIME_MODULE` from `crate::ops::math1::xrom`
    - program.rs CATALOG 2 block contains `xrom_registry` array with 3 `(XromModule, u8)` tuples
    - program.rs CATALOG 2 block has NO parallel if-blocks — single `for (module, bit) in xrom_registry` loop
    - program.rs CATALOG 2 block has `any_module` boolean gating "NO XROM" fallback
    - `catalog_2_lists_time_when_bit2_set` test passes: headers "XROM 7 MATH 1A", "XROM 2 STAT 1B", "XROM 26 TIME 2C" all present
    - `catalog_2_time_only` test passes: header "XROM 26 TIME 2C" present, "NO XROM" absent
    - All 6 op_catalog_xrom tests pass (4 existing + 2 new)
  </acceptance_criteria>
  <done>TIME-GUI-01 confirmed. op_catalog refactored to generic 3-module loop per D-41.5. Two new catalog tests pass. Latent else-if bug at line 364 eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: CalcStateView projections + tick_time command + Tauri permission + handler registration</name>
  <files>hp41-gui/src-tauri/src/types.rs, hp41-gui/src-tauri/src/commands.rs, hp41-gui/src-tauri/src/lib.rs, hp41-gui/src-tauri/permissions/tick-time.toml, hp41-gui/src-tauri/capabilities/default.json</files>
  <read_first>
    - hp41-gui/src-tauri/src/types.rs (CalcStateView struct at line 61, from_state at line 114 — understand field order, constructor shape, JSON size test assertions near lines 252-298)
    - hp41-gui/src-tauri/src/commands.rs (handle_get_state at line 228 — the exact pattern to mirror; import section at lines 1-25)
    - hp41-gui/src-tauri/src/lib.rs (generate_handler! at lines 88-98 — add tick_time entry)
    - hp41-gui/src-tauri/permissions/get-state.toml (6-line template for permission TOML)
    - hp41-gui/src-tauri/capabilities/default.json (permissions array — add "allow-tick-time")
    - hp41-core/src/state.rs (confirm clock_active and stopwatch_keyboard_mode field names and types — both bool, both #[serde(default, skip)])
    - hp41-core/src/ops/time/alarm.rs (check_alarms signature at line 479: `pub fn check_alarms(state: &mut CalcState)`)
  </read_first>
  <action>
    A) CalcStateView fields in types.rs: add `pub clock_active: bool` and `pub stopwatch_keyboard_mode: bool` after the `modal_prompt` field (line 104), with a comment referencing Phase 41 D-41.3. In `from_state()` body, add `let clock_active = state.clock_active;` and `let stopwatch_keyboard_mode = state.stopwatch_keyboard_mode;` in the projection section, then add both to the CalcStateView constructor literal. Update the JSON size budget test assertions — two boolean fields add approximately 52 bytes (`"clock_active":false,"stopwatch_keyboard_mode":false`). Read the existing budget assertions (the `<= 500` and `<= 600` bounds), then measure the actual new size and update both bounds with ~20 bytes headroom. Document the new measured value in the test comment.

    B) tick_time command in commands.rs: add `tick_time` and `handle_tick_time` functions after `handle_get_state` (line 232). The `tick_time` thunk mirrors `get_state` exactly: `#[tauri::command] pub fn tick_time(state: State<'_, AppState>) -> Result<CalcStateView, GuiError>` body acquires mutex with `.lock().unwrap_or_else(|e| e.into_inner())` then calls `handle_tick_time`. The `handle_tick_time` helper (pub, for testability) takes `&mut CalcState`, calls `hp41_core::ops::time::alarm::check_alarms(calc)` as the first line (D-41.4), then drains `print_buffer` and `event_buffer` (identical to `handle_get_state` lines 229-230), then returns `Ok(CalcStateView::from_state(calc, print_lines, event_lines))`. Add a doc comment referencing D-41.1, D-41.4, the 100ms setInterval context, and noting this is a read-only query that should NOT use busyRef.

    Also add a `#[cfg(test)]` unit test `handle_tick_time_drains_event_buffer_and_returns_view`: create `CalcState::new()`, push `"alarm:message:TEST ALARM"` to `calc.event_buffer`, call `handle_tick_time(&mut calc)`, assert `calc.event_buffer.is_empty()`, assert `view.event_buffer == vec!["alarm:message:TEST ALARM"]`. Add another test `handle_tick_time_returns_clock_active_field`: set `calc.clock_active = true`, call `handle_tick_time`, assert `view.clock_active == true`.

    C) Register tick_time in lib.rs: add `commands::tick_time,` to the `generate_handler!` macro invocation (after `commands::submit_modal_with_label,` at line 97), with a Phase 41 D-41.1 comment.

    D) Create tick-time.toml permission file: copy the exact 6-line structure from get-state.toml, change identifier to `"allow-tick-time"`, description to `"Allows the tick_time command."`, commands.allow to `["tick_time"]`.

    E) Update default.json: add `"allow-tick-time"` to the permissions array after `"allow-submit-modal-with-label"`.
  </action>
  <verify>
    <automated>cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | tail -5 && cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml -- handle_tick_time --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - types.rs CalcStateView struct contains `pub clock_active: bool` and `pub stopwatch_keyboard_mode: bool` fields
    - types.rs from_state() projects both fields from `state.clock_active` and `state.stopwatch_keyboard_mode`
    - types.rs JSON size budget tests pass with updated bounds
    - commands.rs contains `pub fn tick_time` with `#[tauri::command]` attribute and `pub fn handle_tick_time`
    - handle_tick_time calls `hp41_core::ops::time::alarm::check_alarms(calc)` before draining buffers
    - handle_tick_time drains both print_buffer and event_buffer
    - `handle_tick_time_drains_event_buffer_and_returns_view` test passes
    - `handle_tick_time_returns_clock_active_field` test passes
    - lib.rs generate_handler! includes `commands::tick_time`
    - tick-time.toml exists with identifier `"allow-tick-time"`
    - default.json permissions array includes `"allow-tick-time"`
    - `cargo check -p hp41-gui` exits 0
  </acceptance_criteria>
  <done>tick_time Tauri command created per D-41.1/D-41.4, CalcStateView projects clock_active + stopwatch_keyboard_mode per D-41.3, command registered in generate_handler! and Tauri permissions, unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React frontend -> Tauri IPC | Frontend invokes tick_time at 100ms intervals; input is the command name only (no user-controlled parameters) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Denial of Service | tick_time concurrent Mutex contention | mitigate | busyRef guard in frontend (Plan 41-03); tick_time is read-only so worst case is brief blocking, no data corruption |
| T-41-02 | Information Disclosure | CalcStateView clock_active/stopwatch_keyboard_mode | accept | boolean flags expose no sensitive data; same trust level as existing CalcStateView fields |
| T-41-03 | Spoofing | Alarm message text in event_buffer | accept | Alarm messages are user-set via XYZALM — no external input source; toast renders plain text |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 41 (D-carried.1) |
</threat_model>

<verification>
cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml exits 0
cargo test -p hp41-core --test op_catalog_xrom exits 0 (6 tests)
cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml -- handle_tick_time exits 0 (2 tests)
grep -c "tick_time" hp41-gui/src-tauri/src/lib.rs returns >= 1
cat hp41-gui/src-tauri/permissions/tick-time.toml contains "allow-tick-time"
grep "allow-tick-time" hp41-gui/src-tauri/capabilities/default.json returns match
</verification>

<success_criteria>
1. TIME-GUI-01: prgm_display.rs compiles with all 35 Time arms (verified by cargo check)
2. TIME-GUI-02: op_catalog enumerates TIME_MODULE via generic 3-module loop, verified by 2 new tests
3. D-41.1/D-41.4: tick_time command exists, calls check_alarms, drains buffers, returns CalcStateView
4. D-41.3: CalcStateView projects clock_active + stopwatch_keyboard_mode booleans
5. Tauri permission + capability registered for tick_time
6. All existing tests continue to pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/41-hp41-gui-gui-integration-live-display/41-01-SUMMARY.md` when done
</output>
