---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 06
plan_id: 33-06
type: execute
wave: 3
depends_on: ["33-01", "33-04", "33-05"]
files_modified:
  - hp41-core/src/ops/stat1/moments.rs
  - hp41-core/src/ops/stat1/anova.rs
  - hp41-core/src/ops/stat1/nonparam.rs
  - hp41-core/src/ops/stats.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
autonomous: true
requirements:
  - STAT-UNI-02
  - STAT-UNI-04
  - STAT-AOV-01
  - STAT-AOV-02
  - STAT-AOV-03
  - STAT-AOV-04
  - STAT-HYP-05
  - STAT-HYP-06

must_haves:
  truths:
    - "XEQ \"ΣMMTUG\" accumulates Σx³ and Σx⁴ into OM-specified extended registers (named consts only — no literal indices)"
    - "XEQ \"ΣMMTGD\" performs grouped (frequency-weighted) variant of ΣMMTUG per OM"
    - "XEQ \"ΣAOVONE\" returns F = 100.0 on 3 groups of 5 samples [1..5],[6..10],[11..15] within 1e-9"
    - "XEQ \"ΣAOVTWO\" computes F_row and F_col on a 3×4 dataset within 1e-7"
    - "XEQ \"ΣANOCOV\" returns adjusted F-ratio per NPS ZA-3 reference within 1e-7"
    - "XEQ \"ΣCTKKK\" on 2×3 [[10,20,30],[40,50,60]] returns χ² ≈ 4.286 within 1e-9"
    - "XEQ \"ΣCTKK\" on 2×2 [[10,20],[30,40]] returns χ² ≈ 0.397 within 1e-9"
    - "op_sigma_minus is extended to mirror every new register written by ΣMMTUG accumulation ([C] correction-key round-trip)"
  artifacts:
    - path: "hp41-core/src/ops/stat1/moments.rs"
      provides: "ΣMMTUG (ungrouped 3rd/4th moments) + ΣMMTGD (grouped) using STAT1_MMTUG_*_REG named consts"
      contains: "pub fn op_sigma_mmtug"
    - path: "hp41-core/src/ops/stat1/anova.rs"
      provides: "ΣAOVONE / ΣAOVTWO / ΣANOCOV with named-const register layout (P21 mitigation)"
      contains: "pub fn op_sigma_aovone"
    - path: "hp41-core/src/ops/stats.rs"
      provides: "op_sigma_minus extended to reverse every new ΣMMTUG accumulation"
      contains: "STAT1_MMTUG"
  key_links:
    - from: "hp41-core/src/ops/stat1/anova.rs"
      to: "hp41-core/src/ops/stat1/mod.rs::STAT1_AOV_*_REG"
      via: "named-const access only — NO literal-integer register indices ≥7"
      pattern: "STAT1_AOV_"
    - from: "hp41-core/src/ops/stats.rs::op_sigma_minus"
      to: "STAT1_MMTUG_CUBE_REG / STAT1_MMTUG_QUAD_REG"
      via: "symmetric reversal for [C] correction-key per STAT-UNI-04"
      pattern: "STAT1_MMTUG"
---

<objective>
Wave 3 — OM-register-layout-dependent cluster per D-33.2. Implements the 7 Ops that depend on extended register access beyond R01–R06: ΣMMTUG/ΣMMTGD (3rd/4th moments using Σx³/Σx⁴), ΣAOVONE/ΣAOVTWO/ΣANOCOV (group-keyed Σ-blocks), ΣCTKKK/ΣCTKK (contingency tables). Every register access uses named consts from `stat1/mod.rs` per P21 mitigation. Extends `op_sigma_minus` for STAT-UNI-04 [C] correction-key round-trip.

Purpose: P21 is "the single most dangerous silent-wrong-answer trap" per STATE.md — these 7 Ops are exactly where register-layout mistakes silently produce wrong F-ratios / moments / χ² values. Concentrating them in one plan lets the executor focus on OM transcription discipline + Welford-safe variance + grouped-accumulator atomicity.

Output: hp41-core compiles green with 7 new Sigma* Op variants; `op_sigma_minus` extended for ΣMMTUG round-trip; moments.rs / anova.rs each ≤300 LOC; nonparam.rs (extended for ΣCTKKK/CTKK) stays ≤300 LOC; oracle tests for all 7 Ops at SPEC.md tolerance bands; 7 more stub swaps in STAT_1.ops.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-04-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-05-SUMMARY.md
@CLAUDE.md

<interfaces>
From hp41-core/src/ops/stat1/mod.rs (post-Plan-33-00 + extensions in Plans 33-04, 33-05):
- `STAT1_MAX_REG: usize` and the family of `STAT1_*_REG` named consts. THIS PLAN MAY ADD MORE named consts if the Plan 33-00 transcription did not cover ΣMMTUG / ΣAOVONE / ΣAOVTWO / ΣANOCOV / ΣCTKKK / ΣCTKK comprehensively.

From hp41-core/src/ops/stats.rs (lines 22–53, 58–85):
- `op_sigma_plus` accumulator (R01–R06) — moments.rs DELEGATES to this for Σx², Σx, n updates and ADDS its own writes for Σx³, Σx⁴ per PATTERNS.md line 458–473.
- `op_sigma_minus` reversal — extended in this plan to mirror ΣMMTUG writes.

From PATTERNS.md §"hp41-core/src/ops/stat1/moments.rs" (line 452–479) and §"hp41-core/src/ops/stat1/anova.rs" (line 482–500):
- Verbatim ΣMMTUG accumulator template using named consts.
- ΣAOVONE F-ratio shell template (OM Results channel deferred to in-plan OM read).

From RESEARCH.md §"ANOVA Family" (line 686–737):
- ΣAOVONE: F = (SSB / dfB) / (SSW / dfW); SSB = Σ nᵢ(x̄ᵢ - x̄)², SSW = Σ(Σxᵢ² - nᵢ x̄ᵢ²); df_between = k-1, df_within = N-k.
- ΣAOVTWO: row/col F-ratios on r×c matrix per RESEARCH.md line 707–722.
- ΣANOCOV: covariate-adjusted F per OM §ANCOVA + NPS ZA-3 reference.

From RESEARCH.md §"ΣCTKKK / ΣCTKK — Contingency Table χ²" (line 776–784):
- Expected_ij = (Row_i · Col_j) / Grand_total; χ² = ΣΣ (O_ij - E_ij)² / E_ij; df = (r-1)(c-1).

From RESEARCH.md §"Pitfall 4 — Welford violated" (line 948–952):
- ANY Stat 1 function that iterates over a RAW data block (not Σ-register-fed) uses Welford. ΣMMTUG accumulates one value at a time via stack X — uses incremental update, NOT raw two-pass.
- Functions that consume the EXISTING Σ-register block (ΣAOVONE means + variances from R01–R06 of per-group blocks) are SAFE because each register slot was already accumulated with 10-digit rounding.

From SPEC.md Req. 8, 10, 11, 12, 13, 14, 28, 29:
- Acceptance oracles for each Op enumerated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ΣMMTUG + ΣMMTGD (3rd/4th moments) + extend op_sigma_minus for [C] correction-key round-trip</name>
  <files>hp41-core/src/ops/stat1/moments.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/stats.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stats.rs (FULL FILE — verify op_sigma_minus reversal pattern lines 58–85; understand the atomic-write discipline used by op_sigma_plus at lines 22–53)
    - hp41-core/src/ops/stat1/mod.rs (verify STAT1_MMTUG_CUBE_REG, STAT1_MMTUG_QUAD_REG, and any frequency-weighted variants exist; ADD with OM page citations if absent)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/moments.rs" (line 452–479 — verbatim ΣMMTUG body using named consts, delegating to op_sigma_plus for Σx/Σx²/n updates and adding own writes for Σx³/Σx⁴) and §"[C] correction-key extension" (line 476–477)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 8, 9, 10 (line 91–104 — ΣMMTUG/MMTGD acceptance + OM transcription gate + [C] correction-key extension)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Σ-Register Layout — Extended Stat 1 Pac registers" (line 506–530)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣMMTUG/ΣMMTGD — confirm register layout + result output channel (skewness γ₁, kurtosis γ₂ pushed to stack vs print buffer)
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/moments.rs`. Disclaim header per PATTERNS.md.

    2. Add `pub mod moments;` to `hp41-core/src/ops/stat1/mod.rs` if missing. Add `STAT1_MMTUG_CUBE_REG`, `STAT1_MMTUG_QUAD_REG`, and `STAT1_MMTGD_*_REG` named consts if not already in `mod.rs` from Plan 33-00 transcription — each carries OM 00041-90030 page citation.

    3. Add `Op::SigmaMmtug` and `Op::SigmaMmtgd` to the `Op` enum.

    4. Implement `pub fn op_sigma_mmtug_accumulate(state: &mut CalcState) -> Result<(), HpError>` per PATTERNS.md line 458–473 verbatim shape:
       - SIZE-floor guard `if state.regs.len() < crate::ops::stat1::STAT1_MAX_REG + 1 { return Err(HpError::InvalidOp); }` (named-const, NOT bare 7).
       - Read x from stack X.
       - Compute new Σx³ and Σx⁴ atomically before writing (atomic discipline matching op_sigma_plus): `let x_cube = x.checked_mul(&x.checked_sq()?)?; let x_quad = x.checked_sq()?.checked_sq()?; let new_cube_reg = state.regs[STAT1_MMTUG_CUBE_REG].checked_add(&x_cube)?; let new_quad_reg = state.regs[STAT1_MMTUG_QUAD_REG].checked_add(&x_quad)?;`
       - Write to registers AFTER both computations succeed (atomic-write discipline per CLAUDE.md / stats.rs Pitfall 5).
       - DELEGATE the n / Σx / Σx² updates to `crate::ops::stats::op_sigma_plus(state)` — never duplicate Σ arithmetic.
       - Return Ok(()).

    5. Implement `pub fn op_sigma_mmtug_compute(state: &mut CalcState) -> Result<(), HpError>` — when the user has fully accumulated, this Op (the second entry point per OM, OR triggered by a different keystroke) computes the moments and pushes results.
       - Read n=R3, Σx=R2, Σx²=R1, Σx³=regs[STAT1_MMTUG_CUBE_REG], Σx⁴=regs[STAT1_MMTUG_QUAD_REG].
       - Compute central moments: μ = Σx/n; μ₂ = Σx²/n − μ²; μ₃ = Σx³/n − 3·μ·Σx²/n + 2·μ³; μ₄ = Σx⁴/n − 4·μ·Σx³/n + 6·μ²·Σx²/n − 3·μ⁴.
       - Compute skewness γ₁ = μ₃ / μ₂^(3/2); kurtosis γ₂ = μ₄ / μ₂² − 3.
       - Push results per OM Results channel (stack push convention: X=γ₂ excess kurtosis, Y=γ₁ skewness, Z=μ₄, T=μ₃ — confirm with OM). Apply LiftEffect::Enable.
       - **Note:** OM may bundle accumulate + compute behind ONE keystroke OR may split them. Read OM §ΣMMTUG section header for the convention. If single entry, this is the only function in the file; if split, both functions exist and are registered separately in STAT_1.ops (which would require either two Op variants or a state-machine — likely just one Op since SPEC.md table assigns only `Op::SigmaMmtug` to ΣMMTUG).

    6. ΣMMTGD (grouped, frequency-weighted) implementation: similar structure, but each (x, f) pair contributes x·f, x²·f, x³·f, x⁴·f to the registers (per OM §ΣMMTGD). Accept f from stack Y. Final compute uses Σf instead of n.

    7. **CRITICAL:** Extend `op_sigma_minus` in `hp41-core/src/ops/stats.rs` per STAT-UNI-04 (SPEC.md Req. 10): the [C] correction-key round-trip MUST reverse every new register written by ΣMMTUG/ΣMMTGD. Read the existing op_sigma_minus body at lines 58–85; identify where it reverses Σx², Σx, n, Σy², Σy, Σxy; ADD parallel reversal for STAT1_MMTUG_CUBE_REG and STAT1_MMTUG_QUAD_REG IFF the register is non-zero (defensive — `op_sigma_minus` is universal but the extended cube/quad registers may not be populated if the user is only using v1.x ΣLIN/etc; defaulting to zero means subtracting zero, a no-op — safe).
       - One way to keep this clean: instead of editing op_sigma_minus directly, add a SECOND public reversal function `pub fn op_sigma_minus_stat1(state) -> Result<(), HpError>` that calls op_sigma_minus then additionally reverses the cube/quad registers. Op variant `Op::SigmaMinusStat1` (or reuse the existing Op::SigmaMinus and dispatch differently when STAT_1 is loaded — depends on how OM specifies the [C] key behavior in Stat 1 Pac).
       - **Recommended:** edit op_sigma_minus IN PLACE to reverse the extended registers — this is a v1.x file edit but stats.rs is NOT in the math1/ freeze list (only math1/ files are frozen per CLAUDE.md; ops/stats.rs is v1.x code that is free to extend). The reversal is conditional on x_cube != 0 + x_quad != 0 to remain backward-compatible with users who only invoke v1.x Σ ops.

    8. Dispatch + execute_op + xrom.rs stub swaps for both Ops (8 sites).

    9. Tests in moments.rs:
       - `mmtug_third_fourth_moments`: load oracle dataset [1..10] (SPEC.md Req. 8 oracle); accumulate via 10 calls to op_sigma_mmtug_accumulate; call op_sigma_mmtug_compute; assert μ₃ within 1e-9 of 0.0; μ₄ within 1e-9 of 33.0; γ₁ within 1e-9 of 0.0; γ₂ within 1e-9 of -1.224 (per RESEARCH.md). Cite scipy.stats.skew + scipy.stats.kurtosis in comments.
       - `mmtgd_grouped_variant`: oracle dataset from OM §ΣMMTGD example.
       - `mmtug_accumulate_then_minus_restores_state` (STAT-UNI-04 round-trip): construct fresh CalcState; accumulate (x=5) via ΣMMTUG; call op_sigma_minus; assert every register including STAT1_MMTUG_CUBE_REG and STAT1_MMTUG_QUAD_REG matches the pre-accumulation state within 1e-12 relative tolerance.
       - `mmtug_size_floor_guard`.

  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::moments::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/moments.rs` succeeds.
    - `grep -c 'pub fn op_sigma_mmtug\|pub fn op_sigma_mmtgd' hp41-core/src/ops/stat1/moments.rs` returns >=2.
    - `grep -c 'STAT1_MMTUG_CUBE_REG\|STAT1_MMTUG_QUAD_REG' hp41-core/src/ops/stat1/moments.rs hp41-core/src/ops/stats.rs hp41-core/src/ops/stat1/mod.rs` returns >=4 (declared in mod.rs, referenced in moments.rs and the extended op_sigma_minus in stats.rs).
    - `grep -c 'crate::ops::stats::op_sigma_plus' hp41-core/src/ops/stat1/moments.rs` returns >=1 (delegate pattern enforced — no duplication of Σ arithmetic).
    - `grep -E 'state.regs\\[[7-9]\\]|state.regs\\[1[0-9]\\]' hp41-core/src/ops/stat1/moments.rs` returns 0 lines (no literal-integer register indices ≥7 — P21 mitigation).
    - `cargo test -p hp41-core --lib ops::stat1::moments::tests` exits 0 (≥4 tests including the STAT-UNI-04 round-trip).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
    - `cargo test -p hp41-core --lib stats_tests` exits 0 (the v1.x stats_tests integration suite — assumed to live at hp41-core/tests/stats_tests.rs; confirm via `ls`; the test suite must still pass after extending op_sigma_minus).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/moments.rs | wc -l` ≤ 300.
    - `bash scripts/check-free42-contamination.sh` exits 0.
  </acceptance_criteria>
  <done>ΣMMTUG (ungrouped) + ΣMMTGD (grouped) ship with extended-register accumulation using only named consts; op_sigma_minus extended for [C] correction-key round-trip with backward-compat zero-test; SPEC.md Req. 8, 10 oracles green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ΣAOVONE + ΣAOVTWO + ΣANOCOV (ANOVA family with OM-verified register layout)</name>
  <files>hp41-core/src/ops/stat1/anova.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/mod.rs (verify STAT1_AOV_*_REG named consts exist; ADD with OM page citations if Plan 33-00 transcription did not cover all three ANOVA Ops)
    - hp41-core/src/ops/stats.rs (Σ-register read pattern + SIZE-floor guard analog)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/anova.rs" (line 482–500 — verbatim template; output channel deferred to in-plan OM read)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ANOVA Family" (line 686–736 — algorithm details for all three Ops)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 11, 12, 13, 14 (line 109–127 — acceptance oracles + OM register layout gate)
    - HP-41C Stat 1 Pac OM 00041-90030 §ANOVA / §ΣAOVONE / §ΣAOVTWO / §ANCOVA — the SINGLE source of truth for per-group register layout
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/anova.rs`. Disclaim header.

    2. Add `pub mod anova;` to `stat1/mod.rs` if missing. Add OM-traceable `STAT1_AOV_GROUP_BASE_REG`, `STAT1_AOV_GROUP_STRIDE`, `STAT1_AOV_GROUP_COUNT_REG`, etc. named consts if absent. Each carries OM page citation.

    3. Add `Op::SigmaAovone`, `Op::SigmaAovtwo`, `Op::SigmaAnocov` to the `Op` enum.

    4. Implement `pub fn op_sigma_aovone(state: &mut CalcState) -> Result<(), HpError>` per RESEARCH.md line 692–705:
       - SIZE-floor guard with STAT1_MAX_REG.
       - Read k (group count) and per-group accumulators (Σxᵢ, Σxᵢ², nᵢ) from named-const register slots derived from OM. The OM specifies the per-group stride (e.g. each group occupies 3 contiguous registers: Σx, Σx², n).
       - Compute grand mean x̄, group means x̄ᵢ, SSB = Σ nᵢ(x̄ᵢ - x̄)², SSW = Σ(Σxᵢ² - nᵢ·x̄ᵢ²), df_between = k-1, df_within = N-k.
       - F = (SSB/df_between) / (SSW/df_within).
       - Push F to X (or per OM Results channel; if OM specifies F + dfs + group means in multiple stack slots, push in OM-defined order).
       - Tolerance: 1e-9 closed-form per SPEC.md Req. 11 acceptance (oracle: 3 groups of 5 [1..5],[6..10],[11..15] → F = 100.0 exactly per scipy.stats.f_oneway).

    5. Implement `pub fn op_sigma_aovtwo(state: &mut CalcState) -> Result<(), HpError>` per RESEARCH.md line 708–722:
       - Read row sums Rᵢ, column sums Cⱼ, grand sum T from OM-specified registers; compute SS_total, SS_row, SS_col, SS_error; df_row=r-1, df_col=c-1, df_error=(r-1)(c-1); F_row, F_col.
       - Push F_row and F_col per OM Results.
       - Tolerance: 1e-7 iterative per SPEC.md Req. 12 (chained cross-product sums lose precision).

    6. Implement `pub fn op_sigma_anocov(state: &mut CalcState) -> Result<(), HpError>` per RESEARCH.md line 724–732:
       - Read each observation as (x, y) where x is response and y is covariate per SPEC.md Req. 13 (NOTE: RESEARCH.md Open Q 5 flags potential reversal of x/y semantics vs OM — read OM ΣANCOVA section to confirm; document inline if SPEC.md is reversed).
       - Compute pooled within-group regression coefficient b_w = SSxy_within / SSyy_within; adjusted SS; F-ratio.
       - Tolerance: 1e-7 per SPEC.md Req. 13.

    7. Dispatch + execute_op + xrom.rs stub swaps (12 sites: 3 Ops × 4 sites).

    8. Tests in anova.rs:
       - `aovone_three_groups_of_five_yields_f_100`: load oracle per SPEC.md Req. 11; assert F within 1e-9 of 100.0. Cite scipy.stats.f_oneway result.
       - `aovtwo_3x4_dataset`: oracle from scipy reference; F_row + F_col within 1e-7.
       - `anocov_nps_za3_example`: oracle from NPS ZA-3 example or scipy reference; F within 1e-7.
       - `aovone_size_floor_guard`.
       - `aovone_named_consts_no_literal_indices`: NOT a runtime test — a grep gate (see acceptance criteria).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::anova::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/anova.rs` succeeds.
    - `grep -cE 'pub fn op_sigma_(aovone|aovtwo|anocov)' hp41-core/src/ops/stat1/anova.rs` returns 3.
    - `grep -c 'STAT1_AOV' hp41-core/src/ops/stat1/anova.rs hp41-core/src/ops/stat1/mod.rs` returns >=4.
    - `grep -E 'state.regs\\[[7-9]\\]|state.regs\\[[1-9][0-9]\\]' hp41-core/src/ops/stat1/anova.rs` returns 0 lines (P21 mitigation — code-review gate).
    - `grep -c 'Op::SigmaAovone\|Op::SigmaAovtwo\|Op::SigmaAnocov' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=12.
    - `grep -c '00041-90030\|OM\|scipy.stats.f_oneway' hp41-core/src/ops/stat1/anova.rs` returns >=3 (oracle / OM citations).
    - `cargo test -p hp41-core --lib ops::stat1::anova::tests` exits 0 (≥4 tests including the SPEC.md Req. 11 F=100.0 oracle).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/anova.rs | wc -l` ≤ 300.
  </acceptance_criteria>
  <done>The three ANOVA Ops ship using only OM-traceable named-const register access; F=100.0 oracle hits exactly; ΣAOVTWO row+col F-ratios within 1e-7; ΣANOCOV within 1e-7 against NPS ZA-3 or scipy reference; anova.rs stays ≤300 LOC.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ΣCTKKK + ΣCTKK in nonparam.rs — contingency-table chi-square Ops</name>
  <files>hp41-core/src/ops/stat1/nonparam.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/nonparam.rs (post-Plan-33-04; extend with the two contingency-table Ops; verify LOC budget stays ≤300 after this task)
    - hp41-core/src/ops/stat1/mod.rs (verify STAT1_CTKKK_*_REG named consts exist; ADD with OM page citations if absent)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"ΣCTKKK / ΣCTKK" (line 777–784 — algorithm)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 28, 29 (line 197–205 — oracle acceptance values)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣCTKKK and §ΣCTKK — confirm register layout: row count r, column count c, cell data O_ij at base register, marginal totals possibly pre-computed or computed on the fly
  </read_first>
  <action>
    1. Extend `hp41-core/src/ops/stat1/nonparam.rs` (post-Plan-33-04) with ΣCTKKK and ΣCTKK implementations.

    2. Add `STAT1_CTKKK_R_REG`, `STAT1_CTKKK_C_REG`, `STAT1_CTKKK_CELL_BASE_REG` named consts to `stat1/mod.rs` if absent (OM-traceable).

    3. Add `Op::SigmaCtkkk` and `Op::SigmaCtkk` to the `Op` enum.

    4. Implement `pub fn op_sigma_ctkkk(state: &mut CalcState) -> Result<(), HpError>`:
       - SIZE-floor guard.
       - Read r, c from named-const registers; read cell data O_ij from contiguous cell base.
       - Compute row sums Rᵢ = Σⱼ O_ij; column sums Cⱼ = Σᵢ O_ij; grand total T = ΣΣ O_ij.
       - Expected_ij = (Rᵢ · Cⱼ) / T.
       - χ² = ΣΣ (O_ij - E_ij)² / E_ij; guard against E_ij == 0 (Err HpError::Domain).
       - Push χ² to stack X; push df = (r-1)·(c-1) to Y (optional, per OM Results); LiftEffect::Enable.
       - Tolerance: 1e-9 closed-form per SPEC.md Req. 46.

    5. Implement `pub fn op_sigma_ctkk(state: &mut CalcState) -> Result<(), HpError>`:
       - Smaller-table variant per OM §ΣCTKK; likely a 2×2 special case with simpler register layout. Read OM to confirm whether it shares the ΣCTKKK register block or has a dedicated 2×2 layout (e.g. R_a..R_d for the four cells).
       - Same χ² formula applied to the 2×2 case.

    6. Dispatch + execute_op + xrom.rs stub swaps (8 sites).

    7. Tests in nonparam.rs (extending the mod tests block):
       - `ctkkk_2x3_oracle`: load [[10,20,30],[40,50,60]] per SPEC.md Req. 28; assert χ² within 1e-9 of 4.286. Verify via `scipy.stats.chi2_contingency` in plan comment; if scipy yields a different value (the standard chi2_contingency uses Yates' continuity correction in 2×2 — but for 2×3 should match), use scipy's value.
       - `ctkk_2x2_oracle`: load [[10,20],[30,40]] per SPEC.md Req. 29; assert χ² within 1e-9 of 0.397. Verify scipy (note: scipy.stats.chi2_contingency by default applies Yates' correction in 2×2 cases — Stat 1 Pac per OM may NOT apply Yates; read OM to confirm; if OM does NOT apply Yates, use `scipy.stats.chi2_contingency(..., correction=False)` for the oracle).
       - Edge: `ctkkk_zero_expected_returns_domain_error` (degenerate marginals).
       - Size-floor: `ctkkk_size_floor_guard`.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::nonparam::tests::ctkk -- --quiet && cargo clippy -p hp41-core -- -D warnings && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'pub fn op_sigma_ctk(kk|k)' hp41-core/src/ops/stat1/nonparam.rs` returns 2.
    - `grep -c 'STAT1_CTKKK' hp41-core/src/ops/stat1/nonparam.rs hp41-core/src/ops/stat1/mod.rs` returns >=2.
    - `grep -c 'Op::SigmaCtkkk\|Op::SigmaCtkk' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=8.
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests::ctkk` exits 0 (≥4 ctkk-related tests including both 2×3 and 2×2 oracles).
    - `cargo test -p hp41-core --lib ops::stat1::nonparam::tests` exits 0 (full nonparam suite green — ΣSPEAR/ΣXSQEV/ΣEFXSQ from Plan 33-04 + new ΣCTKKK/ΣCTKK).
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (7 more stubs swapped — cumulative 18 of 26 after this plan + Plans 33-03/04/05).
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/nonparam.rs | wc -l` ≤ 300 (constrained budget — 5 Ops in one file).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - `bash scripts/check-free42-contamination.sh` exits 0.
  </acceptance_criteria>
  <done>ΣCTKKK (general r×c) and ΣCTKK (smaller table) ship using named-const register layout; both oracle acceptance tests green; nonparam.rs production-code stays ≤300 LOC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OM Storage-Registers transcription ↔ named consts | The single P21 trap-line: if any STAT1_*_REG const value is one slot off from OM, every dependent Op silently produces wrong answers that may even pass simple oracle tests. |
| op_sigma_minus reversal coverage ↔ ΣMMTUG accumulation writes | If op_sigma_minus extension misses STAT1_MMTUG_CUBE_REG or STAT1_MMTUG_QUAD_REG, the [C] correction-key produces inconsistent state — Σ+ then Σ- leaves residue in the cube/quad slots. |
| ANOVA per-group register stride ↔ OM grouping convention | Mis-reading the OM stride (e.g. 4-register-per-group instead of 3) silently reads garbage data — every F-ratio test fails or worse, accidentally matches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering (silent wrong answer — P21 catastrophic class) | All 7 Ops in this plan | mitigate | Every register access uses a STAT1_*_REG named const traceable to an OM 00041-90030 page citation declared in `stat1/mod.rs`. Code-review gate: grep for literal-integer indices ≥7 in moments.rs / anova.rs / nonparam.rs returns empty. Oracle tests include the SPEC.md F=100.0 exact match (3 groups of 5) which is the canonical "did the register layout work?" probe — if F is anything other than 100.0 within 1e-9, the register layout is wrong. |
| T-33-numerical-precision | Tampering (variance instability — P18 / Welford) | op_sigma_mmtug_compute | accept | Variance / moments computed from existing R01–R06 Σ-block which were accumulated one HpNum at a time with 10-digit rounding. Per PATTERNS.md Pitfall 4: this is the SAFE case (Σ-block-fed). Welford is required only for raw-data iteration — Stat 1 Pac does not iterate raw data, only Σ-blocks. |
| T-33-state-persistence | Tampering (incomplete [C] correction-key round-trip) | hp41-core/src/ops/stats.rs::op_sigma_minus | mitigate | Acceptance test `mmtug_accumulate_then_minus_restores_state` proves the round-trip; the extension to op_sigma_minus is backward-compat (zero-check guards keep v1.x users unaffected). Per STAT-UNI-04 / SPEC.md Req. 10. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/stats.rs | accept | This plan edits `stats.rs` which is v1.x code, NOT in the math1/ freeze list per CLAUDE.md (only math1/* files are frozen; stats.rs is at ops/stats.rs root, free to extend). Edit is documented as the STAT-UNI-04 extension. |
| T-33-free42-contamination | Tampering | hp41-core/src/ops/stat1/moments.rs + anova.rs + nonparam.rs | mitigate | Disclaim headers present in all new files; algorithm citations point to OM 00041-90030 + scipy.stats (oracle) + NPS ZA-3 (cross-check). No Free42 code or distinctive identifiers. `bash scripts/check-free42-contamination.sh` exits 0. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1` exits 0 (covers moments + anova + nonparam new tests).
- `cargo test -p hp41-core --lib stats_tests` or `cargo test -p hp41-core --test stats_tests` exits 0 (existing v1.x stats suite passes after op_sigma_minus extension).
- `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0.
- File-level: moments.rs ≤300; anova.rs ≤300; nonparam.rs ≤300 LOC.
- No literal-integer register indices ≥7 in moments.rs / anova.rs / nonparam.rs (P21 mitigation grep gate).
- `bash scripts/check-free42-contamination.sh` exits 0.
- 7 more stubs swapped (cumulative 18 of 26 after this plan).
</verification>

<success_criteria>
SPEC.md Acceptance criteria 8, 10, 11, 12, 13, 14, 28, 29 GREEN. The OM-register-layout-dependent cluster is complete; P21 risk reduced to near-zero via the named-const discipline.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-06-SUMMARY.md` when done summarizing: the OM-confirmed register stride for ANOVA per-group blocks; the [C] correction-key extension strategy (in-place op_sigma_minus edit vs new op_sigma_minus_stat1 — and the rationale); any newly discovered SPEC.md value discrepancies; the cumulative count of swapped stubs (target 18 of 26).
</output>
