---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 02
plan_id: 33-02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - hp41-core/src/ops/stat1/distributions.rs
  - hp41-core/src/ops/stat1/mod.rs
autonomous: true
requirements:
  - STAT-DST-06

must_haves:
  truths:
    - "norm_cdf_inv_f64(0.025) returns a value within 1e-9 of -1.959963984540054"
    - "gamma_regularized_f64(2.0, 1.0) returns a value within 1e-9 of 0.2642411176571153"
    - "beta_regularized_f64(2.0, 2.0, 0.5) returns 0.5 exactly (within 1e-12 epsilon)"
    - "All three distribution primitives pass ≥6 scipy.stats oracle tuples each BEFORE Plan 33-03 lands any Op consuming them"
  artifacts:
    - path: "hp41-core/src/ops/stat1/distributions.rs"
      provides: "norm_cdf_inv_f64 (Acklam/AS 241), gamma_regularized_f64 (AS 239), beta_regularized_f64 (AS 63) + inline scipy oracle tests"
      contains: "pub fn norm_cdf_inv_f64"
      min_lines: 200
  key_links:
    - from: "hp41-core/src/ops/stat1/distributions.rs"
      to: "scipy.stats oracle constants"
      via: "inline (input, scipy_expected, tolerance) tuples in #[cfg(test)] mod tests"
      pattern: "scipy.stats"
---

<objective>
Wave 2 — ship the three hand-coded f64-bridge distribution primitives in `hp41-core/src/ops/stat1/distributions.rs` that Plans 33-03 (ΣNORMD, ΣCHISQD) and 33-07 (ΣPTST, ΣTSTAT) consume. All three are validated against ≥6 inline scipy.stats oracle tuples each per D-33.6. The plan ships ZERO `Op` variants — pure algorithmic foundation with `#[cfg(test)] mod tests` proving correctness BEFORE any downstream Op exists (SPEC.md Req. 33 acceptance contract).

Purpose: This is the algorithmic foundation of v3.1. Acklam coefficients copied wrong → ΣNORMD inverse silently produces wrong answers (Pitfall 7). AS 239 series-vs-CF split coded wrong → ΣCHISQD CDF wrong in the boundary region (Open Q 6). AS 63 boundary swap mis-applied → ΣTSTAT/ΣPTST p-values wrong for one-sided cases (Pitfall 6). Plan 33-02 catches all three by running scipy oracle GREEN before any caller exists.

Output: `stat1/distributions.rs` with three public functions returning `Result<f64, HpError>`, ≥18 oracle tuples total (≥6 per primitive), ≤300 LOC excluding tests (per D-33.5 per-file budget). `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0. NO `Op` variants in `Op` enum. NO modifications to `dispatch()` / `execute_op()`. NO modifications to other stat1/ files except `mod.rs` (which gains the `pub mod distributions;` declaration if not yet present from Plan 33-00).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-00-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- f64-bridge pattern from hp41-core/src/num.rs:184-211 — every Stat 1 distribution primitive follows this shape. -->

From hp41-core/src/num.rs (checked_asin template, lines 184–211):
- Signature: `pub fn checked_asin(&self) -> Result<HpNum, HpError>`
- Body shape: convert to f64 → domain check → call f64 math → convert back to Decimal → rounded HpNum
- Error type: HpError::Domain for input out of range; HpError::Overflow for f64-to-Decimal conversion failure

From hp41-core/src/error.rs (HpError enum — read to confirm Domain and Overflow variants exist; Cancelled and ConvergenceFailed variants may need to be added if absent — verify before coding):
- Existing variants assumed: Domain, Overflow
- Plan 33-03 will need Cancelled + ConvergenceFailed if not present; Plan 33-02 distribution primitives use only Domain and Overflow

From PATTERNS.md §"hp41-core/src/ops/stat1/distributions.rs" (line 254–308):
- Function signature template: `pub fn norm_cdf_inv_f64(p: f64) -> Result<f64, HpError>` — note: returns BARE f64, not HpNum, because the outer Op layer in Plan 33-03 (`normd.rs` / `chisqd.rs`) does the `HpNum→f64→f64→HpNum` round trip.
- Test block template: `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests { use super::*; use approx::assert_relative_eq; ... }`

From RESEARCH.md §"Distribution Primitives — Algorithms & Validation" (line 361–492):
- Algorithm 1 Acklam coefficients verbatim at line 384–408 (21 coefficients across 4 named tables a/b/c/d)
- Algorithm 2 AS 239 gser+gcf split at `x < a + 1`
- Algorithm 3 AS 63 boundary swap at `x < (a+1)/(a+b+2)`
- Validation Table at line 471–490 — 18 oracle tuples (6 Acklam + 4 AS 239 + 3 AS 63 + 5 Op-level — only the first 13 belong in this plan; the Op-level ones land in Plan 33-03)

From CLAUDE.md §"Frozen Invariants — Core engine":
- `#![deny(clippy::unwrap_used)]` — `.expect("reason")` or `?`-propagation only in production code
- Test modules carry `#[allow(clippy::unwrap_used)]`
- No `println!`/`eprintln!` in hp41-core
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement norm_cdf_inv_f64 (Acklam/AS 241) with ≥6 inline scipy oracle tests</name>
  <files>hp41-core/src/ops/stat1/distributions.rs, hp41-core/src/ops/stat1/mod.rs</files>
  <read_first>
    - hp41-core/src/num.rs (lines 184–215 — checked_asin / checked_acos / checked_atan f64-bridge templates)
    - hp41-core/src/error.rs (full file — confirm HpError variants Domain, Overflow exist; if Cancelled or ConvergenceFailed missing, this plan does NOT add them — they are Plan 33-03 territory)
    - hp41-core/src/ops/stat1/mod.rs (existing file from Plan 33-00/33-01; verify whether `pub mod distributions;` is already declared or commented-out)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Algorithm 1: norm_cdf_inv_f64 — Inverse Standard Normal CDF (Acklam / AS 241)" (line 365–412 — full algorithm description + verbatim 21-coefficient table at lines 384–408)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Validation Table" (line 467–492 — rows 1–6 for Acklam oracle tuples)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/distributions.rs" (line 254–308 — function signature + test block template)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 33 (line 229–236 — primitive shapes + ≥6 oracle tuples per primitive)
    - https://stackedboxes.org/2017/05/01/acklams-normal-quantile-function/ — Acklam paper reference; do NOT open Free42 core_math2.cc for coefficient copy (contamination guard)
  </read_first>
  <action>
    Create `hp41-core/src/ops/stat1/distributions.rs` (or extend if Plan 33-01 stubbed it). First three lines: byte-for-byte disclaim header per PATTERNS.md OM template — DISCLAIM_LINE substring identical to math1/ files so contamination allow-list works.

    1. Add the `pub mod distributions;` declaration to `hp41-core/src/ops/stat1/mod.rs` if not already present from Plan 33-00.

    2. Implement `pub fn norm_cdf_inv_f64(p: f64) -> Result<f64, HpError>`:
       - Domain check: `if !p.is_finite() || !(0.0..=1.0).contains(&p) { return Err(HpError::Domain); }`. Edge cases p=0.0 and p=1.0: research recommends `Err(HpError::Domain)` for asymptotes — see RESEARCH.md line 307–311; final OM behavior decision noted in plan summary if OM clarifies otherwise (none expected this plan, since OM defines Op-level behavior in ΣNORMD inverse, not the bare primitive).
       - Use the Acklam coefficient tables a₁..a₆, b₁..b₅, c₁..c₆, d₁..d₄ VERBATIM from RESEARCH.md line 384–408 (lifted from `stackedboxes.org/2017/05/01/acklams-normal-quantile-function/`). Each coefficient gets a `const NAME: f64 = ...;` declaration so the rational-approximation body is readable. Cite Acklam (1999) and `stackedboxes.org` URL in a comment block above the constants.
       - Implement the three regions per Acklam: central (0.02425 ≤ p ≤ 0.97575), lower tail (p < 0.02425), upper tail (p > 0.97575). Lower tail uses `q = sqrt(-2 * ln(p))`; upper tail uses `q = sqrt(-2 * ln(1 - p))` and negates the result.
       - Return the computed `f64` in `Ok(...)`. Do NOT apply Halley refinement initially (per RESEARCH.md line 410–412: pure Acklam suffices for 1e-9 oracle bound; add Halley only if oracle tests fail).
       - The function body must remain ≤ 60 lines (RESEARCH.md line 233: "~30 LOC"). No `println!`/`eprintln!`. Use `.expect("…")` or `?` for any fallible operation (no `.unwrap()`).

    3. Add a `#[cfg(test)] #[allow(clippy::unwrap_used)] mod tests` block with ≥6 inline oracle tuples covering the central + lower-tail + upper-tail + deep-tail regions (lines 471–478 of RESEARCH.md give 6 verified tuples). Each tuple gets a Python-comment line above it citing the scipy.stats command that generated it, per D-33.6: e.g. `// scipy.stats.norm.ppf(0.025) = -1.959963984540054`. Tolerances per SPEC.md Req. 33 + Req. 46 — 1e-9 for the closed-form primitive. Use `approx::assert_relative_eq!(actual, expected, max_relative = 1e-9)` for non-zero tuples; use `epsilon = 1e-12` (absolute) for the p=0.5 → 0.0 tuple per RESEARCH.md table row 5.
       - Tuple 1: p=0.025, expected=-1.959963984540054, max_relative=1e-9
       - Tuple 2: p=0.975, expected=1.959963984540054, max_relative=1e-9
       - Tuple 3: p=0.001, expected=-3.090232306167813, max_relative=1e-9
       - Tuple 4: p=0.999, expected=3.090232306167813, max_relative=1e-9
       - Tuple 5: p=0.5, expected=0.0, epsilon=1e-12
       - Tuple 6: p=1e-6, expected=-4.753424308822543, max_relative=1e-9
       - Edge tests: norm_cdf_inv_f64(0.0).is_err(), norm_cdf_inv_f64(1.0).is_err(), norm_cdf_inv_f64(-0.1).is_err(), norm_cdf_inv_f64(1.1).is_err().

    Per CLAUDE.md §"Free42 GPL-contamination guard": do NOT open `github.com/thomasokken/free42/blob/master/common/core_math2.cc` for coefficient copying. Acklam coefficients come from the public-domain `stackedboxes.org` mirror; cite the URL in the const block.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::distributions::tests::norm_cdf_inv -- --quiet && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/distributions.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'pub fn norm_cdf_inv_f64' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'Acklam' hp41-core/src/ops/stat1/distributions.rs` returns >=1 (algorithm citation).
    - `grep -c 'stackedboxes' hp41-core/src/ops/stat1/distributions.rs` returns >=1 (source URL).
    - `grep -c 'scipy.stats.norm.ppf' hp41-core/src/ops/stat1/distributions.rs` returns >=6 (oracle tuples).
    - `grep -c 'assert_relative_eq' hp41-core/src/ops/stat1/distributions.rs` returns >=5 (one per non-zero tuple; the p=0.5 tuple uses epsilon).
    - `grep -c 'pub mod distributions' hp41-core/src/ops/stat1/mod.rs` returns 1.
    - `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0 (all 10+ test functions green: 6 oracle tuples + 4 edge tests).
    - `bash scripts/check-free42-contamination.sh` exits 0 (Acklam coefficients are NOT distinctive Free42 identifiers; the new file disclaim header allows the file under the existing allow-list).
  </acceptance_criteria>
  <done>`norm_cdf_inv_f64` ships with the Acklam coefficient block (21 named consts), the central + tail-region rational approximation, and ≥6 scipy.stats oracle tuples all passing within 1e-9 relative tolerance. The contamination guard returns clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement gamma_regularized_f64 (AS 239) + ln_gamma helper with ≥6 inline scipy oracle tests</name>
  <files>hp41-core/src/ops/stat1/distributions.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/distributions.rs (full file from Task 1 — extend, do not overwrite)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Algorithm 2: gamma_regularized_f64 — Regularized Lower Incomplete Gamma P(a,x) (AS 239)" (line 414–438 — full algorithm including gser + gcf split + ln Γ helper)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Validation Table" (line 467–492 — rows 7–10 for gamma oracle tuples)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 33 + Req. 34 (line 229–245 — ≥6 oracle tuples per primitive + 50-iter cap)
    - Numerical Recipes 3e §6.1 (`gammln`) + §6.2 (`gammp` / `gser` / `gcf`) — algorithm reference; do NOT consult Free42 core_math2.cc
  </read_first>
  <action>
    Extend `hp41-core/src/ops/stat1/distributions.rs`:

    1. Implement a private helper `fn ln_gamma(a: f64) -> f64` using Stirling's series + Lanczos coefficients per Numerical Recipes 3e §6.1 (`gammln`). Cite NR §6.1 + AS 245 in comment; do NOT cite or copy from Free42. The function returns `ln Γ(a)` for a > 0; for a ≤ 0 it may panic via `.expect` (clippy::unwrap_used is allowed in tests but production code uses Result). To stay clean: signature `fn ln_gamma(a: f64) -> Result<f64, HpError>` returning `Err(HpError::Domain)` for a ≤ 0. ~25 LOC.

    2. Implement `pub fn gamma_regularized_f64(s: f64, x: f64) -> Result<f64, HpError>`:
       - Domain check: `if !s.is_finite() || !x.is_finite() || s <= 0.0 || x < 0.0 { return Err(HpError::Domain); }`.
       - Edge case `x == 0.0`: return `Ok(0.0)` per limit P(s, 0) = 0.
       - Algorithm selection per AS 239 / NR 3e §6.2: `if x < s + 1.0 { gser(s, x) } else { 1.0 - gcf(s, x) }`.
       - `fn gser(s: f64, x: f64) -> Result<f64, HpError>`: power-series expansion with iteration cap of 50 per SPEC.md Req. 34; returns `Err(HpError::Domain)` on non-convergence (since this primitive function has no cancel_requested — the outer ΣCHISQD Op layer in Plan 33-03 wraps with cancel checks). Multiplier `exp(-x + s * ln(x) - ln_gamma(s))`.
       - `fn gcf(s: f64, x: f64) -> Result<f64, HpError>`: continued-fraction expansion via modified Lentz algorithm, cap 50; multiplier `exp(-x + s * ln(x) - ln_gamma(s))`.
       - Total ~50 LOC per RESEARCH.md line 233.

    3. Add oracle tests (≥6 tuples per SPEC.md Req. 33) inside the existing `#[cfg(test)] mod tests` block:
       - Tuple 7 (RESEARCH.md): s=2.0, x=1.0, expected=0.2642411176571153, max_relative=1e-9, command: `scipy.special.gammainc(2, 1)`
       - Tuple 8: s=2.0, x=10.0, expected=0.9995006007726127, max_relative=1e-9
       - Tuple 9: s=1.5, x=5.0, expected=0.9595723180054873, max_relative=1e-9
       - Tuple 10: s=50.0, x=50.0, expected=0.5188083154720433, max_relative=1e-9
       - Boundary tuple at x = s + 1 boundary: s=3.0, x=4.0, expected=`scipy.special.gammainc(3, 4)` (generate the value via scipy command in plan comments before writing), tol=1e-9 — tests the gser/gcf boundary continuity per RESEARCH.md Open Q 6.
       - Edge tests: gamma_regularized_f64(0.0, 1.0).is_err(), gamma_regularized_f64(1.0, -1.0).is_err(), gamma_regularized_f64(2.0, 0.0) returns Ok(0.0).
       Total: ≥5 oracle + 3+ edge = ≥6 per primitive (SPEC.md Req. 33 satisfied).

    Per CLAUDE.md §"Frozen Invariants — Core engine": no `println!`/`eprintln!`; no `.unwrap()` in production code; Lanczos / Stirling references cite Numerical Recipes, NOT Free42.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::distributions::tests::gamma -- --quiet && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn gamma_regularized_f64' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'fn ln_gamma' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'AS 239\|Numerical Recipes\|gammln' hp41-core/src/ops/stat1/distributions.rs` returns >=1 (algorithm citation).
    - `grep -c 'scipy.special.gammainc' hp41-core/src/ops/stat1/distributions.rs` returns >=4 (oracle tuples).
    - `grep -c '50' hp41-core/src/ops/stat1/distributions.rs` returns >=2 (iteration cap appears in both gser and gcf bodies).
    - `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0 (all Acklam + gamma tests green).
    - `bash scripts/check-free42-contamination.sh` exits 0 (no contamination tokens; the >=6 stats-domain tokens from Plan 33-00 PATTERN must NOT trigger on AS 239 / Numerical Recipes prose).
  </acceptance_criteria>
  <done>`gamma_regularized_f64` ships with the gser/gcf algorithm selection, 50-iteration cap, ≥6 scipy.special.gammainc oracle tuples (including a boundary case at x = s + 1), and edge-case error handling. Total ≤80 LOC for the gamma primitive + helper.</done>
</task>

<task type="auto">
  <name>Task 3: Implement beta_regularized_f64 (AS 63) with ≥6 inline scipy oracle tests + file-level audit</name>
  <files>hp41-core/src/ops/stat1/distributions.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/distributions.rs (full file from Tasks 1–2 — extend; verify total LOC budget ≤300 excluding tests per D-33.5)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Algorithm 3: beta_regularized_f64 — Regularized Incomplete Beta I_x(a,b) (AS 63)" (line 440–465 — full algorithm including symmetric boundary swap + ln B helper)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Validation Table" (line 481–485 — rows 11–13 for beta oracle tuples)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 33 (line 229–236)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md §D-33.5 file-layout constraint (line 86–88 — each stat1/*.rs file ≤300 LOC excluding tests)
  </read_first>
  <action>
    Extend `hp41-core/src/ops/stat1/distributions.rs`:

    1. Implement `pub fn beta_regularized_f64(a: f64, b: f64, x: f64) -> Result<f64, HpError>`:
       - Domain check: `if !a.is_finite() || !b.is_finite() || !x.is_finite() || a <= 0.0 || b <= 0.0 || !(0.0..=1.0).contains(&x) { return Err(HpError::Domain); }`.
       - Edge cases: `x == 0.0` → Ok(0.0); `x == 1.0` → Ok(1.0).
       - Symmetric boundary swap per AS 63 / NR 3e §6.4: `if x < (a + 1.0) / (a + b + 2.0) { betacf(a, b, x) } else { 1.0 - betacf(b, a, 1.0 - x) }`.
       - Multiplier `bt = exp(ln_gamma(a + b) - ln_gamma(a) - ln_gamma(b) + a * ln(x) + b * ln(1 - x))`.
       - Final return `bt * betacf(...) / a` for the unswapped path; `1.0 - bt * betacf(...) / b` for the swapped path.
       - `fn betacf(a: f64, b: f64, x: f64) -> Result<f64, HpError>`: continued-fraction via modified Lentz, cap 50 iterations per SPEC.md Req. 34; returns `Err(HpError::Domain)` on non-convergence.
       - Total ~60 LOC per RESEARCH.md line 234.

    2. Add oracle tests (≥6 tuples per SPEC.md Req. 33):
       - Tuple 11 (RESEARCH.md): a=2.0, b=2.0, x=0.5, expected=0.5, epsilon=1e-12 (exact)
       - Tuple 12: a=0.5, b=0.5, x=0.25, expected=0.3333333333333333, max_relative=1e-9
       - Tuple 13: a=2.5, b=0.5, x=1.0, expected=1.0, epsilon=1e-12 (exact)
       - Additional tuples to reach ≥6 (generate via scipy.special.betainc in plan comments before writing):
         - Skew-symmetric: a=10.0, b=10.0, x=0.5, expected=0.5, epsilon=1e-12 (exact, swap not triggered)
         - Tail: a=5.0, b=5.0, x=0.1, expected=`scipy.special.betainc(5, 5, 0.1)`, max_relative=1e-9
         - Cross-boundary: a=3.0, b=4.0, x=0.6, expected=`scipy.special.betainc(3, 4, 0.6)`, max_relative=1e-9 — tests the swap path
       - Edge tests: beta_regularized_f64(0.0, 1.0, 0.5).is_err(), beta_regularized_f64(1.0, 0.0, 0.5).is_err(), beta_regularized_f64(1.0, 1.0, -0.1).is_err(), beta_regularized_f64(1.0, 1.0, 1.1).is_err(), beta_regularized_f64(2.0, 3.0, 0.0) returns Ok(0.0), beta_regularized_f64(2.0, 3.0, 1.0) returns Ok(1.0).

    3. Audit file-level constraints (D-33.5):
       - Run `wc -l hp41-core/src/ops/stat1/distributions.rs` and visually separate production-code lines (above `#[cfg(test)]` mod tests) from test lines; confirm production-code count ≤ 300 (RESEARCH.md line 234 budget is ~140 LOC total: 30 Acklam + 50 gamma + 60 beta + helpers). If approaching 300, refactor by extracting shared helpers (`ln_gamma`) to a private const-block at file head.
       - Confirm `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0 with ALL Acklam + gamma + beta tuples green BEFORE Plan 33-03 starts.

    Per RESEARCH.md confidence note: AS 63 + Numerical Recipes 3e §6.4 are the source; Free42 core_math2.cc must NOT be consulted (contamination guard).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::distributions::tests -- --quiet && bash scripts/check-free42-contamination.sh && cargo clippy -p hp41-core -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn beta_regularized_f64' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'fn betacf' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'AS 63\|Numerical Recipes' hp41-core/src/ops/stat1/distributions.rs` returns >=1.
    - `grep -c 'scipy.special.betainc' hp41-core/src/ops/stat1/distributions.rs` returns >=4.
    - `awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' hp41-core/src/ops/stat1/distributions.rs | wc -l` returns ≤300 (production-code budget per D-33.5).
    - `cargo test -p hp41-core --lib ops::stat1::distributions::tests | grep -c 'ok\\.' ` returns >=18 (≥6 per primitive × 3 primitives, accounting for edge tests). Allow some slack — confirm `--quiet` output ends with "all N passed" where N >= 18.
    - `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
    - `bash scripts/check-free42-contamination.sh` exits 0.
    - `grep -c 'scipy.special.gammainc\|scipy.special.betainc\|scipy.stats.norm.ppf' hp41-core/src/ops/stat1/distributions.rs` returns >=14 (oracle comment lines aggregated across all three primitives).
  </acceptance_criteria>
  <done>All three primitives ship with full AS 241/239/63 algorithms, ≥6 inline scipy oracle tuples each, edge-case error handling, total production-code ≤300 LOC, ALL tests green. Plan 33-03 can now consume the primitives knowing they are oracle-validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Numerical algorithm ↔ scipy oracle | Every distribution primitive's correctness is bounded by the inline scipy.stats / scipy.special oracle tuples — a typo in a coefficient table or a sign error silently produces wrong answers; the only defense is the oracle test suite. |
| f64 ↔ Decimal round-trip | Distribution primitives operate in f64 (~15.9 digits) but the outer Op layer (Plan 33-03) wraps with HpNum (28-digit). Loss of precision at the boundary is acceptable for HP-41's 10-digit display but must not exceed the 1e-9 / 1e-7 tolerance bands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering | norm_cdf_inv_f64 Acklam coefficient table | mitigate | Coefficients copied VERBATIM from the public-domain stackedboxes.org mirror (cited in code comment). 6 oracle tuples spanning central + lower-tail + upper-tail + deep-tail + p=0.5 catch any single-coefficient typo: relative error spikes immediately and the test fails. Per RESEARCH.md Pitfall 7: "any tuple failing > 1e-7 → suspect a typo in the coefficient table." |
| T-33-numerical-precision | Tampering | gamma_regularized_f64 gser/gcf split | mitigate | Algorithm selection at `x < a + 1` follows Numerical Recipes 3e §6.2. Boundary tuple (s=3, x=4.0) explicitly tests the split continuity. Iteration cap 50 returns Err on non-convergence — silent infinite loop impossible. |
| T-33-numerical-precision | Tampering | beta_regularized_f64 symmetric boundary swap | mitigate | Swap condition `x < (a+1)/(a+b+2)` follows AS 63 / NR 3e §6.4. Cross-boundary tuple (a=3, b=4, x=0.6) tests the swap path; symmetric tuples (a=2,b=2,x=0.5; a=10,b=10,x=0.5) test the unswapped path exactly. |
| T-33-free42-contamination | Tampering | hp41-core/src/ops/stat1/distributions.rs | mitigate | DISCLAIM_LINE substring present in the file header (Task 1); algorithm citations point to Acklam / NR 3e / AS 239 / AS 63 / stackedboxes — NOT Free42 / core_math2.cc. `bash scripts/check-free42-contamination.sh` runs in every task acceptance and exits 0. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/ | accept (no math1/ edits in this plan) | This plan ships only in `stat1/` directory; no math1/ files touched. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1::distributions::tests` exits 0; output reports >=18 tests passing across the three primitives.
- `bash scripts/check-free42-contamination.sh` exits 0.
- File-level audit: production-code LOC (above `#[cfg(test)]` mod tests block) ≤ 300 per D-33.5.
- No `Op` variants added in this plan (`grep -c 'Op::Sigma' hp41-core/src/ops/mod.rs` returns 0 for any new Sigma* variant; the Plan 33-01 `Op::Stat1Stub` remains the sole stub).
</verification>

<success_criteria>
The three hand-coded distribution primitives are oracle-validated, dependency-free (no new runtime crates added; `statrs` remains rejected), and ready for consumption by Plan 33-03 (ΣNORMD, ΣCHISQD) and Plan 33-07 (ΣPTST, ΣTSTAT). SPEC.md Req. 33 acceptance contract satisfied: "≥ 6 inline-constant `(input, scipy_expected, tolerance)` oracle tuples in `#[cfg(test)] mod tests`; ALL oracle tests pass BEFORE Plan 33-03 ΣNORMD/ΣCHISQD Op code lands."
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-02-SUMMARY.md` when done summarizing: the final oracle tuple count per primitive, the algorithm sources cited (Acklam URL, AS 239 / AS 63 / NR 3e citations), the total production-code LOC, any test that required a tolerance bump above the SPEC.md default and why.
</output>
