---
phase: 33-hp41-core-xrom-activation-distribution-primitives-all-stat-1
plan: 03
plan_id: 33-03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - hp41-core/src/ops/stat1/normd.rs
  - hp41-core/src/ops/stat1/chisqd.rs
  - hp41-core/src/ops/stat1/modal.rs
  - hp41-core/src/ops/stat1/distributions.rs
  - hp41-core/src/ops/stat1/mod.rs
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/math1/xrom.rs
  - hp41-core/src/error.rs
  - hp41-core/tests/stat1_cancellation.rs
autonomous: true
requirements:
  - STAT-DST-01
  - STAT-DST-02
  - STAT-DST-03
  - STAT-DST-04
  - STAT-DST-05
  - STAT-DST-07

must_haves:
  truths:
    - "XEQ \"ΣNORMD\" with x=1.96 in CDF mode returns Q ≈ 0.0250 within 1e-9"
    - "XEQ \"ΣNORMD\" with x=0 in PDF mode returns φ(0) ≈ 0.3989 within 1e-9"
    - "XEQ \"ΣNORMD\" with p=0.025 in inverse mode returns x ≈ -1.9600 within 1e-7"
    - "XEQ \"ΣCHISQD\" with ν=3, x=7.815 returns P ≈ 0.9500 within 1e-7"
    - "Setting cancel_requested=true before ΣNORMD inverse returns Err(HpError::Cancelled) within < 1 iteration"
    - "Iterative paths cap at 50 iterations and return Err(HpError::ConvergenceFailed) on overflow"
    - "Convergence tolerance is display-mode-tied: 10^(-FIX_decimals − 1), fallback 1e-10 when not FIX"
  artifacts:
    - path: "hp41-core/src/ops/stat1/normd.rs"
      provides: "ΣNORMD 3-mode dispatcher (CDF / PDF / inverse) consuming distributions::norm_cdf_inv_f64"
      contains: "pub fn op_sigma_normd_workflow"
    - path: "hp41-core/src/ops/stat1/chisqd.rs"
      provides: "ΣCHISQD ν-prompt + PDF/CDF dispatcher consuming distributions::gamma_regularized_f64"
      contains: "pub fn op_sigma_chisqd_workflow"
    - path: "hp41-core/src/ops/stat1/distributions.rs"
      provides: "quantile_threshold(mode) helper + QUANTILE_MAX_ITERS const"
      contains: "pub fn quantile_threshold"
    - path: "hp41-core/tests/stat1_cancellation.rs"
      provides: "Integration test asserting Err(HpError::Cancelled) within < 1 iter"
  key_links:
    - from: "hp41-core/src/ops/stat1/normd.rs"
      to: "hp41-core/src/ops/stat1/distributions.rs::norm_cdf_inv_f64"
      via: "iterative inverse path"
      pattern: "norm_cdf_inv_f64"
    - from: "hp41-core/src/ops/stat1/chisqd.rs"
      to: "hp41-core/src/ops/stat1/distributions.rs::gamma_regularized_f64"
      via: "CDF iterative path"
      pattern: "gamma_regularized_f64"
    - from: "hp41-core/src/ops/stat1/modal.rs"
      to: "ModalProgram::Stat1"
      via: "Stat1Step::NormdModeChoice + Stat1Step::ChisqdNuPrompt variants"
      pattern: "Stat1Step"
---

<objective>
Wave 2 — implement ΣNORMD (3-mode dispatcher: CDF, PDF, inverse) and ΣCHISQD (ν-prompt + PDF/CDF) by consuming the Plan 33-02 distribution primitives. Adds the first two real `Op` variants — `SigmaNormdWorkflow` and `SigmaChisqdWorkflow` — plus per-mode sub-Ops as decided in implementation. Lands real Stat1Step variants in the modal enum (NormdModeChoice, ChisqdNuPrompt) replacing the Plan-33-01 placeholder; adds HpError::Cancelled and HpError::ConvergenceFailed variants if not already present; implements quantile_threshold helper + 50-iter cap + per-iter cancel_requested check.

Purpose: ΣNORMD and ΣCHISQD are the highest-impact user-facing Ops in v3.1 (every statistical test ultimately calls one). They also exercise every cross-cutting concern: modal-prompt routing through the new ModalProgram::Stat1 variant; iterative-quantile loops with display-mode-tied tolerance; cancellation via `cancel_requested`; bridging f64 primitives (Plan 33-02) back to HpNum stack values.

Output: hp41-core compiles green with 2+ new real `Sigma*Workflow` Op variants; xrom.rs STAT_1.ops entries for ΣNORMD and ΣCHISQD point to the real variants (Plan 33-01 stubs replaced for these two); stat1_cancellation integration test passes; oracle tests for the 5 Op-level tuples (RESEARCH.md Validation Table rows 14–18) all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-CONTEXT.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-01-SUMMARY.md
@.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Distribution primitives shipped by Plan 33-02 — public surface. -->

From hp41-core/src/ops/stat1/distributions.rs (post-Plan-33-02):
- `pub fn norm_cdf_inv_f64(p: f64) -> Result<f64, HpError>` — closed-form Acklam
- `pub fn gamma_regularized_f64(s: f64, x: f64) -> Result<f64, HpError>` — AS 239 series + CF
- `pub fn beta_regularized_f64(a: f64, b: f64, x: f64) -> Result<f64, HpError>` — not used by this plan, reserved for Plan 33-07
- Private `fn ln_gamma(a: f64) -> Result<f64, HpError>` — reusable for ΣCHISQD PDF

From rust_decimal::MathematicalOps (workspace-pinned 1.42):
- `Decimal::norm_cdf(self) -> Decimal` — closed-form Φ(x), no f64 bridge needed
- `Decimal::norm_pdf(self) -> Decimal` — closed-form φ(x), no f64 bridge needed
- `Decimal::erf(self) -> Decimal` — for any erf-based closed-form bridges
- `Decimal::checked_ln`, `Decimal::checked_exp`, `Decimal::sqrt`, `Decimal::checked_powd` — for ΣCHISQD PDF assembly

From hp41-core/src/ops/math1/integ.rs (display-mode-tied tolerance precedent, line 104–111):
- `pub fn integ_threshold(mode: DisplayMode) -> f64 { 5.0_f64 * 10.0_f64.powi(-(decimals + 1)) }`
- Stat 1 analog: `quantile_threshold(mode)` differs by factor 5 per SPEC.md Req. 34 — uses `10^(-decimals - 1)` directly, fallback 1e-10 for non-FIX.

From hp41-core/src/ops/math1/integ.rs (cancel_requested loop pattern):
- `use std::sync::atomic::Ordering;` + `if state.cancel_requested.load(Ordering::Relaxed) { return Err(HpError::Cancelled); }` inside every iteration body.

From hp41-core/src/ops/stat1/modal.rs (post-Plan-33-01):
- `pub enum Stat1Step { Placeholder }` (or empty per Plan 33-01 strategy) — Plan 33-03 REPLACES Placeholder with real variants.

From hp41-core/src/ops/math1/poly.rs (modal opener template, lines ~80–90):
- `state.modal_program = Some(ModalProgram::Poly(PolyInputStep::DegreePrompt));`
- `state.modal_prompt = Some("DEGREE=?".to_string());`
- `state.print_buffer.push("DEGREE=?".to_string());`

From hp41-core/src/state.rs:
- `pub display_mode: DisplayMode` (existing field used by integ_threshold)
- `pub cancel_requested: std::sync::Arc<std::sync::atomic::AtomicBool>` (existing field; used inside iterative loops)
- `pub stack.x, .y, .z, .t: HpNum` (standard HP-41 4-level stack)
- `pub print_buffer: Vec<String>` (for "MODE?" / "ν=?" prompts)

From hp41-core/src/ops/math1/xrom.rs (post-Plan-33-01):
- `STAT_1.ops` entries for "ΣNORMD" and "ΣCHISQD" currently point to `Op::Stat1Stub`. This plan replaces these two with `Op::SigmaNormdWorkflow` and `Op::SigmaChisqdWorkflow`.
- `stat1_resolve` match arms for these two mnemonics similarly switched.

From SPEC.md §"Stat 1 Pac Mnemonics" (line 49–82, table rows for ΣNORMD and ΣCHISQD):
- ΣNORMD → `Op::SigmaNormdWorkflow` (Plan 33-03)
- ΣCHISQD → `Op::SigmaChisqdWorkflow` (Plan 33-03)

From PATTERNS.md §"hp41-core/src/ops/stat1/normd.rs" + §"hp41-core/src/ops/stat1/chisqd.rs" (line 311–401):
- Modal opener body + quantile_threshold function + cancel-loop body all given verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HpError::Cancelled + HpError::ConvergenceFailed (if missing); add quantile_threshold + QUANTILE_MAX_ITERS to distributions.rs</name>
  <files>hp41-core/src/error.rs, hp41-core/src/ops/stat1/distributions.rs</files>
  <read_first>
    - hp41-core/src/error.rs (FULL FILE — confirm exact HpError variants present; Cancelled may already exist for Math Pac I solvers per RESEARCH.md line 1036–1040 — verify rather than assume)
    - hp41-core/src/ops/math1/integ.rs (lines 100–125 — `integ_threshold` body + DisplayMode pattern match)
    - hp41-core/src/ops/stat1/distributions.rs (post-Plan-33-02; extend with quantile_threshold + const)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Iterative-quantile convergence + cancellation" Req. 34 (line 238–245)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"quantile_threshold" (line 348–360 — verbatim function body)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 34 (line 238–245)
  </read_first>
  <action>
    1. Open `hp41-core/src/error.rs`. If `HpError::Cancelled` is NOT present, add it as a new variant. If `HpError::ConvergenceFailed` is NOT present, add it. For each new variant: doc-comment cites SPEC.md Req. 34 / D-33.5 / Pitfall 11. Adding error variants ripples through any exhaustive `match` on HpError — verify with `cargo check -p hp41-core` after the change; fix any missing arms in callers by adding sensible default behavior (likely just a re-raise or a panic-message arm in test-only code; production code that already matches HpError likely uses `_ =>` since HpError is `#[non_exhaustive]` per RESEARCH.md — verify).

    2. Extend `hp41-core/src/ops/stat1/distributions.rs` (post-Plan-33-02) with:
       ```
       pub const QUANTILE_MAX_ITERS: u32 = 50;
       pub fn quantile_threshold(mode: DisplayMode) -> f64 { ... }
       ```
       Body per PATTERNS.md line 350–356: extract `decimals` from `DisplayMode::Fix(n)` / `Sci(n)` / `Eng(n)`; return `10.0_f64.powi(-(decimals as i32 + 1))`. SPEC.md Req. 34 specifies "fallback `1e-10` when display mode is not FIX" — interpret as: for `Sci` and `Eng` modes, return `1e-10` directly; for `Fix(n)` return `10^(-(n+1))`. Doc-comment cites SPEC.md Req. 34, names the deviation from `integ_threshold` (factor of 5 dropped), and warns that this is the quantile-specific threshold (NOT to be conflated with integration threshold).

    3. Confirm `quantile_threshold(DisplayMode::Fix(4))` returns `1e-5`, `quantile_threshold(DisplayMode::Fix(6))` returns `1e-7`, and `quantile_threshold(DisplayMode::Sci(4))` returns `1e-10` via two new oracle tests in the same `#[cfg(test)] mod tests` block.

    4. If `HpError` variants were added: re-run `cargo test -p hp41-core --lib` and fix any exhaustive-match breakage in callers (RESEARCH.md line 904 + 1036 hint that integ.rs already uses these variants — likely no breakage).
  </action>
  <verify>
    <automated>cargo check -p hp41-core && cargo test -p hp41-core --lib ops::stat1::distributions::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Cancelled' hp41-core/src/error.rs` returns >=1.
    - `grep -c 'ConvergenceFailed' hp41-core/src/error.rs` returns >=1.
    - `grep -c 'pub const QUANTILE_MAX_ITERS' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `grep -c 'pub fn quantile_threshold' hp41-core/src/ops/stat1/distributions.rs` returns 1.
    - `cargo test -p hp41-core --lib ops::stat1::distributions::tests::quantile_threshold` exits 0 (the two new oracle tests for Fix(4)/Fix(6)/Sci(4) values).
    - `cargo check -p hp41-core` exits 0 (any new HpError variants do not break existing exhaustive matches; if they do, this task fixes the breakage).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>HpError has Cancelled + ConvergenceFailed variants (added or pre-existing); `quantile_threshold` and `QUANTILE_MAX_ITERS` are public exports from `distributions.rs` with oracle tests proving the expected values for Fix(n) / Sci(n) / Eng(n) modes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Stat1Step::Placeholder with real variants (NormdModeChoice, ChisqdNuPrompt) + flesh out modal dispatch</name>
  <files>hp41-core/src/ops/stat1/modal.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/modal.rs (post-Plan-33-01; this task swaps Placeholder for the two real variants used by ΣNORMD + ΣCHISQD)
    - hp41-core/src/ops/math1/modal.rs (lines 169–217 — PolyInputStep variant + current_prompt + into_modal helper, the structural analog)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 31, 32 (line 214–227 — ΣNORMD modes + ΣCHISQD ν-prompt)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/normd.rs" + §"hp41-core/src/ops/stat1/chisqd.rs" (line 311–401 — modal opener templates)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Pattern 3: Modal-Prompt Workflow" (line 314–331 — ChisqdStep::PromptForNu example body)
  </read_first>
  <action>
    Replace the placeholder `pub enum Stat1Step { Placeholder }` in `hp41-core/src/ops/stat1/modal.rs` with the real variants used by THIS plan (Plan 33-08 will add further variants — PolypDegreePrompt, SeedPrompt):

    ```
    #[derive(Debug, Clone, PartialEq)]
    pub enum Stat1Step {
        /// ΣNORMD — awaiting mode selection (CDF=E key, PDF=C key, inverse=A key per Plan 33-03 OM read).
        NormdModeChoice,
        /// ΣCHISQD — awaiting degrees-of-freedom ν entry via ALPHA-prompt "ν=?".
        ChisqdNuPrompt,
        /// ΣCHISQD — ν is captured (stored in stack T register or print buffer per OM); awaiting mode selection (PDF / CDF).
        ChisqdModeChoice,
        // Plan 33-08 ADDS: PolypDegreePrompt(u8), SeedPrompt
    }
    ```

    Update the three dispatch functions to exhaustively match the new variants:
    - `submit_step(state, step)`: for `NormdModeChoice` → consume stack X as the mode index, transition to direct evaluation (call `op_sigma_normd_eval_cdf` / `_pdf` / `_inverse` per mode value); for `ChisqdNuPrompt` → consume stack X as ν integer, store in transient state (use existing stack register T or push to print_buffer per OM 00041-90030 ΣCHISQD section read in this plan), transition to `ChisqdModeChoice`; for `ChisqdModeChoice` → consume stack X as mode index, call `op_sigma_chisqd_eval_pdf` / `_cdf`. Plan 33-08 will extend submit_step with the new variants.
    - `current_prompt(step)`: return `Some("ΣNORMD MODE?")` / `Some("\u{03BD}=?")` / `Some("ΣCHISQD MODE?")` per variant. Use `\u{03BD}` (Greek small nu) for the ν=? string.
    - `requires_alpha_label(step)`: return `false` for all three variants (no FUNCTION NAME? prompts in ΣNORMD / ΣCHISQD; Plan 33-08 keeps SeedPrompt at `false` too since SEED takes a numeric value, not an alpha label).

    Per SPEC.md Req. 31 "OM override": the literal "MODE?" string is NOT locked — read OM 00041-90030 §ΣNORMD to confirm the actual prompt convention (tentative per RESEARCH.md: `[E]` = CDF, `[C]` = PDF, `[A]` = inverse). If OM specifies a different prompt-and-key convention, substitute it. The SPEC lock is "deterministic prompt-and-submit workflow reusing existing modal infrastructure with no new transient CalcState fields."
  </action>
  <verify>
    <automated>cargo check -p hp41-core</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NormdModeChoice' hp41-core/src/ops/stat1/modal.rs` returns >=2 (variant + dispatch arms).
    - `grep -c 'ChisqdNuPrompt' hp41-core/src/ops/stat1/modal.rs` returns >=2.
    - `grep -c 'ChisqdModeChoice' hp41-core/src/ops/stat1/modal.rs` returns >=2.
    - `grep -c 'Placeholder' hp41-core/src/ops/stat1/modal.rs` returns 0 (placeholder replaced).
    - `grep -cE 'pub fn submit_step|pub fn current_prompt|pub fn requires_alpha_label' hp41-core/src/ops/stat1/modal.rs` returns 3.
    - `cargo check -p hp41-core` exits 0 (exhaustive match on Stat1Step works for all three variants in all three dispatch functions).
    - `cargo clippy -p hp41-core -- -D warnings` exits 0 (any unused-variant warnings on PolypDegreePrompt etc. would only occur if Task 3/4 below stub them — Plan 33-08 introduces those, NOT here).
  </acceptance_criteria>
  <done>The Plan 33-01 `Placeholder` variant is gone; three real variants are exhaustively dispatched; ΣCHISQD ChisqdNuPrompt is a two-step workflow (ν entry → mode choice) staged for Task 4.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ΣNORMD (CDF / PDF / inverse) — replace Op::Stat1Stub for ΣNORMD with Op::SigmaNormdWorkflow</name>
  <files>hp41-core/src/ops/stat1/normd.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs, hp41-core/src/ops/stat1/mod.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/distributions.rs (post-Task-1; norm_cdf_inv_f64 + quantile_threshold + QUANTILE_MAX_ITERS available)
    - hp41-core/src/ops/math1/integ.rs (lines 28–60 — `Ordering::Relaxed` cancel-check loop pattern from v3.0 SOLVE/INTG)
    - hp41-core/src/ops/math1/poly.rs (lines 80–90 — modal opener template `op_poly_workflow`)
    - hp41-core/src/ops/stat1/mod.rs (verify pub mod normd; declaration; add if missing)
    - hp41-core/src/ops/math1/xrom.rs (the line for `("\u{03A3}NORMD", Op::Stat1Stub)` in STAT_1.ops and the parallel `stat1_resolve` arm — both swap to Op::SigmaNormdWorkflow)
    - hp41-core/src/num.rs (HpNum::from_f64 / to_f64 / checked_ln signatures for the f64 ↔ HpNum bridge)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/normd.rs" (line 311–384 — full normd Op + modal + cancel-loop template)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 31 (line 214–221 — three-mode acceptance contract)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Validation Table" rows 14, 15, 16 (line 486–488 — Op-level oracle tuples)
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/normd.rs`. Disclaim header (3 lines) per PATTERNS.md byte-for-byte OM template.

    2. Add `pub mod normd;` to `hp41-core/src/ops/stat1/mod.rs` if not present.

    3. Add a new variant `Op::SigmaNormdWorkflow` to the `Op` enum in `hp41-core/src/ops/mod.rs`, immediately after the Plan-33-01 `Op::Stat1Stub` placeholder block. Doc-comment cites SPEC.md Req. 31 and Plan 33-03; LiftEffect noted (modal openers are typically Neutral until evaluation completes — but state-of-the-art for Math Pac I POLY shows Neutral on open + Enable on result push, so follow that convention per CONTEXT.md Claude's Discretion).

    4. Implement in `normd.rs`:
       - `pub fn op_sigma_normd_workflow(state: &mut CalcState) -> Result<(), HpError>` — modal opener. Sets `state.modal_program = Some(ModalProgram::Stat1(Stat1Step::NormdModeChoice))`; sets `state.modal_prompt = Some("\u{03A3}NORMD MODE?".to_string())`; pushes the same string to `state.print_buffer`. Returns `Ok(())`. Pattern from PATTERNS.md line 329–335.
       - `pub fn op_sigma_normd_eval_cdf(state: &mut CalcState) -> Result<(), HpError>` — closed-form Q(x) = 1 - Φ(x). Reads X register, calls `state.stack.x.checked_norm_cdf()` (use whatever HpNum wrapper exists for rust_decimal::MathematicalOps::norm_cdf — if no wrapper, add `pub fn checked_norm_cdf(&self) -> Result<HpNum, HpError>` in `hp41-core/src/num.rs` following the `checked_ln` pattern). Then computes Q(x) = `HpNum::one() - cdf_value`. Pushes Q to stack X; applies `LiftEffect::Enable`. No iteration; no cancel check; no display-mode tolerance.
       - `pub fn op_sigma_normd_eval_pdf(state: &mut CalcState) -> Result<(), HpError>` — closed-form φ(x) via `Decimal::norm_pdf`. Same pattern as CDF.
       - `pub fn op_sigma_normd_eval_inverse(state: &mut CalcState) -> Result<(), HpError>` — iterative path. Reads X = p (probability). Converts to f64. Calls `crate::ops::stat1::distributions::norm_cdf_inv_f64(p_f64)` for the closed-form starting point. THEN runs a Newton + bisection refinement loop wrapped with cancel-check + iter-cap per PATTERNS.md line 372–380:
         ```
         use std::sync::atomic::Ordering;
         for _ in 0..crate::ops::stat1::distributions::QUANTILE_MAX_ITERS {
             if state.cancel_requested.load(Ordering::Relaxed) {
                 return Err(HpError::Cancelled);
             }
             // refine x using Φ(x) - p as residual, derivative φ(x);
             // converged when |residual| < quantile_threshold(state.display_mode)
             if residual.abs() < quantile_threshold(state.display_mode) {
                 // ... convert f64 back to HpNum, push to stack X, apply LiftEffect::Enable, return Ok(())
                 return Ok(());
             }
         }
         Err(HpError::ConvergenceFailed)
         ```
         If Acklam's closed-form alone already satisfies the convergence test on iter 0, the loop returns immediately — that is correct behavior. The loop exists for tail-edge cases where Acklam is just outside the 1e-7 band; Newton refines.

    5. In `hp41-core/src/ops/mod.rs::dispatch()` exhaustive match: REPLACE the existing `Op::Stat1Stub => crate::ops::stat1::op_stat1_stub(state)` arm with separate arms — keep `Op::Stat1Stub => ...` (still needed for other 24 stub entries) AND add `Op::SigmaNormdWorkflow => crate::ops::stat1::normd::op_sigma_normd_workflow(state),`.

    6. Same edit in `hp41-core/src/ops/program.rs::execute_op()` exhaustive match.

    7. In `hp41-core/src/ops/math1/xrom.rs`: replace `("\u{03A3}NORMD", Op::Stat1Stub)` in `STAT_1.ops` with `("\u{03A3}NORMD", Op::SigmaNormdWorkflow)`; same swap in `stat1_resolve` match arm.

    8. Modal submit hook in `stat1/modal.rs::submit_step` for `Stat1Step::NormdModeChoice`: read mode from `state.stack.x` (e.g. 1 = CDF, 2 = PDF, 3 = inverse — exact mapping per OM read in this plan, with tentative E/C/A keymap from RESEARCH.md). Call the appropriate eval function, clear `state.modal_program = None` and `state.modal_prompt = None`. Return `Ok(())` on success or the error from the eval call.

    9. Add inline `#[cfg(test)] mod tests` in `normd.rs` with the 3 Op-level oracle tuples (RESEARCH.md rows 14–16):
       - Q(1.96) within 1e-9 of 0.024998
       - φ(0) within 1e-9 of 0.39894228040143270
       - Φ⁻¹(0.025) within 1e-7 of -1.959963984540054
       Plus a behavioral test: a programmatic round-trip `op_sigma_normd_workflow` → `submit_step(NormdModeChoice)` with X=1 → assert CDF computed correctly. Cite scipy.stats commands in comments per D-33.6.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::normd::tests -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/normd.rs` succeeds.
    - `grep -c 'Free42 source consulted only as sanity-check oracle' hp41-core/src/ops/stat1/normd.rs` returns 1.
    - `grep -c 'pub fn op_sigma_normd_workflow' hp41-core/src/ops/stat1/normd.rs` returns 1.
    - `grep -cE 'pub fn op_sigma_normd_eval_(cdf|pdf|inverse)' hp41-core/src/ops/stat1/normd.rs` returns 3.
    - `grep -c 'cancel_requested.load' hp41-core/src/ops/stat1/normd.rs` returns >=1 (cancel-check inside iterative loop).
    - `grep -c 'QUANTILE_MAX_ITERS\|quantile_threshold' hp41-core/src/ops/stat1/normd.rs` returns >=2 (both 50-iter cap and display-mode tolerance used).
    - `grep -c 'norm_cdf_inv_f64' hp41-core/src/ops/stat1/normd.rs` returns >=1 (consumes Plan 33-02 primitive).
    - `grep -c 'Op::SigmaNormdWorkflow' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=4 (enum + 2 dispatch + 1 STAT_1.ops + 1 stat1_resolve, all required).
    - `cargo test -p hp41-core --lib ops::stat1::normd::tests` exits 0.
    - `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (still green after the stub swap — bidirectional consistency preserved).
    - `bash scripts/check-free42-contamination.sh` exits 0.
  </acceptance_criteria>
  <done>ΣNORMD ships with all three modes operational; the 3 Op-level oracle tuples (CDF, PDF, inverse) pass within the documented tolerance bands; the modal-prompt flow round-trips correctly; the Plan 33-01 stub for ΣNORMD is replaced with the real Op variant.</done>
</task>

<task type="auto">
  <name>Task 4: Implement ΣCHISQD (ν-prompt + PDF/CDF) — replace Op::Stat1Stub for ΣCHISQD with Op::SigmaChisqdWorkflow + ship stat1_cancellation integration test</name>
  <files>hp41-core/src/ops/stat1/chisqd.rs, hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/math1/xrom.rs, hp41-core/src/ops/stat1/mod.rs, hp41-core/tests/stat1_cancellation.rs</files>
  <read_first>
    - hp41-core/src/ops/stat1/distributions.rs (post-Plan-33-02 + Task-1; gamma_regularized_f64 + ln_gamma helper available)
    - hp41-core/src/ops/stat1/normd.rs (post-Task-3; reuse the cancel-loop + quantile_threshold pattern verbatim)
    - hp41-core/src/ops/stat1/modal.rs (post-Task-2; ChisqdNuPrompt + ChisqdModeChoice variants in place)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-PATTERNS.md §"hp41-core/src/ops/stat1/chisqd.rs" (line 386–401 — full ΣCHISQD modal + eval template)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC.md Req. 32 (line 222–227 — ΣCHISQD acceptance contract + OM override clause)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-RESEARCH.md §"Validation Table" rows 17, 18 (line 489–490 — Op-level oracle tuples for ΣCHISQD)
    - HP-41C Stat 1 Pac OM 00041-90030 §ΣCHISQD subsection — confirm ν-entry convention (read in this task per SPEC.md OM override clause)
  </read_first>
  <action>
    1. Create `hp41-core/src/ops/stat1/chisqd.rs`. Disclaim header (3 lines) per PATTERNS.md.

    2. Add `pub mod chisqd;` to `hp41-core/src/ops/stat1/mod.rs` if not present.

    3. Add `Op::SigmaChisqdWorkflow` to the `Op` enum (after Op::SigmaNormdWorkflow). Doc-comment cites SPEC.md Req. 32 and the ν-prompt workflow.

    4. Implement in `chisqd.rs`:
       - `pub fn op_sigma_chisqd_workflow(state: &mut CalcState) -> Result<(), HpError>` — modal opener; sets `ModalProgram::Stat1(Stat1Step::ChisqdNuPrompt)` + prompt `"\u{03BD}=?"` per PATTERNS.md line 392–397. Push to print_buffer.
       - `pub fn op_sigma_chisqd_eval_pdf(state: &mut CalcState, nu: u32) -> Result<(), HpError>` — closed-form PDF: `f(x; ν) = (x^(ν/2-1) · e^(-x/2)) / (2^(ν/2) · Γ(ν/2))`. Compute via `rust_decimal::MathematicalOps`: `Decimal::checked_powd`, `Decimal::checked_exp`, plus `ln_gamma` from `distributions::ln_gamma`. Operate in HpNum where possible; bridge to f64 only for `ln_gamma`. Closed-form (no iteration), tolerance 1e-9 per SPEC.md Req. 46.
       - `pub fn op_sigma_chisqd_eval_cdf(state: &mut CalcState, nu: u32) -> Result<(), HpError>` — iterative path: P(x; ν) = `gamma_regularized_f64(nu/2.0, x/2.0)` (per PATTERNS.md line 401). Wrap with cancel-check + quantile_threshold check + 50-iter cap pattern from normd.rs Task 3 (the cancel-check applies even though gamma_regularized_f64 is fast — per Pitfall 11, every iterative Stat 1 quantile path checks cancel inside the outer loop so the GUI request_cancel hook works regardless of which primitive is currently running). For ΣCHISQD CDF, the "iteration" is effectively one call to gamma_regularized_f64; the wrapping loop is symbolic but the cancel-check before the call is real. Cite SPEC.md Req. 34 in doc-comment.

    5. Modal submit hooks in `stat1/modal.rs::submit_step`:
       - `Stat1Step::ChisqdNuPrompt` → consume `state.stack.x` as ν (truncate to integer, error if ν <= 0); store ν transiently. Strategy for ν storage between ChisqdNuPrompt and ChisqdModeChoice WITHOUT adding a new CalcState field: per D-33.5 constraint, store ν in stack T register (the deepest stack slot, untouched by typical user interaction during the modal flow) OR temporarily in `state.print_buffer` as a parseable string. Choose the cleaner option after reviewing how Math Pac I POLY stores its in-flight degree value (read `hp41-core/src/ops/math1/poly.rs` for the precedent). After ν captured, transition `state.modal_program = Some(ModalProgram::Stat1(Stat1Step::ChisqdModeChoice))`, prompt `"\u{03A3}CHISQD MODE?"`.
       - `Stat1Step::ChisqdModeChoice` → consume mode index from X; retrieve ν from wherever Task-4 staged it; call `op_sigma_chisqd_eval_pdf(state, nu)` or `op_sigma_chisqd_eval_cdf(state, nu)`; clear `modal_program` and `modal_prompt`.

    6. dispatch + execute_op + xrom.rs + stat1_resolve updates: identical structure to Task 3 (replace Op::Stat1Stub for ΣCHISQD with Op::SigmaChisqdWorkflow at all 4 sites).

    7. Add inline `#[cfg(test)] mod tests` in `chisqd.rs` with 2 Op-level oracle tuples (RESEARCH.md rows 17, 18):
       - CDF P(7.815; ν=3) within 1e-7 of 0.9499718909781536
       - PDF f(3; ν=3) within 1e-9 of 0.15418032980365303

    8. Create `hp41-core/tests/stat1_cancellation.rs` integration test (Wave 0 deliverable per VALIDATION.md):
       - Single test `cancel_requested_kills_normd_inverse_within_one_iter`: construct CalcState; set `state.cancel_requested.store(true, Ordering::Relaxed)`; push some p=0.025 onto stack X; call `op_sigma_normd_eval_inverse(&mut state)`; assert result is `Err(HpError::Cancelled)`. Per SPEC.md Req. 34 acceptance: must return within < 1 iteration.
       - Second test `display_mode_fix_6_yields_1e_7_threshold`: assert `quantile_threshold(DisplayMode::Fix(6)) == 1e-7`.
       File starts with the byte-for-byte disclaim header (PATTERNS.md template) so contamination guard allow-list works.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib ops::stat1::chisqd::tests -- --quiet && cargo test -p hp41-core --test stat1_cancellation -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hp41-core/src/ops/stat1/chisqd.rs` succeeds.
    - `test -f hp41-core/tests/stat1_cancellation.rs` succeeds.
    - `grep -c 'pub fn op_sigma_chisqd_workflow' hp41-core/src/ops/stat1/chisqd.rs` returns 1.
    - `grep -cE 'pub fn op_sigma_chisqd_eval_(pdf|cdf)' hp41-core/src/ops/stat1/chisqd.rs` returns 2.
    - `grep -c 'gamma_regularized_f64' hp41-core/src/ops/stat1/chisqd.rs` returns >=1.
    - `grep -c 'cancel_requested.load' hp41-core/src/ops/stat1/chisqd.rs hp41-core/src/ops/stat1/normd.rs` returns >=2 (both Ops check cancel before/inside iterative paths).
    - `grep -c 'Op::SigmaChisqdWorkflow' hp41-core/src/ops/mod.rs hp41-core/src/ops/program.rs hp41-core/src/ops/math1/xrom.rs` returns >=4.
    - `cargo test -p hp41-core --lib ops::stat1::chisqd::tests` exits 0 (CDF P(7.815; ν=3) ≈ 0.9500 within 1e-7; PDF f(3; ν=3) ≈ 0.1542 within 1e-9).
    - `cargo test -p hp41-core --test stat1_cancellation::cancel_requested_kills_normd_inverse_within_one_iter` exits 0.
    - `cargo test -p hp41-core --test stat1_cancellation::display_mode_fix_6_yields_1e_7_threshold` exits 0.
    - `bash scripts/check-free42-contamination.sh` exits 0.
    - `cargo clippy -p hp41-core -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>ΣCHISQD ships with ν-prompt + mode-choice + PDF + CDF; cancellation gate proven via integration test; PDF oracle within 1e-9, CDF oracle within 1e-7; Plan 33-01 stubs for ΣCHISQD replaced with the real Op; 5 SPEC.md acceptance criteria covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stack ↔ ν value during ChisqdNuPrompt → ChisqdModeChoice transition | The ν value is captured by submit_step but no transient CalcState field is permitted per D-33.5. Storage choice (stack T register vs print_buffer parsing) carries a state-visibility risk: if the user pushes a new value onto X between the two prompts, intermediate stack state could overwrite ν. |
| f64 ↔ HpNum at distribution-primitive boundary | Distribution primitives return f64; Op layer wraps to HpNum. Loss of precision capped at f64 ~15.9 digits is well above the HP-41 10-digit display, but the 1e-7 iterative tolerance band must be respected. |
| cancel_requested ↔ iterative loop | The GUI `request_cancel` Tauri command (Phase 31 v3.0) sets the AtomicBool; the iterative loop reads it via Ordering::Relaxed per iteration. If a Stat 1 Op forgets to check, the GUI cancel button silently fails — Pitfall 11. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-numerical-precision | Tampering | ΣNORMD CDF/PDF/inverse | mitigate | Three Op-level oracle tuples (rows 14–16 of RESEARCH.md Validation Table) embedded as `#[cfg(test)] mod tests`. Closed-form CDF/PDF use rust_decimal's `norm_cdf` / `norm_pdf` (industry-tested, no in-house implementation). Inverse path uses Plan 33-02 Acklam primitive + optional Newton refinement; the same oracle from Plan 33-02's Tuple 1 (p=0.025 → -1.959964) is re-asserted at the Op level to catch any Op-layer wrapping bug. |
| T-33-numerical-precision | Tampering | ΣCHISQD PDF/CDF | mitigate | Two Op-level oracle tuples (rows 17–18). PDF uses HpNum-native math (rust_decimal `checked_powd` / `checked_exp` + ln_gamma helper); CDF reuses Plan 33-02 gamma_regularized_f64 with the same oracle confidence. |
| T-33-state-persistence | Tampering | ChisqdNuPrompt → ChisqdModeChoice ν storage | mitigate | ν stored transiently (stack T register OR print_buffer string per OM read in Task 4); no new CalcState field per D-33.5. The transient-state choice is locally scoped — Plan 33-08 will use the same pattern for SEED. Document the choice in Task 4's SUMMARY entry so Plan 33-08 picks the same strategy. |
| T-33-numerical-precision | Denial of Service (silent infinite loop) | normd inverse Newton refinement | mitigate | 50-iter cap returns `Err(HpError::ConvergenceFailed)`; per-iter `cancel_requested` check returns `Err(HpError::Cancelled)`. Both proven by `stat1_cancellation.rs` integration test. |
| T-33-freeze-violation-creep | Tampering | hp41-core/src/ops/math1/xrom.rs | mitigate | This plan edits `math1/xrom.rs` for the second time (Plan 33-01 added STAT_1 const; this plan swaps two `Op::Stat1Stub` references for `Op::SigmaNormdWorkflow` and `Op::SigmaChisqdWorkflow`). All edits are within the D-33.3 carve-out; `git diff develop -- hp41-core/src/ops/math1/` still touches only `xrom.rs` and `modal.rs`. |
| T-33-free42-contamination | Tampering | hp41-core/src/ops/stat1/normd.rs + chisqd.rs + stat1_cancellation.rs | mitigate | All new files include the byte-for-byte DISCLAIM_LINE; algorithm citations point to rust_decimal / Plan 33-02 primitives / Numerical Recipes (already cited via Plan 33-02). `bash scripts/check-free42-contamination.sh` exits 0 in every task acceptance. |
</threat_model>

<verification>
- `cargo check -p hp41-core` exits 0.
- `cargo clippy -p hp41-core -- -D warnings` exits 0.
- `cargo test -p hp41-core --lib ops::stat1` exits 0 (covers distributions::tests, normd::tests, chisqd::tests, and the Plan-33-01 xrom/state tests).
- `cargo test -p hp41-core --test stat1_cancellation` exits 0.
- `cargo test -p hp41-core --lib ops::math1::xrom::tests::stat1_ops_mnemonics_resolve_consistently` exits 0 (slice ↔ resolver consistency preserved after swapping 2 of 26 stubs for real Ops).
- `bash scripts/check-free42-contamination.sh` exits 0.
- 4-way invariant: `cargo check -p hp41-cli` still fails on missing op_display_name arms for Op::SigmaNormdWorkflow and Op::SigmaChisqdWorkflow — EXPECTED (Phase 34 fixes).
- `git diff develop -- hp41-core/src/ops/math1/ | grep '+++ b/' | sort -u` lists ONLY `modal.rs` and `xrom.rs` (the two D-33.3/D-33.3b carve-outs).
</verification>

<success_criteria>
SPEC.md Acceptance criteria for Phase 33 Reqs 31, 32, 33 (Op-level), 34 (cancellation + threshold) are GREEN. A user (in tests) can call `op_sigma_normd_workflow(state)` → submit mode → get CDF Q(1.96)=0.0250; can call `op_sigma_chisqd_workflow(state)` → submit ν=3 → submit mode → get CDF P(7.815)=0.9500. A long-running ΣNORMD inverse can be cancelled via `cancel_requested`. The plan adds 2 Sigma* Op variants out of the eventual ~24; Plan 33-04..33-08 continue replacing Op::Stat1Stub references.
</success_criteria>

<output>
Create `.planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-03-SUMMARY.md` when done summarizing: the OM-confirmed key-to-mode mapping for ΣNORMD (E/C/A or alternative); the ν storage strategy chosen between ChisqdNuPrompt and ChisqdModeChoice (so Plan 33-08 uses the same for SEED); the final iter-cap behavior (whether Acklam needed Halley refinement); the count of STAT_1.ops entries swapped from Op::Stat1Stub to real variants (should be 2 of 26 after this plan).
</output>
