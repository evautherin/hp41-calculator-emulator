---
phase: 55-touch-ui-adaptation
plan: 06
type: execute
wave: 6
depends_on: ["55-01", "55-02", "55-03", "55-04", "55-05"]
files_modified: []
autonomous: false
requirements: [TOUCH-01, TOUCH-03, TOUCH-04, TOUCH-05, TOUCH-06, TOUCH-07, TOUCH-08, TOUCH-09, TOUCH-10, TOUCH-11]
user_setup: []

must_haves:
  truths:
    - "On a physical iPhone SE, every one of the 44 keys can be tapped accurately with no accidental adjacent-key activation (TOUCH-01)"
    - "Key taps feel instant — no perceptible 350ms delay and no grey tap-highlight flash, on first tap and rapid successive taps (TOUCH-03)"
    - "The AlphaTouchInput bar stays visible above the iOS software keyboard for both ALPHA mode and a label modal, and characters commit correctly (TOUCH-04)"
    - "Haptics are felt with the correct tier (digit=light, ENTER=medium, SHIFT=heavy) and a distinct error haptic fires on DATA ERROR / NO ROOM (TOUCH-05, TOUCH-08)"
    - "TONE/BEEP audio is audible on the first key use after a cold launch (TOUCH-06)"
    - "SHIFT/ALPHA annunciators and the X/Y/Z/T stack are legible without zooming (TOUCH-07)"
    - "Dragging the calculator body produces no rubber-band overscroll; bottom-sheet inner content scrolls; the stack collapses (TOUCH-11, TOUCH-09, TOUCH-10 on-device confirm)"
  artifacts: []
  key_links: []
---

<objective>
Authoritatively verify the touch behaviors that are observable only on real
hardware (D-55.4), on an iPhone SE — the smallest target. All code is already
implemented and automated-tested in Plans 01–05; this plan runs the final
automated gate, builds + installs the on-device build, and runs the inline human
checkpoints.

Purpose: D-55.4 mandates inline human checkpoints for the criteria a Simulator
cannot prove (44pt fat-finger accuracy, tap delay/flash, haptics felt + tiers +
error pattern, audio on first use, legibility, rubber-band overscroll). This is the
phase's verification gate before /gsd:verify-work.

Output: a green automated gate, a built + installed iOS app on an iPhone SE, and
recorded pass/fail for each on-device checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-touch-ui-adaptation/55-CONTEXT.md
@.planning/phases/55-touch-ui-adaptation/55-VALIDATION.md
@.planning/phases/55-touch-ui-adaptation/55-01-SUMMARY.md
@.planning/phases/55-touch-ui-adaptation/55-02-SUMMARY.md
@.planning/phases/55-touch-ui-adaptation/55-03-SUMMARY.md
@.planning/phases/55-touch-ui-adaptation/55-04-SUMMARY.md
@.planning/phases/55-touch-ui-adaptation/55-05-SUMMARY.md

<interfaces>
<!-- Device-install caveat carried from Phase 53/54 (STATE.md): -->
- Xcode debug ⌘R panics on the missing Tauri dev-server addr file. For standalone installs use the RELEASE `just ios-build` IPA + `xcrun devicectl install/launch`.
- Smoke-check layout/safe-area/overscroll in the Simulator first; authoritative pass is on iPhone SE (D-55.4 / VALIDATION §"Simulator vs Device Decision Criteria").
- Manual checkpoint catalog: 55-VALIDATION.md §"Manual-Only Verifications" (the click-by-click instructions per requirement).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the final automated gate (npm test + iOS cargo check + just gui-ci)</name>
  <read_first>
- .planning/phases/55-touch-ui-adaptation/55-VALIDATION.md §"Sampling Rate" (final pre-verify gate definition)
- .planning/STATE.md §"Accumulated Context" (iOS build env P-iOS-09; cfg(mobile) blind-spot guard)
- CLAUDE.md §"Tech Stack" (just is the sole task runner; cfg(mobile) blind-spot)
  </read_first>
  <action>
Run the full automated gate one final time to confirm everything from Plans 01–05 is
green before the on-device pass. Run each command path-independently using subshells
so directory changes do not chain (never use a chained relative `cd` after a prior
`cd`): the Vitest suite in `hp41-gui`, the iOS-target compile guard in
`hp41-gui/src-tauri` (the cfg(mobile) blind-spot guard, invisible to `just gui-ci`),
and `just gui-ci` for the desktop compile + lint. If ANY of these fail, stop and
surface the error — do not proceed to the build/install task or the on-device
checkpoints.
  </action>
  <verify>
    <automated>(cd hp41-gui && npm test) && (cd hp41-gui/src-tauri && cargo check --target aarch64-apple-ios) && (cd hp41-gui && just gui-ci)</automated>
  </verify>
  <acceptance_criteria>
- `(cd hp41-gui && npm test)` green (full Vitest suite)
- `(cd hp41-gui/src-tauri && cargo check --target aarch64-apple-ios)` clean (cfg(mobile) guard)
- `(cd hp41-gui && just gui-ci)` green (desktop compile + lint)
  </acceptance_criteria>
  <done>All three automated gates green: Vitest suite, iOS-target cargo check, and just gui-ci.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build the iOS release IPA + install on iPhone SE + cold-launch (just ios-build)</name>
  <what-built>The green-gated Plan 01–05 build, packaged as a release IPA and installed on a physical iPhone SE — the on-device build that Tasks 3–5 verify.</what-built>
  <how-to-verify>
This step needs the full Xcode/signing environment, so it is a human-driven build +
install (not the autonomous gate). Run from the project root:
1. Build the release IPA via the established `just ios-build` recipe (release IPA — NOT Xcode debug ⌘R, which panics on the missing dev-server addr file per the 53-04 caveat). Confirm it completes without error.
2. (Optional smoke) Launch the build once in the iOS Simulator to eyeball layout, safe-area insets, and overscroll (approximate only — authoritative pass is on device).
3. Install the release IPA on a physical iPhone SE: `xcrun devicectl device install app <path-to.app/.ipa>` then `xcrun devicectl device process launch <bundle-id>`.
4. Confirm the app COLD-LAUNCHES to the calculator screen on the iPhone SE (kill it first if already running, then launch fresh).
  </how-to-verify>
  <resume-signal>Type "approved" if `just ios-build` produced a release IPA, it installed on the iPhone SE, and it cold-launched to the calculator. Otherwise describe the build/install/launch failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device checkpoint — hit targets, tap feel, audio (TOUCH-01/03/06)</name>
  <what-built>44pt hit-target overlays (Plan 02), touch-action: manipulation + tap-highlight suppression + pointerdown press feedback (Plan 02), and audio-resume on first gesture (Plan 03) — all live on the installed iPhone SE build.</what-built>
  <how-to-verify>
On the iPhone SE running the installed build:
1. TOUCH-01 — Tap every one of the 44 keys in turn (all 5 rows + ON/USER/PRGM/ALPHA + SHIFT + ENTER). Confirm each tap activates the intended key and NEVER an adjacent neighbor (watch the display / annunciators). Pay special attention to the tightly-spaced digit rows.
2. TOUCH-03 — Tap a digit and watch for an immediate press shrink (scale 0.92). Confirm there is NO grey/blue tap-highlight flash and NO perceptible delay before the press visual. Then tap 5+ keys in rapid succession — every tap must feel equally instant (no 350ms lag on first or subsequent taps).
3. TOUCH-06 — Cold-launch the app fresh, then immediately type `9` and run `TONE` (e.g. XEQ TONE 9, or the TONE key path). Confirm a tone is AUDIBLE on this first use (silent switch OFF). Then toggle the hardware silent switch ON and confirm audio mutes (this is correct HP-41 behavior).
  </how-to-verify>
  <resume-signal>Type "approved" if all three pass, or describe which key/behavior failed (e.g. "row 3 key 4 activates neighbor", "tone silent on first use").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device checkpoint — haptics tiers + error haptic, ALPHA/label entry (TOUCH-04/05/08)</name>
  <what-built>Per-key haptics with tiers (light/medium/heavy) + guarded error haptic (Plan 03), and the AlphaTouchInput bar covering ALPHA register AND modal label prompts with visualViewport keyboard tracking (Plan 04) — live on the iPhone SE build.</what-built>
  <how-to-verify>
On the iPhone SE:
1. TOUCH-05/08 (tiers) — Tap a digit (e.g. `5`) and feel a LIGHT tap. Tap ENTER and feel a MEDIUM (stronger) tap. Tap SHIFT and feel a HEAVY (strongest) tap. Confirm the three intensities are distinguishable (digit < ENTER < SHIFT).
2. TOUCH-08 (error) — Force a DATA ERROR (e.g. `0` `1/x`, or SQRT of a negative: `1` CHS SQRT). Confirm a DISTINCT error haptic (double-tap pattern) fires once. Confirm it does NOT keep buzzing while the error stays on screen.
3. TOUCH-04 (ALPHA) — Press ALPHA to enter ALPHA mode. Confirm the AlphaTouchInput bar appears ABOVE the iOS software keyboard (not hidden behind it). Type a few letters; confirm they commit into the ALPHA register. Press Done; confirm ALPHA mode exits.
4. TOUCH-04 (modal label, D-55.2) — Press XEQ (or GTO/LBL) to open a FUNCTION NAME? / label prompt. Confirm the same bar appears above the keyboard with the prompt as its header. Type a label and press Done; confirm the label is accepted (the modal completes — e.g. XEQ runs / GTO jumps).
  </how-to-verify>
  <resume-signal>Type "approved" if all four pass, or describe the failure (e.g. "ENTER and SHIFT feel identical", "label bar hidden behind keyboard", "error haptic buzzes repeatedly").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: On-device checkpoint — legibility, bottom sheets, collapsible stack, overscroll (TOUCH-07/09/10/11)</name>
  <what-built>Annunciator/stack legibility (no size change, Plan 05), print + PRGM pull-up bottom sheets (Plan 05), collapsible stack panel with chevron (Plan 05), and overscroll suppression (Plan 05 / index.css) — live on the iPhone SE build.</what-built>
  <how-to-verify>
On the iPhone SE:
1. TOUCH-07 — Without zooming, read the SHIFT and ALPHA annunciators and the X/Y/Z/T stack register values. Confirm all are legible at arm's length.
2. TOUCH-09 (print sheet) — Run a program or operation that produces print output (or PRX). Confirm a "PRINT LOG" pull-up sheet appears at the bottom; tap its handle/header to expand it and confirm the print lines scroll inside the sheet; tap again to collapse.
3. TOUCH-09 (PRGM sheet) — Enter PRGM mode. Confirm a "PROGRAM" pull-up sheet appears showing the program steps with the current step highlighted; expand/collapse via tap.
4. TOUCH-10 — Confirm the stack panel starts collapsed (only the X row visible) with a chevron. Tap the chevron to expand to all of X/Y/Z/T/L; tap again to collapse. Confirm the keypad gains vertical room when collapsed.
5. TOUCH-11 — Drag/flick the calculator BODY up and down. Confirm there is NO rubber-band bounce of the whole page. Then scroll INSIDE an expanded bottom sheet and confirm it scrolls smoothly without bouncing the outer body.
  </how-to-verify>
  <resume-signal>Type "approved" if all five pass, or describe the failure (e.g. "stack starts expanded on iOS", "calculator body rubber-bands", "PRGM sheet does not appear").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only) | This plan builds and exercises the app; it introduces no new code or input vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-11 | Tampering | iOS build/install pipeline | accept | Uses the established just ios-build + signing team 2P4R8QSWT4 from Phase 53; no new credentials or surface introduced |

No new attack surface — this is a verification-only plan over code threat-modeled in Plans 01–05.
</threat_model>

<verification>
- Automated final gate (Task 1): npm test green + cargo check --target aarch64-apple-ios clean + just gui-ci green
- Release IPA built via just ios-build and installed + cold-launched on iPhone SE (Task 2)
- All four on-device checkpoint tasks (Tasks 3–5) return "approved" (TOUCH-01/03/04/05/06/07/08/09/10/11 confirmed on hardware per D-55.4)
</verification>

<success_criteria>
- iPhone SE on-device confirmation of: 44pt accuracy, no tap delay/flash, audio on first use, haptic tiers + error haptic, ALPHA + label entry above keyboard, legibility, bottom sheets, collapsible stack, no rubber-band overscroll
- Every D-55.4 manual-only verification recorded as pass
</success_criteria>

<output>
Create `.planning/phases/55-touch-ui-adaptation/55-06-SUMMARY.md` when done
</output>
