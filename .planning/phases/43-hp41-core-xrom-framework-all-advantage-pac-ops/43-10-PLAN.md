---
phase: 43-hp41-core-xrom-framework-all-advantage-pac-ops
plan: 10
type: execute
wave: 5
depends_on: ["43-02", "43-03", "43-04", "43-05", "43-06", "43-07", "43-08", "43-09"]
files_modified:
  - hp41-core/src/ops/mod.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/src/ops/advantage/mod.rs
autonomous: true
requirements:
  - ADV-FW-06
must_haves:
  truths:
    - "cargo build -p hp41-core succeeds with zero warnings"
    - "cargo test -p hp41-core passes all tests including new Advantage Pac tests"
    - "All ~117 Op::Adv* variants dispatch to real implementations (no remaining stubs)"
    - "D-43.5 isolation holds: zero references to matrix_dim/matrix_active_reg in advantage/"
    - "Free42 contamination guard exits 0 covering advantage/ directory"
    - "hp41-cli and hp41-gui produce only the expected non-exhaustive patterns compile errors (sanctioned Phase 44/46 break)"
  artifacts:
    - path: "hp41-core/src/ops/mod.rs"
      provides: "All ~117 dispatch() arms route to real implementations"
    - path: "hp41-core/src/ops/program.rs"
      provides: "All ~117 execute_op() arms route to real implementations"
  key_links:
    - from: "hp41-core/src/ops/mod.rs"
      to: "hp41-core/src/ops/advantage/"
      via: "dispatch() arms reference advantage::*::op_adv_* functions"
      pattern: "crate::ops::advantage"
---

<objective>
Final integration verification: ensure all ~117 Op variants dispatch to real implementations (no remaining InvalidOp stubs), run the full test suite, verify D-43.5 isolation, run the contamination guard, and confirm the expected compile-break state for hp41-cli/hp41-gui.

Purpose: This plan catches any wiring gaps, stale stubs, or regressions introduced during the parallel implementation waves. It is the quality gate before Phase 43 is considered complete.

Output: Phase 43 passes all verification criteria. hp41-core compiles clean with all Advantage Pac operations working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-CONTEXT.md
@.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dispatch/execute_op for remaining stubs and fix any gaps</name>
  <files>hp41-core/src/ops/mod.rs, hp41-core/src/ops/program.rs, hp41-core/src/ops/advantage/mod.rs</files>
  <read_first>
    hp41-core/src/ops/mod.rs (dispatch function — search for any remaining Err(HpError::InvalidOp) in Advantage Pac arms),
    hp41-core/src/ops/program.rs (execute_op function — search for remaining stubs),
    hp41-core/src/ops/advantage/mod.rs (re-exports — verify all op functions are publicly accessible),
    hp41-core/src/ops/advantage/conv.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/matrix_ops.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/matrix_linalg.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/matrix_complex.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/complex_ext.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/solvers.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/poly.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/matrix_workflow.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/curve_fit.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/vectors.rs (verify stubs replaced),
    hp41-core/src/ops/advantage/tvm.rs (verify stubs replaced)
  </read_first>
  <action>
    Scan all advantage/ source files for any remaining `Err(HpError::InvalidOp)` stub bodies that should have been replaced by Plans 43-02 through 43-09. For each found stub:
    1. Determine which plan should have implemented it
    2. Implement the operation following the patterns established in the analog file

    Verify the dispatch() function in ops/mod.rs routes every Op::Adv* variant to a real function in advantage/. Verify execute_op() in program.rs has matching arms.

    Verify that `mod advantage;` is declared in `hp41-core/src/ops/mod.rs` so the module is included in the build.

    Run `grep -c "Err(HpError::InvalidOp)" hp41-core/src/ops/advantage/*.rs` — the count should be 0 (all stubs replaced). If any remain, implement them.

    Run the full D-43.5 isolation check: `grep -rn "matrix_dim\|matrix_active_reg" hp41-core/src/ops/advantage/` must return empty.

    Run the Free42 contamination guard: `bash scripts/check-free42-contamination.sh` must exit 0.

    Run the full test suite: `cargo test -p hp41-core` must pass.

    Verify the expected compile-break state: `cargo check -p hp41-cli 2>&1 | grep "non-exhaustive"` should show pattern match errors for the new Op variants in prgm_display.rs (this is the sanctioned Phase 44 break).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator && cargo test -p hp41-core 2>&1 | tail -5 && echo "---" && grep -c "Err(HpError::InvalidOp)" hp41-core/src/ops/advantage/*.rs && echo "---" && grep -rn "matrix_dim|matrix_active_reg" hp41-core/src/ops/advantage/ | head -5 && echo "exit:$?" && bash scripts/check-free42-contamination.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p hp41-core` succeeds with zero errors
    - `cargo test -p hp41-core` passes (all tests green)
    - `grep -c "Err(HpError::InvalidOp)" hp41-core/src/ops/advantage/*.rs` reports 0 for each file (no remaining stubs)
    - `grep -rn "matrix_dim\|matrix_active_reg" hp41-core/src/ops/advantage/` returns empty (D-43.5)
    - `bash scripts/check-free42-contamination.sh` exits 0
    - `cargo check -p hp41-cli 2>&1` shows only expected non-exhaustive pattern errors for Op::Adv* variants in prgm_display.rs (sanctioned Phase 44 break)
  </acceptance_criteria>
  <done>All ~117 Advantage Pac operations have real implementations, full test suite green, all invariants hold</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Final integration | All prior plans' mitigations verified holistically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-17 | Tampering | Stale stubs bypass | mitigate | grep audit for remaining InvalidOp stubs; zero-count gate |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies throughout Phase 43 |
</threat_model>

<verification>
```bash
# Full verification battery
cargo build -p hp41-core
cargo test -p hp41-core
grep -c "Err(HpError::InvalidOp)" hp41-core/src/ops/advantage/*.rs
grep -rn "matrix_dim\|matrix_active_reg" hp41-core/src/ops/advantage/
bash scripts/check-free42-contamination.sh
cargo check -p hp41-cli 2>&1 | grep "non-exhaustive" | head -5
```
</verification>

<success_criteria>
- hp41-core compiles clean with all ~117 Advantage Pac operations
- Full test suite passes
- Zero remaining stubs
- D-43.5 isolation holds
- Contamination guard passes
- Expected CLI/GUI compile breaks confirmed (sanctioned for Phase 44/46)
</success_criteria>

<output>
Create `.planning/phases/43-hp41-core-xrom-framework-all-advantage-pac-ops/43-10-SUMMARY.md` when done
</output>
