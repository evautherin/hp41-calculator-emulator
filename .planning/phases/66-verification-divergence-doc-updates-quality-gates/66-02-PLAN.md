---
phase: 66-verification-divergence-doc-updates-quality-gates
plan: 02
type: execute
wave: 1
depends_on: ["66-01"]
files_modified:
  - hp41-core/src/ops/print.rs
  - hp41-core/tests/print_tests.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/phase_63_interrupting_alarms.rs
  - hp41-cli/src/app.rs
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "PRX/PRA/PRSTK return Err(NonExistent) when neither flag 21 nor flag 55 is set"
    - "PRX/PRA/PRSTK print normally when flag 55 (or flag 21) is set"
    - "Back-arrow clears app.message when an error is displayed (verified-correct, UNC-01)"
    - "op_size truncates silently with an accurate OM-cited comment, no MEMORY LOST display (UNC-03)"
    - "All 7 PITFALLS re-entrancy scenarios are demonstrably covered by the 12 existing tests and the suite is green"
  artifacts:
    - path: "hp41-core/src/ops/print.rs"
      provides: "Printer-presence guard on op_prx/op_pra/op_prstk"
      contains: "NonExistent"
    - path: "hp41-core/tests/print_tests.rs"
      provides: "Reworked print tests (flag 55 in setup) + no-printer NonExistent test"
      contains: "test_prx_returns_nonexistent"
    - path: "hp41-core/tests/phase_63_interrupting_alarms.rs"
      provides: "7-PITFALLS → 12-test-fn mapping header comment"
      contains: "PITFALLS"
  key_links:
    - from: "hp41-core/src/ops/print.rs"
      to: "hp41-core/src/ops/flags.rs flag_get"
      via: "printer-presence guard at top of each print fn"
      pattern: "flag_get\\(state\\.flags, 55\\)"
    - from: "hp41-core/src/ops/print.rs"
      to: "hp41-core/src/error.rs HpError::NonExistent"
      via: "return Err on no-printer"
      pattern: "Err\\(HpError::NonExistent\\)"
---

<objective>
Apply the three OM-verified UNC dispositions in code:

1. **UNC-02 (DIVERGENT — fix):** Guard `op_prx`/`op_pra`/`op_prstk` so they return `Err(HpError::NonExistent)` when no printer is present (neither flag 21 "Printer Enable" nor flag 55 "Printer Existence" set), per OM p.53-58. Rework the 18 existing print tests to enable printing (flag 55) and add one new no-printer test.
2. **UNC-01 (ALREADY-CORRECT — record + optional regression test):** OM p.15 confirms `←` clears error messages; `app.rs` already sets `self.message = None` after `backspace_entry()`. Add an optional CLI regression test; no behavior change.
3. **UNC-03 (ALREADY-CORRECT — comment fix only):** OM p.19/p.57 confirm SIZE reduction is silent and MEMORY LOST is a power-event display. Replace the misleading `op_size` "hardware-faithful 'MEM LOST'" comment with an accurate OM-cited explanation. Do NOT add `display_override`.
4. **Re-entrancy matrix (D-08):** Add the 7-PITFALLS → 12-test-fn mapping as a header comment to `phase_63_interrupting_alarms.rs` and prove the suite green. Research confirmed ZERO gaps — add NO new re-entrancy test.

Purpose: Close VERIFY-01's code-side: fix the one real divergence (UNC-02), record the two already-correct behaviors, and demonstrate the re-entrancy coverage.
Output: Flag-gated print ops, reworked print tests, an accurate op_size comment, an optional UNC-01 regression test, and the re-entrancy mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-01-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Helpers the executor uses directly — no codebase exploration needed. -->

From hp41-core/src/ops/flags.rs (both public):
  pub fn flag_get(flags: u64, n: u8) -> bool;
  pub fn flag_set(flags: u64, n: u8) -> u64;

From hp41-core/src/error.rs (added in Plan 01):
  HpError::NonExistent   // #[error("nonexistent")]

Print fns to guard (hp41-core/src/ops/print.rs):
  pub fn op_prx(state: &mut CalcState)   -> Result<(), HpError>   // line 13
  pub fn op_pra(state: &mut CalcState)   -> Result<(), HpError>   // line 23
  pub fn op_prstk(state: &mut CalcState) -> Result<(), HpError>   // line 35

Flag meanings (OM p.53-54): flag 21 = Printer Enable; flag 55 = Printer Existence.
PITFALLS → test-fn mapping table is fully enumerated in 66-RESEARCH.md ("7-PITFALLS → 12-Test-Fn Mapping Table").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UNC-02 — flag-gate PRX/PRA/PRSTK + rework print tests + new no-printer test</name>
  <files>hp41-core/src/ops/print.rs, hp41-core/tests/print_tests.rs</files>
  <read_first>
    - hp41-core/src/ops/print.rs (full file — the three op fns at lines 13/23/35; print-emulation discipline: they push to state.print_buffer, never println!)
    - hp41-core/src/ops/flags.rs (flag_get / flag_set signatures and the bit indexing — lines 12 and 21)
    - hp41-core/src/error.rs (confirm HpError::NonExistent landed in Plan 01)
    - hp41-core/tests/print_tests.rs (all 18 tests — identify the shared setup/helper so flag 55 can be set in one place)
    - .planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md ("UNC-02 Fix Pattern", "UNC-02 Fix: Blast-Radius Analysis", "Test rework" sections — these enumerate every affected test)
    - docs/manuals/HP-41CV/HP-41C_Operating_Manual.pdf p.53-58 (flags 21/55 + printer-not-connected error)
  </read_first>
  <behavior>
    - Test: with both flags 21 and 55 clear (default state), `op_prx` returns `Err(HpError::NonExistent)`.
    - Test: with flag 55 set, `op_prx`/`op_pra`/`op_prstk` print to `print_buffer` exactly as before (existing 18 assertions still hold once setup sets flag 55).
  </behavior>
  <action>
    In `hp41-core/src/ops/print.rs`, add a printer-presence guard as the FIRST statement of each of `op_prx`, `op_pra`, `op_prstk`: if NEITHER `flag_get(state.flags, 55)` NOR `flag_get(state.flags, 21)` is true, `return Err(HpError::NonExistent)`. Import `flag_get` via `use crate::ops::flags::flag_get;`. Use flags 21 and 55 ONLY — flag 25 is "Error Ignore", NOT a printer flag (66-RESEARCH Pitfall 1). Leave the existing print bodies unchanged. In `hp41-core/tests/print_tests.rs`, set `state.flags = flag_set(state.flags, 55)` in the shared setup path (prefer a single helper/setup site over per-test duplication) so all 18 existing tests print again — this includes the `test_prx_*`, `test_pra_*`, `test_prstk_*`, and the three `*_in_program` tests enumerated in 66-RESEARCH. Add one new test `test_prx_returns_nonexistent_when_no_printer_flag` asserting that on a default-flags state `op_prx` returns `Err(HpError::NonExistent)`. Tests may `#[allow(clippy::unwrap_used)]` per CLAUDE.md. Do NOT edit CATALOG tests (`phase22_catalog.rs`, `op_catalog_xrom.rs`) — `op_catalog` pushes to print_buffer directly, not via these ops; verify this assumption holds before concluding (66-RESEARCH Pitfall 5). Use inline format args in any new code (MSRV 1.88 gate).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test print_tests 2>&1 | tail -6; cargo test -p hp41-core --test phase22_catalog 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/ops/print.rs` contains `flag_get(state.flags, 55)` and `Err(HpError::NonExistent)` inside the print guard.
    - `print.rs` uses flags 21 and 55 ONLY (no `flag_get(state.flags, 25)` anywhere).
    - `hp41-core/tests/print_tests.rs` contains `test_prx_returns_nonexistent_when_no_printer_flag` and a `flag_set(state.flags, 55)` setup call.
    - `cargo test -p hp41-core --test print_tests` exits 0 (all reworked tests + the new test pass).
    - `cargo test -p hp41-core --test phase22_catalog` still exits 0 (CATALOG unaffected — assumption verified, not edited).
    - `print.rs` contains no `println!`/`eprintln!` (print-emulation discipline preserved).
  </acceptance_criteria>
  <done>PRX/PRA/PRSTK error with NonExistent when no printer flag is set and print normally when flag 55 is set; print_tests green incl. the new no-printer test; CATALOG tests untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: UNC-01 regression test (CLI) + UNC-03 op_size comment fix</name>
  <files>hp41-cli/src/app.rs, hp41-core/src/ops/program.rs</files>
  <read_first>
    - hp41-cli/src/app.rs lines 960-990 (the back-arrow / `backspace_entry()` path at line 974 followed by `self.message = None` at line 975 — UNC-01 is already correct)
    - hp41-core/src/ops/program.rs lines 255-290 (`op_size` and the comment at ~line 263 reading "hardware-faithful 'MEM LOST'")
    - .planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md ("UNC-03 Comment Fix" + Pitfall 2 — do NOT add display_override)
    - docs/manuals/HP-41CV/HP-41C_Operating_Manual.pdf p.15 (back-arrow clears error — UNC-01), p.19 (SIZE reduction silent), p.57 (MEMORY LOST = Continuous Memory clear — UNC-03)
  </read_first>
  <action>
    UNC-03: In `hp41-core/src/ops/program.rs` `op_size` (~line 263), replace ONLY the misleading comment "hardware-faithful 'MEM LOST'" with an accurate, OM-cited explanation stating that SIZE reduction truncates the highest registers silently with no display message, and that MEMORY LOST is a power-event/Continuous-Memory-clear display (OM p.19, p.57) — NOT a SIZE event. Change no executable logic; do NOT add `display_override = Some("MEMORY LOST")` (66-RESEARCH Pitfall 2). UNC-01: In `hp41-cli/src/app.rs`, add an optional regression test in the CLI test module asserting that after the back-arrow path runs with an error message active, `app.message` is `None` (mirror the existing `backspace_entry()` + `self.message = None` at lines 974-975). This is verification-only — the production path is already correct (OM p.15); do NOT change the back-arrow handler logic. Use inline format args in any new test code.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli 2>&1 | tail -5; grep -q "MEMORY LOST is a power" hp41-core/src/ops/program.rs || grep -qi "OM p.19" hp41-core/src/ops/program.rs; ! grep -q 'display_override = Some("MEMORY LOST"' hp41-core/src/ops/program.rs</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/ops/program.rs` `op_size` comment cites OM p.19 (and/or p.57) and states truncation is silent with no MEMORY LOST display.
    - `hp41-core/src/ops/program.rs` does NOT contain `display_override = Some("MEMORY LOST"` (no behavior change for UNC-03).
    - `hp41-cli/src/app.rs` contains a test asserting `app.message` is `None` after the back-arrow path with an active error message.
    - `cargo test -p hp41-cli` exits 0.
  </acceptance_criteria>
  <done>op_size comment is OM-accurate (no display_override added); a CLI regression test pins UNC-01 already-correct behavior; CLI tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-entrancy matrix — 7-PITFALLS → 12-test mapping header + suite green</name>
  <files>hp41-core/tests/phase_63_interrupting_alarms.rs</files>
  <read_first>
    - hp41-core/tests/phase_63_interrupting_alarms.rs (all 12 test fns — read names; this is mapping, not authoring)
    - .planning/research/PITFALLS.md (the 7 re-entrancy scenarios — Scenarios 1-7)
    - .planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md ("7-PITFALLS → 12-Test-Fn Mapping Table" — the full mapping is pre-computed there; "Gaps: NONE")
  </read_first>
  <action>
    Add a file-level header comment block to `hp41-core/tests/phase_63_interrupting_alarms.rs` that records the 7-PITFALLS-scenario → test-fn mapping exactly as enumerated in 66-RESEARCH ("7-PITFALLS → 12-Test-Fn Mapping Table"), naming each of the 7 PITFALLS scenarios and the test fn(s) that cover it, plus the 5 additional edge-case tests. State explicitly that all 7 scenarios are covered with zero gaps (D-08), so no new re-entrancy test is added. Do NOT author, rename, duplicate, or delete any test fn — the 12 tests stand as-is. Keep the comment English-only (D-07).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test phase_63_interrupting_alarms 2>&1 | tail -5; grep -qi "PITFALLS" hp41-core/tests/phase_63_interrupting_alarms.rs</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/tests/phase_63_interrupting_alarms.rs` contains a header comment mapping all 7 PITFALLS scenarios to specific test fn names, stating zero gaps.
    - The file still contains exactly the 12 pre-existing test fns (none added, renamed, or removed).
    - `cargo test -p hp41-core --test phase_63_interrupting_alarms` exits 0 (all 12 pass).
  </acceptance_criteria>
  <done>The 7→12 mapping is documented in the test file as a demonstrated-coverage artifact and the re-entrancy suite is green (satisfies ROADMAP success criterion 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — offline calculator fidelity fix; no auth/network/untrusted-input/serialization changes. ASVS L1: N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02 | — | print.rs printer-presence guard | accept | The guard reads two existing in-memory system flags and returns an error; no input parsing, no network, no serialization-format change. Offline calculator behavior only. |
| T-66-03 | — | op_size comment + CLI regression test | accept | Comment-only + test-only; no logic change, no new surface. |
</threat_model>

<verification>
- `cargo test -p hp41-core --test print_tests` green (18 reworked + 1 new no-printer test).
- `cargo test -p hp41-core --test phase22_catalog` green (CATALOG unaffected).
- `cargo test -p hp41-cli` green (UNC-01 regression).
- `cargo test -p hp41-core --test phase_63_interrupting_alarms` green (12 tests).
- print.rs uses flags 21/55 only (no flag 25); no `display_override = Some("MEMORY LOST")` added.
- No `cargo fmt` on `hp41-gui/src-tauri` (D-10; this plan does not touch the GUI crate).
</verification>

<success_criteria>
UNC-02 is fixed (flag-gated print ops + reworked/new tests green), UNC-01 and UNC-03 are confirmed already-correct (regression test + accurate comment, no behavior change), and the 7-PITFALLS → 12-test re-entrancy mapping is documented with a green suite.
</success_criteria>

<output>
Create `.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-02-SUMMARY.md` when done.
</output>
