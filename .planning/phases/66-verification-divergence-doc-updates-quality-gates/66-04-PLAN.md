---
phase: 66-verification-divergence-doc-updates-quality-gates
plan: 04
type: execute
wave: 2
depends_on: ["66-02", "66-03"]
files_modified: []
autonomous: false
requirements: [VERIFY-01]

must_haves:
  truths:
    - "just ci is green (lint + 3-OS test + coverage ≥95% lines / ≥93% regions + license-audit + schema-aliases)"
    - "just ci-msrv is green (pinned clippy 1.88; no uninlined_format_args regressions)"
    - "just gui-ci is green"
    - "numerical accuracy ≥ 98% (843+ cases) — not regressed by the UNC-02 fix"
    - "zero panics in hp41-core"
    - "ungated GUI-crate clippy shows no NEW hits beyond the 2 known noise hits"
    - "v4.3 milestone PR description is updated with the Phase 66 completion summary"
  artifacts: []
  key_links:
    - from: "Phase 66 code+doc changes (Plans 02/03)"
      to: "green CI gate suite"
      via: "just ci && just ci-msrv && just gui-ci"
      pattern: "just ci"
---

<objective>
Final closeout wave for Phase 66 and the v4.3 milestone: run the full quality-gate suite green and update the open v4.3 milestone PR description (D-09/D-10/D-11). No code or doc authoring here — this plan only runs gates and, if a gate exposes a regression, routes the fix back (it should not, given the small, verified changes in Plans 01-03).

Gate suite (66-RESEARCH "Quality-Gate Command List", run in order):
1. `just ci` — lint + 3-OS test + coverage ≥95% lines / ≥93% regions + license-audit + schema-aliases.
2. `just ci-msrv` — pinned clippy 1.88. LANDMINE: `uninlined_format_args` etc. are `-D warnings` under 1.88 (all new code in Plans 01-02 must already use inline format args). Verify with `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings`.
3. `just gui-ci`.
4. Numerical accuracy ≥ 98% (843+ cases) — UNC-02 print rework must not regress it.
5. Zero panics in hp41-core.
6. Ungated GUI-crate clippy: `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml` — 2 known noise hits (types.rs:148, commands.rs `&*p`) are ACCEPTED, not regressions.
7. Update the open v4.3 milestone PR (#26) description with the Phase 66 summary.

D-10: NEVER run `cargo fmt` on `hp41-gui/src-tauri`. If a stray edit dirtied it, `git restore hp41-gui/`.

Purpose: Prove the milestone is green end-to-end and record completion on the PR.
Output: All gates green + an updated PR description. This is the phase's terminal task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-02-SUMMARY.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full quality-gate suite green</name>
  <files>(none — gate execution only)</files>
  <read_first>
    - .planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md ("Quality-Gate Command List" + "MSRV Pitfall — Detailed" + Pitfall 3 (no GUI fmt) + Pitfall 6 (MSRV format args))
    - justfile (confirm `ci`, `ci-msrv`, `gui-ci`, `lint`, `test`, `coverage`, `license-audit`, `schema-aliases-check` recipes)
    - CLAUDE.md (Quality Gates table: coverage ≥95% lines / ≥93% regions, numerical accuracy ≥98%, 0 panics, 0 Free42 contamination; the ungated GUI clippy memory and the GUI-fmt prohibition)
  </read_first>
  <action>
    Run the gates in order and make each green before the next: `just ci`, then `just ci-msrv` (also run `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings` explicitly to surface the MSRV-only style lints — if `uninlined_format_args` or similar fires on Plan 01/02 code, fix to inline format args in the offending file and re-run), then `just gui-ci`, then numerical accuracy via `cargo test -p hp41-core --test numerical_accuracy` (must stay ≥98%, 843+ cases), then the zero-panics check `grep -rn 'unwrap()' hp41-core/src/ | grep -v '#\[allow'` (expect 0), then the ungated GUI clippy `cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml` (accept ONLY the 2 known noise hits at types.rs:148 and commands.rs `&*p`; any NEW hit is a regression to fix). If any working-tree change dirtied `hp41-gui/src-tauri` formatting, run `git restore hp41-gui/` — do NOT `cargo fmt` the GUI crate (D-10). Also check `gh pr checks 26` for remote CI status before declaring green.
  </action>
  <verify>
    <automated>just ci && just ci-msrv && just gui-ci && cargo test -p hp41-core --test numerical_accuracy 2>&1 | tail -3; cargo clippy --manifest-path hp41-gui/src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `just ci` exits 0 (lint + test + coverage thresholds + license-audit + schema-aliases all pass).
    - `just ci-msrv` exits 0 and `cargo +1.88 clippy --workspace --all-targets --all-features -- -D warnings` produces no warnings.
    - `just gui-ci` exits 0.
    - `cargo test -p hp41-core --test numerical_accuracy` reports ≥98% (843+ cases).
    - `grep -rn 'unwrap()' hp41-core/src/ | grep -v '#\[allow'` returns 0 results.
    - GUI-crate clippy shows only the 2 known pre-existing noise hits (no new hits).
    - `hp41-gui/src-tauri` working tree is clean (no fmt churn committed).
  </acceptance_criteria>
  <done>Every gate in the D-09 suite is green; numerical accuracy and zero-panics hold; GUI crate has no new clippy hits and no fmt churn.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full v4.3 Phase 66 changeset: UNC-02 print-flag gating + reworked tests, UNC-01/UNC-03 already-correct records, the 7-PITFALLS re-entrancy mapping, the divergence-doc sweep + v4.3 closure ledger, and a green gate suite (just ci / just ci-msrv / just gui-ci, numerical accuracy ≥98%, zero panics, GUI clippy clean).</what-built>
  <how-to-verify>
    1. Confirm all gates green locally: `just ci && just ci-msrv && just gui-ci` exit 0.
    2. Confirm remote CI: `gh pr checks 26` shows passing checks.
    3. Spot-check that the v4.3 closure ledger in `.planning/research/DIVERGENCE-AUDIT.md` reads correctly and UNC-02 cites OM p.53-58.
    4. Confirm `hp41-gui/src-tauri` has no stray fmt churn (`git status` clean for that path).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the PR description update, or describe any gate/ledger issue to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The v4.3 milestone PR (#26 "v4.3 Hardware Fidelity — Phases 62–65", currently DRAFT, base develop) needs its description updated with the Phase 66 completion summary (D-11). The milestone version bump (4.3.0) is already done — do NOT re-do it.</what-built>
  <how-to-verify>
    Update PR #26's description to append a Phase 66 completion section summarizing: UNC-01 verified-correct (OM p.15), UNC-02 fixed — PRX/PRA/PRSTK now flag-gated on 21/55 returning NonExistent (OM p.53-58), UNC-03 verified-correct comment fix (OM p.19/p.57), 7-PITFALLS re-entrancy matrix proven (zero gaps, 12 tests green), divergence-doc sweep + v4.3 closure ledger, and all gates green. Suggested command: `gh pr edit 26 --body-file <updated-body>` (preserve the existing Phases 62–65 content; append Phase 66). Optionally retitle to span Phases 62–66. Per the standing "update PR per shipped phase" practice, push Phase 66 commits BEFORE announcing completion.
  </how-to-verify>
  <resume-signal>Type "done" once PR #26's description reflects the Phase 66 completion summary.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — offline calculator fidelity fix; no auth/network/untrusted-input/serialization changes. ASVS L1: N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05 | Tampering | npm/cargo installs during gate runs | mitigate | No new dependencies added this phase (zero new runtime deps invariant); `just ci`/`gui-ci` use the existing locked manifests. license-audit gate (Free42 contamination) runs as part of `just ci`. |
| T-66-06 | — | gate execution + PR description | accept | Read-only CI execution + a GitHub PR metadata edit on the project's own milestone PR; no code surface, no untrusted input. |
</threat_model>

<verification>
- `just ci`, `just ci-msrv`, `just gui-ci` all exit 0.
- Numerical accuracy ≥98% (843+ cases); zero panics in hp41-core.
- GUI clippy: only the 2 known noise hits.
- `gh pr checks 26` passing.
- PR #26 description updated with the Phase 66 summary (D-11); version bump NOT re-done.
- No `cargo fmt` run on `hp41-gui/src-tauri` (D-10).
</verification>

<success_criteria>
The entire D-09 gate suite is green, numerical accuracy and zero-panics hold, the GUI crate has no new clippy hits or fmt churn, and the v4.3 milestone PR description records the Phase 66 completion — closing VERIFY-01 and the v4.3 milestone.
</success_criteria>

<output>
Create `.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-04-SUMMARY.md` when done.
</output>
