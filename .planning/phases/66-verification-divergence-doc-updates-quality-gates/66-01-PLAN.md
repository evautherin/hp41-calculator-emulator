---
phase: 66-verification-divergence-doc-updates-quality-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - hp41-core/src/error.rs
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "HpError::NonExistent exists and its Display string is the OM NONEXISTENT message class"
    - "The new variant is usable by the UNC-02 print guard in the next wave"
  artifacts:
    - path: "hp41-core/src/error.rs"
      provides: "HpError::NonExistent variant + Display unit test"
      contains: "NonExistent"
  key_links:
    - from: "hp41-core/src/error.rs"
      to: "thiserror Display derive"
      via: "#[error(\"nonexistent\")] attribute on the variant"
      pattern: "#\\[error\\(\"nonexistent\"\\)\\]"
---

<objective>
Add the `HpError::NonExistent` variant to `hp41-core/src/error.rs` so the UNC-02 printer-presence guard (Plan 02) can return it. This is the BLOCKING Wave-0 dependency: `print.rs` cannot compile against a variant that does not yet exist (66-RESEARCH Pitfall 4 — error.rs currently has 18 variants, none named NonExistent).

Purpose: Provide the OM NONEXISTENT-class error (HP-41C OM p.58) that PRX/PRA/PRSTK raise when no printer is present, isolated as its own plan so the dependency edge into the print fix is explicit.
Output: One new enum variant + one Display unit test, both green under stable and MSRV 1.88 clippy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md

<interfaces>
<!-- error.rs already uses thiserror with #[error("...")] on every variant. -->
<!-- Existing Display-test pattern to mirror (hp41-core/src/error.rs, ~line 95): -->

#[test]
fn file_not_found_display() {
    assert_eq!(HpError::FileNotFound.to_string(), "fl not found");
}

<!-- HpError is a plain unit-variant enum deriving Debug, Clone, PartialEq, thiserror::Error. -->
<!-- NonExistent is a unit variant (no payload) — same shape as Overflow / DivideByZero / FileNotFound. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HpError::NonExistent variant + Display test</name>
  <files>hp41-core/src/error.rs</files>
  <read_first>
    - hp41-core/src/error.rs (read the full enum + the existing test module; mirror the `#[error(...)]` attribute style and the `file_not_found_display` test at ~line 95)
    - .planning/phases/66-verification-divergence-doc-updates-quality-gates/66-RESEARCH.md ("Don't Hand-Roll" table + Pitfall 4 — variant name and message class are pinned there)
    - docs/manuals/HP-41CV/HP-41C_Operating_Manual.pdf p.57-58 (NONEXISTENT error message class — the source of the Display string)
  </read_first>
  <behavior>
    - Test: `HpError::NonExistent.to_string()` equals `"nonexistent"`.
    - The variant is a unit variant (no fields), consistent with the other error variants.
  </behavior>
  <action>
    Add a unit variant `NonExistent` to the `HpError` enum with attribute `#[error("nonexistent")]` (lowercase, matching the existing variants' Display style and the OM "NONEXISTENT" message class, OM p.58). Place it alongside the other no-printer-class errors; do NOT reuse or rename an existing variant (verified: none named NonExistent). In the existing `#[cfg(test)]` module add a Display test fn named `nonexistent_display` that asserts `HpError::NonExistent.to_string()` is `"nonexistent"`, following the `file_not_found_display` pattern exactly. Use inline format args anywhere a format string is needed (MSRV 1.88 `uninlined_format_args` is `-D warnings`). Do NOT touch any match arm elsewhere — `HpError` variants are not part of the 4-way Op exhaustive-match invariant (this is an error enum, not an Op).
  </action>
  <verify>
    <automated>cargo test -p hp41-core --lib error 2>&1 | tail -5; grep -q 'NonExistent' hp41-core/src/error.rs && grep -q '#\[error("nonexistent")\]' hp41-core/src/error.rs</automated>
  </verify>
  <acceptance_criteria>
    - `hp41-core/src/error.rs` contains a `NonExistent` variant carrying `#[error("nonexistent")]`.
    - `hp41-core/src/error.rs` contains a test asserting `HpError::NonExistent.to_string() == "nonexistent"`.
    - `cargo test -p hp41-core --lib error` exits 0 (the new Display test passes).
    - `cargo build -p hp41-core` succeeds (variant compiles; no exhaustive-match breakage because HpError is not matched non-exhaustively anywhere).
  </acceptance_criteria>
  <done>HpError::NonExistent exists with the correct Display string and a passing Display test; hp41-core builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — offline calculator fidelity fix; no auth/network/untrusted-input/serialization changes. ASVS L1: N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | — | hp41-core/src/error.rs (new error variant) | accept | No new attack surface — a Display string on an offline calculator error enum; no input, no network, no serialization-format change. |
</threat_model>

<verification>
- `cargo test -p hp41-core --lib error` green.
- New variant + Display test present in error.rs (grep gates above).
- No `cargo fmt` run on `hp41-gui/src-tauri` (D-10; this plan does not touch the GUI crate at all).
</verification>

<success_criteria>
HpError::NonExistent is defined with `#[error("nonexistent")]`, a Display test passes, and hp41-core compiles — unblocking the UNC-02 print guard in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/66-verification-divergence-doc-updates-quality-gates/66-01-SUMMARY.md` when done.
</output>
