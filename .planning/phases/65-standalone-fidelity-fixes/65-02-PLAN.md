---
phase: 65-standalone-fidelity-fixes
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - hp41-cli/src/ui.rs
  - hp41-core/src/state.rs
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "After VIEW or AVIEW (or PROMPT) executes, the CLI main display shows the register name/value or ALPHA string carried in state.display_override, matching the GUI"
    - "When display_override is None, the CLI display falls through to its prior behavior (prgm/alpha/X)"
    - "The stale 'DISP-01 deferred to v4.4' comments in state.rs are removed/updated"
  artifacts:
    - path: "hp41-cli/src/ui.rs"
      provides: "get_display_string reads state.display_override at the D-06 priority slot"
      contains: "display_override"
    - path: "hp41-core/src/state.rs"
      provides: "stale DISP-01-deferred comments resolved (D-11)"
      contains: "Phase 65"
  key_links:
    - from: "hp41-cli/src/ui.rs"
      to: "state.display_override"
      via: "priority chain between prgm_mode and alpha_mode"
      pattern: "display_override"
---

<objective>
Make the CLI render `state.display_override` (written by VIEW/AVIEW/PROMPT) on the main display, mirroring the GUI which already does this. Resolve the stale "DISP-01 deferred to v4.4" comments.

Implements DISP-01 (D-05, D-06, D-11). Light plan, independent of MATH-01 (Plan 01) — no shared files.

Purpose: CLI↔GUI display parity (D-25.6). The `display_override` field already exists and is written by the core; the CLI simply never read it.
Output: One new branch in `get_display_string` + comment cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-standalone-fidelity-fixes/65-CONTEXT.md
@.planning/phases/65-standalone-fidelity-fixes/65-PATTERNS.md
@.planning/phases/65-standalone-fidelity-fixes/65-VALIDATION.md

<interfaces>
get_display_string (ui.rs ~131-161) priority chain:
clock > stopwatch > entry_buf > prgm_mode > alpha_mode > X.
DISP-01 inserts `display_override` BETWEEN prgm_mode and alpha_mode (D-06): `else if let Some(ref s) = st.display_override { s.clone() }`.
`state.display_override: Option<String>` (state.rs ~194), written by op_view/op_aview/op_prompt (display_ops.rs ~23/32).
Stale comments: state.rs line ~80 (YieldState doc) and line ~531 (pending_yield field doc) — D-11.
GUI analog chain: App.tsx `pending_yield?.text ?? display_override ?? display_str`; clear semantics governed by core (CLD / next overwrite / start of entry) — D-05 rejects CLI-side clear-on-keypress.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert display_override branch in get_display_string + update priority comment</name>
  <read_first>
    - hp41-cli/src/ui.rs lines 1-30 (imports incl. `use hp41_core::{format_alpha, format_hpnum, AngleMode}`) and 131-161 (get_display_string)
    - 65-PATTERNS.md §"hp41-cli/src/ui.rs (DISP-01 insertion point)"
    - 65-CONTEXT.md D-05, D-06
  </read_first>
  <action>
    In `get_display_string`, add a new arm BETWEEN the `prgm_mode` branch and the `alpha_mode` branch: `} else if let Some(ref s) = st.display_override { s.clone() }`. This yields the chain `clock > stopwatch > entry_buf > prgm > display_override > alpha > X` (D-06). Do NOT clear the override from the CLI — dismissal stays core-governed (D-05). Update the doc comment at line ~132 to list `display_override` in the priority order.
    NOTE: this plan does NOT add the AON flag-48 branch — that is Plan 04 (DISP-03). Leave the final `else` (X fallback) untouched here; Plan 04 runs in Wave 3 after this.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli</automated>
  </verify>
  <done>get_display_string renders display_override when Some, between prgm and alpha; comment updated; `cargo build -p hp41-cli` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — DISP-01 CLI integration test + resolve stale state.rs comments</name>
  <read_first>
    - hp41-cli existing display tests (grep `get_display_string` and any `display_override` test scaffolding in hp41-cli/src/ui.rs #[cfg(test)] or hp41-cli/tests/)
    - hp41-core/src/state.rs lines 78-82 and 528-533 (the two stale comments)
    - 65-VALIDATION.md (DISP-01 = Wave 0 gap, `cargo test -p hp41-cli`)
    - 65-PATTERNS.md §"hp41-core/src/state.rs (D-11 stale-comment cleanup)"
  </read_first>
  <action>
    Add an integration test (in hp41-cli, e.g. a #[cfg(test)] block in ui.rs or hp41-cli/tests/) that builds an App, sets `app.state.display_override = Some("R01 1.000".into())` (or runs op_view via the core to populate it), and asserts `get_display_string(&app)` returns that override string while at rest; and asserts that with `display_override = None` it falls through to the X fallback. `#[allow(clippy::unwrap_used)]` on the test module.
    Resolve the D-11 stale comments in state.rs: line ~80 -> "/// display_override is NOT written by these yield paths (D-04); DISP-01 resolved in Phase 65."; line ~531 -> "/// Leaves display_override untouched (D-04); DISP-01 resolved in Phase 65." (per PATTERNS exact wording).
  </action>
  <verify>
    <automated>cargo test -p hp41-cli && grep -rn "deferred to v4.4" hp41-core/src/state.rs | grep -v "^#" | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>Integration test asserts display_override renders and None falls through; both stale "deferred to v4.4" comments gone (grep returns 0); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend-only display read of an existing core-owned string field. No authentication, session, network, or new untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Information Disclosure | display_override rendering | accept | display_override carries only register/ALPHA values the user already entered; no PII, no secrets. Local single-user TUI. |

Per RESEARCH §Security Domain: NOT APPLICABLE beyond the above — no HIGH/MEDIUM threats; this is local calculator display logic. No package installs.
</threat_model>

<verification>
- `cargo test -p hp41-cli` green.
- CLI shows VIEW/AVIEW/PROMPT value at rest, matching GUI (D-25.6).
- No "deferred to v4.4" comments remain in state.rs.
- `just test` green at wave merge.
</verification>

<success_criteria>
After VIEW/AVIEW/PROMPT, the CLI display shows the same register-name/value or ALPHA string as the GUI (ROADMAP SC-1). Falls through correctly when override is None. Stale comments resolved.
</success_criteria>

<output>
Create `.planning/phases/65-standalone-fidelity-fixes/65-02-SUMMARY.md` when done.
</output>
