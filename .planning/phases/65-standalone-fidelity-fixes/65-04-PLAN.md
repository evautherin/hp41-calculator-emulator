---
phase: 65-standalone-fidelity-fixes
plan: 04
type: execute
wave: 3
depends_on: ["65-02"]
files_modified:
  - hp41-cli/src/ui.rs
  - hp41-gui/src-tauri/src/types.rs
autonomous: true
requirements: [DISP-03]

must_haves:
  truths:
    - "With flag 48 (AON) set, at rest the CLI display shows the ALPHA register instead of X"
    - "With flag 48 set, at rest the GUI display_str shows the ALPHA register instead of X (baked into from_state, no IPC/App.tsx change)"
    - "A fresh VIEW still shows its value while flag 48 is set (display_override above AON)"
    - "Entry/clock/prgm/alpha_mode still take over while active (AON branch is last before the X fallback)"
    - "AOFF (flag 48 cleared) reverts both frontends to the X fallback"
  artifacts:
    - path: "hp41-cli/src/ui.rs"
      provides: "flag-48 AON branch as the last else-if before the X fallback in get_display_string"
      contains: "1u64 << 48"
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "flag-48 AON branch baked into from_state display_str before the X fallback"
      contains: "1u64 << 48"
  key_links:
    - from: "hp41-cli/src/ui.rs"
      to: "state.flags bit 48"
      via: "last else-if before X fallback -> format_alpha(alpha_reg)"
      pattern: "1u64 << 48"
    - from: "hp41-gui/src-tauri/src/types.rs"
      to: "state.flags bit 48"
      via: "display_str last else-if before X fallback -> format_alpha(alpha_reg)"
      pattern: "1u64 << 48"
---

<objective>
Wire AON (flag 48) auto-display into BOTH frontends: at rest, with flag 48 set, show the ALPHA register instead of X. AOFF reverts to X.

Implements DISP-03 (D-09, D-10). Light plan. Touches `hp41-cli/src/ui.rs` (shared with DISP-01) so it runs in Wave 3 after Plan 02 to avoid a file-ownership conflict; the GUI side (`types.rs`) is exclusive.

Purpose: CLI↔GUI parity (D-25.6) — neither frontend reads flag 48 today. The GUI side is server-side in `from_state` (bake into `display_str`); NO new IPC field, NO App.tsx change (CONTEXT discretion: reuse existing projection, no IPC change).
Output: One else-if in CLI `get_display_string` and one in GUI `from_state` display_str chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-standalone-fidelity-fixes/65-CONTEXT.md
@.planning/phases/65-standalone-fidelity-fixes/65-PATTERNS.md
@.planning/phases/65-standalone-fidelity-fixes/65-VALIDATION.md
@.planning/phases/65-standalone-fidelity-fixes/65-02-SUMMARY.md

<interfaces>
Flag-bit access (display_ops.rs flag_set, used consistently): `state.flags & (1u64 << 48) != 0`.
format_alpha: `format_alpha(&state.alpha_reg)` (CLI: `&st.alpha_reg`).
CLI get_display_string (ui.rs ~131-161): after Plan 02 the chain is clock>stopwatch>entry>prgm>display_override>alpha_mode>X. DISP-03 replaces the final `else` (X) with `else if st.flags & (1u64<<48) != 0 { format_alpha(&st.alpha_reg) } else { format_hpnum(&st.stack.x, &st.display_mode) }` — flag-48 branch is LAST before X, AFTER alpha_mode (D-10, Pitfall 4).
GUI from_state display_str (types.rs ~177-199): chain ends `... else if state.alpha_mode { format_alpha } else { format_hpnum }`. DISP-03 inserts `else if state.flags & (1u64<<48) != 0 { format_alpha(&state.alpha_reg) }` before the final else. Uses raw `state.flags` u64, NOT the projected Vec<u8>.
What does NOT change: CalcStateView fields, IPC contract, App.tsx chain (`pending_yield?.text ?? display_override ?? display_str`). The AON string flows through the existing display_str field.
Stale comment types.rs line ~152: "display_override is NOT written by yield paths (D-04 / DISP-01 deferred)" — update to note DISP-01 resolved in Phase 65.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI AON flag-48 branch in get_display_string</name>
  <read_first>
    - hp41-cli/src/ui.rs lines 131-161 (get_display_string AFTER Plan 02's display_override insert — re-read current state)
    - hp41-core/src/ops/display_ops.rs lines 50-65 (op_aon/op_aoff set/clear flag 48; flag_set/flag_get pattern)
    - 65-PATTERNS.md §"hp41-cli/src/ui.rs (DISP-03 insertion point)" + §"Flag-bit access pattern"
    - 65-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    In `get_display_string`, replace the final `else { format_hpnum(&st.stack.x, &st.display_mode) }` with `else if st.flags & (1u64 << 48) != 0 { format_alpha(&st.alpha_reg) } else { format_hpnum(&st.stack.x, &st.display_mode) }`. The flag-48 branch MUST be the last else-if before the X fallback and MUST sit AFTER the `alpha_mode` branch (Pitfall 4 — wrong order conflicts with ALPHA-mode display). Update the priority comment to append `> (flag48 ? alpha : X)`.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli && cargo build -p hp41-cli</automated>
  </verify>
  <done>CLI shows alpha_reg at rest when flag 48 set, X otherwise; display_override (Plan 02) still takes precedence; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: GUI from_state AON flag-48 branch (display_str) + stale comment</name>
  <read_first>
    - hp41-gui/src-tauri/src/types.rs lines 145-200 (from_state display_str chain ~177-199; stale comment ~152; flags projection ~239-241)
    - 65-PATTERNS.md §"hp41-gui/src-tauri/src/types.rs (DISP-03 GUI side)"
    - 65-CONTEXT.md D-09, D-10; CONTEXT discretion (reuse flags, no IPC change)
  </read_first>
  <action>
    In `from_state`, replace the final `else { format_hpnum(&state.stack.x, &state.display_mode) }` of the `display_str` chain with `else if state.flags & (1u64 << 48) != 0 { format_alpha(&state.alpha_reg) } else { format_hpnum(&state.stack.x, &state.display_mode) }`. Use the raw `state.flags` u64 (NOT the projected Vec<u8>), AFTER the `alpha_mode` branch. Do NOT add any CalcStateView field, IPC command, or App.tsx change — the AON string flows through the existing `display_str`. Update the stale comment at ~line 152 to note DISP-01 is resolved in Phase 65.
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --lib types</automated>
  </verify>
  <done>from_state display_str shows alpha_reg at rest when flag 48 set, X otherwise; no IPC/App.tsx change; stale comment updated; GUI crate builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — DISP-03 AON unit tests (CLI + GUI from_state)</name>
  <read_first>
    - 65-VALIDATION.md (DISP-03 Wave 0 gap: unit in core/types + cli; `cargo test -p hp41-core --lib types` + `cargo test -p hp41-cli`)
    - existing from_state tests in hp41-gui/src-tauri/src/types.rs #[cfg(test)] (model the assertion shape)
  </read_first>
  <action>
    GUI side: add a unit test in types.rs #[cfg(test)] building a CalcState with alpha_reg set and flag 48 set (via the same flag_set path or direct `state.flags |= 1u64 << 48`), assert `CalcStateView::from_state(&state).display_str == format_alpha(&state.alpha_reg)`; clear flag 48 (AOFF), assert display_str reverts to the X format. Also assert that with display_override Some, the override still wins (precedence) — note App.tsx applies display_override above display_str, so this assertion verifies the at-rest fallback ordering within display_str only.
    CLI side: add a unit/integration test that sets app.state flag 48 + alpha_reg and asserts get_display_string returns format_alpha(alpha_reg) at rest, and reverts to X format when flag 48 cleared. `#[allow(clippy::unwrap_used)]` on test modules.
  </action>
  <verify>
    <automated>cargo test -p hp41-cli && cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --lib types</automated>
  </verify>
  <done>Both frontends have an AON flag-48 test asserting alpha_reg at rest and X after AOFF; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend display read of an existing core-owned flag bit and ALPHA register. No authentication, session, network, or new untrusted-input surface. No new IPC field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-05 | Information Disclosure | AON alpha_reg rendering | accept | alpha_reg holds only user-entered ALPHA text; no PII/secrets; local single-user app. |
| T-65-06 | Spoofing | flag-48 read parity CLI vs GUI | mitigate | Both frontends use identical `state.flags & (1u64 << 48) != 0` expression and `format_alpha(alpha_reg)`; parity asserted by Task 3 tests (D-25.6). |

Per RESEARCH §Security Domain: NOT APPLICABLE beyond the above — no HIGH/MEDIUM threats; local calculator display logic. No package installs.
</threat_model>

<verification>
- `cargo test -p hp41-cli` green; `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml --lib types` green.
- CLI and GUI both show ALPHA register at rest under AON, identical behavior (D-25.6).
- VIEW override still wins; AOFF reverts to X on both.
- `just test` + `just gui-ci` green at wave merge.
</verification>

<success_criteria>
AON (flag 48) auto-displays the ALPHA register at rest on BOTH CLI and GUI (ROADMAP SC-3); display_override takes precedence; AOFF reverts to X. No IPC/App.tsx change.
</success_criteria>

<output>
Create `.planning/phases/65-standalone-fidelity-fixes/65-04-SUMMARY.md` when done.
</output>
