---
phase: 37-test-hardening-quality-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 37-02
  - 37-03
files_modified:
  - hp41-core/tests/numerical_accuracy.rs
autonomous: true
requirements:
  - STAT-QUAL-04
  - STAT-QUAL-05

must_haves:
  truths:
    - "numerical_accuracy.rs contains ~30 Stat 1 oracle cases with scipy-derived reference values"
    - "Combined suite passes at >= 98% rate (v1.x 503-case floor preserved, v3.0 768-case floor preserved)"
    - "Closed-form cases use 1e-9 tolerance, iterative cases use 1e-7 tolerance"
  artifacts:
    - path: "hp41-core/tests/numerical_accuracy.rs"
      provides: "Extended accuracy suite with Stat 1 cases (~798 total cases)"
      contains: "ITER_TOL"
  key_links:
    - from: "hp41-core/tests/numerical_accuracy.rs"
      to: "hp41-core/src/ops/stat1/"
      via: "dispatch + CalcState result assertions"
      pattern: "Op::Sigma"
---

<objective>
Extend numerical_accuracy.rs with ~30 Stat 1 Pac oracle cases derived independently from scipy.stats.

Purpose: This is the FINAL numerical accuracy gate before the "feature-complete" hard-claim (D-35.3). Per D-37.1, every oracle value is independently derived from scipy.stats, NOT extracted from existing Phase 33 inline tests. The two-level tolerance discipline (STAT-QUAL-05) is enforced via a new `iter` arm in the `case!` macro: 1e-9 for closed-form ops, 1e-7 for iterative paths. The combined suite grows from 768 to ~798 cases with the >= 98% pass-rate gate.

Output: Extended numerical_accuracy.rs with ITER_TOL constant, `iter` macro arm, and ~30 Stat 1 cases covering all major algorithm families.
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-hardening-quality-gates/37-CONTEXT.md
@.planning/phases/37-test-hardening-quality-gates/37-PATTERNS.md
@.planning/phases/37-test-hardening-quality-gates/37-RESEARCH.md (Pattern 4: numerical_accuracy.rs inline extension)

<interfaces>
<!-- Key types and contracts from the accuracy suite -->

From hp41-core/tests/numerical_accuracy.rs:
- const TOLERANCE: f64 = 1e-9 — default relative tolerance for closed-form ops
- const WIDE_TOL: f64 = 1e-6 — wide tolerance for known-imprecise paths
- struct AccuracyCase { id, domain, description, expected, actual, tol }
- macro case!($domain, $desc, $expected, $actual) — default arm (TOLERANCE)
- macro case!($domain, $desc, $expected, $actual, wide) — wide arm (WIDE_TOL)
- fn push(state, val_str) — helper to push string into stack X
- fn get_x(state) -> f64 — helper to read stack X as f64
- fn test_numerical_accuracy_suite() — main test fn, 768 cases, >= 98% gate
- v1.x baseline floor: first 503 cases, pinned failing set [124, 279, 344, 438, 480]
- v3.0 floor: 768 cases, 763 passing (99.3%)

From hp41-core/src/ops/stat1/modal.rs:
- Stat1Step::NormdModeChoice — mode 1=CDF, 2=PDF, 3=inverse
- Stat1Step::ChisqdNuPrompt — reads X as nu
- Stat1Step::ChisqdModeChoice — mode 1=PDF, 2=CDF
- submit_step(state, step) -> Result<(), HpError>

From hp41-core/src/ops/mod.rs:
- Op::SigmaNormdWorkflow, Op::SigmaChisqdWorkflow, Op::SigmaSpear, Op::SigmaBstat, etc.
- dispatch(state, op) -> Result<(), HpError>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ITER_TOL constant and `iter` macro arm</name>
  <files>hp41-core/tests/numerical_accuracy.rs</files>
  <read_first>
    - hp41-core/tests/numerical_accuracy.rs (lines 1-130 for constants and macro definition)
  </read_first>
  <action>
    Add to `hp41-core/tests/numerical_accuracy.rs`:

    1. A new constant `ITER_TOL: f64 = 1e-7` placed after the existing `WIDE_TOL` constant. Add a comment: `// STAT-QUAL-05: iterative-path tolerance tier (1e-9 closed-form, 1e-7 iterative)`

    2. A new `iter` arm to the `case!` macro, appended after the existing `wide` arm. The arm matches `($domain:expr, $desc:expr, $expected:expr, $actual:expr, iter)` and uses `tol: ITER_TOL`. The body is identical to the `wide` arm except for the tolerance value.

    These additions do NOT modify any existing cases or the gate logic. The existing `TOLERANCE`, `WIDE_TOL`, `default` arm, and `wide` arm remain unchanged. The new `iter` arm is used exclusively by the Stat 1 cases added in Task 2.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test numerical_accuracy -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/numerical_accuracy.rs contains `const ITER_TOL: f64 = 1e-7`
    - The `case!` macro has a third arm matching the `iter` keyword
    - Existing 768 cases still pass at >= 98% rate (no regression from macro change)
    - `cargo test -p hp41-core --test numerical_accuracy` exits 0
  </acceptance_criteria>
  <done>ITER_TOL constant and `iter` macro arm installed; existing 768-case suite unaffected</done>
</task>

<task type="auto">
  <name>Task 2: Append ~30 Stat 1 scipy-derived accuracy cases (STAT-QUAL-04)</name>
  <files>hp41-core/tests/numerical_accuracy.rs</files>
  <read_first>
    - hp41-core/tests/numerical_accuracy.rs (lines 6900-6950 for the end of existing cases and the gate logic section; lines 100-130 for case! macro usage examples)
    - hp41-core/src/ops/stat1/modal.rs (submit_step for ΣNORMD and ΣCHISQD modal flow — needed to set up multi-step accuracy cases)
    - hp41-core/src/ops/stat1/normd.rs (op_sigma_normd_eval_cdf, _pdf, _inverse — for understanding call flow)
    - .planning/phases/33-hp41-core-xrom-activation-distribution-primitives-all-stat-1/33-SPEC-AMENDMENT.md (6 corrected oracle values — accuracy cases must use the corrected values)
  </read_first>
  <action>
    Append ~30 `case!()` entries AFTER the existing 768 cases in `hp41-core/tests/numerical_accuracy.rs`, inside the same `test_numerical_accuracy_suite` test function. The new block starts with a section comment: `// ── Stat 1 Pac (XROM 2) accuracy cases — scipy-derived per D-37.1 ──`

    CRITICAL: Every oracle reference value must be FRESHLY DERIVED from a Python scipy.stats session (D-37.1). Run the following scipy derivations before writing cases:

    ```python
    from scipy.stats import norm, chi2, t, spearmanr
    import numpy as np
    # ΣNORMD CDF/PDF/inverse — see 37-RESEARCH.md §scipy Oracle Derivation
    # ΣCHISQD CDF — chi2.cdf(x, df=nu)
    # ΣTSTAT — t.sf(t_stat, df) for p-value
    # ΣSPEAR — spearmanr(x, y).statistic
    # ΣBSTAT — np.std(x, ddof=1) / np.mean(x) for CV
    ```

    Each case carries three comment lines per established convention:
    - `// Source: scipy.stats.<fn>(<args>) = <value>`
    - `// Free42: N/A — Stat 1 Pac oracle; scipy.stats ground truth per D-37.1`
    - `// Catches: <regression class>`

    Case allocation per D-37.4:

    ΣNORMD CDF (4 cases, `default` arm 1e-9): Q(1.96), Q(0), Q(2.576), Q(-1.96) — upper-tail via mode 1. For each: push z-score via ENTER, dispatch Op::SigmaNormdWorkflow (opens modal), set state.stack.x to mode index 1, call submit_step(state, Stat1Step::NormdModeChoice), read result from state.stack.x.

    ΣNORMD PDF (2 cases, `default` arm 1e-9): phi(0), phi(1) — via mode 2.

    ΣNORMD inverse (4 cases, `iter` arm 1e-7): ppf(0.975), ppf(0.5), ppf(0.025), ppf(0.001) — via mode 3.

    ΣCHISQD CDF (3 cases, `iter` arm 1e-7): P(3.841; nu=1), P(5.991; nu=2), P(7.815; nu=3) — two-step modal: dispatch workflow, submit nu via ChisqdNuPrompt, submit mode=2 via ChisqdModeChoice.

    ΣTSTAT p-value (2 cases, `iter` arm 1e-7): standard two-sample cases — set up Sigma registers with known sample data, dispatch Op::SigmaTstat, read p-value from stack.

    ΣSPEAR (2 cases, `default` arm 1e-9): rank correlation on small datasets — push data pairs via Sigma+, dispatch Op::SigmaSpear, assert rho_s.

    ΣBSTAT/ΣBSTG (3 cases, `default` arm 1e-9): CV, mean, std dev on known datasets.

    ΣLIN/ΣEXP/ΣLOGI/ΣPOW (4 cases, `default` arm 1e-9): slope + intercept on simple datasets.

    ΣXSQEV/ΣEFXSQ (2 cases, `default` arm 1e-9): chi-square goodness-of-fit on known observed/expected.

    ΣMLRXY (2 cases, `iter` arm 1e-7): 2-predictor regression coefficient from normal-equation solve.

    ΣAOVONE (2 cases, `default` arm 1e-9): F-ratio on known group data (use the corrected oracle F=50.0 from 33-SPEC-AMENDMENT.md D-35-03, NOT the original F=100.0).

    Total: ~30 cases. Combined suite ~798 cases. The existing gate logic (ceil(total * 0.98)) automatically adjusts. The v1.x floor (first 503, pinned failures) and v3.0 floor (first 768) are preserved because new cases are appended with higher IDs.

    For multi-step modal cases (ΣNORMD, ΣCHISQD), the executor must manage the modal state machine: dispatch the workflow Op, then manually set stack.x to the mode index and call `hp41_core::ops::stat1::modal::submit_step(state, step)`. Import the `submit_step` function and `Stat1Step` enum.
  </action>
  <verify>
    <automated>cargo test -p hp41-core --test numerical_accuracy -- --nocapture 2>&1 | grep -E "pass|fail|ACCURACY" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hp41-core/tests/numerical_accuracy.rs contains >= 28 new Stat 1 cases (total >= 796)
    - Combined pass rate >= 98% (>= ceil(total * 0.98) passing)
    - v1.x 503-case floor preserved (498/503 or better)
    - v3.0 768-case floor preserved (763/768 or better)
    - All ΣNORMD CDF/PDF cases use `default` arm (1e-9)
    - All ΣNORMD inverse, ΣCHISQD CDF, ΣTSTAT, ΣMLRXY cases use `iter` arm (1e-7)
    - Each case carries the three-line comment block (Source / Free42 / Catches)
    - scipy-derived values used (not Phase 33 inline test values)
    - `cargo test -p hp41-core --test numerical_accuracy` exits 0
  </acceptance_criteria>
  <done>~30 Stat 1 accuracy cases appended; combined suite at >= 98% pass rate; two-level tolerance discipline enforced; STAT-QUAL-04 and STAT-QUAL-05 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test file modification only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
cargo test -p hp41-core --test numerical_accuracy -- --nocapture
</verification>

<success_criteria>
- ~30 Stat 1 cases appended with scipy-derived reference values
- Combined accuracy >= 98%
- Two-level tolerance discipline (1e-9 / 1e-7) enforced via ITER_TOL
- v1.x and v3.0 sub-gates preserved
</success_criteria>

<output>
Create `.planning/phases/37-test-hardening-quality-gates/37-04-SUMMARY.md` when done
</output>
