---
phase: 64-interactive-getkey
plan: 04
type: execute
wave: 3
depends_on: ["64-03"]
files_modified:
  - hp41-gui/src/App.tsx
  - hp41-gui/src/App.test.tsx
autonomous: true
requirements: [PRGM-03]

must_haves:
  truths:
    - "When pending_yield.kind === 'wait_for_key', the GUI yield driver does NOT schedule a setTimeout resume (event-driven, not timer-driven)."
    - "During a wait_for_key suspend, an on-screen key tap or physical keypress invokes resume_program_with_key with the key's HP-41 keyCode (not dispatch_op)."
    - "Keys with no keyCode (CHS, x>=y, left-arrow) are ignored during the wait; the wait continues."
    - "During a wait_for_key suspend, the cancel/Esc path invokes resume_program_with_key({ keycode: 0 }) (sentinel) — detected via pending_yield.kind, not is_running."
  artifacts:
    - path: "hp41-gui/src/App.tsx"
      provides: "yield-driver wait_for_key early return + key-event resume guard (on-screen + physical) + cancel-to-0 branch"
      contains: "wait_for_key"
    - path: "hp41-gui/src/App.test.tsx"
      provides: "Group Q tests PRGM-03-k and PRGM-03-l"
      contains: "getkey_yield_driver_skips_wait_for_key"
  key_links:
    - from: "hp41-gui/src/App.tsx (invokeForKey / handleKey guard)"
      to: "Tauri resume_program_with_key"
      via: "invoke('resume_program_with_key', { keycode })"
      pattern: "resume_program_with_key"
---

<objective>
Wire the GUI TypeScript frontend for interactive GETKEY: make the no-poll yield driver skip scheduling a timer for wait_for_key, route the next key event (on-screen tap + physical keyboard) to resume_program_with_key with the key's HP-41 keyCode, ignore keys with no keyCode, and route Esc/cancel during the suspend to resume_program_with_key({ keycode: 0 }). Add the Group Q Vitest tests.

Purpose: Closes the GUI D-04 frontend gap and completes CLI↔GUI parity (D-25.6) for PRGM-03. Honors D-11 (no polling) by routing resume through key events, not get_state.
Output: App.tsx guards + App.test.tsx Group Q (PRGM-03-k, PRGM-03-l).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-interactive-getkey/64-RESEARCH.md
@.planning/phases/64-interactive-getkey/64-PATTERNS.md
@.planning/phases/64-interactive-getkey/64-03-SUMMARY.md
@CLAUDE.md

<interfaces>
hp41-gui/src/App.tsx:
- CalcStateView TS type (line ~74): pending_yield: { kind: string; text: string; resume_ms: number } | null.
- yield-driver useEffect (lines ~592-606): currently returns early if no pending_yield, else guards resumeScheduledRef, schedules setTimeout(resume_ms) -> invoke('resume_program'). Add `if (calcState.pending_yield.kind === 'wait_for_key') return;` before the resumeScheduledRef guard.
- invokeForKey (line ~115+): the key-dispatch routing chain (resolves key id, then invoke('dispatch_op', ...)). Add a wait_for_key guard at the TOP that routes to resume_program_with_key using the key's keyCode.
- handleKey (line ~938+): physical-keyboard handler. Add the same wait_for_key guard, looking up keyCode via KEY_DEFS.find(k => k.id === resolvedId)?.keyCode.
- R/S / cancel handler: existing guard uses is_running; during GETKEY is_running is false — branch on calcState?.pending_yield?.kind === 'wait_for_key' instead, calling resume_program_with_key({ keycode: 0 }).
- setCalcState, setErrorMessage, showToast, extractErrMessage helpers exist.
- KEY_DEFS (from Keyboard.tsx) — each entry has id + optional keyCode (HP-41 row×col). R/S keyCode = 31; ENTER keyCode = 84; CHS / x>=y / left-arrow keyCode undefined.

hp41-gui/src/App.test.tsx:
- existing Vitest groups; globals:false -> afterEach(cleanup) for portaled nodes. Tauri invoke is mocked. Mirror the Phase 63 yield-driver tests for the new Group Q.

Tauri command (from Plan 03): resume_program_with_key({ keycode: number }) -> CalcStateView.
</interfaces>

LOCKED: R/S keyCode = 31. Keys without keyCode are no-ops during the wait (Pitfall 3/4). D-11: never poll get_state — resume only via key events / command return. D-25.6 parity with the CLI (Plan 02).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Yield-driver wait_for_key skip + key-event resume guard (on-screen + physical) + cancel-to-0</name>
  <read_first>
    - hp41-gui/src/App.tsx (yield-driver useEffect ~592-606; invokeForKey ~115; handleKey ~938; R/S/cancel handler; CalcStateView type ~74)
    - hp41-gui/src/Keyboard.tsx (KEY_DEFS — id + keyCode fields; R/S=31, ENTER=84, undefined for CHS/x>=y/left-arrow)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (App.tsx section — all four guards: useEffect early return, invokeForKey guard, handleKey guard, cancel branch)
    - .planning/phases/64-interactive-getkey/64-RESEARCH.md (GUI Capture Path; Pitfalls 2, 3, 4)
  </read_first>
  <action>
    In hp41-gui/src/App.tsx: (1) yield-driver useEffect — add `if (calcState.pending_yield.kind === 'wait_for_key') return;` immediately after the `if (!calcState?.pending_yield) return;` line and BEFORE the resumeScheduledRef guard (Pitfall 2: timer path must never resume an event-driven yield). (2) invokeForKey — at the TOP of the body, before all existing rule branches: if `calcState?.pending_yield?.kind === 'wait_for_key'`, look up the key's HP-41 code (the keyCode of the resolved key id) and, if defined, `return invoke('resume_program_with_key', { keycode: hp41Code }).then(view => { setCalcState(view); setErrorMessage(null); }).catch(err => showToast(extractErrMessage(err)));`; if the key has no keyCode, return a resolved no-op (wait continues). (3) handleKey (physical) — same guard at the top, resolving keyCode via `KEY_DEFS.find(k => k.id === resolvedId)?.keyCode`; no-op when undefined. (4) cancel/Esc handler — before the existing `is_running`-based request_cancel call, add: if `calcState?.pending_yield?.kind === 'wait_for_key'`, `invoke('resume_program_with_key', { keycode: 0 })` (sentinel) then update state and return. Do NOT add any get_state polling (D-11).
  </action>
  <verify>
    <automated>rtk grep -c "wait_for_key\|resume_program_with_key" hp41-gui/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx yield-driver useEffect early-returns when pending_yield.kind === 'wait_for_key' (no setTimeout scheduled).
    - invokeForKey and handleKey route to invoke('resume_program_with_key', { keycode }) when pending_yield.kind === 'wait_for_key', using the key's keyCode; keys with undefined keyCode are no-ops.
    - The cancel/Esc path calls invoke('resume_program_with_key', { keycode: 0 }) when pending_yield.kind === 'wait_for_key' (branch on pending_yield.kind, not is_running).
    - No get_state polling added (D-11).
    - rtk grep -c "resume_program_with_key" hp41-gui/src/App.tsx returns >= 1.
  </acceptance_criteria>
  <done>GUI yield driver skips the timer for wait_for_key; key events (on-screen + physical) resume with the keyCode; unmapped keys ignored; Esc/cancel resumes with sentinel 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Group Q Vitest tests — PRGM-03-k and PRGM-03-l</name>
  <read_first>
    - hp41-gui/src/App.test.tsx (existing groups; Tauri invoke mock setup; afterEach(cleanup) for globals:false)
    - hp41-gui/src/App.tsx (the guards added in Task 1)
    - .planning/phases/64-interactive-getkey/64-VALIDATION.md (PRGM-03-k getkey_yield_driver_skips_wait_for_key; PRGM-03-l getkey_key_press_routes_to_resume)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (No-Poll Yield Driver Pattern)
  </read_first>
  <action>
    In hp41-gui/src/App.test.tsx add a "Group Q" describe block with two tests: (k) getkey_yield_driver_skips_wait_for_key — render App with calcState having pending_yield.kind === 'wait_for_key', assert that no resume_program call is scheduled/invoked via a timer (use fake timers; advance time; assert the resume_program mock was NOT called for a wait_for_key yield). (l) getkey_key_press_routes_to_resume — with a wait_for_key pending_yield, simulate a key event (on-screen tap or physical key with a defined keyCode) and assert the Tauri mock recorded an invoke('resume_program_with_key', { keycode: <expected> }) call and NOT invoke('dispatch_op', ...). Mirror the existing Tauri-invoke mock assertions; keep afterEach(cleanup).
  </action>
  <verify>
    <automated>just gui-ci -- getkey_yield_driver_skips_wait_for_key getkey_key_press_routes_to_resume</automated>
  </verify>
  <acceptance_criteria>
    - App.test.tsx contains tests named getkey_yield_driver_skips_wait_for_key and getkey_key_press_routes_to_resume.
    - PRGM-03-k asserts no timer-based resume_program is scheduled for a wait_for_key yield.
    - PRGM-03-l asserts a key press during wait_for_key invokes resume_program_with_key (with the correct keycode) and not dispatch_op.
    - just gui-ci -- getkey_yield_driver_skips_wait_for_key getkey_key_press_routes_to_resume exits 0.
  </acceptance_criteria>
  <done>Group Q tests green; PRGM-03-k and PRGM-03-l covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key event (DOM / on-screen) -> App.tsx -> Tauri IPC | a keyCode crosses from the UI into resume_program_with_key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-TS-01 | Tampering | keyCode passed to resume_program_with_key | accept | keyCode comes from the static KEY_DEFS table (11-85) or is undefined (ignored); the Rust command types it as u8; bounded. |
| T-64-TS-02 | DoS | yield-driver duplicate scheduling | mitigate | Early return when kind === 'wait_for_key' prevents resumeScheduledRef timer churn (Pitfall 2). |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- just gui-ci green (Group Q PRGM-03-k/l + existing suite).
- No get_state polling added (D-11); R/S keyCode = 31; CLI↔GUI parity (D-25.6) with Plan 02.
- Manual (VALIDATION.md): just gui-dev — run a GETKEY program; tap on-screen + press physical keys -> both resume with the correct code; confirm CHS / x>=y / left-arrow taps are ignored.
</verification>

<success_criteria>
- Yield driver ignores wait_for_key (no timer); key events resume with the keyCode; unmapped keys ignored; Esc/cancel resumes with sentinel 0.
- Group Q tests green; full GUI parity with CLI behavior.
</success_criteria>

<output>
Create `.planning/phases/64-interactive-getkey/64-04-SUMMARY.md` when done.
</output>
