---
phase: 64-interactive-getkey
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - hp41-gui/src-tauri/src/types.rs
  - hp41-gui/src-tauri/src/commands.rs
  - hp41-gui/src-tauri/src/lib.rs
  - hp41-gui/src-tauri/permissions/allow-resume-program-with-key.toml
  - hp41-gui/src-tauri/capabilities/default.json
autonomous: true
requirements: [PRGM-03]

must_haves:
  truths:
    - "The GUI CalcStateView projects a WaitForKey yield as kind \"wait_for_key\" with text \"\" and resume_ms 0."
    - "A resume_program_with_key Tauri command threads a captured keycode (u8) into hp41-core and returns the updated CalcStateView."
    - "The command is registered in the invoke_handler and permitted via a permission TOML + capability entry."
  artifacts:
    - path: "hp41-gui/src-tauri/src/types.rs"
      provides: "WaitForKey to wait_for_key projection arm + from_state_projects_wait_for_key test"
      contains: "wait_for_key"
    - path: "hp41-gui/src-tauri/src/commands.rs"
      provides: "resume_program_with_key SC-4 thin-glue command"
      contains: "resume_program_with_key"
    - path: "hp41-gui/src-tauri/src/lib.rs"
      provides: "command registration in invoke_handler"
      contains: "resume_program_with_key"
    - path: "hp41-gui/src-tauri/permissions/allow-resume-program-with-key.toml"
      provides: "Tauri v2 permission for the command"
      contains: "allow-resume-program-with-key"
    - path: "hp41-gui/src-tauri/capabilities/default.json"
      provides: "capability reference to the new permission"
      contains: "allow-resume-program-with-key"
  key_links:
    - from: "hp41-gui/src-tauri/src/commands.rs (resume_program_with_key)"
      to: "hp41-core resume_program_with_key"
      via: "hp41_core::ops::program::resume_program_with_key(&mut calc, keycode)"
      pattern: "resume_program_with_key"
    - from: "hp41-gui/src-tauri/src/types.rs (YieldView::from_yield_state)"
      to: "hp41-core YieldKind::WaitForKey"
      via: "match arm YieldKind::WaitForKey => wait_for_key"
      pattern: "wait_for_key"
---

<objective>
Build the GUI-Rust (Tauri) half of interactive GETKEY: project the WaitForKey yield kind to the frontend as "wait_for_key", add the resume_program_with_key Tauri command (SC-4 thin glue), register it in the invoke_handler, and grant it via a permission TOML + capability entry.

Purpose: Closes the GUI D-04 gap — the GUI currently never reports a row×col code. This plan gives the TS frontend (Plan 04) the IPC surface it needs.
Output: types.rs projection arm + test, resume_program_with_key command, lib.rs registration, permission TOML, capability entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-interactive-getkey/64-RESEARCH.md
@.planning/phases/64-interactive-getkey/64-PATTERNS.md
@.planning/phases/64-interactive-getkey/64-01-SUMMARY.md
@CLAUDE.md

<interfaces>
hp41-gui/src-tauri/src/types.rs:
- YieldView struct { kind: String, text: String, resume_ms: u64 } (lines 62-66) — no struct change needed.
- YieldView::from_yield_state (68-81): kind match Pse=>"pse" / View=>"view" / Aview=>"aview" (70-75) — add YieldKind::WaitForKey => "wait_for_key".
- CalcStateView::from_state exists; test from_state_projects_pending_yield_when_set (~line 1373) is the analog for the new from_state_projects_wait_for_key test.

hp41-gui/src-tauri/src/commands.rs:
- resume_program command (416-423): #[tauri::command] pub fn resume_program(state: State<'_, AppState>) -> Result<CalcStateView, GuiError> — lock (state.lock().unwrap_or_else(|e| e.into_inner())) -> hp41_core::ops::program::resume_program(&mut calc).map_err(GuiError::from)? -> drain print_buffer + event_buffer -> CalcStateView::from_state(&calc, print_lines, event_lines).

hp41-gui/src-tauri/src/lib.rs:
- Phase 63 registration block (261-263): commands::run_program, commands::resume_program, in the invoke_handler list.

hp41-gui/src-tauri/permissions/resume-program.toml (analog):
- "$schema" = "../gen/schemas/desktop-schema.json" + [[permission]] identifier = "allow-resume-program" ... commands.allow = ["resume_program"].

hp41-gui/src-tauri/capabilities/default.json:
- contains "allow-resume-program" (~line 29) — add "allow-resume-program-with-key" after it.

hp41-core (from Plan 01): hp41_core::ops::program::resume_program_with_key(&mut calc, keycode: u8), hp41_core::state::YieldKind::WaitForKey.
</interfaces>

LOCKED: SC-4 (no calculator logic in src-tauri) — command is pure lock->core->drain->from_state. Tauri v2 param order: custom params (keycode) FIRST, State extractor LAST. Run cargo check first to generate the permission registry before referencing the new permission.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project WaitForKey as wait_for_key + from_state_projects_wait_for_key test (PRGM-03-j)</name>
  <read_first>
    - hp41-gui/src-tauri/src/types.rs (YieldView 62-66, from_yield_state 68-81, from_state_projects_pending_yield_when_set ~1373)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (types.rs section — extended match + the from_state_projects_wait_for_key test)
    - .planning/phases/64-interactive-getkey/64-VALIDATION.md (PRGM-03-j row)
  </read_first>
  <action>
    In hp41-gui/src-tauri/src/types.rs add the YieldKind::WaitForKey => "wait_for_key" arm to the kind match in YieldView::from_yield_state (no struct change — text is "" and resume_ms is 0 for WaitForKey). Add a unit test from_state_projects_wait_for_key mirroring from_state_projects_pending_yield_when_set: construct a CalcState with pending_yield = Some(YieldState { kind: YieldKind::WaitForKey, text: String::new(), resume_ms: 0 }), call CalcStateView::from_state, assert the projected pending_yield kind == "wait_for_key", resume_ms == 0, text == "".
  </action>
  <verify>
    <automated>cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml -- from_state_projects_wait_for_key</automated>
  </verify>
  <acceptance_criteria>
    - types.rs from_yield_state has a YieldKind::WaitForKey => "wait_for_key" match arm (exhaustive match compiles).
    - Test from_state_projects_wait_for_key asserts kind == "wait_for_key", resume_ms == 0, text == "".
    - cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml -- from_state_projects_wait_for_key exits 0 (PRGM-03-j green).
  </acceptance_criteria>
  <done>WaitForKey projects as wait_for_key; PRGM-03-j test green.</done>
</task>

<task type="auto">
  <name>Task 2: resume_program_with_key Tauri command + invoke_handler registration</name>
  <read_first>
    - hp41-gui/src-tauri/src/commands.rs (resume_program 416-423 — SC-4 pattern)
    - hp41-gui/src-tauri/src/lib.rs (Phase 63 registration block 261-263)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (commands.rs section + lib.rs section)
  </read_first>
  <action>
    In hp41-gui/src-tauri/src/commands.rs add #[tauri::command] pub fn resume_program_with_key(keycode: u8, state: State<'_, AppState>) -> Result<CalcStateView, GuiError> as a direct sibling of resume_program: lock the AppState (state.lock().unwrap_or_else(|e| e.into_inner())), call hp41_core::ops::program::resume_program_with_key(&mut calc, keycode).map_err(GuiError::from)?, drain print_buffer + event_buffer into Vec<String>, return CalcStateView::from_state(&calc, print_lines, event_lines). Keep custom param keycode FIRST, State LAST (Tauri v2). In hp41-gui/src-tauri/src/lib.rs add commands::resume_program_with_key, to the invoke_handler list after commands::resume_program, (Phase 64 comment). No calculator logic in the command (SC-4).
  </action>
  <verify>
    <automated>cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - commands.rs defines #[tauri::command] pub fn resume_program_with_key(keycode: u8, state: State<'_, AppState>) that only locks -> calls core resume_program_with_key -> drains buffers -> from_state (SC-4: no calculator logic; the SC-4 grep guard `grep -rn "fn op_(add|sub|...)" hp41-gui/src-tauri/src/` must still be empty).
    - lib.rs invoke_handler lists commands::resume_program_with_key.
    - cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml finishes without errors.
  </acceptance_criteria>
  <done>resume_program_with_key command exists, SC-4-clean, registered in invoke_handler; cargo check passes (generates the permission registry for Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Permission TOML + capability entry + iOS-target check</name>
  <read_first>
    - hp41-gui/src-tauri/permissions/resume-program.toml (analog file shape)
    - hp41-gui/src-tauri/capabilities/default.json (the allow-resume-program entry ~line 29)
    - .planning/phases/64-interactive-getkey/64-PATTERNS.md (permissions TOML section + capabilities note)
    - CLAUDE.md (Tauri v2.11 permissions; cfg(mobile) blind spot)
  </read_first>
  <action>
    Create hp41-gui/src-tauri/permissions/allow-resume-program-with-key.toml mirroring resume-program.toml: "$schema" = "../gen/schemas/desktop-schema.json", a [[permission]] with identifier = "allow-resume-program-with-key", a description, and commands.allow = ["resume_program_with_key"]. Add "allow-resume-program-with-key" to hp41-gui/src-tauri/capabilities/default.json after "allow-resume-program". Verify the GUI builds for host AND that the shared command code compiles for the iOS target (cfg(mobile) blind spot — just gui-ci host target cannot see #[cfg(mobile)] errors).
  </action>
  <verify>
    <automated>cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml && cargo check --manifest-path hp41-gui/src-tauri/Cargo.toml --target aarch64-apple-ios</automated>
  </verify>
  <acceptance_criteria>
    - hp41-gui/src-tauri/permissions/allow-resume-program-with-key.toml exists with identifier allow-resume-program-with-key and commands.allow = ["resume_program_with_key"].
    - capabilities/default.json contains "allow-resume-program-with-key".
    - cargo check (host) AND cargo check --target aarch64-apple-ios both finish without errors (cfg(mobile) verified per CLAUDE.md acceptance note).
  </acceptance_criteria>
  <done>Permission + capability wired; command callable from the frontend; host and iOS-target compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend (WebView IPC) -> Tauri command -> core | keycode u8 crosses the IPC boundary into resume_program_with_key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-GUI-01 | Tampering | resume_program_with_key keycode (IPC) | accept | Typed as u8 (0-255); HP-41 codes are 11-85; value pushed to X only, no dispatch on it. Bounded by type. |
| T-64-GUI-02 | Elevation of Privilege | new Tauri command exposure | mitigate | Command gated by allow-resume-program-with-key permission referenced in capabilities/default.json (least-privilege, mirrors resume_program). |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml passes (incl. from_state_projects_wait_for_key — PRGM-03-j).
- cargo check (host) and cargo check --target aarch64-apple-ios both clean.
- SC-4 grep guard stays empty.
- just gui-ci green at phase gate.
</verification>

<success_criteria>
- WaitForKey projects as "wait_for_key" (text "", resume_ms 0).
- resume_program_with_key command threads the keycode into core and returns CalcStateView; registered + permitted + capability-referenced.
- Host and iOS targets compile clean.
</success_criteria>

<output>
Create `.planning/phases/64-interactive-getkey/64-03-SUMMARY.md` when done.
</output>
