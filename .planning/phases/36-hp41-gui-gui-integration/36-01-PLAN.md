---
phase: 36-hp41-gui-gui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - hp41-gui/src-tauri/src/prgm_display.rs
  - hp41-core/src/ops/program.rs
  - hp41-core/tests/op_catalog_xrom.rs
  - hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs
autonomous: true
requirements:
  - STAT-GUI-01
  - STAT-GUI-02
  - STAT-GUI-05

must_haves:
  truths:
    - "GUI prgm_display.rs compiles without non-exhaustive patterns warning — all 26 Stat 1 Op variants have explicit arms"
    - "CATALOG 2 lists both MATH 1A and STAT 1B when both XROM bits are set"
    - "STAT-GUI-05 is reassigned from Phase 36 to Phase 37 in REQUIREMENTS.md and ROADMAP.md"
    - "SC-4 invariant holds — grep for op_* math functions in hp41-gui/src-tauri/src/ returns empty"
  artifacts:
    - path: "hp41-gui/src-tauri/src/prgm_display.rs"
      provides: "26 new Op::Sigma*/Rand/Seed display name arms"
      contains: "Op::SigmaBstat"
    - path: "hp41-core/src/ops/program.rs"
      provides: "CATALOG 2 bit-1 conditional block for STAT_1"
      contains: "STAT_1.name"
    - path: "hp41-core/tests/op_catalog_xrom.rs"
      provides: "catalog_2_lists_stat1_when_bit1_set test"
      contains: "STAT 1B"
  key_links:
    - from: "hp41-gui/src-tauri/src/prgm_display.rs"
      to: "hp41-core/src/ops/mod.rs"
      via: "exhaustive match on Op enum"
      pattern: "Op::SigmaBstat"
    - from: "hp41-core/src/ops/program.rs"
      to: "hp41-core/src/ops/math1/xrom.rs"
      via: "STAT_1 const import"
      pattern: "use.*xrom.*STAT_1"
---

<objective>
Close the 4-way exhaustive-match invariant item 4 (GUI prgm_display.rs) by adding 26 Stat 1
Op display-name arms, extend CATALOG 2 with STAT_1 enumeration via a surgical hp41-core touch,
and reassign STAT-GUI-05 to Phase 37 per D-36.2/D-36.3.

Purpose: Seals the intentional `non-exhaustive patterns` CI break in hp41-gui that has been
open since Phase 33 shipped. After this plan, `just gui-ci` exits 0 and CATALOG 2 shows both
XROM modules to the user.

Output: GUI compiles clean; CATALOG 2 test green; REQUIREMENTS.md + ROADMAP.md updated.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-hp41-gui-gui-integration/36-CONTEXT.md
@.planning/phases/36-hp41-gui-gui-integration/36-RESEARCH.md
@.planning/phases/36-hp41-gui-gui-integration/36-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hp41-core/src/ops/mod.rs (the 26 new Stat 1 Op variants to match):
  Op::SigmaBstat, Op::SigmaBstg, Op::SigmaMmtug, Op::SigmaMmtgd,
  Op::SigmaAovone, Op::SigmaAovtwo, Op::SigmaAnocov,
  Op::SigmaLin, Op::SigmaExp, Op::SigmaLogi, Op::SigmaPow,
  Op::SigmaMlrxy, Op::SigmaMlrxyz, Op::SigmaPolypWorkflow, Op::SigmaPolyc,
  Op::SigmaPtst, Op::SigmaTstat,
  Op::SigmaXsqev, Op::SigmaEfxsq, Op::SigmaCtkkk, Op::SigmaCtkk, Op::SigmaSpear,
  Op::SigmaNormdWorkflow, Op::SigmaChisqdWorkflow,
  Op::Rand, Op::Seed

From hp41-core/src/ops/math1/xrom.rs:
  pub const STAT_1: XromModule  (id = 2, name = "STAT 1B", ops = 26-entry slice)
  pub const MATH_1: XromModule  (id = 7, name = "MATH 1A")

From hp41-core/src/ops/program.rs (line 301):
  pub fn op_catalog(state: &mut CalcState, n: u8) -> Result<(), HpError>

From hp41-cli/src/prgm_display.rs (lines 295-327 — the source-of-truth for GUI arm strings):
  Op::SigmaBstat => "\u{03A3}BSTAT".to_string(),
  Op::SigmaBstg  => "\u{03A3}BSTG".to_string(),
  ... (26 arms total — copy verbatim per D-25.6 parity invariant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reassign STAT-GUI-05 Phase 36 to Phase 37 (D-36.3 bookkeeping)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find STAT-GUI-05 at line 91, traceability row 222)
    - .planning/ROADMAP.md (Phase 36 section lines 158-171, Phase 37 section lines 173-186)
    - .planning/phases/36-hp41-gui-gui-integration/36-CONTEXT.md (D-36.2, D-36.3 rationale)
  </read_first>
  <action>
    Update REQUIREMENTS.md:
    - Line 91 (STAT-GUI-05 requirement text): append to the existing text: " -- reassessed Phase 36 planning: bounded 50-iter primitives (Acklam closed-form / gser+gcf with ITER_CAP=50) do not need cancellation; deferred to Phase 37 STAT-QUAL block per 36-CONTEXT D-36.2"
    - Traceability table row for STAT-GUI-05 (currently shows Phase 36 / Pending): change Phase column from "Phase 36" to "Phase 37"

    Update ROADMAP.md:
    - Phase 36 Success Criteria item #4 (currently about ΣNORMD inverse cancellation): replace with: "4. STAT-GUI-05 (request_cancel reuse) reassigned to Phase 37 per D-36.2 — bounded 50-iter distribution primitives complete in microseconds; no cancellation needed at this timescale"
    - Phase 37 Requirements line: append ", STAT-GUI-05" after the existing "STAT-QUAL-01, ..., STAT-QUAL-11"
    - Phase 36 Plans line: update from "TBD" to "3 plans"
    - Add plan list under Phase 36 per the v3.1 plan-list convention
  </action>
  <verify>
    <automated>grep "Phase 37" .planning/REQUIREMENTS.md | grep -c "STAT-GUI-05"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md STAT-GUI-05 traceability row shows "Phase 37" (not "Phase 36")
    - REQUIREMENTS.md STAT-GUI-05 requirement text includes "deferred to Phase 37 STAT-QUAL block per 36-CONTEXT D-36.2"
    - ROADMAP.md Phase 36 success criterion #4 contains "reassigned to Phase 37 per D-36.2"
    - ROADMAP.md Phase 37 Requirements line includes "STAT-GUI-05"
    - ROADMAP.md Phase 36 Plans shows "3 plans"
  </acceptance_criteria>
  <done>STAT-GUI-05 reassigned from Phase 36 to Phase 37 in both REQUIREMENTS.md and ROADMAP.md with bounded-iter rationale documented</done>
</task>

<task type="auto">
  <name>Task 2: Add 26 Stat 1 op_display_name arms to GUI prgm_display.rs + CATALOG 2 bit-1 block + tests</name>
  <files>hp41-gui/src-tauri/src/prgm_display.rs, hp41-core/src/ops/program.rs, hp41-core/tests/op_catalog_xrom.rs, hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs</files>
  <read_first>
    - hp41-gui/src-tauri/src/prgm_display.rs (full file — see current arms ending at line 312 with Op::Trans3d, header comment at line 46)
    - hp41-cli/src/prgm_display.rs (lines 295-327 — the 26 Stat 1 arm strings to copy verbatim)
    - hp41-core/src/ops/program.rs (lines 295-365 — op_catalog function, bit-0 block at lines 343-354)
    - hp41-core/tests/op_catalog_xrom.rs (full file — existing test structure, imports)
    - hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs (full file — MATH1_VARIANT_IDS array + no-catch-all scan)
    - hp41-core/src/ops/math1/xrom.rs (lines 120-190 — STAT_1 const, id, name, ops)
  </read_first>
  <action>
    **Part A — GUI prgm_display.rs (26 arms, D-25.6 parity, STAT-GUI-01):**

    1. Update the file header comment at line 46 from "Covers all 35 Op variants exhaustively" to "Covers all Op variants exhaustively (v2.2 built-ins + Math Pac I + Stat 1 Pac)." Do NOT use a specific count number (it goes stale too easily).

    2. Insert 26 new match arms immediately before the closing `}` of the `match op {` block (after line 312 `Op::Trans3d => "T3D".to_string(),`). Copy the EXACT arm strings from `hp41-cli/src/prgm_display.rs` lines 295-327 verbatim per D-25.6 parity invariant. The 26 arms are:
       - SigmaBstat through SigmaMmtgd (4 univariate/bivariate)
       - SigmaAovone through SigmaAnocov (3 ANOVA)
       - SigmaLin through SigmaPolyc (9 curve fitting + regression)
       - SigmaPtst, SigmaTstat (2 hypothesis tests)
       - SigmaXsqev through SigmaSpear (5 nonparam/chi-sq/contingency)
       - SigmaNormdWorkflow, SigmaChisqdWorkflow (2 distributions)
       - Rand, Seed (2 RNG)
       Include the same section-separator comments as the CLI file.

    3. Do NOT add a `_ =>` catch-all. The exhaustive match IS the invariant.

    **Part B — hp41-core op_catalog bit-1 block (D-36.1, STAT-GUI-02):**

    4. In `hp41-core/src/ops/program.rs`, update the import at line 18 from `use crate::ops::math1::xrom::MATH_1;` to `use crate::ops::math1::xrom::{MATH_1, STAT_1};`.

    5. In the `op_catalog` function, restructure the existing `if/else` at lines 343-354 into two parallel `if` blocks plus a combined `else` fallback:
       - Keep the existing bit-0 MATH_1 block as-is but remove its trailing `else`.
       - Add a new STAT_1 bit-1 block as a sibling `if state.xrom_modules & 0b0000_0010 != 0 { ... }` immediately after, using the same push-to-print_buffer pattern (push header line `format!("XROM {} {}", STAT_1.id, STAT_1.name)`, then iterate `STAT_1.ops` pushing each name).
       - Add the "NO XROM" fallback as `else if state.xrom_modules & 0b0000_0001 == 0 { ... }` attached to the STAT_1 block — fires only when BOTH bits are clear. See 36-PATTERNS.md Pattern 2 for the exact restructuring. Pitfall 3: do NOT nest the STAT_1 block inside the MATH_1 else branch.

    **Part C — Tests:**

    6. In `hp41-core/tests/op_catalog_xrom.rs`, add import `STAT_1` alongside existing `MATH_1` import. Add a new test function `catalog_2_lists_stat1_when_bit1_set` that:
       - Creates `CalcState { xrom_modules: 0b0000_0011, ..CalcState::default() }`.
       - Calls `op_catalog(&mut state, 2).unwrap()`.
       - Asserts `print_buffer` contains "MATH 1A" (bit-0 module header) AND "STAT 1B" (bit-1 module header).
       - Spot-checks one entry from each pac: "Y^X" for Math 1, "\u{03A3}BSTAT" for Stat 1.

    7. In `hp41-gui/src-tauri/tests/prgm_display_math1_arms.rs`, add a `STAT1_VARIANT_IDS` const array listing all 26 Stat 1 Op variant identifiers: "SigmaBstat", "SigmaBstg", "SigmaMmtug", "SigmaMmtgd", "SigmaAovone", "SigmaAovtwo", "SigmaAnocov", "SigmaLin", "SigmaExp", "SigmaLogi", "SigmaPow", "SigmaMlrxy", "SigmaMlrxyz", "SigmaPolypWorkflow", "SigmaPolyc", "SigmaPtst", "SigmaTstat", "SigmaXsqev", "SigmaEfxsq", "SigmaCtkkk", "SigmaCtkk", "SigmaSpear", "SigmaNormdWorkflow", "SigmaChisqdWorkflow", "Rand", "Seed". Add a test `every_stat1_op_appears_in_prgm_display` that loops over `STAT1_VARIANT_IDS`, checks each appears as `Op::<Id>` in `PRGM_DISPLAY_SRC`, and collects any missing into a failure message. Mirror the existing `every_math1_op_appears_in_prgm_display` test structure.
  </action>
  <verify>
    <automated>cargo check -p hp41-gui 2>&1 | grep -c "non-exhaustive" && echo "FAIL: non-exhaustive patterns still present" || echo "PASS: no non-exhaustive warning" ; cargo test -p hp41-core catalog_2_lists_stat1 -- --nocapture ; cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display -- --nocapture ; grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/ && echo "FAIL: SC-4 violation" || echo "PASS: SC-4 clean"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p hp41-gui` emits zero `non-exhaustive patterns` warnings (4-way invariant item 4 sealed)
    - `cargo test -p hp41-core catalog_2_lists_stat1` passes — print_buffer contains both "MATH 1A" and "STAT 1B"
    - `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display` passes — both Math 1 and Stat 1 variant IDs found, no `_ =>` catch-all detected
    - `grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/` returns empty (SC-4 clean)
    - GUI prgm_display.rs contains exactly 201 `Op::` arm lines (175 existing + 26 new), matching CLI parity
    - op_catalog function in program.rs contains two parallel `if` blocks (bit-0 and bit-1), NOT nested if/else-if
  </acceptance_criteria>
  <done>4-way exhaustive-match invariant item 4 sealed; CATALOG 2 lists STAT 1B alongside MATH 1A; SC-4 holds; file-text-scan test covers all 26 Stat 1 variant IDs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 36 Plan 36-01 is display-layer string additions + test code; no new authentication, input validation, or IPC surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | prgm_display.rs Op arm strings | accept | Pure string lookup table; no user input flows into these strings; copy verbatim from CLI per D-25.6 |
| T-36-02 | Information Disclosure | op_catalog print_buffer | accept | CATALOG 2 enumerates XROM module names already visible to the user via XEQ; no secrets |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in Phase 36 |
</threat_model>

<verification>
cargo check -p hp41-gui 2>&1 | grep -v "warning" | tail -5
cargo test -p hp41-core catalog_2
cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display
grep -rn "fn op_(add|sub|mul|div|sin|cos|tan|sto|rcl|flush_entry|format_hpnum)" hp41-gui/src-tauri/src/
</verification>

<success_criteria>
- `cargo check -p hp41-gui` exits 0 with zero non-exhaustive warnings
- `cargo test -p hp41-core catalog_2` — all tests pass including the new stat1 test
- `cargo test --manifest-path hp41-gui/src-tauri/Cargo.toml prgm_display` — all tests pass
- SC-4 grep returns empty
- REQUIREMENTS.md + ROADMAP.md correctly reflect STAT-GUI-05 reassignment
</success_criteria>

<output>
Create `.planning/phases/36-hp41-gui-gui-integration/36-01-SUMMARY.md` when done
</output>
