---
phase: 49-onboarding-gui-keyboard-parity
plan: "03"
type: execute
wave: 2
depends_on:
  - "49-01"
  - "49-02"
files_modified:
  - hp41-gui/src/OnboardingWizard.tsx
  - hp41-gui/src/OnboardingWizard.test.tsx
  - hp41-gui/src/HelpOverlay.tsx
  - hp41-gui/src/HelpOverlay.test.tsx
  - hp41-gui/src/help_data.ts
  - hp41-gui/src/App.css
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-03
  - ONBOARD-04
  - KBD-03

must_haves:
  truths:
    - "OnboardingWizard renders 5 panels with Back/Next navigation and panel counter"
    - "Wizard resets to panel 1 on each open"
    - "HelpOverlay has Keyboard Shortcuts as first collapsible section"
    - "Function entries with example/notes expand on click to show detail"
    - "Keyboard Shortcuts section is excluded from search filtering"
    - "help_data.ts exports getKeyboardShortcuts() and extended HelpEntry with example/notes"
  artifacts:
    - path: "hp41-gui/src/OnboardingWizard.tsx"
      provides: "5-panel wizard overlay component"
      exports: ["OnboardingWizard", "OnboardingWizardProps"]
    - path: "hp41-gui/src/OnboardingWizard.test.tsx"
      provides: "Unit tests for wizard rendering, navigation, Esc, panel counter"
    - path: "hp41-gui/src/HelpOverlay.tsx"
      provides: "Extended overlay with KBD section + expandable entries"
    - path: "hp41-gui/src/help_data.ts"
      provides: "Extended HelpEntry interface + getKeyboardShortcuts accessor"
      exports: ["HelpEntry", "getKeyboardShortcuts", "KeyboardShortcut"]
  key_links:
    - from: "hp41-gui/src/help_data.ts"
      to: "docs/keyboard-shortcuts.json"
      via: "Vite static JSON-import"
      pattern: "import.*keyboard-shortcuts.json"
    - from: "hp41-gui/src/HelpOverlay.tsx"
      to: "hp41-gui/src/help_data.ts"
      via: "getKeyboardShortcuts import"
      pattern: "getKeyboardShortcuts"
---

<objective>
Create OnboardingWizard component and extend HelpOverlay with Keyboard Shortcuts section and expandable function entries.

Purpose: New users get a guided 5-panel introduction to HP-41C RPN operation (ONBOARD-01), power users can discover keyboard shortcuts in the `?` overlay (KBD-03), and all users can explore function examples/notes via expandable rows (ONBOARD-03).

Output: New OnboardingWizard.tsx + test file; modified HelpOverlay.tsx, help_data.ts, App.css.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-onboarding-gui-keyboard-parity/49-CONTEXT.md
@.planning/phases/49-onboarding-gui-keyboard-parity/49-RESEARCH.md
@.planning/phases/49-onboarding-gui-keyboard-parity/49-PATTERNS.md
@.planning/phases/49-onboarding-gui-keyboard-parity/49-UI-SPEC.md
@.planning/phases/49-onboarding-gui-keyboard-parity/49-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output + existing codebase -->

From hp41-gui/src/help_data.ts (current, to be extended):
- `export interface HelpEntry { op_variant, display_name, category, status, phase, key_path, description, divergences?, xrom? }`
- `export function helpEntriesAll(): readonly HelpEntry[]` (5-pool concatenation)
- Vite static import pattern: `import functions from '../../docs/hp41cv-functions.json'`

From hp41-gui/src/HelpOverlay.tsx (current):
- `interface SectionDef { id: 'hp41cv' | 'math1' | 'stat1' | 'time' | 'adv22' | 'adv24'; heading: string; predicate: (e: HelpEntry) => boolean }`
- `const SECTIONS: SectionDef[]` — 6 entries
- `expanded` state type: `{ hp41cv: boolean; math1: boolean; stat1: boolean; time: boolean; adv22: boolean; adv24: boolean }`
- `toggleSection(id: ...)` — parameter union matches SectionDef.id
- Row render at lines 232-238: `<div className="help-overlay-row">` with key/op/desc spans

From hp41-gui/src/HelpOverlay.tsx (props):
- `export type HelpOverlayProps = { open: boolean; onClose: () => void }`

From hp41-gui/src/SettingsPanel.test.tsx (test pattern):
- `vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn().mockResolvedValue(undefined) }))`
- `render(<Component open={true} onClose={vi.fn()} />)` pattern
- `fireEvent.keyDown(window, { key: 'Escape' })` for Esc test

From docs/keyboard-shortcuts.json (created in Plan 01):
- Array of `{ key: string, op: string, description: string }` objects, >=30 entries

From 49-UI-SPEC.md CSS classes:
- `.wizard-overlay`, `.wizard-panel`, `.wizard-panel-header`, `.wizard-panel-counter`, `.wizard-panel-body`, `.wizard-panel-nav`, `.wizard-nav-btn`, `.wizard-nav-btn-primary`, `.wizard-dismiss-link`, `.wizard-stack-diagram`, `.wizard-shortcut-table`, `.wizard-shortcut-row`, `.wizard-shortcut-key`, `.wizard-shortcut-fn`
- `.shortcut-table`, `.shortcut-row`, `.shortcut-key-col`, `.shortcut-fn-col`
- `.help-entry-expand-btn`, `.help-entry-detail`, `.help-entry-example`, `.help-entry-notes`

From 49-UI-SPEC.md z-index: wizard-overlay z-index 60 (same as help-overlay; mutually exclusive)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend help_data.ts + create OnboardingWizard component + tests</name>
  <files>
    hp41-gui/src/help_data.ts,
    hp41-gui/src/OnboardingWizard.tsx,
    hp41-gui/src/OnboardingWizard.test.tsx
  </files>
  <read_first>
    hp41-gui/src/help_data.ts,
    hp41-gui/src/HelpOverlay.tsx,
    hp41-gui/src/SettingsPanel.tsx,
    hp41-gui/src/SettingsPanel.test.tsx,
    hp41-gui/src/App.css,
    .planning/phases/49-onboarding-gui-keyboard-parity/49-UI-SPEC.md,
    .planning/phases/49-onboarding-gui-keyboard-parity/49-PATTERNS.md
  </read_first>
  <action>
    **help_data.ts** — Two additions:

    1. Extend `HelpEntry` interface: add `example?: string` and `notes?: string` as optional fields after the existing `xrom?` field (D-49.5). These are the enrichment fields populated by Plan 02.

    2. Add keyboard shortcuts import and accessor after the existing `advantageFunctions` import (line 32). Import `keyboardShortcutsData from '../../docs/keyboard-shortcuts.json'`. Export a `KeyboardShortcut` interface with fields `key: string`, `op: string`, `description: string`. Export a `getKeyboardShortcuts()` function returning `keyboardShortcutsData as readonly KeyboardShortcut[]`. Follow the exact same Vite static JSON-import pattern as the 5 function JSONs.

    **OnboardingWizard.tsx** — New component following the HelpOverlay overlay pattern exactly (D-49.3):

    Props interface: `OnboardingWizardProps { open: boolean; onClose: () => void; isFirstRun: boolean }`. The `isFirstRun` prop controls dismiss behavior: on first-run, Esc and click-outside do NOT dismiss (user must click Skip or Finish); on re-open from settings, Esc and click-outside DO dismiss (49-UI-SPEC interaction contract).

    Component structure:
    - `useState(0)` for `panelIdx`
    - `useEffect` to reset `panelIdx` to 0 when `open` transitions to true (D-49.9)
    - `useEffect` for Esc keydown handler — only fires when `!isFirstRun` (re-open mode). When Esc fires, call `onClose()`.
    - `if (!open) return null` early-return pattern
    - `PANELS` const array with 5 entries, each with `title: string` and `content: JSX.Element` (or a render function):

    Panel 1 "Welcome to HP-41C": subhead "An RPN calculator -- numbers first, operator second." Body explains RPN: type 3, press ENTER, type 4, press +, result is 7. Use `.wizard-stack-diagram` for a simple ASCII stack visualization.

    Panel 2 "The Four-Level Stack": subhead "X, Y, Z, T -- the calculator's working memory." Body explains the 4-register stack, how ENTER pushes values up, how operations consume X and Y. Show a `.wizard-stack-diagram` with T/Z/Y/X labels and sample values.

    Panel 3 "SHIFT & Function Access": subhead "Tab arms SHIFT. ? opens the function list. XEQ runs by name." Body explains one-shot SHIFT via Tab key, the `?` overlay for browsing functions, and XEQ for running functions by name.

    Panel 4 "Keyboard Shortcuts": subhead "The top 10 shortcuts -- full list in the ? reference." Body shows a `.wizard-shortcut-table` with ~10 key shortcuts: Enter, Backspace, Tab, +/-/*//, r (R-down), x (X<>Y), Ctrl+S (Save), F7/F8 (SST/BST).

    Panel 5 "Programming & XEQ": subhead "Press R/S to run a program. F5 saves your session." Body explains PRGM mode toggle (p key), entering program steps, R/S to execute, and F5/Ctrl+S to save.

    Navigation footer: "Back" button (disabled on panel 1 with `disabled` attribute + `opacity: 0.4`), "Next" button (panels 1-4), "Start Using HP-41C" on panel 5 (accent style). "Skip for now" link visible on ALL panels when `isFirstRun` is true; hidden on re-open.

    Panel counter: "N of 5" in header, with `aria-label="Step N of 5"`.

    Accessibility: `role="dialog"`, `aria-label="HP-41C Quick Start Guide"`, `aria-modal="true"`. "Skip for now" uses `<button>` element (not div).

    Overlay root: `className="wizard-overlay"`, `position: absolute`, `z-index: 60`, `background: var(--overlay-bg)`.

    Use `system-ui, sans-serif` font family for all wizard content (prose, not monospace). Panel headers: 14px bold uppercase `var(--accent)`. Body text: 14px regular `var(--text-secondary)`. Stack diagrams: 12px monospace using `.wizard-stack-diagram` class. Counter: 12px `var(--accent)`.

    **OnboardingWizard.test.tsx** — Unit tests using Vitest + @testing-library/react:

    1. `renders null when open=false` — container.firstChild is null
    2. `renders wizard with panel 1 when open=true` — text "Welcome to HP-41C" visible
    3. `shows panel counter "1 of 5"` — text "1 of 5" visible
    4. `Next button advances to panel 2` — click Next, "The Four-Level Stack" visible
    5. `Back button returns to panel 1` — click Next then Back, "Welcome" visible again
    6. `Back button disabled on panel 1` — Back button has `disabled` attribute
    7. `panel 5 shows "Start Using HP-41C" button` — advance to panel 5, button visible
    8. `clicking "Start Using HP-41C" calls onClose` — onClose mock called
    9. `Esc does NOT close on first-run` — isFirstRun=true, fireEvent Escape, onClose NOT called
    10. `Esc closes on re-open` — isFirstRun=false, fireEvent Escape, onClose called
    11. `resets to panel 1 on re-open` — advance to panel 3, set open=false then true, panel 1 visible

    Mock Tauri invoke: `vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn().mockResolvedValue(undefined) }))`.
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npx vitest run --reporter=verbose src/OnboardingWizard.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - help_data.ts `HelpEntry` interface includes `example?: string` and `notes?: string`
    - help_data.ts exports `KeyboardShortcut` interface and `getKeyboardShortcuts()` function
    - help_data.ts imports from `../../docs/keyboard-shortcuts.json`
    - OnboardingWizard.tsx exports `OnboardingWizard` component with `OnboardingWizardProps`
    - OnboardingWizard renders 5 panels with titles matching D-49.1 exactly
    - Panel counter shows "N of 5" format
    - Back button has `disabled` attribute on panel 1
    - Panel 5 shows "Start Using HP-41C" instead of "Next"
    - "Skip for now" visible only when `isFirstRun=true`
    - Esc behavior differs based on `isFirstRun` prop
    - `role="dialog"` and `aria-label="HP-41C Quick Start Guide"` present on overlay root
    - All 11 OnboardingWizard tests pass
  </acceptance_criteria>
  <done>
    help_data.ts extended with KeyboardShortcut type + HelpEntry enrichment fields. OnboardingWizard component renders 5-panel wizard with proper navigation, accessibility, and first-run vs re-open behavior. 11 unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend HelpOverlay with Keyboard Shortcuts section + expandable entries + CSS</name>
  <files>
    hp41-gui/src/HelpOverlay.tsx,
    hp41-gui/src/HelpOverlay.test.tsx,
    hp41-gui/src/App.css
  </files>
  <read_first>
    hp41-gui/src/HelpOverlay.tsx,
    hp41-gui/src/HelpOverlay.test.tsx,
    hp41-gui/src/help_data.ts,
    hp41-gui/src/App.css,
    .planning/phases/49-onboarding-gui-keyboard-parity/49-UI-SPEC.md,
    .planning/phases/49-onboarding-gui-keyboard-parity/49-PATTERNS.md
  </read_first>
  <action>
    **HelpOverlay.tsx** — Three coordinated changes:

    **Change 1: Keyboard Shortcuts section (D-49.10 / KBD-03)**

    Import `getKeyboardShortcuts` from `./help_data`. The Keyboard Shortcuts section is NOT part of the `SECTIONS` array (which is for function entries with predicates). Instead, it renders as a separate block ABOVE the `sectionGroups.map()` loop — a dedicated collapsible section with its own expand/collapse state.

    Add a new state variable `kbdExpanded` using `useState(false)` (collapsed by default per UI-SPEC). Reset `kbdExpanded` to `false` in the existing `open` reset `useEffect` (line 118-122).

    Render the Keyboard Shortcuts section between the search header and the function sections. Use the existing `.help-overlay-section-heading` button class for the heading "KEYBOARD SHORTCUTS" (uppercase). Inside the collapsed body, render a `.shortcut-table` with `.shortcut-row` entries from `getKeyboardShortcuts()`, each showing `.shortcut-key-col` (the `key` field) and `.shortcut-fn-col` (the `op` + ` -- ` + `description`).

    The shortcuts section is NOT affected by the search query filter. It always renders (visible/hidden by its own collapse state), regardless of what the user types in the search box. This is intentional: keyboard shortcut semantics differ from function lookup (D-49.10 / UI-SPEC).

    **Change 2: Expandable function entries (D-49.6 / ONBOARD-03)**

    Modify the entry row render (lines 232-238) to support expandable detail. For each entry, check if `entry.example || entry.notes` is truthy. If so, render a toggle button (`.help-entry-expand-btn`) prepended to the row with ">" (collapsed) or "v" (expanded) indicator.

    Use a local `expandedEntries` state: `useState<Set<string>>(new Set())` tracking expanded `op_variant` keys. Toggle on click. Reset the set to empty in the `open` reset useEffect.

    When an entry is expanded, render a `.help-entry-detail` div below the row containing:
    - `.help-entry-example` span with the `example` text (if present)
    - `.help-entry-notes` span with the `notes` text (if present)

    Entries without `example`/`notes`: no toggle button, no visual change from current layout. The expand toggle uses `aria-expanded="true"/"false"` and `aria-label="Show example for {display_name}"`.

    **Change 3: No SectionDef changes needed** since the Keyboard Shortcuts section is rendered separately from the function sections. The `SectionDef` id union and `expanded` state type remain unchanged.

    **HelpOverlay.test.tsx** — Add these tests to the existing test file:

    1. `renders Keyboard Shortcuts section heading` — text "KEYBOARD SHORTCUTS" visible when open
    2. `keyboard shortcuts section collapsed by default` — heading visible, shortcut rows NOT visible initially
    3. `clicking Keyboard Shortcuts heading expands section` — click heading, shortcut rows become visible
    4. `shortcut entries show key and op columns` — expand section, verify at least one row has key + op text
    5. `expandable entry shows expand toggle` — mock an entry with `example` field, verify toggle button present
    6. `clicking expand toggle reveals example text` — click toggle, example text visible
    7. `entries without example/notes have no toggle` — verify entries without enrichment have no expand button

    **App.css** — Add CSS classes per UI-SPEC:

    Wizard overlay styles (`.wizard-overlay`, `.wizard-panel`, `.wizard-panel-header`, `.wizard-panel-counter`, `.wizard-panel-body`, `.wizard-panel-nav`, `.wizard-nav-btn`, `.wizard-nav-btn-primary`, `.wizard-dismiss-link`, `.wizard-stack-diagram`, `.wizard-shortcut-table`, `.wizard-shortcut-row`, `.wizard-shortcut-key`, `.wizard-shortcut-fn`).

    Keyboard shortcut section styles (`.shortcut-table`, `.shortcut-row`, `.shortcut-key-col`, `.shortcut-fn-col`).

    Expandable entry styles (`.help-entry-expand-btn`, `.help-entry-detail`, `.help-entry-example`, `.help-entry-notes`).

    Settings action button style (`.settings-action-btn`, `.settings-action-btn:hover`, `.settings-section-divider`).

    All colors via CSS custom properties (never hardcode hex). Follow the exact CSS values from 49-UI-SPEC.md Component Inventory section. Wizard overlay uses `system-ui, sans-serif` for font-family (prose content). Help overlay entries use existing monospace font. Wizard z-index: 60 (same as help-overlay; they are mutually exclusive).
  </action>
  <verify>
    <automated>cd /Users/daniel/GitRepository/hp41-calculator-emulator/hp41-gui && npx vitest run --reporter=verbose src/HelpOverlay.test.tsx src/OnboardingWizard.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - HelpOverlay renders "KEYBOARD SHORTCUTS" section heading when open
    - Keyboard Shortcuts section is collapsed by default
    - Clicking the heading toggles expand/collapse
    - Shortcut rows show key + op data from keyboard-shortcuts.json
    - Keyboard Shortcuts section is not filtered by search query
    - Entries with example/notes show expand toggle button
    - Clicking toggle reveals .help-entry-detail with example and notes
    - Entries without enrichment have no toggle button
    - `kbdExpanded` and `expandedEntries` reset on overlay close/open
    - App.css contains all wizard, shortcut, and expandable entry CSS classes per UI-SPEC
    - All HelpOverlay tests pass (existing + new)
    - All OnboardingWizard tests still pass
  </acceptance_criteria>
  <done>
    HelpOverlay extended with Keyboard Shortcuts collapsible section (collapsed by default, not searchable) and expandable function entries (toggle for entries with example/notes). Wizard overlay CSS and all new CSS classes added to App.css. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON data -> React render | keyboard-shortcuts.json and enriched function data rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Spoofing | OnboardingWizard content | accept | Static text only (D-49.2); no user input accepted in wizard. No data exfiltration path. |
| T-49-06 | Information Disclosure | keyboard-shortcuts.json | accept | Public knowledge — keyboard shortcuts are intentionally disclosed to users. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. Zero new runtime deps policy maintained. |
</threat_model>

<verification>
cd hp41-gui && npx vitest run (all frontend tests pass)
cd hp41-gui && npx tsc --noEmit (TypeScript clean)
grep -c "wizard-overlay\|shortcut-table\|help-entry-expand-btn\|settings-action-btn" hp41-gui/src/App.css (>= 4 CSS classes present)
</verification>

<success_criteria>
OnboardingWizard renders 5 panels with proper navigation and first-run behavior. HelpOverlay has Keyboard Shortcuts section and expandable entries. All CSS follows UI-SPEC with CSS custom properties. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/49-onboarding-gui-keyboard-parity/49-03-SUMMARY.md` when done
</output>
